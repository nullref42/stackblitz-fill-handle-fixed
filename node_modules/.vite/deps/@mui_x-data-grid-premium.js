import "./chunk-2PHJYOJ2.js";
import {
  require_react_dom
} from "./chunk-5MXL5BYH.js";
import {
  Typography_default,
  capitalize_default,
  createSimplePaletteValueFilter,
  globalCss,
  memoTheme_default,
  typographyClasses_default,
  useDefaultProps
} from "./chunk-YMNANCAG.js";
import {
  getUnit,
  toUnitless
} from "./chunk-FK6MO6LY.js";
import {
  alpha,
  capitalize,
  clsx_default,
  composeClasses,
  css,
  darken,
  deepmerge,
  exactProp,
  generateUtilityClass,
  generateUtilityClasses,
  getOverlayAlpha,
  getThemeProps,
  isMuiElement,
  keyframes,
  lighten,
  require_prop_types,
  require_react_is,
  resolveProps,
  rootShouldForwardProp_default,
  shouldForwardProp,
  slotShouldForwardProp_default,
  styled_default,
  styled_default2,
  useEnhancedEffect_default,
  useId,
  useRtl,
  useTheme
} from "./chunk-4V4XNJWD.js";
import {
  _extends
} from "./chunk-EQCCHGRT.js";
import {
  require_jsx_runtime
} from "./chunk-BCIG5HOZ.js";
import {
  require_react
} from "./chunk-EUHAVL2Q.js";
import {
  __commonJS,
  __export,
  __publicField,
  __toESM
} from "./chunk-2TUXWMP5.js";

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
var require_use_sync_external_store_shim_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
    "use strict";
    (function() {
      function is2(x, y) {
        return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
      }
      function useSyncExternalStore$2(subscribe, getSnapshot) {
        didWarnOld18Alpha || void 0 === React374.startTransition || (didWarnOld18Alpha = true, console.error(
          "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
        ));
        var value = getSnapshot();
        if (!didWarnUncachedGetSnapshot) {
          var cachedValue = getSnapshot();
          objectIs(value, cachedValue) || (console.error(
            "The result of getSnapshot should be cached to avoid an infinite loop"
          ), didWarnUncachedGetSnapshot = true);
        }
        cachedValue = useState62({
          inst: { value, getSnapshot }
        });
        var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
        useLayoutEffect9(
          function() {
            inst.value = value;
            inst.getSnapshot = getSnapshot;
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          },
          [subscribe, value, getSnapshot]
        );
        useEffect96(
          function() {
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            return subscribe(function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            });
          },
          [subscribe]
        );
        useDebugValue(value);
        return value;
      }
      function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        inst = inst.value;
        try {
          var nextValue = latestGetSnapshot();
          return !objectIs(inst, nextValue);
        } catch (error) {
          return true;
        }
      }
      function useSyncExternalStore$1(subscribe, getSnapshot) {
        return getSnapshot();
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React374 = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is2, useState62 = React374.useState, useEffect96 = React374.useEffect, useLayoutEffect9 = React374.useLayoutEffect, useDebugValue = React374.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
      exports.useSyncExternalStore = void 0 !== React374.useSyncExternalStore ? React374.useSyncExternalStore : shim;
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_use_sync_external_store_shim_development();
    }
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js
var require_with_selector_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js"(exports) {
    "use strict";
    (function() {
      function is2(x, y) {
        return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React374 = require_react(), shim = require_shim(), objectIs = "function" === typeof Object.is ? Object.is : is2, useSyncExternalStore4 = shim.useSyncExternalStore, useRef128 = React374.useRef, useEffect96 = React374.useEffect, useMemo69 = React374.useMemo, useDebugValue = React374.useDebugValue;
      exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
        var instRef = useRef128(null);
        if (null === instRef.current) {
          var inst = { hasValue: false, value: null };
          instRef.current = inst;
        } else inst = instRef.current;
        instRef = useMemo69(
          function() {
            function memoizedSelector(nextSnapshot) {
              if (!hasMemo) {
                hasMemo = true;
                memoizedSnapshot = nextSnapshot;
                nextSnapshot = selector(nextSnapshot);
                if (void 0 !== isEqual && inst.hasValue) {
                  var currentSelection = inst.value;
                  if (isEqual(currentSelection, nextSnapshot))
                    return memoizedSelection = currentSelection;
                }
                return memoizedSelection = nextSnapshot;
              }
              currentSelection = memoizedSelection;
              if (objectIs(memoizedSnapshot, nextSnapshot))
                return currentSelection;
              var nextSelection = selector(nextSnapshot);
              if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                return memoizedSnapshot = nextSnapshot, currentSelection;
              memoizedSnapshot = nextSnapshot;
              return memoizedSelection = nextSelection;
            }
            var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
            return [
              function() {
                return memoizedSelector(getSnapshot());
              },
              null === maybeGetServerSnapshot ? void 0 : function() {
                return memoizedSelector(maybeGetServerSnapshot());
              }
            ];
          },
          [getSnapshot, getServerSnapshot, selector, isEqual]
        );
        var value = useSyncExternalStore4(subscribe, instRef[0], instRef[1]);
        useEffect96(
          function() {
            inst.hasValue = true;
            inst.value = value;
          },
          [value]
        );
        useDebugValue(value);
        return value;
      };
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/with-selector.js
var require_with_selector = __commonJS({
  "node_modules/use-sync-external-store/shim/with-selector.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_with_selector_development();
    }
  }
});

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScroller.js
var React59 = __toESM(require_react(), 1);

// node_modules/reselect/dist/reselect.mjs
var runIdentityFunctionCheck = (resultFunc, inputSelectorsResults, outputSelectorResult) => {
  if (inputSelectorsResults.length === 1 && inputSelectorsResults[0] === outputSelectorResult) {
    let isInputSameAsOutput = false;
    try {
      const emptyObject = {};
      if (resultFunc(emptyObject) === emptyObject)
        isInputSameAsOutput = true;
    } catch {
    }
    if (isInputSameAsOutput) {
      let stack = void 0;
      try {
        throw new Error();
      } catch (e) {
        ;
        ({ stack } = e);
      }
      console.warn(
        "The result function returned its own inputs without modification. e.g\n`createSelector([state => state.todos], todos => todos)`\nThis could lead to inefficient memoization and unnecessary re-renders.\nEnsure transformation logic is in the result function, and extraction logic is in the input selectors.",
        { stack }
      );
    }
  }
};
var runInputStabilityCheck = (inputSelectorResultsObject, options3, inputSelectorArgs) => {
  const { memoize, memoizeOptions } = options3;
  const { inputSelectorResults, inputSelectorResultsCopy } = inputSelectorResultsObject;
  const createAnEmptyObject = memoize(() => ({}), ...memoizeOptions);
  const areInputSelectorResultsEqual = createAnEmptyObject.apply(null, inputSelectorResults) === createAnEmptyObject.apply(null, inputSelectorResultsCopy);
  if (!areInputSelectorResultsEqual) {
    let stack = void 0;
    try {
      throw new Error();
    } catch (e) {
      ;
      ({ stack } = e);
    }
    console.warn(
      "An input selector returned a different result when passed same arguments.\nThis means your output selector will likely run more frequently than intended.\nAvoid returning a new reference inside your input selector, e.g.\n`createSelector([state => state.todos.map(todo => todo.id)], todoIds => todoIds.length)`",
      {
        arguments: inputSelectorArgs,
        firstInputs: inputSelectorResults,
        secondInputs: inputSelectorResultsCopy,
        stack
      }
    );
  }
};
var globalDevModeChecks = {
  inputStabilityCheck: "once",
  identityFunctionCheck: "once"
};
var NOT_FOUND = Symbol("NOT_FOUND");
function assertIsFunction(func, errorMessage = `expected a function, instead received ${typeof func}`) {
  if (typeof func !== "function") {
    throw new TypeError(errorMessage);
  }
}
function assertIsObject(object, errorMessage = `expected an object, instead received ${typeof object}`) {
  if (typeof object !== "object") {
    throw new TypeError(errorMessage);
  }
}
function assertIsArrayOfFunctions(array, errorMessage = `expected all items to be functions, instead received the following types: `) {
  if (!array.every((item) => typeof item === "function")) {
    const itemTypes = array.map(
      (item) => typeof item === "function" ? `function ${item.name || "unnamed"}()` : typeof item
    ).join(", ");
    throw new TypeError(`${errorMessage}[${itemTypes}]`);
  }
}
var ensureIsArray = (item) => {
  return Array.isArray(item) ? item : [item];
};
function getDependencies(createSelectorArgs) {
  const dependencies = Array.isArray(createSelectorArgs[0]) ? createSelectorArgs[0] : createSelectorArgs;
  assertIsArrayOfFunctions(
    dependencies,
    `createSelector expects all input-selectors to be functions, but received the following types: `
  );
  return dependencies;
}
function collectInputSelectorResults(dependencies, inputSelectorArgs) {
  const inputSelectorResults = [];
  const { length } = dependencies;
  for (let i2 = 0; i2 < length; i2++) {
    inputSelectorResults.push(dependencies[i2].apply(null, inputSelectorArgs));
  }
  return inputSelectorResults;
}
var getDevModeChecksExecutionInfo = (firstRun, devModeChecks) => {
  const { identityFunctionCheck, inputStabilityCheck } = {
    ...globalDevModeChecks,
    ...devModeChecks
  };
  return {
    identityFunctionCheck: {
      shouldRun: identityFunctionCheck === "always" || identityFunctionCheck === "once" && firstRun,
      run: runIdentityFunctionCheck
    },
    inputStabilityCheck: {
      shouldRun: inputStabilityCheck === "always" || inputStabilityCheck === "once" && firstRun,
      run: runInputStabilityCheck
    }
  };
};
var REDUX_PROXY_LABEL = Symbol();
var proto = Object.getPrototypeOf({});
function createSingletonCache(equals) {
  let entry;
  return {
    get(key) {
      if (entry && equals(entry.key, key)) {
        return entry.value;
      }
      return NOT_FOUND;
    },
    put(key, value) {
      entry = { key, value };
    },
    getEntries() {
      return entry ? [entry] : [];
    },
    clear() {
      entry = void 0;
    }
  };
}
function createLruCache(maxSize, equals) {
  let entries = [];
  function get(key) {
    const cacheIndex = entries.findIndex((entry) => equals(key, entry.key));
    if (cacheIndex > -1) {
      const entry = entries[cacheIndex];
      if (cacheIndex > 0) {
        entries.splice(cacheIndex, 1);
        entries.unshift(entry);
      }
      return entry.value;
    }
    return NOT_FOUND;
  }
  function put(key, value) {
    if (get(key) === NOT_FOUND) {
      entries.unshift({ key, value });
      if (entries.length > maxSize) {
        entries.pop();
      }
    }
  }
  function getEntries() {
    return entries;
  }
  function clear() {
    entries = [];
  }
  return { get, put, getEntries, clear };
}
var referenceEqualityCheck = (a, b) => a === b;
function createCacheKeyComparator(equalityCheck) {
  return function areArgumentsShallowlyEqual(prev, next) {
    if (prev === null || next === null || prev.length !== next.length) {
      return false;
    }
    const { length } = prev;
    for (let i2 = 0; i2 < length; i2++) {
      if (!equalityCheck(prev[i2], next[i2])) {
        return false;
      }
    }
    return true;
  };
}
function lruMemoize(func, equalityCheckOrOptions) {
  const providedOptions = typeof equalityCheckOrOptions === "object" ? equalityCheckOrOptions : { equalityCheck: equalityCheckOrOptions };
  const {
    equalityCheck = referenceEqualityCheck,
    maxSize = 1,
    resultEqualityCheck
  } = providedOptions;
  const comparator = createCacheKeyComparator(equalityCheck);
  let resultsCount = 0;
  const cache = maxSize <= 1 ? createSingletonCache(comparator) : createLruCache(maxSize, comparator);
  function memoized() {
    let value = cache.get(arguments);
    if (value === NOT_FOUND) {
      value = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const entries = cache.getEntries();
        const matchingEntry = entries.find(
          (entry) => resultEqualityCheck(entry.value, value)
        );
        if (matchingEntry) {
          value = matchingEntry.value;
          resultsCount !== 0 && resultsCount--;
        }
      }
      cache.put(arguments, value);
    }
    return value;
  }
  memoized.clearCache = () => {
    cache.clear();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}
var StrongRef = class {
  constructor(value) {
    this.value = value;
  }
  deref() {
    return this.value;
  }
};
var Ref = typeof WeakRef !== "undefined" ? WeakRef : StrongRef;
var UNTERMINATED = 0;
var TERMINATED = 1;
function createCacheNode() {
  return {
    s: UNTERMINATED,
    v: void 0,
    o: null,
    p: null
  };
}
function weakMapMemoize(func, options3 = {}) {
  let fnNode = createCacheNode();
  const { resultEqualityCheck } = options3;
  let lastResult;
  let resultsCount = 0;
  function memoized() {
    var _a;
    let cacheNode = fnNode;
    const { length } = arguments;
    for (let i2 = 0, l = length; i2 < l; i2++) {
      const arg = arguments[i2];
      if (typeof arg === "function" || typeof arg === "object" && arg !== null) {
        let objectCache = cacheNode.o;
        if (objectCache === null) {
          cacheNode.o = objectCache = /* @__PURE__ */ new WeakMap();
        }
        const objectNode = objectCache.get(arg);
        if (objectNode === void 0) {
          cacheNode = createCacheNode();
          objectCache.set(arg, cacheNode);
        } else {
          cacheNode = objectNode;
        }
      } else {
        let primitiveCache = cacheNode.p;
        if (primitiveCache === null) {
          cacheNode.p = primitiveCache = /* @__PURE__ */ new Map();
        }
        const primitiveNode = primitiveCache.get(arg);
        if (primitiveNode === void 0) {
          cacheNode = createCacheNode();
          primitiveCache.set(arg, cacheNode);
        } else {
          cacheNode = primitiveNode;
        }
      }
    }
    const terminatedNode = cacheNode;
    let result;
    if (cacheNode.s === TERMINATED) {
      result = cacheNode.v;
    } else {
      result = func.apply(null, arguments);
      resultsCount++;
      if (resultEqualityCheck) {
        const lastResultValue = ((_a = lastResult == null ? void 0 : lastResult.deref) == null ? void 0 : _a.call(lastResult)) ?? lastResult;
        if (lastResultValue != null && resultEqualityCheck(lastResultValue, result)) {
          result = lastResultValue;
          resultsCount !== 0 && resultsCount--;
        }
        const needsWeakRef = typeof result === "object" && result !== null || typeof result === "function";
        lastResult = needsWeakRef ? new Ref(result) : result;
      }
    }
    terminatedNode.s = TERMINATED;
    terminatedNode.v = result;
    return result;
  }
  memoized.clearCache = () => {
    fnNode = createCacheNode();
    memoized.resetResultsCount();
  };
  memoized.resultsCount = () => resultsCount;
  memoized.resetResultsCount = () => {
    resultsCount = 0;
  };
  return memoized;
}
function createSelectorCreator(memoizeOrOptions, ...memoizeOptionsFromArgs) {
  const createSelectorCreatorOptions = typeof memoizeOrOptions === "function" ? {
    memoize: memoizeOrOptions,
    memoizeOptions: memoizeOptionsFromArgs
  } : memoizeOrOptions;
  const createSelector22 = (...createSelectorArgs) => {
    let recomputations = 0;
    let dependencyRecomputations = 0;
    let lastResult;
    let directlyPassedOptions = {};
    let resultFunc = createSelectorArgs.pop();
    if (typeof resultFunc === "object") {
      directlyPassedOptions = resultFunc;
      resultFunc = createSelectorArgs.pop();
    }
    assertIsFunction(
      resultFunc,
      `createSelector expects an output function after the inputs, but received: [${typeof resultFunc}]`
    );
    const combinedOptions = {
      ...createSelectorCreatorOptions,
      ...directlyPassedOptions
    };
    const {
      memoize,
      memoizeOptions = [],
      argsMemoize = weakMapMemoize,
      argsMemoizeOptions = [],
      devModeChecks = {}
    } = combinedOptions;
    const finalMemoizeOptions = ensureIsArray(memoizeOptions);
    const finalArgsMemoizeOptions = ensureIsArray(argsMemoizeOptions);
    const dependencies = getDependencies(createSelectorArgs);
    const memoizedResultFunc = memoize(function recomputationWrapper() {
      recomputations++;
      return resultFunc.apply(
        null,
        arguments
      );
    }, ...finalMemoizeOptions);
    let firstRun = true;
    const selector = argsMemoize(function dependenciesChecker() {
      dependencyRecomputations++;
      const inputSelectorResults = collectInputSelectorResults(
        dependencies,
        arguments
      );
      lastResult = memoizedResultFunc.apply(null, inputSelectorResults);
      if (true) {
        const { identityFunctionCheck, inputStabilityCheck } = getDevModeChecksExecutionInfo(firstRun, devModeChecks);
        if (identityFunctionCheck.shouldRun) {
          identityFunctionCheck.run(
            resultFunc,
            inputSelectorResults,
            lastResult
          );
        }
        if (inputStabilityCheck.shouldRun) {
          const inputSelectorResultsCopy = collectInputSelectorResults(
            dependencies,
            arguments
          );
          inputStabilityCheck.run(
            { inputSelectorResults, inputSelectorResultsCopy },
            { memoize, memoizeOptions: finalMemoizeOptions },
            arguments
          );
        }
        if (firstRun)
          firstRun = false;
      }
      return lastResult;
    }, ...finalArgsMemoizeOptions);
    return Object.assign(selector, {
      resultFunc,
      memoizedResultFunc,
      dependencies,
      dependencyRecomputations: () => dependencyRecomputations,
      resetDependencyRecomputations: () => {
        dependencyRecomputations = 0;
      },
      lastResult: () => lastResult,
      recomputations: () => recomputations,
      resetRecomputations: () => {
        recomputations = 0;
      },
      memoize,
      argsMemoize
    });
  };
  Object.assign(createSelector22, {
    withTypes: () => createSelector22
  });
  return createSelector22;
}
var createSelector = createSelectorCreator(weakMapMemoize);
var createStructuredSelector = Object.assign(
  (inputSelectorsObject, selectorCreator = createSelector) => {
    assertIsObject(
      inputSelectorsObject,
      `createStructuredSelector expects first argument to be an object where each property is a selector, instead received a ${typeof inputSelectorsObject}`
    );
    const inputSelectorKeys = Object.keys(inputSelectorsObject);
    const dependencies = inputSelectorKeys.map(
      (key) => inputSelectorsObject[key]
    );
    const structuredSelector = selectorCreator(
      dependencies,
      (...inputSelectorResults) => {
        return inputSelectorResults.reduce((composition, value, index) => {
          composition[inputSelectorKeys[index]] = value;
          return composition;
        }, {});
      }
    );
    return structuredSelector;
  },
  { withTypes: () => createStructuredSelector }
);

// node_modules/@mui/x-internals/esm/store/createSelector.js
var reselectCreateSelector = createSelectorCreator({
  memoize: lruMemoize,
  memoizeOptions: {
    maxSize: 1,
    equalityCheck: Object.is
  }
});
var createSelector2 = (a, b, c, d, e, f, g, h, ...other) => {
  if (other.length > 0) {
    throw new Error("Unsupported number of selectors");
  }
  let selector;
  if (a && b && c && d && e && f && g && h) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      const vc = c(state, a1, a2, a3);
      const vd = d(state, a1, a2, a3);
      const ve = e(state, a1, a2, a3);
      const vf = f(state, a1, a2, a3);
      const vg = g(state, a1, a2, a3);
      return h(va, vb, vc, vd, ve, vf, vg, a1, a2, a3);
    };
  } else if (a && b && c && d && e && f && g) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      const vc = c(state, a1, a2, a3);
      const vd = d(state, a1, a2, a3);
      const ve = e(state, a1, a2, a3);
      const vf = f(state, a1, a2, a3);
      return g(va, vb, vc, vd, ve, vf, a1, a2, a3);
    };
  } else if (a && b && c && d && e && f) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      const vc = c(state, a1, a2, a3);
      const vd = d(state, a1, a2, a3);
      const ve = e(state, a1, a2, a3);
      return f(va, vb, vc, vd, ve, a1, a2, a3);
    };
  } else if (a && b && c && d && e) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      const vc = c(state, a1, a2, a3);
      const vd = d(state, a1, a2, a3);
      return e(va, vb, vc, vd, a1, a2, a3);
    };
  } else if (a && b && c && d) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      const vc = c(state, a1, a2, a3);
      return d(va, vb, vc, a1, a2, a3);
    };
  } else if (a && b && c) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      const vb = b(state, a1, a2, a3);
      return c(va, vb, a1, a2, a3);
    };
  } else if (a && b) {
    selector = (state, a1, a2, a3) => {
      const va = a(state, a1, a2, a3);
      return b(va, a1, a2, a3);
    };
  } else if (a) {
    selector = a;
  } else {
    throw new Error("Missing arguments");
  }
  return selector;
};
var createSelectorMemoizedWithOptions = (options3) => (...inputs) => {
  const cache = /* @__PURE__ */ new WeakMap();
  let nextCacheId = 1;
  const combiner = inputs[inputs.length - 1];
  const nSelectors = inputs.length - 1 || 1;
  const argsLength = Math.max(combiner.length - nSelectors, 0);
  if (argsLength > 3) {
    throw new Error("Unsupported number of arguments");
  }
  const selector = (state, a1, a2, a3) => {
    let cacheKey = state.__cacheKey__;
    if (!cacheKey) {
      cacheKey = {
        id: nextCacheId
      };
      state.__cacheKey__ = cacheKey;
      nextCacheId += 1;
    }
    let fn2 = cache.get(cacheKey);
    if (!fn2) {
      const selectors6 = inputs.length === 1 ? [(x) => x, combiner] : inputs;
      let reselectArgs = inputs;
      const selectorArgs = [void 0, void 0, void 0];
      switch (argsLength) {
        case 0:
          break;
        case 1: {
          reselectArgs = [...selectors6.slice(0, -1), () => selectorArgs[0], combiner];
          break;
        }
        case 2: {
          reselectArgs = [...selectors6.slice(0, -1), () => selectorArgs[0], () => selectorArgs[1], combiner];
          break;
        }
        case 3: {
          reselectArgs = [...selectors6.slice(0, -1), () => selectorArgs[0], () => selectorArgs[1], () => selectorArgs[2], combiner];
          break;
        }
        default:
          throw new Error("Unsupported number of arguments");
      }
      if (options3) {
        reselectArgs = [...reselectArgs, options3];
      }
      fn2 = reselectCreateSelector(...reselectArgs);
      fn2.selectorArgs = selectorArgs;
      cache.set(cacheKey, fn2);
    }
    switch (argsLength) {
      case 3:
        fn2.selectorArgs[2] = a3;
      case 2:
        fn2.selectorArgs[1] = a2;
      case 1:
        fn2.selectorArgs[0] = a1;
      case 0:
      default:
    }
    switch (argsLength) {
      case 0:
        return fn2(state);
      case 1:
        return fn2(state, a1);
      case 2:
        return fn2(state, a1, a2);
      case 3:
        return fn2(state, a1, a2, a3);
      default:
        throw new Error("unreachable");
    }
  };
  return selector;
};
var createSelectorMemoized = createSelectorMemoizedWithOptions();

// node_modules/@mui/x-internals/esm/store/useStore.js
var React2 = __toESM(require_react(), 1);
var import_shim = __toESM(require_shim(), 1);
var import_with_selector = __toESM(require_with_selector(), 1);

// node_modules/@mui/x-internals/esm/reactMajor/index.js
var React = __toESM(require_react(), 1);
var reactMajor_default = parseInt(React.version, 10);

// node_modules/@mui/x-internals/esm/store/useStore.js
var canUseRawUseSyncExternalStore = reactMajor_default >= 19;
var useStoreImplementation = canUseRawUseSyncExternalStore ? useStoreR19 : useStoreLegacy;
function useStore(store, selector, a1, a2, a3) {
  return useStoreImplementation(store, selector, a1, a2, a3);
}
function useStoreR19(store, selector, a1, a2, a3) {
  const getSelection2 = React2.useCallback(() => selector(store.getSnapshot(), a1, a2, a3), [store, selector, a1, a2, a3]);
  return (0, import_shim.useSyncExternalStore)(store.subscribe, getSelection2, getSelection2);
}
function useStoreLegacy(store, selector, a1, a2, a3) {
  return (0, import_with_selector.useSyncExternalStoreWithSelector)(store.subscribe, store.getSnapshot, store.getSnapshot, (state) => selector(state, a1, a2, a3));
}

// node_modules/@mui/utils/esm/useLazyRef/useLazyRef.js
var React3 = __toESM(require_react(), 1);
var UNINITIALIZED = {};
function useLazyRef(init, initArg) {
  const ref = React3.useRef(UNINITIALIZED);
  if (ref.current === UNINITIALIZED) {
    ref.current = init(initArg);
  }
  return ref;
}

// node_modules/@mui/utils/esm/useOnMount/useOnMount.js
var React4 = __toESM(require_react(), 1);
var EMPTY = [];
function useOnMount(fn2) {
  React4.useEffect(fn2, EMPTY);
}

// node_modules/@mui/x-internals/esm/store/useStoreEffect.js
var noop = () => {
};
function useStoreEffect(store, selector, effect4) {
  const instance = useLazyRef(initialize, {
    store,
    selector
  }).current;
  instance.effect = effect4;
  useOnMount(instance.onMount);
}
function initialize(params) {
  const {
    store,
    selector
  } = params;
  let previousState = selector(store.state);
  const instance = {
    effect: noop,
    dispose: null,
    // We want a single subscription done right away and cleared on unmount only,
    // but React triggers `useOnMount` multiple times in dev, so we need to manage
    // the subscription anyway.
    subscribe: () => {
      instance.dispose ?? (instance.dispose = store.subscribe((state) => {
        const nextState = selector(state);
        if (!Object.is(previousState, nextState)) {
          const prev = previousState;
          previousState = nextState;
          instance.effect(prev, nextState);
        }
      }));
    },
    onMount: () => {
      instance.subscribe();
      return () => {
        var _a;
        (_a = instance.dispose) == null ? void 0 : _a.call(instance);
        instance.dispose = null;
      };
    }
  };
  instance.subscribe();
  return instance;
}

// node_modules/@mui/x-internals/esm/store/Store.js
var Store = class _Store {
  constructor(state) {
    __publicField(this, "subscribe", (fn2) => {
      this.listeners.add(fn2);
      return () => {
        this.listeners.delete(fn2);
      };
    });
    /**
     * Returns the current state snapshot. Meant for usage with `useSyncExternalStore`.
     * If you want to access the state, use the `state` property instead.
     */
    __publicField(this, "getSnapshot", () => {
      return this.state;
    });
    __publicField(this, "use", /* @__PURE__ */ (() => (selector, a1, a2, a3) => {
      return useStore(this, selector, a1, a2, a3);
    })());
    this.state = state;
    this.listeners = /* @__PURE__ */ new Set();
    this.updateTick = 0;
  }
  // HACK: `any` fixes adding listeners that accept partial state.
  // Internal state to handle recursive `setState()` calls
  static create(state) {
    return new _Store(state);
  }
  setState(newState) {
    this.state = newState;
    this.updateTick += 1;
    const currentTick = this.updateTick;
    const it = this.listeners.values();
    let result;
    while (result = it.next(), !result.done) {
      if (currentTick !== this.updateTick) {
        return;
      }
      const listener = result.value;
      listener(newState);
    }
  }
  update(changes) {
    for (const key in changes) {
      if (!Object.is(this.state[key], changes[key])) {
        this.setState(_extends({}, this.state, changes));
        return;
      }
    }
  }
  set(key, value) {
    if (!Object.is(this.state[key], value)) {
      this.setState(_extends({}, this.state, {
        [key]: value
      }));
    }
  }
};

// node_modules/@mui/x-data-grid/esm/utils/createSelector.js
var createSelector3 = (...args) => {
  const baseSelector = createSelector2(...args);
  const selector = (apiRef, a1, a2, a3) => baseSelector(unwrapIfNeeded(apiRef), a1, a2, a3);
  return selector;
};
var createSelectorMemoized2 = (...args) => {
  const baseSelector = createSelectorMemoized(...args);
  const selector = (apiRef, a1, a2, a3) => baseSelector(unwrapIfNeeded(apiRef), a1, a2, a3);
  return selector;
};
var createRootSelector = (fn2) => (apiRef, args) => fn2(unwrapIfNeeded(apiRef), args);
function unwrapIfNeeded(refOrState) {
  if ("current" in refOrState) {
    return refOrState.current.state;
  }
  return refOrState;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/dimensions/gridDimensionsSelectors.js
var gridDimensionsSelector = createRootSelector((state) => state.dimensions);
var gridColumnsTotalWidthSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.columnsTotalWidth);
var gridRowHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.rowHeight);
var gridContentHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.contentSize.height);
var gridHasScrollXSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.hasScrollX);
var gridHasScrollYSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.hasScrollY);
var gridHasFillerSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.columnsTotalWidth < dimensions.viewportOuterSize.width);
var gridHeaderHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.headerHeight);
var gridGroupHeaderHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.groupHeaderHeight);
var gridHeaderFilterHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.headerFilterHeight);
var gridHorizontalScrollbarHeightSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.hasScrollX ? dimensions.scrollbarSize : 0);
var gridVerticalScrollbarWidthSelector = createSelector3(gridDimensionsSelector, (dimensions) => dimensions.hasScrollY ? dimensions.scrollbarSize : 0);
var gridHasBottomFillerSelector = createSelector3(gridDimensionsSelector, gridHorizontalScrollbarHeightSelector, (dimensions, height) => {
  const needsLastRowBorder = dimensions.viewportOuterSize.height - dimensions.minimumSize.height > 0;
  if (height === 0 && !needsLastRowBorder) {
    return false;
  }
  return true;
});

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/gridRowsMetaSelector.js
var gridRowsMetaSelector = createRootSelector((state) => state.rowsMeta);

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/gridRowsSelector.js
var gridRowsStateSelector = createRootSelector((state) => state.rows);
var gridRowCountSelector = createSelector3(gridRowsStateSelector, (rows) => rows.totalRowCount);
var gridRowsLoadingSelector = createSelector3(gridRowsStateSelector, (rows) => rows.loading);
var gridTopLevelRowCountSelector = createSelector3(gridRowsStateSelector, (rows) => rows.totalTopLevelRowCount);
var gridRowsLookupSelector = createSelector3(gridRowsStateSelector, (rows) => rows.dataRowIdToModelLookup);
var gridRowSelector = createSelector3(gridRowsLookupSelector, (rows, id) => rows[id]);
var gridRowTreeSelector = createSelector3(gridRowsStateSelector, (rows) => rows.tree);
var gridRowNodeSelector = createSelector3(gridRowTreeSelector, (rowTree, rowId) => rowTree[rowId]);
var gridRowGroupsToFetchSelector = createSelector3(gridRowsStateSelector, (rows) => rows.groupsToFetch);
var gridRowGroupingNameSelector = createSelector3(gridRowsStateSelector, (rows) => rows.groupingName);
var gridRowTreeDepthsSelector = createSelector3(gridRowsStateSelector, (rows) => rows.treeDepths);
var gridRowMaximumTreeDepthSelector = createSelectorMemoized2(gridRowsStateSelector, (rows) => {
  const entries = Object.entries(rows.treeDepths);
  if (entries.length === 0) {
    return 1;
  }
  return (entries.filter(([, nodeCount]) => nodeCount > 0).map(([depth]) => Number(depth)).sort((a, b) => b - a)[0] ?? 0) + 1;
});
var gridDataRowIdsSelector = createSelector3(gridRowsStateSelector, (rows) => rows.dataRowIds);
var gridDataRowsSelector = createSelectorMemoized2(gridDataRowIdsSelector, gridRowsLookupSelector, (dataRowIds, rowsLookup) => dataRowIds.reduce((acc, id) => {
  if (!rowsLookup[id]) {
    return acc;
  }
  acc.push(rowsLookup[id]);
  return acc;
}, []));
var gridAdditionalRowGroupsSelector = createSelector3(gridRowsStateSelector, (rows) => rows == null ? void 0 : rows.additionalRowGroups);
var gridPinnedRowsSelector = createSelectorMemoized2(gridAdditionalRowGroupsSelector, (additionalRowGroups) => {
  var _a, _b;
  const rawPinnedRows = additionalRowGroups == null ? void 0 : additionalRowGroups.pinnedRows;
  return {
    bottom: ((_a = rawPinnedRows == null ? void 0 : rawPinnedRows.bottom) == null ? void 0 : _a.map((rowEntry) => ({
      id: rowEntry.id,
      model: rowEntry.model ?? {}
    }))) ?? [],
    top: ((_b = rawPinnedRows == null ? void 0 : rawPinnedRows.top) == null ? void 0 : _b.map((rowEntry) => ({
      id: rowEntry.id,
      model: rowEntry.model ?? {}
    }))) ?? []
  };
});
var gridPinnedRowsCountSelector = createSelector3(gridPinnedRowsSelector, (pinnedRows) => {
  var _a, _b;
  return (((_a = pinnedRows == null ? void 0 : pinnedRows.top) == null ? void 0 : _a.length) || 0) + (((_b = pinnedRows == null ? void 0 : pinnedRows.bottom) == null ? void 0 : _b.length) || 0);
});

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/gridRowsUtils.js
var GRID_ROOT_GROUP_ID = `auto-generated-group-node-root`;
var GRID_ID_AUTOGENERATED = Symbol("mui.id_autogenerated");
var buildRootGroup = () => ({
  type: "group",
  id: GRID_ROOT_GROUP_ID,
  depth: -1,
  groupingField: null,
  groupingKey: null,
  isAutoGenerated: true,
  children: [],
  childrenFromPath: {},
  childrenExpanded: true,
  parent: null
});
function checkGridRowIdIsValid(id, row, detailErrorMessage = "A row was provided without id in the rows prop:") {
  if (id == null) {
    throw new Error(["MUI X: The Data Grid component requires all rows to have a unique `id` property.", "Alternatively, you can use the `getRowId` prop to specify a custom id for each row.", detailErrorMessage, JSON.stringify(row)].join("\n"));
  }
}
var getRowIdFromRowModel = (rowModel, getRowId, detailErrorMessage) => {
  const id = getRowId ? getRowId(rowModel) : rowModel.id;
  checkGridRowIdIsValid(id, rowModel, detailErrorMessage);
  return id;
};
var getRowValue = (row, colDef, apiRef) => {
  const field = colDef.field;
  if (!colDef || !colDef.valueGetter) {
    return row[field];
  }
  const value = row[colDef.field];
  return colDef.valueGetter(value, row, colDef, apiRef);
};
var createRowsInternalCache = ({
  rows,
  getRowId,
  loading,
  rowCount
}) => {
  const updates = {
    type: "full",
    rows: []
  };
  const dataRowIdToModelLookup = {};
  for (let i2 = 0; i2 < rows.length; i2 += 1) {
    const model = rows[i2];
    const id = getRowIdFromRowModel(model, getRowId);
    dataRowIdToModelLookup[id] = model;
    updates.rows.push(id);
  }
  return {
    rowsBeforePartialUpdates: rows,
    loadingPropBeforePartialUpdates: loading,
    rowCountPropBeforePartialUpdates: rowCount,
    updates,
    dataRowIdToModelLookup
  };
};
var getTopLevelRowCount = ({
  tree,
  rowCountProp = 0
}) => {
  const rootGroupNode = tree[GRID_ROOT_GROUP_ID];
  return Math.max(rowCountProp, rootGroupNode.children.length + (rootGroupNode.footerId == null ? 0 : 1));
};
var getRowsStateFromCache = ({
  apiRef,
  rowCountProp = 0,
  loadingProp,
  previousTree,
  previousTreeDepths,
  previousGroupsToFetch
}) => {
  const cache = apiRef.current.caches.rows;
  const {
    tree: unProcessedTree,
    treeDepths: unProcessedTreeDepths,
    dataRowIds: unProcessedDataRowIds,
    groupingName,
    groupsToFetch = []
  } = apiRef.current.applyStrategyProcessor("rowTreeCreation", {
    previousTree,
    previousTreeDepths,
    updates: cache.updates,
    dataRowIdToModelLookup: cache.dataRowIdToModelLookup,
    previousGroupsToFetch
  });
  const groupingParamsWithHydrateRows = apiRef.current.unstable_applyPipeProcessors("hydrateRows", {
    tree: unProcessedTree,
    treeDepths: unProcessedTreeDepths,
    dataRowIds: unProcessedDataRowIds,
    dataRowIdToModelLookup: cache.dataRowIdToModelLookup
  });
  apiRef.current.caches.rows.updates = {
    type: "partial",
    actions: {
      insert: [],
      modify: [],
      remove: []
    },
    idToActionLookup: {}
  };
  return _extends({}, groupingParamsWithHydrateRows, {
    totalRowCount: Math.max(rowCountProp, groupingParamsWithHydrateRows.dataRowIds.length),
    totalTopLevelRowCount: getTopLevelRowCount({
      tree: groupingParamsWithHydrateRows.tree,
      rowCountProp
    }),
    groupingName,
    loading: loadingProp,
    groupsToFetch
  });
};
var isAutogeneratedRow = (row) => GRID_ID_AUTOGENERATED in row;
var isAutogeneratedRowNode = (rowNode) => rowNode.type === "skeletonRow" || rowNode.type === "footer" || rowNode.type === "group" && rowNode.isAutoGenerated || rowNode.type === "pinnedRow" && rowNode.isAutoGenerated;
var getTreeNodeDescendants = (tree, parentId, skipAutoGeneratedRows, directChildrenOnly) => {
  const node = tree[parentId];
  if (node.type !== "group") {
    return [];
  }
  const validDescendants = [];
  for (let i2 = 0; i2 < node.children.length; i2 += 1) {
    const child = node.children[i2];
    if (!skipAutoGeneratedRows || !isAutogeneratedRowNode(tree[child])) {
      validDescendants.push(child);
    }
    if (directChildrenOnly) {
      continue;
    }
    const childDescendants = getTreeNodeDescendants(tree, child, skipAutoGeneratedRows, directChildrenOnly);
    for (let j = 0; j < childDescendants.length; j += 1) {
      validDescendants.push(childDescendants[j]);
    }
  }
  if (!skipAutoGeneratedRows && node.footerId != null) {
    validDescendants.push(node.footerId);
  }
  return validDescendants;
};
var updateCacheWithNewRows = ({
  previousCache,
  getRowId,
  updates,
  groupKeys
}) => {
  if (previousCache.updates.type === "full") {
    throw new Error("MUI X: Unable to prepare a partial update if a full update is not applied yet.");
  }
  const uniqueUpdates = /* @__PURE__ */ new Map();
  updates.forEach((update) => {
    const id = getRowIdFromRowModel(update, getRowId, "A row was provided without id when calling updateRows():");
    if (uniqueUpdates.has(id)) {
      uniqueUpdates.set(id, _extends({}, uniqueUpdates.get(id), update));
    } else {
      uniqueUpdates.set(id, update);
    }
  });
  const partialUpdates = {
    type: "partial",
    actions: {
      insert: [...previousCache.updates.actions.insert ?? []],
      modify: [...previousCache.updates.actions.modify ?? []],
      remove: [...previousCache.updates.actions.remove ?? []]
    },
    idToActionLookup: _extends({}, previousCache.updates.idToActionLookup),
    groupKeys
  };
  const dataRowIdToModelLookup = _extends({}, previousCache.dataRowIdToModelLookup);
  const alreadyAppliedActionsToRemove = {
    insert: {},
    modify: {},
    remove: {}
  };
  uniqueUpdates.forEach((partialRow, id) => {
    const actionAlreadyAppliedToRow = partialUpdates.idToActionLookup[id];
    if (partialRow._action === "delete") {
      if (actionAlreadyAppliedToRow === "remove" || !dataRowIdToModelLookup[id]) {
        return;
      }
      if (actionAlreadyAppliedToRow != null) {
        alreadyAppliedActionsToRemove[actionAlreadyAppliedToRow][id] = true;
      }
      partialUpdates.actions.remove.push(id);
      delete dataRowIdToModelLookup[id];
      return;
    }
    const oldRow = dataRowIdToModelLookup[id];
    if (oldRow) {
      if (actionAlreadyAppliedToRow === "remove") {
        alreadyAppliedActionsToRemove.remove[id] = true;
        partialUpdates.actions.modify.push(id);
      } else if (actionAlreadyAppliedToRow == null) {
        partialUpdates.actions.modify.push(id);
      }
      dataRowIdToModelLookup[id] = _extends({}, oldRow, partialRow);
      return;
    }
    if (actionAlreadyAppliedToRow === "remove") {
      alreadyAppliedActionsToRemove.remove[id] = true;
      partialUpdates.actions.insert.push(id);
    } else if (actionAlreadyAppliedToRow == null) {
      partialUpdates.actions.insert.push(id);
    }
    dataRowIdToModelLookup[id] = partialRow;
  });
  const actionTypeWithActionsToRemove = Object.keys(alreadyAppliedActionsToRemove);
  for (let i2 = 0; i2 < actionTypeWithActionsToRemove.length; i2 += 1) {
    const actionType = actionTypeWithActionsToRemove[i2];
    const idsToRemove = alreadyAppliedActionsToRemove[actionType];
    if (Object.keys(idsToRemove).length > 0) {
      partialUpdates.actions[actionType] = partialUpdates.actions[actionType].filter((id) => !idsToRemove[id]);
    }
  }
  return {
    dataRowIdToModelLookup,
    updates: partialUpdates,
    rowsBeforePartialUpdates: previousCache.rowsBeforePartialUpdates,
    loadingPropBeforePartialUpdates: previousCache.loadingPropBeforePartialUpdates,
    rowCountPropBeforePartialUpdates: previousCache.rowCountPropBeforePartialUpdates
  };
};
var minimalContentHeight = "var(--DataGrid-overlayHeight, calc(var(--height) * 2))";
function computeRowsUpdates(apiRef, updates, getRowId) {
  const nonPinnedRowsUpdates = [];
  updates.forEach((update) => {
    const id = getRowIdFromRowModel(update, getRowId, "A row was provided without id when calling updateRows():");
    const rowNode = gridRowNodeSelector(apiRef, id);
    if ((rowNode == null ? void 0 : rowNode.type) === "pinnedRow") {
      const pinnedRowsCache = apiRef.current.caches.pinnedRows;
      const prevModel = pinnedRowsCache.idLookup[id];
      if (prevModel) {
        pinnedRowsCache.idLookup[id] = _extends({}, prevModel, update);
      }
    } else {
      nonPinnedRowsUpdates.push(update);
    }
  });
  return nonPinnedRowsUpdates;
}
var warnedOnceInvalidRowHeight = false;
var getValidRowHeight = (rowHeightProp, defaultRowHeight, warningMessage) => {
  if (typeof rowHeightProp === "number" && rowHeightProp > 0) {
    return rowHeightProp;
  }
  if (!warnedOnceInvalidRowHeight && typeof rowHeightProp !== "undefined" && rowHeightProp !== null) {
    console.warn(warningMessage);
    warnedOnceInvalidRowHeight = true;
  }
  return defaultRowHeight;
};
var rowHeightWarning = [`MUI X: The \`rowHeight\` prop should be a number greater than 0.`, `The default value will be used instead.`].join("\n");
var getRowHeightWarning = [`MUI X: The \`getRowHeight\` prop should return a number greater than 0 or 'auto'.`, `The default value will be used instead.`].join("\n");

// node_modules/@mui/x-data-grid/esm/components/GridScrollArea.js
var React36 = __toESM(require_react(), 1);

// node_modules/@mui/utils/esm/useEventCallback/useEventCallback.js
var React5 = __toESM(require_react(), 1);
function useEventCallback(fn2) {
  const ref = React5.useRef(fn2);
  useEnhancedEffect_default(() => {
    ref.current = fn2;
  });
  return React5.useRef((...args) => (
    // @ts-expect-error hide `this`
    (0, ref.current)(...args)
  )).current;
}
var useEventCallback_default = useEventCallback;

// node_modules/@mui/x-internals/esm/fastMemo/fastMemo.js
var React6 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/fastObjectShallowCompare/fastObjectShallowCompare.js
var is = Object.is;
function fastObjectShallowCompare(a, b) {
  if (a === b) {
    return true;
  }
  if (!(a instanceof Object) || !(b instanceof Object)) {
    return false;
  }
  let aLength = 0;
  let bLength = 0;
  for (const key in a) {
    aLength += 1;
    if (!is(a[key], b[key])) {
      return false;
    }
    if (!(key in b)) {
      return false;
    }
  }
  for (const _ in b) {
    bLength += 1;
  }
  return aLength === bLength;
}

// node_modules/@mui/x-internals/esm/fastMemo/fastMemo.js
function fastMemo(component) {
  return React6.memo(component, fastObjectShallowCompare);
}

// node_modules/@mui/x-internals/esm/forwardRef/forwardRef.js
var React7 = __toESM(require_react(), 1);
var forwardRef2 = (render) => {
  if (reactMajor_default >= 19) {
    const Component = (props) => render(props, props.ref ?? null);
    Component.displayName = render.displayName ?? render.name;
    return Component;
  }
  return React7.forwardRef(render);
};
if (true) forwardRef2.displayName = "forwardRef";

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridRootProps.js
var React9 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/context/GridRootPropsContext.js
var React8 = __toESM(require_react(), 1);
var GridRootPropsContext = React8.createContext(void 0);
if (true) GridRootPropsContext.displayName = "GridRootPropsContext";

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridRootProps.js
var useGridRootProps = () => {
  const contextValue = React9.useContext(GridRootPropsContext);
  if (!contextValue) {
    throw new Error("MUI X: useGridRootProps should only be used inside the DataGrid, DataGridPro or DataGridPremium component.");
  }
  return contextValue;
};

// node_modules/@mui/x-data-grid/esm/constants/envConstants.js
var GRID_EXPERIMENTAL_ENABLED = false;

// node_modules/@mui/x-data-grid/esm/constants/localeTextConstants.js
var GRID_DEFAULT_LOCALE_TEXT = {
  // Root
  noRowsLabel: "No rows",
  noResultsOverlayLabel: "No results found.",
  noColumnsOverlayLabel: "No columns",
  noColumnsOverlayManageColumns: "Manage columns",
  emptyPivotOverlayLabel: "Add fields to rows, columns, and values to create a pivot table",
  // Density selector toolbar button text
  toolbarDensity: "Density",
  toolbarDensityLabel: "Density",
  toolbarDensityCompact: "Compact",
  toolbarDensityStandard: "Standard",
  toolbarDensityComfortable: "Comfortable",
  // Undo/redo toolbar button text
  toolbarUndo: "Undo",
  toolbarRedo: "Redo",
  // Columns selector toolbar button text
  toolbarColumns: "Columns",
  toolbarColumnsLabel: "Select columns",
  // Filters toolbar button text
  toolbarFilters: "Filters",
  toolbarFiltersLabel: "Show filters",
  toolbarFiltersTooltipHide: "Hide filters",
  toolbarFiltersTooltipShow: "Show filters",
  toolbarFiltersTooltipActive: (count) => count !== 1 ? `${count} active filters` : `${count} active filter`,
  // Quick filter toolbar field
  toolbarQuickFilterPlaceholder: "Search…",
  toolbarQuickFilterLabel: "Search",
  toolbarQuickFilterDeleteIconLabel: "Clear",
  // Export selector toolbar button text
  toolbarExport: "Export",
  toolbarExportLabel: "Export",
  toolbarExportCSV: "Download as CSV",
  toolbarExportPrint: "Print",
  toolbarExportExcel: "Download as Excel",
  // Toolbar pivot button
  toolbarPivot: "Pivot",
  // Toolbar charts button
  toolbarCharts: "Charts",
  // Toolbar AI Assistant button
  toolbarAssistant: "AI Assistant",
  // Columns management text
  columnsManagementSearchTitle: "Search",
  columnsManagementNoColumns: "No columns",
  columnsManagementShowHideAllText: "Show/Hide All",
  columnsManagementReset: "Reset",
  columnsManagementDeleteIconLabel: "Clear",
  // Filter panel text
  filterPanelAddFilter: "Add filter",
  filterPanelRemoveAll: "Remove all",
  filterPanelDeleteIconLabel: "Delete",
  filterPanelLogicOperator: "Logic operator",
  filterPanelOperator: "Operator",
  filterPanelOperatorAnd: "And",
  filterPanelOperatorOr: "Or",
  filterPanelColumns: "Columns",
  filterPanelInputLabel: "Value",
  filterPanelInputPlaceholder: "Filter value",
  // Filter operators text
  filterOperatorContains: "contains",
  filterOperatorDoesNotContain: "does not contain",
  filterOperatorEquals: "equals",
  filterOperatorDoesNotEqual: "does not equal",
  filterOperatorStartsWith: "starts with",
  filterOperatorEndsWith: "ends with",
  filterOperatorIs: "is",
  filterOperatorNot: "is not",
  filterOperatorAfter: "is after",
  filterOperatorOnOrAfter: "is on or after",
  filterOperatorBefore: "is before",
  filterOperatorOnOrBefore: "is on or before",
  filterOperatorIsEmpty: "is empty",
  filterOperatorIsNotEmpty: "is not empty",
  filterOperatorIsAnyOf: "is any of",
  "filterOperator=": "=",
  "filterOperator!=": "!=",
  "filterOperator>": ">",
  "filterOperator>=": ">=",
  "filterOperator<": "<",
  "filterOperator<=": "<=",
  // Header filter operators text
  headerFilterOperatorContains: "Contains",
  headerFilterOperatorDoesNotContain: "Does not contain",
  headerFilterOperatorEquals: "Equals",
  headerFilterOperatorDoesNotEqual: "Does not equal",
  headerFilterOperatorStartsWith: "Starts with",
  headerFilterOperatorEndsWith: "Ends with",
  headerFilterOperatorIs: "Is",
  headerFilterOperatorNot: "Is not",
  headerFilterOperatorAfter: "Is after",
  headerFilterOperatorOnOrAfter: "Is on or after",
  headerFilterOperatorBefore: "Is before",
  headerFilterOperatorOnOrBefore: "Is on or before",
  headerFilterOperatorIsEmpty: "Is empty",
  headerFilterOperatorIsNotEmpty: "Is not empty",
  headerFilterOperatorIsAnyOf: "Is any of",
  "headerFilterOperator=": "Equals",
  "headerFilterOperator!=": "Not equals",
  "headerFilterOperator>": "Greater than",
  "headerFilterOperator>=": "Greater than or equal to",
  "headerFilterOperator<": "Less than",
  "headerFilterOperator<=": "Less than or equal to",
  headerFilterClear: "Clear filter",
  // Filter values text
  filterValueAny: "any",
  filterValueTrue: "true",
  filterValueFalse: "false",
  // Column menu text
  columnMenuLabel: "Menu",
  columnMenuAriaLabel: (columnName) => `${columnName} column menu`,
  columnMenuShowColumns: "Show columns",
  columnMenuManageColumns: "Manage columns",
  columnMenuFilter: "Filter",
  columnMenuHideColumn: "Hide column",
  columnMenuUnsort: "Unsort",
  columnMenuSortAsc: "Sort by ASC",
  columnMenuSortDesc: "Sort by DESC",
  columnMenuManagePivot: "Manage pivot",
  columnMenuManageCharts: "Manage charts",
  // Column header text
  columnHeaderFiltersTooltipActive: (count) => count !== 1 ? `${count} active filters` : `${count} active filter`,
  columnHeaderFiltersLabel: "Show filters",
  columnHeaderSortIconLabel: "Sort",
  // Rows selected footer text
  footerRowSelected: (count) => count !== 1 ? `${count.toLocaleString()} rows selected` : `${count.toLocaleString()} row selected`,
  // Total row amount footer text
  footerTotalRows: "Total Rows:",
  // Total visible row amount footer text
  footerTotalVisibleRows: (visibleCount, totalCount) => `${visibleCount.toLocaleString()} of ${totalCount.toLocaleString()}`,
  // Checkbox selection text
  checkboxSelectionHeaderName: "Checkbox selection",
  checkboxSelectionSelectAllRows: "Select all rows",
  checkboxSelectionUnselectAllRows: "Unselect all rows",
  checkboxSelectionSelectRow: "Select row",
  checkboxSelectionUnselectRow: "Unselect row",
  // Boolean cell text
  booleanCellTrueLabel: "yes",
  booleanCellFalseLabel: "no",
  // Long text cell
  longTextCellExpandLabel: "Expand",
  longTextCellCollapseLabel: "Collapse",
  // Actions cell more text
  actionsCellMore: "more",
  // Column pinning text
  pinToLeft: "Pin to left",
  pinToRight: "Pin to right",
  unpin: "Unpin",
  // Tree Data
  treeDataGroupingHeaderName: "Group",
  treeDataExpand: "see children",
  treeDataCollapse: "hide children",
  // Grouping columns
  groupingColumnHeaderName: "Group",
  groupColumn: (name) => `Group by ${name}`,
  unGroupColumn: (name) => `Stop grouping by ${name}`,
  // Master/detail
  detailPanelToggle: "Detail panel toggle",
  expandDetailPanel: "Expand",
  collapseDetailPanel: "Collapse",
  // Pagination
  paginationRowsPerPage: "Rows per page:",
  paginationDisplayedRows: ({
    from,
    to,
    count,
    estimated
  }) => {
    if (!estimated) {
      return `${from}–${to} of ${count !== -1 ? count : `more than ${to}`}`;
    }
    const estimatedLabel = estimated && estimated > to ? `around ${estimated}` : `more than ${to}`;
    return `${from}–${to} of ${count !== -1 ? count : estimatedLabel}`;
  },
  paginationItemAriaLabel: (type) => {
    if (type === "first") {
      return "Go to first page";
    }
    if (type === "last") {
      return "Go to last page";
    }
    if (type === "next") {
      return "Go to next page";
    }
    return "Go to previous page";
  },
  // Row reordering text
  rowReorderingHeaderName: "Row reordering",
  // Aggregation
  aggregationMenuItemHeader: "Aggregation",
  aggregationFunctionLabelNone: "none",
  aggregationFunctionLabelSum: "sum",
  aggregationFunctionLabelAvg: "avg",
  aggregationFunctionLabelMin: "min",
  aggregationFunctionLabelMax: "max",
  aggregationFunctionLabelSize: "size",
  // Pivot panel
  pivotToggleLabel: "Pivot",
  pivotRows: "Rows",
  pivotColumns: "Columns",
  pivotValues: "Values",
  pivotCloseButton: "Close pivot settings",
  pivotSearchButton: "Search fields",
  pivotSearchControlPlaceholder: "Search fields",
  pivotSearchControlLabel: "Search fields",
  pivotSearchControlClear: "Clear search",
  pivotNoFields: "No fields",
  pivotMenuMoveUp: "Move up",
  pivotMenuMoveDown: "Move down",
  pivotMenuMoveToTop: "Move to top",
  pivotMenuMoveToBottom: "Move to bottom",
  pivotMenuRows: "Rows",
  pivotMenuColumns: "Columns",
  pivotMenuValues: "Values",
  pivotMenuOptions: "Field options",
  pivotMenuAddToRows: "Add to Rows",
  pivotMenuAddToColumns: "Add to Columns",
  pivotMenuAddToValues: "Add to Values",
  pivotMenuRemove: "Remove",
  pivotDragToRows: "Drag here to create rows",
  pivotDragToColumns: "Drag here to create columns",
  pivotDragToValues: "Drag here to create values",
  pivotYearColumnHeaderName: "(Year)",
  pivotQuarterColumnHeaderName: "(Quarter)",
  // Charts configuration panel
  chartsNoCharts: "There are no charts available",
  chartsChartNotSelected: "Select a chart type to configure its options",
  chartsTabChart: "Chart",
  chartsTabFields: "Fields",
  chartsTabCustomize: "Customize",
  chartsCloseButton: "Close charts configuration",
  chartsSyncButtonLabel: "Sync chart",
  chartsSearchPlaceholder: "Search fields",
  chartsSearchLabel: "Search fields",
  chartsSearchClear: "Clear search",
  chartsNoFields: "No fields",
  chartsFieldBlocked: "This field cannot be added to any section",
  chartsCategories: "Categories",
  chartsSeries: "Series",
  chartsMenuAddToDimensions: (dimensionLabel) => `Add to ${dimensionLabel}`,
  chartsMenuAddToValues: (valuesLabel) => `Add to ${valuesLabel}`,
  chartsMenuMoveUp: "Move up",
  chartsMenuMoveDown: "Move down",
  chartsMenuMoveToTop: "Move to top",
  chartsMenuMoveToBottom: "Move to bottom",
  chartsMenuOptions: "Field options",
  chartsMenuRemove: "Remove",
  chartsDragToDimensions: (dimensionLabel) => `Drag here to use column as ${dimensionLabel}`,
  chartsDragToValues: (valuesLabel) => `Drag here to use column as ${valuesLabel}`,
  // AI Assistant panel
  aiAssistantPanelTitle: "AI Assistant",
  aiAssistantPanelClose: "Close AI Assistant",
  aiAssistantPanelNewConversation: "New conversation",
  aiAssistantPanelConversationHistory: "Conversation history",
  aiAssistantPanelEmptyConversation: "No prompt history",
  aiAssistantSuggestions: "Suggestions",
  // Prompt field
  promptFieldLabel: "Prompt",
  promptFieldPlaceholder: "Type a prompt…",
  promptFieldPlaceholderWithRecording: "Type or record a prompt…",
  promptFieldPlaceholderListening: "Listening for prompt…",
  promptFieldSpeechRecognitionNotSupported: "Speech recognition is not supported in this browser",
  promptFieldSend: "Send",
  promptFieldRecord: "Record",
  promptFieldStopRecording: "Stop recording",
  // Prompt
  promptRerun: "Run again",
  promptProcessing: "Processing…",
  promptAppliedChanges: "Applied changes",
  // Prompt changes
  promptChangeGroupDescription: (column) => `Group by ${column}`,
  promptChangeAggregationLabel: (column, aggregation) => `${column} (${aggregation})`,
  promptChangeAggregationDescription: (column, aggregation) => `Aggregate ${column} (${aggregation})`,
  promptChangeFilterLabel: (column, operator, value) => {
    if (operator === "is any of") {
      return `${column} is any of: ${value}`;
    }
    return `${column} ${operator} ${value}`;
  },
  promptChangeFilterDescription: (column, operator, value) => {
    if (operator === "is any of") {
      return `Filter where ${column} is any of: ${value}`;
    }
    return `Filter where ${column} ${operator} ${value}`;
  },
  promptChangeSortDescription: (column, direction) => `Sort by ${column} (${direction})`,
  promptChangePivotEnableLabel: "Pivot",
  promptChangePivotEnableDescription: "Enable pivot",
  promptChangePivotColumnsLabel: (count) => `Columns (${count})`,
  promptChangePivotColumnsDescription: (column, direction) => `${column}${direction ? ` (${direction})` : ""}`,
  promptChangePivotRowsLabel: (count) => `Rows (${count})`,
  promptChangePivotValuesLabel: (count) => `Values (${count})`,
  promptChangePivotValuesDescription: (column, aggregation) => `${column} (${aggregation})`,
  promptChangeChartsLabel: (dimensionsCount, valuesCount) => `Dimensions (${dimensionsCount}), Values (${valuesCount})`
};

// node_modules/@mui/x-data-grid/esm/constants/gridClasses.js
function getDataGridUtilityClass(slot) {
  return generateUtilityClass("MuiDataGrid", slot);
}
var gridClassesOverrides = {
  root: ["autoHeight", "autosizing", "root--densityStandard", "root--densityComfortable", "root--densityCompact", "root--disableUserSelection", "root--noToolbar", "withVerticalBorder"],
  children: [
    "actionsCell",
    "booleanCell",
    "cell",
    "cell--editable",
    "cell--editing",
    "cell--flex",
    "cell--pinnedLeft",
    "cell--pinnedRight",
    "cell--rangeBottom",
    "cell--rangeLeft",
    "cell--rangeRight",
    "cell--rangeTop",
    "cell--selectionMode",
    "cell--textCenter",
    "cell--textLeft",
    "cell--textRight",
    "cell--withLeftBorder",
    "cell--withRightBorder",
    "cellCheckbox",
    "cellEmpty",
    "cellOffsetLeft",
    "cellSkeleton",
    "checkboxInput",
    "columnHeader",
    "columnHeader--alignCenter",
    "columnHeader--alignLeft",
    "columnHeader--alignRight",
    "columnHeader--dragging",
    "columnHeader--emptyGroup",
    "columnHeader--filledGroup",
    "columnHeader--filtered",
    "columnHeader--last",
    "columnHeader--moving",
    "columnHeader--numeric",
    "columnHeader--pinnedLeft",
    "columnHeader--pinnedRight",
    "columnHeader--siblingFocused",
    "columnHeader--sortable",
    "columnHeader--sorted",
    "columnHeader--withLeftBorder",
    "columnHeader--withRightBorder",
    "columnHeaderCheckbox",
    "columnHeaderDraggableContainer",
    "columnHeaderTitleContainer",
    "columnHeaderTitleContainerContent",
    "columnSeparator",
    "columnSeparator--resizable",
    "columnSeparator--resizing",
    "columnSeparator--sideLeft",
    "columnSeparator--sideRight",
    "container--bottom",
    "container--top",
    "detailPanelToggleCell",
    "detailPanelToggleCell--expanded",
    "editBooleanCell",
    "filterIcon",
    "filler--borderBottom",
    "filler--pinnedLeft",
    "filler--pinnedRight",
    "groupingCriteriaCell",
    "groupingCriteriaCellLoadingContainer",
    "groupingCriteriaCellToggle",
    "headerFilterRow",
    "iconSeparator",
    "menuIcon",
    "menuIconButton",
    "menuList",
    "menuOpen",
    "overlayWrapperInner",
    "pinnedRows",
    "pinnedRows--bottom",
    "pinnedRows--top",
    "row",
    "row--borderBottom",
    "row--detailPanelExpanded",
    // TODO v9: Rename to `cell--dragging`
    "row--dragging",
    "row--dynamicHeight",
    "row--editable",
    "row--editing",
    "row--firstVisible",
    "row--lastVisible",
    "rowReorderCell",
    "rowReorderCell--draggable",
    "rowReorderCellContainer",
    "rowReorderCellPlaceholder",
    "rowSkeleton",
    "scrollbar",
    "scrollbar--horizontal",
    "scrollbar--vertical",
    "scrollbarFiller",
    "scrollbarFiller--pinnedRight",
    "sortIcon",
    "treeDataGroupingCell",
    "treeDataGroupingCellLoadingContainer",
    "treeDataGroupingCellToggle",
    "withBorderColor",
    "row--beingDragged"
  ]
};
var gridClasses = generateUtilityClasses("MuiDataGrid", [...gridClassesOverrides.root, ...gridClassesOverrides.children, "aiAssistantPanel", "aiAssistantPanelHeader", "aiAssistantPanelTitleContainer", "aiAssistantPanelTitle", "aiAssistantPanelBody", "aiAssistantPanelEmptyText", "aiAssistantPanelFooter", "aiAssistantPanelConversation", "aiAssistantPanelConversationList", "aiAssistantPanelConversationTitle", "aiAssistantPanelSuggestions", "aiAssistantPanelSuggestionsList", "aiAssistantPanelSuggestionsItem", "aiAssistantPanelSuggestionsLabel", "aggregationColumnHeader", "aggregationColumnHeader--alignLeft", "aggregationColumnHeader--alignCenter", "aggregationColumnHeader--alignRight", "aggregationColumnHeaderLabel", "aggregationRowOverlayWrapper", "mainContent", "withSidePanel", "collapsible", "collapsibleTrigger", "collapsibleIcon", "collapsiblePanel", "columnHeader--filter", "columnHeaderFilterInput", "columnHeaderFilterOperatorLabel", "columnHeaderTitle", "columnHeaders", "columnsManagement", "columnsManagementRow", "columnsManagementHeader", "columnsManagementSearchInput", "columnsManagementFooter", "columnsManagementScrollArea", "columnsManagementEmptyText", "detailPanel", "footerCell", "panel", "panelHeader", "panelWrapper", "panelContent", "panelFooter", "paper", "editInputCell", "longTextCell", "longTextCellContent", "longTextCellExpandButton", "longTextCellCollapseButton", "longTextCellPopup", "longTextCellPopperContent", "editLongTextCell", "editLongTextCellValue", "editLongTextCellPopup", "editLongTextCellPopperContent", "editLongTextCellTextarea", "filler", "filterForm", "filterFormDeleteIcon", "filterFormLogicOperatorInput", "filterFormColumnInput", "filterFormOperatorInput", "filterFormValueInput", "footerContainer", "iconButtonContainer", "main", "main--hasPinnedRight", "main--hiddenContent", "menu", "overlay", "overlayWrapper", "root", "rowCount", "rowReorderIcon", "scrollArea--left", "scrollArea--right", "scrollArea--up", "scrollArea--down", "scrollArea", "scrollShadow", "scrollShadow--vertical", "scrollShadow--horizontal", "selectedRowCount", "sortButton", "shadowScrollArea", "sidebar", "sidebarHeader", "toolbarContainer", "toolbar", "toolbarLabel", "toolbarDivider", "toolbarFilterList", "toolbarQuickFilter", "toolbarQuickFilterTrigger", "toolbarQuickFilterControl", "virtualScroller", "virtualScroller--hasScrollX", "virtualScrollerContent", "virtualScrollerContent--overflowed", "virtualScrollerRenderZone", "pivotPanelAvailableFields", "pivotPanelField", "pivotPanelField--sorted", "pivotPanelFieldActionContainer", "pivotPanelFieldCheckbox", "pivotPanelFieldDragIcon", "pivotPanelFieldList", "pivotPanelFieldName", "pivotPanelHeader", "pivotPanelPlaceholder", "pivotPanelScrollArea", "pivotPanelSearchContainer", "pivotPanelSection", "pivotPanelSectionTitle", "pivotPanelSections", "pivotPanelSwitch", "pivotPanelSwitchLabel", "prompt", "promptContent", "promptText", "promptFeedback", "promptChangeList", "promptChangesToggle", "promptChangesToggleIcon", "promptIcon", "promptIconContainer", "promptError", "promptAction", "resizablePanelHandle", "resizablePanelHandle--horizontal", "resizablePanelHandle--vertical"]);

// node_modules/@mui/x-data-grid/esm/constants/signature.js
var GridSignature = (function(GridSignature2) {
  GridSignature2["DataGrid"] = "DataGrid";
  GridSignature2["DataGridPro"] = "DataGridPro";
  GridSignature2["DataGridPremium"] = "DataGridPremium";
  return GridSignature2;
})({});

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridApiContext.js
var React11 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/GridApiContext.js
var React10 = __toESM(require_react(), 1);
var GridApiContext = React10.createContext(void 0);
if (true) GridApiContext.displayName = "GridApiContext";

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridApiContext.js
function useGridApiContext() {
  const apiRef = React11.useContext(GridApiContext);
  if (apiRef === void 0) {
    throw new Error(["MUI X: Could not find the Data Grid context.", "It looks like you rendered your component outside of a DataGrid, DataGridPro or DataGridPremium parent component.", "This can also happen if you are bundling multiple versions of the Data Grid."].join("\n"));
  }
  return apiRef;
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridEvent.js
var React12 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/utils/cleanupTracking/TimerBasedCleanupTracking.js
var CLEANUP_TIMER_LOOP_MILLIS = 1e3;
var TimerBasedCleanupTracking = class {
  constructor(timeout2 = CLEANUP_TIMER_LOOP_MILLIS) {
    __publicField(this, "timeouts", /* @__PURE__ */ (() => /* @__PURE__ */ new Map())());
    __publicField(this, "cleanupTimeout", /* @__PURE__ */ (() => CLEANUP_TIMER_LOOP_MILLIS)());
    this.cleanupTimeout = timeout2;
  }
  register(object, unsubscribe, unregisterToken) {
    if (!this.timeouts) {
      this.timeouts = /* @__PURE__ */ new Map();
    }
    const timeout2 = setTimeout(() => {
      if (typeof unsubscribe === "function") {
        unsubscribe();
      }
      this.timeouts.delete(unregisterToken.cleanupToken);
    }, this.cleanupTimeout);
    this.timeouts.set(unregisterToken.cleanupToken, timeout2);
  }
  unregister(unregisterToken) {
    const timeout2 = this.timeouts.get(unregisterToken.cleanupToken);
    if (timeout2) {
      this.timeouts.delete(unregisterToken.cleanupToken);
      clearTimeout(timeout2);
    }
  }
  reset() {
    if (this.timeouts) {
      this.timeouts.forEach((value, key) => {
        this.unregister({
          cleanupToken: key
        });
      });
      this.timeouts = void 0;
    }
  }
};

// node_modules/@mui/x-data-grid/esm/utils/cleanupTracking/FinalizationRegistryBasedCleanupTracking.js
var FinalizationRegistryBasedCleanupTracking = class {
  constructor() {
    __publicField(this, "registry", (() => new FinalizationRegistry((unsubscribe) => {
      if (typeof unsubscribe === "function") {
        unsubscribe();
      }
    }))());
  }
  register(object, unsubscribe, unregisterToken) {
    this.registry.register(object, unsubscribe, unregisterToken);
  }
  unregister(unregisterToken) {
    this.registry.unregister(unregisterToken);
  }
  reset() {
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridEvent.js
var ObjectToBeRetainedByReact = class _ObjectToBeRetainedByReact {
  static create() {
    return new _ObjectToBeRetainedByReact();
  }
};
var registryContainer = {
  current: createRegistry()
};
var cleanupTokensCounter = 0;
function useGridEvent(apiRef, eventName, handler, options3) {
  const objectRetainedByReact = React12.useState(ObjectToBeRetainedByReact.create)[0];
  const subscription = React12.useRef(null);
  const handlerRef = React12.useRef(null);
  handlerRef.current = handler;
  const cleanupTokenRef = React12.useRef(null);
  if (!subscription.current && handlerRef.current) {
    const enhancedHandler = (params, event, details) => {
      var _a;
      if (event && !event.defaultMuiPrevented) {
        (_a = handlerRef.current) == null ? void 0 : _a.call(handlerRef, params, event, details);
      }
    };
    subscription.current = apiRef.current.subscribeEvent(eventName, enhancedHandler, options3);
    cleanupTokensCounter += 1;
    cleanupTokenRef.current = {
      cleanupToken: cleanupTokensCounter
    };
    registryContainer.current.register(
      objectRetainedByReact,
      // The callback below will be called once this reference stops being retained
      () => {
        var _a;
        (_a = subscription.current) == null ? void 0 : _a.call(subscription);
        subscription.current = null;
        cleanupTokenRef.current = null;
      },
      cleanupTokenRef.current
    );
  } else if (!handlerRef.current && subscription.current) {
    subscription.current();
    subscription.current = null;
    if (cleanupTokenRef.current) {
      registryContainer.current.unregister(cleanupTokenRef.current);
      cleanupTokenRef.current = null;
    }
  }
  React12.useEffect(() => {
    if (!subscription.current && handlerRef.current) {
      const enhancedHandler = (params, event, details) => {
        var _a;
        if (event && !event.defaultMuiPrevented) {
          (_a = handlerRef.current) == null ? void 0 : _a.call(handlerRef, params, event, details);
        }
      };
      subscription.current = apiRef.current.subscribeEvent(eventName, enhancedHandler, options3);
    }
    if (cleanupTokenRef.current && registryContainer.current) {
      registryContainer.current.unregister(cleanupTokenRef.current);
      cleanupTokenRef.current = null;
    }
    return () => {
      var _a;
      (_a = subscription.current) == null ? void 0 : _a.call(subscription);
      subscription.current = null;
    };
  }, [apiRef, eventName, options3]);
}
var OPTIONS_IS_FIRST = {
  isFirst: true
};
function useGridEventPriority(apiRef, eventName, handler) {
  useGridEvent(apiRef, eventName, handler, OPTIONS_IS_FIRST);
}
function unstable_resetCleanupTracking() {
  var _a;
  (_a = registryContainer.current) == null ? void 0 : _a.reset();
  registryContainer.current = createRegistry();
}
function createRegistry() {
  return typeof FinalizationRegistry !== "undefined" ? new FinalizationRegistryBasedCleanupTracking() : new TimerBasedCleanupTracking();
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridSelector.js
var React13 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/warning/warning.js
var warnedOnceCache = /* @__PURE__ */ new Set();
function warnOnce(message, gravity = "warning") {
  if (false) {
    return;
  }
  const cleanMessage = Array.isArray(message) ? message.join("\n") : message;
  if (!warnedOnceCache.has(cleanMessage)) {
    warnedOnceCache.add(cleanMessage);
    if (gravity === "error") {
      console.error(cleanMessage);
    } else {
      console.warn(cleanMessage);
    }
  }
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridSelector.js
var import_shim2 = __toESM(require_shim(), 1);
var defaultCompare = Object.is;
var objectShallowCompare = fastObjectShallowCompare;
var arrayShallowCompare = (a, b) => {
  if (a === b) {
    return true;
  }
  return a.length === b.length && a.every((v, i2) => v === b[i2]);
};
var argsEqual = (prev, curr) => {
  let fn2 = Object.is;
  if (curr instanceof Array) {
    fn2 = arrayShallowCompare;
  } else if (curr instanceof Object) {
    fn2 = objectShallowCompare;
  }
  return fn2(prev, curr);
};
var createRefs = () => ({
  state: null,
  equals: null,
  selector: null,
  args: void 0
});
var EMPTY2 = [];
var emptyGetSnapshot = () => null;
function useGridSelector(apiRef, selector, args = void 0, equals = defaultCompare) {
  if (!apiRef.current.state) {
    warnOnce(["MUI X: `useGridSelector` has been called before the initialization of the state.", "This hook can only be used inside the context of the grid."]);
  }
  const refs = useLazyRef(createRefs);
  const didInit = refs.current.selector !== null;
  const [state, setState2] = React13.useState(
    // We don't use an initialization function to avoid allocations
    didInit ? null : selector(apiRef, args)
  );
  refs.current.state = state;
  refs.current.equals = equals;
  refs.current.selector = selector;
  const prevArgs = refs.current.args;
  refs.current.args = args;
  if (didInit && !argsEqual(prevArgs, args)) {
    const newState = refs.current.selector(apiRef, refs.current.args);
    if (!refs.current.equals(refs.current.state, newState)) {
      refs.current.state = newState;
      setState2(newState);
    }
  }
  const subscribe = React13.useCallback(
    () => {
      if (refs.current.subscription) {
        return null;
      }
      refs.current.subscription = apiRef.current.store.subscribe(() => {
        const newState = refs.current.selector(apiRef, refs.current.args);
        if (!refs.current.equals(refs.current.state, newState)) {
          refs.current.state = newState;
          setState2(newState);
        }
      });
      return null;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    EMPTY2
  );
  const unsubscribe = React13.useCallback(() => {
    if (!refs.current.subscription) {
      subscribe();
    }
    return () => {
      if (refs.current.subscription) {
        refs.current.subscription();
        refs.current.subscription = void 0;
      }
    };
  }, EMPTY2);
  (0, import_shim2.useSyncExternalStore)(unsubscribe, subscribe, emptyGetSnapshot);
  return state;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/density/densitySelector.js
var COMPACT_DENSITY_FACTOR = 0.7;
var COMFORTABLE_DENSITY_FACTOR = 1.3;
var DENSITY_FACTORS = {
  compact: COMPACT_DENSITY_FACTOR,
  comfortable: COMFORTABLE_DENSITY_FACTOR,
  standard: 1
};
var gridDensitySelector = createRootSelector((state) => state.density);
var gridDensityFactorSelector = createSelector3(gridDensitySelector, (density) => DENSITY_FACTORS[density]);

// node_modules/@mui/utils/esm/useTimeout/useTimeout.js
var Timeout = class _Timeout {
  constructor() {
    __publicField(this, "currentId", null);
    __publicField(this, "clear", () => {
      if (this.currentId !== null) {
        clearTimeout(this.currentId);
        this.currentId = null;
      }
    });
    __publicField(this, "disposeEffect", () => {
      return this.clear;
    });
  }
  static create() {
    return new _Timeout();
  }
  /**
   * Executes `fn` after `delay`, clearing any previously scheduled call.
   */
  start(delay, fn2) {
    this.clear();
    this.currentId = setTimeout(() => {
      this.currentId = null;
      fn2();
    }, delay);
  }
};
function useTimeout() {
  const timeout2 = useLazyRef(Timeout.create).current;
  useOnMount(timeout2.disposeEffect);
  return timeout2;
}

// node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js
function _objectWithoutPropertiesLoose(r, e) {
  if (null == r) return {};
  var t = {};
  for (var n in r) if ({}.hasOwnProperty.call(r, n)) {
    if (-1 !== e.indexOf(n)) continue;
    t[n] = r[n];
  }
  return t;
}

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditInputCell.js
var React14 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/utils/assert.js
function NotRendered(_props) {
  throw new Error("Failed assertion: should not be rendered");
}

// node_modules/@mui/x-data-grid/esm/constants/cssVariables.js
var breakpoints = {
  values: {
    xs: 0,
    // phone
    sm: 600,
    // tablet
    md: 900,
    // small laptop
    lg: 1200,
    // desktop
    xl: 1536
    // large screen
  },
  up: (key) => {
    const values3 = breakpoints.values;
    const value = typeof values3[key] === "number" ? values3[key] : key;
    return `@media (min-width:${value}px)`;
  }
};
var keys = {
  spacingUnit: "--DataGrid-t-spacing-unit",
  /* Variables */
  colors: {
    border: {
      base: "--DataGrid-t-color-border-base"
    },
    foreground: {
      base: "--DataGrid-t-color-foreground-base",
      muted: "--DataGrid-t-color-foreground-muted",
      accent: "--DataGrid-t-color-foreground-accent",
      disabled: "--DataGrid-t-color-foreground-disabled",
      error: "--DataGrid-t-color-foreground-error"
    },
    background: {
      base: "--DataGrid-t-color-background-base",
      overlay: "--DataGrid-t-color-background-overlay",
      backdrop: "--DataGrid-t-color-background-backdrop"
    },
    interactive: {
      hover: "--DataGrid-t-color-interactive-hover",
      hoverOpacity: "--DataGrid-t-color-interactive-hover-opacity",
      focus: "--DataGrid-t-color-interactive-focus",
      focusOpacity: "--DataGrid-t-color-interactive-focus-opacity",
      disabled: "--DataGrid-t-color-interactive-disabled",
      disabledOpacity: "--DataGrid-t-color-interactive-disabled-opacity",
      selected: "--DataGrid-t-color-interactive-selected",
      selectedOpacity: "--DataGrid-t-color-interactive-selected-opacity"
    }
  },
  header: {
    background: {
      base: "--DataGrid-t-header-background-base"
    }
  },
  cell: {
    background: {
      pinned: "--DataGrid-t-cell-background-pinned"
    }
  },
  radius: {
    base: "--DataGrid-t-radius-base"
  },
  typography: {
    font: {
      body: "--DataGrid-t-typography-font-body",
      small: "--DataGrid-t-typography-font-small",
      large: "--DataGrid-t-typography-font-large"
    },
    fontFamily: {
      base: "--DataGrid-t-typography-font-family-base"
    },
    fontWeight: {
      light: "--DataGrid-t-typography-font-weight-light",
      regular: "--DataGrid-t-typography-font-weight-regular",
      medium: "--DataGrid-t-typography-font-weight-medium",
      bold: "--DataGrid-t-typography-font-weight-bold"
    }
  },
  transitions: {
    easing: {
      easeIn: "--DataGrid-t-transition-easing-ease-in",
      easeOut: "--DataGrid-t-transition-easing-ease-out",
      easeInOut: "--DataGrid-t-transition-easing-ease-in-out"
    },
    duration: {
      short: "--DataGrid-t-transition-duration-short",
      base: "--DataGrid-t-transition-duration-base",
      long: "--DataGrid-t-transition-duration-long"
    }
  },
  shadows: {
    base: "--DataGrid-t-shadow-base",
    overlay: "--DataGrid-t-shadow-overlay"
  },
  zIndex: {
    panel: "--DataGrid-t-z-index-panel",
    menu: "--DataGrid-t-z-index-menu"
  }
};
var values = wrap(keys);
var vars = _extends({
  breakpoints,
  spacing,
  transition,
  keys
}, values);
function spacing(a, b, c, d) {
  if (a === void 0) {
    return spacingString(1);
  }
  if (b === void 0) {
    return spacingString(a);
  }
  if (c === void 0) {
    return spacingString(a) + " " + spacingString(b);
  }
  if (d === void 0) {
    return spacingString(a) + " " + spacingString(b) + " " + spacingString(c);
  }
  return spacingString(a) + " " + spacingString(b) + " " + spacingString(c) + " " + spacingString(d);
}
function spacingString(value) {
  if (value === 0) {
    return "0";
  }
  return `calc(var(--DataGrid-t-spacing-unit) * ${value})`;
}
function transition(props, options3) {
  const {
    duration = vars.transitions.duration.base,
    easing = vars.transitions.easing.easeInOut,
    delay = 0
  } = options3 ?? {};
  return props.map((prop) => `${prop} ${duration} ${easing} ${delay}ms`).join(", ");
}
function wrap(input) {
  if (typeof input === "string") {
    return `var(${input})`;
  }
  const result = {};
  for (const key in input) {
    if (Object.hasOwn(input, key)) {
      result[key] = wrap(input[key]);
    }
  }
  return result;
}

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditInputCell.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var _excluded = ["id", "value", "formattedValue", "api", "field", "row", "rowNode", "colDef", "cellMode", "isEditable", "tabIndex", "hasFocus", "isValidating", "debounceMs", "isProcessingProps", "onValueChange", "slotProps"];
var useUtilityClasses = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["editInputCell"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridEditInputCellRoot = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "EditInputCell"
})({
  font: vars.typography.font.body,
  padding: "1px 0",
  "& input": {
    padding: "0 16px",
    height: "100%"
  }
});
var GridEditInputCell = forwardRef2((props, ref) => {
  const rootProps = useGridRootProps();
  const {
    id,
    value,
    field,
    colDef,
    hasFocus,
    debounceMs = 200,
    isProcessingProps,
    onValueChange,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded);
  const apiRef = useGridApiContext();
  const inputRef = React14.useRef(null);
  const [valueState, setValueState] = React14.useState(value);
  const classes2 = useUtilityClasses(rootProps);
  const handleChange = React14.useCallback(async (event) => {
    const newValue = event.target.value;
    const column = apiRef.current.getColumn(field);
    let parsedValue = newValue;
    if (column.valueParser) {
      parsedValue = column.valueParser(newValue, apiRef.current.getRow(id), column, apiRef);
    }
    setValueState(parsedValue);
    apiRef.current.setEditCellValue({
      id,
      field,
      value: parsedValue,
      debounceMs,
      unstable_skipValueParser: true
    }, event);
    if (onValueChange) {
      await onValueChange(event, newValue);
    }
  }, [apiRef, debounceMs, field, id, onValueChange]);
  const meta = apiRef.current.unstable_getEditCellMeta(id, field);
  React14.useEffect(() => {
    if ((meta == null ? void 0 : meta.changeReason) !== "debouncedSetEditCellValue") {
      setValueState(value);
    }
  }, [meta, value]);
  useEnhancedEffect_default(() => {
    if (hasFocus) {
      inputRef.current.focus();
    }
  }, [hasFocus]);
  return (0, import_jsx_runtime.jsx)(GridEditInputCellRoot, _extends({
    as: rootProps.slots.baseInput,
    inputRef,
    className: classes2.root,
    ownerState: rootProps,
    fullWidth: true,
    type: colDef.type === "number" ? colDef.type : "text",
    value: valueState ?? "",
    onChange: handleChange,
    endAdornment: isProcessingProps ? (0, import_jsx_runtime.jsx)(rootProps.slots.loadIcon, {
      fontSize: "small",
      color: "action"
    }) : void 0
  }, other, slotProps == null ? void 0 : slotProps.root, {
    ref
  }));
});
if (true) GridEditInputCell.displayName = "GridEditInputCell";
var renderEditInputCell = (params) => (0, import_jsx_runtime.jsx)(GridEditInputCell, _extends({}, params));
if (true) renderEditInputCell.displayName = "renderEditInputCell";

// node_modules/@mui/x-data-grid/esm/hooks/features/sorting/gridSortingUtils.js
var sanitizeSortModel = (model, disableMultipleColumnsSorting) => {
  if (disableMultipleColumnsSorting && model.length > 1) {
    warnOnce(["MUI X: The `sortModel` can only contain a single item when the `disableMultipleColumnsSorting` prop is set to `true`.", "If you are using the community version of the Data Grid, this prop is always `true`."], "error");
    return [model[0]];
  }
  return model;
};
var mergeStateWithSortModel = (sortModel, disableMultipleColumnsSorting) => (state) => _extends({}, state, {
  sorting: _extends({}, state.sorting, {
    sortModel: sanitizeSortModel(sortModel, disableMultipleColumnsSorting)
  })
});
var isDesc = (direction) => direction === "desc";
var parseSortItem = (sortItem, apiRef) => {
  const column = apiRef.current.getColumn(sortItem.field);
  if (!column || sortItem.sort === null) {
    return null;
  }
  let comparator;
  if (column.getSortComparator) {
    comparator = column.getSortComparator(sortItem.sort);
  } else {
    comparator = isDesc(sortItem.sort) ? (...args) => -1 * column.sortComparator(...args) : column.sortComparator;
  }
  if (!comparator) {
    return null;
  }
  const getSortCellParams = (id) => ({
    id,
    field: column.field,
    rowNode: gridRowNodeSelector(apiRef, id),
    value: apiRef.current.getCellValue(id, column.field),
    api: apiRef.current
  });
  return {
    getSortCellParams,
    comparator
  };
};
var compareRows = (parsedSortItems, row1, row2) => {
  return parsedSortItems.reduce((res, item, index) => {
    if (res !== 0) {
      return res;
    }
    const sortCellParams1 = row1.params[index];
    const sortCellParams2 = row2.params[index];
    res = item.comparator(sortCellParams1.value, sortCellParams2.value, sortCellParams1, sortCellParams2);
    return res;
  }, 0);
};
var buildAggregatedSortingApplier = (sortModel, apiRef) => {
  const comparatorList = sortModel.map((item) => parseSortItem(item, apiRef)).filter((comparator) => !!comparator);
  if (comparatorList.length === 0) {
    return null;
  }
  return (rowList) => rowList.map((node) => ({
    node,
    params: comparatorList.map((el) => el.getSortCellParams(node.id))
  })).sort((a, b) => compareRows(comparatorList, a, b)).map((row) => row.node.id);
};
var getNextGridSortDirection = (sortingOrder, current) => {
  const currentIdx = sortingOrder.indexOf(current);
  if (!current || currentIdx === -1 || currentIdx + 1 === sortingOrder.length) {
    return sortingOrder[0];
  }
  return sortingOrder[currentIdx + 1];
};
var gridNillComparator = (v1, v2) => {
  if (v1 == null && v2 != null) {
    return -1;
  }
  if (v2 == null && v1 != null) {
    return 1;
  }
  if (v1 == null && v2 == null) {
    return 0;
  }
  return null;
};
var collator = new Intl.Collator();
var gridStringOrNumberComparator = (value1, value2) => {
  const nillResult = gridNillComparator(value1, value2);
  if (nillResult !== null) {
    return nillResult;
  }
  if (typeof value1 === "string") {
    return collator.compare(value1.toString(), value2.toString());
  }
  return value1 - value2;
};
var gridNumberComparator = (value1, value2) => {
  const nillResult = gridNillComparator(value1, value2);
  if (nillResult !== null) {
    return nillResult;
  }
  return Number(value1) - Number(value2);
};
var gridDateComparator = (value1, value2) => {
  const nillResult = gridNillComparator(value1, value2);
  if (nillResult !== null) {
    return nillResult;
  }
  if (value1 > value2) {
    return 1;
  }
  if (value1 < value2) {
    return -1;
  }
  return 0;
};

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputValue.js
var React15 = __toESM(require_react(), 1);
var import_prop_types = __toESM(require_prop_types(), 1);
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var _excluded2 = ["item", "applyValue", "type", "apiRef", "focusElementRef", "tabIndex", "disabled", "isFilterActive", "slotProps", "clearButton", "headerFilterMenu"];
function GridFilterInputValue(props) {
  var _a, _b, _c;
  const {
    item,
    applyValue,
    type,
    apiRef,
    focusElementRef,
    tabIndex,
    disabled,
    slotProps,
    clearButton,
    headerFilterMenu
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded2);
  const textFieldProps = slotProps == null ? void 0 : slotProps.root;
  const filterTimeout = useTimeout();
  const [filterValueState, setFilterValueState] = React15.useState(sanitizeFilterItemValue(item.value));
  const [applying, setIsApplying] = React15.useState(false);
  const id = useId();
  const rootProps = useGridRootProps();
  const onFilterChange = React15.useCallback((event) => {
    const value = sanitizeFilterItemValue(event.target.value);
    setFilterValueState(value);
    setIsApplying(true);
    filterTimeout.start(rootProps.filterDebounceMs, () => {
      const newItem = _extends({}, item, {
        value: type === "number" && !Number.isNaN(Number(value)) ? Number(value) : value,
        fromInput: id
      });
      applyValue(newItem);
      setIsApplying(false);
    });
  }, [filterTimeout, rootProps.filterDebounceMs, item, type, id, applyValue]);
  React15.useEffect(() => {
    const itemPlusTag = item;
    if (itemPlusTag.fromInput !== id || item.value == null) {
      setFilterValueState(sanitizeFilterItemValue(item.value));
    }
  }, [id, item]);
  return (0, import_jsx_runtime2.jsxs)(React15.Fragment, {
    children: [(0, import_jsx_runtime2.jsx)(rootProps.slots.baseTextField, _extends({
      id,
      label: apiRef.current.getLocaleText("filterPanelInputLabel"),
      placeholder: apiRef.current.getLocaleText("filterPanelInputPlaceholder"),
      value: filterValueState ?? "",
      onChange: onFilterChange,
      type: type || "text",
      disabled,
      slotProps: _extends({}, textFieldProps == null ? void 0 : textFieldProps.slotProps, {
        input: _extends({
          endAdornment: applying ? (0, import_jsx_runtime2.jsx)(rootProps.slots.loadIcon, {
            fontSize: "small",
            color: "action"
          }) : null
        }, (_a = textFieldProps == null ? void 0 : textFieldProps.slotProps) == null ? void 0 : _a.input),
        htmlInput: _extends({
          tabIndex
        }, (_b = textFieldProps == null ? void 0 : textFieldProps.slotProps) == null ? void 0 : _b.htmlInput)
      }),
      inputRef: focusElementRef
    }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseTextField, other, textFieldProps)), headerFilterMenu, clearButton]
  });
}
function sanitizeFilterItemValue(value) {
  if (value == null || value === "") {
    return void 0;
  }
  return String(value);
}
true ? GridFilterInputValue.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types.default.shape({
    current: import_prop_types.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types.default.func.isRequired,
  className: import_prop_types.default.string,
  clearButton: import_prop_types.default.node,
  disabled: import_prop_types.default.bool,
  focusElementRef: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.object]),
  headerFilterMenu: import_prop_types.default.node,
  inputRef: import_prop_types.default.oneOfType([import_prop_types.default.func, import_prop_types.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types.default.bool,
  item: import_prop_types.default.shape({
    field: import_prop_types.default.string.isRequired,
    id: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.string]),
    operator: import_prop_types.default.string.isRequired,
    value: import_prop_types.default.any
  }).isRequired,
  onBlur: import_prop_types.default.func,
  onFocus: import_prop_types.default.func,
  slotProps: import_prop_types.default.object,
  tabIndex: import_prop_types.default.number,
  type: import_prop_types.default.oneOf(["date", "datetime-local", "number", "text"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/utils/utils.js
function isNumber(value) {
  return typeof value === "number" && !Number.isNaN(value);
}
function isFunction(value) {
  return typeof value === "function";
}
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function localStorageAvailable() {
  try {
    const key = "__some_random_key_you_are_not_going_to_use__";
    window.localStorage.setItem(key, key);
    window.localStorage.removeItem(key);
    return true;
  } catch (err) {
    return false;
  }
}
function escapeRegExp(value) {
  return value.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
}
var clamp = (value, min2, max2) => Math.max(min2, Math.min(max2, value));
function mulberry32(a) {
  return () => {
    let t = a += 1831565813;
    t = Math.imul(t ^ t >>> 15, t | 1);
    t ^= t + Math.imul(t ^ t >>> 7, t | 61);
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}
function createRandomNumberGenerator(seed) {
  const random = mulberry32(seed);
  return (min2, max2) => min2 + (max2 - min2) * random();
}
function deepClone(obj) {
  if (typeof structuredClone === "function") {
    return structuredClone(obj);
  }
  return JSON.parse(JSON.stringify(obj));
}
function eslintUseValue(_) {
}
var runIf = (condition, fn2) => (...params) => {
  if (condition) {
    fn2(...params);
  }
};

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputMultipleValue.js
var React16 = __toESM(require_react(), 1);
var import_prop_types2 = __toESM(require_prop_types(), 1);
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var _excluded3 = ["item", "applyValue", "type", "apiRef", "focusElementRef", "slotProps"];
function GridFilterInputMultipleValue(props) {
  const {
    item,
    applyValue,
    type,
    apiRef,
    focusElementRef,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded3);
  const id = useId();
  const [options3, setOptions] = React16.useState([]);
  const [filterValueState, setFilterValueState] = React16.useState(item.value || []);
  const rootProps = useGridRootProps();
  React16.useEffect(() => {
    const itemValue = item.value ?? [];
    setFilterValueState(itemValue.map(String));
  }, [item.value]);
  const handleChange = React16.useCallback((event, value) => {
    setFilterValueState(value.map(String));
    applyValue(_extends({}, item, {
      value: [...value.map((filterItemValue) => type === "number" ? Number(filterItemValue) : filterItemValue)]
    }));
  }, [applyValue, item, type]);
  const handleInputChange = React16.useCallback((event, value) => {
    if (value === "") {
      setOptions([]);
    } else {
      setOptions([value]);
    }
  }, [setOptions]);
  const BaseAutocomplete2 = rootProps.slots.baseAutocomplete;
  return (0, import_jsx_runtime3.jsx)(BaseAutocomplete2, _extends({
    multiple: true,
    freeSolo: true,
    options: options3,
    id,
    value: filterValueState,
    onChange: handleChange,
    onInputChange: handleInputChange,
    label: apiRef.current.getLocaleText("filterPanelInputLabel"),
    placeholder: apiRef.current.getLocaleText("filterPanelInputPlaceholder"),
    slotProps: {
      textField: {
        type: type || "text",
        inputRef: focusElementRef
      }
    }
  }, other, slotProps == null ? void 0 : slotProps.root));
}
true ? GridFilterInputMultipleValue.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types2.default.shape({
    current: import_prop_types2.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types2.default.func.isRequired,
  className: import_prop_types2.default.string,
  clearButton: import_prop_types2.default.node,
  disabled: import_prop_types2.default.bool,
  focusElementRef: import_prop_types2.default.oneOfType([import_prop_types2.default.func, import_prop_types2.default.object]),
  headerFilterMenu: import_prop_types2.default.node,
  inputRef: import_prop_types2.default.oneOfType([import_prop_types2.default.func, import_prop_types2.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types2.default.bool,
  item: import_prop_types2.default.shape({
    field: import_prop_types2.default.string.isRequired,
    id: import_prop_types2.default.oneOfType([import_prop_types2.default.number, import_prop_types2.default.string]),
    operator: import_prop_types2.default.string.isRequired,
    value: import_prop_types2.default.any
  }).isRequired,
  onBlur: import_prop_types2.default.func,
  onFocus: import_prop_types2.default.func,
  slotProps: import_prop_types2.default.object,
  tabIndex: import_prop_types2.default.number,
  type: import_prop_types2.default.oneOf(["date", "datetime-local", "number", "text"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/models/gridEditRowModel.js
var GridEditModes = (function(GridEditModes2) {
  GridEditModes2["Cell"] = "cell";
  GridEditModes2["Row"] = "row";
  return GridEditModes2;
})(GridEditModes || {});
var GridCellModes = (function(GridCellModes2) {
  GridCellModes2["Edit"] = "edit";
  GridCellModes2["View"] = "view";
  return GridCellModes2;
})(GridCellModes || {});
var GridRowModes = (function(GridRowModes2) {
  GridRowModes2["Edit"] = "edit";
  GridRowModes2["View"] = "view";
  return GridRowModes2;
})(GridRowModes || {});

// node_modules/@mui/x-data-grid/esm/models/gridFilterItem.js
var GridLogicOperator = (function(GridLogicOperator2) {
  GridLogicOperator2["And"] = "and";
  GridLogicOperator2["Or"] = "or";
  return GridLogicOperator2;
})(GridLogicOperator || {});

// node_modules/@mui/x-data-grid/esm/models/gridRowSelectionManager.js
var IncludeManager = class {
  constructor(model) {
    this.data = model.ids;
  }
  has(id) {
    return this.data.has(id);
  }
  select(id) {
    this.data.add(id);
  }
  unselect(id) {
    this.data.delete(id);
  }
};
var ExcludeManager = class {
  constructor(model) {
    this.data = model.ids;
  }
  has(id) {
    return !this.data.has(id);
  }
  select(id) {
    this.data.delete(id);
  }
  unselect(id) {
    this.data.add(id);
  }
};
var createRowSelectionManager = (model) => {
  if (model.type === "include") {
    return new IncludeManager(model);
  }
  return new ExcludeManager(model);
};

// node_modules/@mui/x-data-grid/esm/models/params/gridEditCellParams.js
var GridCellEditStartReasons = (function(GridCellEditStartReasons2) {
  GridCellEditStartReasons2["enterKeyDown"] = "enterKeyDown";
  GridCellEditStartReasons2["cellDoubleClick"] = "cellDoubleClick";
  GridCellEditStartReasons2["printableKeyDown"] = "printableKeyDown";
  GridCellEditStartReasons2["deleteKeyDown"] = "deleteKeyDown";
  GridCellEditStartReasons2["pasteKeyDown"] = "pasteKeyDown";
  return GridCellEditStartReasons2;
})(GridCellEditStartReasons || {});
var GridCellEditStopReasons = (function(GridCellEditStopReasons2) {
  GridCellEditStopReasons2["cellFocusOut"] = "cellFocusOut";
  GridCellEditStopReasons2["escapeKeyDown"] = "escapeKeyDown";
  GridCellEditStopReasons2["enterKeyDown"] = "enterKeyDown";
  GridCellEditStopReasons2["tabKeyDown"] = "tabKeyDown";
  GridCellEditStopReasons2["shiftTabKeyDown"] = "shiftTabKeyDown";
  return GridCellEditStopReasons2;
})(GridCellEditStopReasons || {});

// node_modules/@mui/x-data-grid/esm/models/params/gridRowParams.js
var GridRowEditStartReasons = (function(GridRowEditStartReasons2) {
  GridRowEditStartReasons2["enterKeyDown"] = "enterKeyDown";
  GridRowEditStartReasons2["cellDoubleClick"] = "cellDoubleClick";
  GridRowEditStartReasons2["printableKeyDown"] = "printableKeyDown";
  GridRowEditStartReasons2["deleteKeyDown"] = "deleteKeyDown";
  return GridRowEditStartReasons2;
})(GridRowEditStartReasons || {});
var GridRowEditStopReasons = (function(GridRowEditStopReasons2) {
  GridRowEditStopReasons2["rowFocusOut"] = "rowFocusOut";
  GridRowEditStopReasons2["escapeKeyDown"] = "escapeKeyDown";
  GridRowEditStopReasons2["enterKeyDown"] = "enterKeyDown";
  GridRowEditStopReasons2["tabKeyDown"] = "tabKeyDown";
  GridRowEditStopReasons2["shiftTabKeyDown"] = "shiftTabKeyDown";
  return GridRowEditStopReasons2;
})(GridRowEditStopReasons || {});

// node_modules/@mui/x-data-grid/esm/models/gridColumnGrouping.js
function isLeaf(node) {
  return node.field !== void 0;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/filter/gridFilterState.js
var defaultGridFilterLookup = {
  filteredRowsLookup: {},
  filteredChildrenCountLookup: {},
  filteredDescendantCountLookup: {}
};
var getDefaultGridFilterModel = () => ({
  items: [],
  logicOperator: GridLogicOperator.And,
  quickFilterValues: [],
  quickFilterLogicOperator: GridLogicOperator.And
});

// node_modules/@mui/x-data-grid/esm/utils/getPublicApiRef.js
function getPublicApiRef(apiRef) {
  return {
    current: apiRef.current.getPublicApi()
  };
}

// node_modules/@mui/x-data-grid/esm/hooks/features/columns/gridColumnsInterfaces.js
var GridPinnedColumnPosition = (function(GridPinnedColumnPosition2) {
  GridPinnedColumnPosition2["LEFT"] = "left";
  GridPinnedColumnPosition2["RIGHT"] = "right";
  return GridPinnedColumnPosition2;
})({});
var EMPTY_PINNED_COLUMN_FIELDS = {
  left: [],
  right: []
};

// node_modules/@mui/x-data-grid/esm/hooks/features/listView/gridListViewSelectors.js
var gridListViewSelector = createRootSelector((state) => state.props.listView ?? false);
var gridListColumnSelector = createRootSelector((state) => state.listViewColumn);

// node_modules/@mui/x-data-grid/esm/hooks/features/columns/gridColumnsSelector.js
var gridColumnsStateSelector = createRootSelector((state) => state.columns);
var gridColumnFieldsSelector = createSelector3(gridColumnsStateSelector, (columnsState) => columnsState.orderedFields);
var gridColumnLookupSelector = createSelector3(gridColumnsStateSelector, (columnsState) => columnsState.lookup);
var gridColumnDefinitionsSelector = createSelectorMemoized2(gridColumnFieldsSelector, gridColumnLookupSelector, (allFields, lookup) => allFields.map((field) => lookup[field]));
var gridColumnVisibilityModelSelector = createSelector3(gridColumnsStateSelector, (columnsState) => columnsState.columnVisibilityModel);
var gridInitialColumnVisibilityModelSelector = createSelector3(gridColumnsStateSelector, (columnsState) => columnsState.initialColumnVisibilityModel);
var gridVisibleColumnDefinitionsSelector = createSelectorMemoized2(gridColumnDefinitionsSelector, gridColumnVisibilityModelSelector, gridListViewSelector, gridListColumnSelector, (columns, columnVisibilityModel, listView, listColumn) => listView && listColumn ? [listColumn] : columns.filter((column) => columnVisibilityModel[column.field] !== false));
var gridVisibleColumnFieldsSelector = createSelectorMemoized2(gridVisibleColumnDefinitionsSelector, (visibleColumns) => visibleColumns.map((column) => column.field));
var gridPinnedColumnsSelector = createRootSelector((state) => state.pinnedColumns);
var gridExistingPinnedColumnSelector = createSelectorMemoized2(gridPinnedColumnsSelector, gridColumnFieldsSelector, (model, orderedFields) => filterMissingColumns(model, orderedFields));
var gridVisiblePinnedColumnDefinitionsSelector = createSelectorMemoized2(gridColumnsStateSelector, gridPinnedColumnsSelector, gridVisibleColumnFieldsSelector, gridListViewSelector, (columnsState, model, visibleColumnFields, listView) => {
  if (listView) {
    return EMPTY_PINNED_COLUMN_FIELDS;
  }
  const visiblePinnedFields = filterMissingColumns(model, visibleColumnFields);
  const visiblePinnedColumns = {
    left: visiblePinnedFields.left.map((field) => columnsState.lookup[field]),
    right: visiblePinnedFields.right.map((field) => columnsState.lookup[field])
  };
  return visiblePinnedColumns;
});
function filterMissingColumns(pinnedColumns, columns) {
  var _a, _b;
  if (!Array.isArray(pinnedColumns.left) && !Array.isArray(pinnedColumns.right)) {
    return EMPTY_PINNED_COLUMN_FIELDS;
  }
  if (((_a = pinnedColumns.left) == null ? void 0 : _a.length) === 0 && ((_b = pinnedColumns.right) == null ? void 0 : _b.length) === 0) {
    return EMPTY_PINNED_COLUMN_FIELDS;
  }
  const filter = (newPinnedColumns, remainingColumns) => {
    if (!Array.isArray(newPinnedColumns)) {
      return [];
    }
    return newPinnedColumns.filter((field) => remainingColumns.includes(field));
  };
  const leftPinnedColumns = filter(pinnedColumns.left, columns);
  const columnsWithoutLeftPinnedColumns = columns.filter((field) => !leftPinnedColumns.includes(field));
  const rightPinnedColumns = filter(pinnedColumns.right, columnsWithoutLeftPinnedColumns);
  return {
    left: leftPinnedColumns,
    right: rightPinnedColumns
  };
}
var gridColumnPositionsSelector = createSelectorMemoized2(gridVisibleColumnDefinitionsSelector, (visibleColumns) => {
  const positions = [];
  let currentPosition = 0;
  for (let i2 = 0; i2 < visibleColumns.length; i2 += 1) {
    positions.push(currentPosition);
    currentPosition += visibleColumns[i2].computedWidth;
  }
  return positions;
});
var gridFilterableColumnDefinitionsSelector = createSelectorMemoized2(gridColumnDefinitionsSelector, (columns) => columns.filter((col) => col.filterable));
var gridFilterableColumnLookupSelector = createSelectorMemoized2(gridColumnDefinitionsSelector, (columns) => columns.reduce((acc, col) => {
  if (col.filterable) {
    acc[col.field] = col;
  }
  return acc;
}, {}));
var gridHasColSpanSelector = createSelectorMemoized2(gridColumnDefinitionsSelector, (columns) => columns.some((column) => column.colSpan !== void 0));

// node_modules/@mui/x-data-grid/esm/hooks/features/filter/gridFilterUtils.js
var hasEval;
function getHasEval() {
  if (hasEval !== void 0) {
    return hasEval;
  }
  try {
    hasEval = new Function("return true")();
  } catch (_) {
    hasEval = false;
  }
  return hasEval;
}
var cleanFilterItem = (item, apiRef) => {
  const cleanItem = _extends({}, item);
  if (cleanItem.id == null) {
    cleanItem.id = Math.round(Math.random() * 1e5);
  }
  if (cleanItem.operator == null) {
    const column = gridColumnLookupSelector(apiRef)[cleanItem.field];
    cleanItem.operator = column && column.filterOperators[0].value;
  }
  return cleanItem;
};
var sanitizeFilterModel = (model, disableMultipleColumnsFiltering, apiRef) => {
  const hasSeveralItems = model.items.length > 1;
  let items;
  if (hasSeveralItems && disableMultipleColumnsFiltering) {
    warnOnce(["MUI X: The `filterModel` can only contain a single item when the `disableMultipleColumnsFiltering` prop is set to `true`.", "If you are using the community version of the Data Grid, this prop is always `true`."], "error");
    items = [model.items[0]];
  } else {
    items = model.items;
  }
  const hasItemsWithoutIds = hasSeveralItems && items.some((item) => item.id == null);
  const hasItemWithoutOperator = items.some((item) => item.operator == null);
  if (hasItemsWithoutIds) {
    warnOnce("MUI X: The `id` field is required on `filterModel.items` when you use multiple filters.", "error");
  }
  if (hasItemWithoutOperator) {
    warnOnce("MUI X: The `operator` field is required on `filterModel.items`, one or more of your filtering item has no `operator` provided.", "error");
  }
  if (hasItemWithoutOperator || hasItemsWithoutIds) {
    return _extends({}, model, {
      items: items.map((item) => cleanFilterItem(item, apiRef))
    });
  }
  if (model.items !== items) {
    return _extends({}, model, {
      items
    });
  }
  return model;
};
var mergeStateWithFilterModel = (filterModel, disableMultipleColumnsFiltering, apiRef) => (filteringState) => _extends({}, filteringState, {
  filterModel: sanitizeFilterModel(filterModel, disableMultipleColumnsFiltering, apiRef)
});
var removeDiacritics = (value) => {
  if (typeof value === "string") {
    return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
  }
  return value;
};
var getFilterCallbackFromItem = (filterItem, filterValueGetter, apiRef) => {
  var _a;
  if (!filterItem.field || !filterItem.operator) {
    return null;
  }
  const column = apiRef.current.getColumn(filterItem.field);
  if (!column) {
    return null;
  }
  let parsedValue;
  if (column.valueParser) {
    const parser = column.valueParser;
    parsedValue = Array.isArray(filterItem.value) ? (_a = filterItem.value) == null ? void 0 : _a.map((x) => parser(x, void 0, column, apiRef)) : parser(filterItem.value, void 0, column, apiRef);
  } else {
    parsedValue = filterItem.value;
  }
  const {
    ignoreDiacritics
  } = apiRef.current.rootProps;
  if (ignoreDiacritics) {
    parsedValue = removeDiacritics(parsedValue);
  }
  const newFilterItem = _extends({}, filterItem, {
    value: parsedValue
  });
  const filterOperators = column.filterOperators;
  if (!(filterOperators == null ? void 0 : filterOperators.length)) {
    throw new Error(`MUI X: No filter operators found for column '${column.field}'.`);
  }
  const filterOperator = filterOperators.find((operator) => operator.value === newFilterItem.operator);
  if (!filterOperator) {
    throw new Error(`MUI X: No filter operator found for column '${column.field}' and operator value '${newFilterItem.operator}'.`);
  }
  const publicApiRef = getPublicApiRef(apiRef);
  const applyFilterOnRow = filterOperator.getApplyFilterFn(newFilterItem, column);
  if (typeof applyFilterOnRow !== "function") {
    return null;
  }
  return {
    item: newFilterItem,
    fn: (row) => {
      let value = filterValueGetter(row, column);
      if (ignoreDiacritics) {
        value = removeDiacritics(value);
      }
      return applyFilterOnRow(value, row, column, publicApiRef);
    }
  };
};
var filterItemsApplierId = 1;
var buildAggregatedFilterItemsApplier = (filterModel, filterValueGetter, apiRef, disableEval) => {
  const {
    items
  } = filterModel;
  const appliers = items.map((item) => getFilterCallbackFromItem(item, filterValueGetter, apiRef)).filter((callback) => !!callback);
  if (appliers.length === 0) {
    return null;
  }
  if (disableEval || !getHasEval()) {
    return (row, shouldApplyFilter) => {
      const resultPerItemId = {};
      for (let i2 = 0; i2 < appliers.length; i2 += 1) {
        const applier = appliers[i2];
        if (!shouldApplyFilter || shouldApplyFilter(applier.item.field)) {
          resultPerItemId[applier.item.id] = applier.fn(row);
        }
      }
      return resultPerItemId;
    };
  }
  const filterItemCore = new Function("appliers", "row", "shouldApplyFilter", `"use strict";
${appliers.map((applier, i2) => `const shouldApply${i2} = !shouldApplyFilter || shouldApplyFilter(${JSON.stringify(applier.item.field)});`).join("\n")}

const result$$ = {
${appliers.map((applier, i2) => `  ${JSON.stringify(String(applier.item.id))}: !shouldApply${i2} ? false : appliers[${i2}].fn(row),`).join("\n")}
};

return result$$;`.replaceAll("$$", String(filterItemsApplierId)));
  filterItemsApplierId += 1;
  const filterItem = (row, shouldApplyItem) => filterItemCore(appliers, row, shouldApplyItem);
  return filterItem;
};
var shouldQuickFilterExcludeHiddenColumns = (filterModel) => {
  return filterModel.quickFilterExcludeHiddenColumns ?? true;
};
var buildAggregatedQuickFilterApplier = (filterModel, filterValueGetter, apiRef) => {
  var _a;
  const quickFilterValues = ((_a = filterModel.quickFilterValues) == null ? void 0 : _a.filter(Boolean)) ?? [];
  if (quickFilterValues.length === 0) {
    return null;
  }
  const allColumnFields = gridColumnFieldsSelector(apiRef);
  const columnVisibilityModel = gridColumnVisibilityModelSelector(apiRef);
  let columnFields;
  if (shouldQuickFilterExcludeHiddenColumns(filterModel)) {
    columnFields = allColumnFields.filter((field) => columnVisibilityModel[field] !== false);
  } else {
    columnFields = allColumnFields;
  }
  const appliersPerField = [];
  const {
    ignoreDiacritics
  } = apiRef.current.rootProps;
  const publicApiRef = getPublicApiRef(apiRef);
  columnFields.forEach((field) => {
    const column = apiRef.current.getColumn(field);
    const getApplyQuickFilterFn = column == null ? void 0 : column.getApplyQuickFilterFn;
    if (getApplyQuickFilterFn) {
      appliersPerField.push({
        column,
        appliers: quickFilterValues.map((quickFilterValue) => {
          const value = ignoreDiacritics ? removeDiacritics(quickFilterValue) : quickFilterValue;
          return {
            fn: getApplyQuickFilterFn(value, column, publicApiRef)
          };
        })
      });
    }
  });
  return function isRowMatchingQuickFilter(row, shouldApplyFilter) {
    const result = {};
    outer: for (let v = 0; v < quickFilterValues.length; v += 1) {
      const filterValue = quickFilterValues[v];
      for (let i2 = 0; i2 < appliersPerField.length; i2 += 1) {
        const {
          column,
          appliers
        } = appliersPerField[i2];
        const {
          field
        } = column;
        if (shouldApplyFilter && !shouldApplyFilter(field)) {
          continue;
        }
        const applier = appliers[v];
        let value = filterValueGetter(row, column);
        if (applier.fn === null) {
          continue;
        }
        if (ignoreDiacritics) {
          value = removeDiacritics(value);
        }
        const isMatching = applier.fn(value, row, column, publicApiRef);
        if (isMatching) {
          result[filterValue] = true;
          continue outer;
        }
      }
      result[filterValue] = false;
    }
    return result;
  };
};
var buildAggregatedFilterApplier = (filterModel, filterValueGetter, apiRef, disableEval) => {
  const isRowMatchingFilterItems = buildAggregatedFilterItemsApplier(filterModel, filterValueGetter, apiRef, disableEval);
  const isRowMatchingQuickFilter = buildAggregatedQuickFilterApplier(filterModel, filterValueGetter, apiRef);
  return function isRowMatchingFilters(row, shouldApplyFilter, result) {
    result.passingFilterItems = (isRowMatchingFilterItems == null ? void 0 : isRowMatchingFilterItems(row, shouldApplyFilter)) ?? null;
    result.passingQuickFilterValues = (isRowMatchingQuickFilter == null ? void 0 : isRowMatchingQuickFilter(row, shouldApplyFilter)) ?? null;
  };
};
var isNotNull = (result) => result != null;
var filterModelItems = (cache, filterValueGetter, apiRef, items) => {
  if (!cache.cleanedFilterItems) {
    cache.cleanedFilterItems = items.filter((item) => getFilterCallbackFromItem(item, filterValueGetter, apiRef) !== null);
  }
  return cache.cleanedFilterItems;
};
var passFilterLogic = (allFilterItemResults, allQuickFilterResults, filterModel, filterValueGetter, apiRef, cache) => {
  const cleanedFilterItems = filterModelItems(cache, filterValueGetter, apiRef, filterModel.items);
  const cleanedFilterItemResults = allFilterItemResults.filter(isNotNull);
  const cleanedQuickFilterResults = allQuickFilterResults.filter(isNotNull);
  if (cleanedFilterItemResults.length > 0) {
    const filterItemPredicate = (item) => {
      return cleanedFilterItemResults.some((filterItemResult) => filterItemResult[item.id]);
    };
    const logicOperator = filterModel.logicOperator ?? getDefaultGridFilterModel().logicOperator;
    if (logicOperator === GridLogicOperator.And) {
      const passesAllFilters = cleanedFilterItems.every(filterItemPredicate);
      if (!passesAllFilters) {
        return false;
      }
    } else {
      const passesSomeFilters = cleanedFilterItems.some(filterItemPredicate);
      if (!passesSomeFilters) {
        return false;
      }
    }
  }
  if (cleanedQuickFilterResults.length > 0 && filterModel.quickFilterValues != null) {
    const quickFilterValuePredicate = (value) => {
      return cleanedQuickFilterResults.some((quickFilterValueResult) => quickFilterValueResult[value]);
    };
    const quickFilterLogicOperator = filterModel.quickFilterLogicOperator ?? getDefaultGridFilterModel().quickFilterLogicOperator;
    if (quickFilterLogicOperator === GridLogicOperator.And) {
      const passesAllQuickFilterValues = filterModel.quickFilterValues.every(quickFilterValuePredicate);
      if (!passesAllQuickFilterValues) {
        return false;
      }
    } else {
      const passesSomeQuickFilterValues = filterModel.quickFilterValues.some(quickFilterValuePredicate);
      if (!passesSomeQuickFilterValues) {
        return false;
      }
    }
  }
  return true;
};

// node_modules/@mui/x-data-grid/esm/colDef/gridStringOperators.js
var getGridStringQuickFilterFn = (value) => {
  if (!value) {
    return null;
  }
  const filterRegex = new RegExp(escapeRegExp(value), "i");
  return (_, row, column, apiRef) => {
    let columnValue = apiRef.current.getRowFormattedValue(row, column);
    if (apiRef.current.ignoreDiacritics) {
      columnValue = removeDiacritics(columnValue);
    }
    return columnValue != null ? filterRegex.test(columnValue.toString()) : false;
  };
};
var createContainsFilterFn = (disableTrim, negate) => (filterItem) => {
  if (!filterItem.value) {
    return null;
  }
  const trimmedValue = disableTrim ? filterItem.value : filterItem.value.trim();
  const filterRegex = new RegExp(escapeRegExp(trimmedValue), "i");
  return (value) => {
    if (value == null) {
      return negate;
    }
    const matches = filterRegex.test(String(value));
    return negate ? !matches : matches;
  };
};
var createEqualityFilterFn = (disableTrim, negate) => (filterItem) => {
  if (!filterItem.value) {
    return null;
  }
  const trimmedValue = disableTrim ? filterItem.value : filterItem.value.trim();
  const collator4 = new Intl.Collator(void 0, {
    sensitivity: "base",
    usage: "search"
  });
  return (value) => {
    if (value == null) {
      return negate;
    }
    const isEqual = collator4.compare(trimmedValue, value.toString()) === 0;
    return negate ? !isEqual : isEqual;
  };
};
var createEmptyFilterFn = (negate) => () => {
  return (value) => {
    const isEmpty3 = value === "" || value == null;
    return negate ? !isEmpty3 : isEmpty3;
  };
};
var getGridStringOperators = (disableTrim = false) => [{
  value: "contains",
  getApplyFilterFn: createContainsFilterFn(disableTrim, false),
  InputComponent: GridFilterInputValue
}, {
  value: "doesNotContain",
  getApplyFilterFn: createContainsFilterFn(disableTrim, true),
  InputComponent: GridFilterInputValue
}, {
  value: "equals",
  getApplyFilterFn: createEqualityFilterFn(disableTrim, false),
  InputComponent: GridFilterInputValue
}, {
  value: "doesNotEqual",
  getApplyFilterFn: createEqualityFilterFn(disableTrim, true),
  InputComponent: GridFilterInputValue
}, {
  value: "startsWith",
  getApplyFilterFn: (filterItem) => {
    if (!filterItem.value) {
      return null;
    }
    const filterItemValue = disableTrim ? filterItem.value : filterItem.value.trim();
    const filterRegex = new RegExp(`^${escapeRegExp(filterItemValue)}.*$`, "i");
    return (value) => {
      return value != null ? filterRegex.test(value.toString()) : false;
    };
  },
  InputComponent: GridFilterInputValue
}, {
  value: "endsWith",
  getApplyFilterFn: (filterItem) => {
    if (!filterItem.value) {
      return null;
    }
    const filterItemValue = disableTrim ? filterItem.value : filterItem.value.trim();
    const filterRegex = new RegExp(`.*${escapeRegExp(filterItemValue)}$`, "i");
    return (value) => {
      return value != null ? filterRegex.test(value.toString()) : false;
    };
  },
  InputComponent: GridFilterInputValue
}, {
  value: "isEmpty",
  getApplyFilterFn: createEmptyFilterFn(false),
  requiresFilterValue: false
}, {
  value: "isNotEmpty",
  getApplyFilterFn: createEmptyFilterFn(true),
  requiresFilterValue: false
}, {
  value: "isAnyOf",
  getApplyFilterFn: (filterItem) => {
    if (!Array.isArray(filterItem.value) || filterItem.value.length === 0) {
      return null;
    }
    const filterItemValue = disableTrim ? filterItem.value : filterItem.value.map((val) => val.trim());
    const collator4 = new Intl.Collator(void 0, {
      sensitivity: "base",
      usage: "search"
    });
    return (value) => value != null ? filterItemValue.some((filterValue) => {
      return collator4.compare(filterValue, value.toString() || "") === 0;
    }) : false;
  },
  InputComponent: GridFilterInputMultipleValue
}];

// node_modules/@mui/x-data-grid/esm/colDef/gridStringColDef.js
var GRID_STRING_COL_DEF = {
  width: 100,
  minWidth: 50,
  maxWidth: Infinity,
  hideable: true,
  sortable: true,
  resizable: true,
  filterable: true,
  groupable: true,
  pinnable: true,
  // @ts-ignore
  aggregable: true,
  chartable: true,
  editable: false,
  sortComparator: gridStringOrNumberComparator,
  type: "string",
  align: "left",
  filterOperators: getGridStringOperators(),
  renderEditCell: renderEditInputCell,
  getApplyQuickFilterFn: getGridStringQuickFilterFn
};

// node_modules/@mui/x-data-grid/esm/components/cell/GridActionsCell.js
var React22 = __toESM(require_react(), 1);
var import_prop_types4 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/menu/GridMenu.js
var React20 = __toESM(require_react(), 1);
var import_prop_types3 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/HTMLElementType/HTMLElementType.js
function HTMLElementType(props, propName, componentName, location, propFullName) {
  if (false) {
    return null;
  }
  const propValue = props[propName];
  const safePropName = propFullName || propName;
  if (propValue == null) {
    return null;
  }
  if (propValue && propValue.nodeType !== 1) {
    return new Error(`Invalid ${location} \`${safePropName}\` supplied to \`${componentName}\`. Expected an HTMLElement.`);
  }
  return null;
}

// node_modules/@mui/x-data-grid/esm/utils/keyboardUtils.js
function isPrintableKey(event) {
  return event.key.length === 1 && !event.ctrlKey && !event.metaKey;
}
var isNavigationKey = (key) => key.indexOf("Arrow") === 0 || key.indexOf("Page") === 0 || key === " " || key === "Home" || key === "End";
var isKeyboardEvent = (event) => !!event.key;
var isHideMenuKey = (key) => key === "Tab" || key === "Escape";
function isPasteShortcut(event) {
  return (event.ctrlKey || event.metaKey) && // We can't use event.code === 'KeyV' as event.code assumes a QWERTY keyboard layout,
  // for example, it would be another letter on a Dvorak physical keyboard.
  // We can't use event.key === 'v' as event.key is not stable with key modifiers and keyboard layouts,
  // for example, it would be ה on a Hebrew keyboard layout.
  // https://github.com/w3c/uievents/issues/377 could be a long-term solution
  String.fromCharCode(event.keyCode) === "V" && !event.shiftKey && !event.altKey;
}
function isCopyShortcut(event) {
  return (event.ctrlKey || event.metaKey) && String.fromCharCode(event.keyCode) === "C" && !event.shiftKey && !event.altKey;
}
function isUndoShortcut(event) {
  return (event.ctrlKey || event.metaKey) && String.fromCharCode(event.keyCode) === "Z" && !event.shiftKey && !event.altKey;
}
function isRedoShortcut(event) {
  return (event.ctrlKey || event.metaKey) && (String.fromCharCode(event.keyCode) === "Z" && event.shiftKey || String.fromCharCode(event.keyCode) === "Y" && !event.shiftKey) && !event.altKey;
}

// node_modules/@mui/x-data-grid/esm/utils/css/context.js
var React19 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridConfiguration.js
var React18 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/GridConfigurationContext.js
var React17 = __toESM(require_react(), 1);
var GridConfigurationContext = React17.createContext(void 0);
if (true) GridConfigurationContext.displayName = "GridConfigurationContext";

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridConfiguration.js
var useGridConfiguration = () => {
  const configuration4 = React18.useContext(GridConfigurationContext);
  if (configuration4 === void 0) {
    throw new Error(["MUI X: Could not find the Data Grid configuration context.", "It looks like you rendered your component outside of a DataGrid, DataGridPro or DataGridPremium parent component.", "This can also happen if you are bundling multiple versions of the Data Grid."].join("\n"));
  }
  return configuration4;
};

// node_modules/@mui/x-data-grid/esm/utils/css/context.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var CLASSNAME_PREFIX = "MuiDataGridVariables";
var CSSVariablesContext = React19.createContext({
  className: "unset",
  tag: (0, import_jsx_runtime4.jsx)("style", {
    href: "/unset"
  })
});
if (true) CSSVariablesContext.displayName = "CSSVariablesContext";
function useCSSVariablesClass() {
  return React19.useContext(CSSVariablesContext).className;
}
function useCSSVariablesContext() {
  return React19.useContext(CSSVariablesContext);
}
function GridPortalWrapper({
  children: children2
}) {
  const className = useCSSVariablesClass();
  return (0, import_jsx_runtime4.jsx)("div", {
    className,
    children: children2
  });
}
function GridCSSVariablesContext(props) {
  const config = useGridConfiguration();
  const rootProps = useGridRootProps();
  const description = config.hooks.useCSSVariables();
  const context = React19.useMemo(() => {
    const className = `${CLASSNAME_PREFIX}-${description.id}`;
    const cssString = `.${className}{${variablesToString(description.variables)}}`;
    const tag = (0, import_jsx_runtime4.jsx)("style", {
      href: `/${className}`,
      nonce: rootProps.nonce,
      children: cssString
    });
    return {
      className,
      tag
    };
  }, [rootProps.nonce, description]);
  return (0, import_jsx_runtime4.jsx)(CSSVariablesContext.Provider, {
    value: context,
    children: props.children
  });
}
function variablesToString(variables) {
  let output = "";
  for (const key in variables) {
    if (Object.hasOwn(variables, key) && variables[key] !== void 0) {
      output += `${key}:${variables[key]};`;
    }
  }
  return output;
}

// node_modules/@mui/x-data-grid/esm/components/menu/GridMenu.js
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var _excluded4 = ["open", "target", "onClose", "children", "position", "className", "onExited"];
var useUtilityClasses2 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["menu"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridMenuRoot = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "Menu"
})({
  zIndex: vars.zIndex.menu,
  [`& .${gridClasses.menuList}`]: {
    outline: 0
  }
});
function GridMenu(props) {
  var _a;
  const {
    open,
    target,
    onClose,
    children: children2,
    position,
    className,
    onExited
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded4);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses2(rootProps);
  const variablesClass = useCSSVariablesClass();
  const savedFocusRef = React20.useRef(null);
  useEnhancedEffect_default(() => {
    var _a2, _b;
    if (open) {
      savedFocusRef.current = document.activeElement instanceof HTMLElement ? document.activeElement : null;
    } else {
      (_b = (_a2 = savedFocusRef.current) == null ? void 0 : _a2.focus) == null ? void 0 : _b.call(_a2);
      savedFocusRef.current = null;
    }
  }, [open]);
  React20.useEffect(() => {
    const eventName = open ? "menuOpen" : "menuClose";
    apiRef.current.publishEvent(eventName, {
      target
    });
  }, [apiRef, open, target]);
  const handleClickAway = (event) => {
    if (event.target && (target === event.target || (target == null ? void 0 : target.contains(event.target)))) {
      return;
    }
    onClose(event);
  };
  const handleKeyDown = (event) => {
    if (isHideMenuKey(event.key)) {
      onClose(event);
    }
  };
  return (0, import_jsx_runtime5.jsx)(GridMenuRoot, _extends({
    as: rootProps.slots.basePopper,
    className: clsx_default(classes2.root, className, variablesClass),
    ownerState: rootProps,
    open,
    target,
    transition: true,
    placement: position,
    onClickAway: handleClickAway,
    onExited,
    clickAwayMouseEvent: "onMouseDown",
    onKeyDown: handleKeyDown
  }, other, (_a = rootProps.slotProps) == null ? void 0 : _a.basePopper, {
    children: children2
  }));
}
true ? GridMenu.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  children: import_prop_types3.default.node,
  className: import_prop_types3.default.string,
  onClose: import_prop_types3.default.func.isRequired,
  onExited: import_prop_types3.default.func,
  open: import_prop_types3.default.bool.isRequired,
  position: import_prop_types3.default.oneOf(["bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
  target: HTMLElementType
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/cell/GridActionsCellItem.js
var React21 = __toESM(require_react(), 1);
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var _excluded5 = ["label", "icon", "showInMenu", "onClick"];
var _excluded22 = ["label", "icon", "showInMenu", "onClick", "closeMenuOnClick", "closeMenu"];
var GridActionsCellItem = forwardRef2((props, ref) => {
  var _a;
  const rootProps = useGridRootProps();
  if (!props.showInMenu) {
    const {
      label: label2,
      icon: icon2,
      onClick: onClick2
    } = props, other2 = _objectWithoutPropertiesLoose(props, _excluded5);
    const handleClick2 = (event) => {
      onClick2 == null ? void 0 : onClick2(event);
    };
    return (0, import_jsx_runtime6.jsx)(rootProps.slots.baseIconButton, _extends({
      size: "small",
      role: "menuitem",
      "aria-label": label2
    }, other2, {
      onClick: handleClick2
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
      ref,
      children: React21.cloneElement(icon2, {
        fontSize: "small"
      })
    }));
  }
  const {
    label,
    icon,
    onClick,
    closeMenuOnClick = true,
    closeMenu
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded22);
  const handleClick = (event) => {
    onClick == null ? void 0 : onClick(event);
    if (closeMenuOnClick) {
      closeMenu == null ? void 0 : closeMenu();
    }
  };
  return (0, import_jsx_runtime6.jsx)(rootProps.slots.baseMenuItem, _extends({
    ref
  }, other, {
    onClick: handleClick,
    iconStart: icon,
    children: label
  }));
});
if (true) GridActionsCellItem.displayName = "GridActionsCellItem";

// node_modules/@mui/x-data-grid/esm/components/cell/GridActionsCell.js
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
var _excluded6 = ["api", "colDef", "id", "hasFocus", "isEditable", "field", "value", "formattedValue", "row", "rowNode", "cellMode", "tabIndex", "position", "onMenuOpen", "onMenuClose", "children", "suppressChildrenValidation"];
var hasActions = (colDef) => typeof colDef.getActions === "function";
function GridActionsCell(props) {
  var _a;
  const {
    id,
    hasFocus,
    tabIndex,
    position = "bottom-end",
    onMenuOpen,
    onMenuClose,
    children: children2,
    suppressChildrenValidation
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded6);
  const [focusedButtonIndex, setFocusedButtonIndex] = React22.useState(-1);
  const [open, setOpen] = React22.useState(false);
  const apiRef = useGridApiContext();
  const rootRef = React22.useRef(null);
  const buttonRef = React22.useRef(null);
  const ignoreCallToFocus = React22.useRef(false);
  const touchRippleRefs = React22.useRef({});
  const isRtl = useRtl();
  const menuId = useId();
  const buttonId = useId();
  const rootProps = useGridRootProps();
  const rowParams = apiRef.current.getRowParams(id);
  const actions = [];
  React22.Children.forEach(children2, (child) => {
    if (React22.isValidElement(child)) {
      if (child.type === React22.Fragment) {
        React22.Children.forEach(child.props.children, (fragChild) => {
          if (React22.isValidElement(fragChild)) {
            actions.push(fragChild);
          }
        });
      } else if (child.type === GridActionsCellItem || suppressChildrenValidation) {
        actions.push(child);
      } else {
        const childType = typeof child.type === "function" ? child.type.name : child.type;
        warnOnce(`MUI X: Invalid child type in \`GridActionsCell\`. Expected \`GridActionsCellItem\` or \`React.Fragment\`, got \`${childType}\`.
If this is intentional, you can suppress this warning by passing the \`suppressChildrenValidation\` prop to \`GridActionsCell\`.`, "error");
      }
    }
  });
  const iconButtons = actions.filter((option) => !option.props.showInMenu);
  const menuButtons = actions.filter((option) => option.props.showInMenu);
  const numberOfButtons = iconButtons.length + (menuButtons.length ? 1 : 0);
  React22.useLayoutEffect(() => {
    if (!hasFocus) {
      Object.entries(touchRippleRefs.current).forEach(([index, ref]) => {
        ref == null ? void 0 : ref.stop({}, () => {
          delete touchRippleRefs.current[index];
        });
      });
    }
  }, [hasFocus]);
  React22.useEffect(() => {
    if (focusedButtonIndex < 0 || !rootRef.current) {
      return;
    }
    if (focusedButtonIndex >= rootRef.current.children.length) {
      return;
    }
    const child = rootRef.current.children[focusedButtonIndex];
    child.focus({
      preventScroll: true
    });
  }, [focusedButtonIndex]);
  const firstFocusableButtonIndex = actions.findIndex((o) => !o.props.disabled);
  React22.useEffect(() => {
    if (hasFocus && focusedButtonIndex === -1) {
      setFocusedButtonIndex(firstFocusableButtonIndex);
    }
    if (!hasFocus) {
      setFocusedButtonIndex(-1);
      ignoreCallToFocus.current = false;
    }
  }, [hasFocus, focusedButtonIndex, firstFocusableButtonIndex]);
  React22.useEffect(() => {
    if (focusedButtonIndex >= numberOfButtons) {
      setFocusedButtonIndex(numberOfButtons - 1);
    }
  }, [focusedButtonIndex, numberOfButtons]);
  const showMenu = (event) => {
    if (onMenuOpen && !onMenuOpen(rowParams, event)) {
      return;
    }
    setOpen(true);
    setFocusedButtonIndex(numberOfButtons - 1);
    ignoreCallToFocus.current = true;
  };
  const hideMenu = (event) => {
    if (onMenuClose && !onMenuClose(rowParams, event)) {
      return;
    }
    setOpen(false);
  };
  const toggleMenu = (event) => {
    event.stopPropagation();
    event.preventDefault();
    if (open) {
      hideMenu(event);
    } else {
      showMenu(event);
    }
  };
  const handleTouchRippleRef = (index) => (instance) => {
    touchRippleRefs.current[index] = instance;
  };
  const handleButtonClick = (index, onClick) => (event) => {
    setFocusedButtonIndex(index);
    ignoreCallToFocus.current = true;
    if (onClick) {
      onClick(event);
    }
  };
  const handleRootKeyDown = (event) => {
    if (numberOfButtons <= 1) {
      return;
    }
    const getNewIndex = (index, direction) => {
      var _a2;
      if (index < 0 || index > actions.length) {
        return index;
      }
      const rtlMod = isRtl ? -1 : 1;
      const indexMod = (direction === "left" ? -1 : 1) * rtlMod;
      return ((_a2 = actions[index + indexMod]) == null ? void 0 : _a2.props.disabled) ? getNewIndex(index + indexMod, direction) : index + indexMod;
    };
    let newIndex = focusedButtonIndex;
    if (event.key === "ArrowRight") {
      newIndex = getNewIndex(focusedButtonIndex, "right");
    } else if (event.key === "ArrowLeft") {
      newIndex = getNewIndex(focusedButtonIndex, "left");
    }
    if (newIndex < 0 || newIndex >= numberOfButtons) {
      return;
    }
    if (newIndex !== focusedButtonIndex) {
      event.preventDefault();
      event.stopPropagation();
      setFocusedButtonIndex(newIndex);
    }
  };
  const attributes = numberOfButtons > 0 ? {
    role: "menu",
    onKeyDown: handleRootKeyDown
  } : void 0;
  return (0, import_jsx_runtime7.jsxs)("div", _extends({
    ref: rootRef,
    tabIndex: -1,
    className: gridClasses.actionsCell
  }, attributes, other, {
    children: [iconButtons.map((button, index) => React22.cloneElement(button, {
      key: index,
      touchRippleRef: handleTouchRippleRef(index),
      onClick: handleButtonClick(index, button.props.onClick),
      tabIndex: focusedButtonIndex === index ? tabIndex : -1
    })), menuButtons.length > 0 && buttonId && (0, import_jsx_runtime7.jsx)(rootProps.slots.baseIconButton, _extends({
      ref: buttonRef,
      id: buttonId,
      "aria-label": apiRef.current.getLocaleText("actionsCellMore"),
      "aria-haspopup": "menu",
      "aria-expanded": open,
      "aria-controls": open ? menuId : void 0,
      role: "menuitem",
      size: "small",
      onClick: toggleMenu,
      touchRippleRef: handleTouchRippleRef(buttonId),
      tabIndex: focusedButtonIndex === iconButtons.length ? tabIndex : -1
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
      children: (0, import_jsx_runtime7.jsx)(rootProps.slots.moreActionsIcon, {
        fontSize: "small"
      })
    })), menuButtons.length > 0 && (0, import_jsx_runtime7.jsx)(GridMenu, {
      open,
      target: buttonRef.current,
      position,
      onClose: hideMenu,
      children: (0, import_jsx_runtime7.jsx)(rootProps.slots.baseMenuList, {
        id: menuId,
        className: gridClasses.menuList,
        "aria-labelledby": buttonId,
        autoFocusItem: true,
        children: menuButtons.map((button, index) => React22.cloneElement(button, {
          key: index,
          closeMenu: hideMenu
        }))
      })
    })]
  }));
}
true ? GridActionsCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  api: import_prop_types4.default.object,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types4.default.oneOf(["edit", "view"]).isRequired,
  children: import_prop_types4.default.node.isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types4.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types4.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types4.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types4.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types4.default.oneOfType([import_prop_types4.default.number, import_prop_types4.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types4.default.bool,
  position: import_prop_types4.default.oneOf(["bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types4.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types4.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types4.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types4.default.any
} : void 0;
function GridActionsCellWrapper(props) {
  const {
    colDef,
    id
  } = props;
  const apiRef = useGridApiContext();
  if (!hasActions(colDef)) {
    throw new Error("MUI X: Missing the `getActions` property in the `GridColDef`.");
  }
  const actions = colDef.getActions(apiRef.current.getRowParams(id));
  return (0, import_jsx_runtime7.jsx)(GridActionsCell, _extends({
    suppressChildrenValidation: true
  }, props, {
    children: actions
  }));
}
var renderActionsCell = (params) => (0, import_jsx_runtime7.jsx)(GridActionsCellWrapper, _extends({}, params));
if (true) renderActionsCell.displayName = "renderActionsCell";

// node_modules/@mui/x-data-grid/esm/colDef/gridActionsColDef.js
var GRID_ACTIONS_COLUMN_TYPE = "actions";
var GRID_ACTIONS_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  sortable: false,
  filterable: false,
  // @ts-ignore
  aggregable: false,
  chartable: false,
  width: 100,
  display: "flex",
  align: "center",
  headerAlign: "center",
  headerName: "",
  disableColumnMenu: true,
  disableExport: true,
  renderCell: renderActionsCell,
  getApplyQuickFilterFn: () => null
});

// node_modules/@mui/x-data-grid/esm/components/cell/GridBooleanCell.js
var React24 = __toESM(require_react(), 1);
var import_prop_types5 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/internals/constants.js
var GRID_TREE_DATA_GROUPING_FIELD = "__tree_data_group__";
var GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD = "__row_group_by_columns_group__";
var GRID_DETAIL_PANEL_TOGGLE_FIELD = "__detail_panel_toggle__";
var PinnedColumnPosition = (function(PinnedColumnPosition2) {
  PinnedColumnPosition2[PinnedColumnPosition2["NONE"] = 0] = "NONE";
  PinnedColumnPosition2[PinnedColumnPosition2["LEFT"] = 1] = "LEFT";
  PinnedColumnPosition2[PinnedColumnPosition2["RIGHT"] = 2] = "RIGHT";
  PinnedColumnPosition2[PinnedColumnPosition2["VIRTUAL"] = 3] = "VIRTUAL";
  return PinnedColumnPosition2;
})({});

// node_modules/@mui/x-data-grid/esm/components/cell/GridFooterCell.js
var React23 = __toESM(require_react(), 1);
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var _excluded7 = ["formattedValue", "colDef", "cellMode", "row", "api", "id", "value", "rowNode", "field", "hasFocus", "tabIndex", "isEditable"];
var GridFooterCellRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FooterCell"
})({
  fontWeight: vars.typography.fontWeight.medium,
  color: vars.colors.foreground.accent
});
var useUtilityClasses3 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["footerCell"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridFooterCellRaw(props) {
  const {
    formattedValue
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded7);
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses3(ownerState);
  return (0, import_jsx_runtime8.jsx)(GridFooterCellRoot, _extends({
    ownerState,
    className: classes2.root
  }, other, {
    children: formattedValue
  }));
}
var GridFooterCell = React23.memo(GridFooterCellRaw);
if (true) GridFooterCell.displayName = "GridFooterCell";

// node_modules/@mui/x-data-grid/esm/components/cell/GridBooleanCell.js
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
var _excluded8 = ["id", "value", "formattedValue", "api", "field", "row", "rowNode", "colDef", "cellMode", "isEditable", "hasFocus", "tabIndex", "hideDescendantCount"];
var useUtilityClasses4 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["booleanCell"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridBooleanCellRaw(props) {
  const {
    value,
    rowNode
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded8);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses4(ownerState);
  const maxDepth = useGridSelector(apiRef, gridRowMaximumTreeDepthSelector);
  const isServerSideRowGroupingRow = (
    // @ts-expect-error - Access tree data prop
    maxDepth > 0 && rowNode.type === "group" && rootProps.treeData === false
  );
  const Icon = React24.useMemo(() => value ? rootProps.slots.booleanCellTrueIcon : rootProps.slots.booleanCellFalseIcon, [rootProps.slots.booleanCellFalseIcon, rootProps.slots.booleanCellTrueIcon, value]);
  if (isServerSideRowGroupingRow && value === void 0) {
    return null;
  }
  return (0, import_jsx_runtime9.jsx)(Icon, _extends({
    fontSize: "small",
    className: classes2.root,
    titleAccess: apiRef.current.getLocaleText(value ? "booleanCellTrueLabel" : "booleanCellFalseLabel"),
    "data-value": Boolean(value)
  }, other));
}
true ? GridBooleanCellRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types5.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types5.default.oneOf(["edit", "view"]).isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types5.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types5.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types5.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types5.default.bool.isRequired,
  hideDescendantCount: import_prop_types5.default.bool,
  /**
   * The grid row id.
   */
  id: import_prop_types5.default.oneOfType([import_prop_types5.default.number, import_prop_types5.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types5.default.bool,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types5.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types5.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types5.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types5.default.any
} : void 0;
var GridBooleanCell = React24.memo(GridBooleanCellRaw);
if (true) GridBooleanCell.displayName = "GridBooleanCell";
var renderBooleanCell = (params) => {
  if (params.field !== GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD && isAutogeneratedRowNode(params.rowNode)) {
    const aggregationMetaData = params.aggregation;
    if (!aggregationMetaData) {
      return "";
    }
    if (aggregationMetaData.position === "footer") {
      return (0, import_jsx_runtime9.jsx)(GridFooterCell, _extends({}, params));
    }
    return params.formattedValue;
  }
  return (0, import_jsx_runtime9.jsx)(GridBooleanCell, _extends({}, params));
};
if (true) renderBooleanCell.displayName = "renderBooleanCell";

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditBooleanCell.js
var React25 = __toESM(require_react(), 1);
var import_prop_types6 = __toESM(require_prop_types(), 1);
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
var _excluded9 = ["id", "value", "formattedValue", "api", "field", "row", "rowNode", "colDef", "cellMode", "isEditable", "tabIndex", "className", "hasFocus", "isValidating", "isProcessingProps", "error", "onValueChange"];
var useUtilityClasses5 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["editBooleanCell"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridEditBooleanCell(props) {
  var _a;
  const {
    id: idProp,
    value,
    field,
    className,
    hasFocus,
    onValueChange
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded9);
  const apiRef = useGridApiContext();
  const inputRef = React25.useRef(null);
  const id = useId();
  const [valueState, setValueState] = React25.useState(value);
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses5(ownerState);
  const handleChange = React25.useCallback(async (event) => {
    const newValue = event.target.checked;
    if (onValueChange) {
      await onValueChange(event, newValue);
    }
    setValueState(newValue);
    await apiRef.current.setEditCellValue({
      id: idProp,
      field,
      value: newValue
    }, event);
  }, [apiRef, field, idProp, onValueChange]);
  React25.useEffect(() => {
    setValueState(value);
  }, [value]);
  useEnhancedEffect_default(() => {
    if (hasFocus) {
      inputRef.current.focus();
    }
  }, [hasFocus]);
  return (0, import_jsx_runtime10.jsx)("label", _extends({
    htmlFor: id,
    className: clsx_default(classes2.root, className)
  }, other, {
    children: (0, import_jsx_runtime10.jsx)(rootProps.slots.baseCheckbox, _extends({
      id,
      inputRef,
      checked: Boolean(valueState),
      onChange: handleChange,
      size: "small"
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseCheckbox))
  }));
}
true ? GridEditBooleanCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types6.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types6.default.oneOf(["edit", "view"]).isRequired,
  changeReason: import_prop_types6.default.oneOf(["debouncedSetEditCellValue", "setEditCellValue"]),
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types6.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types6.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types6.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types6.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types6.default.oneOfType([import_prop_types6.default.number, import_prop_types6.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types6.default.bool,
  isProcessingProps: import_prop_types6.default.bool,
  isValidating: import_prop_types6.default.bool,
  /**
   * Callback called when the value is changed by the user.
   * @param {React.ChangeEvent<HTMLInputElement>} event The event source of the callback.
   * @param {boolean} newValue The value that is going to be passed to `apiRef.current.setEditCellValue`.
   * @returns {Promise<void> | void} A promise to be awaited before calling `apiRef.current.setEditCellValue`
   */
  onValueChange: import_prop_types6.default.func,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types6.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types6.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types6.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types6.default.any
} : void 0;
var renderEditBooleanCell = (params) => (0, import_jsx_runtime10.jsx)(GridEditBooleanCell, _extends({}, params));
if (true) renderEditBooleanCell.displayName = "renderEditBooleanCell";

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputBoolean.js
var React26 = __toESM(require_react(), 1);
var import_prop_types8 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/refType/refType.js
var import_prop_types7 = __toESM(require_prop_types(), 1);
var refType = import_prop_types7.default.oneOfType([import_prop_types7.default.func, import_prop_types7.default.object]);
var refType_default = refType;

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputBoolean.js
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var _excluded10 = ["item", "applyValue", "apiRef", "focusElementRef", "isFilterActive", "headerFilterMenu", "clearButton", "tabIndex", "slotProps"];
function GridFilterInputBoolean(props) {
  var _a, _b;
  const {
    item,
    applyValue,
    apiRef,
    focusElementRef,
    headerFilterMenu,
    clearButton,
    tabIndex,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded10);
  const [filterValueState, setFilterValueState] = React26.useState(sanitizeFilterItemValue2(item.value));
  const rootProps = useGridRootProps();
  const labelId = useId();
  const selectId = useId();
  const baseSelectProps = ((_a = rootProps.slotProps) == null ? void 0 : _a.baseSelect) || {};
  const isSelectNative = baseSelectProps.native ?? false;
  const baseSelectOptionProps = ((_b = rootProps.slotProps) == null ? void 0 : _b.baseSelectOption) || {};
  const onFilterChange = React26.useCallback((event) => {
    const value = sanitizeFilterItemValue2(event.target.value);
    setFilterValueState(value);
    applyValue(_extends({}, item, {
      value
    }));
  }, [applyValue, item]);
  React26.useEffect(() => {
    setFilterValueState(sanitizeFilterItemValue2(item.value));
  }, [item.value]);
  const label = (slotProps == null ? void 0 : slotProps.root.label) ?? apiRef.current.getLocaleText("filterPanelInputLabel");
  const rootSlotProps = slotProps == null ? void 0 : slotProps.root.slotProps;
  return (0, import_jsx_runtime11.jsxs)(React26.Fragment, {
    children: [(0, import_jsx_runtime11.jsxs)(rootProps.slots.baseSelect, _extends({
      fullWidth: true,
      labelId,
      id: selectId,
      label,
      value: filterValueState === void 0 ? "" : String(filterValueState),
      onChange: onFilterChange,
      native: isSelectNative,
      slotProps: {
        htmlInput: _extends({
          ref: focusElementRef,
          tabIndex
        }, rootSlotProps == null ? void 0 : rootSlotProps.htmlInput)
      }
    }, baseSelectProps, other, slotProps == null ? void 0 : slotProps.root, {
      children: [(0, import_jsx_runtime11.jsx)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isSelectNative,
        value: "",
        children: apiRef.current.getLocaleText("filterValueAny")
      })), (0, import_jsx_runtime11.jsx)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isSelectNative,
        value: "true",
        children: apiRef.current.getLocaleText("filterValueTrue")
      })), (0, import_jsx_runtime11.jsx)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isSelectNative,
        value: "false",
        children: apiRef.current.getLocaleText("filterValueFalse")
      }))]
    })), headerFilterMenu, clearButton]
  });
}
function sanitizeFilterItemValue2(value) {
  if (String(value).toLowerCase() === "true") {
    return true;
  }
  if (String(value).toLowerCase() === "false") {
    return false;
  }
  return void 0;
}
true ? GridFilterInputBoolean.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types8.default.shape({
    current: import_prop_types8.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types8.default.func.isRequired,
  className: import_prop_types8.default.string,
  clearButton: import_prop_types8.default.node,
  disabled: import_prop_types8.default.bool,
  focusElementRef: refType_default,
  headerFilterMenu: import_prop_types8.default.node,
  inputRef: import_prop_types8.default.oneOfType([import_prop_types8.default.func, import_prop_types8.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types8.default.bool,
  item: import_prop_types8.default.shape({
    field: import_prop_types8.default.string.isRequired,
    id: import_prop_types8.default.oneOfType([import_prop_types8.default.number, import_prop_types8.default.string]),
    operator: import_prop_types8.default.string.isRequired,
    value: import_prop_types8.default.any
  }).isRequired,
  onBlur: import_prop_types8.default.func,
  onFocus: import_prop_types8.default.func,
  slotProps: import_prop_types8.default.object,
  tabIndex: import_prop_types8.default.number
} : void 0;

// node_modules/@mui/x-data-grid/esm/colDef/gridBooleanOperators.js
var getGridBooleanOperators = () => [{
  value: "is",
  getApplyFilterFn: (filterItem) => {
    const sanitizedValue = sanitizeFilterItemValue2(filterItem.value);
    if (sanitizedValue === void 0) {
      return null;
    }
    return (value) => Boolean(value) === sanitizedValue;
  },
  InputComponent: GridFilterInputBoolean
}];

// node_modules/@mui/x-data-grid/esm/colDef/gridBooleanColDef.js
var gridBooleanFormatter = (value, row, column, apiRef) => {
  return value ? apiRef.current.getLocaleText("booleanCellTrueLabel") : apiRef.current.getLocaleText("booleanCellFalseLabel");
};
var stringToBoolean = (value) => {
  switch (value.toLowerCase().trim()) {
    case "true":
    case "yes":
    case "1":
      return true;
    case "false":
    case "no":
    case "0":
    case "null":
    case "undefined":
      return false;
    default:
      return void 0;
  }
};
var GRID_BOOLEAN_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "boolean",
  display: "flex",
  align: "center",
  headerAlign: "center",
  renderCell: renderBooleanCell,
  renderEditCell: renderEditBooleanCell,
  sortComparator: gridNumberComparator,
  valueFormatter: gridBooleanFormatter,
  filterOperators: getGridBooleanOperators(),
  getApplyQuickFilterFn: () => null,
  // @ts-ignore
  chartable: false,
  // @ts-ignore
  pastedValueParser: (value) => stringToBoolean(value)
});

// node_modules/@mui/x-data-grid/esm/components/columnSelection/GridCellCheckboxRenderer.js
var React27 = __toESM(require_react(), 1);
var import_prop_types9 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-internals/esm/isObjectEmpty/isObjectEmpty.js
function isObjectEmpty(object) {
  for (const _ in object) {
    return false;
  }
  return true;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/sorting/gridSortingSelector.js
var gridSortingStateSelector = createRootSelector((state) => state.sorting);
var gridSortedRowIdsSelector = createSelector3(gridSortingStateSelector, (sortingState) => sortingState.sortedRows);
var gridSortedRowEntriesSelector = createSelectorMemoized2(gridSortedRowIdsSelector, gridRowsLookupSelector, gridRowTreeSelector, (sortedIds, idRowsLookup, rowTree) => sortedIds.reduce((acc, id) => {
  const model = idRowsLookup[id];
  if (model) {
    acc.push({
      id,
      model
    });
  } else {
    const rowNode = rowTree[id];
    if (rowNode && isAutogeneratedRowNode(rowNode)) {
      acc.push({
        id,
        model: {
          [GRID_ID_AUTOGENERATED]: id
        }
      });
    }
  }
  return acc;
}, []));
var gridSortModelSelector = createSelector3(gridSortingStateSelector, (sorting) => sorting.sortModel);
var gridSortColumnLookupSelector = createSelectorMemoized2(gridSortModelSelector, (sortModel) => {
  const result = sortModel.reduce((res, sortItem, index) => {
    res[sortItem.field] = {
      sortDirection: sortItem.sort,
      sortIndex: sortModel.length > 1 ? index + 1 : void 0
    };
    return res;
  }, {});
  return result;
});
var gridSortedRowIndexLookupSelector = createSelectorMemoized2(gridSortedRowIdsSelector, (sortedIds) => {
  return sortedIds.reduce((acc, id, index) => {
    acc[id] = index;
    return acc;
  }, /* @__PURE__ */ Object.create(null));
});

// node_modules/@mui/x-data-grid/esm/hooks/features/filter/gridFilterSelector.js
var gridFilterStateSelector = createRootSelector((state) => state.filter);
var gridFilterModelSelector = createSelector3(gridFilterStateSelector, (filterState) => filterState.filterModel);
var gridQuickFilterValuesSelector = createSelector3(gridFilterModelSelector, (filterModel) => filterModel.quickFilterValues);
var gridVisibleRowsLookupSelector = createRootSelector((state) => state.visibleRowsLookup);
var gridFilteredRowsLookupSelector = createSelector3(gridFilterStateSelector, (filterState) => filterState.filteredRowsLookup);
var gridFilteredChildrenCountLookupSelector = createSelector3(gridFilterStateSelector, (filterState) => filterState.filteredChildrenCountLookup);
var gridFilteredDescendantCountLookupSelector = createSelector3(gridFilterStateSelector, (filterState) => filterState.filteredDescendantCountLookup);
var gridExpandedSortedRowEntriesSelector = createSelectorMemoized2(gridVisibleRowsLookupSelector, gridSortedRowEntriesSelector, (visibleRowsLookup, sortedRows) => {
  if (isObjectEmpty(visibleRowsLookup)) {
    return sortedRows;
  }
  return sortedRows.filter((row) => visibleRowsLookup[row.id] !== false);
});
var gridExpandedSortedRowIdsSelector = createSelectorMemoized2(gridExpandedSortedRowEntriesSelector, (visibleSortedRowEntries) => visibleSortedRowEntries.map((row) => row.id));
var gridFilteredSortedRowEntriesSelector = createSelectorMemoized2(gridFilteredRowsLookupSelector, gridSortedRowEntriesSelector, (filteredRowsLookup, sortedRows) => isObjectEmpty(filteredRowsLookup) ? sortedRows : sortedRows.filter((row) => filteredRowsLookup[row.id] !== false));
var gridFilteredSortedRowIdsSelector = createSelectorMemoized2(gridFilteredSortedRowEntriesSelector, (filteredSortedRowEntries) => filteredSortedRowEntries.map((row) => row.id));
var gridExpandedSortedRowTreeLevelPositionLookupSelector = createSelectorMemoized2(gridExpandedSortedRowIdsSelector, gridRowTreeSelector, (visibleSortedRowIds, rowTree) => {
  const depthPositionCounter = {};
  let lastDepth = 0;
  return visibleSortedRowIds.reduce((acc, rowId) => {
    const rowNode = rowTree[rowId];
    if (!depthPositionCounter[rowNode.depth]) {
      depthPositionCounter[rowNode.depth] = 0;
    }
    if (rowNode.depth > lastDepth) {
      depthPositionCounter[rowNode.depth] = 0;
    }
    lastDepth = rowNode.depth;
    depthPositionCounter[rowNode.depth] += 1;
    acc[rowId] = depthPositionCounter[rowNode.depth];
    return acc;
  }, {});
});
var gridFilteredSortedDepthRowEntriesSelector = createSelectorMemoized2(gridFilteredSortedRowEntriesSelector, gridRowTreeSelector, gridRowMaximumTreeDepthSelector, (sortedRows, rowTree, rowTreeDepth) => {
  if (rowTreeDepth < 2) {
    return [sortedRows];
  }
  return sortedRows.reduce((acc, row) => {
    var _a;
    const depth = (_a = rowTree[row.id]) == null ? void 0 : _a.depth;
    if (depth === void 0) {
      return acc;
    }
    if (!acc[depth]) {
      acc[depth] = [];
    }
    acc[depth].push(row);
    return acc;
  }, [[]]);
});
var gridFilteredSortedTopLevelRowEntriesSelector = createSelector3(gridFilteredSortedDepthRowEntriesSelector, (filteredSortedDepthRows) => filteredSortedDepthRows[0] ?? []);
var gridExpandedRowCountSelector = createSelector3(gridExpandedSortedRowEntriesSelector, (visibleSortedRows) => visibleSortedRows.length);
var gridFilteredTopLevelRowCountSelector = createSelector3(gridFilteredSortedTopLevelRowEntriesSelector, (visibleSortedTopLevelRows) => visibleSortedTopLevelRows.length);
var gridFilteredRowCountSelector = createSelector3(gridFilteredSortedRowEntriesSelector, (filteredSortedRowEntries) => filteredSortedRowEntries.length);
var gridFilteredDescendantRowCountSelector = createSelector3(gridFilteredRowCountSelector, gridFilteredTopLevelRowCountSelector, (totalRowCount, topLevelRowCount) => totalRowCount - topLevelRowCount);
var gridFilterActiveItemsSelector = createSelectorMemoized2(gridFilterModelSelector, gridColumnLookupSelector, (filterModel, columnLookup) => {
  var _a;
  return (_a = filterModel.items) == null ? void 0 : _a.filter((item) => {
    var _a2, _b;
    if (!item.field) {
      return false;
    }
    const column = columnLookup[item.field];
    if (!(column == null ? void 0 : column.filterOperators) || ((_a2 = column == null ? void 0 : column.filterOperators) == null ? void 0 : _a2.length) === 0) {
      return false;
    }
    const filterOperator = column.filterOperators.find((operator) => operator.value === item.operator);
    if (!filterOperator) {
      return false;
    }
    return !filterOperator.InputComponent || item.value != null && ((_b = item.value) == null ? void 0 : _b.toString()) !== "";
  });
});
var gridFilterActiveItemsLookupSelector = createSelectorMemoized2(gridFilterActiveItemsSelector, (activeFilters) => {
  const result = activeFilters.reduce((res, filterItem) => {
    if (!res[filterItem.field]) {
      res[filterItem.field] = [filterItem];
    } else {
      res[filterItem.field].push(filterItem);
    }
    return res;
  }, {});
  return result;
});
var gridExpandedSortedRowIndexLookupSelector = createSelectorMemoized2(gridExpandedSortedRowIdsSelector, (expandedSortedIds) => {
  return expandedSortedIds.reduce((acc, id, index) => {
    acc[id] = index;
    return acc;
  }, /* @__PURE__ */ Object.create(null));
});

// node_modules/@mui/x-data-grid/esm/hooks/features/rowSelection/gridRowSelectionSelector.js
var gridRowSelectionStateSelector = createRootSelector((state) => state.rowSelection);
var gridRowSelectionManagerSelector = createSelectorMemoized2(gridRowSelectionStateSelector, createRowSelectionManager);
var gridRowSelectionCountSelector = createSelector3(gridRowSelectionStateSelector, gridFilteredRowCountSelector, (selection, filteredRowCount) => {
  if (selection.type === "include") {
    return selection.ids.size;
  }
  return filteredRowCount - selection.ids.size;
});
var gridRowSelectionIdsSelector = createSelectorMemoized2(gridRowSelectionStateSelector, gridRowsLookupSelector, gridDataRowIdsSelector, (selectionModel, rowsLookup, rowIds) => {
  const map = /* @__PURE__ */ new Map();
  if (selectionModel.type === "include") {
    for (const id of selectionModel.ids) {
      map.set(id, rowsLookup[id]);
    }
  } else {
    for (let i2 = 0; i2 < rowIds.length; i2 += 1) {
      const id = rowIds[i2];
      if (!selectionModel.ids.has(id)) {
        map.set(id, rowsLookup[id]);
      }
    }
  }
  return map;
});

// node_modules/@mui/x-data-grid/esm/hooks/core/gridPropsSelectors.js
var gridRowIdSelector = createRootSelector((state, row) => {
  if (GRID_ID_AUTOGENERATED in row) {
    return row[GRID_ID_AUTOGENERATED];
  }
  return state.props.getRowId ? state.props.getRowId(row) : row.id;
});
var gridRowSelectableSelector = createRootSelector((state) => {
  return state.props.isRowSelectable;
});

// node_modules/@mui/x-data-grid/esm/hooks/features/rowSelection/utils.js
var ROW_SELECTION_PROPAGATION_DEFAULT = {
  parents: true,
  descendants: true
};
function getGridRowGroupSelectableDescendants(apiRef, groupId) {
  var _a;
  const rowTree = gridRowTreeSelector(apiRef);
  const sortedRowIds = gridSortedRowIdsSelector(apiRef);
  const filteredRowsLookup = gridFilteredRowsLookupSelector(apiRef);
  const groupNode = rowTree[groupId];
  if (!groupNode || groupNode.type !== "group") {
    return [];
  }
  const descendants = [];
  const startIndex = sortedRowIds.findIndex((id) => id === groupId) + 1;
  for (let index = startIndex; index < sortedRowIds.length && ((_a = rowTree[sortedRowIds[index]]) == null ? void 0 : _a.depth) > groupNode.depth; index += 1) {
    const id = sortedRowIds[index];
    if (filteredRowsLookup[id] !== false && apiRef.current.isRowSelectable(id)) {
      descendants.push(id);
    }
  }
  return descendants;
}
var checkboxPropsSelector = createSelector3(gridColumnDefinitionsSelector, gridRowTreeSelector, gridFilteredRowsLookupSelector, gridRowSelectionManagerSelector, gridRowsLookupSelector, gridRowSelectableSelector, (columns, rowTree, filteredRowsLookup, rowSelectionManager, rowsLookup, isRowSelectable, {
  groupId,
  autoSelectParents
}) => {
  const groupNode = rowTree[groupId];
  const rowParams = {
    id: groupId,
    row: rowsLookup[groupId],
    columns
  };
  let isSelectable = true;
  if (typeof isRowSelectable === "function" && rowsLookup[groupId]) {
    isSelectable = isRowSelectable(rowParams);
  }
  if (!groupNode || groupNode.type !== "group" || rowSelectionManager.has(groupId)) {
    return {
      isIndeterminate: false,
      isChecked: rowSelectionManager.has(groupId),
      isSelectable
    };
  }
  let hasSelectedDescendant = false;
  let hasUnSelectedDescendant = false;
  const traverseDescendants = (itemToTraverseId) => {
    if (filteredRowsLookup[itemToTraverseId] === false || // Perf: Skip checking the rest of the descendants if we already
    // know that there is a selected and an unselected descendant
    hasSelectedDescendant && hasUnSelectedDescendant) {
      return;
    }
    const node = rowTree[itemToTraverseId];
    if ((node == null ? void 0 : node.type) === "group") {
      node.children.forEach(traverseDescendants);
    }
    const descendantRowParams = {
      id: itemToTraverseId,
      row: rowsLookup[itemToTraverseId],
      columns
    };
    const rowIsSelectable = typeof isRowSelectable === "function" && rowsLookup[itemToTraverseId] ? isRowSelectable(descendantRowParams) : true;
    if (rowIsSelectable) {
      if (rowSelectionManager.has(itemToTraverseId)) {
        hasSelectedDescendant = true;
      } else {
        hasUnSelectedDescendant = true;
      }
    }
  };
  traverseDescendants(groupId);
  return {
    isIndeterminate: hasSelectedDescendant && hasUnSelectedDescendant,
    isChecked: autoSelectParents ? hasSelectedDescendant && !hasUnSelectedDescendant : false,
    isSelectable
  };
});
function isMultipleRowSelectionEnabled(props) {
  if (props.signature === GridSignature.DataGrid) {
    return props.checkboxSelection && props.disableMultipleRowSelection !== true;
  }
  return !props.disableMultipleRowSelection;
}
var getRowNodeParents = (tree, id) => {
  const parents = [];
  let parent = id;
  while (parent != null && parent !== GRID_ROOT_GROUP_ID) {
    const node = tree[parent];
    if (!node) {
      return parents;
    }
    parents.push(parent);
    parent = node.parent;
  }
  return parents;
};
var getFilteredRowNodeSiblings = (tree, filteredRows, id) => {
  const node = tree[id];
  if (!node) {
    return [];
  }
  const parent = node.parent;
  if (parent == null) {
    return [];
  }
  const parentNode = tree[parent];
  return parentNode.children.filter((childId) => childId !== id && filteredRows[childId] !== false);
};
var findRowsToSelect = (apiRef, tree, selectedRow, autoSelectDescendants, autoSelectParents, addRow, rowSelectionManager = gridRowSelectionManagerSelector(apiRef)) => {
  const filteredRows = gridFilteredRowsLookupSelector(apiRef);
  const selectedDescendants = /* @__PURE__ */ new Set([]);
  if (!autoSelectDescendants && !autoSelectParents || filteredRows[selectedRow] === false) {
    return;
  }
  if (autoSelectDescendants) {
    const rowNode = tree[selectedRow];
    if ((rowNode == null ? void 0 : rowNode.type) === "group") {
      const descendants = getGridRowGroupSelectableDescendants(apiRef, selectedRow);
      descendants.forEach((rowId) => {
        addRow(rowId);
        selectedDescendants.add(rowId);
      });
    }
  }
  if (autoSelectParents) {
    const checkAllDescendantsSelected = (rowId) => {
      const node = tree[rowId];
      if (!node) {
        return false;
      }
      if (node.type !== "group") {
        if (apiRef.current.isRowSelectable(rowId)) {
          return rowSelectionManager.has(rowId) || selectedDescendants.has(rowId);
        }
        return true;
      }
      if (rowSelectionManager.has(rowId) || selectedDescendants.has(rowId)) {
        return true;
      }
      return node.children.every(checkAllDescendantsSelected);
    };
    const traverseParents = (rowId) => {
      const siblings = getFilteredRowNodeSiblings(tree, filteredRows, rowId);
      const allSelectableSiblingsSelected = siblings.every((siblingId) => {
        if (!apiRef.current.isRowSelectable(siblingId)) {
          return true;
        }
        return checkAllDescendantsSelected(siblingId);
      });
      if (siblings.length === 0 || allSelectableSiblingsSelected) {
        const rowNode2 = tree[rowId];
        const parent = rowNode2 == null ? void 0 : rowNode2.parent;
        if (parent != null && parent !== GRID_ROOT_GROUP_ID && apiRef.current.isRowSelectable(parent)) {
          addRow(parent);
          selectedDescendants.add(parent);
          traverseParents(parent);
        }
      }
    };
    const rowNode = tree[selectedRow];
    if (!rowNode || rowNode.parent === GRID_ROOT_GROUP_ID) {
      return;
    }
    traverseParents(selectedRow);
  }
};
var findRowsToDeselect = (apiRef, tree, deselectedRow, autoSelectDescendants, autoSelectParents, removeRow) => {
  const rowSelectionManager = gridRowSelectionManagerSelector(apiRef);
  if (!autoSelectParents && !autoSelectDescendants) {
    return;
  }
  if (autoSelectParents) {
    const allParents = getRowNodeParents(tree, deselectedRow);
    allParents.forEach((parent) => {
      const isSelected = rowSelectionManager.has(parent);
      if (isSelected) {
        removeRow(parent);
      }
    });
  }
  if (autoSelectDescendants) {
    const rowNode = tree[deselectedRow];
    if ((rowNode == null ? void 0 : rowNode.type) === "group") {
      const descendants = getGridRowGroupSelectableDescendants(apiRef, deselectedRow);
      descendants.forEach((descendant) => {
        removeRow(descendant);
      });
    }
  }
};

// node_modules/@mui/x-data-grid/esm/components/columnSelection/GridCellCheckboxRenderer.js
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
var _excluded11 = ["field", "id", "formattedValue", "row", "rowNode", "colDef", "isEditable", "cellMode", "hasFocus", "tabIndex", "api"];
var useUtilityClasses6 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["checkboxInput"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridCellCheckboxForwardRef = forwardRef2(function GridCellCheckboxRenderer(props, ref) {
  var _a, _b;
  const {
    field,
    id,
    rowNode,
    tabIndex
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded11);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses6(ownerState);
  const {
    isIndeterminate,
    isChecked,
    isSelectable
  } = useGridSelector(apiRef, checkboxPropsSelector, {
    groupId: id,
    autoSelectParents: ((_a = rootProps.rowSelectionPropagation) == null ? void 0 : _a.parents) ?? false
  }, objectShallowCompare);
  const disabled = !isSelectable;
  const handleChange = (event) => {
    if (disabled) {
      return;
    }
    const params = {
      value: event.target.checked,
      id
    };
    apiRef.current.publishEvent("rowSelectionCheckboxChange", params, event);
  };
  React27.useLayoutEffect(() => {
    if (tabIndex === 0 && !disabled) {
      const element = apiRef.current.getCellElement(id, field);
      if (element) {
        element.tabIndex = -1;
      }
    }
  }, [apiRef, tabIndex, id, field, disabled]);
  const handleKeyDown = useEventCallback_default((event) => {
    if (event.key === " ") {
      event.stopPropagation();
    }
    if (disabled) {
      return;
    }
  });
  const handleClick = useEventCallback_default((event) => {
    if (disabled) {
      event.preventDefault();
      return;
    }
  });
  const handleMouseDown = useEventCallback_default(() => {
    if (disabled) {
      return;
    }
  });
  if (rowNode.type === "footer" || rowNode.type === "pinnedRow") {
    return null;
  }
  const label = apiRef.current.getLocaleText(isChecked && !isIndeterminate ? "checkboxSelectionUnselectRow" : "checkboxSelectionSelectRow");
  return (0, import_jsx_runtime12.jsx)(rootProps.slots.baseCheckbox, _extends({
    tabIndex: disabled ? -1 : tabIndex,
    checked: isChecked && !isIndeterminate,
    onChange: handleChange,
    onClick: handleClick,
    onMouseDown: handleMouseDown,
    className: clsx_default(classes2.root, disabled && "Mui-disabled"),
    disabled,
    material: {
      disableRipple: disabled
    },
    slotProps: {
      htmlInput: {
        "aria-disabled": disabled || void 0,
        "aria-label": label,
        name: "select_row"
      }
    },
    onKeyDown: handleKeyDown,
    indeterminate: isIndeterminate
  }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseCheckbox, other, {
    ref
  }));
});
if (true) GridCellCheckboxForwardRef.displayName = "GridCellCheckboxForwardRef";
true ? GridCellCheckboxForwardRef.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types9.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types9.default.oneOf(["edit", "view"]).isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types9.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types9.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types9.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types9.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types9.default.oneOfType([import_prop_types9.default.number, import_prop_types9.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types9.default.bool,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types9.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types9.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types9.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types9.default.any
} : void 0;
var GridCellCheckboxRenderer2 = GridCellCheckboxForwardRef;

// node_modules/@mui/x-data-grid/esm/components/columnSelection/GridHeaderCheckbox.js
var React28 = __toESM(require_react(), 1);
var import_prop_types10 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/focus/gridFocusStateSelector.js
var gridFocusStateSelector = createRootSelector((state) => state.focus);
var gridFocusCellSelector = createSelector3(gridFocusStateSelector, (focusState) => focusState.cell);
var gridFocusColumnHeaderSelector = createSelector3(gridFocusStateSelector, (focusState) => focusState.columnHeader);
var gridFocusColumnHeaderFilterSelector = createSelector3(gridFocusStateSelector, (focusState) => focusState.columnHeaderFilter);
var gridFocusColumnGroupHeaderSelector = createSelector3(gridFocusStateSelector, (focusState) => focusState.columnGroupHeader);
var gridTabIndexStateSelector = createRootSelector((state) => state.tabIndex);
var gridTabIndexCellSelector = createSelector3(gridTabIndexStateSelector, (state) => state.cell);
var gridTabIndexColumnHeaderSelector = createSelector3(gridTabIndexStateSelector, (state) => state.columnHeader);
var gridTabIndexColumnHeaderFilterSelector = createSelector3(gridTabIndexStateSelector, (state) => state.columnHeaderFilter);
var gridTabIndexColumnGroupHeaderSelector = createSelector3(gridTabIndexStateSelector, (state) => state.columnGroupHeader);

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/gridPaginationUtils.js
var MAX_PAGE_SIZE = 100;
var defaultPageSize = (autoPageSize) => autoPageSize ? 0 : 100;
var getPageCount = (rowCount, pageSize2, page) => {
  if (pageSize2 > 0 && rowCount > 0) {
    return Math.ceil(rowCount / pageSize2);
  }
  if (rowCount === -1) {
    return page + 2;
  }
  return 0;
};
var getDefaultGridPaginationModel = (autoPageSize) => ({
  page: 0,
  pageSize: autoPageSize ? 0 : 100
});
var getValidPage = (page, pageCount = 0) => {
  if (pageCount === 0) {
    return page;
  }
  return Math.max(Math.min(page, pageCount - 1), 0);
};
var throwIfPageSizeExceedsTheLimit = (pageSize2, signatureProp) => {
  if (signatureProp === GridSignature.DataGrid && pageSize2 > MAX_PAGE_SIZE) {
    throw new Error(["MUI X: `pageSize` cannot exceed 100 in the MIT version of the DataGrid.", "You need to upgrade to DataGridPro or DataGridPremium component to unlock this feature."].join("\n"));
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/gridPaginationSelector.js
var ALL_RESULTS_PAGE_VALUE = -1;
var gridPaginationSelector = createRootSelector((state) => state.pagination);
var gridPaginationEnabledClientSideSelector = createSelector3(gridPaginationSelector, (pagination) => pagination.enabled && pagination.paginationMode === "client");
var gridPaginationModelSelector = createSelector3(gridPaginationSelector, (pagination) => pagination.paginationModel);
var gridPaginationRowCountSelector = createSelector3(gridPaginationSelector, (pagination) => pagination.rowCount);
var gridPaginationMetaSelector = createSelector3(gridPaginationSelector, (pagination) => pagination.meta);
var gridPageSelector = createSelector3(gridPaginationModelSelector, (paginationModel) => paginationModel.page);
var gridPageSizeSelector = createSelector3(gridPaginationModelSelector, (paginationModel) => paginationModel.pageSize);
var gridPageCountSelector = createSelector3(gridPaginationModelSelector, gridPaginationRowCountSelector, (paginationModel, rowCount) => getPageCount(rowCount, paginationModel.pageSize, paginationModel.page));
var gridPaginationRowRangeSelector = createSelectorMemoized2(gridPaginationEnabledClientSideSelector, gridPaginationModelSelector, gridRowTreeSelector, gridRowMaximumTreeDepthSelector, gridExpandedSortedRowEntriesSelector, gridFilteredSortedTopLevelRowEntriesSelector, (clientSidePaginationEnabled, paginationModel, rowTree, rowTreeDepth, visibleSortedRowEntries, visibleSortedTopLevelRowEntries) => {
  var _a;
  if (!clientSidePaginationEnabled) {
    return null;
  }
  if (!visibleSortedRowEntries || visibleSortedRowEntries.length === 0) {
    return null;
  }
  const visibleTopLevelRowCount = visibleSortedTopLevelRowEntries.length;
  const topLevelFirstRowIndex = Math.min(paginationModel.pageSize * paginationModel.page, visibleTopLevelRowCount - 1);
  const topLevelLastRowIndex = paginationModel.pageSize === ALL_RESULTS_PAGE_VALUE ? visibleTopLevelRowCount - 1 : Math.min(topLevelFirstRowIndex + paginationModel.pageSize - 1, visibleTopLevelRowCount - 1);
  if (topLevelFirstRowIndex === -1 || topLevelLastRowIndex === -1) {
    return null;
  }
  if (rowTreeDepth < 2) {
    return {
      firstRowIndex: topLevelFirstRowIndex,
      lastRowIndex: topLevelLastRowIndex
    };
  }
  const topLevelFirstRow = visibleSortedTopLevelRowEntries[topLevelFirstRowIndex];
  const topLevelRowsInCurrentPageCount = topLevelLastRowIndex - topLevelFirstRowIndex + 1;
  const firstRowIndex = visibleSortedRowEntries.findIndex((row) => row.id === topLevelFirstRow.id);
  let lastRowIndex = firstRowIndex;
  let topLevelRowAdded = 0;
  while (lastRowIndex < visibleSortedRowEntries.length && topLevelRowAdded <= topLevelRowsInCurrentPageCount) {
    const row = visibleSortedRowEntries[lastRowIndex];
    const depth = (_a = rowTree[row.id]) == null ? void 0 : _a.depth;
    if (depth === void 0) {
      lastRowIndex += 1;
    } else {
      if (topLevelRowAdded < topLevelRowsInCurrentPageCount || depth > 0) {
        lastRowIndex += 1;
      }
      if (depth === 0) {
        topLevelRowAdded += 1;
      }
    }
  }
  return {
    firstRowIndex,
    lastRowIndex: lastRowIndex - 1
  };
});
var gridPaginatedVisibleSortedGridRowEntriesSelector = createSelectorMemoized2(gridExpandedSortedRowEntriesSelector, gridPaginationRowRangeSelector, (visibleSortedRowEntries, paginationRange) => {
  if (!paginationRange) {
    return [];
  }
  return visibleSortedRowEntries.slice(paginationRange.firstRowIndex, paginationRange.lastRowIndex + 1);
});
var gridPaginatedVisibleSortedGridRowIdsSelector = createSelectorMemoized2(gridExpandedSortedRowIdsSelector, gridPaginationRowRangeSelector, (visibleSortedRowIds, paginationRange) => {
  if (!paginationRange) {
    return [];
  }
  return visibleSortedRowIds.slice(paginationRange.firstRowIndex, paginationRange.lastRowIndex + 1);
});
var gridVisibleRowsSelector = createSelectorMemoized2(gridPaginationEnabledClientSideSelector, gridPaginationRowRangeSelector, gridPaginatedVisibleSortedGridRowEntriesSelector, gridExpandedSortedRowEntriesSelector, (clientPaginationEnabled, paginationRowRange, paginationRows, expandedSortedRowEntries) => {
  if (clientPaginationEnabled) {
    return {
      rows: paginationRows,
      range: paginationRowRange,
      rowIdToIndexMap: paginationRows.reduce((lookup, row, index) => {
        lookup.set(row.id, index);
        return lookup;
      }, /* @__PURE__ */ new Map())
    };
  }
  return {
    rows: expandedSortedRowEntries,
    range: expandedSortedRowEntries.length === 0 ? null : {
      firstRowIndex: 0,
      lastRowIndex: expandedSortedRowEntries.length - 1
    },
    rowIdToIndexMap: expandedSortedRowEntries.reduce((lookup, row, index) => {
      lookup.set(row.id, index);
      return lookup;
    }, /* @__PURE__ */ new Map())
  };
});

// node_modules/@mui/x-data-grid/esm/components/columnSelection/GridHeaderCheckbox.js
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var _excluded12 = ["field", "colDef"];
var useUtilityClasses7 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["checkboxInput"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridHeaderCheckbox = forwardRef2(function GridHeaderCheckbox2(props, ref) {
  var _a;
  const other = _objectWithoutPropertiesLoose(props, _excluded12);
  const [, forceUpdate] = React28.useState(false);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses7(ownerState);
  const tabIndexState = useGridSelector(apiRef, gridTabIndexColumnHeaderSelector);
  const selection = useGridSelector(apiRef, gridRowSelectionStateSelector);
  const visibleRowIds = useGridSelector(apiRef, gridExpandedSortedRowIdsSelector);
  const paginatedVisibleRowIds = useGridSelector(apiRef, gridPaginatedVisibleSortedGridRowIdsSelector);
  const filteredSelection = React28.useMemo(() => {
    const isRowSelectable = rootProps.isRowSelectable;
    if (typeof isRowSelectable !== "function") {
      return selection;
    }
    if (selection.type === "exclude") {
      return selection;
    }
    const selectionModel = {
      type: "include",
      ids: /* @__PURE__ */ new Set()
    };
    for (const id of selection.ids) {
      if (rootProps.keepNonExistentRowsSelected) {
        selectionModel.ids.add(id);
      }
      if (!apiRef.current.getRow(id)) {
        continue;
      }
      if (isRowSelectable(apiRef.current.getRowParams(id))) {
        selectionModel.ids.add(id);
      }
    }
    return selectionModel;
  }, [apiRef, rootProps.isRowSelectable, rootProps.keepNonExistentRowsSelected, selection]);
  const selectionCandidates = React28.useMemo(() => {
    const rowIds = !rootProps.pagination || !rootProps.checkboxSelectionVisibleOnly || rootProps.paginationMode === "server" ? visibleRowIds : paginatedVisibleRowIds;
    const candidates = /* @__PURE__ */ new Set();
    for (let i2 = 0; i2 < rowIds.length; i2 += 1) {
      const id = rowIds[i2];
      if (!apiRef.current.getRow(id)) {
        continue;
      }
      if (apiRef.current.isRowSelectable(id)) {
        candidates.add(id);
      }
    }
    return candidates;
  }, [apiRef, rootProps.pagination, rootProps.paginationMode, rootProps.checkboxSelectionVisibleOnly, paginatedVisibleRowIds, visibleRowIds]);
  const currentSelectionSize = React28.useMemo(() => {
    const selectionManager = createRowSelectionManager(filteredSelection);
    let size = 0;
    for (const id of selectionCandidates) {
      if (selectionManager.has(id)) {
        size += 1;
      }
    }
    return size;
  }, [filteredSelection, selectionCandidates]);
  const isIndeterminate = React28.useMemo(() => {
    if (currentSelectionSize === 0) {
      return false;
    }
    const selectionManager = createRowSelectionManager(filteredSelection);
    for (const rowId of selectionCandidates) {
      if (!selectionManager.has(rowId)) {
        return true;
      }
    }
    return false;
  }, [currentSelectionSize, filteredSelection, selectionCandidates]);
  const isChecked = currentSelectionSize > 0;
  const handleChange = (event) => {
    const params = {
      value: event.target.checked
    };
    apiRef.current.publishEvent("headerSelectionCheckboxChange", params);
  };
  const multipleSelectionEnabled = isMultipleRowSelectionEnabled(rootProps);
  const tabIndex = tabIndexState !== null && tabIndexState.field === props.field && multipleSelectionEnabled ? 0 : -1;
  useEnhancedEffect_default(() => {
    const element = apiRef.current.getColumnHeaderElement(props.field);
    if (tabIndex === 0 && element && multipleSelectionEnabled) {
      element.tabIndex = -1;
    }
  }, [tabIndex, apiRef, props.field, multipleSelectionEnabled]);
  const handleKeyDown = React28.useCallback((event) => {
    if (event.key === " ") {
      apiRef.current.publishEvent("headerSelectionCheckboxChange", {
        value: !isChecked
      });
    }
  }, [apiRef, isChecked]);
  const handleSelectionChange = React28.useCallback(() => {
    forceUpdate((p) => !p);
  }, []);
  React28.useEffect(() => {
    return apiRef.current.subscribeEvent("rowSelectionChange", handleSelectionChange);
  }, [apiRef, handleSelectionChange]);
  const label = apiRef.current.getLocaleText(isChecked && !isIndeterminate ? "checkboxSelectionUnselectAllRows" : "checkboxSelectionSelectAllRows");
  return (0, import_jsx_runtime13.jsx)(rootProps.slots.baseCheckbox, _extends({
    indeterminate: isIndeterminate,
    checked: isChecked && !isIndeterminate,
    onChange: handleChange,
    className: classes2.root,
    slotProps: {
      htmlInput: {
        "aria-label": label,
        name: "select_all_rows"
      }
    },
    tabIndex,
    onKeyDown: handleKeyDown,
    disabled: !multipleSelectionEnabled
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseCheckbox, other, {
    ref
  }));
});
if (true) GridHeaderCheckbox.displayName = "GridHeaderCheckbox";
true ? GridHeaderCheckbox.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The column of the current header component.
   */
  colDef: import_prop_types10.default.object.isRequired,
  /**
   * The column field of the column that triggered the event
   */
  field: import_prop_types10.default.string.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/colDef/gridCheckboxSelectionColDef.js
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var GRID_CHECKBOX_SELECTION_FIELD = "__check__";
var GRID_CHECKBOX_SELECTION_COL_DEF = _extends({}, GRID_BOOLEAN_COL_DEF, {
  type: "custom",
  field: GRID_CHECKBOX_SELECTION_FIELD,
  width: 50,
  resizable: false,
  sortable: false,
  filterable: false,
  // @ts-ignore
  aggregable: false,
  chartable: false,
  disableColumnMenu: true,
  disableReorder: true,
  disableExport: true,
  getApplyQuickFilterFn: () => null,
  display: "flex",
  valueGetter: (value, row, column, apiRef) => {
    const rowId = gridRowIdSelector(apiRef, row);
    return apiRef.current.isRowSelected(rowId);
  },
  rowSpanValueGetter: (_, row, column, apiRef) => gridRowIdSelector(apiRef, row),
  renderHeader: (params) => (0, import_jsx_runtime14.jsx)(GridHeaderCheckbox, _extends({}, params)),
  renderCell: (params) => (0, import_jsx_runtime14.jsx)(GridCellCheckboxRenderer2, _extends({}, params))
});

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputDate.js
var React29 = __toESM(require_react(), 1);
var import_prop_types11 = __toESM(require_prop_types(), 1);
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
var _excluded13 = ["item", "applyValue", "type", "apiRef", "focusElementRef", "slotProps", "isFilterActive", "headerFilterMenu", "clearButton", "tabIndex", "disabled"];
function convertFilterItemValueToInputValue(itemValue, inputType) {
  if (itemValue == null) {
    return "";
  }
  const dateCopy = new Date(itemValue);
  if (Number.isNaN(dateCopy.getTime())) {
    return "";
  }
  if (inputType === "date") {
    return dateCopy.toISOString().substring(0, 10);
  }
  if (inputType === "datetime-local") {
    dateCopy.setMinutes(dateCopy.getMinutes() - dateCopy.getTimezoneOffset());
    return dateCopy.toISOString().substring(0, 19);
  }
  return dateCopy.toISOString().substring(0, 10);
}
function GridFilterInputDate(props) {
  var _a;
  const {
    item,
    applyValue,
    type,
    apiRef,
    focusElementRef,
    slotProps,
    headerFilterMenu,
    clearButton,
    tabIndex,
    disabled
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded13);
  const rootSlotProps = slotProps == null ? void 0 : slotProps.root.slotProps;
  const filterTimeout = useTimeout();
  const [filterValueState, setFilterValueState] = React29.useState(() => convertFilterItemValueToInputValue(item.value, type));
  const [applying, setIsApplying] = React29.useState(false);
  const id = useId();
  const rootProps = useGridRootProps();
  const onFilterChange = React29.useCallback((event) => {
    filterTimeout.clear();
    const value = event.target.value;
    setFilterValueState(value);
    setIsApplying(true);
    filterTimeout.start(rootProps.filterDebounceMs, () => {
      const date = new Date(value);
      applyValue(_extends({}, item, {
        value: Number.isNaN(date.getTime()) ? void 0 : date
      }));
      setIsApplying(false);
    });
  }, [applyValue, item, rootProps.filterDebounceMs, filterTimeout]);
  React29.useEffect(() => {
    const value = convertFilterItemValueToInputValue(item.value, type);
    setFilterValueState(value);
  }, [item.value, type]);
  return (0, import_jsx_runtime15.jsxs)(React29.Fragment, {
    children: [(0, import_jsx_runtime15.jsx)(rootProps.slots.baseTextField, _extends({
      fullWidth: true,
      id,
      label: apiRef.current.getLocaleText("filterPanelInputLabel"),
      placeholder: apiRef.current.getLocaleText("filterPanelInputPlaceholder"),
      value: filterValueState,
      onChange: onFilterChange,
      type: type || "text",
      disabled,
      inputRef: focusElementRef,
      slotProps: _extends({}, rootSlotProps, {
        input: _extends({
          endAdornment: applying ? (0, import_jsx_runtime15.jsx)(rootProps.slots.loadIcon, {
            fontSize: "small",
            color: "action"
          }) : null
        }, rootSlotProps == null ? void 0 : rootSlotProps.input),
        htmlInput: _extends({
          max: type === "datetime-local" ? "9999-12-31T23:59" : "9999-12-31",
          tabIndex
        }, rootSlotProps == null ? void 0 : rootSlotProps.htmlInput)
      })
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, other, slotProps == null ? void 0 : slotProps.root)), headerFilterMenu, clearButton]
  });
}
true ? GridFilterInputDate.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types11.default.shape({
    current: import_prop_types11.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types11.default.func.isRequired,
  className: import_prop_types11.default.string,
  clearButton: import_prop_types11.default.node,
  disabled: import_prop_types11.default.bool,
  focusElementRef: import_prop_types11.default.oneOfType([import_prop_types11.default.func, import_prop_types11.default.object]),
  headerFilterMenu: import_prop_types11.default.node,
  inputRef: import_prop_types11.default.oneOfType([import_prop_types11.default.func, import_prop_types11.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types11.default.bool,
  item: import_prop_types11.default.shape({
    field: import_prop_types11.default.string.isRequired,
    id: import_prop_types11.default.oneOfType([import_prop_types11.default.number, import_prop_types11.default.string]),
    operator: import_prop_types11.default.string.isRequired,
    value: import_prop_types11.default.any
  }).isRequired,
  onBlur: import_prop_types11.default.func,
  onFocus: import_prop_types11.default.func,
  slotProps: import_prop_types11.default.object,
  tabIndex: import_prop_types11.default.number,
  type: import_prop_types11.default.oneOf(["date", "datetime-local"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/colDef/gridDateOperators.js
function buildApplyFilterFn(filterItem, compareFn, showTime, keepRawComparison) {
  if (!filterItem.value) {
    return null;
  }
  const date = new Date(filterItem.value);
  if (showTime) {
    date.setSeconds(0, 0);
  } else {
    date.setMinutes(date.getMinutes() + date.getTimezoneOffset());
    date.setHours(0, 0, 0, 0);
  }
  const time = date.getTime();
  return (value) => {
    if (!value) {
      return false;
    }
    if (keepRawComparison) {
      return compareFn(value.getTime(), time);
    }
    const dateCopy = new Date(value);
    if (showTime) {
      dateCopy.setSeconds(0, 0);
    } else {
      dateCopy.setHours(0, 0, 0, 0);
    }
    return compareFn(dateCopy.getTime(), time);
  };
}
var getGridDateOperators = (showTime) => [{
  value: "is",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 === value2, showTime);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "not",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 !== value2, showTime);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "after",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 > value2, showTime, showTime);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "onOrAfter",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 >= value2, showTime, showTime);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "before",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 < value2, showTime, true);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "onOrBefore",
  getApplyFilterFn: (filterItem) => {
    return buildApplyFilterFn(filterItem, (value1, value2) => value1 <= value2, showTime, showTime);
  },
  InputComponent: GridFilterInputDate,
  InputComponentProps: {
    type: showTime ? "datetime-local" : "date"
  }
}, {
  value: "isEmpty",
  getApplyFilterFn: () => {
    return (value) => {
      return value == null;
    };
  },
  requiresFilterValue: false
}, {
  value: "isNotEmpty",
  getApplyFilterFn: () => {
    return (value) => {
      return value != null;
    };
  },
  requiresFilterValue: false
}];

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditDateCell.js
var React30 = __toESM(require_react(), 1);
var import_prop_types12 = __toESM(require_prop_types(), 1);
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
var _excluded14 = ["id", "value", "formattedValue", "api", "field", "row", "rowNode", "colDef", "cellMode", "isEditable", "tabIndex", "hasFocus", "inputProps", "isValidating", "isProcessingProps", "onValueChange", "slotProps"];
var StyledInputBase = styled_default2(NotRendered, {
  slot: "internal"
})({
  fontSize: "inherit"
});
var useUtilityClasses8 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["editInputCell"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridEditDateCell(props) {
  var _a, _b;
  const {
    id,
    value: valueProp,
    field,
    colDef,
    hasFocus,
    onValueChange,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded14);
  const isDateTime = colDef.type === "dateTime";
  const apiRef = useGridApiContext();
  const inputRef = React30.useRef(null);
  const valueTransformed = React30.useMemo(() => {
    let parsedDate;
    if (valueProp == null) {
      parsedDate = null;
    } else if (valueProp instanceof Date) {
      parsedDate = valueProp;
    } else {
      parsedDate = new Date((valueProp ?? "").toString());
    }
    let formattedDate;
    if (parsedDate == null || Number.isNaN(parsedDate.getTime())) {
      formattedDate = "";
    } else {
      const localDate = new Date(parsedDate.getTime() - parsedDate.getTimezoneOffset() * 60 * 1e3);
      formattedDate = localDate.toISOString().substr(0, isDateTime ? 16 : 10);
    }
    return {
      parsed: parsedDate,
      formatted: formattedDate
    };
  }, [valueProp, isDateTime]);
  const [valueState, setValueState] = React30.useState(valueTransformed);
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses8(ownerState);
  const parseValueToDate = React30.useCallback((value) => {
    if (value === "") {
      return null;
    }
    const [date, time] = value.split("T");
    const [year, month, day] = date.split("-");
    const parsedDate = /* @__PURE__ */ new Date();
    parsedDate.setFullYear(Number(year), Number(month) - 1, Number(day));
    parsedDate.setHours(0, 0, 0, 0);
    if (time) {
      const [hours, minutes] = time.split(":");
      parsedDate.setHours(Number(hours), Number(minutes), 0, 0);
    }
    return parsedDate;
  }, []);
  const handleChange = React30.useCallback(async (event) => {
    const newFormattedDate = event.target.value;
    const newParsedDate = parseValueToDate(newFormattedDate);
    if (onValueChange) {
      await onValueChange(event, newParsedDate);
    }
    setValueState({
      parsed: newParsedDate,
      formatted: newFormattedDate
    });
    apiRef.current.setEditCellValue({
      id,
      field,
      value: newParsedDate
    }, event);
  }, [apiRef, field, id, onValueChange, parseValueToDate]);
  React30.useEffect(() => {
    setValueState((state) => {
      var _a2, _b2;
      if (valueTransformed.parsed !== state.parsed && ((_a2 = valueTransformed.parsed) == null ? void 0 : _a2.getTime()) !== ((_b2 = state.parsed) == null ? void 0 : _b2.getTime())) {
        return valueTransformed;
      }
      return state;
    });
  }, [valueTransformed]);
  useEnhancedEffect_default(() => {
    if (hasFocus) {
      inputRef.current.focus();
    }
  }, [hasFocus]);
  return (0, import_jsx_runtime16.jsx)(StyledInputBase, _extends({
    as: rootProps.slots.baseInput,
    inputRef,
    fullWidth: true,
    className: classes2.root,
    type: isDateTime ? "datetime-local" : "date",
    value: valueState.formatted,
    onChange: handleChange
  }, other, slotProps == null ? void 0 : slotProps.root, {
    slotProps: {
      htmlInput: _extends({
        max: isDateTime ? "9999-12-31T23:59" : "9999-12-31"
      }, (_b = (_a = slotProps == null ? void 0 : slotProps.root) == null ? void 0 : _a.slotProps) == null ? void 0 : _b.htmlInput)
    }
  }));
}
true ? GridEditDateCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types12.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types12.default.oneOf(["edit", "view"]).isRequired,
  changeReason: import_prop_types12.default.oneOf(["debouncedSetEditCellValue", "setEditCellValue"]),
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types12.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types12.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types12.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types12.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types12.default.oneOfType([import_prop_types12.default.number, import_prop_types12.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types12.default.bool,
  isProcessingProps: import_prop_types12.default.bool,
  isValidating: import_prop_types12.default.bool,
  /**
   * Callback called when the value is changed by the user.
   * @param {React.ChangeEvent<HTMLInputElement>} event The event source of the callback.
   * @param {Date | null} newValue The value that is going to be passed to `apiRef.current.setEditCellValue`.
   * @returns {Promise<void> | void} A promise to be awaited before calling `apiRef.current.setEditCellValue`
   */
  onValueChange: import_prop_types12.default.func,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types12.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types12.default.object.isRequired,
  slotProps: import_prop_types12.default.object,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types12.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types12.default.any
} : void 0;
var renderEditDateCell = (params) => (0, import_jsx_runtime16.jsx)(GridEditDateCell, _extends({}, params));
if (true) renderEditDateCell.displayName = "renderEditDateCell";

// node_modules/@mui/x-data-grid/esm/colDef/gridDateColDef.js
function throwIfNotDateObject({
  value,
  columnType,
  rowId,
  field
}) {
  if (!(value instanceof Date)) {
    throw new Error([`MUI X: \`${columnType}\` column type only accepts \`Date\` objects as values.`, "Use `valueGetter` to transform the value into a `Date` object.", `Row ID: ${rowId}, field: "${field}".`].join("\n"));
  }
}
var gridDateFormatter = (value, row, column, apiRef) => {
  if (!value) {
    return "";
  }
  const rowId = gridRowIdSelector(apiRef, row);
  if (isAutogeneratedRow(row) && !(value instanceof Date)) {
    return value;
  }
  throwIfNotDateObject({
    value,
    columnType: "date",
    rowId,
    field: column.field
  });
  return value.toLocaleDateString();
};
var gridDateTimeFormatter = (value, row, column, apiRef) => {
  if (!value) {
    return "";
  }
  const rowId = gridRowIdSelector(apiRef, row);
  if (isAutogeneratedRow(row) && !(value instanceof Date)) {
    return value;
  }
  throwIfNotDateObject({
    value,
    columnType: "dateTime",
    rowId,
    field: column.field
  });
  return value.toLocaleString();
};
var GRID_DATE_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "date",
  sortComparator: gridDateComparator,
  valueFormatter: gridDateFormatter,
  filterOperators: getGridDateOperators(),
  renderEditCell: renderEditDateCell,
  // @ts-ignore
  pastedValueParser: (value) => new Date(value)
});
var GRID_DATETIME_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "dateTime",
  sortComparator: gridDateComparator,
  valueFormatter: gridDateTimeFormatter,
  filterOperators: getGridDateOperators(true),
  renderEditCell: renderEditDateCell,
  // @ts-ignore
  pastedValueParser: (value) => new Date(value)
});

// node_modules/@mui/x-data-grid/esm/colDef/gridNumericOperators.js
var parseNumericValue = (value) => {
  if (value == null) {
    return null;
  }
  return Number(value);
};
var getGridNumericQuickFilterFn = (value) => {
  if (value == null || Number.isNaN(value) || value === "") {
    return null;
  }
  return (columnValue) => {
    return parseNumericValue(columnValue) === parseNumericValue(value);
  };
};
var getGridNumericOperators = () => [{
  value: "=",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      return parseNumericValue(value) === filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: "!=",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      return parseNumericValue(value) !== filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: ">",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      if (value == null) {
        return false;
      }
      return parseNumericValue(value) > filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: ">=",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      if (value == null) {
        return false;
      }
      return parseNumericValue(value) >= filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: "<",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      if (value == null) {
        return false;
      }
      return parseNumericValue(value) < filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: "<=",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || Number.isNaN(filterItem.value)) {
      return null;
    }
    return (value) => {
      if (value == null) {
        return false;
      }
      return parseNumericValue(value) <= filterItem.value;
    };
  },
  InputComponent: GridFilterInputValue,
  InputComponentProps: {
    type: "number"
  }
}, {
  value: "isEmpty",
  getApplyFilterFn: () => {
    return (value) => {
      return value == null;
    };
  },
  requiresFilterValue: false
}, {
  value: "isNotEmpty",
  getApplyFilterFn: () => {
    return (value) => {
      return value != null;
    };
  },
  requiresFilterValue: false
}, {
  value: "isAnyOf",
  getApplyFilterFn: (filterItem) => {
    if (!Array.isArray(filterItem.value) || filterItem.value.length === 0) {
      return null;
    }
    return (value) => {
      return value != null && filterItem.value.includes(Number(value));
    };
  },
  InputComponent: GridFilterInputMultipleValue,
  InputComponentProps: {
    type: "number"
  }
}];

// node_modules/@mui/x-data-grid/esm/colDef/gridNumericColDef.js
var GRID_NUMERIC_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "number",
  align: "right",
  headerAlign: "right",
  sortComparator: gridNumberComparator,
  valueParser: (value) => value === "" ? null : Number(value),
  valueFormatter: (value) => isNumber(value) ? value.toLocaleString() : value || "",
  filterOperators: getGridNumericOperators(),
  getApplyQuickFilterFn: getGridNumericQuickFilterFn
});

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditSingleSelectCell.js
var React31 = __toESM(require_react(), 1);
var import_prop_types13 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/filterPanelUtils.js
function isSingleSelectColDef(colDef) {
  return (colDef == null ? void 0 : colDef.type) === "singleSelect";
}
function getValueOptions(column, additionalParams) {
  if (!column) {
    return void 0;
  }
  return typeof column.valueOptions === "function" ? column.valueOptions(_extends({
    field: column.field
  }, additionalParams)) : column.valueOptions;
}
function getValueFromValueOptions(value, valueOptions, getOptionValue) {
  if (valueOptions === void 0) {
    return void 0;
  }
  const result = valueOptions.find((option) => {
    const optionValue = getOptionValue(option);
    return String(optionValue) === String(value);
  });
  return getOptionValue(result);
}

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditSingleSelectCell.js
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var _excluded15 = ["id", "value", "formattedValue", "api", "field", "row", "rowNode", "colDef", "cellMode", "isEditable", "tabIndex", "className", "hasFocus", "isValidating", "isProcessingProps", "error", "onValueChange", "initialOpen", "slotProps"];
function isKeyboardEvent2(event) {
  return "key" in event && !!event.key;
}
function GridEditSingleSelectCell(props) {
  var _a, _b;
  const rootProps = useGridRootProps();
  const {
    id,
    value: valueProp,
    field,
    row,
    colDef,
    hasFocus,
    error,
    onValueChange,
    initialOpen = rootProps.editMode === GridEditModes.Cell,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded15);
  const apiRef = useGridApiContext();
  const ref = React31.useRef(null);
  const inputRef = React31.useRef(null);
  const [open, setOpen] = React31.useState(initialOpen);
  const baseSelectProps = ((_a = rootProps.slotProps) == null ? void 0 : _a.baseSelect) || {};
  const isSelectNative = baseSelectProps.native ?? false;
  useEnhancedEffect_default(() => {
    var _a2;
    if (hasFocus) {
      (_a2 = inputRef.current) == null ? void 0 : _a2.focus();
    }
  }, [hasFocus]);
  if (!isSingleSelectColDef(colDef)) {
    return null;
  }
  const valueOptions = getValueOptions(colDef, {
    id,
    row
  });
  if (!valueOptions) {
    return null;
  }
  const getOptionValue = colDef.getOptionValue;
  const getOptionLabel = colDef.getOptionLabel;
  const handleChange = async (event) => {
    if (!isSingleSelectColDef(colDef) || !valueOptions) {
      return;
    }
    setOpen(false);
    const target = event.target;
    const formattedTargetValue = getValueFromValueOptions(target.value, valueOptions, getOptionValue);
    if (onValueChange) {
      await onValueChange(event, formattedTargetValue);
    }
    await apiRef.current.setEditCellValue({
      id,
      field,
      value: formattedTargetValue
    }, event);
  };
  const handleClose = (event, reason) => {
    if (rootProps.editMode === GridEditModes.Row) {
      setOpen(false);
      return;
    }
    if (reason === "backdropClick" || event.key === "Escape") {
      const params = apiRef.current.getCellParams(id, field);
      apiRef.current.publishEvent("cellEditStop", _extends({}, params, {
        reason: event.key === "Escape" ? GridCellEditStopReasons.escapeKeyDown : GridCellEditStopReasons.cellFocusOut
      }));
    }
  };
  const handleOpen = (event) => {
    if (isKeyboardEvent2(event) && event.key === "Enter") {
      return;
    }
    setOpen(true);
  };
  if (!valueOptions || !colDef) {
    return null;
  }
  return (0, import_jsx_runtime17.jsx)(rootProps.slots.baseSelect, _extends({
    ref,
    value: valueProp,
    onChange: handleChange,
    open,
    onOpen: handleOpen,
    onClose: handleClose,
    error,
    native: isSelectNative,
    fullWidth: true,
    slotProps: {
      htmlInput: {
        ref: inputRef
      }
    }
  }, other, slotProps == null ? void 0 : slotProps.root, (_b = rootProps.slotProps) == null ? void 0 : _b.baseSelect, {
    children: valueOptions.map((valueOption) => {
      var _a2;
      const value = getOptionValue(valueOption);
      return (0, import_react.createElement)(rootProps.slots.baseSelectOption, _extends({}, ((_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseSelectOption) || {}, {
        native: isSelectNative,
        key: value,
        value
      }), getOptionLabel(valueOption));
    })
  }));
}
true ? GridEditSingleSelectCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types13.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types13.default.oneOf(["edit", "view"]).isRequired,
  changeReason: import_prop_types13.default.oneOf(["debouncedSetEditCellValue", "setEditCellValue"]),
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types13.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types13.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types13.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types13.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types13.default.oneOfType([import_prop_types13.default.number, import_prop_types13.default.string]).isRequired,
  /**
   * If true, the select opens by default.
   */
  initialOpen: import_prop_types13.default.bool,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types13.default.bool,
  isProcessingProps: import_prop_types13.default.bool,
  isValidating: import_prop_types13.default.bool,
  /**
   * Callback called when the value is changed by the user.
   * @param {Event<any>} event The event source of the callback.
   * @param {any} newValue The value that is going to be passed to `apiRef.current.setEditCellValue`.
   * @returns {Promise<void> | void} A promise to be awaited before calling `apiRef.current.setEditCellValue`
   */
  onValueChange: import_prop_types13.default.func,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types13.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types13.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types13.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types13.default.any
} : void 0;
var renderEditSingleSelectCell = (params) => (0, import_jsx_runtime17.jsx)(GridEditSingleSelectCell, _extends({}, params));
if (true) renderEditSingleSelectCell.displayName = "renderEditSingleSelectCell";

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputSingleSelect.js
var React32 = __toESM(require_react(), 1);
var import_prop_types14 = __toESM(require_prop_types(), 1);
var import_react2 = __toESM(require_react(), 1);
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
var _excluded16 = ["item", "applyValue", "type", "apiRef", "focusElementRef", "tabIndex", "isFilterActive", "clearButton", "headerFilterMenu", "slotProps"];
var renderSingleSelectOptions = ({
  column,
  OptionComponent,
  getOptionLabel,
  getOptionValue,
  isSelectNative,
  baseSelectOptionProps
}) => {
  const iterableColumnValues = ["", ...getValueOptions(column) || []];
  return iterableColumnValues.map((option) => {
    const value = getOptionValue(option);
    let label = getOptionLabel(option);
    if (label === "") {
      label = " ";
    }
    return (0, import_react2.createElement)(OptionComponent, _extends({}, baseSelectOptionProps, {
      native: isSelectNative,
      key: value,
      value
    }), label);
  });
};
function GridFilterInputSingleSelect(props) {
  var _a, _b, _c, _d, _e;
  const {
    item,
    applyValue,
    type,
    apiRef,
    focusElementRef,
    tabIndex,
    clearButton,
    headerFilterMenu,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded16);
  const filterValue = item.value ?? "";
  const id = useId();
  const labelId = useId();
  const rootProps = useGridRootProps();
  const isSelectNative = ((_b = (_a = rootProps.slotProps) == null ? void 0 : _a.baseSelect) == null ? void 0 : _b.native) ?? false;
  const resolvedColumn = apiRef.current.getColumn(item.field);
  const getOptionValue = resolvedColumn.getOptionValue;
  const getOptionLabel = resolvedColumn.getOptionLabel;
  const currentValueOptions = React32.useMemo(() => {
    return getValueOptions(resolvedColumn);
  }, [resolvedColumn]);
  const onFilterChange = React32.useCallback((event) => {
    let value = event.target.value;
    value = getValueFromValueOptions(value, currentValueOptions, getOptionValue);
    applyValue(_extends({}, item, {
      value
    }));
  }, [currentValueOptions, getOptionValue, applyValue, item]);
  if (!resolvedColumn || !isSingleSelectColDef(resolvedColumn)) {
    return null;
  }
  const label = (slotProps == null ? void 0 : slotProps.root.label) ?? apiRef.current.getLocaleText("filterPanelInputLabel");
  return (0, import_jsx_runtime18.jsxs)(React32.Fragment, {
    children: [(0, import_jsx_runtime18.jsx)(rootProps.slots.baseSelect, _extends({
      fullWidth: true,
      id,
      label,
      labelId,
      value: filterValue,
      onChange: onFilterChange,
      slotProps: {
        htmlInput: _extends({
          tabIndex,
          ref: focusElementRef,
          type: type || "text",
          placeholder: (slotProps == null ? void 0 : slotProps.root.placeholder) ?? apiRef.current.getLocaleText("filterPanelInputPlaceholder")
        }, (_c = slotProps == null ? void 0 : slotProps.root.slotProps) == null ? void 0 : _c.htmlInput)
      },
      native: isSelectNative
    }, (_d = rootProps.slotProps) == null ? void 0 : _d.baseSelect, other, slotProps == null ? void 0 : slotProps.root, {
      children: renderSingleSelectOptions({
        column: resolvedColumn,
        OptionComponent: rootProps.slots.baseSelectOption,
        getOptionLabel,
        getOptionValue,
        isSelectNative,
        baseSelectOptionProps: (_e = rootProps.slotProps) == null ? void 0 : _e.baseSelectOption
      })
    })), headerFilterMenu, clearButton]
  });
}
true ? GridFilterInputSingleSelect.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types14.default.shape({
    current: import_prop_types14.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types14.default.func.isRequired,
  className: import_prop_types14.default.string,
  clearButton: import_prop_types14.default.node,
  disabled: import_prop_types14.default.bool,
  focusElementRef: import_prop_types14.default.oneOfType([import_prop_types14.default.func, import_prop_types14.default.object]),
  headerFilterMenu: import_prop_types14.default.node,
  inputRef: import_prop_types14.default.oneOfType([import_prop_types14.default.func, import_prop_types14.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types14.default.bool,
  item: import_prop_types14.default.shape({
    field: import_prop_types14.default.string.isRequired,
    id: import_prop_types14.default.oneOfType([import_prop_types14.default.number, import_prop_types14.default.string]),
    operator: import_prop_types14.default.string.isRequired,
    value: import_prop_types14.default.any
  }).isRequired,
  onBlur: import_prop_types14.default.func,
  onFocus: import_prop_types14.default.func,
  slotProps: import_prop_types14.default.object,
  tabIndex: import_prop_types14.default.number,
  type: import_prop_types14.default.oneOf(["singleSelect"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterInputMultipleSingleSelect.js
var React33 = __toESM(require_react(), 1);
var import_prop_types15 = __toESM(require_prop_types(), 1);
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
var _excluded17 = ["item", "applyValue", "type", "apiRef", "focusElementRef", "slotProps"];
function GridFilterInputMultipleSingleSelect(props) {
  const {
    item,
    applyValue,
    type,
    apiRef,
    focusElementRef,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded17);
  const id = useId();
  const rootProps = useGridRootProps();
  const resolvedColumn = apiRef.current.getColumn(item.field);
  const getOptionValue = resolvedColumn.getOptionValue;
  const getOptionLabel = resolvedColumn.getOptionLabel;
  const isOptionEqualToValue = React33.useCallback((option, value) => getOptionValue(option) === getOptionValue(value), [getOptionValue]);
  const resolvedValueOptions = React33.useMemo(() => {
    return getValueOptions(resolvedColumn) || [];
  }, [resolvedColumn]);
  const filteredValues = React33.useMemo(() => {
    if (!Array.isArray(item.value)) {
      return [];
    }
    return item.value.reduce((acc, value) => {
      const resolvedValue = resolvedValueOptions.find((v) => getOptionValue(v) === value);
      if (resolvedValue != null) {
        acc.push(resolvedValue);
      }
      return acc;
    }, []);
  }, [getOptionValue, item.value, resolvedValueOptions]);
  const handleChange = React33.useCallback((event, value) => {
    applyValue(_extends({}, item, {
      value: value.map(getOptionValue)
    }));
  }, [applyValue, item, getOptionValue]);
  if (!resolvedColumn || !isSingleSelectColDef(resolvedColumn)) {
    return null;
  }
  const BaseAutocomplete2 = rootProps.slots.baseAutocomplete;
  return (0, import_jsx_runtime19.jsx)(BaseAutocomplete2, _extends({
    multiple: true,
    options: resolvedValueOptions,
    isOptionEqualToValue,
    id,
    value: filteredValues,
    onChange: handleChange,
    getOptionLabel,
    label: apiRef.current.getLocaleText("filterPanelInputLabel"),
    placeholder: apiRef.current.getLocaleText("filterPanelInputPlaceholder"),
    slotProps: {
      textField: {
        type: type || "text",
        inputRef: focusElementRef
      }
    }
  }, other, slotProps == null ? void 0 : slotProps.root));
}
true ? GridFilterInputMultipleSingleSelect.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  apiRef: import_prop_types15.default.shape({
    current: import_prop_types15.default.object.isRequired
  }).isRequired,
  applyValue: import_prop_types15.default.func.isRequired,
  className: import_prop_types15.default.string,
  clearButton: import_prop_types15.default.node,
  disabled: import_prop_types15.default.bool,
  focusElementRef: import_prop_types15.default.oneOfType([import_prop_types15.default.func, import_prop_types15.default.object]),
  headerFilterMenu: import_prop_types15.default.node,
  inputRef: import_prop_types15.default.oneOfType([import_prop_types15.default.func, import_prop_types15.default.shape({
    current: (props, propName) => {
      if (props[propName] == null) {
        return null;
      }
      if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
        return new Error(`Expected prop '${propName}' to be of type Element`);
      }
      return null;
    }
  })]),
  /**
   * It is `true` if the filter either has a value or an operator with no value
   * required is selected (for example `isEmpty`)
   */
  isFilterActive: import_prop_types15.default.bool,
  item: import_prop_types15.default.shape({
    field: import_prop_types15.default.string.isRequired,
    id: import_prop_types15.default.oneOfType([import_prop_types15.default.number, import_prop_types15.default.string]),
    operator: import_prop_types15.default.string.isRequired,
    value: import_prop_types15.default.any
  }).isRequired,
  onBlur: import_prop_types15.default.func,
  onFocus: import_prop_types15.default.func,
  slotProps: import_prop_types15.default.object,
  tabIndex: import_prop_types15.default.number,
  type: import_prop_types15.default.oneOf(["singleSelect"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/colDef/gridSingleSelectOperators.js
var parseObjectValue = (value) => {
  if (value == null || !isObject(value)) {
    return value;
  }
  return value.value;
};
var getGridSingleSelectOperators = () => [{
  value: "is",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || filterItem.value === "") {
      return null;
    }
    return (value) => parseObjectValue(value) === parseObjectValue(filterItem.value);
  },
  InputComponent: GridFilterInputSingleSelect
}, {
  value: "not",
  getApplyFilterFn: (filterItem) => {
    if (filterItem.value == null || filterItem.value === "") {
      return null;
    }
    return (value) => parseObjectValue(value) !== parseObjectValue(filterItem.value);
  },
  InputComponent: GridFilterInputSingleSelect
}, {
  value: "isAnyOf",
  getApplyFilterFn: (filterItem) => {
    if (!Array.isArray(filterItem.value) || filterItem.value.length === 0) {
      return null;
    }
    const filterItemValues = filterItem.value.map(parseObjectValue);
    return (value) => filterItemValues.includes(parseObjectValue(value));
  },
  InputComponent: GridFilterInputMultipleSingleSelect
}];

// node_modules/@mui/x-data-grid/esm/colDef/gridSingleSelectColDef.js
var isArrayOfObjects = (options3) => {
  return typeof options3[0] === "object";
};
var defaultGetOptionValue = (value) => {
  return isObject(value) ? value.value : value;
};
var defaultGetOptionLabel = (value) => {
  return isObject(value) ? value.label : String(value);
};
var GRID_SINGLE_SELECT_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "singleSelect",
  getOptionLabel: defaultGetOptionLabel,
  getOptionValue: defaultGetOptionValue,
  valueFormatter(value, row, colDef, apiRef) {
    const rowId = gridRowIdSelector(apiRef, row);
    if (!isSingleSelectColDef(colDef)) {
      return "";
    }
    const valueOptions = getValueOptions(colDef, {
      id: rowId,
      row
    });
    if (value == null) {
      return "";
    }
    if (!valueOptions) {
      return value;
    }
    if (!isArrayOfObjects(valueOptions)) {
      return colDef.getOptionLabel(value);
    }
    const valueOption = valueOptions.find((option) => colDef.getOptionValue(option) === value);
    return valueOption ? colDef.getOptionLabel(valueOption) : "";
  },
  renderEditCell: renderEditSingleSelectCell,
  filterOperators: getGridSingleSelectOperators(),
  // @ts-ignore
  pastedValueParser: (value, row, column) => {
    const colDef = column;
    const valueOptions = getValueOptions(colDef) || [];
    const getOptionValue = colDef.getOptionValue;
    const valueOption = valueOptions.find((option) => {
      if (getOptionValue(option) === value) {
        return true;
      }
      return false;
    });
    if (valueOption) {
      return value;
    }
    return void 0;
  }
});

// node_modules/@mui/x-data-grid/esm/components/cell/GridLongTextCell.js
var React34 = __toESM(require_react(), 1);
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses9 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["longTextCell"],
    content: ["longTextCellContent"],
    expandButton: ["longTextCellExpandButton"],
    collapseButton: ["longTextCellCollapseButton"],
    popup: ["longTextCellPopup"],
    popperContent: ["longTextCellPopperContent"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridLongTextCellRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "LongTextCell"
})({
  display: "flex",
  alignItems: "center",
  width: "100%",
  height: "100%",
  position: "relative"
});
var GridLongTextCellContent = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "LongTextCellContent"
})({
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap",
  flex: 1
});
var GridLongTextCellPopperContent = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "LongTextCellPopperContent"
})(({
  theme
}) => _extends({}, theme.typography.body2, {
  letterSpacing: "normal",
  paddingBlock: 15.5,
  paddingInline: 9,
  maxHeight: 52 * 3,
  overflow: "auto",
  whiteSpace: "pre-wrap",
  wordBreak: "break-word",
  width: "var(--_width)",
  border: `1px solid ${(theme.vars || theme).palette.divider}`,
  boxSizing: "border-box"
}));
var GridLongTextCellCornerButton = styled_default2("button", {
  name: "MuiDataGrid",
  slot: "LongTextCellCornerButton"
})(({
  theme
}) => ({
  lineHeight: 0,
  position: "absolute",
  bottom: 1,
  right: 0,
  border: "1px solid",
  color: (theme.vars || theme).palette.text.secondary,
  borderColor: (theme.vars || theme).palette.divider,
  backgroundColor: (theme.vars || theme).palette.background.paper,
  borderRadius: 0,
  fontSize: "0.875rem",
  padding: 2,
  "&:focus-visible": {
    outline: "none"
  },
  "&:hover": {
    backgroundColor: (theme.vars || theme).palette.background.paper,
    color: (theme.vars || theme).palette.text.primary
  },
  [`&.${gridClasses.longTextCellExpandButton}`]: {
    right: -9,
    opacity: 0,
    [`.${gridClasses.longTextCell}:hover &, .${gridClasses.longTextCell}.Mui-focused &`]: {
      opacity: 1
    }
  },
  [`&.${gridClasses.longTextCellCollapseButton}`]: {
    bottom: 2,
    right: 2,
    border: "none"
  }
}));
var GridLongTextCellPopper = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "LongTextCellPopper"
})(({
  theme
}) => ({
  zIndex: vars.zIndex.menu,
  background: (theme.vars || theme).palette.background.paper,
  "&[data-popper-reference-hidden]": {
    visibility: "hidden",
    pointerEvents: "none"
  }
}));
function GridLongTextCell(props) {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  const {
    id,
    value = "",
    colDef,
    hasFocus,
    slotProps,
    renderContent
  } = props;
  const popupId = `${id}-${colDef.field}-longtext-popup`;
  const rootProps = useGridRootProps();
  const apiRef = useGridApiContext();
  const classes2 = useUtilityClasses9(rootProps);
  const rowHeight = useGridSelector(apiRef, gridRowHeightSelector);
  const [popupOpen, setPopupOpen] = React34.useState(false);
  const cellRef = React34.useRef(null);
  const cornerButtonRef = React34.useRef(null);
  React34.useEffect(() => {
    if (hasFocus && !popupOpen) {
      if (cornerButtonRef.current && cornerButtonRef.current !== document.activeElement) {
        cornerButtonRef.current.focus();
      }
    }
    if (!hasFocus) {
      setPopupOpen(false);
    }
  }, [hasFocus, popupOpen]);
  const handleExpandClick = (event) => {
    if (event.detail === 0) {
      return;
    }
    event.stopPropagation();
    setPopupOpen(true);
  };
  const handleExpandKeyDown = (event) => {
    if (event.key === " " && !event.shiftKey) {
      event.preventDefault();
      event.stopPropagation();
      setPopupOpen((prev) => !prev);
    }
    if (event.key === "Escape" && popupOpen) {
      event.stopPropagation();
      setPopupOpen(false);
    }
  };
  const handleClickAway = () => {
    setPopupOpen(false);
  };
  const handleCollapseClick = (event) => {
    var _a2;
    event.stopPropagation();
    setPopupOpen(false);
    (_a2 = apiRef.current.getCellElement(id, colDef.field)) == null ? void 0 : _a2.focus();
  };
  return (0, import_jsx_runtime20.jsxs)(GridLongTextCellRoot, _extends({
    ref: cellRef
  }, slotProps == null ? void 0 : slotProps.root, {
    className: clsx_default(classes2.root, (_a = slotProps == null ? void 0 : slotProps.root) == null ? void 0 : _a.className, hasFocus && "Mui-focused"),
    children: [(0, import_jsx_runtime20.jsx)(GridLongTextCellContent, _extends({}, slotProps == null ? void 0 : slotProps.content, {
      className: clsx_default(classes2.content, (_b = slotProps == null ? void 0 : slotProps.content) == null ? void 0 : _b.className),
      children: value
    })), (0, import_jsx_runtime20.jsx)(GridLongTextCellCornerButton, _extends({
      ref: cornerButtonRef,
      "aria-label": `${value}, ${apiRef.current.getLocaleText("longTextCellExpandLabel")}`,
      "aria-haspopup": "dialog",
      "aria-controls": popupOpen ? popupId : void 0,
      "aria-expanded": popupOpen,
      "aria-keyshortcuts": "Space",
      tabIndex: 0
    }, slotProps == null ? void 0 : slotProps.expandButton, {
      className: clsx_default(classes2.expandButton, (_c = slotProps == null ? void 0 : slotProps.expandButton) == null ? void 0 : _c.className),
      onClick: handleExpandClick,
      onKeyDown: handleExpandKeyDown,
      children: (0, import_jsx_runtime20.jsx)(rootProps.slots.longTextCellExpandIcon, {
        fontSize: "inherit"
      })
    })), (0, import_jsx_runtime20.jsx)(GridLongTextCellPopper, _extends({
      id: popupId,
      role: "dialog",
      "aria-label": colDef.headerName || colDef.field,
      as: rootProps.slots.basePopper,
      ownerState: rootProps,
      open: popupOpen,
      target: cellRef.current,
      placement: "bottom-start",
      onClickAway: handleClickAway,
      clickAwayMouseEvent: "onMouseDown",
      flip: true,
      material: {
        container: (_d = cellRef.current) == null ? void 0 : _d.closest('[role="row"]'),
        modifiers: [{
          name: "offset",
          options: {
            offset: [-10, -rowHeight]
          }
        }]
      }
    }, slotProps == null ? void 0 : slotProps.popper, {
      className: clsx_default(classes2.popup, (_e = slotProps == null ? void 0 : slotProps.popper) == null ? void 0 : _e.className),
      children: (0, import_jsx_runtime20.jsxs)(GridLongTextCellPopperContent, _extends({
        tabIndex: -1,
        onKeyDown: (event) => {
          var _a2;
          if (event.key === "Escape") {
            event.stopPropagation();
            setPopupOpen(false);
            (_a2 = apiRef.current.getCellElement(id, colDef.field)) == null ? void 0 : _a2.focus();
          }
        }
      }, slotProps == null ? void 0 : slotProps.popperContent, {
        className: clsx_default(classes2.popperContent, (_f = slotProps == null ? void 0 : slotProps.popperContent) == null ? void 0 : _f.className),
        style: _extends({
          "--_width": `${colDef.computedWidth}px`
        }, (_g = slotProps == null ? void 0 : slotProps.popperContent) == null ? void 0 : _g.style),
        children: [renderContent ? renderContent(value) : value, (0, import_jsx_runtime20.jsx)(GridLongTextCellCornerButton, _extends({
          "aria-label": apiRef.current.getLocaleText("longTextCellCollapseLabel"),
          "aria-keyshortcuts": "Escape"
        }, slotProps == null ? void 0 : slotProps.collapseButton, {
          className: clsx_default(classes2.collapseButton, (_h = slotProps == null ? void 0 : slotProps.collapseButton) == null ? void 0 : _h.className),
          onClick: handleCollapseClick,
          children: (0, import_jsx_runtime20.jsx)(rootProps.slots.longTextCellCollapseIcon, {
            fontSize: "inherit"
          })
        }))]
      }))
    }))]
  }));
}
var renderLongTextCell = (params) => (0, import_jsx_runtime20.jsx)(GridLongTextCell, _extends({}, params));
if (true) renderLongTextCell.displayName = "renderLongTextCell";

// node_modules/@mui/x-data-grid/esm/components/cell/GridEditLongTextCell.js
var React35 = __toESM(require_react(), 1);
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses10 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["editLongTextCell"],
    value: ["editLongTextCellValue"],
    popup: ["editLongTextCellPopup"],
    popperContent: ["editLongTextCellPopperContent"],
    textarea: ["editLongTextCellTextarea"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridEditLongTextCellTextarea = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "EditLongTextCellTextarea"
})(({
  theme
}) => _extends({
  width: "100%",
  padding: 0
}, theme.typography.body2, {
  letterSpacing: "normal",
  outline: "none",
  background: "transparent",
  border: "none",
  resize: "vertical"
}));
var GridEditLongTextCellRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "EditLongTextCell"
})({
  display: "flex",
  alignItems: "center",
  width: "100%",
  height: "100%",
  position: "relative"
});
var GridEditLongTextCellValue = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "EditLongTextCellValue"
})({
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap",
  width: "100%",
  paddingInline: 10
});
var GridEditLongTextCellPopper = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "EditLongTextCellPopper"
})(({
  theme
}) => ({
  zIndex: vars.zIndex.menu,
  background: (theme.vars || theme).palette.background.paper,
  "&[data-popper-reference-hidden]": {
    opacity: 0
    // use opacity to preserve focus.
  }
}));
var GridEditLongTextCellPopperContent = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "EditLongTextCellPopperContent"
})(({
  theme
}) => _extends({}, theme.typography.body2, {
  letterSpacing: "normal",
  paddingBlock: 15.5,
  paddingInline: 9,
  height: "max-content",
  overflow: "auto",
  whiteSpace: "pre-wrap",
  wordBreak: "break-word",
  width: "var(--_width)",
  border: `1px solid ${(theme.vars || theme).palette.divider}`,
  boxShadow: (theme.vars || theme).shadows[4],
  boxSizing: "border-box"
}));
function GridEditLongTextCell(props) {
  var _a, _b, _c, _d;
  const {
    id,
    value,
    field,
    colDef,
    hasFocus,
    cellMode,
    slotProps
  } = props;
  const rootProps = useGridRootProps();
  const apiRef = useGridApiContext();
  const classes2 = useUtilityClasses10(rootProps);
  const rowHeight = useGridSelector(apiRef, gridRowHeightSelector);
  const [valueState, setValueState] = React35.useState(value);
  const [anchorEl, setAnchorEl] = React35.useState(null);
  const meta = apiRef.current.unstable_getEditCellMeta(id, field);
  const popupId = `${id}-${field}-longtext-edit-popup`;
  const showPopup = hasFocus && Boolean(anchorEl);
  React35.useEffect(() => {
    if ((meta == null ? void 0 : meta.changeReason) !== "debouncedSetEditCellValue") {
      setValueState(value);
    }
  }, [meta, value]);
  return (0, import_jsx_runtime21.jsxs)(GridEditLongTextCellRoot, _extends({
    tabIndex: cellMode === "edit" && rootProps.editMode === "row" ? 0 : void 0,
    ref: setAnchorEl,
    "aria-controls": showPopup ? popupId : void 0,
    "aria-expanded": showPopup
  }, slotProps == null ? void 0 : slotProps.root, {
    className: clsx_default(classes2.root, (_a = slotProps == null ? void 0 : slotProps.root) == null ? void 0 : _a.className),
    children: [(0, import_jsx_runtime21.jsx)(GridEditLongTextCellValue, _extends({}, slotProps == null ? void 0 : slotProps.value, {
      className: clsx_default(classes2.value, (_b = slotProps == null ? void 0 : slotProps.value) == null ? void 0 : _b.className),
      children: valueState
    })), (0, import_jsx_runtime21.jsx)(GridEditLongTextCellPopper, _extends({
      as: rootProps.slots.basePopper,
      ownerState: rootProps,
      id: popupId,
      role: "dialog",
      "aria-label": colDef.headerName || field,
      open: showPopup,
      target: anchorEl,
      placement: "bottom-start",
      flip: true,
      material: {
        container: anchorEl == null ? void 0 : anchorEl.closest('[role="row"]'),
        modifiers: [{
          name: "offset",
          options: {
            offset: [-1, -rowHeight]
          }
        }]
      }
    }, slotProps == null ? void 0 : slotProps.popper, {
      className: clsx_default(classes2.popup, (_c = slotProps == null ? void 0 : slotProps.popper) == null ? void 0 : _c.className),
      children: (0, import_jsx_runtime21.jsx)(GridEditLongTextCellPopperContent, _extends({}, slotProps == null ? void 0 : slotProps.popperContent, {
        className: clsx_default(classes2.popperContent, (_d = slotProps == null ? void 0 : slotProps.popperContent) == null ? void 0 : _d.className),
        style: {
          "--_width": `${colDef.computedWidth}px`
        },
        children: (0, import_jsx_runtime21.jsx)(GridEditLongTextarea, _extends({}, props, {
          valueState,
          setValueState
        }))
      }))
    }))]
  }));
}
function GridEditLongTextarea(props) {
  var _a;
  const {
    id,
    field,
    colDef,
    debounceMs = 200,
    onValueChange,
    valueState,
    setValueState,
    hasFocus,
    slotProps
  } = props;
  const textareaRef = React35.useRef(null);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses10(rootProps);
  useEnhancedEffect_default(() => {
    if (hasFocus && textareaRef.current) {
      textareaRef.current.focus({
        preventScroll: true
      });
      const length = textareaRef.current.value.length;
      textareaRef.current.setSelectionRange(length, length);
    }
  }, [hasFocus]);
  const handleChange = React35.useCallback(async (event) => {
    const newValue = event.target.value;
    const column = apiRef.current.getColumn(field);
    let parsedValue = newValue;
    if (column.valueParser) {
      parsedValue = column.valueParser(newValue, apiRef.current.getRow(id), column, apiRef);
    }
    setValueState(parsedValue);
    apiRef.current.setEditCellValue({
      id,
      field,
      value: parsedValue,
      debounceMs,
      unstable_skipValueParser: true
    }, event);
    if (onValueChange) {
      await onValueChange(event, newValue);
    }
  }, [apiRef, debounceMs, field, id, onValueChange, setValueState]);
  const handleKeyDown = React35.useCallback((event) => {
    if (event.key === "Enter" && event.shiftKey) {
      event.stopPropagation();
    }
    if (rootProps.editMode === "cell" && event.key === "Escape") {
      apiRef.current.stopCellEditMode({
        id,
        field,
        ignoreModifications: true
      });
    }
  }, [apiRef, field, id, rootProps.editMode]);
  return (0, import_jsx_runtime21.jsx)(GridEditLongTextCellTextarea, _extends({
    ref: textareaRef,
    as: rootProps.slots.baseTextarea,
    ownerState: rootProps,
    "aria-label": colDef.headerName || field,
    value: valueState ?? "",
    onChange: handleChange,
    onKeyDown: handleKeyDown
  }, slotProps == null ? void 0 : slotProps.textarea, {
    className: clsx_default(classes2.textarea, (_a = slotProps == null ? void 0 : slotProps.textarea) == null ? void 0 : _a.className)
  }));
}
var renderEditLongTextCell = (params) => (0, import_jsx_runtime21.jsx)(GridEditLongTextCell, _extends({}, params));
if (true) renderEditLongTextCell.displayName = "renderEditLongTextCell";

// node_modules/@mui/x-data-grid/esm/colDef/gridLongTextColDef.js
var GRID_LONG_TEXT_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "longText",
  display: "flex",
  renderCell: renderLongTextCell,
  renderEditCell: renderEditLongTextCell
});

// node_modules/@mui/x-data-grid/esm/colDef/gridDefaultColumnTypes.js
var DEFAULT_GRID_COL_TYPE_KEY = "string";
var getGridDefaultColumnTypes = () => {
  const nativeColumnTypes = {
    string: GRID_STRING_COL_DEF,
    number: GRID_NUMERIC_COL_DEF,
    date: GRID_DATE_COL_DEF,
    dateTime: GRID_DATETIME_COL_DEF,
    boolean: GRID_BOOLEAN_COL_DEF,
    singleSelect: GRID_SINGLE_SELECT_COL_DEF,
    [GRID_ACTIONS_COLUMN_TYPE]: GRID_ACTIONS_COL_DEF,
    custom: GRID_STRING_COL_DEF,
    longText: GRID_LONG_TEXT_COL_DEF
  };
  return nativeColumnTypes;
};

// node_modules/@mui/x-data-grid/esm/hooks/features/headerFiltering/gridHeaderFilteringSelectors.js
var gridHeaderFilteringStateSelector = createRootSelector((state) => state.headerFiltering);
var gridHeaderFilteringEnabledSelector = createSelector3(
  gridHeaderFilteringStateSelector,
  // No initialization in MIT, so we need to default to false to be used by `getTotalHeaderHeight`
  (headerFilteringState) => (headerFilteringState == null ? void 0 : headerFilteringState.enabled) ?? false
);
var gridHeaderFilteringEditFieldSelector = createSelector3(gridHeaderFilteringStateSelector, (headerFilteringState) => headerFilteringState.editing);
var gridHeaderFilteringMenuSelector = createSelector3(gridHeaderFilteringStateSelector, (headerFilteringState) => headerFilteringState.menuOpen);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnGrouping/gridColumnGroupsSelector.js
var gridColumnGroupingSelector = createRootSelector((state) => state.columnGrouping);
var gridColumnGroupsUnwrappedModelSelector = createSelectorMemoized2(gridColumnGroupingSelector, (columnGrouping) => (columnGrouping == null ? void 0 : columnGrouping.unwrappedGroupingModel) ?? {});
var gridColumnGroupsLookupSelector = createSelectorMemoized2(gridColumnGroupingSelector, (columnGrouping) => (columnGrouping == null ? void 0 : columnGrouping.lookup) ?? {});
var gridColumnGroupsHeaderStructureSelector = createSelectorMemoized2(gridColumnGroupingSelector, (columnGrouping) => (columnGrouping == null ? void 0 : columnGrouping.headerStructure) ?? []);
var gridColumnGroupsHeaderMaxDepthSelector = createSelector3(gridColumnGroupingSelector, (columnGrouping) => (columnGrouping == null ? void 0 : columnGrouping.maxDepth) ?? 0);

// node_modules/@mui/x-data-grid/esm/hooks/features/columns/gridColumnsUtils.js
var COLUMNS_DIMENSION_PROPERTIES = ["maxWidth", "minWidth", "width", "flex"];
var COLUMN_TYPES = getGridDefaultColumnTypes();
function computeFlexColumnsWidth({
  initialFreeSpace,
  totalFlexUnits,
  flexColumns
}) {
  const uniqueFlexColumns = new Set(flexColumns.map((col) => col.field));
  const flexColumnsLookup = {
    all: {},
    frozenFields: [],
    freeze: (field) => {
      const value = flexColumnsLookup.all[field];
      if (value && value.frozen !== true) {
        flexColumnsLookup.all[field].frozen = true;
        flexColumnsLookup.frozenFields.push(field);
      }
    }
  };
  function loopOverFlexItems() {
    if (flexColumnsLookup.frozenFields.length === uniqueFlexColumns.size) {
      return;
    }
    const violationsLookup = {
      min: {},
      max: {}
    };
    let remainingFreeSpace = initialFreeSpace;
    let flexUnits = totalFlexUnits;
    let totalViolation = 0;
    flexColumnsLookup.frozenFields.forEach((field) => {
      remainingFreeSpace -= flexColumnsLookup.all[field].computedWidth;
      flexUnits -= flexColumnsLookup.all[field].flex;
    });
    for (let i2 = 0; i2 < flexColumns.length; i2 += 1) {
      const column = flexColumns[i2];
      if (flexColumnsLookup.all[column.field] && flexColumnsLookup.all[column.field].frozen === true) {
        continue;
      }
      const widthPerFlexUnit = remainingFreeSpace / flexUnits;
      let computedWidth = widthPerFlexUnit * column.flex;
      if (computedWidth < column.minWidth) {
        totalViolation += column.minWidth - computedWidth;
        computedWidth = column.minWidth;
        violationsLookup.min[column.field] = true;
      } else if (computedWidth > column.maxWidth) {
        totalViolation += column.maxWidth - computedWidth;
        computedWidth = column.maxWidth;
        violationsLookup.max[column.field] = true;
      }
      flexColumnsLookup.all[column.field] = {
        frozen: false,
        computedWidth,
        flex: column.flex
      };
    }
    if (totalViolation < 0) {
      Object.keys(violationsLookup.max).forEach((field) => {
        flexColumnsLookup.freeze(field);
      });
    } else if (totalViolation > 0) {
      Object.keys(violationsLookup.min).forEach((field) => {
        flexColumnsLookup.freeze(field);
      });
    } else {
      flexColumns.forEach(({
        field
      }) => {
        flexColumnsLookup.freeze(field);
      });
    }
    loopOverFlexItems();
  }
  loopOverFlexItems();
  return flexColumnsLookup.all;
}
var hydrateColumnsWidth = (rawState, dimensions) => {
  const columnsLookup = {};
  let totalFlexUnits = 0;
  let widthAllocatedBeforeFlex = 0;
  const flexColumns = [];
  rawState.orderedFields.forEach((columnField) => {
    let column = rawState.lookup[columnField];
    let computedWidth = 0;
    let isFlex = false;
    if (rawState.columnVisibilityModel[columnField] !== false) {
      if (column.flex && column.flex > 0) {
        totalFlexUnits += column.flex;
        isFlex = true;
      } else {
        computedWidth = clamp(column.width || GRID_STRING_COL_DEF.width, column.minWidth || GRID_STRING_COL_DEF.minWidth, column.maxWidth || GRID_STRING_COL_DEF.maxWidth);
      }
      widthAllocatedBeforeFlex += computedWidth;
    }
    if (column.computedWidth !== computedWidth) {
      column = _extends({}, column, {
        computedWidth
      });
    }
    if (isFlex) {
      flexColumns.push(column);
    }
    columnsLookup[columnField] = column;
  });
  const availableWidth = dimensions === void 0 ? 0 : dimensions.viewportOuterSize.width - (dimensions.hasScrollY ? dimensions.scrollbarSize : 0);
  const initialFreeSpace = Math.max(availableWidth - widthAllocatedBeforeFlex, 0);
  if (totalFlexUnits > 0 && availableWidth > 0) {
    const computedColumnWidths = computeFlexColumnsWidth({
      initialFreeSpace,
      totalFlexUnits,
      flexColumns
    });
    Object.keys(computedColumnWidths).forEach((field) => {
      columnsLookup[field] = _extends({}, columnsLookup[field], {
        computedWidth: computedColumnWidths[field].computedWidth
      });
    });
  }
  return _extends({}, rawState, {
    lookup: columnsLookup
  });
};
var applyInitialState = (columnsState, initialState) => {
  if (!initialState) {
    return columnsState;
  }
  const {
    orderedFields = [],
    dimensions = {}
  } = initialState;
  const columnsWithUpdatedDimensions = Object.keys(dimensions);
  if (columnsWithUpdatedDimensions.length === 0 && orderedFields.length === 0) {
    return columnsState;
  }
  const orderedFieldsLookup = {};
  const cleanOrderedFields = [];
  for (let i2 = 0; i2 < orderedFields.length; i2 += 1) {
    const field = orderedFields[i2];
    if (columnsState.lookup[field]) {
      orderedFieldsLookup[field] = true;
      cleanOrderedFields.push(field);
    }
  }
  const newOrderedFields = cleanOrderedFields.length === 0 ? columnsState.orderedFields : [...cleanOrderedFields, ...columnsState.orderedFields.filter((field) => !orderedFieldsLookup[field])];
  const newColumnLookup = _extends({}, columnsState.lookup);
  for (let i2 = 0; i2 < columnsWithUpdatedDimensions.length; i2 += 1) {
    const field = columnsWithUpdatedDimensions[i2];
    const newColDef = _extends({}, newColumnLookup[field], {
      hasBeenResized: true
    });
    Object.entries(dimensions[field]).forEach(([key, value]) => {
      newColDef[key] = value === -1 ? Infinity : value;
    });
    newColumnLookup[field] = newColDef;
  }
  const newColumnsState = _extends({}, columnsState, {
    orderedFields: newOrderedFields,
    lookup: newColumnLookup
  });
  return newColumnsState;
};
function getDefaultColTypeDef(type) {
  let colDef = COLUMN_TYPES[DEFAULT_GRID_COL_TYPE_KEY];
  if (type && COLUMN_TYPES[type]) {
    colDef = COLUMN_TYPES[type];
  }
  return colDef;
}
var createColumnsState = ({
  apiRef,
  columnsToUpsert,
  initialState,
  columnVisibilityModel = gridColumnVisibilityModelSelector(apiRef),
  keepOnlyColumnsToUpsert = false,
  updateInitialVisibilityModel = false
}) => {
  var _a, _b;
  const isInsideStateInitializer = !apiRef.current.state.columns;
  let columnsState;
  if (isInsideStateInitializer) {
    columnsState = {
      orderedFields: [],
      lookup: {},
      columnVisibilityModel,
      initialColumnVisibilityModel: columnVisibilityModel
    };
  } else {
    const currentState = gridColumnsStateSelector(apiRef);
    columnsState = {
      orderedFields: keepOnlyColumnsToUpsert ? [] : [...currentState.orderedFields],
      lookup: keepOnlyColumnsToUpsert ? {} : _extends({}, currentState.lookup),
      columnVisibilityModel,
      initialColumnVisibilityModel: updateInitialVisibilityModel ? columnVisibilityModel : currentState.initialColumnVisibilityModel
    };
  }
  const columnsToKeep = {};
  if (keepOnlyColumnsToUpsert && !isInsideStateInitializer) {
    for (const key in columnsState.lookup) {
      if (Object.prototype.hasOwnProperty.call(columnsState.lookup, key)) {
        columnsToKeep[key] = false;
      }
    }
  }
  columnsToUpsert.forEach((newColumn) => {
    const {
      field
    } = newColumn;
    columnsToKeep[field] = true;
    let existingState = columnsState.lookup[field];
    if (existingState == null) {
      existingState = _extends({}, getDefaultColTypeDef(newColumn.type), {
        field,
        hasBeenResized: false
      });
      columnsState.orderedFields.push(field);
    } else if (keepOnlyColumnsToUpsert) {
      columnsState.orderedFields.push(field);
    }
    if (existingState && existingState.type !== newColumn.type) {
      existingState = _extends({}, getDefaultColTypeDef(newColumn.type), {
        field
      });
    }
    let hasBeenResized = existingState.hasBeenResized;
    COLUMNS_DIMENSION_PROPERTIES.forEach((key2) => {
      if (newColumn[key2] !== void 0) {
        hasBeenResized = true;
        if (newColumn[key2] === -1) {
          newColumn[key2] = Infinity;
        }
      }
    });
    const mergedProps = _extends({}, getDefaultColTypeDef(newColumn.type), {
      hasBeenResized,
      field
    });
    let key;
    for (key in newColumn) {
      if (newColumn[key] !== void 0 && key !== "field") {
        mergedProps[key] = newColumn[key];
      }
    }
    columnsState.lookup[field] = resolveProps(existingState, mergedProps);
  });
  if (keepOnlyColumnsToUpsert && !isInsideStateInitializer) {
    Object.keys(columnsState.lookup).forEach((field) => {
      if (!columnsToKeep[field]) {
        delete columnsState.lookup[field];
      }
    });
  }
  const columnsStateWithPreProcessing = apiRef.current.unstable_applyPipeProcessors("hydrateColumns", columnsState);
  const columnsStateWithPortableColumns = applyInitialState(columnsStateWithPreProcessing, initialState);
  return hydrateColumnsWidth(columnsStateWithPortableColumns, ((_b = (_a = apiRef.current).getRootDimensions) == null ? void 0 : _b.call(_a)) ?? void 0);
};
function getTotalHeaderHeight(apiRef, props) {
  if (props.listView) {
    return 0;
  }
  const densityFactor = gridDensityFactorSelector(apiRef);
  const maxDepth = gridColumnGroupsHeaderMaxDepthSelector(apiRef);
  const isHeaderFilteringEnabled = gridHeaderFilteringEnabledSelector(apiRef);
  const columnHeadersHeight = Math.floor(props.columnHeaderHeight * densityFactor);
  const columnGroupHeadersHeight = Math.floor((props.columnGroupHeaderHeight ?? props.columnHeaderHeight) * densityFactor);
  const filterHeadersHeight = isHeaderFilteringEnabled ? Math.floor((props.headerFilterHeight ?? props.columnHeaderHeight) * densityFactor) : 0;
  return columnHeadersHeight + columnGroupHeadersHeight * maxDepth + filterHeadersHeight;
}

// node_modules/@mui/x-data-grid/esm/components/GridScrollArea.js
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var CLIFF = 1;
var SLOP = 1.5;
var useUtilityClasses11 = (ownerState) => {
  const {
    scrollDirection,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["scrollArea", `scrollArea--${scrollDirection}`]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridScrollAreaRawRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ScrollArea",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.scrollArea, styles5[`scrollArea--${ownerState.scrollDirection}`]];
  }
})(() => ({
  position: "absolute",
  zIndex: 101,
  // Horizontal scroll areas
  [`&.${gridClasses["scrollArea--left"]}`]: {
    top: 0,
    left: 0,
    width: 20,
    bottom: 0
  },
  [`&.${gridClasses["scrollArea--right"]}`]: {
    top: 0,
    right: 0,
    width: 20,
    bottom: 0
  },
  // Vertical scroll areas
  [`&.${gridClasses["scrollArea--up"]}`]: {
    top: 0,
    left: 0,
    right: 0,
    height: 20
  },
  [`&.${gridClasses["scrollArea--down"]}`]: {
    bottom: 0,
    left: 0,
    right: 0,
    height: 20
  }
}));
var offsetSelector = createSelector3(gridDimensionsSelector, (dimensions, direction) => {
  if (direction === "left") {
    return dimensions.leftPinnedWidth;
  }
  if (direction === "right") {
    return dimensions.rightPinnedWidth + (dimensions.hasScrollX ? dimensions.scrollbarSize : 0);
  }
  return 0;
});
function GridScrollAreaWrapper(props) {
  const apiRef = useGridApiContext();
  const [dragDirection, setDragDirection] = React36.useState("none");
  useGridEvent(apiRef, "columnHeaderDragStart", () => setDragDirection("horizontal"));
  useGridEvent(apiRef, "columnHeaderDragEnd", () => setDragDirection("none"));
  useGridEvent(apiRef, "rowDragStart", () => setDragDirection("vertical"));
  useGridEvent(apiRef, "rowDragEnd", () => setDragDirection("none"));
  if (dragDirection === "none") {
    return null;
  }
  if (dragDirection === "horizontal") {
    return (0, import_jsx_runtime22.jsx)(GridHorizontalScrollAreaContent, _extends({}, props));
  }
  return (0, import_jsx_runtime22.jsx)(GridVerticalScrollAreaContent, _extends({}, props));
}
function GridHorizontalScrollAreaContent(props) {
  const {
    scrollDirection,
    scrollPosition
  } = props;
  const rootRef = React36.useRef(null);
  const apiRef = useGridApiContext();
  const timeout2 = useTimeout();
  const densityFactor = useGridSelector(apiRef, gridDensityFactorSelector);
  const columnsTotalWidth = useGridSelector(apiRef, gridColumnsTotalWidthSelector);
  const sideOffset = useGridSelector(apiRef, offsetSelector, scrollDirection);
  const getCanScrollMore = () => {
    const dimensions = gridDimensionsSelector(apiRef);
    if (scrollDirection === "left") {
      return scrollPosition.current.left > 0;
    }
    if (scrollDirection === "right") {
      const maxScrollLeft = columnsTotalWidth - dimensions.viewportInnerSize.width;
      return scrollPosition.current.left < maxScrollLeft;
    }
    return false;
  };
  const rootProps = useGridRootProps();
  const totalHeaderHeight = getTotalHeaderHeight(apiRef, rootProps);
  const headerHeight = Math.floor(rootProps.columnHeaderHeight * densityFactor);
  const style = _extends({
    height: headerHeight,
    top: totalHeaderHeight - headerHeight
  }, scrollDirection === "left" ? {
    left: sideOffset
  } : {}, scrollDirection === "right" ? {
    right: sideOffset
  } : {});
  const handleDragOver = useEventCallback_default((event) => {
    let offset2;
    event.preventDefault();
    if (scrollDirection === "left") {
      offset2 = event.clientX - rootRef.current.getBoundingClientRect().right;
    } else if (scrollDirection === "right") {
      offset2 = Math.max(1, event.clientX - rootRef.current.getBoundingClientRect().left);
    } else {
      throw new Error("MUI X: Wrong drag direction");
    }
    offset2 = (offset2 - CLIFF) * SLOP + CLIFF;
    timeout2.start(0, () => {
      apiRef.current.scroll({
        left: scrollPosition.current.left + offset2,
        top: scrollPosition.current.top
      });
    });
  });
  return (0, import_jsx_runtime22.jsx)(GridScrollAreaContent, _extends({}, props, {
    ref: rootRef,
    getCanScrollMore,
    style,
    handleDragOver
  }));
}
function GridVerticalScrollAreaContent(props) {
  const {
    scrollDirection,
    scrollPosition
  } = props;
  const rootRef = React36.useRef(null);
  const apiRef = useGridApiContext();
  const timeout2 = useTimeout();
  const rowsMeta = useGridSelector(apiRef, gridRowsMetaSelector);
  const getCanScrollMore = () => {
    const dimensions = gridDimensionsSelector(apiRef);
    if (scrollDirection === "up") {
      return scrollPosition.current.top > 0;
    }
    if (scrollDirection === "down") {
      const totalRowsHeight = rowsMeta.currentPageTotalHeight || 0;
      const maxScrollTop = totalRowsHeight - dimensions.viewportInnerSize.height - (dimensions.hasScrollX ? dimensions.scrollbarSize : 0);
      return scrollPosition.current.top < maxScrollTop;
    }
    return false;
  };
  const rootProps = useGridRootProps();
  const totalHeaderHeight = getTotalHeaderHeight(apiRef, rootProps);
  const style = {
    top: scrollDirection === "up" ? totalHeaderHeight : void 0,
    bottom: scrollDirection === "down" ? 0 : void 0
  };
  const handleDragOver = useEventCallback_default((event) => {
    let offset2;
    event.preventDefault();
    if (scrollDirection === "up") {
      offset2 = event.clientY - rootRef.current.getBoundingClientRect().bottom;
    } else if (scrollDirection === "down") {
      offset2 = Math.max(1, event.clientY - rootRef.current.getBoundingClientRect().top);
    } else {
      throw new Error("MUI X: Wrong drag direction");
    }
    offset2 = (offset2 - CLIFF) * SLOP + CLIFF;
    timeout2.start(0, () => {
      apiRef.current.scroll({
        left: scrollPosition.current.left,
        top: scrollPosition.current.top + offset2
      });
    });
  });
  return (0, import_jsx_runtime22.jsx)(GridScrollAreaContent, _extends({}, props, {
    ref: rootRef,
    getCanScrollMore,
    style,
    handleDragOver
  }));
}
var GridScrollAreaContent = forwardRef2(function GridScrollAreaContent2(props, ref) {
  const {
    scrollDirection,
    getCanScrollMore,
    style,
    handleDragOver
  } = props;
  const apiRef = useGridApiContext();
  const [canScrollMore, setCanScrollMore] = React36.useState(getCanScrollMore);
  const rootProps = useGridRootProps();
  const ownerState = _extends({}, rootProps, {
    scrollDirection
  });
  const classes2 = useUtilityClasses11(ownerState);
  const handleScrolling = () => {
    setCanScrollMore(getCanScrollMore);
  };
  useGridEvent(apiRef, "scrollPositionChange", handleScrolling);
  if (!canScrollMore) {
    return null;
  }
  return (0, import_jsx_runtime22.jsx)(GridScrollAreaRawRoot, {
    ref,
    className: classes2.root,
    ownerState,
    onDragOver: handleDragOver,
    style
  });
});
if (true) GridScrollAreaContent.displayName = "GridScrollAreaContent";
var GridScrollArea = fastMemo(GridScrollAreaWrapper);

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridPrivateApiContext.js
var React37 = __toESM(require_react(), 1);
var GridPrivateApiContext = React37.createContext(void 0);
if (true) GridPrivateApiContext.displayName = "GridPrivateApiContext";
function useGridPrivateApiContext() {
  const privateApiRef = React37.useContext(GridPrivateApiContext);
  if (privateApiRef === void 0) {
    throw new Error(["MUI X: Could not find the Data Grid private context.", "It looks like you rendered your component outside of a DataGrid, DataGridPro or DataGridPremium parent component.", "This can also happen if you are bundling multiple versions of the Data Grid."].join("\n"));
  }
  return privateApiRef;
}

// node_modules/@mui/x-internals/esm/useRunOnce/useRunOnce.js
var React38 = __toESM(require_react(), 1);
var noop2 = () => {
};
var useRunOnce = (condition, effect4) => {
  const didRun = React38.useRef(false);
  useEnhancedEffect_default(() => {
    if (didRun.current || !condition) {
      return noop2;
    }
    didRun.current = true;
    return effect4();
  }, [didRun.current || condition]);
};

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridApiMethod.js
var React39 = __toESM(require_react(), 1);
function useGridApiMethod(privateApiRef, apiMethods, visibility) {
  const isFirstRender = React39.useRef(true);
  useEnhancedEffect_default(() => {
    isFirstRender.current = false;
    privateApiRef.current.register(visibility, apiMethods);
  }, [privateApiRef, visibility, apiMethods]);
  if (isFirstRender.current) {
    privateApiRef.current.register(visibility, apiMethods);
  }
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridLogger.js
var React40 = __toESM(require_react(), 1);
function useGridLogger(privateApiRef, name) {
  const logger = React40.useRef(null);
  if (logger.current) {
    return logger.current;
  }
  const newLogger = privateApiRef.current.getLogger(name);
  logger.current = newLogger;
  return newLogger;
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridNativeEventListener.js
var useGridNativeEventListener = (apiRef, ref, eventName, handler, options3) => {
  const logger = useGridLogger(apiRef, "useNativeEventListener");
  useGridEventPriority(apiRef, "rootMount", () => {
    const targetElement = ref();
    if (!targetElement || !eventName) {
      return void 0;
    }
    logger.debug(`Binding native ${eventName} event`);
    targetElement.addEventListener(eventName, handler, options3);
    return () => {
      logger.debug(`Clearing native ${eventName} event`);
      targetElement.removeEventListener(eventName, handler, options3);
    };
  });
};

// node_modules/@mui/x-internals/esm/useFirstRender/useFirstRender.js
var React41 = __toESM(require_react(), 1);
function useFirstRender(callback) {
  const isFirstRender = React41.useRef(true);
  if (isFirstRender.current) {
    isFirstRender.current = false;
    callback();
  }
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useRunOncePerLoop.js
var React42 = __toESM(require_react(), 1);
function useRunOncePerLoop(callback) {
  const scheduledCallbackRef = React42.useRef(null);
  const schedule = React42.useCallback((...args) => {
    if (!scheduledCallbackRef.current) {
      queueMicrotask(() => {
        if (scheduledCallbackRef.current) {
          scheduledCallbackRef.current();
        }
      });
    }
    scheduledCallbackRef.current = () => {
      scheduledCallbackRef.current = null;
      callback(...args);
    };
  }, [callback]);
  React42.useLayoutEffect(() => {
    if (scheduledCallbackRef.current) {
      scheduledCallbackRef.current();
    }
  });
  return {
    schedule,
    cancel: () => {
      if (scheduledCallbackRef.current) {
        scheduledCallbackRef.current = null;
        return true;
      }
      return false;
    }
  };
}

// node_modules/@mui/x-data-grid/esm/hooks/features/pivoting/gridPivotingSelectors.js
var gridPivotingStateSelector = createRootSelector(
  // @ts-ignore
  (state) => state.pivoting
);
var gridPivotActiveSelector = createSelector3(gridPivotingStateSelector, (pivoting) => pivoting == null ? void 0 : pivoting.active);
var emptyColumns = /* @__PURE__ */ new Map();
var gridPivotInitialColumnsSelector = createSelector3(gridPivotingStateSelector, (pivoting) => (pivoting == null ? void 0 : pivoting.initialColumns) || emptyColumns);

// node_modules/@mui/x-data-grid/esm/hooks/features/overlays/useGridOverlays.js
var useGridOverlays = (apiRef, props) => {
  var _a, _b;
  const totalRowCount = useGridSelector(apiRef, gridRowCountSelector);
  const visibleRowCount = useGridSelector(apiRef, gridExpandedRowCountSelector);
  const pinnedRowsCount = useGridSelector(apiRef, gridPinnedRowsCountSelector);
  const visibleColumns = useGridSelector(apiRef, gridVisibleColumnDefinitionsSelector);
  const noRows = totalRowCount === 0 && pinnedRowsCount === 0;
  const loading = useGridSelector(apiRef, gridRowsLoadingSelector);
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const showNoRowsOverlay = !loading && noRows;
  const showNoResultsOverlay = !loading && totalRowCount > 0 && visibleRowCount === 0;
  const showNoColumnsOverlay = !loading && visibleColumns.length === 0;
  const showEmptyPivotOverlay = showNoRowsOverlay && pivotActive;
  let overlayType = null;
  let loadingOverlayVariant = null;
  if (showNoRowsOverlay) {
    overlayType = "noRowsOverlay";
  }
  if (showNoColumnsOverlay) {
    overlayType = "noColumnsOverlay";
  }
  if (showEmptyPivotOverlay) {
    overlayType = "emptyPivotOverlay";
  }
  if (showNoResultsOverlay) {
    overlayType = "noResultsOverlay";
  }
  if (loading) {
    overlayType = "loadingOverlay";
    loadingOverlayVariant = ((_b = (_a = props.slotProps) == null ? void 0 : _a.loadingOverlay) == null ? void 0 : _b[noRows ? "noRowsVariant" : "variant"]) ?? (noRows ? "skeleton" : "linear-progress");
  }
  return {
    overlayType,
    loadingOverlayVariant
  };
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columnMenu/columnMenuSelector.js
var gridColumnMenuSelector = createRootSelector((state) => state.columnMenu);

// node_modules/@mui/x-data-grid/esm/components/GridHeaders.js
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
function GridHeaders() {
  var _a;
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const visibleColumns = useGridSelector(apiRef, gridVisibleColumnDefinitionsSelector);
  const filterColumnLookup = useGridSelector(apiRef, gridFilterActiveItemsLookupSelector);
  const sortColumnLookup = useGridSelector(apiRef, gridSortColumnLookupSelector);
  const columnHeaderTabIndexState = useGridSelector(apiRef, gridTabIndexColumnHeaderSelector);
  const hasNoCellTabIndexState = useGridSelector(apiRef, () => gridTabIndexCellSelector(apiRef) === null);
  const columnGroupHeaderTabIndexState = useGridSelector(apiRef, gridTabIndexColumnGroupHeaderSelector);
  const columnHeaderFocus = useGridSelector(apiRef, gridFocusColumnHeaderSelector);
  const columnGroupHeaderFocus = useGridSelector(apiRef, gridFocusColumnGroupHeaderSelector);
  const headerGroupingMaxDepth = useGridSelector(apiRef, gridColumnGroupsHeaderMaxDepthSelector);
  const columnMenuState = useGridSelector(apiRef, gridColumnMenuSelector);
  const columnVisibility = useGridSelector(apiRef, gridColumnVisibilityModelSelector);
  const columnGroupsHeaderStructure = useGridSelector(apiRef, gridColumnGroupsHeaderStructureSelector);
  const hasOtherElementInTabSequence = !(columnGroupHeaderTabIndexState === null && columnHeaderTabIndexState === null && hasNoCellTabIndexState);
  const columnsContainerRef = apiRef.current.columnHeadersContainerRef;
  return (0, import_jsx_runtime23.jsx)(rootProps.slots.columnHeaders, _extends({
    ref: columnsContainerRef,
    visibleColumns,
    filterColumnLookup,
    sortColumnLookup,
    columnHeaderTabIndexState,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure,
    hasOtherElementInTabSequence
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.columnHeaders));
}
var MemoizedGridHeaders = fastMemo(GridHeaders);

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridMainContainer.js
var React43 = __toESM(require_react(), 1);
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
var GridPanelAnchor = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "absolute",
  top: `var(--DataGrid-headersTotalHeight)`,
  left: 0,
  width: "calc(100% - (var(--DataGrid-hasScrollY) * var(--DataGrid-scrollbarSize)))"
});
var Element2 = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "Main",
  overridesResolver: (props, styles5) => {
    const {
      ownerState,
      loadingOverlayVariant,
      overlayType
    } = props;
    const hideContent = loadingOverlayVariant === "skeleton" || overlayType === "noColumnsOverlay";
    return [styles5.main, ownerState.hasPinnedRight && styles5["main--hasPinnedRight"], hideContent && styles5["main--hiddenContent"]];
  }
})({
  flexGrow: 1,
  position: "relative",
  overflow: "hidden",
  display: "flex",
  flexDirection: "column"
});
var GridMainContainer = forwardRef2((props, ref) => {
  var _a;
  const {
    ownerState
  } = props;
  const rootProps = useGridRootProps();
  const configuration4 = useGridConfiguration();
  const ariaAttributes = configuration4.hooks.useGridAriaAttributes();
  return (0, import_jsx_runtime24.jsxs)(Element2, _extends({
    ownerState,
    className: props.className,
    tabIndex: -1
  }, ariaAttributes, (_a = rootProps.slotProps) == null ? void 0 : _a.main, {
    ref,
    children: [(0, import_jsx_runtime24.jsx)(GridPanelAnchor, {
      role: "presentation",
      "data-id": "gridPanelAnchor"
    }), props.children]
  }));
});
if (true) GridMainContainer.displayName = "GridMainContainer";

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridTopContainer.js
var React44 = __toESM(require_react(), 1);
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses12 = () => {
  const slots = {
    root: ["topContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, {});
};
var Element3 = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "sticky",
  zIndex: 40,
  top: 0
});
function GridTopContainer(props) {
  const classes2 = useUtilityClasses12();
  return (0, import_jsx_runtime25.jsx)(Element3, _extends({}, props, {
    className: clsx_default(classes2.root, gridClasses["container--top"]),
    role: "presentation"
  }));
}

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScrollerContent.js
var React45 = __toESM(require_react(), 1);
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses13 = (props, overflowedContent) => {
  const {
    classes: classes2
  } = props;
  const slots = {
    root: ["virtualScrollerContent", overflowedContent && "virtualScrollerContent--overflowed"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var VirtualScrollerContentRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "VirtualScrollerContent",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.virtualScrollerContent, ownerState.overflowedContent && styles5["virtualScrollerContent--overflowed"]];
  }
})({});
var GridVirtualScrollerContent = forwardRef2(function GridVirtualScrollerContent2(props, ref) {
  var _a;
  const rootProps = useGridRootProps();
  const overflowedContent = !rootProps.autoHeight && ((_a = props.style) == null ? void 0 : _a.minHeight) === "auto";
  const classes2 = useUtilityClasses13(rootProps, overflowedContent);
  const ownerState = {
    classes: rootProps.classes,
    overflowedContent
  };
  return (0, import_jsx_runtime26.jsx)(VirtualScrollerContentRoot, _extends({}, props, {
    ownerState,
    className: clsx_default(classes2.root, props.className),
    ref
  }));
});
if (true) GridVirtualScrollerContent.displayName = "GridVirtualScrollerContent";

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScrollerFiller.js
var React46 = __toESM(require_react(), 1);
var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
var Filler = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  display: "flex",
  flexDirection: "row",
  width: "var(--DataGrid-rowWidth)",
  boxSizing: "border-box"
});
var Pinned = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "sticky",
  height: "100%",
  boxSizing: "border-box",
  borderTop: "1px solid var(--rowBorderColor)",
  backgroundColor: vars.cell.background.pinned
});
var PinnedLeft = styled_default2(Pinned, {
  slot: "internal"
})({
  left: 0
});
var PinnedRight = styled_default2(Pinned, {
  slot: "internal"
})({
  right: 0
});
var Main = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  flexGrow: 1,
  borderTop: "1px solid var(--rowBorderColor)"
});
function GridVirtualScrollerFiller({
  rowsLength
}) {
  const apiRef = useGridApiContext();
  const {
    viewportOuterSize,
    minimumSize,
    hasScrollX,
    hasScrollY,
    scrollbarSize,
    leftPinnedWidth,
    rightPinnedWidth
  } = useGridSelector(apiRef, gridDimensionsSelector);
  const height = hasScrollX ? scrollbarSize : 0;
  const needsLastRowBorder = viewportOuterSize.height - minimumSize.height > 0;
  if (height === 0 && !needsLastRowBorder) {
    return null;
  }
  return (0, import_jsx_runtime27.jsxs)(Filler, {
    className: gridClasses.filler,
    role: "presentation",
    style: {
      height,
      "--rowBorderColor": rowsLength === 0 ? "transparent" : "var(--DataGrid-rowBorderColor)"
    },
    children: [leftPinnedWidth > 0 && (0, import_jsx_runtime27.jsx)(PinnedLeft, {
      className: gridClasses["filler--pinnedLeft"],
      style: {
        width: leftPinnedWidth
      }
    }), (0, import_jsx_runtime27.jsx)(Main, {}), rightPinnedWidth > 0 && (0, import_jsx_runtime27.jsx)(PinnedRight, {
      className: gridClasses["filler--pinnedRight"],
      style: {
        width: rightPinnedWidth + (hasScrollY ? scrollbarSize : 0)
      }
    })]
  });
}
var Memoized = fastMemo(GridVirtualScrollerFiller);

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScrollerRenderZone.js
var React47 = __toESM(require_react(), 1);
var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses14 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["virtualScrollerRenderZone"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var VirtualScrollerRenderZoneRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "VirtualScrollerRenderZone"
})({
  position: "absolute",
  display: "flex",
  // Prevents margin collapsing when using `getRowSpacing`
  flexDirection: "column"
});
var GridVirtualScrollerRenderZone = forwardRef2(function GridVirtualScrollerRenderZone2(props, ref) {
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses14(rootProps);
  return (0, import_jsx_runtime28.jsx)(VirtualScrollerRenderZoneRoot, _extends({
    ownerState: rootProps
  }, props, {
    className: clsx_default(classes2.root, props.className),
    ref
  }));
});
if (true) GridVirtualScrollerRenderZone.displayName = "GridVirtualScrollerRenderZone";

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScrollbar.js
var React52 = __toESM(require_react(), 1);

// node_modules/@mui/utils/esm/useForkRef/useForkRef.js
var React48 = __toESM(require_react(), 1);
function useForkRef(...refs) {
  const cleanupRef = React48.useRef(void 0);
  const refEffect = React48.useCallback((instance) => {
    const cleanups = refs.map((ref) => {
      if (ref == null) {
        return null;
      }
      if (typeof ref === "function") {
        const refCallback = ref;
        const refCleanup = refCallback(instance);
        return typeof refCleanup === "function" ? refCleanup : () => {
          refCallback(null);
        };
      }
      ref.current = instance;
      return () => {
        ref.current = null;
      };
    });
    return () => {
      cleanups.forEach((refCleanup) => refCleanup == null ? void 0 : refCleanup());
    };
  }, refs);
  return React48.useMemo(() => {
    if (refs.every((ref) => ref == null)) {
      return null;
    }
    return (value) => {
      if (cleanupRef.current) {
        cleanupRef.current();
        cleanupRef.current = void 0;
      }
      if (value != null) {
        cleanupRef.current = refEffect(value);
      }
    };
  }, refs);
}

// node_modules/@mui/x-data-grid/esm/hooks/features/columnResize/columnResizeSelector.js
var gridColumnResizeSelector = createRootSelector((state) => state.columnResize);
var gridResizingColumnFieldSelector = createSelector3(gridColumnResizeSelector, (columnResize) => columnResize.resizingColumnField);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnResize/gridColumnResizeApi.js
var DEFAULT_GRID_AUTOSIZE_OPTIONS = {
  includeHeaders: true,
  includeHeaderFilters: false,
  includeOutliers: false,
  outliersFactor: 1.5,
  expand: false,
  disableColumnVirtualization: true
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/gridEditingSelectors.js
var gridEditRowsStateSelector = createRootSelector((state) => state.editRows);
var gridRowIsEditingSelector = createSelector3(gridEditRowsStateSelector, (editRows, {
  rowId,
  editMode
}) => editMode === GridEditModes.Row && Boolean(editRows[rowId]));
var gridEditCellStateSelector = createSelector3(gridEditRowsStateSelector, (editRows, {
  rowId,
  field
}) => {
  var _a;
  return ((_a = editRows[rowId]) == null ? void 0 : _a[field]) ?? null;
});

// node_modules/@mui/x-data-grid/esm/hooks/features/preferencesPanel/gridPreferencePanelSelector.js
var gridPreferencePanelStateSelector = createRootSelector((state) => state.preferencePanel);
var gridPreferencePanelSelectorWithLabel = createSelector3(gridPreferencePanelStateSelector, (panel, labelId) => {
  if (panel.open && panel.labelId === labelId) {
    return true;
  }
  return false;
});

// node_modules/@mui/x-data-grid/esm/hooks/features/preferencesPanel/gridPreferencePanelsValue.js
var GridPreferencePanelsValue = (function(GridPreferencePanelsValue2) {
  GridPreferencePanelsValue2["filters"] = "filters";
  GridPreferencePanelsValue2["columns"] = "columns";
  GridPreferencePanelsValue2["aiAssistant"] = "aiAssistant";
  return GridPreferencePanelsValue2;
})(GridPreferencePanelsValue || {});

// node_modules/@mui/x-virtualizer/esm/features/colspan.js
var selectors = {};
var Colspan = {
  initialize: initializeState,
  use: useColspan,
  selectors
};
function initializeState(_params) {
  return {
    colspanMap: /* @__PURE__ */ new Map()
  };
}
function useColspan(store, params, api) {
  var _a;
  const getColspan = (_a = params.colspan) == null ? void 0 : _a.getColspan;
  const resetColSpan = () => {
    store.state.colspanMap = /* @__PURE__ */ new Map();
  };
  const getCellColSpanInfo = (rowId, columnIndex) => {
    var _a2;
    return (_a2 = store.state.colspanMap.get(rowId)) == null ? void 0 : _a2[columnIndex];
  };
  const calculateColSpan = useEventCallback_default(getColspan ? (rowId, minFirstColumn, maxLastColumn, columns) => {
    for (let i2 = minFirstColumn; i2 < maxLastColumn; i2 += 1) {
      const cellProps = calculateCellColSpan(store.state.colspanMap, i2, rowId, minFirstColumn, maxLastColumn, columns, getColspan);
      if (cellProps.colSpan > 1) {
        i2 += cellProps.colSpan - 1;
      }
    }
  } : () => {
  });
  api.calculateColSpan = calculateColSpan;
  return {
    resetColSpan,
    getCellColSpanInfo,
    calculateColSpan
  };
}
function calculateCellColSpan(lookup, columnIndex, rowId, minFirstColumnIndex, maxLastColumnIndex, columns, getColspan) {
  const columnsLength = columns.length;
  const column = columns[columnIndex];
  const colSpan = getColspan(rowId, column, columnIndex);
  if (!colSpan || colSpan === 1) {
    setCellColSpanInfo(lookup, rowId, columnIndex, {
      spannedByColSpan: false,
      cellProps: {
        colSpan: 1,
        width: column.computedWidth
      }
    });
    return {
      colSpan: 1
    };
  }
  let width = column.computedWidth;
  for (let j = 1; j < colSpan; j += 1) {
    const nextColumnIndex = columnIndex + j;
    if (nextColumnIndex >= minFirstColumnIndex && nextColumnIndex < maxLastColumnIndex) {
      const nextColumn = columns[nextColumnIndex];
      width += nextColumn.computedWidth;
      setCellColSpanInfo(lookup, rowId, columnIndex + j, {
        spannedByColSpan: true,
        rightVisibleCellIndex: Math.min(columnIndex + colSpan, columnsLength - 1),
        leftVisibleCellIndex: columnIndex
      });
    }
    setCellColSpanInfo(lookup, rowId, columnIndex, {
      spannedByColSpan: false,
      cellProps: {
        colSpan,
        width
      }
    });
  }
  return {
    colSpan
  };
}
function setCellColSpanInfo(colspanMap, rowId, columnIndex, cellColSpanInfo) {
  let columnInfo = colspanMap.get(rowId);
  if (!columnInfo) {
    columnInfo = {};
    colspanMap.set(rowId, columnInfo);
  }
  columnInfo[columnIndex] = cellColSpanInfo;
}

// node_modules/@mui/x-virtualizer/esm/features/dimensions.js
var React49 = __toESM(require_react(), 1);

// node_modules/@mui/utils/esm/ownerDocument/ownerDocument.js
function ownerDocument(node) {
  return node && node.ownerDocument || document;
}

// node_modules/@mui/x-internals/esm/throttle/throttle.js
function throttle(func, wait = 166) {
  let timeout2;
  let lastArgs;
  const later = () => {
    timeout2 = void 0;
    func(...lastArgs);
  };
  function throttled(...args) {
    lastArgs = args;
    if (timeout2 === void 0) {
      timeout2 = setTimeout(later, wait);
    }
  }
  throttled.clear = () => {
    clearTimeout(timeout2);
    timeout2 = void 0;
  };
  return throttled;
}

// node_modules/@mui/x-internals/esm/isDeepEqual/isDeepEqual.js
function isDeepEqual(a, b) {
  if (a === b) {
    return true;
  }
  if (a && b && typeof a === "object" && typeof b === "object") {
    if (a.constructor !== b.constructor) {
      return false;
    }
    if (Array.isArray(a)) {
      const length2 = a.length;
      if (length2 !== b.length) {
        return false;
      }
      for (let i2 = 0; i2 < length2; i2 += 1) {
        if (!isDeepEqual(a[i2], b[i2])) {
          return false;
        }
      }
      return true;
    }
    if (a instanceof Map && b instanceof Map) {
      if (a.size !== b.size) {
        return false;
      }
      const entriesA = Array.from(a.entries());
      for (let i2 = 0; i2 < entriesA.length; i2 += 1) {
        if (!b.has(entriesA[i2][0])) {
          return false;
        }
      }
      for (let i2 = 0; i2 < entriesA.length; i2 += 1) {
        const entryA = entriesA[i2];
        if (!isDeepEqual(entryA[1], b.get(entryA[0]))) {
          return false;
        }
      }
      return true;
    }
    if (a instanceof Set && b instanceof Set) {
      if (a.size !== b.size) {
        return false;
      }
      const entries = Array.from(a.entries());
      for (let i2 = 0; i2 < entries.length; i2 += 1) {
        if (!b.has(entries[i2][0])) {
          return false;
        }
      }
      return true;
    }
    if (ArrayBuffer.isView(a) && ArrayBuffer.isView(b)) {
      const length2 = a.length;
      if (length2 !== b.length) {
        return false;
      }
      for (let i2 = 0; i2 < length2; i2 += 1) {
        if (a[i2] !== b[i2]) {
          return false;
        }
      }
      return true;
    }
    if (a.constructor === RegExp) {
      return a.source === b.source && a.flags === b.flags;
    }
    if (a.valueOf !== Object.prototype.valueOf) {
      return a.valueOf() === b.valueOf();
    }
    if (a.toString !== Object.prototype.toString) {
      return a.toString() === b.toString();
    }
    const keys2 = Object.keys(a);
    const length = keys2.length;
    if (length !== Object.keys(b).length) {
      return false;
    }
    for (let i2 = 0; i2 < length; i2 += 1) {
      if (!Object.prototype.hasOwnProperty.call(b, keys2[i2])) {
        return false;
      }
    }
    for (let i2 = 0; i2 < length; i2 += 1) {
      const key = keys2[i2];
      if (!isDeepEqual(a[key], b[key])) {
        return false;
      }
    }
    return true;
  }
  return a !== a && b !== b;
}

// node_modules/@mui/x-internals/esm/math/index.js
function roundToDecimalPlaces(value, decimals) {
  return Math.round(value * 10 ** decimals) / 10 ** decimals;
}

// node_modules/@mui/x-virtualizer/esm/models/core.js
var Size = {
  EMPTY: {
    width: 0,
    height: 0
  },
  equals: (a, b) => a.width === b.width && a.height === b.height
};
var PinnedRows = {
  EMPTY: {
    top: [],
    bottom: []
  }
};
var PinnedColumns = {
  EMPTY: {
    left: [],
    right: []
  }
};
var ScrollPosition = {
  EMPTY: {
    top: 0,
    left: 0
  },
  equals: (a, b) => a.top === b.top && a.left === b.left
};
var ScrollDirection = (function(ScrollDirection2) {
  ScrollDirection2[ScrollDirection2["NONE"] = 0] = "NONE";
  ScrollDirection2[ScrollDirection2["UP"] = 1] = "UP";
  ScrollDirection2[ScrollDirection2["DOWN"] = 2] = "DOWN";
  ScrollDirection2[ScrollDirection2["LEFT"] = 3] = "LEFT";
  ScrollDirection2[ScrollDirection2["RIGHT"] = 4] = "RIGHT";
  return ScrollDirection2;
})({});
(function(_ScrollDirection) {
  function forDelta(dx, dy) {
    if (dx === 0 && dy === 0) {
      return ScrollDirection.NONE;
    }
    if (Math.abs(dy) >= Math.abs(dx)) {
      if (dy > 0) {
        return ScrollDirection.DOWN;
      } else {
        return ScrollDirection.UP;
      }
    } else {
      if (dx > 0) {
        return ScrollDirection.RIGHT;
      } else {
        return ScrollDirection.LEFT;
      }
    }
  }
  _ScrollDirection.forDelta = forDelta;
})(ScrollDirection || (ScrollDirection = {}));

// node_modules/@mui/x-virtualizer/esm/features/dimensions.js
var EMPTY_DIMENSIONS = {
  isReady: false,
  root: Size.EMPTY,
  viewportOuterSize: Size.EMPTY,
  viewportInnerSize: Size.EMPTY,
  contentSize: Size.EMPTY,
  minimumSize: Size.EMPTY,
  hasScrollX: false,
  hasScrollY: false,
  scrollbarSize: 0,
  rowWidth: 0,
  rowHeight: 0,
  columnsTotalWidth: 0,
  leftPinnedWidth: 0,
  rightPinnedWidth: 0,
  topContainerHeight: 0,
  bottomContainerHeight: 0,
  autoHeight: false,
  minimalContentHeight: void 0
};
var selectors2 = {
  rootSize: (state) => state.rootSize,
  dimensions: (state) => state.dimensions,
  rowHeight: (state) => state.dimensions.rowHeight,
  columnsTotalWidth: (state) => state.dimensions.columnsTotalWidth,
  contentHeight: (state) => state.dimensions.contentSize.height,
  autoHeight: (state) => state.dimensions.autoHeight,
  minimalContentHeight: (state) => state.dimensions.minimalContentHeight,
  rowsMeta: (state) => state.rowsMeta,
  rowPositions: (state) => state.rowsMeta.positions,
  columnPositions: createSelectorMemoized((_, columns) => {
    const positions = [];
    let currentPosition = 0;
    for (let i2 = 0; i2 < columns.length; i2 += 1) {
      positions.push(currentPosition);
      currentPosition += columns[i2].computedWidth;
    }
    return positions;
  }),
  needsHorizontalScrollbar: (state) => state.dimensions.viewportOuterSize.width > 0 && state.dimensions.columnsTotalWidth > state.dimensions.viewportOuterSize.width
};
var Dimensions = {
  initialize: initializeState2,
  use: useDimensions,
  selectors: selectors2
};
function initializeState2(params) {
  const dimensions = _extends({}, EMPTY_DIMENSIONS, params.dimensions, {
    autoHeight: params.dimensions.autoHeight,
    minimalContentHeight: params.dimensions.minimalContentHeight
  });
  const {
    rowCount
  } = params;
  const {
    rowHeight
  } = dimensions;
  const rowsMeta = {
    currentPageTotalHeight: rowCount * rowHeight,
    positions: Array.from({
      length: rowCount
    }, (_, i2) => i2 * rowHeight),
    pinnedTopRowsTotalHeight: 0,
    pinnedBottomRowsTotalHeight: 0
  };
  const rowHeights = /* @__PURE__ */ new Map();
  return {
    rootSize: Size.EMPTY,
    dimensions,
    rowsMeta,
    rowHeights
  };
}
function useDimensions(store, params, _api) {
  const isFirstSizing = React49.useRef(true);
  const {
    layout,
    dimensions: {
      rowHeight,
      columnsTotalWidth,
      leftPinnedWidth,
      rightPinnedWidth,
      topPinnedHeight,
      bottomPinnedHeight
    },
    onResize
  } = params;
  const updateDimensions = React49.useCallback((firstUpdate) => {
    if (firstUpdate) {
      isFirstSizing.current = false;
    }
    if (isFirstSizing.current) {
      return;
    }
    const containerNode = layout.refs.container.current;
    const rootSize = selectors2.rootSize(store.state);
    const rowsMeta2 = selectors2.rowsMeta(store.state);
    const scrollbarSize = measureScrollbarSize(containerNode, params.dimensions.scrollbarSize);
    const topContainerHeight = topPinnedHeight + rowsMeta2.pinnedTopRowsTotalHeight;
    const bottomContainerHeight = bottomPinnedHeight + rowsMeta2.pinnedBottomRowsTotalHeight;
    const contentSize = {
      width: columnsTotalWidth,
      height: roundToDecimalPlaces(rowsMeta2.currentPageTotalHeight, 1)
    };
    let viewportOuterSize;
    let viewportInnerSize;
    let hasScrollX = false;
    let hasScrollY = false;
    if (params.dimensions.autoHeight) {
      hasScrollY = false;
      hasScrollX = Math.round(columnsTotalWidth) > Math.round(rootSize.width);
      viewportOuterSize = {
        width: rootSize.width,
        height: topContainerHeight + bottomContainerHeight + contentSize.height
      };
      viewportInnerSize = {
        width: Math.max(0, viewportOuterSize.width - (hasScrollY ? scrollbarSize : 0)),
        height: Math.max(0, viewportOuterSize.height - (hasScrollX ? scrollbarSize : 0))
      };
    } else {
      viewportOuterSize = {
        width: rootSize.width,
        height: rootSize.height
      };
      viewportInnerSize = {
        width: Math.max(0, viewportOuterSize.width),
        height: Math.max(0, viewportOuterSize.height - topContainerHeight - bottomContainerHeight)
      };
      const content = contentSize;
      const container = viewportInnerSize;
      const hasScrollXIfNoYScrollBar = content.width > container.width;
      const hasScrollYIfNoXScrollBar = content.height > container.height;
      if (hasScrollXIfNoYScrollBar || hasScrollYIfNoXScrollBar) {
        hasScrollY = hasScrollYIfNoXScrollBar;
        hasScrollX = content.width + (hasScrollY ? scrollbarSize : 0) > container.width;
        if (hasScrollX) {
          hasScrollY = content.height + scrollbarSize > container.height;
        }
      }
      if (hasScrollY) {
        viewportInnerSize.width -= scrollbarSize;
      }
      if (hasScrollX) {
        viewportInnerSize.height -= scrollbarSize;
      }
    }
    if (params.disableHorizontalScroll) {
      hasScrollX = false;
    }
    if (params.disableVerticalScroll) {
      hasScrollY = false;
    }
    const rowWidth = Math.max(viewportOuterSize.width, columnsTotalWidth + (hasScrollY ? scrollbarSize : 0));
    const minimumSize = {
      width: columnsTotalWidth,
      height: topContainerHeight + contentSize.height + bottomContainerHeight
    };
    const newDimensions = {
      isReady: true,
      root: rootSize,
      viewportOuterSize,
      viewportInnerSize,
      contentSize,
      minimumSize,
      hasScrollX,
      hasScrollY,
      scrollbarSize,
      rowWidth,
      rowHeight,
      columnsTotalWidth,
      leftPinnedWidth,
      rightPinnedWidth,
      topContainerHeight,
      bottomContainerHeight,
      autoHeight: params.dimensions.autoHeight,
      minimalContentHeight: params.dimensions.minimalContentHeight
    };
    const prevDimensions = store.state.dimensions;
    if (isDeepEqual(prevDimensions, newDimensions)) {
      return;
    }
    store.update({
      dimensions: newDimensions
    });
    onResize == null ? void 0 : onResize(newDimensions.root);
  }, [store, layout.refs.container, params.dimensions.scrollbarSize, params.dimensions.autoHeight, params.dimensions.minimalContentHeight, params.disableHorizontalScroll, params.disableVerticalScroll, onResize, rowHeight, columnsTotalWidth, leftPinnedWidth, rightPinnedWidth, topPinnedHeight, bottomPinnedHeight]);
  const {
    resizeThrottleMs
  } = params;
  const updateDimensionCallback = useEventCallback_default(updateDimensions);
  const debouncedUpdateDimensions = React49.useMemo(() => resizeThrottleMs > 0 ? throttle(updateDimensionCallback, resizeThrottleMs) : void 0, [resizeThrottleMs, updateDimensionCallback]);
  React49.useEffect(() => debouncedUpdateDimensions == null ? void 0 : debouncedUpdateDimensions.clear, [debouncedUpdateDimensions]);
  useEnhancedEffect_default(updateDimensions, [updateDimensions]);
  useEnhancedEffect_default(() => {
    store.update({
      dimensions: _extends({}, store.state.dimensions, {
        autoHeight: params.dimensions.autoHeight,
        minimalContentHeight: params.dimensions.minimalContentHeight
      })
    });
  }, [store, params.dimensions.autoHeight, params.dimensions.minimalContentHeight]);
  const rowsMeta = useRowsMeta(store, params, updateDimensions);
  return {
    updateDimensions,
    debouncedUpdateDimensions,
    rowsMeta
  };
}
function useRowsMeta(store, params, updateDimensions) {
  const heightCache = store.state.rowHeights;
  const {
    rows,
    getRowHeight: getRowHeightProp,
    getRowSpacing,
    getEstimatedRowHeight
  } = params;
  const lastMeasuredRowIndex = React49.useRef(-1);
  const hasRowWithAutoHeight = React49.useRef(false);
  const isHeightMetaValid = React49.useRef(false);
  const pinnedRows = params.pinnedRows;
  const rowHeight = useStore(store, selectors2.rowHeight);
  const getRowHeightEntry = useEventCallback_default((rowId) => {
    let entry = heightCache.get(rowId);
    if (entry === void 0) {
      entry = {
        content: store.state.dimensions.rowHeight,
        spacingTop: 0,
        spacingBottom: 0,
        detail: 0,
        autoHeight: false,
        needsFirstMeasurement: true
      };
      heightCache.set(rowId, entry);
    }
    return entry;
  });
  const {
    applyRowHeight
  } = params;
  const processHeightEntry = React49.useCallback((row) => {
    eslintUseValue2(rowHeight);
    const dimensions = selectors2.dimensions(store.state);
    const baseRowHeight = dimensions.rowHeight;
    const entry = getRowHeightEntry(row.id);
    if (!getRowHeightProp) {
      entry.content = baseRowHeight;
      entry.needsFirstMeasurement = false;
    } else {
      const rowHeightFromUser = getRowHeightProp(row);
      if (rowHeightFromUser === "auto") {
        if (entry.needsFirstMeasurement) {
          const estimatedRowHeight = getEstimatedRowHeight ? getEstimatedRowHeight(row) : baseRowHeight;
          entry.content = estimatedRowHeight ?? baseRowHeight;
        }
        hasRowWithAutoHeight.current = true;
        entry.autoHeight = true;
      } else {
        entry.content = rowHeightFromUser ?? dimensions.rowHeight;
        entry.needsFirstMeasurement = false;
        entry.autoHeight = false;
      }
    }
    if (getRowSpacing) {
      const spacing2 = getRowSpacing(row);
      entry.spacingTop = spacing2.top ?? 0;
      entry.spacingBottom = spacing2.bottom ?? 0;
    } else {
      entry.spacingTop = 0;
      entry.spacingBottom = 0;
    }
    applyRowHeight == null ? void 0 : applyRowHeight(entry, row);
    return entry;
  }, [store, getRowHeightProp, getRowHeightEntry, getEstimatedRowHeight, rowHeight, getRowSpacing, applyRowHeight]);
  const hydrateRowsMeta = React49.useCallback(() => {
    hasRowWithAutoHeight.current = false;
    const pinnedTopRowsTotalHeight = (pinnedRows == null ? void 0 : pinnedRows.top.reduce((acc, row) => {
      const entry = processHeightEntry(row);
      return acc + entry.content + entry.spacingTop + entry.spacingBottom + entry.detail;
    }, 0)) ?? 0;
    const pinnedBottomRowsTotalHeight = (pinnedRows == null ? void 0 : pinnedRows.bottom.reduce((acc, row) => {
      const entry = processHeightEntry(row);
      return acc + entry.content + entry.spacingTop + entry.spacingBottom + entry.detail;
    }, 0)) ?? 0;
    const positions = [];
    const currentPageTotalHeight = rows.reduce((acc, row) => {
      positions.push(acc);
      const entry = processHeightEntry(row);
      const total = entry.content + entry.spacingTop + entry.spacingBottom + entry.detail;
      return acc + total;
    }, 0);
    if (!hasRowWithAutoHeight.current) {
      lastMeasuredRowIndex.current = Infinity;
    }
    const didHeightsChange = pinnedTopRowsTotalHeight !== store.state.rowsMeta.pinnedTopRowsTotalHeight || pinnedBottomRowsTotalHeight !== store.state.rowsMeta.pinnedBottomRowsTotalHeight || currentPageTotalHeight !== store.state.rowsMeta.currentPageTotalHeight;
    const rowsMeta = {
      currentPageTotalHeight,
      positions,
      pinnedTopRowsTotalHeight,
      pinnedBottomRowsTotalHeight
    };
    store.set("rowsMeta", rowsMeta);
    if (didHeightsChange) {
      updateDimensions();
    }
    isHeightMetaValid.current = true;
  }, [store, pinnedRows, rows, processHeightEntry, updateDimensions]);
  const hydrateRowsMetaLatest = useEventCallback_default(hydrateRowsMeta);
  const getRowHeight = (rowId) => {
    var _a;
    return ((_a = heightCache.get(rowId)) == null ? void 0 : _a.content) ?? selectors2.rowHeight(store.state);
  };
  const storeRowHeightMeasurement = (id, height) => {
    const entry = getRowHeightEntry(id);
    const didChange = entry.content !== height;
    entry.needsFirstMeasurement = false;
    entry.content = height;
    isHeightMetaValid.current && (isHeightMetaValid.current = !didChange);
  };
  const rowHasAutoHeight = (id) => {
    var _a;
    return ((_a = heightCache.get(id)) == null ? void 0 : _a.autoHeight) ?? false;
  };
  const getLastMeasuredRowIndex = () => {
    return lastMeasuredRowIndex.current;
  };
  const setLastMeasuredRowIndex = (index) => {
    if (hasRowWithAutoHeight.current && index > lastMeasuredRowIndex.current) {
      lastMeasuredRowIndex.current = index;
    }
  };
  const resetRowHeights = () => {
    heightCache.clear();
    hydrateRowsMeta();
  };
  const resizeObserver = useLazyRef(() => typeof ResizeObserver === "undefined" ? void 0 : new ResizeObserver((entries) => {
    var _a, _b;
    for (let i2 = 0; i2 < entries.length; i2 += 1) {
      const entry = entries[i2];
      const height = entry.borderBoxSize && entry.borderBoxSize.length > 0 ? entry.borderBoxSize[0].blockSize : entry.contentRect.height;
      const rowId = entry.target.__mui_id;
      const focusedVirtualRowId = (_b = (_a = params.focusedVirtualCell) == null ? void 0 : _a.call(params)) == null ? void 0 : _b.id;
      if (focusedVirtualRowId === rowId && height === 0) {
        return;
      }
      storeRowHeightMeasurement(rowId, height);
    }
    if (!isHeightMetaValid.current) {
      requestAnimationFrame(() => {
        hydrateRowsMetaLatest();
      });
    }
  })).current;
  const observeRowHeight = (element, rowId) => {
    element.__mui_id = rowId;
    resizeObserver == null ? void 0 : resizeObserver.observe(element);
    return () => resizeObserver == null ? void 0 : resizeObserver.unobserve(element);
  };
  useEnhancedEffect_default(() => {
    hydrateRowsMeta();
  }, [hydrateRowsMeta]);
  return {
    getRowHeight,
    setLastMeasuredRowIndex,
    storeRowHeightMeasurement,
    hydrateRowsMeta,
    observeRowHeight,
    rowHasAutoHeight,
    getRowHeightEntry,
    getLastMeasuredRowIndex,
    resetRowHeights
  };
}
function observeRootNode(node, store, setRootSize) {
  if (!node) {
    return void 0;
  }
  const bounds = node.getBoundingClientRect();
  const initialSize = {
    width: roundToDecimalPlaces(bounds.width, 1),
    height: roundToDecimalPlaces(bounds.height, 1)
  };
  if (store.state.rootSize === Size.EMPTY || !Size.equals(initialSize, store.state.rootSize)) {
    setRootSize(initialSize);
  }
  if (typeof ResizeObserver === "undefined") {
    return void 0;
  }
  const observer = new ResizeObserver(([entry]) => {
    if (!entry) {
      return;
    }
    const rootSize = {
      width: roundToDecimalPlaces(entry.contentRect.width, 1),
      height: roundToDecimalPlaces(entry.contentRect.height, 1)
    };
    if (!Size.equals(rootSize, store.state.rootSize)) {
      setRootSize(rootSize);
    }
  });
  observer.observe(node);
  return () => {
    observer.disconnect();
  };
}
var scrollbarSizeCache = /* @__PURE__ */ new WeakMap();
function measureScrollbarSize(element, scrollbarSize) {
  if (scrollbarSize !== void 0) {
    return scrollbarSize;
  }
  if (element === null) {
    return 0;
  }
  const cachedSize = scrollbarSizeCache.get(element);
  if (cachedSize !== void 0) {
    return cachedSize;
  }
  const doc = ownerDocument(element);
  const scrollDiv = doc.createElement("div");
  scrollDiv.style.width = "99px";
  scrollDiv.style.height = "99px";
  scrollDiv.style.position = "absolute";
  scrollDiv.style.overflow = "scroll";
  scrollDiv.className = "scrollDiv";
  element.appendChild(scrollDiv);
  const size = scrollDiv.offsetWidth - scrollDiv.clientWidth;
  element.removeChild(scrollDiv);
  scrollbarSizeCache.set(element, size);
  return size;
}
function eslintUseValue2(_) {
}

// node_modules/@mui/x-internals/esm/platform/index.js
var userAgent = typeof navigator !== "undefined" ? navigator.userAgent.toLowerCase() : "empty";
var isFirefox = userAgent.includes("firefox");
var isJSDOM = typeof window !== "undefined" && /jsdom|HappyDOM/.test(window.navigator.userAgent);

// node_modules/@mui/x-virtualizer/esm/features/virtualization/virtualization.js
var React50 = __toESM(require_react(), 1);
var ReactDOM = __toESM(require_react_dom(), 1);
var clamp2 = (value, min2, max2) => Math.max(min2, Math.min(max2, value));
var MINIMUM_COLUMN_WIDTH = 50;
var EMPTY_SCROLL_POSITION = {
  top: 0,
  left: 0
};
var EMPTY_DETAIL_PANELS = Object.freeze(/* @__PURE__ */ new Map());
var EMPTY_RENDER_CONTEXT = {
  firstRowIndex: 0,
  lastRowIndex: 0,
  firstColumnIndex: 0,
  lastColumnIndex: 0
};
var selectors3 = (() => {
  const firstRowIndexSelector = createSelector2((state) => state.virtualization.renderContext.firstRowIndex);
  return {
    store: createSelector2((state) => state.virtualization),
    renderContext: createSelector2((state) => state.virtualization.renderContext),
    enabledForRows: createSelector2((state) => state.virtualization.enabledForRows),
    enabledForColumns: createSelector2((state) => state.virtualization.enabledForColumns),
    offsetTop: createSelector2(Dimensions.selectors.rowPositions, firstRowIndexSelector, (rowPositions, firstRowIndex) => rowPositions[firstRowIndex] ?? 0),
    context: createSelector2((state) => state.virtualization.context),
    scrollPosition: createSelector2((state) => state.virtualization.scrollPosition)
  };
})();
var Virtualization = {
  initialize: initializeState3,
  use: useVirtualization,
  selectors: selectors3
};
function initializeState3(params) {
  var _a;
  const state = {
    virtualization: _extends({
      enabled: !isJSDOM,
      enabledForRows: !isJSDOM,
      enabledForColumns: !isJSDOM,
      renderContext: EMPTY_RENDER_CONTEXT,
      props: params.layout.constructor.elements.reduce((acc, key) => (acc[key], acc), {}),
      context: {},
      scrollPosition: {
        current: ScrollPosition.EMPTY
      }
    }, (_a = params.initialState) == null ? void 0 : _a.virtualization),
    // FIXME: refactor once the state shape is settled
    getters: null
  };
  return state;
}
function useVirtualization(store, params, api) {
  const {
    layout,
    dimensions: {
      rowHeight,
      columnsTotalWidth = 0
    },
    virtualization: {
      isRtl = false,
      rowBufferPx = 150,
      columnBufferPx = 150
    },
    colspan,
    initialState,
    rows,
    range,
    columns,
    pinnedRows = PinnedRows.EMPTY,
    pinnedColumns = PinnedColumns.EMPTY,
    onWheel,
    onTouchMove,
    onRenderContextChange,
    onScrollChange,
    scrollReset,
    renderRow,
    renderInfiniteLoadingTrigger
  } = params;
  const hasBottomPinnedRows = pinnedRows.bottom.length > 0;
  const [panels, setPanels] = React50.useState(EMPTY_DETAIL_PANELS);
  const isUpdateScheduled = React50.useRef(false);
  const isRenderContextReady = React50.useRef(false);
  const renderContext = useStore(store, selectors3.renderContext);
  const enabledForRows = useStore(store, selectors3.enabledForRows);
  const enabledForColumns = useStore(store, selectors3.enabledForColumns);
  const contentHeight = useStore(store, Dimensions.selectors.contentHeight);
  const scrollPosition = React50.useRef((initialState == null ? void 0 : initialState.scroll) ?? EMPTY_SCROLL_POSITION);
  const ignoreNextScrollEvent = React50.useRef(false);
  const previousContextScrollPosition = React50.useRef(EMPTY_SCROLL_POSITION);
  const previousRowContext = React50.useRef(EMPTY_RENDER_CONTEXT);
  const scrollTimeout = useTimeout();
  const frozenContext = React50.useRef(void 0);
  const scrollCache = useLazyRef(() => createScrollCache(isRtl, rowBufferPx, columnBufferPx, rowHeight * 15, MINIMUM_COLUMN_WIDTH * 6)).current;
  const updateRenderContext = React50.useCallback((nextRenderContext) => {
    if (!areRenderContextsEqual(nextRenderContext, store.state.virtualization.renderContext)) {
      store.set("virtualization", _extends({}, store.state.virtualization, {
        renderContext: nextRenderContext,
        scrollPosition: {
          current: _extends({}, scrollPosition.current)
        }
      }));
    }
    const isReady = Dimensions.selectors.dimensions(store.state).isReady;
    const didRowsIntervalChange = nextRenderContext.firstRowIndex !== previousRowContext.current.firstRowIndex || nextRenderContext.lastRowIndex !== previousRowContext.current.lastRowIndex;
    if (isReady && didRowsIntervalChange) {
      previousRowContext.current = nextRenderContext;
      onRenderContextChange == null ? void 0 : onRenderContextChange(nextRenderContext);
    }
    previousContextScrollPosition.current = scrollPosition.current;
  }, [store, onRenderContextChange]);
  const triggerUpdateRenderContext = useEventCallback_default(() => {
    const scroller = layout.refs.scroller.current;
    if (!scroller) {
      return void 0;
    }
    const dimensions = Dimensions.selectors.dimensions(store.state);
    const maxScrollTop = Math.ceil(dimensions.contentSize.height - dimensions.viewportInnerSize.height);
    const maxScrollLeft = Math.ceil(dimensions.contentSize.width - dimensions.viewportInnerSize.width);
    const newScroll = {
      top: clamp2(scroller.scrollTop, 0, maxScrollTop),
      left: isRtl ? clamp2(scroller.scrollLeft, -Math.abs(maxScrollLeft), 0) : clamp2(scroller.scrollLeft, 0, maxScrollLeft)
    };
    const dx = newScroll.left - scrollPosition.current.left;
    const dy = newScroll.top - scrollPosition.current.top;
    const isScrolling = dx !== 0 || dy !== 0;
    scrollPosition.current = newScroll;
    const direction = isScrolling ? ScrollDirection.forDelta(dx, dy) : ScrollDirection.NONE;
    const rowScroll = Math.abs(scrollPosition.current.top - previousContextScrollPosition.current.top);
    const columnScroll = Math.abs(scrollPosition.current.left - previousContextScrollPosition.current.left);
    const didCrossThreshold = rowScroll >= rowHeight || columnScroll >= MINIMUM_COLUMN_WIDTH;
    const didChangeDirection = scrollCache.direction !== direction;
    const shouldUpdate = didCrossThreshold || didChangeDirection;
    if (!shouldUpdate) {
      store.set("virtualization", _extends({}, store.state.virtualization, {
        scrollPosition: {
          current: _extends({}, scrollPosition.current)
        }
      }));
      return renderContext;
    }
    if (didChangeDirection) {
      switch (direction) {
        case ScrollDirection.NONE:
        case ScrollDirection.LEFT:
        case ScrollDirection.RIGHT:
          frozenContext.current = void 0;
          break;
        default:
          frozenContext.current = renderContext;
          break;
      }
    }
    scrollCache.direction = direction;
    scrollCache.buffer = bufferForDirection(isRtl, direction, rowBufferPx, columnBufferPx, rowHeight * 15, MINIMUM_COLUMN_WIDTH * 6);
    const inputs = inputsSelector(store, params, api, enabledForRows, enabledForColumns);
    const nextRenderContext = computeRenderContext(inputs, scrollPosition.current, scrollCache);
    if (!areRenderContextsEqual(nextRenderContext, renderContext)) {
      ReactDOM.flushSync(() => {
        updateRenderContext(nextRenderContext);
      });
      scrollTimeout.start(1e3, triggerUpdateRenderContext);
    } else {
      store.set("virtualization", _extends({}, store.state.virtualization, {
        scrollPosition: {
          current: _extends({}, scrollPosition.current)
        }
      }));
    }
    return nextRenderContext;
  });
  const forceUpdateRenderContext = () => {
    if (!Dimensions.selectors.dimensions(store.state).isReady && (enabledForRows || enabledForColumns)) {
      return;
    }
    const inputs = inputsSelector(store, params, api, enabledForRows, enabledForColumns);
    const nextRenderContext = computeRenderContext(inputs, scrollPosition.current, scrollCache);
    frozenContext.current = void 0;
    updateRenderContext(nextRenderContext);
  };
  const forceUpdateRenderContextCallback = useEventCallback_default(forceUpdateRenderContext);
  useStoreEffect(store, Dimensions.selectors.dimensions, (previous, next) => {
    if (next.isReady) {
      forceUpdateRenderContext();
    }
  });
  useEnhancedEffect_default(() => {
    if (isUpdateScheduled.current) {
      forceUpdateRenderContext();
      isUpdateScheduled.current = false;
    }
  });
  const scheduleUpdateRenderContext = () => {
    isUpdateScheduled.current = true;
  };
  const handleScroll = useEventCallback_default(() => {
    if (ignoreNextScrollEvent.current) {
      ignoreNextScrollEvent.current = false;
      return;
    }
    const nextRenderContext = triggerUpdateRenderContext();
    if (nextRenderContext) {
      onScrollChange == null ? void 0 : onScrollChange(scrollPosition.current, nextRenderContext);
    }
  });
  const getRows = (rowParams = {}, unstable_rowTree) => {
    var _a;
    if (!rowParams.rows && !range) {
      return [];
    }
    let baseRenderContext = renderContext;
    if (rowParams.renderContext) {
      baseRenderContext = rowParams.renderContext;
      baseRenderContext.firstColumnIndex = renderContext.firstColumnIndex;
      baseRenderContext.lastColumnIndex = renderContext.lastColumnIndex;
    }
    const isLastSection = !hasBottomPinnedRows && rowParams.position === void 0 || hasBottomPinnedRows && rowParams.position === "bottom";
    const isPinnedSection = rowParams.position !== void 0;
    let rowIndexOffset;
    switch (rowParams.position) {
      case "top":
        rowIndexOffset = 0;
        break;
      case "bottom":
        rowIndexOffset = pinnedRows.top.length + rows.length;
        break;
      case void 0:
      default:
        rowIndexOffset = pinnedRows.top.length;
        break;
    }
    const rowModels = rowParams.rows ?? rows;
    const firstRowToRender = baseRenderContext.firstRowIndex;
    const lastRowToRender = Math.min(baseRenderContext.lastRowIndex, rowModels.length);
    const rowIndexes = rowParams.rows ? createRange(0, rowParams.rows.length) : createRange(firstRowToRender, lastRowToRender);
    let virtualRowIndex = -1;
    const focusedVirtualCell = (_a = params.focusedVirtualCell) == null ? void 0 : _a.call(params);
    if (!isPinnedSection && focusedVirtualCell) {
      if (focusedVirtualCell.rowIndex < firstRowToRender) {
        rowIndexes.unshift(focusedVirtualCell.rowIndex);
        virtualRowIndex = focusedVirtualCell.rowIndex;
      }
      if (focusedVirtualCell.rowIndex > lastRowToRender) {
        rowIndexes.push(focusedVirtualCell.rowIndex);
        virtualRowIndex = focusedVirtualCell.rowIndex;
      }
    }
    const rowElements = [];
    const columnPositions = Dimensions.selectors.columnPositions(store.state, columns);
    rowIndexes.forEach((rowIndexInPage) => {
      const {
        id,
        model
      } = rowModels[rowIndexInPage];
      if (unstable_rowTree && !unstable_rowTree[id]) {
        return;
      }
      const rowIndex = ((range == null ? void 0 : range.firstRowIndex) || 0) + rowIndexOffset + rowIndexInPage;
      if (colspan == null ? void 0 : colspan.enabled) {
        const minFirstColumn = pinnedColumns.left.length;
        const maxLastColumn = columns.length - pinnedColumns.right.length;
        api.calculateColSpan(id, minFirstColumn, maxLastColumn, columns);
        if (pinnedColumns.left.length > 0) {
          api.calculateColSpan(id, 0, pinnedColumns.left.length, columns);
        }
        if (pinnedColumns.right.length > 0) {
          api.calculateColSpan(id, columns.length - pinnedColumns.right.length, columns.length, columns);
        }
      }
      const baseRowHeight = !api.rowsMeta.rowHasAutoHeight(id) ? api.rowsMeta.getRowHeight(id) : "auto";
      let isFirstVisible = false;
      if (rowParams.position === void 0) {
        isFirstVisible = rowIndexInPage === 0;
      }
      let isLastVisible = false;
      const isLastVisibleInSection = rowIndexInPage === rowModels.length - 1;
      if (isLastSection) {
        if (!isPinnedSection) {
          const lastIndex = rows.length - 1;
          const isLastVisibleRowIndex = rowIndexInPage === lastIndex;
          if (isLastVisibleRowIndex) {
            isLastVisible = true;
          }
        } else {
          isLastVisible = isLastVisibleInSection;
        }
      }
      let currentRenderContext = baseRenderContext;
      if (frozenContext.current && rowIndexInPage >= frozenContext.current.firstRowIndex && rowIndexInPage < frozenContext.current.lastRowIndex) {
        currentRenderContext = frozenContext.current;
      }
      const isVirtualFocusRow = rowIndexInPage === virtualRowIndex;
      const isVirtualFocusColumn = (focusedVirtualCell == null ? void 0 : focusedVirtualCell.rowIndex) === rowIndex;
      const offsetLeft = computeOffsetLeft(columnPositions, currentRenderContext, pinnedColumns.left.length);
      const showBottomBorder = isLastVisibleInSection && rowParams.position === "top";
      const firstColumnIndex = currentRenderContext.firstColumnIndex;
      const lastColumnIndex = currentRenderContext.lastColumnIndex;
      rowElements.push(renderRow({
        id,
        model,
        rowIndex,
        offsetLeft,
        columnsTotalWidth,
        baseRowHeight,
        firstColumnIndex,
        lastColumnIndex,
        focusedColumnIndex: isVirtualFocusColumn ? focusedVirtualCell.columnIndex : void 0,
        isFirstVisible,
        isLastVisible,
        isVirtualFocusRow,
        showBottomBorder
      }));
      if (isVirtualFocusRow) {
        return;
      }
      const panel = panels.get(id);
      if (panel) {
        rowElements.push(panel);
      }
      if (rowParams.position === void 0 && isLastVisibleInSection && renderInfiniteLoadingTrigger) {
        rowElements.push(renderInfiniteLoadingTrigger(id));
      }
    });
    return rowElements;
  };
  const scrollRestoreCallback = React50.useRef(null);
  useEnhancedEffect_default(() => {
    if (!isRenderContextReady.current) {
      return;
    }
    forceUpdateRenderContextCallback();
  }, [enabledForColumns, enabledForRows, forceUpdateRenderContextCallback]);
  useEnhancedEffect_default(() => {
    if (layout.refs.scroller.current) {
      layout.refs.scroller.current.scrollLeft = 0;
    }
  }, [layout.refs.scroller, scrollReset]);
  useRunOnce(renderContext !== EMPTY_RENDER_CONTEXT, () => {
    onScrollChange == null ? void 0 : onScrollChange(scrollPosition.current, renderContext);
    isRenderContextReady.current = true;
    if ((initialState == null ? void 0 : initialState.scroll) && layout.refs.scroller.current) {
      const scroller = layout.refs.scroller.current;
      const {
        top: top2,
        left: left2
      } = initialState.scroll;
      const isScrollRestored = {
        top: !(top2 > 0),
        left: !(left2 > 0)
      };
      if (!isScrollRestored.left && columnsTotalWidth) {
        scroller.scrollLeft = left2;
        isScrollRestored.left = true;
        ignoreNextScrollEvent.current = true;
      }
      if (!isScrollRestored.top && contentHeight) {
        scroller.scrollTop = top2;
        ignoreNextScrollEvent.current = true;
      }
      if (!isScrollRestored.top || !isScrollRestored.left) {
        scrollRestoreCallback.current = (columnsTotalWidthCurrent, contentHeightCurrent) => {
          if (!isScrollRestored.left && columnsTotalWidthCurrent) {
            scroller.scrollLeft = left2;
            isScrollRestored.left = true;
            ignoreNextScrollEvent.current = true;
          }
          if (!isScrollRestored.top && contentHeightCurrent) {
            scroller.scrollTop = top2;
            isScrollRestored.top = true;
            ignoreNextScrollEvent.current = true;
          }
          if (isScrollRestored.left && isScrollRestored.top) {
            scrollRestoreCallback.current = null;
          }
        };
      }
    }
  });
  useStoreEffect(store, Dimensions.selectors.dimensions, forceUpdateRenderContext);
  useEnhancedEffect_default(() => {
    var _a;
    if (layout.refs.scroller) {
      (_a = scrollRestoreCallback.current) == null ? void 0 : _a.call(scrollRestoreCallback, columnsTotalWidth, contentHeight);
    }
  }, [layout.refs.scroller, columnsTotalWidth, contentHeight]);
  const isFirstSizing = React50.useRef(true);
  const containerRef = useRefCallback((node) => {
    layout.refs.container.current = node;
    const unsubscribe = observeRootNode(node, store, (rootSize) => {
      if (rootSize.width === 0 && rootSize.height === 0 && store.state.rootSize.height !== 0 && store.state.rootSize.width !== 0) {
        return;
      }
      store.state.rootSize = rootSize;
      if (isFirstSizing.current || !api.debouncedUpdateDimensions) {
        api.updateDimensions(isFirstSizing.current);
        isFirstSizing.current = false;
      } else {
        api.debouncedUpdateDimensions();
      }
    });
    return () => {
      unsubscribe == null ? void 0 : unsubscribe();
      layout.refs.container.current = null;
    };
  });
  const scrollerRef = useRefCallback((node) => {
    layout.refs.scroller.current = node;
    const opts = {
      passive: true
    };
    node.addEventListener("scroll", handleScroll, opts);
    node.addEventListener("wheel", onWheel, opts);
    node.addEventListener("touchmove", onTouchMove, opts);
    return () => {
      node.removeEventListener("scroll", handleScroll, opts);
      node.removeEventListener("wheel", onWheel, opts);
      node.removeEventListener("touchmove", onTouchMove, opts);
      layout.refs.scroller.current = null;
    };
  });
  const layoutParams = {
    containerRef,
    scrollerRef
  };
  const layoutAPI = layout.use(store, params, api, layoutParams);
  const getters = _extends({
    setPanels,
    getRows,
    rows: params.rows
  }, layoutAPI);
  return _extends({
    getters,
    setPanels,
    forceUpdateRenderContext,
    scheduleUpdateRenderContext
  }, createSpanningAPI());
}
function useRefCallback(fn2) {
  const refCleanup = React50.useRef(void 0);
  const refCallback = useEventCallback_default((node) => {
    var _a;
    if (!node) {
      (_a = refCleanup.current) == null ? void 0 : _a.call(refCleanup);
      return;
    }
    refCleanup.current = fn2(node);
    if (reactMajor_default >= 19) {
      return refCleanup.current;
    }
  });
  return refCallback;
}
function inputsSelector(store, params, api, enabledForRows, enabledForColumns) {
  var _a;
  const dimensions = Dimensions.selectors.dimensions(store.state);
  const rows = params.rows;
  const range = params.range;
  const columns = params.columns;
  const hiddenCellsOriginMap = api.getHiddenCellsOrigin();
  const lastRowId = (_a = params.rows.at(-1)) == null ? void 0 : _a.id;
  const lastColumn = columns.at(-1);
  return {
    api,
    enabledForRows,
    enabledForColumns,
    autoHeight: dimensions.autoHeight,
    rowBufferPx: params.virtualization.rowBufferPx,
    columnBufferPx: params.virtualization.columnBufferPx,
    leftPinnedWidth: dimensions.leftPinnedWidth,
    columnsTotalWidth: dimensions.columnsTotalWidth,
    viewportInnerWidth: dimensions.viewportInnerSize.width,
    viewportInnerHeight: dimensions.viewportInnerSize.height,
    lastRowHeight: lastRowId !== void 0 ? api.rowsMeta.getRowHeight(lastRowId) : 0,
    lastColumnWidth: (lastColumn == null ? void 0 : lastColumn.computedWidth) ?? 0,
    rowsMeta: Dimensions.selectors.rowsMeta(store.state),
    columnPositions: Dimensions.selectors.columnPositions(store.state, params.columns),
    rows,
    range,
    pinnedColumns: params.pinnedColumns,
    columns,
    hiddenCellsOriginMap,
    virtualizeColumnsWithAutoRowHeight: params.virtualizeColumnsWithAutoRowHeight
  };
}
function computeRenderContext(inputs, scrollPosition, scrollCache) {
  const renderContext = {
    firstRowIndex: 0,
    lastRowIndex: inputs.rows.length,
    firstColumnIndex: 0,
    lastColumnIndex: inputs.columns.length
  };
  const {
    top: top2,
    left: left2
  } = scrollPosition;
  const realLeft = Math.abs(left2) + inputs.leftPinnedWidth;
  if (inputs.enabledForRows) {
    let firstRowIndex = Math.min(getNearestIndexToRender(inputs, top2, {
      atStart: true,
      lastPosition: inputs.rowsMeta.positions[inputs.rowsMeta.positions.length - 1] + inputs.lastRowHeight
    }), inputs.rowsMeta.positions.length - 1);
    const rowSpanHiddenCellOrigin = inputs.hiddenCellsOriginMap[firstRowIndex];
    if (rowSpanHiddenCellOrigin) {
      const minSpannedRowIndex = Math.min(...Object.values(rowSpanHiddenCellOrigin));
      firstRowIndex = Math.min(firstRowIndex, minSpannedRowIndex);
    }
    const lastRowIndex = inputs.autoHeight ? firstRowIndex + inputs.rows.length : getNearestIndexToRender(inputs, top2 + inputs.viewportInnerHeight);
    renderContext.firstRowIndex = firstRowIndex;
    renderContext.lastRowIndex = lastRowIndex;
  }
  if (inputs.enabledForColumns) {
    let firstColumnIndex = 0;
    let lastColumnIndex = inputs.columnPositions.length;
    let hasRowWithAutoHeight = false;
    const [firstRowToRender, lastRowToRender] = getIndexesToRender({
      firstIndex: renderContext.firstRowIndex,
      lastIndex: renderContext.lastRowIndex,
      minFirstIndex: 0,
      maxLastIndex: inputs.rows.length,
      bufferBefore: scrollCache.buffer.rowBefore,
      bufferAfter: scrollCache.buffer.rowAfter,
      positions: inputs.rowsMeta.positions,
      lastSize: inputs.lastRowHeight
    });
    if (!inputs.virtualizeColumnsWithAutoRowHeight) {
      for (let i2 = firstRowToRender; i2 < lastRowToRender && !hasRowWithAutoHeight; i2 += 1) {
        const row = inputs.rows[i2];
        hasRowWithAutoHeight = inputs.api.rowsMeta.rowHasAutoHeight(row.id);
      }
    }
    if (!hasRowWithAutoHeight || inputs.virtualizeColumnsWithAutoRowHeight) {
      firstColumnIndex = binarySearch(realLeft, inputs.columnPositions, {
        atStart: true,
        lastPosition: inputs.columnsTotalWidth
      });
      lastColumnIndex = binarySearch(realLeft + inputs.viewportInnerWidth, inputs.columnPositions);
    }
    renderContext.firstColumnIndex = firstColumnIndex;
    renderContext.lastColumnIndex = lastColumnIndex;
  }
  const actualRenderContext = deriveRenderContext(inputs, renderContext, scrollCache);
  return actualRenderContext;
}
function getNearestIndexToRender(inputs, offset2, options3) {
  var _a, _b;
  const lastMeasuredIndexRelativeToAllRows = inputs.api.rowsMeta.getLastMeasuredRowIndex();
  let allRowsMeasured = lastMeasuredIndexRelativeToAllRows === Infinity;
  if (((_a = inputs.range) == null ? void 0 : _a.lastRowIndex) && !allRowsMeasured) {
    allRowsMeasured = lastMeasuredIndexRelativeToAllRows >= inputs.range.lastRowIndex;
  }
  const lastMeasuredIndexRelativeToCurrentPage = clamp2(lastMeasuredIndexRelativeToAllRows - (((_b = inputs.range) == null ? void 0 : _b.firstRowIndex) || 0), 0, inputs.rowsMeta.positions.length);
  if (allRowsMeasured || inputs.rowsMeta.positions[lastMeasuredIndexRelativeToCurrentPage] >= offset2) {
    return binarySearch(offset2, inputs.rowsMeta.positions, options3);
  }
  return exponentialSearch(offset2, inputs.rowsMeta.positions, lastMeasuredIndexRelativeToCurrentPage, options3);
}
function deriveRenderContext(inputs, nextRenderContext, scrollCache) {
  var _a, _b;
  const [firstRowToRender, lastRowToRender] = getIndexesToRender({
    firstIndex: nextRenderContext.firstRowIndex,
    lastIndex: nextRenderContext.lastRowIndex,
    minFirstIndex: 0,
    maxLastIndex: inputs.rows.length,
    bufferBefore: scrollCache.buffer.rowBefore,
    bufferAfter: scrollCache.buffer.rowAfter,
    positions: inputs.rowsMeta.positions,
    lastSize: inputs.lastRowHeight
  });
  const [initialFirstColumnToRender, lastColumnToRender] = getIndexesToRender({
    firstIndex: nextRenderContext.firstColumnIndex,
    lastIndex: nextRenderContext.lastColumnIndex,
    minFirstIndex: ((_a = inputs.pinnedColumns) == null ? void 0 : _a.left.length) ?? 0,
    maxLastIndex: inputs.columns.length - (((_b = inputs.pinnedColumns) == null ? void 0 : _b.right.length) ?? 0),
    bufferBefore: scrollCache.buffer.columnBefore,
    bufferAfter: scrollCache.buffer.columnAfter,
    positions: inputs.columnPositions,
    lastSize: inputs.lastColumnWidth
  });
  const firstColumnToRender = getFirstNonSpannedColumnToRender({
    api: inputs.api,
    firstColumnToRender: initialFirstColumnToRender,
    firstRowToRender,
    lastRowToRender,
    visibleRows: inputs.rows
  });
  return {
    firstRowIndex: firstRowToRender,
    lastRowIndex: lastRowToRender,
    firstColumnIndex: firstColumnToRender,
    lastColumnIndex: lastColumnToRender
  };
}
function binarySearch(offset2, positions, options3 = void 0, sliceStart = 0, sliceEnd = positions.length) {
  if (positions.length <= 0) {
    return -1;
  }
  if (sliceStart >= sliceEnd) {
    return sliceStart;
  }
  const pivot = sliceStart + Math.floor((sliceEnd - sliceStart) / 2);
  const position = positions[pivot];
  let isBefore;
  if (options3 == null ? void 0 : options3.atStart) {
    const width = (pivot === positions.length - 1 ? options3.lastPosition : positions[pivot + 1]) - position;
    isBefore = offset2 - width < position;
  } else {
    isBefore = offset2 <= position;
  }
  return isBefore ? binarySearch(offset2, positions, options3, sliceStart, pivot) : binarySearch(offset2, positions, options3, pivot + 1, sliceEnd);
}
function exponentialSearch(offset2, positions, index, options3 = void 0) {
  let interval = 1;
  while (index < positions.length && Math.abs(positions[index]) < offset2) {
    index += interval;
    interval *= 2;
  }
  return binarySearch(offset2, positions, options3, Math.floor(index / 2), Math.min(index, positions.length));
}
function getIndexesToRender({
  firstIndex,
  lastIndex,
  bufferBefore,
  bufferAfter,
  minFirstIndex,
  maxLastIndex,
  positions,
  lastSize
}) {
  const firstPosition = positions[firstIndex] - bufferBefore;
  const lastPosition = positions[lastIndex] + bufferAfter;
  const firstIndexPadded = binarySearch(firstPosition, positions, {
    atStart: true,
    lastPosition: positions[positions.length - 1] + lastSize
  });
  const lastIndexPadded = binarySearch(lastPosition, positions);
  return [clamp2(firstIndexPadded, minFirstIndex, maxLastIndex), clamp2(lastIndexPadded, minFirstIndex, maxLastIndex)];
}
function areRenderContextsEqual(context1, context2) {
  if (context1 === context2) {
    return true;
  }
  return context1.firstRowIndex === context2.firstRowIndex && context1.lastRowIndex === context2.lastRowIndex && context1.firstColumnIndex === context2.firstColumnIndex && context1.lastColumnIndex === context2.lastColumnIndex;
}
function computeOffsetLeft(columnPositions, renderContext, pinnedLeftLength) {
  const left2 = (columnPositions[renderContext.firstColumnIndex] ?? 0) - (columnPositions[pinnedLeftLength] ?? 0);
  return Math.abs(left2);
}
function bufferForDirection(isRtl, direction, rowBufferPx, columnBufferPx, verticalBuffer, horizontalBuffer) {
  if (isRtl) {
    switch (direction) {
      case ScrollDirection.LEFT:
        direction = ScrollDirection.RIGHT;
        break;
      case ScrollDirection.RIGHT:
        direction = ScrollDirection.LEFT;
        break;
      default:
    }
  }
  switch (direction) {
    case ScrollDirection.NONE:
      return {
        rowAfter: rowBufferPx,
        rowBefore: rowBufferPx,
        columnAfter: columnBufferPx,
        columnBefore: columnBufferPx
      };
    case ScrollDirection.LEFT:
      return {
        rowAfter: 0,
        rowBefore: 0,
        columnAfter: 0,
        columnBefore: horizontalBuffer
      };
    case ScrollDirection.RIGHT:
      return {
        rowAfter: 0,
        rowBefore: 0,
        columnAfter: horizontalBuffer,
        columnBefore: 0
      };
    case ScrollDirection.UP:
      return {
        rowAfter: 0,
        rowBefore: verticalBuffer,
        columnAfter: 0,
        columnBefore: 0
      };
    case ScrollDirection.DOWN:
      return {
        rowAfter: verticalBuffer,
        rowBefore: 0,
        columnAfter: 0,
        columnBefore: 0
      };
    default:
      throw new Error("unreachable");
  }
}
function createScrollCache(isRtl, rowBufferPx, columnBufferPx, verticalBuffer, horizontalBuffer) {
  return {
    direction: ScrollDirection.NONE,
    buffer: bufferForDirection(isRtl, ScrollDirection.NONE, rowBufferPx, columnBufferPx, verticalBuffer, horizontalBuffer)
  };
}
function createRange(from, to) {
  return Array.from({
    length: to - from
  }).map((_, i2) => from + i2);
}
function getFirstNonSpannedColumnToRender({
  api,
  firstColumnToRender,
  firstRowToRender,
  lastRowToRender,
  visibleRows
}) {
  let firstNonSpannedColumnToRender = firstColumnToRender;
  let foundStableColumn = false;
  while (!foundStableColumn && firstNonSpannedColumnToRender >= 0) {
    foundStableColumn = true;
    for (let i2 = firstRowToRender; i2 < lastRowToRender; i2 += 1) {
      const row = visibleRows[i2];
      if (row) {
        const rowId = visibleRows[i2].id;
        const cellColSpanInfo = api.getCellColSpanInfo(rowId, firstNonSpannedColumnToRender);
        if (cellColSpanInfo && cellColSpanInfo.spannedByColSpan && cellColSpanInfo.leftVisibleCellIndex < firstNonSpannedColumnToRender) {
          firstNonSpannedColumnToRender = cellColSpanInfo.leftVisibleCellIndex;
          foundStableColumn = false;
          break;
        }
      }
    }
  }
  return firstNonSpannedColumnToRender;
}
function createSpanningAPI() {
  const getCellColSpanInfo = () => {
    throw new Error("Unimplemented: colspan feature is required");
  };
  const calculateColSpan = () => {
    throw new Error("Unimplemented: colspan feature is required");
  };
  const getHiddenCellsOrigin = () => {
    throw new Error("Unimplemented: rowspan feature is required");
  };
  return {
    getCellColSpanInfo,
    calculateColSpan,
    getHiddenCellsOrigin
  };
}

// node_modules/@mui/x-virtualizer/esm/features/virtualization/layout.js
var Layout = class {
  constructor(refs) {
    this.refs = refs;
  }
  refSetter(name) {
    return (node) => {
      if (node && this.refs[name].current !== node) {
        this.refs[name].current = node;
      }
    };
  }
};
__publicField(Layout, "elements", ["scroller", "container"]);
var LayoutDataGrid = class extends Layout {
  use(store, _params, _api, layoutParams) {
    const {
      scrollerRef,
      containerRef
    } = layoutParams;
    const scrollbarVerticalRef = useEventCallback_default(this.refSetter("scrollbarVertical"));
    const scrollbarHorizontalRef = useEventCallback_default(this.refSetter("scrollbarHorizontal"));
    store.state.virtualization.context = {
      scrollerRef,
      containerRef,
      scrollbarVerticalRef,
      scrollbarHorizontalRef
    };
  }
};
__publicField(LayoutDataGrid, "elements", /* @__PURE__ */ (() => ["scroller", "container", "content", "positioner", "scrollbarVertical", "scrollbarHorizontal"])());
__publicField(LayoutDataGrid, "selectors", (() => ({
  containerProps: createSelectorMemoized(Virtualization.selectors.context, (context) => ({
    ref: context.containerRef
  })),
  scrollerProps: createSelectorMemoized(Virtualization.selectors.context, Dimensions.selectors.autoHeight, Dimensions.selectors.needsHorizontalScrollbar, (context, autoHeight, needsHorizontalScrollbar) => ({
    ref: context.scrollerRef,
    style: {
      overflowX: !needsHorizontalScrollbar ? "hidden" : void 0,
      overflowY: autoHeight ? "hidden" : void 0
    },
    role: "presentation",
    // `tabIndex` shouldn't be used along role=presentation, but it fixes a Firefox bug
    // https://github.com/mui/mui-x/pull/13891#discussion_r1683416024
    tabIndex: isFirefox ? -1 : void 0
  })),
  contentProps: createSelectorMemoized(Dimensions.selectors.contentHeight, Dimensions.selectors.minimalContentHeight, Dimensions.selectors.columnsTotalWidth, Dimensions.selectors.needsHorizontalScrollbar, (contentHeight, minimalContentHeight2, columnsTotalWidth, needsHorizontalScrollbar) => ({
    style: {
      width: needsHorizontalScrollbar ? columnsTotalWidth : "auto",
      flexBasis: contentHeight === 0 ? minimalContentHeight2 : contentHeight,
      flexShrink: 0
    },
    role: "presentation"
  })),
  positionerProps: createSelectorMemoized(Virtualization.selectors.offsetTop, (offsetTop) => ({
    style: {
      transform: `translate3d(0, ${offsetTop}px, 0)`
    }
  })),
  scrollbarHorizontalProps: createSelectorMemoized(Virtualization.selectors.context, Virtualization.selectors.scrollPosition, (context, scrollPosition) => ({
    ref: context.scrollbarHorizontalRef,
    scrollPosition
  })),
  scrollbarVerticalProps: createSelectorMemoized(Virtualization.selectors.context, Virtualization.selectors.scrollPosition, (context, scrollPosition) => ({
    ref: context.scrollbarVerticalRef,
    scrollPosition
  })),
  scrollAreaProps: createSelectorMemoized(Virtualization.selectors.scrollPosition, (scrollPosition) => ({
    scrollPosition
  }))
}))());
var LayoutDataGridLegacy = class extends LayoutDataGrid {
  use(store, _params, _api, layoutParams) {
    super.use(store, _params, _api, layoutParams);
    const containerProps = store.use(LayoutDataGrid.selectors.containerProps);
    const scrollerProps = store.use(LayoutDataGrid.selectors.scrollerProps);
    const contentProps = store.use(LayoutDataGrid.selectors.contentProps);
    const positionerProps = store.use(LayoutDataGrid.selectors.positionerProps);
    const scrollbarVerticalProps = store.use(LayoutDataGrid.selectors.scrollbarVerticalProps);
    const scrollbarHorizontalProps = store.use(LayoutDataGrid.selectors.scrollbarHorizontalProps);
    const scrollAreaProps = store.use(LayoutDataGrid.selectors.scrollAreaProps);
    return {
      getContainerProps: () => containerProps,
      getScrollerProps: () => scrollerProps,
      getContentProps: () => contentProps,
      getPositionerProps: () => positionerProps,
      getScrollbarVerticalProps: () => scrollbarVerticalProps,
      getScrollbarHorizontalProps: () => scrollbarHorizontalProps,
      getScrollAreaProps: () => scrollAreaProps
    };
  }
};
var LayoutList = class extends Layout {
  use(store, _params, _api, layoutParams) {
    const {
      scrollerRef,
      containerRef
    } = layoutParams;
    const mergedRef = useForkRef(scrollerRef, containerRef);
    store.state.virtualization.context = {
      mergedRef
    };
  }
};
__publicField(LayoutList, "elements", /* @__PURE__ */ (() => ["scroller", "container", "content", "positioner"])());
__publicField(LayoutList, "selectors", (() => ({
  containerProps: createSelectorMemoized(Virtualization.selectors.context, Dimensions.selectors.autoHeight, Dimensions.selectors.needsHorizontalScrollbar, (context, autoHeight, needsHorizontalScrollbar) => ({
    ref: context.mergedRef,
    style: {
      overflowX: !needsHorizontalScrollbar ? "hidden" : void 0,
      overflowY: autoHeight ? "hidden" : void 0,
      position: "relative"
    },
    role: "presentation",
    // `tabIndex` shouldn't be used along role=presentation, but it fixes a Firefox bug
    // https://github.com/mui/mui-x/pull/13891#discussion_r1683416024
    tabIndex: isFirefox ? -1 : void 0
  })),
  contentProps: createSelectorMemoized(Dimensions.selectors.contentHeight, (contentHeight) => {
    return {
      style: {
        position: "absolute",
        display: "inline-block",
        width: "100%",
        height: contentHeight,
        top: 0,
        left: 0,
        zIndex: -1
      },
      role: "presentation"
    };
  }),
  positionerProps: createSelectorMemoized(Virtualization.selectors.offsetTop, (offsetTop) => ({
    style: {
      height: offsetTop
    }
  }))
}))());

// node_modules/@mui/x-virtualizer/esm/features/keyboard.js
var selectors4 = {};
var Keyboard = {
  initialize: initializeState4,
  use: useKeyboard,
  selectors: selectors4
};
function initializeState4(_params) {
  return {};
}
function useKeyboard(store, params, _api) {
  const getViewportPageSize = () => {
    const dimensions = Dimensions.selectors.dimensions(store.state);
    if (!dimensions.isReady) {
      return 0;
    }
    if (params.getRowHeight) {
      const renderContext = Virtualization.selectors.renderContext(store.state);
      const viewportPageSize = renderContext.lastRowIndex - renderContext.firstRowIndex;
      return Math.min(viewportPageSize - 1, params.rows.length);
    }
    const maximumPageSizeWithoutScrollBar = Math.floor(dimensions.viewportInnerSize.height / dimensions.rowHeight);
    return Math.min(maximumPageSizeWithoutScrollBar, params.rows.length);
  };
  return {
    getViewportPageSize
  };
}

// node_modules/@mui/x-virtualizer/esm/features/rowspan.js
var EMPTY_RANGE = {
  firstRowIndex: 0,
  lastRowIndex: 0
};
var EMPTY_CACHES = {
  spannedCells: {},
  hiddenCells: {},
  hiddenCellOriginMap: {}
};
var selectors5 = {
  state: (state) => state.rowSpanning,
  hiddenCells: (state) => state.rowSpanning.caches.hiddenCells,
  spannedCells: (state) => state.rowSpanning.caches.spannedCells,
  hiddenCellsOriginMap: (state) => state.rowSpanning.caches.hiddenCellOriginMap
};
var Rowspan = {
  initialize: initializeState5,
  use: useRowspan,
  selectors: selectors5
};
function initializeState5(params) {
  var _a;
  return {
    rowSpanning: ((_a = params.initialState) == null ? void 0 : _a.rowSpanning) ?? {
      caches: EMPTY_CACHES,
      processedRange: EMPTY_RANGE
    }
  };
}
function useRowspan(store, _params, _api) {
  const getHiddenCellsOrigin = () => selectors5.hiddenCellsOriginMap(store.state);
  return {
    getHiddenCellsOrigin
  };
}

// node_modules/@mui/x-virtualizer/esm/constants.js
var DEFAULT_COLUMNS = [{
  field: "DEFAULT_COLUMN"
}];
var DEFAULT_PARAMS = {
  resizeThrottleMs: 100,
  columns: DEFAULT_COLUMNS,
  dimensions: {
    autoHeight: false,
    columnsTotalWidth: 0,
    leftPinnedWidth: 0,
    rightPinnedWidth: 0,
    topPinnedHeight: 0,
    bottomPinnedHeight: 0
  },
  virtualization: {
    isRtl: false,
    rowBufferPx: 150,
    columnBufferPx: 150
  }
};

// node_modules/@mui/x-virtualizer/esm/useVirtualizer.js
var FEATURES = [Dimensions, Virtualization, Colspan, Rowspan, Keyboard];
var useVirtualizer = (params) => {
  const paramsWithDefault = mergeDefaults(params, DEFAULT_PARAMS);
  const store = useLazyRef(() => {
    return new Store(FEATURES.map((f) => f.initialize(paramsWithDefault)).reduce((state, partial) => Object.assign(state, partial), {}));
  }).current;
  const api = {};
  for (const feature of FEATURES) {
    Object.assign(api, feature.use(store, paramsWithDefault, api));
  }
  return {
    store,
    api
  };
};
function mergeDefaults(params, defaults) {
  const result = _extends({}, params);
  for (const key in defaults) {
    if (Object.hasOwn(defaults, key)) {
      const value = defaults[key];
      if (value !== null && typeof value === "object" && !Array.isArray(value)) {
        result[key] = mergeDefaults(params[key] ?? {}, value);
      } else {
        result[key] = params[key] ?? value;
      }
    }
  }
  return result;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/virtualization/useGridVirtualization.js
var React51 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/utils/isJSDOM.js
var isJSDOM2 = typeof window !== "undefined" && /jsdom|HappyDOM/.test(window.navigator.userAgent);

// node_modules/@mui/x-data-grid/esm/hooks/features/virtualization/useGridVirtualization.js
var HAS_LAYOUT = !isJSDOM2;
var virtualizationStateInitializer = (state, props) => {
  const {
    disableVirtualization,
    autoHeight
  } = props;
  const virtualization = {
    enabled: !disableVirtualization && HAS_LAYOUT,
    enabledForColumns: !disableVirtualization && HAS_LAYOUT,
    enabledForRows: !disableVirtualization && !autoHeight && HAS_LAYOUT,
    renderContext: EMPTY_RENDER_CONTEXT
  };
  return _extends({}, state, {
    virtualization
  });
};
function useGridVirtualization(apiRef, rootProps) {
  const {
    autoHeight,
    disableVirtualization
  } = rootProps;
  const setVirtualization = (enabled) => {
    const {
      virtualizer
    } = apiRef.current;
    enabled && (enabled = HAS_LAYOUT);
    const snapshot = virtualizer.store.getSnapshot();
    if (snapshot.virtualization.enabled === enabled && snapshot.virtualization.enabledForRows === enabled && snapshot.virtualization.enabledForColumns === enabled) {
      return;
    }
    virtualizer.store.set("virtualization", _extends({}, virtualizer.store.state.virtualization, {
      enabled,
      enabledForColumns: enabled,
      enabledForRows: enabled && !autoHeight
    }));
  };
  const setColumnVirtualization = (enabled) => {
    const {
      virtualizer
    } = apiRef.current;
    enabled && (enabled = HAS_LAYOUT);
    const snapshot = virtualizer.store.getSnapshot();
    if (snapshot.virtualization.enabledForColumns === enabled) {
      return;
    }
    virtualizer.store.set("virtualization", _extends({}, virtualizer.store.state.virtualization, {
      enabledForColumns: enabled
    }));
  };
  const api = {
    unstable_setVirtualization: setVirtualization,
    unstable_setColumnVirtualization: setColumnVirtualization
  };
  useGridApiMethod(apiRef, api, "public");
  const forceUpdateRenderContext = () => {
    const {
      virtualizer
    } = apiRef.current;
    virtualizer == null ? void 0 : virtualizer.api.scheduleUpdateRenderContext();
  };
  apiRef.current.register("private", {
    updateRenderContext: forceUpdateRenderContext
  });
  useGridEventPriority(apiRef, "sortedRowsSet", forceUpdateRenderContext);
  useGridEventPriority(apiRef, "paginationModelChange", forceUpdateRenderContext);
  useGridEventPriority(apiRef, "columnsChange", forceUpdateRenderContext);
  React51.useEffect(() => {
    if (!apiRef.current.virtualizer) {
      return;
    }
    setVirtualization(!rootProps.disableVirtualization);
  }, [apiRef, disableVirtualization, autoHeight]);
}

// node_modules/@mui/x-data-grid/esm/hooks/features/virtualization/gridVirtualizationSelectors.js
var gridVirtualizationSelector = createRootSelector((state) => state.virtualization);
var gridVirtualizationEnabledSelector = createSelector3(gridVirtualizationSelector, (state) => state.enabled);
var gridVirtualizationColumnEnabledSelector = createSelector3(gridVirtualizationSelector, (state) => state.enabledForColumns);
var gridVirtualizationRowEnabledSelector = createSelector3(gridVirtualizationSelector, (state) => state.enabledForRows);
var gridRenderContextSelector = createSelector3(gridVirtualizationSelector, (state) => state.renderContext);
var firstColumnIndexSelector = createRootSelector((state) => state.virtualization.renderContext.firstColumnIndex);
var lastColumnIndexSelector = createRootSelector((state) => state.virtualization.renderContext.lastColumnIndex);
var gridRenderContextColumnsSelector = createSelectorMemoized2(firstColumnIndexSelector, lastColumnIndexSelector, (firstColumnIndex, lastColumnIndex) => ({
  firstColumnIndex,
  lastColumnIndex
}));

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/cache.js
function getKeyDefault(params) {
  return JSON.stringify([params.filterModel, params.sortModel, params.start, params.end]);
}
var GridDataSourceCacheDefault = class {
  constructor({
    ttl = 3e5,
    getKey = getKeyDefault
  }) {
    this.cache = {};
    this.ttl = ttl;
    this.getKey = getKey;
  }
  set(key, value) {
    const keyString = this.getKey(key);
    const expiry = Date.now() + this.ttl;
    this.cache[keyString] = {
      value,
      expiry
    };
  }
  get(key) {
    const keyString = this.getKey(key);
    const entry = this.cache[keyString];
    if (!entry) {
      return void 0;
    }
    if (Date.now() > entry.expiry) {
      delete this.cache[keyString];
      return void 0;
    }
    return entry.value;
  }
  clear() {
    this.cache = {};
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/gridDataSourceError.js
var GridGetRowsError = class extends Error {
  /**
   * The parameters used in the failed request
   */
  /**
   * The original error that caused this error
   */
  constructor(options3) {
    super(options3.message);
    this.name = "GridGetRowsError";
    this.params = options3.params;
    this.cause = options3.cause;
  }
};
var GridUpdateRowError = class extends Error {
  /**
   * The parameters used in the failed request
   */
  /**
   * The original error that caused this error
   */
  constructor(options3) {
    super(options3.message);
    this.name = "GridUpdateRowError";
    this.params = options3.params;
    this.cause = options3.cause;
  }
};

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScrollbar.js
var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses15 = (ownerState, position) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["scrollbar", `scrollbar--${position}`],
    content: ["scrollbarContent"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var scrollbarSizeCssExpression = "calc(max(var(--DataGrid-scrollbarSize), 14px))";
var Scrollbar = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "absolute",
  display: "inline-block",
  zIndex: 60,
  "&:hover": {
    zIndex: 70
  },
  "--size": scrollbarSizeCssExpression
});
var ScrollbarVertical = styled_default2(Scrollbar, {
  slot: "internal"
})({
  width: "var(--size)",
  height: "calc(var(--DataGrid-hasScrollY) * (100% - var(--DataGrid-headersTotalHeight) - var(--DataGrid-hasScrollX) * var(--DataGrid-scrollbarSize)))",
  overflowY: "auto",
  overflowX: "hidden",
  // Disable focus-visible style, it's a scrollbar.
  outline: 0,
  "& > div": {
    width: "var(--size)"
  },
  top: "var(--DataGrid-headersTotalHeight)",
  right: 0
});
var ScrollbarHorizontal = styled_default2(Scrollbar, {
  slot: "internal"
})({
  width: "calc(var(--DataGrid-hasScrollX) * (100% - var(--DataGrid-hasScrollY) * var(--DataGrid-scrollbarSize)))",
  height: "var(--size)",
  overflowY: "hidden",
  overflowX: "auto",
  // Disable focus-visible style, it's a scrollbar.
  outline: 0,
  "& > div": {
    height: "var(--size)"
  },
  bottom: 0
});
var ScrollbarCorner = styled_default2(Scrollbar, {
  slot: "internal"
})({
  width: "var(--size)",
  height: "var(--size)",
  right: 0,
  bottom: 0,
  overflow: "scroll",
  "@media print": {
    display: "none"
  }
});
var GridVirtualScrollbar = forwardRef2(function GridVirtualScrollbar2(props, ref) {
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const isLocked = React52.useRef(false);
  const lastPosition = React52.useRef(0);
  const scrollbarRef = React52.useRef(null);
  const classes2 = useUtilityClasses15(rootProps, props.position);
  const dimensions = useGridSelector(apiRef, gridDimensionsSelector);
  const propertyDimension = props.position === "vertical" ? "height" : "width";
  const propertyScroll = props.position === "vertical" ? "scrollTop" : "scrollLeft";
  const propertyScrollPosition = props.position === "vertical" ? "top" : "left";
  const scrollbarInnerSize = props.position === "horizontal" ? dimensions.minimumSize.width : dimensions.minimumSize.height - dimensions.headersTotalHeight;
  const onScrollerScroll = useEventCallback_default(() => {
    const scrollbar = scrollbarRef.current;
    const scrollPosition = props.scrollPosition.current;
    if (!scrollbar) {
      return;
    }
    if (scrollPosition[propertyScrollPosition] === lastPosition.current) {
      return;
    }
    lastPosition.current = scrollPosition[propertyScrollPosition];
    if (isLocked.current) {
      isLocked.current = false;
      return;
    }
    isLocked.current = true;
    scrollbar[propertyScroll] = props.scrollPosition.current[propertyScrollPosition];
  });
  const onScrollbarScroll = useEventCallback_default(() => {
    const scroller = apiRef.current.virtualScrollerRef.current;
    const scrollbar = scrollbarRef.current;
    if (!scrollbar) {
      return;
    }
    if (isLocked.current) {
      isLocked.current = false;
      return;
    }
    isLocked.current = true;
    scroller[propertyScroll] = scrollbar[propertyScroll];
  });
  useOnMount(() => {
    const scroller = apiRef.current.virtualScrollerRef.current;
    const scrollbar = scrollbarRef.current;
    const options3 = {
      passive: true
    };
    scroller.addEventListener("scroll", onScrollerScroll, options3);
    scrollbar.addEventListener("scroll", onScrollbarScroll, options3);
    return () => {
      scroller.removeEventListener("scroll", onScrollerScroll, options3);
      scrollbar.removeEventListener("scroll", onScrollbarScroll, options3);
    };
  });
  const Container = props.position === "vertical" ? ScrollbarVertical : ScrollbarHorizontal;
  const scrollbarInnerStyle = React52.useMemo(() => ({
    [propertyDimension]: `${scrollbarInnerSize}px`
  }), [propertyDimension, scrollbarInnerSize]);
  return (0, import_jsx_runtime29.jsx)(Container, {
    ref: useForkRef(ref, scrollbarRef),
    className: classes2.root,
    tabIndex: -1,
    "aria-hidden": "true",
    onFocus: (event) => {
      event.target.blur();
    },
    children: (0, import_jsx_runtime29.jsx)("div", {
      className: classes2.content,
      style: scrollbarInnerStyle
    })
  });
});
if (true) GridVirtualScrollbar.displayName = "GridVirtualScrollbar";

// node_modules/@mui/x-data-grid/esm/components/GridScrollShadows.js
var React53 = __toESM(require_react(), 1);
var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses16 = (ownerState) => {
  const {
    classes: classes2,
    position
  } = ownerState;
  const slots = {
    root: ["scrollShadow", `scrollShadow--${position}`]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var ScrollShadow = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ScrollShadow",
  overridesResolver: (props, styles5) => [styles5.root, styles5[props.position]]
})(({
  theme
}) => ({
  position: "absolute",
  inset: 0,
  pointerEvents: "none",
  transition: vars.transition(["box-shadow"], {
    duration: vars.transitions.duration.short
  }),
  "--length": theme.palette.mode === "dark" ? "8px" : "6px",
  "--length-inverse": "calc(var(--length) * -1)",
  "--opacity": theme.palette.mode === "dark" ? "0.7" : "0.18",
  "--blur": "var(--length)",
  "--spread": "calc(var(--length) * -1)",
  "--color": "0, 0, 0",
  "--color-start": "rgba(var(--color), calc(var(--hasScrollStart) * var(--opacity)))",
  "--color-end": "rgba(var(--color), calc(var(--hasScrollEnd) * var(--opacity)))",
  variants: [{
    props: {
      position: "vertical"
    },
    style: {
      top: "var(--DataGrid-topContainerHeight)",
      bottom: "calc(var(--DataGrid-bottomContainerHeight) + var(--DataGrid-hasScrollX) * var(--DataGrid-scrollbarSize))",
      boxShadow: "inset 0 var(--length) var(--blur) var(--spread) var(--color-start), inset 0 var(--length-inverse) var(--blur) var(--spread) var(--color-end)"
    }
  }, {
    props: {
      position: "horizontal"
    },
    style: {
      left: "var(--DataGrid-leftPinnedWidth)",
      right: "calc(var(--DataGrid-rightPinnedWidth) + var(--DataGrid-hasScrollY) * var(--DataGrid-scrollbarSize))",
      boxShadow: "inset var(--length) 0 var(--blur) var(--spread) var(--color-start), inset var(--length-inverse) 0 var(--blur) var(--spread) var(--color-end)"
    }
  }]
}));
function GridScrollShadows(props) {
  var _a, _b, _c;
  const {
    position
  } = props;
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes,
    position
  };
  const classes2 = useUtilityClasses16(ownerState);
  const ref = React53.useRef(null);
  const apiRef = useGridPrivateApiContext();
  const hasScrollX = useGridSelector(apiRef, gridHasScrollXSelector);
  const hasScrollY = useGridSelector(apiRef, gridHasScrollYSelector);
  const pinnedRows = useGridSelector(apiRef, gridPinnedRowsSelector);
  const pinnedColumns = useGridSelector(apiRef, gridPinnedColumnsSelector);
  const initialScrollable = position === "vertical" ? hasScrollY && ((_a = pinnedRows == null ? void 0 : pinnedRows.bottom) == null ? void 0 : _a.length) > 0 : hasScrollX && ((_b = pinnedColumns == null ? void 0 : pinnedColumns.right) == null ? void 0 : _b.length) !== void 0 && ((_c = pinnedColumns == null ? void 0 : pinnedColumns.right) == null ? void 0 : _c.length) > 0;
  const isRtl = useRtl();
  const updateScrollShadowVisibility = React53.useCallback((scrollPosition) => {
    var _a2, _b2, _c2, _d, _e, _f;
    if (!ref.current) {
      return;
    }
    const scroll = Math.abs(Math.round(scrollPosition));
    const dimensions = gridDimensionsSelector(apiRef);
    const maxScroll = Math.round(dimensions.contentSize[position === "vertical" ? "height" : "width"] - dimensions.viewportInnerSize[position === "vertical" ? "height" : "width"]);
    const hasPinnedStart = position === "vertical" ? ((_a2 = pinnedRows == null ? void 0 : pinnedRows.top) == null ? void 0 : _a2.length) > 0 : ((_b2 = pinnedColumns == null ? void 0 : pinnedColumns.left) == null ? void 0 : _b2.length) !== void 0 && ((_c2 = pinnedColumns == null ? void 0 : pinnedColumns.left) == null ? void 0 : _c2.length) > 0;
    const hasPinnedEnd = position === "vertical" ? ((_d = pinnedRows == null ? void 0 : pinnedRows.bottom) == null ? void 0 : _d.length) > 0 : ((_e = pinnedColumns == null ? void 0 : pinnedColumns.right) == null ? void 0 : _e.length) !== void 0 && ((_f = pinnedColumns == null ? void 0 : pinnedColumns.right) == null ? void 0 : _f.length) > 0;
    const scrollIsNotAtStart = isRtl ? scroll < maxScroll : scroll > 0;
    const scrollIsNotAtEnd = isRtl ? scroll > 0 : scroll < maxScroll;
    ref.current.style.setProperty("--hasScrollStart", hasPinnedStart && scrollIsNotAtStart ? "1" : "0");
    ref.current.style.setProperty("--hasScrollEnd", hasPinnedEnd && scrollIsNotAtEnd ? "1" : "0");
  }, [pinnedRows, pinnedColumns, isRtl, position, apiRef]);
  const handleScrolling = (scrollParams) => {
    updateScrollShadowVisibility(scrollParams[position === "vertical" ? "top" : "left"]);
  };
  const handleColumnResizeStop = () => {
    var _a2, _b2;
    if (position === "horizontal") {
      updateScrollShadowVisibility(((_b2 = (_a2 = apiRef.current.virtualScrollerRef) == null ? void 0 : _a2.current) == null ? void 0 : _b2.scrollLeft) || 0);
    }
  };
  useGridEvent(apiRef, "scrollPositionChange", handleScrolling);
  useGridEvent(apiRef, "columnResizeStop", handleColumnResizeStop);
  React53.useEffect(() => {
    var _a2, _b2, _c2, _d;
    updateScrollShadowVisibility((position === "horizontal" ? (_b2 = (_a2 = apiRef.current.virtualScrollerRef) == null ? void 0 : _a2.current) == null ? void 0 : _b2.scrollLeft : (_d = (_c2 = apiRef.current.virtualScrollerRef) == null ? void 0 : _c2.current) == null ? void 0 : _d.scrollTop) ?? 0);
  }, [updateScrollShadowVisibility, apiRef, position]);
  return (0, import_jsx_runtime30.jsx)(ScrollShadow, {
    className: classes2.root,
    ownerState,
    ref,
    style: {
      "--hasScrollStart": 0,
      "--hasScrollEnd": initialScrollable ? "1" : "0"
    }
  });
}

// node_modules/@mui/x-data-grid/esm/components/base/GridOverlays.js
var React54 = __toESM(require_react(), 1);
var import_prop_types16 = __toESM(require_prop_types(), 1);
var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
var GridOverlayWrapperRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "OverlayWrapper",
  shouldForwardProp: (prop) => prop !== "overlayType" && prop !== "loadingOverlayVariant" && prop !== "right"
})(({
  overlayType,
  loadingOverlayVariant,
  right: right2
}) => (
  // Skeleton overlay should flow with the scroll container and not be sticky
  loadingOverlayVariant !== "skeleton" ? {
    position: "sticky",
    // To stay in place while scrolling
    top: "var(--DataGrid-headersTotalHeight)",
    // TODO: take pinned rows into account
    left: 0,
    right: `${right2}px`,
    width: 0,
    // To stay above the content instead of shifting it down
    height: 0,
    // To stay above the content instead of shifting it down
    zIndex: overlayType === "loadingOverlay" ? 5 : 4
    // Should be above pinned columns and detail panel
  } : {}
));
var GridOverlayWrapperInner = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "OverlayWrapperInner",
  shouldForwardProp: (prop) => prop !== "overlayType" && prop !== "loadingOverlayVariant"
})({});
var useUtilityClasses17 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["overlayWrapper"],
    inner: ["overlayWrapperInner"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridOverlayWrapper(props) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const dimensions = useGridSelector(apiRef, gridDimensionsSelector);
  let height = Math.max(dimensions.viewportOuterSize.height - dimensions.topContainerHeight - dimensions.bottomContainerHeight - (dimensions.hasScrollX ? dimensions.scrollbarSize : 0), 0);
  if (height === 0) {
    height = minimalContentHeight;
  }
  const classes2 = useUtilityClasses17(_extends({}, props, {
    classes: rootProps.classes
  }));
  return (0, import_jsx_runtime31.jsx)(GridOverlayWrapperRoot, _extends({
    className: classes2.root
  }, props, {
    right: dimensions.columnsTotalWidth - dimensions.viewportOuterSize.width,
    children: (0, import_jsx_runtime31.jsx)(GridOverlayWrapperInner, _extends({
      className: classes2.inner,
      style: {
        height,
        width: dimensions.viewportOuterSize.width
      }
    }, props))
  }));
}
true ? GridOverlayWrapper.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  loadingOverlayVariant: import_prop_types16.default.oneOf(["circular-progress", "linear-progress", "skeleton"]),
  overlayType: import_prop_types16.default.oneOf(["loadingOverlay", "noResultsOverlay", "noRowsOverlay", "noColumnsOverlay", "emptyPivotOverlay"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridVirtualizer.js
var React58 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridVisibleRows.js
var getVisibleRows = (apiRef, props) => {
  return gridVisibleRowsSelector(apiRef);
};
var useGridVisibleRows = (apiRef, props) => {
  return useGridSelector(apiRef, gridVisibleRowsSelector);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/virtualization/gridFocusedVirtualCellSelector.js
var gridIsFocusedCellOutOfContext = createSelector3(gridFocusCellSelector, gridRenderContextSelector, gridVisibleRowsSelector, gridVisibleColumnDefinitionsSelector, (focusedCell, renderContext, currentPage, visibleColumns) => {
  if (!focusedCell) {
    return false;
  }
  const rowIndex = currentPage.rowIdToIndexMap.get(focusedCell.id);
  const columnIndex = visibleColumns.slice(renderContext.firstColumnIndex, renderContext.lastColumnIndex).findIndex((column) => column.field === focusedCell.field);
  const isInRenderContext = rowIndex !== void 0 && columnIndex !== -1 && rowIndex >= renderContext.firstRowIndex && rowIndex <= renderContext.lastRowIndex;
  return !isInRenderContext;
});
var gridFocusedVirtualCellSelector = createSelectorMemoized2(gridIsFocusedCellOutOfContext, gridVisibleColumnDefinitionsSelector, gridVisibleRowsSelector, gridFocusCellSelector, (isFocusedCellOutOfRenderContext, visibleColumns, currentPage, focusedCell) => {
  if (!isFocusedCellOutOfRenderContext) {
    return null;
  }
  const rowIndex = currentPage.rowIdToIndexMap.get(focusedCell.id);
  if (rowIndex === void 0) {
    return null;
  }
  const columnIndex = visibleColumns.findIndex((column) => column.field === focusedCell.field);
  if (columnIndex === -1) {
    return null;
  }
  return _extends({}, focusedCell, {
    rowIndex,
    columnIndex
  });
});

// node_modules/@mui/x-data-grid/esm/constants/dataGridPropsDefaultValues.js
var DATA_GRID_PROPS_DEFAULT_VALUES = {
  autoHeight: false,
  autoPageSize: false,
  autosizeOnMount: false,
  checkboxSelection: false,
  checkboxSelectionVisibleOnly: false,
  clipboardCopyCellDelimiter: "	",
  columnBufferPx: 150,
  columnFilterDebounceMs: 150,
  columnHeaderHeight: 56,
  disableAutosize: false,
  disableColumnFilter: false,
  disableColumnMenu: false,
  disableColumnReorder: false,
  disableColumnResize: false,
  disableColumnSelector: false,
  disableColumnSorting: false,
  disableDensitySelector: false,
  disableEval: false,
  disableMultipleColumnsFiltering: false,
  disableMultipleColumnsSorting: false,
  disableMultipleRowSelection: false,
  dataSourceRevalidateMs: 0,
  disableRowSelectionOnClick: false,
  disableRowSelectionExcludeModel: false,
  disableVirtualization: false,
  editMode: GridEditModes.Cell,
  filterDebounceMs: 150,
  filterMode: "client",
  hideFooter: false,
  hideFooterPagination: false,
  hideFooterRowCount: false,
  hideFooterSelectedRowCount: false,
  ignoreDiacritics: false,
  ignoreValueFormatterDuringExport: false,
  keepColumnPositionIfDraggedOutside: false,
  keepNonExistentRowsSelected: false,
  loading: false,
  logger: console,
  logLevel: false ? "error" : "warn",
  pageSizeOptions: [25, 50, 100],
  pagination: false,
  paginationMode: "client",
  resizeThrottleMs: 60,
  rowBufferPx: 150,
  rowHeight: 52,
  rows: [],
  rowSelection: true,
  rowSpacingType: "margin",
  rowSpanning: false,
  showCellVerticalBorder: false,
  showColumnVerticalBorder: false,
  showToolbar: false,
  sortingMode: "client",
  sortingOrder: ["asc", "desc", null],
  throttleRowsMs: 0,
  virtualizeColumnsWithAutoRowHeight: false,
  tabNavigation: "none"
};

// node_modules/@babel/runtime/helpers/esm/typeof.js
function _typeof(o) {
  "@babel/helpers - typeof";
  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof(o);
}

// node_modules/@babel/runtime/helpers/esm/toPrimitive.js
function toPrimitive(t, r) {
  if ("object" != _typeof(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i2 = e.call(t, r || "default");
    if ("object" != _typeof(i2)) return i2;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r ? String : Number)(t);
}

// node_modules/@babel/runtime/helpers/esm/toPropertyKey.js
function toPropertyKey(t) {
  var i2 = toPrimitive(t, "string");
  return "symbol" == _typeof(i2) ? i2 : i2 + "";
}

// node_modules/@mui/x-data-grid/esm/hooks/core/pipeProcessing/useGridPipeProcessing.js
var React55 = __toESM(require_react(), 1);
var useGridPipeProcessing = (apiRef) => {
  const cache = React55.useRef({});
  const isRunning = React55.useRef(false);
  const runAppliers = React55.useCallback((groupCache) => {
    if (isRunning.current || !groupCache) {
      return;
    }
    isRunning.current = true;
    Object.values(groupCache.appliers).forEach((callback) => {
      callback();
    });
    isRunning.current = false;
  }, []);
  const registerPipeProcessor = React55.useCallback((group, id, processor) => {
    if (!cache.current[group]) {
      cache.current[group] = {
        processors: /* @__PURE__ */ new Map(),
        processorsAsArray: [],
        appliers: {},
        processorsUpdated: false
      };
    }
    const groupCache = cache.current[group];
    const oldProcessor = groupCache.processors.get(id);
    if (oldProcessor !== processor) {
      groupCache.processors.set(id, processor);
      groupCache.processorsAsArray = Array.from(cache.current[group].processors.values()).filter((processorValue) => processorValue !== null);
      groupCache.processorsUpdated = true;
    }
    return () => {
      cache.current[group].processors.set(id, null);
      cache.current[group].processorsAsArray = Array.from(cache.current[group].processors.values()).filter((processorValue) => processorValue !== null);
    };
  }, []);
  const registerPipeApplier = React55.useCallback((group, id, applier) => {
    if (!cache.current[group]) {
      cache.current[group] = {
        processors: /* @__PURE__ */ new Map(),
        processorsAsArray: [],
        appliers: {},
        processorsUpdated: false
      };
    }
    cache.current[group].appliers[id] = applier;
    return () => {
      const _appliers = cache.current[group].appliers, otherAppliers = _objectWithoutPropertiesLoose(_appliers, [id].map(toPropertyKey));
      cache.current[group].appliers = otherAppliers;
    };
  }, []);
  const requestPipeProcessorsApplication = React55.useCallback((group) => {
    runAppliers(cache.current[group]);
  }, [runAppliers]);
  const runAppliersForPendingProcessors = React55.useCallback(() => {
    for (const group in cache.current) {
      if (!Object.prototype.hasOwnProperty.call(cache.current, group)) {
        continue;
      }
      const groupCache = cache.current[group];
      if (groupCache.processorsUpdated) {
        groupCache.processorsUpdated = false;
        runAppliers(groupCache);
      }
    }
  }, [runAppliers]);
  const applyPipeProcessors = React55.useCallback((...args) => {
    const [group, value, context] = args;
    if (!cache.current[group]) {
      return value;
    }
    const processors = cache.current[group].processorsAsArray;
    let result = value;
    for (let i2 = 0; i2 < processors.length; i2 += 1) {
      result = processors[i2](result, context);
    }
    return result;
  }, []);
  const preProcessingPrivateApi = {
    registerPipeProcessor,
    registerPipeApplier,
    requestPipeProcessorsApplication,
    runAppliersForPendingProcessors
  };
  const preProcessingPublicApi = {
    unstable_applyPipeProcessors: applyPipeProcessors
  };
  useGridApiMethod(apiRef, preProcessingPrivateApi, "private");
  useGridApiMethod(apiRef, preProcessingPublicApi, "public");
};

// node_modules/@mui/x-data-grid/esm/hooks/core/pipeProcessing/useGridRegisterPipeProcessor.js
var React56 = __toESM(require_react(), 1);
var useGridRegisterPipeProcessor = (apiRef, group, callback, enabled = true) => {
  const cleanup = React56.useRef(null);
  const id = React56.useRef(`mui-${Math.round(Math.random() * 1e9)}`);
  const registerPreProcessor = React56.useCallback(() => {
    cleanup.current = apiRef.current.registerPipeProcessor(group, id.current, callback);
  }, [apiRef, callback, group]);
  useFirstRender(() => {
    if (enabled) {
      registerPreProcessor();
    }
  });
  const isFirstRender = React56.useRef(true);
  React56.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
    } else if (enabled) {
      registerPreProcessor();
    }
    return () => {
      if (cleanup.current) {
        cleanup.current();
        cleanup.current = null;
      }
    };
  }, [registerPreProcessor, enabled]);
};

// node_modules/@mui/x-data-grid/esm/hooks/core/pipeProcessing/useGridRegisterPipeApplier.js
var React57 = __toESM(require_react(), 1);
var useGridRegisterPipeApplier = (apiRef, group, callback) => {
  const cleanup = React57.useRef(null);
  const id = React57.useRef(`mui-${Math.round(Math.random() * 1e9)}`);
  const registerPreProcessor = React57.useCallback(() => {
    cleanup.current = apiRef.current.registerPipeApplier(group, id.current, callback);
  }, [apiRef, callback, group]);
  useFirstRender(() => {
    registerPreProcessor();
  });
  const isFirstRender = React57.useRef(true);
  React57.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
    } else {
      registerPreProcessor();
    }
    return () => {
      if (cleanup.current) {
        cleanup.current();
        cleanup.current = null;
      }
    };
  }, [registerPreProcessor]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowsMeta.js
var rowsMetaStateInitializer = (state, props, apiRef) => {
  const baseRowHeight = gridRowHeightSelector(apiRef);
  const dataRowCount = gridRowCountSelector(apiRef);
  const pagination = gridPaginationSelector(apiRef);
  const rowCount = Math.min(pagination.enabled ? pagination.paginationModel.pageSize : dataRowCount, dataRowCount);
  return _extends({}, state, {
    rowsMeta: {
      currentPageTotalHeight: rowCount * baseRowHeight,
      positions: Array.from({
        length: rowCount
      }, (_, i2) => i2 * baseRowHeight),
      pinnedTopRowsTotalHeight: 0,
      pinnedBottomRowsTotalHeight: 0
    }
  });
};
var useGridRowsMeta = (apiRef, _props) => {
  const virtualizer = apiRef.current.virtualizer;
  const {
    getRowHeight,
    setLastMeasuredRowIndex,
    storeRowHeightMeasurement,
    resetRowHeights,
    hydrateRowsMeta,
    observeRowHeight,
    rowHasAutoHeight,
    getRowHeightEntry,
    getLastMeasuredRowIndex
  } = virtualizer.api.rowsMeta;
  useGridRegisterPipeApplier(apiRef, "rowHeight", hydrateRowsMeta);
  const rowsMetaApi = {
    unstable_getRowHeight: getRowHeight,
    unstable_setLastMeasuredRowIndex: setLastMeasuredRowIndex,
    unstable_storeRowHeightMeasurement: storeRowHeightMeasurement,
    resetRowHeights
  };
  const rowsMetaPrivateApi = {
    hydrateRowsMeta,
    observeRowHeight,
    rowHasAutoHeight,
    getRowHeightEntry,
    getLastMeasuredRowIndex
  };
  useGridApiMethod(apiRef, rowsMetaApi, "public");
  useGridApiMethod(apiRef, rowsMetaPrivateApi, "private");
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridVirtualizer.js
var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
var columnsTotalWidthSelector = createSelector3(gridVisibleColumnDefinitionsSelector, gridColumnPositionsSelector, (visibleColumns, positions) => {
  const colCount = visibleColumns.length;
  if (colCount === 0) {
    return 0;
  }
  return roundToDecimalPlaces(positions[colCount - 1] + visibleColumns[colCount - 1].computedWidth, 1);
});
var addGridDimensionsCreator = () => lruMemoize((dimensions, headerHeight, groupHeaderHeight, headerFilterHeight, headersTotalHeight) => {
  return _extends({}, dimensions, {
    headerHeight,
    groupHeaderHeight,
    headerFilterHeight,
    headersTotalHeight
  });
}, {
  maxSize: 1
});
function useGridVirtualizer() {
  var _a, _b;
  const isRtl = useRtl();
  const rootProps = useGridRootProps();
  const apiRef = useGridPrivateApiContext();
  const {
    listView
  } = rootProps;
  const visibleColumns = useGridSelector(apiRef, gridVisibleColumnDefinitionsSelector);
  const pinnedRows = useGridSelector(apiRef, gridPinnedRowsSelector);
  const pinnedColumns = gridVisiblePinnedColumnDefinitionsSelector(apiRef);
  const rowSelectionManager = useGridSelector(apiRef, gridRowSelectionManagerSelector);
  const isRowSelected = React58.useCallback((id) => rowSelectionManager.has(id) && apiRef.current.isRowSelectable(id), [rowSelectionManager, apiRef]);
  const currentPage = useGridVisibleRows(apiRef);
  const hasColSpan = useGridSelector(apiRef, gridHasColSpanSelector);
  const verticalScrollbarWidth = useGridSelector(apiRef, gridVerticalScrollbarWidthSelector);
  const hasFiller = useGridSelector(apiRef, gridHasFillerSelector);
  const {
    autoHeight
  } = rootProps;
  const scrollReset = listView;
  const density = useGridSelector(apiRef, gridDensityFactorSelector);
  const baseRowHeight = getValidRowHeight(rootProps.rowHeight, DATA_GRID_PROPS_DEFAULT_VALUES.rowHeight, rowHeightWarning);
  const rowHeight = Math.floor(baseRowHeight * density);
  const headerHeight = Math.floor(rootProps.columnHeaderHeight * density);
  const groupHeaderHeight = Math.floor((rootProps.columnGroupHeaderHeight ?? rootProps.columnHeaderHeight) * density);
  const headerFilterHeight = Math.floor((rootProps.headerFilterHeight ?? rootProps.columnHeaderHeight) * density);
  const columnsTotalWidth = useGridSelector(apiRef, columnsTotalWidthSelector);
  const headersTotalHeight = getTotalHeaderHeight(apiRef, rootProps);
  const leftPinnedWidth = pinnedColumns.left.reduce((w, col) => w + col.computedWidth, 0);
  const rightPinnedWidth = pinnedColumns.right.reduce((w, col) => w + col.computedWidth, 0);
  const overlayState = useGridOverlays(apiRef, rootProps);
  const dimensionsParams = {
    rowHeight,
    headerHeight,
    columnsTotalWidth,
    leftPinnedWidth,
    rightPinnedWidth,
    topPinnedHeight: headersTotalHeight,
    bottomPinnedHeight: 0,
    autoHeight,
    minimalContentHeight,
    scrollbarSize: rootProps.scrollbarSize
  };
  const addGridDimensions = useLazyRef(addGridDimensionsCreator).current;
  const dataRowCount = useGridSelector(apiRef, gridRowCountSelector);
  const pagination = useGridSelector(apiRef, gridPaginationSelector);
  const rowCount = Math.min(pagination.enabled ? pagination.paginationModel.pageSize : dataRowCount, dataRowCount);
  const {
    getRowHeight,
    getEstimatedRowHeight,
    getRowSpacing
  } = rootProps;
  const RowSlot = rootProps.slots.row;
  const rowSlotProps = (_a = rootProps.slotProps) == null ? void 0 : _a.row;
  const focusedVirtualCell = useGridSelector(apiRef, gridFocusedVirtualCellSelector);
  eslintUseValue(focusedVirtualCell);
  const layout = useLazyRef(() => new LayoutDataGridLegacy({
    container: apiRef.current.mainElementRef,
    scroller: apiRef.current.virtualScrollerRef,
    scrollbarVertical: apiRef.current.virtualScrollbarVerticalRef,
    scrollbarHorizontal: apiRef.current.virtualScrollbarHorizontalRef
  })).current;
  const virtualizer = useVirtualizer({
    layout,
    dimensions: dimensionsParams,
    virtualization: {
      isRtl,
      rowBufferPx: rootProps.rowBufferPx,
      columnBufferPx: rootProps.columnBufferPx
    },
    colspan: {
      enabled: hasColSpan,
      getColspan: React58.useCallback((rowId, column) => {
        if (typeof column.colSpan === "function") {
          const row = apiRef.current.getRow(rowId);
          const value = apiRef.current.getRowValue(row, column);
          return column.colSpan(value, row, column, apiRef) ?? 0;
        }
        return column.colSpan ?? 1;
      }, [apiRef])
    },
    initialState: {
      scroll: (_b = rootProps.initialState) == null ? void 0 : _b.scroll,
      rowSpanning: apiRef.current.state.rowSpanning,
      virtualization: apiRef.current.state.virtualization
    },
    rows: currentPage.rows,
    range: currentPage.range,
    rowCount,
    columns: visibleColumns,
    pinnedRows,
    pinnedColumns,
    disableHorizontalScroll: listView,
    disableVerticalScroll: overlayState.overlayType === "noColumnsOverlay" || overlayState.loadingOverlayVariant === "skeleton",
    getRowHeight: React58.useMemo(() => {
      if (!getRowHeight) {
        return void 0;
      }
      return (rowEntry) => getRowHeight(_extends({}, rowEntry, {
        densityFactor: density
      }));
    }, [getRowHeight, density]),
    getEstimatedRowHeight: React58.useMemo(() => getEstimatedRowHeight ? (rowEntry) => getEstimatedRowHeight(_extends({}, rowEntry, {
      densityFactor: density
    })) : void 0, [getEstimatedRowHeight, density]),
    getRowSpacing: React58.useMemo(() => getRowSpacing ? (rowEntry) => {
      const indexRelativeToCurrentPage = currentPage.rowIdToIndexMap.get(rowEntry.id) ?? -1;
      const visibility = {
        isFirstVisible: indexRelativeToCurrentPage === 0,
        isLastVisible: indexRelativeToCurrentPage === currentPage.rows.length - 1,
        indexRelativeToCurrentPage
      };
      return getRowSpacing(_extends({}, rowEntry, visibility, {
        indexRelativeToCurrentPage: apiRef.current.getRowIndexRelativeToVisibleRows(rowEntry.id)
      }));
    } : void 0, [apiRef, getRowSpacing, currentPage.rows, currentPage.rowIdToIndexMap]),
    applyRowHeight: useEventCallback_default((entry, row) => apiRef.current.unstable_applyPipeProcessors("rowHeight", entry, row)),
    virtualizeColumnsWithAutoRowHeight: rootProps.virtualizeColumnsWithAutoRowHeight,
    focusedVirtualCell: useEventCallback_default(() => gridFocusedVirtualCellSelector(apiRef)),
    resizeThrottleMs: rootProps.resizeThrottleMs,
    onResize: useEventCallback_default((size) => apiRef.current.publishEvent("resize", size)),
    onWheel: useEventCallback_default((event) => {
      apiRef.current.publishEvent("virtualScrollerWheel", {}, event);
    }),
    onTouchMove: useEventCallback_default((event) => {
      apiRef.current.publishEvent("virtualScrollerTouchMove", {}, event);
    }),
    onRenderContextChange: useEventCallback_default((nextRenderContext) => {
      apiRef.current.publishEvent("renderedRowsIntervalChange", nextRenderContext);
    }),
    onScrollChange: React58.useCallback((scrollPosition, nextRenderContext) => {
      apiRef.current.publishEvent("scrollPositionChange", {
        top: scrollPosition.top,
        left: scrollPosition.left,
        renderContext: nextRenderContext
      });
    }, [apiRef]),
    scrollReset,
    renderRow: React58.useCallback((params) => (0, import_jsx_runtime32.jsx)(RowSlot, _extends({
      row: params.model,
      rowId: params.id,
      index: params.rowIndex,
      selected: isRowSelected(params.id),
      offsetLeft: params.offsetLeft,
      columnsTotalWidth,
      rowHeight: params.baseRowHeight,
      pinnedColumns,
      visibleColumns,
      firstColumnIndex: params.firstColumnIndex,
      lastColumnIndex: params.lastColumnIndex,
      focusedColumnIndex: params.focusedColumnIndex,
      isFirstVisible: params.isFirstVisible,
      isLastVisible: params.isLastVisible,
      isNotVisible: params.isVirtualFocusRow,
      showBottomBorder: params.showBottomBorder,
      scrollbarWidth: verticalScrollbarWidth,
      gridHasFiller: hasFiller
    }, rowSlotProps), params.id), [columnsTotalWidth, hasFiller, isRowSelected, pinnedColumns, RowSlot, rowSlotProps, verticalScrollbarWidth, visibleColumns]),
    renderInfiniteLoadingTrigger: React58.useCallback((id) => {
      var _a2, _b2;
      return (_b2 = (_a2 = apiRef.current).getInfiniteLoadingTriggerElement) == null ? void 0 : _b2.call(_a2, {
        lastRowId: id
      });
    }, [apiRef])
  });
  useFirstRender(() => {
    apiRef.current.store.state.dimensions = addGridDimensions(virtualizer.store.state.dimensions, headerHeight, groupHeaderHeight, headerFilterHeight, headersTotalHeight);
    apiRef.current.store.state.rowsMeta = virtualizer.store.state.rowsMeta;
    apiRef.current.store.state.virtualization = virtualizer.store.state.virtualization;
  });
  useStoreEffect(virtualizer.store, Dimensions.selectors.dimensions, (_, dimensions) => {
    if (!dimensions.isReady) {
      return;
    }
    apiRef.current.setState((gridState) => _extends({}, gridState, {
      dimensions: addGridDimensions(dimensions, headerHeight, groupHeaderHeight, headerFilterHeight, headersTotalHeight)
    }));
  });
  useStoreEffect(virtualizer.store, Dimensions.selectors.rowsMeta, (_, rowsMeta) => {
    if (rowsMeta !== apiRef.current.state.rowsMeta) {
      apiRef.current.setState((gridState) => _extends({}, gridState, {
        rowsMeta
      }));
    }
  });
  useStoreEffect(virtualizer.store, Virtualization.selectors.store, (_, virtualization) => {
    if (virtualization.renderContext === EMPTY_RENDER_CONTEXT) {
      return;
    }
    if (virtualization !== apiRef.current.state.virtualization) {
      apiRef.current.setState((gridState) => _extends({}, gridState, {
        virtualization
      }));
    }
  });
  apiRef.current.register("private", {
    virtualizer
  });
  useGridRowsMeta(apiRef, rootProps);
  return virtualizer;
}

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridVirtualScroller.js
var import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses18 = (ownerState) => {
  const {
    classes: classes2,
    hasScrollX,
    hasPinnedRight,
    loadingOverlayVariant,
    overlayType
  } = ownerState;
  const hideContent = loadingOverlayVariant === "skeleton" || overlayType === "noColumnsOverlay";
  const slots = {
    root: ["main", hasPinnedRight && "main--hasPinnedRight", hideContent && "main--hiddenContent"],
    scroller: ["virtualScroller", hasScrollX && "virtualScroller--hasScrollX"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var Scroller = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "VirtualScroller",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.virtualScroller, ownerState.hasScrollX && styles5["virtualScroller--hasScrollX"]];
  }
})({
  position: "relative",
  height: "100%",
  flexGrow: 1,
  overflow: "scroll",
  scrollbarWidth: "none",
  display: "flex",
  flexDirection: "column",
  "&::-webkit-scrollbar": {
    display: "none"
    /* Safari and Chrome */
  },
  "@media print": {
    overflow: "hidden"
  },
  // See https://github.com/mui/mui-x/issues/10547
  zIndex: 0
});
var hasPinnedRightSelector = (apiRef) => apiRef.current.state.dimensions.rightPinnedWidth > 0;
function GridVirtualScroller(props) {
  var _a, _b, _c, _d;
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const hasScrollY = useGridSelector(apiRef, gridHasScrollYSelector);
  const hasScrollX = useGridSelector(apiRef, gridHasScrollXSelector);
  const hasPinnedRight = useGridSelector(apiRef, hasPinnedRightSelector);
  const hasBottomFiller = useGridSelector(apiRef, gridHasBottomFillerSelector);
  const {
    overlayType,
    loadingOverlayVariant
  } = useGridOverlays(apiRef, rootProps);
  const Overlay = (_a = rootProps.slots) == null ? void 0 : _a[overlayType];
  const ownerState = {
    classes: rootProps.classes,
    hasScrollX,
    hasPinnedRight,
    overlayType,
    loadingOverlayVariant
  };
  const classes2 = useUtilityClasses18(ownerState);
  const virtualScroller = useGridVirtualizer().api.getters;
  const {
    getContainerProps,
    getScrollerProps,
    getContentProps,
    getPositionerProps,
    getScrollbarVerticalProps,
    getScrollbarHorizontalProps,
    getRows,
    getScrollAreaProps
  } = virtualScroller;
  const rows = getRows(void 0, gridRowTreeSelector(apiRef));
  return (0, import_jsx_runtime33.jsxs)(GridMainContainer, _extends({
    className: classes2.root
  }, getContainerProps(), {
    ownerState,
    children: [(0, import_jsx_runtime33.jsx)(GridScrollArea, _extends({
      scrollDirection: "left"
    }, getScrollAreaProps())), (0, import_jsx_runtime33.jsx)(GridScrollArea, _extends({
      scrollDirection: "right"
    }, getScrollAreaProps())), (0, import_jsx_runtime33.jsx)(GridScrollArea, _extends({
      scrollDirection: "up"
    }, getScrollAreaProps())), (0, import_jsx_runtime33.jsx)(GridScrollArea, _extends({
      scrollDirection: "down"
    }, getScrollAreaProps())), (0, import_jsx_runtime33.jsxs)(Scroller, _extends({
      className: classes2.scroller
    }, getScrollerProps(), {
      ownerState,
      children: [(0, import_jsx_runtime33.jsxs)(GridTopContainer, {
        children: [!rootProps.listView && (0, import_jsx_runtime33.jsx)(MemoizedGridHeaders, {}), (0, import_jsx_runtime33.jsx)(rootProps.slots.pinnedRows, {
          position: "top",
          virtualScroller
        })]
      }), overlayType && (0, import_jsx_runtime33.jsx)(GridOverlayWrapper, {
        overlayType,
        loadingOverlayVariant,
        children: (0, import_jsx_runtime33.jsx)(Overlay, _extends({}, (_b = rootProps.slotProps) == null ? void 0 : _b[overlayType]))
      }), (0, import_jsx_runtime33.jsx)(GridVirtualScrollerContent, _extends({}, getContentProps(), {
        children: (0, import_jsx_runtime33.jsxs)(GridVirtualScrollerRenderZone, _extends({
          role: "rowgroup"
        }, getPositionerProps(), {
          children: [rows, (0, import_jsx_runtime33.jsx)(rootProps.slots.detailPanels, {
            virtualScroller
          })]
        }))
      })), hasBottomFiller && (0, import_jsx_runtime33.jsx)(Memoized, {
        rowsLength: rows.length
      }), (0, import_jsx_runtime33.jsx)(rootProps.slots.bottomContainer, {
        children: (0, import_jsx_runtime33.jsx)(rootProps.slots.pinnedRows, {
          position: "bottom",
          virtualScroller
        })
      })]
    })), hasScrollX && (0, import_jsx_runtime33.jsxs)(React59.Fragment, {
      children: [((_c = rootProps.pinnedColumnsSectionSeparator) == null ? void 0 : _c.endsWith("shadow")) && (0, import_jsx_runtime33.jsx)(GridScrollShadows, {
        position: "horizontal"
      }), (0, import_jsx_runtime33.jsx)(GridVirtualScrollbar, _extends({
        position: "horizontal"
      }, getScrollbarHorizontalProps()))]
    }), hasScrollY && (0, import_jsx_runtime33.jsxs)(React59.Fragment, {
      children: [((_d = rootProps.pinnedRowsSectionSeparator) == null ? void 0 : _d.endsWith("shadow")) && (0, import_jsx_runtime33.jsx)(GridScrollShadows, {
        position: "vertical"
      }), (0, import_jsx_runtime33.jsx)(GridVirtualScrollbar, _extends({
        position: "vertical"
      }, getScrollbarVerticalProps()))]
    }), hasScrollX && hasScrollY && (0, import_jsx_runtime33.jsx)(ScrollbarCorner, {
      "aria-hidden": "true"
    }), props.children]
  }));
}

// node_modules/@mui/x-data-grid/esm/components/base/GridFooterPlaceholder.js
var import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
function GridFooterPlaceholder() {
  var _a;
  const rootProps = useGridRootProps();
  if (rootProps.hideFooter) {
    return null;
  }
  return (0, import_jsx_runtime34.jsx)(rootProps.slots.footer, _extends(
    {},
    (_a = rootProps.slotProps) == null ? void 0 : _a.footer
    /* FIXME: typing error */
  ));
}

// node_modules/@mui/x-data-grid/esm/components/cell/GridCell.js
var React60 = __toESM(require_react(), 1);
var import_prop_types17 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/utils/doesSupportPreventScroll.js
var cachedSupportsPreventScroll;
function doesSupportPreventScroll() {
  if (cachedSupportsPreventScroll === void 0) {
    document.createElement("div").focus({
      get preventScroll() {
        cachedSupportsPreventScroll = true;
        return false;
      }
    });
  }
  return cachedSupportsPreventScroll;
}

// node_modules/@mui/x-data-grid/esm/internals/utils/computeSlots.js
function computeSlots({
  defaultSlots: defaultSlots4,
  slots
}) {
  const overrides = slots;
  if (!overrides || Object.keys(overrides).length === 0) {
    return defaultSlots4;
  }
  const result = _extends({}, defaultSlots4);
  Object.keys(overrides).forEach((key) => {
    const k2 = key;
    if (overrides[k2] !== void 0) {
      result[k2] = overrides[k2];
    }
  });
  return result;
}

// node_modules/@mui/x-data-grid/esm/internals/utils/propValidation.js
var propValidatorsDataGrid = [(props) => props.autoPageSize && props.autoHeight && ["MUI X: `<DataGrid autoPageSize={true} autoHeight={true} />` are not valid props.", "You cannot use both the `autoPageSize` and `autoHeight` props at the same time because `autoHeight` scales the height of the Data Grid according to the `pageSize`.", "", "Please remove one of these two props."].join("\n") || void 0, (props) => props.paginationMode === "client" && props.paginationMeta != null && ['MUI X: Usage of the `paginationMeta` prop with client-side pagination (`paginationMode="client"`) has no effect.', '`paginationMeta` is only meant to be used with `paginationMode="server"`.'].join("\n") || void 0, (props) => props.signature === GridSignature.DataGrid && props.paginationMode === "client" && isNumber(props.rowCount) && ['MUI X: Usage of the `rowCount` prop with client side pagination (`paginationMode="client"`) has no effect.', '`rowCount` is only meant to be used with `paginationMode="server"`.'].join("\n") || void 0, (props) => props.paginationMode === "server" && props.rowCount == null && !props.dataSource && ["MUI X: The `rowCount` prop must be passed using `paginationMode='server'`", "For more detail, see http://mui.com/components/data-grid/pagination/#index-based-pagination"].join("\n") || void 0];
function validateProps(props, validators) {
  validators.forEach((validator2) => {
    const message = validator2(props);
    if (message) {
      warnOnce(message, "error");
    }
  });
}

// node_modules/@mui/x-data-grid/esm/internals/utils/gridRowGroupingUtils.js
var getRowGroupingCriteriaFromGroupingField = (groupingColDefField) => {
  const match = groupingColDefField.match(/^__row_group_by_columns_group_(.*)__$/);
  if (!match) {
    return null;
  }
  return match[1];
};
var isGroupingColumn = (field) => field === GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD || getRowGroupingCriteriaFromGroupingField(field) !== null;

// node_modules/@mui/x-data-grid/esm/utils/rtlFlipSide.js
var rtlFlipSide = (position, isRtl) => {
  if (!position) {
    return void 0;
  }
  if (!isRtl) {
    if (position === PinnedColumnPosition.LEFT) {
      return "left";
    }
    if (position === PinnedColumnPosition.RIGHT) {
      return "right";
    }
  } else {
    if (position === PinnedColumnPosition.LEFT) {
      return "right";
    }
    if (position === PinnedColumnPosition.RIGHT) {
      return "left";
    }
  }
  return void 0;
};

// node_modules/@mui/x-data-grid/esm/internals/utils/attachPinnedStyle.js
function attachPinnedStyle(style, isRtl, pinnedPosition, pinnedOffset) {
  const side = rtlFlipSide(pinnedPosition, isRtl);
  if (!side || pinnedOffset === void 0) {
    return style;
  }
  style[side] = pinnedOffset;
  return style;
}

// node_modules/@mui/x-data-grid/esm/components/cell/GridCell.js
var import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);
var _excluded18 = ["column", "row", "rowId", "rowNode", "align", "children", "colIndex", "width", "className", "style", "colSpan", "disableDragEvents", "isNotVisible", "pinnedOffset", "pinnedPosition", "showRightBorder", "showLeftBorder", "onClick", "onDoubleClick", "onMouseDown", "onMouseUp", "onMouseOver", "onKeyDown", "onKeyUp", "onDragEnter", "onDragOver"];
var _excluded23 = ["changeReason", "unstable_updateValueOnRender"];
var gridPinnedColumnPositionLookup = {
  [PinnedColumnPosition.LEFT]: GridPinnedColumnPosition.LEFT,
  [PinnedColumnPosition.RIGHT]: GridPinnedColumnPosition.RIGHT,
  [PinnedColumnPosition.NONE]: void 0,
  [PinnedColumnPosition.VIRTUAL]: void 0
};
var useUtilityClasses19 = (ownerState) => {
  const {
    align,
    showLeftBorder,
    showRightBorder,
    pinnedPosition,
    isEditable,
    isSelected,
    isSelectionMode,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["cell", `cell--text${capitalize(align)}`, isSelected && "selected", isEditable && "cell--editable", showLeftBorder && "cell--withLeftBorder", showRightBorder && "cell--withRightBorder", pinnedPosition === PinnedColumnPosition.LEFT && "cell--pinnedLeft", pinnedPosition === PinnedColumnPosition.RIGHT && "cell--pinnedRight", isSelectionMode && !isEditable && "cell--selectionMode"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridCell = forwardRef2(function GridCell2(props, ref) {
  var _a, _b;
  const {
    column,
    row,
    rowId,
    rowNode,
    align,
    colIndex,
    width,
    className,
    style: styleProp,
    colSpan,
    disableDragEvents,
    isNotVisible,
    pinnedOffset,
    pinnedPosition,
    showRightBorder,
    showLeftBorder,
    onClick,
    onDoubleClick,
    onMouseDown,
    onMouseUp,
    onMouseOver,
    onKeyDown,
    onKeyUp,
    onDragEnter,
    onDragOver
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded18);
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const isRtl = useRtl();
  const field = column.field;
  const editCellState = useGridSelector(apiRef, gridEditCellStateSelector, {
    rowId,
    field
  });
  const config = useGridConfiguration();
  const cellAggregationResult = config.hooks.useCellAggregationResult(rowId, field);
  const cellMode = editCellState ? GridCellModes.Edit : GridCellModes.View;
  const {
    value: forcedValue,
    formattedValue: forcedFormattedValue
  } = cellAggregationResult || {};
  const cellParams = apiRef.current.getCellParamsForRow(rowId, field, row, {
    colDef: column,
    cellMode,
    rowNode,
    tabIndex: useGridSelector(apiRef, () => {
      const cellTabIndex = gridTabIndexCellSelector(apiRef);
      return cellTabIndex && cellTabIndex.field === field && cellTabIndex.id === rowId ? 0 : -1;
    }),
    hasFocus: useGridSelector(apiRef, () => {
      const focus = gridFocusCellSelector(apiRef);
      return (focus == null ? void 0 : focus.id) === rowId && focus.field === field;
    }),
    value: forcedValue,
    formattedValue: forcedFormattedValue
  });
  cellParams.api = apiRef.current;
  useGridSelector(apiRef, () => apiRef.current.isCellEditable(cellParams));
  const isSelected = useGridSelector(apiRef, () => apiRef.current.unstable_applyPipeProcessors("isCellSelected", false, {
    id: rowId,
    field
  }));
  const store = apiRef.current.virtualizer.store;
  const hiddenCells = useStore(store, Rowspan.selectors.hiddenCells);
  const spannedCells = useStore(store, Rowspan.selectors.spannedCells);
  const {
    hasFocus,
    isEditable = false,
    value
  } = cellParams;
  const tabIndex = (cellMode === "view" || !isEditable) && column.type !== "actions" ? cellParams.tabIndex : -1;
  const {
    classes: rootClasses,
    getCellClassName
  } = rootProps;
  const pipesClassName = useGridSelector(apiRef, () => apiRef.current.unstable_applyPipeProcessors("cellClassName", [], {
    id: rowId,
    field
  }).filter(Boolean).join(" "));
  const classNames = [pipesClassName];
  if (column.cellClassName) {
    classNames.push(typeof column.cellClassName === "function" ? column.cellClassName(cellParams) : column.cellClassName);
  }
  if (column.display === "flex") {
    classNames.push(gridClasses["cell--flex"]);
  }
  if (getCellClassName) {
    classNames.push(getCellClassName(cellParams));
  }
  const valueToRender = cellParams.formattedValue ?? value;
  const cellRef = React60.useRef(null);
  const handleRef = useForkRef(ref, cellRef);
  const isSelectionMode = rootProps.cellSelection ?? false;
  const ownerState = {
    align,
    showLeftBorder,
    showRightBorder,
    isEditable,
    classes: rootProps.classes,
    pinnedPosition,
    isSelected,
    isSelectionMode
  };
  const classes2 = useUtilityClasses19(ownerState);
  const publishMouseUp = React60.useCallback((eventName) => (event) => {
    const params = apiRef.current.getCellParams(rowId, field || "");
    apiRef.current.publishEvent(eventName, params, event);
    if (onMouseUp) {
      onMouseUp(event);
    }
  }, [apiRef, field, onMouseUp, rowId]);
  const publishMouseDown = React60.useCallback((eventName) => (event) => {
    const params = apiRef.current.getCellParams(rowId, field || "");
    apiRef.current.publishEvent(eventName, params, event);
    if (onMouseDown) {
      onMouseDown(event);
    }
  }, [apiRef, field, onMouseDown, rowId]);
  const publish = React60.useCallback((eventName, propHandler) => (event) => {
    if (!apiRef.current.getRow(rowId)) {
      return;
    }
    const params = apiRef.current.getCellParams(rowId, field || "");
    apiRef.current.publishEvent(eventName, params, event);
    if (propHandler) {
      propHandler(event);
    }
  }, [apiRef, field, rowId]);
  const isCellRowSpanned = ((_a = hiddenCells[rowId]) == null ? void 0 : _a[colIndex]) ?? false;
  const rowSpan = ((_b = spannedCells[rowId]) == null ? void 0 : _b[colIndex]) ?? 1;
  const style = React60.useMemo(() => {
    if (isNotVisible) {
      return {
        padding: 0,
        opacity: 0,
        width: 0,
        height: 0,
        border: 0
      };
    }
    const cellStyle = attachPinnedStyle(_extends({
      "--width": `${width}px`
    }, styleProp), isRtl, pinnedPosition, pinnedOffset);
    const isLeftPinned = pinnedPosition === PinnedColumnPosition.LEFT;
    const isRightPinned = pinnedPosition === PinnedColumnPosition.RIGHT;
    if (rowSpan > 1) {
      cellStyle.height = `calc(var(--height) * ${rowSpan})`;
      cellStyle.zIndex = 10;
      if (isLeftPinned || isRightPinned) {
        cellStyle.zIndex = 40;
      }
    }
    return cellStyle;
  }, [width, isNotVisible, styleProp, pinnedOffset, pinnedPosition, isRtl, rowSpan]);
  useEnhancedEffect_default(() => {
    if (!hasFocus || cellMode === GridCellModes.Edit) {
      return;
    }
    const doc = ownerDocument(apiRef.current.rootElementRef.current);
    if (cellRef.current && !cellRef.current.contains(doc.activeElement)) {
      const focusableElement = cellRef.current.querySelector('[tabindex="0"]');
      const elementToFocus = focusableElement || cellRef.current;
      if (doesSupportPreventScroll()) {
        elementToFocus.focus({
          preventScroll: true
        });
      } else {
        const scrollPosition = apiRef.current.getScrollPosition();
        elementToFocus.focus();
        apiRef.current.scroll(scrollPosition);
      }
    }
  }, [hasFocus, cellMode, apiRef]);
  if (isCellRowSpanned) {
    return (0, import_jsx_runtime35.jsx)("div", {
      "data-colindex": colIndex,
      role: "presentation",
      style: _extends({
        width: "var(--width)"
      }, style)
    });
  }
  let handleFocus = other.onFocus;
  if (false) {
    handleFocus = (event) => {
      const focusedCell = gridFocusCellSelector(apiRef);
      if ((focusedCell == null ? void 0 : focusedCell.id) === rowId && focusedCell.field === field) {
        if (typeof other.onFocus === "function") {
          other.onFocus(event);
        }
        return;
      }
      if (!warnedOnce) {
        console.warn([`MUI X: The cell with id=${rowId} and field=${field} received focus.`, `According to the state, the focus should be at id=${focusedCell == null ? void 0 : focusedCell.id}, field=${focusedCell == null ? void 0 : focusedCell.field}.`, "Not syncing the state may cause unwanted behaviors since the `cellFocusIn` event won't be fired.", "Call `fireEvent.mouseUp` before the `fireEvent.click` to sync the focus with the state."].join("\n"));
        warnedOnce = true;
      }
    };
  }
  let children2;
  let title;
  if (editCellState === null && column.renderCell) {
    children2 = column.renderCell(cellParams);
  }
  if (editCellState !== null && column.renderEditCell) {
    const updatedRow = apiRef.current.getRowWithUpdatedValues(rowId, column.field);
    const editCellStateRest = _objectWithoutPropertiesLoose(editCellState, _excluded23);
    const formattedValue = column.valueFormatter ? column.valueFormatter(editCellState.value, updatedRow, column, apiRef) : cellParams.formattedValue;
    const params = _extends({}, cellParams, {
      row: updatedRow,
      formattedValue
    }, editCellStateRest);
    children2 = column.renderEditCell(params);
    classNames.push(gridClasses["cell--editing"]);
    classNames.push(rootClasses == null ? void 0 : rootClasses["cell--editing"]);
  }
  if (children2 === void 0) {
    const valueString = valueToRender == null ? void 0 : valueToRender.toString();
    children2 = valueString;
    title = valueString;
  }
  const draggableEventHandlers = disableDragEvents ? null : {
    onDragEnter: publish("cellDragEnter", onDragEnter),
    onDragOver: publish("cellDragOver", onDragOver)
  };
  return (0, import_jsx_runtime35.jsx)("div", _extends({
    className: clsx_default(classes2.root, classNames, className),
    role: "gridcell",
    "data-field": field,
    "data-colindex": colIndex,
    "aria-colindex": colIndex + 1,
    "aria-colspan": colSpan,
    "aria-rowspan": rowSpan,
    style,
    title,
    tabIndex,
    onClick: publish("cellClick", onClick),
    onDoubleClick: publish("cellDoubleClick", onDoubleClick),
    onMouseOver: publish("cellMouseOver", onMouseOver),
    onMouseDown: publishMouseDown("cellMouseDown"),
    onMouseUp: publishMouseUp("cellMouseUp"),
    onKeyDown: publish("cellKeyDown", onKeyDown),
    onKeyUp: publish("cellKeyUp", onKeyUp)
  }, draggableEventHandlers, other, {
    onFocus: handleFocus,
    ref: handleRef,
    children: children2
  }));
});
if (true) GridCell.displayName = "GridCell";
true ? GridCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  align: import_prop_types17.default.oneOf(["center", "left", "right"]).isRequired,
  colIndex: import_prop_types17.default.number.isRequired,
  colSpan: import_prop_types17.default.number,
  column: import_prop_types17.default.object.isRequired,
  disableDragEvents: import_prop_types17.default.bool,
  isNotVisible: import_prop_types17.default.bool.isRequired,
  pinnedOffset: import_prop_types17.default.number,
  pinnedPosition: import_prop_types17.default.oneOf([0, 1, 2, 3]).isRequired,
  row: import_prop_types17.default.object.isRequired,
  rowId: import_prop_types17.default.oneOfType([import_prop_types17.default.number, import_prop_types17.default.string]).isRequired,
  rowNode: import_prop_types17.default.object.isRequired,
  showLeftBorder: import_prop_types17.default.bool.isRequired,
  showRightBorder: import_prop_types17.default.bool.isRequired,
  width: import_prop_types17.default.number.isRequired
} : void 0;
var MemoizedGridCell = fastMemo(GridCell);

// node_modules/@mui/x-data-grid/esm/components/cell/GridSkeletonCell.js
var React61 = __toESM(require_react(), 1);
var import_prop_types18 = __toESM(require_prop_types(), 1);
var import_jsx_runtime36 = __toESM(require_jsx_runtime(), 1);
var _excluded19 = ["field", "type", "align", "width", "height", "empty", "style", "className"];
var CIRCULAR_CONTENT_SIZE = "1.3em";
var CONTENT_HEIGHT = "1.2em";
var DEFAULT_CONTENT_WIDTH_RANGE = [40, 80];
var CONTENT_WIDTH_RANGE_BY_TYPE = {
  number: [40, 60],
  string: [40, 80],
  date: [40, 60],
  dateTime: [60, 80],
  singleSelect: [40, 80]
};
var useUtilityClasses20 = (ownerState) => {
  const {
    align,
    classes: classes2,
    empty
  } = ownerState;
  const slots = {
    root: ["cell", "cellSkeleton", `cell--text${align ? capitalize(align) : "Left"}`, empty && "cellEmpty"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var randomNumberGenerator = createRandomNumberGenerator(12345);
function GridSkeletonCell(props) {
  const {
    field,
    type,
    align,
    width,
    height,
    empty = false,
    style,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded19);
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes,
    align,
    empty
  };
  const classes2 = useUtilityClasses20(ownerState);
  const skeletonProps = React61.useMemo(() => {
    const isCircularContent = type === "boolean" || type === "actions";
    if (isCircularContent) {
      return {
        variant: "circular",
        width: CIRCULAR_CONTENT_SIZE,
        height: CIRCULAR_CONTENT_SIZE
      };
    }
    const [min2, max2] = type ? CONTENT_WIDTH_RANGE_BY_TYPE[type] ?? DEFAULT_CONTENT_WIDTH_RANGE : DEFAULT_CONTENT_WIDTH_RANGE;
    return {
      variant: "text",
      width: `${Math.round(randomNumberGenerator(min2, max2))}%`,
      height: CONTENT_HEIGHT
    };
  }, [type]);
  return (0, import_jsx_runtime36.jsx)("div", _extends({
    "data-field": field,
    className: clsx_default(classes2.root, className),
    style: _extends({
      height,
      maxWidth: width,
      minWidth: width
    }, style)
  }, other, {
    children: !empty && (0, import_jsx_runtime36.jsx)(rootProps.slots.baseSkeleton, _extends({}, skeletonProps))
  }));
}
true ? GridSkeletonCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  align: import_prop_types18.default.string,
  /**
   * If `true`, the cell will not display the skeleton but still reserve the cell space.
   * @default false
   */
  empty: import_prop_types18.default.bool,
  field: import_prop_types18.default.string,
  height: import_prop_types18.default.oneOfType([import_prop_types18.default.oneOf(["auto"]), import_prop_types18.default.number]),
  type: import_prop_types18.default.oneOf(["actions", "boolean", "custom", "date", "dateTime", "number", "singleSelect", "string"]),
  width: import_prop_types18.default.oneOfType([import_prop_types18.default.number, import_prop_types18.default.string])
} : void 0;
var Memoized2 = fastMemo(GridSkeletonCell);

// node_modules/@mui/x-data-grid/esm/components/containers/GridRoot.js
var React64 = __toESM(require_react(), 1);
var import_prop_types19 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/containers/GridRootStyles.js
var columnSeparatorTargetSize = 10;
var columnSeparatorOffset = -5;
var focusOutlineWidth = 1;
var separatorIconDragStyles = {
  width: 3,
  rx: 1.5,
  x: 10.5
};
var ignoreSsrWarning = "/* emotion-disable-server-rendering-unsafe-selector-warning-please-do-not-use-this-the-warning-exists-for-a-reason */";
var shouldShowBorderTopRightRadiusSelector = (apiRef) => !apiRef.current.state.dimensions.isReady ? apiRef.current.state.dimensions.scrollbarSize === 0 : apiRef.current.state.dimensions.hasScrollX && (!apiRef.current.state.dimensions.hasScrollY || apiRef.current.state.dimensions.scrollbarSize === 0);
var GridRootStyles = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const overrides = [styles5.root];
    gridClassesOverrides.root.forEach((key) => {
      overrides.push({
        [`&.${gridClasses[key]}`]: styles5[key]
      });
    });
    gridClassesOverrides.children.forEach((key) => {
      overrides.push({
        [`& .${gridClasses[key]}`]: styles5[key]
      });
    });
    return overrides;
  }
})(() => {
  const apiRef = useGridPrivateApiContext();
  const shouldShowBorderTopRightRadius = useGridSelector(apiRef, shouldShowBorderTopRightRadiusSelector);
  const baseBackground = vars.colors.background.base;
  const headerBackground = vars.header.background.base;
  const pinnedBackground = vars.cell.background.pinned;
  const hoverColor = removeOpacity(vars.colors.interactive.hover);
  const hoverOpacity = vars.colors.interactive.hoverOpacity;
  const selectedColor = vars.colors.interactive.selected;
  const selectedOpacity = vars.colors.interactive.selectedOpacity;
  const selectedHoverColor = selectedColor;
  const selectedHoverOpacity = `calc(${selectedOpacity} + ${hoverOpacity})`;
  const fallbackColors = {
    hover: vars.colors.interactive.hover,
    selected: selectedColor,
    selectedHover: selectedColor
  };
  const hoverBackground = mix(baseBackground, hoverColor, hoverOpacity, fallbackColors.hover);
  const selectedBackground = mix(baseBackground, selectedColor, selectedOpacity, fallbackColors.selected);
  const selectedHoverBackground = mix(baseBackground, selectedHoverColor, selectedHoverOpacity, fallbackColors.selectedHover);
  const pinnedHoverBackground = mix(pinnedBackground, hoverColor, hoverOpacity, fallbackColors.hover);
  const pinnedSelectedBackground = mix(pinnedBackground, selectedColor, selectedOpacity, fallbackColors.selected);
  const pinnedSelectedHoverBackground = mix(pinnedBackground, selectedHoverColor, selectedHoverOpacity, fallbackColors.selectedHover);
  const getPinnedBackgroundStyles = (backgroundColor) => ({
    [`& .${gridClasses["cell--pinnedLeft"]}, & .${gridClasses["cell--pinnedRight"]}`]: {
      backgroundColor,
      "&.Mui-selected": {
        backgroundColor: mix(backgroundColor, selectedBackground, selectedOpacity, fallbackColors.selected),
        "&:hover": {
          backgroundColor: mix(backgroundColor, selectedHoverBackground, selectedHoverOpacity, fallbackColors.selectedHover)
        }
      }
    }
  });
  const pinnedHoverStyles = getPinnedBackgroundStyles(pinnedHoverBackground);
  const pinnedSelectedStyles = getPinnedBackgroundStyles(pinnedSelectedBackground);
  const pinnedSelectedHoverStyles = getPinnedBackgroundStyles(pinnedSelectedHoverBackground);
  const selectedStyles = {
    backgroundColor: selectedBackground,
    "&:hover": {
      backgroundColor: selectedHoverBackground,
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        backgroundColor: selectedBackground
      }
    }
  };
  const gridStyle = {
    "--unstable_DataGrid-radius": vars.radius.base,
    "--unstable_DataGrid-headWeight": vars.typography.fontWeight.medium,
    "--DataGrid-rowBorderColor": vars.colors.border.base,
    "--DataGrid-cellOffsetMultiplier": 2,
    "--DataGrid-width": "0px",
    "--DataGrid-hasScrollX": "0",
    "--DataGrid-hasScrollY": "0",
    "--DataGrid-scrollbarSize": "10px",
    "--DataGrid-rowWidth": "0px",
    "--DataGrid-columnsTotalWidth": "0px",
    "--DataGrid-leftPinnedWidth": "0px",
    "--DataGrid-rightPinnedWidth": "0px",
    "--DataGrid-headerHeight": "0px",
    "--DataGrid-headersTotalHeight": "0px",
    "--DataGrid-topContainerHeight": "0px",
    "--DataGrid-bottomContainerHeight": "0px",
    flex: 1,
    boxSizing: "border-box",
    position: "relative",
    borderWidth: "1px",
    borderStyle: "solid",
    borderColor: vars.colors.border.base,
    borderRadius: "var(--unstable_DataGrid-radius)",
    backgroundColor: vars.colors.background.base,
    color: vars.colors.foreground.base,
    font: vars.typography.font.body,
    outline: "none",
    height: "100%",
    display: "flex",
    minWidth: 0,
    // See https://github.com/mui/mui-x/issues/8547
    minHeight: 0,
    flexDirection: "column",
    overflow: "hidden",
    overflowAnchor: "none",
    // Keep the same scrolling position
    transform: "translate(0, 0)",
    // Create a stacking context to keep scrollbars from showing on top
    [`.${gridClasses.main} > *:first-child${ignoreSsrWarning}`]: {
      borderTopLeftRadius: "var(--unstable_DataGrid-radius)",
      borderTopRightRadius: "var(--unstable_DataGrid-radius)"
    },
    [`&.${gridClasses.autoHeight}`]: {
      height: "auto"
    },
    [`&.${gridClasses.autosizing}`]: {
      [`& .${gridClasses.columnHeaderTitleContainerContent} > *`]: {
        overflow: "visible !important"
      },
      "@media (hover: hover)": {
        [`& .${gridClasses.menuIcon}`]: {
          width: "0 !important",
          visibility: "hidden !important"
        }
      },
      [`& .${gridClasses.cell}`]: {
        overflow: "visible !important",
        whiteSpace: "nowrap",
        minWidth: "max-content !important",
        maxWidth: "max-content !important"
      },
      [`& .${gridClasses.groupingCriteriaCell}`]: {
        width: "unset"
      },
      [`& .${gridClasses.treeDataGroupingCell}`]: {
        width: "unset"
      },
      [`& .${gridClasses["columnHeader--filter"]}`]: {
        flex: "none !important",
        width: "unset !important"
      }
    },
    [`&.${gridClasses.withSidePanel}`]: {
      flexDirection: "row"
    },
    [`& .${gridClasses.mainContent}`]: {
      display: "flex",
      flexDirection: "column",
      overflow: "hidden",
      flex: 1
    },
    [`& .${gridClasses.columnHeader}, & .${gridClasses.cell}`]: {
      WebkitTapHighlightColor: "transparent",
      padding: "0 10px",
      boxSizing: "border-box"
    },
    [`& .${gridClasses.columnHeader}:focus-within, & .${gridClasses.cell}:focus-within`]: {
      outline: `solid ${setOpacity(vars.colors.interactive.focus, 0.5)} ${focusOutlineWidth}px`,
      outlineOffset: focusOutlineWidth * -1
    },
    [`& .${gridClasses.columnHeader}:focus, & .${gridClasses.cell}:focus`]: {
      outline: `solid ${vars.colors.interactive.focus} ${focusOutlineWidth}px`,
      outlineOffset: focusOutlineWidth * -1
    },
    // Hide the column separator when:
    // - the column is focused and has an outline
    // - the next column is focused and has an outline
    // - the column has a left or right border
    // - the next column is pinned right and has a left border
    [`& .${gridClasses.columnHeader}:focus,
      & .${gridClasses["columnHeader--withLeftBorder"]},
      & .${gridClasses["columnHeader--withRightBorder"]},
      & .${gridClasses["columnHeader--siblingFocused"]}
      `]: {
      [`& .${gridClasses.columnSeparator}`]: {
        opacity: 0
      },
      // Show resizable separators at all times on touch devices
      "@media (hover: none)": {
        [`& .${gridClasses["columnSeparator--resizable"]}`]: {
          opacity: 1
        }
      },
      [`& .${gridClasses["columnSeparator--resizable"]}:hover`]: {
        opacity: 1
      }
    },
    [`&.${gridClasses["root--noToolbar"]} [aria-rowindex="1"] [aria-colindex="1"]`]: {
      borderTopLeftRadius: "calc(var(--unstable_DataGrid-radius) - 1px)"
    },
    [`&.${gridClasses["root--noToolbar"]} [aria-rowindex="1"] .${gridClasses["columnHeader--last"]}`]: {
      borderTopRightRadius: shouldShowBorderTopRightRadius ? "calc(var(--unstable_DataGrid-radius) - 1px)" : void 0
    },
    [`& .${gridClasses.columnHeaderCheckbox}, & .${gridClasses.cellCheckbox}`]: {
      padding: 0,
      justifyContent: "center",
      alignItems: "center"
    },
    [`& .${gridClasses.columnHeader}`]: {
      position: "relative",
      display: "flex",
      alignItems: "center",
      backgroundColor: headerBackground
    },
    [`& .${gridClasses.columnHeader} .${gridClasses.sortButton}`]: {
      backgroundColor: vars.header.background.base
    },
    [`& .${gridClasses["columnHeader--filter"]}`]: {
      paddingTop: 8,
      paddingBottom: 8,
      paddingRight: 5,
      minHeight: "min-content",
      overflow: "hidden"
    },
    [`&.${gridClasses["root--densityCompact"]} .${gridClasses["columnHeader--filter"]}`]: {
      paddingTop: 4,
      paddingBottom: 4
    },
    [`& .${gridClasses["virtualScroller--hasScrollX"]} .${gridClasses["columnHeader--last"]}`]: {
      overflow: "hidden"
    },
    [`& .${gridClasses["pivotPanelField--sorted"]} .${gridClasses.iconButtonContainer},
      & .${gridClasses["columnHeader--sorted"]} .${gridClasses.iconButtonContainer},
      & .${gridClasses["columnHeader--filtered"]} .${gridClasses.iconButtonContainer}`]: {
      visibility: "visible",
      width: "auto"
    },
    [`& .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}) .${gridClasses.sortButton},
      & .${gridClasses.columnHeader}:not(.${gridClasses["columnHeader--sorted"]}) .${gridClasses.sortButton}`]: {
      opacity: 0,
      transition: vars.transition(["opacity"], {
        duration: vars.transitions.duration.short
      })
    },
    [`& .${gridClasses.columnHeaderTitleContainer}`]: {
      display: "flex",
      alignItems: "center",
      gap: vars.spacing(0.25),
      minWidth: 0,
      flex: 1,
      whiteSpace: "nowrap",
      overflow: "hidden"
    },
    [`& .${gridClasses.columnHeaderTitleContainerContent}`]: {
      overflow: "hidden",
      display: "flex",
      alignItems: "center"
    },
    [`& .${gridClasses["columnHeader--filledGroup"]} .${gridClasses.columnHeaderTitleContainer}`]: {
      borderBottomWidth: "1px",
      borderBottomStyle: "solid",
      boxSizing: "border-box"
    },
    [`& .${gridClasses.sortIcon}, & .${gridClasses.filterIcon}`]: {
      fontSize: "inherit"
    },
    [`& .${gridClasses["columnHeader--sortable"]}`]: {
      cursor: "pointer"
    },
    [`& .${gridClasses["columnHeader--alignCenter"]} .${gridClasses.columnHeaderTitleContainer}`]: {
      justifyContent: "center"
    },
    [`& .${gridClasses["columnHeader--alignRight"]} .${gridClasses.columnHeaderDraggableContainer}, & .${gridClasses["columnHeader--alignRight"]} .${gridClasses.columnHeaderTitleContainer}`]: {
      flexDirection: "row-reverse"
    },
    [`& .${gridClasses["columnHeader--alignCenter"]} .${gridClasses.menuIcon}`]: {
      marginLeft: "auto"
    },
    [`& .${gridClasses["columnHeader--alignRight"]} .${gridClasses.menuIcon}`]: {
      marginRight: "auto",
      marginLeft: -5
    },
    [`& .${gridClasses["columnHeader--moving"]}`]: {
      backgroundColor: hoverBackground
    },
    [`& .${gridClasses["columnHeader--pinnedLeft"]}, & .${gridClasses["columnHeader--pinnedRight"]}`]: {
      position: "sticky",
      zIndex: 40,
      // Should be above the column separator
      background: vars.header.background.base
    },
    [`& .${gridClasses.columnSeparator}`]: {
      position: "absolute",
      overflow: "hidden",
      zIndex: 30,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center",
      maxWidth: columnSeparatorTargetSize,
      color: vars.colors.border.base
    },
    [`& .${gridClasses.columnHeaders}`]: {
      width: "var(--DataGrid-rowWidth)",
      backgroundColor: headerBackground
    },
    "@media (hover: hover)": {
      [`& .${gridClasses.columnHeader}:hover`]: {
        [`& .${gridClasses.menuIcon}`]: {
          width: "auto",
          visibility: "visible"
        },
        [`& .${gridClasses.iconButtonContainer}`]: {
          visibility: "visible",
          width: "auto"
        }
      },
      [`& .${gridClasses.columnHeader}:not(.${gridClasses["columnHeader--sorted"]}):hover .${gridClasses.sortButton},
        & .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}):hover .${gridClasses.sortButton},
        & .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}) .${gridClasses.sortButton}:focus-visible`]: {
        opacity: 1
      },
      // Add opacity only to the button content to avoid affecting the background color
      [`& .${gridClasses.columnHeader}:not(.${gridClasses["columnHeader--sorted"]}):hover .${gridClasses.sortButton} > *,
        & .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}):hover .${gridClasses.sortButton} > *,
        & .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}) .${gridClasses.sortButton}:focus-visible > *`]: {
        opacity: 0.78
      },
      [`& .${gridClasses.pivotPanelFieldActionContainer} button:hover`]: {
        backgroundColor: vars.colors.background.base
      }
    },
    "@media (hover: none)": {
      [`& .${gridClasses.columnHeader} .${gridClasses.menuIcon}`]: {
        width: "auto",
        visibility: "visible"
      },
      [`& .${gridClasses.columnHeader}:focus,
        & .${gridClasses["columnHeader--siblingFocused"]}`]: {
        [`.${gridClasses["columnSeparator--resizable"]}`]: {
          color: vars.colors.foreground.accent
        }
      },
      [`& .${gridClasses.pivotPanelField}:not(.${gridClasses["pivotPanelField--sorted"]}) .${gridClasses.sortButton}`]: {
        opacity: 0.78
      }
    },
    // Hide the column separator when the column has border and it is not resizable
    // In this case, this column separator may block interaction with the separator from the adjacent column that is resizable
    [`& .${gridClasses["columnHeader--withLeftBorder"]} .${gridClasses["columnSeparator--sideLeft"]}:not(.${gridClasses["columnSeparator--resizable"]}), & .${gridClasses["columnHeader--withRightBorder"]} .${gridClasses["columnSeparator--sideRight"]}:not(.${gridClasses["columnSeparator--resizable"]})`]: {
      display: "none"
    },
    [`& .${gridClasses["columnSeparator--sideLeft"]}`]: {
      left: columnSeparatorOffset
    },
    [`& .${gridClasses["columnSeparator--sideRight"]}`]: {
      right: columnSeparatorOffset
    },
    [`& .${gridClasses["columnHeader--withRightBorder"]} .${gridClasses["columnSeparator--sideLeft"]}`]: {
      left: columnSeparatorOffset - 0.5
    },
    [`& .${gridClasses["columnHeader--withRightBorder"]} .${gridClasses["columnSeparator--sideRight"]}`]: {
      right: columnSeparatorOffset - 0.5
    },
    [`& .${gridClasses["columnSeparator--resizable"]}`]: {
      cursor: "col-resize",
      touchAction: "none",
      [`&.${gridClasses["columnSeparator--resizing"]}`]: {
        color: vars.colors.foreground.accent
      },
      // Always appear as draggable on touch devices
      "@media (hover: none)": {
        [`& .${gridClasses.iconSeparator} rect`]: separatorIconDragStyles
      },
      "@media (hover: hover)": {
        "&:hover": {
          color: vars.colors.foreground.accent,
          [`& .${gridClasses.iconSeparator} rect`]: separatorIconDragStyles
        }
      },
      "& svg": {
        pointerEvents: "none"
      }
    },
    [`& .${gridClasses.iconSeparator}`]: {
      color: "inherit",
      transition: vars.transition(["color", "width"], {
        duration: vars.transitions.duration.short
      })
    },
    [`& .${gridClasses.menuIcon}`]: {
      width: 0,
      visibility: "hidden",
      fontSize: 20,
      marginRight: -5,
      display: "flex",
      alignItems: "center"
    },
    [`.${gridClasses.menuOpen}`]: {
      visibility: "visible",
      width: "auto"
    },
    [`& .${gridClasses.headerFilterRow}`]: {
      [`& .${gridClasses.columnHeader}, & .${gridClasses.scrollbarFiller}`]: {
        boxSizing: "border-box",
        borderBottom: "1px solid var(--DataGrid-rowBorderColor)"
      }
    },
    /* Bottom border of the top-container */
    [`& .${gridClasses["row--borderBottom"]} .${gridClasses.columnHeader},
      & .${gridClasses["row--borderBottom"]} .${gridClasses.filler},
      & .${gridClasses["row--borderBottom"]} .${gridClasses.scrollbarFiller}`]: {
      borderBottom: `1px solid var(--DataGrid-rowBorderColor)`
    },
    [`& .${gridClasses["row--borderBottom"]} .${gridClasses.cell}`]: {
      borderBottom: `1px solid var(--rowBorderColor)`
    },
    /* Row styles */
    [`.${gridClasses.row}`]: {
      display: "flex",
      width: "var(--DataGrid-rowWidth)",
      breakInside: "avoid",
      // Avoid the row to be broken in two different print pages.
      "--rowBorderColor": "var(--DataGrid-rowBorderColor)",
      [`&.${gridClasses["row--firstVisible"]}`]: {
        "--rowBorderColor": "transparent"
      },
      "&:hover": {
        backgroundColor: hoverBackground,
        // Reset on touch devices, it doesn't add specificity
        "@media (hover: none)": {
          backgroundColor: "transparent"
        }
      },
      [`&.${gridClasses.rowSkeleton}:hover`]: {
        backgroundColor: "transparent"
      },
      "&.Mui-selected": selectedStyles,
      position: "relative"
    },
    /* Cell styles */
    [`& .${gridClasses.cell}`]: {
      flex: "0 0 auto",
      height: "var(--height)",
      width: "var(--width)",
      lineHeight: "calc(var(--height) - 1px)",
      // -1px for the border
      boxSizing: "border-box",
      borderTop: `1px solid var(--rowBorderColor)`,
      overflow: "hidden",
      whiteSpace: "nowrap",
      textOverflow: "ellipsis",
      "&.Mui-selected": selectedStyles
    },
    [`& .${gridClasses["virtualScrollerContent--overflowed"]} .${gridClasses["row--lastVisible"]} .${gridClasses.cell}`]: {
      borderTopColor: "transparent"
    },
    [`& .${gridClasses.pinnedRows} .${gridClasses.row}, .${gridClasses.aggregationRowOverlayWrapper} .${gridClasses.row}`]: {
      backgroundColor: pinnedBackground,
      "&:hover": {
        backgroundColor: pinnedHoverBackground
      }
    },
    [`& .${gridClasses["pinnedRows--top"]} :first-of-type`]: {
      [`& .${gridClasses.cell}, .${gridClasses.scrollbarFiller}`]: {
        borderTop: "none"
      }
    },
    [`&.${gridClasses["root--disableUserSelection"]}`]: {
      userSelect: "none"
    },
    [`& .${gridClasses["row--dynamicHeight"]} > .${gridClasses.cell}`]: {
      whiteSpace: "initial",
      lineHeight: "inherit"
    },
    [`& .${gridClasses.cellEmpty}`]: {
      flex: 1,
      padding: 0,
      height: "unset"
    },
    [`& .${gridClasses.cell}.${gridClasses["cell--selectionMode"]}`]: {
      cursor: "default"
    },
    [`& .${gridClasses.cell}.${gridClasses["cell--editing"]}`]: {
      padding: 1,
      display: "flex",
      boxShadow: vars.shadows.base,
      backgroundColor: vars.colors.background.overlay,
      "&:focus-within": {
        outline: `${focusOutlineWidth}px solid ${vars.colors.interactive.focus}`,
        outlineOffset: focusOutlineWidth * -1
      }
    },
    [`& .${gridClasses["cell--editing"]}`]: {
      "& .MuiInputBase-root": {
        height: "100%"
      }
    },
    [`& .${gridClasses["row--editing"]}`]: {
      boxShadow: vars.shadows.base
    },
    [`& .${gridClasses["row--editing"]} .${gridClasses.cell}`]: {
      boxShadow: "none",
      backgroundColor: vars.colors.background.overlay
    },
    [`& .${gridClasses.editBooleanCell}`]: {
      display: "flex",
      height: "100%",
      width: "100%",
      alignItems: "center",
      justifyContent: "center"
    },
    [`& .${gridClasses.booleanCell}[data-value="true"]`]: {
      color: vars.colors.foreground.muted
    },
    [`& .${gridClasses.booleanCell}[data-value="false"]`]: {
      color: vars.colors.foreground.disabled
    },
    [`& .${gridClasses.actionsCell}`]: {
      display: "inline-flex",
      alignItems: "center",
      gridGap: vars.spacing(1)
    },
    [`& .${gridClasses.rowReorderCell}`]: {
      display: "inline-flex",
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
      opacity: vars.colors.interactive.disabledOpacity
    },
    [`& .${gridClasses["rowReorderCell--draggable"]}`]: {
      cursor: "grab",
      opacity: 1
    },
    [`& .${gridClasses.rowReorderCellContainer}`]: {
      padding: 0,
      display: "flex",
      alignItems: "stretch"
    },
    [`.${gridClasses.withBorderColor}`]: {
      borderColor: vars.colors.border.base
    },
    [`& .${gridClasses["cell--withLeftBorder"]}, & .${gridClasses["columnHeader--withLeftBorder"]}`]: {
      borderLeftColor: "var(--DataGrid-rowBorderColor)",
      borderLeftWidth: "1px",
      borderLeftStyle: "solid"
    },
    [`& .${gridClasses["cell--withRightBorder"]}, & .${gridClasses["columnHeader--withRightBorder"]}`]: {
      borderRightColor: "var(--DataGrid-rowBorderColor)",
      borderRightWidth: "1px",
      borderRightStyle: "solid"
    },
    [`& .${gridClasses["cell--flex"]}`]: {
      display: "flex",
      alignItems: "center",
      lineHeight: "inherit"
    },
    [`& .${gridClasses["cell--textLeft"]}`]: {
      textAlign: "left",
      justifyContent: "flex-start"
    },
    [`& .${gridClasses["cell--textRight"]}`]: {
      textAlign: "right",
      justifyContent: "flex-end"
    },
    [`& .${gridClasses["cell--textCenter"]}`]: {
      textAlign: "center",
      justifyContent: "center"
    },
    [`& .${gridClasses["cell--pinnedLeft"]}, & .${gridClasses["cell--pinnedRight"]}`]: {
      position: "sticky",
      zIndex: 30,
      background: vars.cell.background.pinned,
      "&.Mui-selected": {
        backgroundColor: pinnedSelectedBackground
      }
    },
    [`& .${gridClasses.row}`]: {
      "&:hover": pinnedHoverStyles,
      "&.Mui-selected": pinnedSelectedStyles,
      "&.Mui-selected:hover": pinnedSelectedHoverStyles
    },
    [`& .${gridClasses.cellOffsetLeft}`]: {
      flex: "0 0 auto",
      display: "inline-block"
    },
    [`& .${gridClasses.cellSkeleton}`]: {
      flex: "0 0 auto",
      height: "100%",
      display: "inline-flex",
      alignItems: "center"
    },
    [`& .${gridClasses.columnHeaderDraggableContainer}`]: {
      display: "flex",
      width: "100%",
      height: "100%"
    },
    [`& .${gridClasses.rowReorderCellPlaceholder}`]: {
      display: "none"
    },
    [`& .${gridClasses["columnHeader--dragging"]}`]: {
      background: vars.colors.background.overlay,
      padding: "0 12px",
      borderRadius: "var(--unstable_DataGrid-radius)",
      opacity: vars.colors.interactive.disabledOpacity
    },
    [`& .${gridClasses["row--dragging"]}`]: {
      background: vars.colors.background.overlay,
      padding: "0 12px",
      borderRadius: "var(--unstable_DataGrid-radius)",
      border: "1px solid var(--DataGrid-rowBorderColor)",
      color: vars.colors.foreground.base,
      transform: "translateZ(0)",
      [`& .${gridClasses.rowReorderCellPlaceholder}`]: {
        padding: "0 6px",
        display: "flex"
      }
    },
    [`& .${gridClasses.treeDataGroupingCell}`]: {
      display: "flex",
      alignItems: "center",
      width: "100%"
    },
    [`& .${gridClasses.treeDataGroupingCellToggle}`]: {
      flex: "0 0 28px",
      alignSelf: "stretch",
      marginRight: vars.spacing(2)
    },
    [`& .${gridClasses.treeDataGroupingCellLoadingContainer}, .${gridClasses.groupingCriteriaCellLoadingContainer}`]: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      height: "100%"
    },
    [`& .${gridClasses.groupingCriteriaCell}`]: {
      display: "flex",
      alignItems: "center",
      width: "100%"
    },
    [`& .${gridClasses.groupingCriteriaCellToggle}`]: {
      flex: "0 0 28px",
      alignSelf: "stretch",
      marginRight: vars.spacing(2)
    },
    /* ScrollbarFiller styles */
    [`& .${gridClasses.columnHeaders} .${gridClasses.scrollbarFiller}`]: {
      backgroundColor: headerBackground
    },
    [`.${gridClasses.scrollbarFiller}`]: {
      minWidth: "calc(var(--DataGrid-hasScrollY) * var(--DataGrid-scrollbarSize))",
      alignSelf: "stretch",
      backgroundColor: headerBackground,
      [`&.${gridClasses["scrollbarFiller--pinnedRight"]}`]: {
        position: "sticky",
        zIndex: 40,
        // Should be above the column separator
        right: 0
      }
    },
    [`& .${gridClasses.filler}`]: {
      flex: "1 0 auto"
    },
    [`& .${gridClasses["filler--borderBottom"]}`]: {
      borderBottom: "1px solid var(--DataGrid-rowBorderColor)"
    },
    [`& .${gridClasses.columnHeaders} .${gridClasses.filler}`]: {
      backgroundColor: headerBackground
    },
    /* Used when skeleton/no columns overlay is visible */
    [`& .${gridClasses["main--hiddenContent"]}`]: {
      [`& .${gridClasses.virtualScrollerContent}`]: {
        // We use visibility hidden so that the virtual scroller content retains its height.
        // Position fixed is used to remove the virtual scroller content from the flow.
        // https://github.com/mui/mui-x/issues/14061
        position: "fixed",
        visibility: "hidden"
      },
      // Hide grid content
      [`& .${gridClasses.pinnedRows}`]: {
        display: "none"
      }
    },
    [`& .${gridClasses["row--beingDragged"]}`]: {
      color: vars.colors.foreground.disabled,
      "&:hover": {
        backgroundColor: "transparent"
      }
    }
  };
  return gridStyle;
});
function setOpacity(color, opacity) {
  return `rgba(from ${color} r g b / ${opacity})`;
}
function removeOpacity(color) {
  return setOpacity(color, 1);
}
var supportsColorMix = typeof CSS !== "undefined" && typeof CSS.supports === "function" && CSS.supports("color", "color-mix(in srgb, red 50%, blue 50%)");
var colorMixIfSupported = (colorMixValue, fallback) => {
  if (!supportsColorMix) {
    return fallback;
  }
  return colorMixValue;
};
function mix(background, overlay, opacity, fallback) {
  return colorMixIfSupported(`color-mix(in srgb,${background}, ${overlay} calc(${opacity} * 100%))`, fallback);
}

// node_modules/@mui/x-data-grid/esm/hooks/utils/useIsSSR.js
var import_shim3 = __toESM(require_shim(), 1);
var emptySubscribe = () => () => {
};
var clientSnapshot = () => false;
var serverSnapshot = () => true;
var useIsSSR = () => (0, import_shim3.useSyncExternalStore)(emptySubscribe, clientSnapshot, serverSnapshot);

// node_modules/@mui/x-data-grid/esm/components/GridHeader.js
var React63 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanelContext.js
var React62 = __toESM(require_react(), 1);
var import_jsx_runtime37 = __toESM(require_jsx_runtime(), 1);
var GridPanelContext = React62.createContext(void 0);
if (true) GridPanelContext.displayName = "GridPanelContext";
function useGridPanelContext() {
  const context = React62.useContext(GridPanelContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context.");
  }
  return context;
}
function GridPanelContextProvider({
  children: children2
}) {
  const columnsPanelTriggerRef = React62.useRef(null);
  const filterPanelTriggerRef = React62.useRef(null);
  const aiAssistantPanelTriggerRef = React62.useRef(null);
  const value = React62.useMemo(() => ({
    columnsPanelTriggerRef,
    filterPanelTriggerRef,
    aiAssistantPanelTriggerRef
  }), []);
  return (0, import_jsx_runtime37.jsx)(GridPanelContext.Provider, {
    value,
    children: children2
  });
}

// node_modules/@mui/x-data-grid/esm/components/panel/GridPreferencesPanel.js
var import_jsx_runtime38 = __toESM(require_jsx_runtime(), 1);
function GridPreferencesPanel() {
  var _a;
  const apiRef = useGridApiContext();
  const columns = useGridSelector(apiRef, gridColumnDefinitionsSelector);
  const rootProps = useGridRootProps();
  const preferencePanelState = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const {
    columnsPanelTriggerRef,
    filterPanelTriggerRef,
    aiAssistantPanelTriggerRef
  } = useGridPanelContext();
  const panelContent = apiRef.current.unstable_applyPipeProcessors("preferencePanel", null, preferencePanelState.openedPanelValue ?? GridPreferencePanelsValue.filters);
  let target = null;
  switch (preferencePanelState.openedPanelValue) {
    case GridPreferencePanelsValue.filters:
      target = filterPanelTriggerRef.current;
      break;
    case GridPreferencePanelsValue.columns:
      target = columnsPanelTriggerRef.current;
      break;
    case GridPreferencePanelsValue.aiAssistant:
      target = aiAssistantPanelTriggerRef.current;
      break;
    default:
  }
  return (0, import_jsx_runtime38.jsx)(rootProps.slots.panel, _extends({
    id: preferencePanelState.panelId,
    open: columns.length > 0 && preferencePanelState.open,
    "aria-labelledby": preferencePanelState.labelId,
    target,
    onClose: () => apiRef.current.hidePreferences()
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.panel, {
    children: panelContent
  }));
}

// node_modules/@mui/x-data-grid/esm/components/GridHeader.js
var import_jsx_runtime39 = __toESM(require_jsx_runtime(), 1);
function GridHeader() {
  var _a;
  const rootProps = useGridRootProps();
  return (0, import_jsx_runtime39.jsxs)(React63.Fragment, {
    children: [(0, import_jsx_runtime39.jsx)(GridPreferencesPanel, {}), rootProps.showToolbar && (0, import_jsx_runtime39.jsx)(rootProps.slots.toolbar, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.toolbar))]
  });
}

// node_modules/@mui/x-data-grid/esm/components/containers/GridRoot.js
var import_jsx_runtime40 = __toESM(require_jsx_runtime(), 1);
var _excluded20 = ["className", "children", "sidePanel"];
var useUtilityClasses21 = (ownerState, density) => {
  const {
    autoHeight,
    classes: classes2,
    showCellVerticalBorder
  } = ownerState;
  const slots = {
    root: ["root", autoHeight && "autoHeight", `root--density${capitalize(density)}`, ownerState.slots.toolbar === null && "root--noToolbar", "withBorderColor", showCellVerticalBorder && "withVerticalBorder"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridRoot = forwardRef2(function GridRoot2(props, ref) {
  const rootProps = useGridRootProps();
  const {
    className,
    children: children2,
    sidePanel
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded20);
  const apiRef = useGridPrivateApiContext();
  const density = useGridSelector(apiRef, gridDensitySelector);
  const rootElementRef = apiRef.current.rootElementRef;
  const rootMountCallback = React64.useCallback((node) => {
    if (node === null) {
      return;
    }
    apiRef.current.publishEvent("rootMount", node);
  }, [apiRef]);
  const handleRef = useForkRef(rootElementRef, ref, rootMountCallback);
  const ownerState = rootProps;
  const classes2 = useUtilityClasses21(ownerState, density);
  const cssVariables = useCSSVariablesContext();
  const isSSR = useIsSSR();
  if (isSSR) {
    return null;
  }
  return (0, import_jsx_runtime40.jsxs)(GridRootStyles, _extends({
    className: clsx_default(classes2.root, className, cssVariables.className, sidePanel && gridClasses.withSidePanel),
    ownerState
  }, other, {
    ref: handleRef,
    children: [(0, import_jsx_runtime40.jsxs)("div", {
      className: gridClasses.mainContent,
      role: "presentation",
      children: [(0, import_jsx_runtime40.jsx)(GridHeader, {}), (0, import_jsx_runtime40.jsx)(GridVirtualScroller, {
        children: children2
      }), (0, import_jsx_runtime40.jsx)(GridFooterPlaceholder, {})]
    }), sidePanel, cssVariables.tag]
  }));
});
if (true) GridRoot.displayName = "GridRoot";
true ? GridRoot.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sidePanel: import_prop_types19.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types19.default.oneOfType([import_prop_types19.default.arrayOf(import_prop_types19.default.oneOfType([import_prop_types19.default.func, import_prop_types19.default.object, import_prop_types19.default.bool])), import_prop_types19.default.func, import_prop_types19.default.object])
} : void 0;
var MemoizedGridRoot = fastMemo(GridRoot);

// node_modules/@mui/x-data-grid/esm/components/containers/GridFooterContainer.js
var React65 = __toESM(require_react(), 1);
var import_prop_types20 = __toESM(require_prop_types(), 1);
var import_jsx_runtime41 = __toESM(require_jsx_runtime(), 1);
var _excluded21 = ["className"];
var useUtilityClasses22 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["footerContainer", "withBorderColor"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridFooterContainerRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "FooterContainer"
})({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  minHeight: 52,
  borderTop: "1px solid"
});
var GridFooterContainer = forwardRef2(function GridFooterContainer2(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded21);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses22(rootProps);
  return (0, import_jsx_runtime41.jsx)(GridFooterContainerRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref
  }));
});
if (true) GridFooterContainer.displayName = "GridFooterContainer";
true ? GridFooterContainer.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types20.default.oneOfType([import_prop_types20.default.arrayOf(import_prop_types20.default.oneOfType([import_prop_types20.default.func, import_prop_types20.default.object, import_prop_types20.default.bool])), import_prop_types20.default.func, import_prop_types20.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/containers/GridOverlay.js
var React66 = __toESM(require_react(), 1);
var import_prop_types21 = __toESM(require_prop_types(), 1);
var import_jsx_runtime42 = __toESM(require_jsx_runtime(), 1);
var _excluded24 = ["className"];
var useUtilityClasses23 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["overlay"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridOverlayRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "Overlay"
})({
  width: "100%",
  height: "100%",
  display: "flex",
  gap: vars.spacing(1),
  flexDirection: "column",
  alignSelf: "center",
  alignItems: "center",
  justifyContent: "center",
  textAlign: "center",
  textWrap: "balance",
  backgroundColor: vars.colors.background.backdrop
});
var GridOverlay = forwardRef2(function GridOverlay2(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded24);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses23(rootProps);
  return (0, import_jsx_runtime42.jsx)(GridOverlayRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref
  }));
});
if (true) GridOverlay.displayName = "GridOverlay";
true ? GridOverlay.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types21.default.oneOfType([import_prop_types21.default.arrayOf(import_prop_types21.default.oneOfType([import_prop_types21.default.func, import_prop_types21.default.object, import_prop_types21.default.bool])), import_prop_types21.default.func, import_prop_types21.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/containers/GridToolbarContainer.js
var React71 = __toESM(require_react(), 1);
var import_prop_types24 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/Toolbar.js
var React69 = __toESM(require_react(), 1);
var import_prop_types22 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-internals/esm/useComponentRenderer/useComponentRenderer.js
var React67 = __toESM(require_react(), 1);
function useComponentRenderer(defaultElement, render, props, state = {}) {
  if (typeof render === "function") {
    return render(props, state);
  }
  if (render) {
    if (render.props.className) {
      props.className = mergeClassNames(render.props.className, props.className);
    }
    if (render.props.style || props.style) {
      props.style = _extends({}, props.style, render.props.style);
    }
    return React67.cloneElement(render, props);
  }
  return React67.createElement(defaultElement, props);
}
function mergeClassNames(className, otherClassName) {
  if (!className || !otherClassName) {
    return className || otherClassName;
  }
  return `${className} ${otherClassName}`;
}

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/ToolbarContext.js
var React68 = __toESM(require_react(), 1);
var ToolbarContext = React68.createContext(void 0);
if (true) ToolbarContext.displayName = "ToolbarContext";
function useToolbarContext() {
  const context = React68.useContext(ToolbarContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context. Toolbar subcomponents must be placed within a <Toolbar /> component.");
  }
  return context;
}

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/utils.js
function sortByDocumentPosition(a, b) {
  if (!a.ref.current || !b.ref.current) {
    return 0;
  }
  const position = a.ref.current.compareDocumentPosition(b.ref.current);
  if (!position) {
    return 0;
  }
  if (position & Node.DOCUMENT_POSITION_FOLLOWING || position & Node.DOCUMENT_POSITION_CONTAINED_BY) {
    return -1;
  }
  if (position & Node.DOCUMENT_POSITION_PRECEDING || position & Node.DOCUMENT_POSITION_CONTAINS) {
    return 1;
  }
  return 0;
}

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/Toolbar.js
var import_jsx_runtime43 = __toESM(require_jsx_runtime(), 1);
var _excluded25 = ["render", "className"];
var useUtilityClasses24 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["toolbar"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var ToolbarRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "Toolbar"
})({
  flex: "0 1 1px",
  display: "flex",
  alignItems: "center",
  justifyContent: "end",
  gap: vars.spacing(0.25),
  padding: vars.spacing(0.75),
  minHeight: 52,
  boxSizing: "border-box",
  borderBottom: `1px solid ${vars.colors.border.base}`
});
var Toolbar = forwardRef2(function Toolbar2(props, ref) {
  const {
    render,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded25);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses24(rootProps);
  const [focusableItemId, setFocusableItemId] = React69.useState(null);
  const [items, setItems] = React69.useState([]);
  const getSortedItems = React69.useCallback(() => items.sort(sortByDocumentPosition), [items]);
  const findEnabledItem = React69.useCallback((startIndex, step, wrap2 = true) => {
    var _a, _b;
    let index = startIndex;
    const sortedItems = getSortedItems();
    const itemCount = sortedItems.length;
    for (let i2 = 0; i2 < itemCount; i2 += 1) {
      index += step;
      if (index >= itemCount) {
        if (!wrap2) {
          return -1;
        }
        index = 0;
      } else if (index < 0) {
        if (!wrap2) {
          return -1;
        }
        index = itemCount - 1;
      }
      if (!((_a = sortedItems[index].ref.current) == null ? void 0 : _a.disabled) && ((_b = sortedItems[index].ref.current) == null ? void 0 : _b.ariaDisabled) !== "true") {
        return index;
      }
    }
    return -1;
  }, [getSortedItems]);
  const registerItem = React69.useCallback((id, itemRef) => {
    setItems((prevItems) => [...prevItems, {
      id,
      ref: itemRef
    }]);
  }, []);
  const unregisterItem = React69.useCallback((id) => {
    setItems((prevItems) => prevItems.filter((i2) => i2.id !== id));
  }, []);
  const onItemKeyDown = React69.useCallback((event) => {
    var _a;
    if (!focusableItemId) {
      return;
    }
    const sortedItems = getSortedItems();
    const focusableItemIndex = sortedItems.findIndex((item) => item.id === focusableItemId);
    let newIndex = -1;
    if (event.key === "ArrowRight") {
      event.preventDefault();
      newIndex = findEnabledItem(focusableItemIndex, 1);
    } else if (event.key === "ArrowLeft") {
      event.preventDefault();
      newIndex = findEnabledItem(focusableItemIndex, -1);
    } else if (event.key === "Home") {
      event.preventDefault();
      newIndex = findEnabledItem(-1, 1, false);
    } else if (event.key === "End") {
      event.preventDefault();
      newIndex = findEnabledItem(sortedItems.length, -1, false);
    }
    if (newIndex >= 0 && newIndex < sortedItems.length) {
      const item = sortedItems[newIndex];
      setFocusableItemId(item.id);
      (_a = item.ref.current) == null ? void 0 : _a.focus();
    }
  }, [getSortedItems, focusableItemId, findEnabledItem]);
  const onItemFocus = React69.useCallback((id) => {
    if (focusableItemId !== id) {
      setFocusableItemId(id);
    }
  }, [focusableItemId, setFocusableItemId]);
  const onItemDisabled = React69.useCallback((id) => {
    var _a;
    const sortedItems = getSortedItems();
    const currentIndex = sortedItems.findIndex((item) => item.id === id);
    const newIndex = findEnabledItem(currentIndex, 1);
    if (newIndex >= 0 && newIndex < sortedItems.length) {
      const item = sortedItems[newIndex];
      setFocusableItemId(item.id);
      (_a = item.ref.current) == null ? void 0 : _a.focus();
    }
  }, [getSortedItems, findEnabledItem]);
  React69.useEffect(() => {
    var _a, _b;
    const sortedItems = getSortedItems();
    if (sortedItems.length > 0) {
      if (!focusableItemId) {
        setFocusableItemId(sortedItems[0].id);
        return;
      }
      const focusableItemIndex = sortedItems.findIndex((item) => item.id === focusableItemId);
      if (!sortedItems[focusableItemIndex]) {
        const item = sortedItems[sortedItems.length - 1];
        if (item) {
          setFocusableItemId(item.id);
          (_a = item.ref.current) == null ? void 0 : _a.focus();
        }
      } else if (focusableItemIndex === -1) {
        const item = sortedItems[focusableItemIndex];
        if (item) {
          setFocusableItemId(item.id);
          (_b = item.ref.current) == null ? void 0 : _b.focus();
        }
      }
    }
  }, [getSortedItems, findEnabledItem]);
  const contextValue = React69.useMemo(() => ({
    focusableItemId,
    registerItem,
    unregisterItem,
    onItemKeyDown,
    onItemFocus,
    onItemDisabled
  }), [focusableItemId, registerItem, unregisterItem, onItemKeyDown, onItemFocus, onItemDisabled]);
  const element = useComponentRenderer(ToolbarRoot, render, _extends({
    role: "toolbar",
    "aria-orientation": "horizontal",
    "aria-label": rootProps.label || void 0,
    className: clsx_default(classes2.root, className)
  }, other, {
    ref
  }));
  return (0, import_jsx_runtime43.jsx)(ToolbarContext.Provider, {
    value: contextValue,
    children: element
  });
});
if (true) Toolbar.displayName = "Toolbar";
true ? Toolbar.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types22.default.oneOfType([import_prop_types22.default.element, import_prop_types22.default.func])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/ToolbarButton.js
var React70 = __toESM(require_react(), 1);
var import_prop_types23 = __toESM(require_prop_types(), 1);
var import_jsx_runtime44 = __toESM(require_jsx_runtime(), 1);
var _excluded26 = ["render", "onKeyDown", "onFocus", "disabled", "aria-disabled"];
var ToolbarButton = forwardRef2(function ToolbarButton2(props, ref) {
  var _a;
  const {
    render,
    onKeyDown,
    onFocus,
    disabled,
    "aria-disabled": ariaDisabled
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded26);
  const id = useId();
  const rootProps = useGridRootProps();
  const buttonRef = React70.useRef(null);
  const handleRef = useForkRef(buttonRef, ref);
  const {
    focusableItemId,
    registerItem,
    unregisterItem,
    onItemKeyDown,
    onItemFocus,
    onItemDisabled
  } = useToolbarContext();
  const handleKeyDown = (event) => {
    onItemKeyDown(event);
    onKeyDown == null ? void 0 : onKeyDown(event);
  };
  const handleFocus = (event) => {
    onItemFocus(id);
    onFocus == null ? void 0 : onFocus(event);
  };
  React70.useEffect(() => {
    registerItem(id, buttonRef);
    return () => unregisterItem(id);
  }, []);
  const previousDisabled = React70.useRef(disabled);
  React70.useEffect(() => {
    if (previousDisabled.current !== disabled && disabled === true) {
      onItemDisabled(id, disabled);
    }
    previousDisabled.current = disabled;
  }, [disabled, id, onItemDisabled]);
  const previousAriaDisabled = React70.useRef(ariaDisabled);
  React70.useEffect(() => {
    if (previousAriaDisabled.current !== ariaDisabled && ariaDisabled === true) {
      onItemDisabled(id, true);
    }
    previousAriaDisabled.current = ariaDisabled;
  }, [ariaDisabled, id, onItemDisabled]);
  const element = useComponentRenderer(rootProps.slots.baseIconButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    tabIndex: focusableItemId === id ? 0 : -1
  }, other, {
    disabled,
    "aria-disabled": ariaDisabled,
    onKeyDown: handleKeyDown,
    onFocus: handleFocus,
    ref: handleRef
  }));
  return (0, import_jsx_runtime44.jsx)(React70.Fragment, {
    children: element
  });
});
if (true) ToolbarButton.displayName = "ToolbarButton";
true ? ToolbarButton.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types23.default.string,
  color: import_prop_types23.default.oneOf(["default", "inherit", "primary"]),
  disabled: import_prop_types23.default.bool,
  edge: import_prop_types23.default.oneOf(["end", "start", false]),
  id: import_prop_types23.default.string,
  label: import_prop_types23.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types23.default.oneOfType([import_prop_types23.default.element, import_prop_types23.default.func]),
  role: import_prop_types23.default.string,
  size: import_prop_types23.default.oneOf(["large", "medium", "small"]),
  style: import_prop_types23.default.object,
  tabIndex: import_prop_types23.default.number,
  title: import_prop_types23.default.string,
  touchRippleRef: import_prop_types23.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/containers/GridToolbarContainer.js
var import_jsx_runtime45 = __toESM(require_jsx_runtime(), 1);
var _excluded27 = ["className", "children"];
var useUtilityClasses25 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["toolbarContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridToolbarContainerRoot = styled_default(Toolbar, {
  name: "MuiDataGrid",
  slot: "ToolbarContainer",
  shouldForwardProp: (prop) => prop !== "ownerState"
})({
  display: "flex",
  alignItems: "center",
  flexWrap: "wrap",
  gap: vars.spacing(1),
  padding: vars.spacing(0.5),
  minHeight: "auto"
});
var GridToolbarContainer = forwardRef2(function GridToolbarContainer2(props, ref) {
  const {
    className,
    children: children2
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded27);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses25(rootProps);
  if (!children2) {
    return null;
  }
  return (0, import_jsx_runtime45.jsx)(GridToolbarContainerRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref,
    children: children2
  }));
});
if (true) GridToolbarContainer.displayName = "GridToolbarContainer";
true ? GridToolbarContainer.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types24.default.oneOfType([import_prop_types24.default.arrayOf(import_prop_types24.default.oneOfType([import_prop_types24.default.func, import_prop_types24.default.object, import_prop_types24.default.bool])), import_prop_types24.default.func, import_prop_types24.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderItem.js
var React77 = __toESM(require_react(), 1);
var import_prop_types28 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnMenu/getColumnMenuItemKeys.js
function getColumnMenuItemKeys(params) {
  const {
    apiRef,
    colDef,
    defaultSlots: defaultSlots4,
    defaultSlotProps,
    slots = {},
    slotProps = {}
  } = params;
  const processedComponents = _extends({}, defaultSlots4, slots);
  let processedSlotProps = defaultSlotProps;
  if (slotProps && Object.keys(slotProps).length > 0) {
    const mergedProps = _extends({}, slotProps);
    Object.entries(defaultSlotProps).forEach(([key, currentSlotProps]) => {
      mergedProps[key] = _extends({}, currentSlotProps, slotProps[key] || {});
    });
    processedSlotProps = mergedProps;
  }
  const defaultItems = apiRef.current.unstable_applyPipeProcessors("columnMenu", [], colDef);
  const defaultComponentKeys = Object.keys(defaultSlots4);
  const userItems = Object.keys(slots).filter((key) => !defaultComponentKeys.includes(key));
  const uniqueItems = Array.from(/* @__PURE__ */ new Set([...defaultItems, ...userItems]));
  const cleansedItems = uniqueItems.filter((key) => processedComponents[key] != null);
  return cleansedItems.sort((a, b) => {
    const leftItemProps = processedSlotProps[a];
    const rightItemProps = processedSlotProps[b];
    const leftDisplayOrder = Number.isFinite(leftItemProps == null ? void 0 : leftItemProps.displayOrder) ? leftItemProps.displayOrder : 100;
    const rightDisplayOrder = Number.isFinite(rightItemProps == null ? void 0 : rightItemProps.displayOrder) ? rightItemProps.displayOrder : 100;
    return leftDisplayOrder - rightDisplayOrder;
  });
}

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/ColumnHeaderMenuIcon.js
var React72 = __toESM(require_react(), 1);
var import_jsx_runtime46 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses26 = (ownerState) => {
  const {
    classes: classes2,
    open
  } = ownerState;
  const slots = {
    root: ["menuIcon", open && "menuOpen"],
    button: ["menuIconButton"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var ColumnHeaderMenuIcon = React72.memo((props) => {
  var _a, _b;
  const {
    colDef,
    open,
    columnMenuId,
    columnMenuButtonId,
    iconButtonRef
  } = props;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = _extends({}, props, {
    classes: rootProps.classes
  });
  const classes2 = useUtilityClasses26(ownerState);
  const handleMenuIconClick = React72.useCallback((event) => {
    event.preventDefault();
    event.stopPropagation();
    apiRef.current.toggleColumnMenu(colDef.field);
  }, [apiRef, colDef.field]);
  const columnName = colDef.headerName ?? colDef.field;
  return (0, import_jsx_runtime46.jsx)("div", {
    className: classes2.root,
    children: (0, import_jsx_runtime46.jsx)(rootProps.slots.baseTooltip, _extends({
      title: apiRef.current.getLocaleText("columnMenuLabel"),
      enterDelay: 1e3
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, {
      children: (0, import_jsx_runtime46.jsx)(rootProps.slots.baseIconButton, _extends({
        ref: iconButtonRef,
        tabIndex: -1,
        className: classes2.button,
        "aria-label": apiRef.current.getLocaleText("columnMenuAriaLabel")(columnName),
        size: "small",
        onClick: handleMenuIconClick,
        "aria-haspopup": "menu",
        "aria-expanded": open,
        "aria-controls": open ? columnMenuId : void 0,
        id: columnMenuButtonId
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseIconButton, {
        children: (0, import_jsx_runtime46.jsx)(rootProps.slots.columnMenuIcon, {
          fontSize: "inherit"
        })
      }))
    }))
  });
});
if (true) ColumnHeaderMenuIcon.displayName = "ColumnHeaderMenuIcon";

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/GridColumnHeaderMenu.js
var React73 = __toESM(require_react(), 1);
var import_prop_types25 = __toESM(require_prop_types(), 1);
var import_jsx_runtime47 = __toESM(require_jsx_runtime(), 1);
function GridColumnHeaderMenu({
  columnMenuId,
  columnMenuButtonId,
  ContentComponent,
  contentComponentProps,
  field,
  open,
  target,
  onExited
}) {
  const apiRef = useGridApiContext();
  const colDef = apiRef.current.getColumn(field);
  const hideMenu = useEventCallback_default((event) => {
    if (event) {
      event.stopPropagation();
      if (target == null ? void 0 : target.contains(event.target)) {
        return;
      }
    }
    apiRef.current.hideColumnMenu();
  });
  if (!target || !colDef) {
    return null;
  }
  return (0, import_jsx_runtime47.jsx)(GridMenu, {
    position: `bottom-${colDef.align === "right" ? "start" : "end"}`,
    open,
    target,
    onClose: hideMenu,
    onExited,
    children: (0, import_jsx_runtime47.jsx)(ContentComponent, _extends({
      colDef,
      hideMenu,
      open,
      id: columnMenuId,
      labelledby: columnMenuButtonId
    }, contentComponentProps))
  });
}
true ? GridColumnHeaderMenu.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  columnMenuButtonId: import_prop_types25.default.string,
  columnMenuId: import_prop_types25.default.string,
  ContentComponent: import_prop_types25.default.elementType.isRequired,
  contentComponentProps: import_prop_types25.default.any,
  field: import_prop_types25.default.string.isRequired,
  onExited: import_prop_types25.default.func,
  open: import_prop_types25.default.bool.isRequired,
  target: HTMLElementType
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridGenericColumnHeaderItem.js
var React76 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderTitle.js
var React74 = __toESM(require_react(), 1);
var import_prop_types26 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/utils/domUtils.js
function isOverflown(element) {
  return element.scrollHeight > element.clientHeight || element.scrollWidth > element.clientWidth;
}
function findParentElementFromClassName(elem, className) {
  return elem.closest(`.${className}`);
}
function escapeOperandAttributeSelector(operand) {
  return operand.replace(/["\\]/g, "\\$&");
}
function getGridColumnHeaderElement(root, field) {
  return root.querySelector(`[role="columnheader"][data-field="${escapeOperandAttributeSelector(field)}"]`);
}
function getGridRowElementSelector(id) {
  return `.${gridClasses.row}[data-id="${escapeOperandAttributeSelector(String(id))}"]`;
}
function getGridRowElement(root, id) {
  return root.querySelector(getGridRowElementSelector(id));
}
function getGridCellElement(root, {
  id,
  field
}) {
  const rowSelector = getGridRowElementSelector(id);
  const cellSelector = `.${gridClasses.cell}[data-field="${escapeOperandAttributeSelector(field)}"]`;
  const selector = `${rowSelector} ${cellSelector}`;
  return root.querySelector(selector);
}
var getActiveElement = (root = document) => {
  const activeEl = root.activeElement;
  if (!activeEl) {
    return null;
  }
  if (activeEl.shadowRoot) {
    return getActiveElement(activeEl.shadowRoot);
  }
  return activeEl;
};
function isEventTargetInPortal(event) {
  if (
    // The target is not an element when triggered by a Select inside the cell
    // See https://github.com/mui/material-ui/issues/10534
    event.target.nodeType === 1 && !event.currentTarget.contains(event.target)
  ) {
    return true;
  }
  return false;
}
function getFieldFromHeaderElem(colCellEl) {
  return colCellEl.getAttribute("data-field");
}
function findHeaderElementFromField(elem, field) {
  return elem.querySelector(`[data-field="${escapeOperandAttributeSelector(field)}"]`);
}
function getFieldsFromGroupHeaderElem(colCellEl) {
  return colCellEl.getAttribute("data-fields").slice(2, -2).split("-|-");
}
function findGroupHeaderElementsFromField(elem, field) {
  return Array.from(elem.querySelectorAll(`[data-fields*="|-${escapeOperandAttributeSelector(field)}-|"]`) ?? []);
}
function findGridCellElementsFromCol(col, api) {
  var _a;
  const root = findParentElementFromClassName(col, gridClasses.root);
  if (!root) {
    throw new Error("MUI X: The root element is not found.");
  }
  const ariaColIndex = col.getAttribute("aria-colindex");
  if (!ariaColIndex) {
    return [];
  }
  const colIndex = Number(ariaColIndex) - 1;
  const cells = [];
  if (!((_a = api.virtualScrollerRef) == null ? void 0 : _a.current)) {
    return [];
  }
  queryRows(api).forEach((rowElement) => {
    const rowId = rowElement.getAttribute("data-id");
    if (!rowId) {
      return;
    }
    let columnIndex = colIndex;
    const cellColSpanInfo = api.unstable_getCellColSpanInfo(rowId, colIndex);
    if (cellColSpanInfo && cellColSpanInfo.spannedByColSpan) {
      columnIndex = cellColSpanInfo.leftVisibleCellIndex;
    }
    const cell = rowElement.querySelector(`[data-colindex="${columnIndex}"]`);
    if (cell) {
      cells.push(cell);
    }
  });
  return cells;
}
function findGridElement(api, klass) {
  return api.rootElementRef.current.querySelector(`.${gridClasses[klass]}`);
}
var findPinnedCells = ({
  api,
  colIndex,
  position,
  filterFn
}) => {
  if (colIndex === null) {
    return [];
  }
  const cells = [];
  queryRows(api).forEach((rowElement) => {
    const rowId = rowElement.getAttribute("data-id");
    if (!rowId) {
      return;
    }
    rowElement.querySelectorAll(`.${gridClasses[position === "left" ? "cell--pinnedLeft" : "cell--pinnedRight"]}`).forEach((cell) => {
      const currentColIndex = parseCellColIndex(cell);
      if (currentColIndex !== null && filterFn(currentColIndex)) {
        cells.push(cell);
      }
    });
  });
  return cells;
};
function findLeftPinnedCellsAfterCol(api, col, isRtl) {
  const colIndex = parseCellColIndex(col);
  return findPinnedCells({
    api,
    colIndex,
    position: isRtl ? "right" : "left",
    filterFn: (index) => isRtl ? index < colIndex : index > colIndex
  });
}
function findRightPinnedCellsBeforeCol(api, col, isRtl) {
  const colIndex = parseCellColIndex(col);
  return findPinnedCells({
    api,
    colIndex,
    position: isRtl ? "left" : "right",
    filterFn: (index) => isRtl ? index > colIndex : index < colIndex
  });
}
var findPinnedHeaders = ({
  api,
  colIndex,
  position,
  filterFn
}) => {
  var _a;
  if (!((_a = api.columnHeadersContainerRef) == null ? void 0 : _a.current)) {
    return [];
  }
  if (colIndex === null) {
    return [];
  }
  const elements = [];
  api.columnHeadersContainerRef.current.querySelectorAll(`.${gridClasses[position === "left" ? "columnHeader--pinnedLeft" : "columnHeader--pinnedRight"]}`).forEach((element) => {
    const currentColIndex = parseCellColIndex(element);
    if (currentColIndex !== null && filterFn(currentColIndex, element)) {
      elements.push(element);
    }
  });
  return elements;
};
function findLeftPinnedHeadersAfterCol(api, col, isRtl) {
  const colIndex = parseCellColIndex(col);
  return findPinnedHeaders({
    api,
    position: isRtl ? "right" : "left",
    colIndex,
    filterFn: (index) => isRtl ? index < colIndex : index > colIndex
  });
}
function findRightPinnedHeadersBeforeCol(api, col, isRtl) {
  const colIndex = parseCellColIndex(col);
  return findPinnedHeaders({
    api,
    position: isRtl ? "left" : "right",
    colIndex,
    filterFn: (index, element) => {
      if (element.classList.contains(gridClasses["columnHeader--last"])) {
        return false;
      }
      return isRtl ? index > colIndex : index < colIndex;
    }
  });
}
function findGridHeader(api, field) {
  const headers = api.columnHeadersContainerRef.current;
  return headers.querySelector(`:scope > div > [data-field="${escapeOperandAttributeSelector(field)}"][role="columnheader"]`);
}
function findGridHeaderFilter(api, field) {
  const headers = api.columnHeadersContainerRef.current;
  return headers.querySelector(`:scope > .${gridClasses.headerFilterRow} > [data-field="${escapeOperandAttributeSelector(field)}"]`);
}
function findGridCells(api, field) {
  const container = api.virtualScrollerRef.current;
  return Array.from(container.querySelectorAll(`:scope > div > div > div > [data-field="${escapeOperandAttributeSelector(field)}"][role="gridcell"]`));
}
function queryRows(api) {
  return api.virtualScrollerRef.current.querySelectorAll(
    // Use > to ignore rows from nested Data Grids (for example in detail panel)
    `:scope > div > div > .${gridClasses.row}`
  );
}
function parseCellColIndex(col) {
  const ariaColIndex = col.getAttribute("aria-colindex");
  if (!ariaColIndex) {
    return null;
  }
  return Number(ariaColIndex) - 1;
}

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderTitle.js
var import_jsx_runtime48 = __toESM(require_jsx_runtime(), 1);
var _excluded28 = ["className", "aria-label"];
var useUtilityClasses27 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["columnHeaderTitle"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridColumnHeaderTitleRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnHeaderTitle"
})({
  textOverflow: "ellipsis",
  overflow: "hidden",
  whiteSpace: "nowrap",
  fontWeight: "var(--unstable_DataGrid-headWeight)",
  lineHeight: "normal"
});
var ColumnHeaderInnerTitle = forwardRef2(function ColumnHeaderInnerTitle2(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded28);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses27(rootProps);
  return (0, import_jsx_runtime48.jsx)(GridColumnHeaderTitleRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref
  }));
});
if (true) ColumnHeaderInnerTitle.displayName = "ColumnHeaderInnerTitle";
function GridColumnHeaderTitle(props) {
  var _a;
  const {
    label,
    description
  } = props;
  const rootProps = useGridRootProps();
  const titleRef = React74.useRef(null);
  const [tooltip, setTooltip] = React74.useState("");
  const handleMouseOver = React74.useCallback(() => {
    if (!description && (titleRef == null ? void 0 : titleRef.current)) {
      const isOver = isOverflown(titleRef.current);
      if (isOver) {
        setTooltip(label);
      } else {
        setTooltip("");
      }
    }
  }, [description, label]);
  return (0, import_jsx_runtime48.jsx)(rootProps.slots.baseTooltip, _extends({
    title: description || tooltip
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, {
    children: (0, import_jsx_runtime48.jsx)(ColumnHeaderInnerTitle, {
      onMouseOver: handleMouseOver,
      ref: titleRef,
      children: label
    })
  }));
}
true ? GridColumnHeaderTitle.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  columnWidth: import_prop_types26.default.number.isRequired,
  description: import_prop_types26.default.node,
  label: import_prop_types26.default.string.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderSeparator.js
var React75 = __toESM(require_react(), 1);
var import_prop_types27 = __toESM(require_prop_types(), 1);
var import_jsx_runtime49 = __toESM(require_jsx_runtime(), 1);
var _excluded29 = ["resizable", "resizing", "height", "side"];
var GridColumnHeaderSeparatorSides = (function(GridColumnHeaderSeparatorSides2) {
  GridColumnHeaderSeparatorSides2["Left"] = "left";
  GridColumnHeaderSeparatorSides2["Right"] = "right";
  return GridColumnHeaderSeparatorSides2;
})(GridColumnHeaderSeparatorSides || {});
var useUtilityClasses28 = (ownerState) => {
  const {
    resizable,
    resizing,
    classes: classes2,
    side
  } = ownerState;
  const slots = {
    root: ["columnSeparator", resizable && "columnSeparator--resizable", resizing && "columnSeparator--resizing", side && `columnSeparator--side${capitalize(side)}`],
    icon: ["iconSeparator"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridColumnHeaderSeparatorRaw(props) {
  const {
    height,
    side = GridColumnHeaderSeparatorSides.Right
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded29);
  const rootProps = useGridRootProps();
  const ownerState = _extends({}, props, {
    side,
    classes: rootProps.classes
  });
  const classes2 = useUtilityClasses28(ownerState);
  const stopClick = React75.useCallback((event) => {
    event.preventDefault();
    event.stopPropagation();
  }, []);
  return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events,jsx-a11y/no-static-element-interactions
    (0, import_jsx_runtime49.jsx)("div", _extends({
      className: classes2.root,
      style: {
        minHeight: height
      }
    }, other, {
      onClick: stopClick,
      children: (0, import_jsx_runtime49.jsx)(rootProps.slots.columnResizeIcon, {
        className: classes2.icon
      })
    }))
  );
}
var GridColumnHeaderSeparator = React75.memo(GridColumnHeaderSeparatorRaw);
if (true) GridColumnHeaderSeparator.displayName = "GridColumnHeaderSeparator";
true ? GridColumnHeaderSeparatorRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  height: import_prop_types27.default.number.isRequired,
  resizable: import_prop_types27.default.bool.isRequired,
  resizing: import_prop_types27.default.bool.isRequired,
  side: import_prop_types27.default.oneOf(["left", "right"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridGenericColumnHeaderItem.js
var import_jsx_runtime50 = __toESM(require_jsx_runtime(), 1);
var _excluded30 = ["classes", "columnMenuOpen", "colIndex", "height", "isResizing", "sortDirection", "hasFocus", "tabIndex", "separatorSide", "isDraggable", "headerComponent", "description", "elementId", "width", "columnMenuIconButton", "columnMenu", "columnTitleIconButtons", "headerClassName", "label", "resizable", "draggableContainerProps", "columnHeaderSeparatorProps", "style"];
var GridGenericColumnHeaderItem = forwardRef2(function GridGenericColumnHeaderItem2(props, ref) {
  const {
    classes: classes2,
    colIndex,
    height,
    isResizing,
    sortDirection,
    tabIndex,
    separatorSide,
    isDraggable,
    headerComponent,
    description,
    width,
    columnMenuIconButton = null,
    columnMenu = null,
    columnTitleIconButtons = null,
    headerClassName,
    label,
    resizable,
    draggableContainerProps,
    columnHeaderSeparatorProps,
    style
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded30);
  const rootProps = useGridRootProps();
  const headerCellRef = React76.useRef(null);
  const handleRef = useForkRef(headerCellRef, ref);
  let ariaSort = "none";
  if (sortDirection != null) {
    ariaSort = sortDirection === "asc" ? "ascending" : "descending";
  }
  return (0, import_jsx_runtime50.jsxs)("div", _extends({
    className: clsx_default(classes2.root, headerClassName),
    style: _extends({}, style, {
      width
    }),
    role: "columnheader",
    tabIndex,
    "aria-colindex": colIndex + 1,
    "aria-sort": ariaSort
  }, other, {
    ref: handleRef,
    children: [(0, import_jsx_runtime50.jsxs)("div", _extends({
      className: classes2.draggableContainer,
      draggable: isDraggable,
      role: "presentation"
    }, draggableContainerProps, {
      children: [(0, import_jsx_runtime50.jsxs)("div", {
        className: classes2.titleContainer,
        role: "presentation",
        children: [(0, import_jsx_runtime50.jsx)("div", {
          className: classes2.titleContainerContent,
          children: headerComponent !== void 0 ? headerComponent : (0, import_jsx_runtime50.jsx)(GridColumnHeaderTitle, {
            label,
            description,
            columnWidth: width
          })
        }), columnTitleIconButtons]
      }), columnMenuIconButton]
    })), (0, import_jsx_runtime50.jsx)(GridColumnHeaderSeparator, _extends({
      resizable: !rootProps.disableColumnResize && !!resizable,
      resizing: isResizing,
      height,
      side: separatorSide
    }, columnHeaderSeparatorProps)), columnMenu]
  }));
});
if (true) GridGenericColumnHeaderItem.displayName = "GridGenericColumnHeaderItem";

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderItem.js
var import_jsx_runtime51 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses29 = (ownerState) => {
  const {
    disableColumnSorting
  } = useGridRootProps();
  const {
    colDef,
    classes: classes2,
    isDragging,
    sortDirection,
    showRightBorder,
    showLeftBorder,
    filterItemsCounter,
    pinnedPosition,
    isSiblingFocused
  } = ownerState;
  const isColumnSortable = colDef.sortable && !disableColumnSorting;
  const isColumnSorted = sortDirection != null;
  const isColumnFiltered = filterItemsCounter != null && filterItemsCounter > 0;
  const isColumnNumeric = colDef.type === "number";
  const slots = {
    root: [
      "columnHeader",
      colDef.headerAlign && `columnHeader--align${capitalize(colDef.headerAlign)}`,
      isColumnSortable && "columnHeader--sortable",
      isDragging && "columnHeader--moving",
      isColumnSorted && "columnHeader--sorted",
      isColumnFiltered && "columnHeader--filtered",
      isColumnNumeric && "columnHeader--numeric",
      "withBorderColor",
      showRightBorder && "columnHeader--withRightBorder",
      showLeftBorder && "columnHeader--withLeftBorder",
      pinnedPosition === PinnedColumnPosition.LEFT && "columnHeader--pinnedLeft",
      pinnedPosition === PinnedColumnPosition.RIGHT && "columnHeader--pinnedRight",
      // TODO: Remove classes below and restore `:has` selectors when they are supported in jsdom
      // See https://github.com/mui/mui-x/pull/14559
      isSiblingFocused && "columnHeader--siblingFocused"
    ],
    draggableContainer: ["columnHeaderDraggableContainer"],
    titleContainer: ["columnHeaderTitleContainer"],
    titleContainerContent: ["columnHeaderTitleContainerContent"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridColumnHeaderItem(props) {
  var _a, _b, _c, _d;
  const {
    colDef,
    columnMenuOpen,
    colIndex,
    headerHeight,
    isResizing,
    isLast,
    sortDirection,
    sortIndex,
    filterItemsCounter,
    hasFocus,
    tabIndex,
    disableReorder,
    separatorSide,
    showLeftBorder,
    showRightBorder,
    pinnedPosition,
    pinnedOffset
  } = props;
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const isRtl = useRtl();
  const headerCellRef = React77.useRef(null);
  const columnMenuId = useId();
  const columnMenuButtonId = useId();
  const iconButtonRef = React77.useRef(null);
  const [showColumnMenuIcon, setShowColumnMenuIcon] = React77.useState(columnMenuOpen);
  const columnMenuSlotProps = (_a = rootProps.slotProps) == null ? void 0 : _a.columnMenu;
  const columnMenuComponent = rootProps.slots.columnMenu;
  const defaultSlots4 = columnMenuComponent == null ? void 0 : columnMenuComponent.defaultSlots;
  const defaultSlotProps = columnMenuComponent == null ? void 0 : columnMenuComponent.defaultSlotProps;
  const hasKnownDefaultColumnMenu = defaultSlots4 != null && defaultSlotProps != null;
  const columnMenuItemKeys = React77.useMemo(() => {
    if (!hasKnownDefaultColumnMenu) {
      return [];
    }
    return getColumnMenuItemKeys({
      apiRef,
      colDef,
      defaultSlots: defaultSlots4,
      defaultSlotProps,
      slots: columnMenuSlotProps == null ? void 0 : columnMenuSlotProps.slots,
      slotProps: columnMenuSlotProps == null ? void 0 : columnMenuSlotProps.slotProps
    });
  }, [apiRef, colDef, defaultSlotProps, defaultSlots4, hasKnownDefaultColumnMenu, columnMenuSlotProps == null ? void 0 : columnMenuSlotProps.slotProps, columnMenuSlotProps == null ? void 0 : columnMenuSlotProps.slots]);
  const hasColumnMenuItems = !hasKnownDefaultColumnMenu || columnMenuItemKeys.length > 0;
  const isDraggable = !rootProps.disableColumnReorder && !disableReorder && !colDef.disableReorder;
  let headerComponent;
  if (colDef.renderHeader) {
    headerComponent = colDef.renderHeader(apiRef.current.getColumnHeaderParams(colDef.field));
  }
  const ownerState = _extends({}, props, {
    classes: rootProps.classes,
    showRightBorder,
    showLeftBorder
  });
  const classes2 = useUtilityClasses29(ownerState);
  const publish = React77.useCallback((eventName) => (event) => {
    if (isEventTargetInPortal(event)) {
      return;
    }
    apiRef.current.publishEvent(eventName, apiRef.current.getColumnHeaderParams(colDef.field), event);
  }, [apiRef, colDef.field]);
  const mouseEventsHandlers = React77.useMemo(() => ({
    onClick: publish("columnHeaderClick"),
    onContextMenu: publish("columnHeaderContextMenu"),
    onDoubleClick: publish("columnHeaderDoubleClick"),
    onMouseOver: publish("columnHeaderOver"),
    // TODO remove as it's not used
    onMouseOut: publish("columnHeaderOut"),
    // TODO remove as it's not used
    onMouseEnter: publish("columnHeaderEnter"),
    // TODO remove as it's not used
    onMouseLeave: publish("columnHeaderLeave"),
    // TODO remove as it's not used
    onKeyDown: publish("columnHeaderKeyDown"),
    onFocus: publish("columnHeaderFocus"),
    onBlur: publish("columnHeaderBlur")
  }), [publish]);
  const draggableEventHandlers = React77.useMemo(() => isDraggable ? {
    onDragStart: publish("columnHeaderDragStart"),
    onDragEnter: publish("columnHeaderDragEnter"),
    onDragOver: publish("columnHeaderDragOver"),
    onDragEndCapture: publish("columnHeaderDragEnd")
  } : {}, [isDraggable, publish]);
  const columnHeaderSeparatorProps = React77.useMemo(() => ({
    onMouseDown: publish("columnSeparatorMouseDown"),
    onDoubleClick: publish("columnSeparatorDoubleClick")
  }), [publish]);
  React77.useEffect(() => {
    if (!showColumnMenuIcon && columnMenuOpen) {
      setShowColumnMenuIcon(columnMenuOpen);
    }
  }, [showColumnMenuIcon, columnMenuOpen]);
  React77.useEffect(() => {
    if (hasKnownDefaultColumnMenu && columnMenuOpen && !hasColumnMenuItems) {
      apiRef.current.hideColumnMenu();
    }
  }, [apiRef, columnMenuOpen, hasColumnMenuItems, hasKnownDefaultColumnMenu]);
  const handleExited = React77.useCallback(() => {
    setShowColumnMenuIcon(false);
  }, []);
  const columnMenuIconButton = !rootProps.disableColumnMenu && !colDef.disableColumnMenu && hasColumnMenuItems && (0, import_jsx_runtime51.jsx)(ColumnHeaderMenuIcon, {
    colDef,
    columnMenuId,
    columnMenuButtonId,
    open: showColumnMenuIcon,
    iconButtonRef
  });
  const columnMenu = (0, import_jsx_runtime51.jsx)(GridColumnHeaderMenu, {
    columnMenuId,
    columnMenuButtonId,
    field: colDef.field,
    open: columnMenuOpen,
    target: iconButtonRef.current,
    ContentComponent: rootProps.slots.columnMenu,
    contentComponentProps: (_b = rootProps.slotProps) == null ? void 0 : _b.columnMenu,
    onExited: handleExited
  });
  const sortingOrder = colDef.sortingOrder ?? rootProps.sortingOrder;
  const showSortIcon = (colDef.sortable || sortDirection != null) && !colDef.hideSortIcons && !rootProps.disableColumnSorting;
  const columnTitleIconButtons = (0, import_jsx_runtime51.jsxs)(React77.Fragment, {
    children: [!rootProps.disableColumnFilter && (0, import_jsx_runtime51.jsx)(rootProps.slots.columnHeaderFilterIconButton, _extends({
      field: colDef.field,
      counter: filterItemsCounter
    }, (_c = rootProps.slotProps) == null ? void 0 : _c.columnHeaderFilterIconButton)), showSortIcon && (0, import_jsx_runtime51.jsx)(rootProps.slots.columnHeaderSortIcon, _extends({
      field: colDef.field,
      direction: sortDirection,
      index: sortIndex,
      sortingOrder,
      disabled: !colDef.sortable
    }, (_d = rootProps.slotProps) == null ? void 0 : _d.columnHeaderSortIcon))]
  });
  React77.useLayoutEffect(() => {
    const columnMenuState = apiRef.current.state.columnMenu;
    if (hasFocus && !columnMenuState.open) {
      const focusableElement = headerCellRef.current.querySelector('[tabindex="0"]');
      const elementToFocus = focusableElement || headerCellRef.current;
      if (!elementToFocus) {
        return;
      }
      if (doesSupportPreventScroll()) {
        elementToFocus.focus({
          preventScroll: true
        });
      } else {
        const scrollPosition = apiRef.current.getScrollPosition();
        elementToFocus.focus();
        apiRef.current.scroll(scrollPosition);
      }
    }
  }, [apiRef, hasFocus]);
  const headerClassName = typeof colDef.headerClassName === "function" ? colDef.headerClassName({
    field: colDef.field,
    colDef
  }) : colDef.headerClassName;
  const label = colDef.headerName ?? colDef.field;
  const style = React77.useMemo(() => attachPinnedStyle(_extends({}, props.style), isRtl, pinnedPosition, pinnedOffset), [pinnedPosition, pinnedOffset, props.style, isRtl]);
  return (0, import_jsx_runtime51.jsx)(GridGenericColumnHeaderItem, _extends({
    ref: headerCellRef,
    classes: classes2,
    columnMenuOpen,
    colIndex,
    height: headerHeight,
    isResizing,
    sortDirection,
    hasFocus,
    tabIndex,
    separatorSide,
    isDraggable,
    headerComponent,
    description: colDef.description,
    elementId: colDef.field,
    width: colDef.computedWidth,
    columnMenuIconButton,
    columnTitleIconButtons,
    headerClassName: clsx_default(headerClassName, isLast && gridClasses["columnHeader--last"]),
    label,
    resizable: !rootProps.disableColumnResize && !!colDef.resizable,
    "data-field": colDef.field,
    columnMenu,
    draggableContainerProps: draggableEventHandlers,
    columnHeaderSeparatorProps,
    style
  }, mouseEventsHandlers));
}
true ? GridColumnHeaderItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types28.default.object.isRequired,
  colIndex: import_prop_types28.default.number.isRequired,
  columnMenuOpen: import_prop_types28.default.bool.isRequired,
  disableReorder: import_prop_types28.default.bool,
  filterItemsCounter: import_prop_types28.default.number,
  hasFocus: import_prop_types28.default.bool,
  headerHeight: import_prop_types28.default.number.isRequired,
  isDragging: import_prop_types28.default.bool.isRequired,
  isLast: import_prop_types28.default.bool.isRequired,
  isResizing: import_prop_types28.default.bool.isRequired,
  isSiblingFocused: import_prop_types28.default.bool.isRequired,
  pinnedOffset: import_prop_types28.default.number,
  pinnedPosition: import_prop_types28.default.oneOf([0, 1, 2, 3]),
  separatorSide: import_prop_types28.default.oneOf(["left", "right"]),
  showLeftBorder: import_prop_types28.default.bool.isRequired,
  showRightBorder: import_prop_types28.default.bool.isRequired,
  sortDirection: import_prop_types28.default.oneOf(["asc", "desc"]),
  sortIndex: import_prop_types28.default.number,
  style: import_prop_types28.default.object,
  tabIndex: import_prop_types28.default.oneOf([-1, 0]).isRequired
} : void 0;
var Memoized3 = fastMemo(GridColumnHeaderItem);

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderSortIcon.js
var React80 = __toESM(require_react(), 1);
var import_prop_types30 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridIconButtonContainer.js
var React78 = __toESM(require_react(), 1);
var import_jsx_runtime52 = __toESM(require_jsx_runtime(), 1);
var _excluded31 = ["className"];
var useUtilityClasses30 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["iconButtonContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridIconButtonContainerRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "IconButtonContainer"
})(() => ({
  display: "flex",
  visibility: "hidden",
  width: 0
}));
var GridIconButtonContainer = forwardRef2(function GridIconButtonContainer2(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded31);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses30(rootProps);
  return (0, import_jsx_runtime52.jsx)(GridIconButtonContainerRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref
  }));
});
if (true) GridIconButtonContainer.displayName = "GridIconButtonContainer";

// node_modules/@mui/x-data-grid/esm/components/GridColumnSortButton.js
var React79 = __toESM(require_react(), 1);
var import_prop_types29 = __toESM(require_prop_types(), 1);
var import_jsx_runtime53 = __toESM(require_jsx_runtime(), 1);
var _excluded32 = ["direction", "index", "sortingOrder", "disabled", "className"];
var useUtilityClasses31 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["sortButton"],
    icon: ["sortIcon"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridColumnSortButtonRoot = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "SortButton"
})({
  transition: vars.transition(["opacity"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  })
});
function getIcon(icons, direction, className, sortingOrder) {
  let Icon;
  const iconProps = {};
  if (direction === "asc") {
    Icon = icons.columnSortedAscendingIcon;
  } else if (direction === "desc") {
    Icon = icons.columnSortedDescendingIcon;
  } else {
    Icon = icons.columnUnsortedIcon;
    iconProps.sortingOrder = sortingOrder;
  }
  return Icon ? (0, import_jsx_runtime53.jsx)(Icon, _extends({
    fontSize: "small",
    className
  }, iconProps)) : null;
}
function GridColumnSortButton(props) {
  var _a, _b;
  const {
    direction,
    index,
    sortingOrder,
    disabled,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded32);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = _extends({}, props, {
    classes: rootProps.classes
  });
  const classes2 = useUtilityClasses31(ownerState);
  const iconElement = getIcon(rootProps.slots, direction, classes2.icon, sortingOrder);
  if (!iconElement) {
    return null;
  }
  const iconButton = (0, import_jsx_runtime53.jsx)(GridColumnSortButtonRoot, _extends({
    as: rootProps.slots.baseIconButton,
    ownerState,
    "aria-label": apiRef.current.getLocaleText("columnHeaderSortIconLabel"),
    size: "small",
    disabled,
    className: clsx_default(classes2.root, className)
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, other, {
    children: iconElement
  }));
  return (0, import_jsx_runtime53.jsx)(rootProps.slots.baseTooltip, _extends({
    title: apiRef.current.getLocaleText("columnHeaderSortIconLabel"),
    enterDelay: 1e3
  }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseTooltip, {
    children: (0, import_jsx_runtime53.jsxs)("span", {
      children: [index != null && (0, import_jsx_runtime53.jsx)(rootProps.slots.baseBadge, {
        badgeContent: index,
        color: "default",
        overlap: "circular",
        children: iconButton
      }), index == null && iconButton]
    })
  }));
}
true ? GridColumnSortButton.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  direction: import_prop_types29.default.oneOf(["asc", "desc"]),
  disabled: import_prop_types29.default.bool,
  field: import_prop_types29.default.string.isRequired,
  index: import_prop_types29.default.number,
  onClick: import_prop_types29.default.func,
  sortingOrder: import_prop_types29.default.arrayOf(import_prop_types29.default.oneOf(["asc", "desc"])).isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderSortIcon.js
var import_jsx_runtime54 = __toESM(require_jsx_runtime(), 1);
function GridColumnHeaderSortIconRaw(props) {
  return (0, import_jsx_runtime54.jsx)(GridIconButtonContainer, {
    children: (0, import_jsx_runtime54.jsx)(GridColumnSortButton, _extends({}, props, {
      tabIndex: -1
    }))
  });
}
var GridColumnHeaderSortIcon = React80.memo(GridColumnHeaderSortIconRaw);
if (true) GridColumnHeaderSortIcon.displayName = "GridColumnHeaderSortIcon";
true ? GridColumnHeaderSortIconRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types30.default.string,
  color: import_prop_types30.default.oneOf(["default", "inherit", "primary"]),
  direction: import_prop_types30.default.oneOf(["asc", "desc"]),
  disabled: import_prop_types30.default.bool,
  edge: import_prop_types30.default.oneOf(["end", "start", false]),
  field: import_prop_types30.default.string.isRequired,
  id: import_prop_types30.default.string,
  index: import_prop_types30.default.number,
  label: import_prop_types30.default.string,
  role: import_prop_types30.default.string,
  size: import_prop_types30.default.oneOf(["large", "medium", "small"]),
  sortingOrder: import_prop_types30.default.arrayOf(import_prop_types30.default.oneOf(["asc", "desc"])).isRequired,
  style: import_prop_types30.default.object,
  tabIndex: import_prop_types30.default.number,
  title: import_prop_types30.default.string,
  touchRippleRef: import_prop_types30.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnHeaderFilterIconButton.js
var React81 = __toESM(require_react(), 1);
var import_prop_types31 = __toESM(require_prop_types(), 1);
var import_jsx_runtime55 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses32 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    icon: ["filterIcon"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridColumnHeaderFilterIconButtonWrapped(props) {
  if (!props.counter) {
    return null;
  }
  return (0, import_jsx_runtime55.jsx)(GridColumnHeaderFilterIconButton, _extends({}, props));
}
true ? GridColumnHeaderFilterIconButtonWrapped.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  counter: import_prop_types31.default.number,
  field: import_prop_types31.default.string.isRequired,
  onClick: import_prop_types31.default.func
} : void 0;
function GridColumnHeaderFilterIconButton(props) {
  var _a, _b;
  const {
    counter,
    field,
    onClick
  } = props;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = _extends({}, props, {
    classes: rootProps.classes
  });
  const classes2 = useUtilityClasses32(ownerState);
  const labelId = useId();
  const isOpen = useGridSelector(apiRef, gridPreferencePanelSelectorWithLabel, labelId);
  const panelId = useId();
  const toggleFilter = React81.useCallback((event) => {
    event.preventDefault();
    event.stopPropagation();
    const {
      open,
      openedPanelValue
    } = gridPreferencePanelStateSelector(apiRef);
    if (open && openedPanelValue === GridPreferencePanelsValue.filters) {
      apiRef.current.hideFilterPanel();
    } else {
      apiRef.current.showFilterPanel(void 0, panelId, labelId);
    }
    if (onClick) {
      onClick(apiRef.current.getColumnHeaderParams(field), event);
    }
  }, [apiRef, field, onClick, panelId, labelId]);
  if (!counter) {
    return null;
  }
  const iconButton = (0, import_jsx_runtime55.jsx)(rootProps.slots.baseIconButton, _extends({
    id: labelId,
    onClick: toggleFilter,
    "aria-label": apiRef.current.getLocaleText("columnHeaderFiltersLabel"),
    size: "small",
    tabIndex: -1,
    "aria-haspopup": "menu",
    "aria-expanded": isOpen,
    "aria-controls": isOpen ? panelId : void 0
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    children: (0, import_jsx_runtime55.jsx)(rootProps.slots.columnFilteredIcon, {
      className: classes2.icon,
      fontSize: "small"
    })
  }));
  return (0, import_jsx_runtime55.jsx)(rootProps.slots.baseTooltip, _extends({
    title: apiRef.current.getLocaleText("columnHeaderFiltersTooltipActive")(counter),
    enterDelay: 1e3
  }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseTooltip, {
    children: (0, import_jsx_runtime55.jsxs)(GridIconButtonContainer, {
      children: [counter > 1 && (0, import_jsx_runtime55.jsx)(rootProps.slots.baseBadge, {
        badgeContent: counter,
        color: "default",
        children: iconButton
      }), counter === 1 && iconButton]
    })
  }));
}
true ? GridColumnHeaderFilterIconButton.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  counter: import_prop_types31.default.number,
  field: import_prop_types31.default.string.isRequired,
  onClick: import_prop_types31.default.func
} : void 0;

// node_modules/@mui/utils/esm/createChainedFunction/createChainedFunction.js
function createChainedFunction(...funcs) {
  return funcs.reduce((acc, func) => {
    if (func == null) {
      return acc;
    }
    return function chainedFunction(...args) {
      acc.apply(this, args);
      func.apply(this, args);
    };
  }, () => {
  });
}

// node_modules/@mui/material/esm/utils/createSvgIcon.js
var React83 = __toESM(require_react(), 1);

// node_modules/@mui/material/esm/SvgIcon/SvgIcon.js
var React82 = __toESM(require_react(), 1);
var import_prop_types32 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/SvgIcon/svgIconClasses.js
function getSvgIconUtilityClass(slot) {
  return generateUtilityClass("MuiSvgIcon", slot);
}
var svgIconClasses = generateUtilityClasses("MuiSvgIcon", ["root", "colorPrimary", "colorSecondary", "colorAction", "colorError", "colorDisabled", "fontSizeInherit", "fontSizeSmall", "fontSizeMedium", "fontSizeLarge"]);

// node_modules/@mui/material/esm/SvgIcon/SvgIcon.js
var import_jsx_runtime56 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses33 = (ownerState) => {
  const {
    color,
    fontSize,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", color !== "inherit" && `color${capitalize_default(color)}`, `fontSize${capitalize_default(fontSize)}`]
  };
  return composeClasses(slots, getSvgIconUtilityClass, classes2);
};
var SvgIconRoot = styled_default2("svg", {
  name: "MuiSvgIcon",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.color !== "inherit" && styles5[`color${capitalize_default(ownerState.color)}`], styles5[`fontSize${capitalize_default(ownerState.fontSize)}`]];
  }
})(memoTheme_default(({
  theme
}) => {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
  return {
    userSelect: "none",
    width: "1em",
    height: "1em",
    display: "inline-block",
    flexShrink: 0,
    transition: (_d = (_a = theme.transitions) == null ? void 0 : _a.create) == null ? void 0 : _d.call(_a, "fill", {
      duration: (_c = (_b = (theme.vars ?? theme).transitions) == null ? void 0 : _b.duration) == null ? void 0 : _c.shorter
    }),
    variants: [
      {
        props: (props) => !props.hasSvgAsChild,
        style: {
          // the <svg> will define the property that has `currentColor`
          // for example heroicons uses fill="none" and stroke="currentColor"
          fill: "currentColor"
        }
      },
      {
        props: {
          fontSize: "inherit"
        },
        style: {
          fontSize: "inherit"
        }
      },
      {
        props: {
          fontSize: "small"
        },
        style: {
          fontSize: ((_f = (_e = theme.typography) == null ? void 0 : _e.pxToRem) == null ? void 0 : _f.call(_e, 20)) || "1.25rem"
        }
      },
      {
        props: {
          fontSize: "medium"
        },
        style: {
          fontSize: ((_h = (_g = theme.typography) == null ? void 0 : _g.pxToRem) == null ? void 0 : _h.call(_g, 24)) || "1.5rem"
        }
      },
      {
        props: {
          fontSize: "large"
        },
        style: {
          fontSize: ((_j = (_i = theme.typography) == null ? void 0 : _i.pxToRem) == null ? void 0 : _j.call(_i, 35)) || "2.1875rem"
        }
      },
      // TODO v5 deprecate color prop, v6 remove for sx
      ...Object.entries((theme.vars ?? theme).palette).filter(([, value]) => value && value.main).map(([color]) => {
        var _a2, _b2;
        return {
          props: {
            color
          },
          style: {
            color: (_b2 = (_a2 = (theme.vars ?? theme).palette) == null ? void 0 : _a2[color]) == null ? void 0 : _b2.main
          }
        };
      }),
      {
        props: {
          color: "action"
        },
        style: {
          color: (_l = (_k = (theme.vars ?? theme).palette) == null ? void 0 : _k.action) == null ? void 0 : _l.active
        }
      },
      {
        props: {
          color: "disabled"
        },
        style: {
          color: (_n = (_m = (theme.vars ?? theme).palette) == null ? void 0 : _m.action) == null ? void 0 : _n.disabled
        }
      },
      {
        props: {
          color: "inherit"
        },
        style: {
          color: void 0
        }
      }
    ]
  };
}));
var SvgIcon = React82.forwardRef(function SvgIcon2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiSvgIcon"
  });
  const {
    children: children2,
    className,
    color = "inherit",
    component = "svg",
    fontSize = "medium",
    htmlColor,
    inheritViewBox = false,
    titleAccess,
    viewBox = "0 0 24 24",
    ...other
  } = props;
  const hasSvgAsChild = React82.isValidElement(children2) && children2.type === "svg";
  const ownerState = {
    ...props,
    color,
    component,
    fontSize,
    instanceFontSize: inProps.fontSize,
    inheritViewBox,
    viewBox,
    hasSvgAsChild
  };
  const more = {};
  if (!inheritViewBox) {
    more.viewBox = viewBox;
  }
  const classes2 = useUtilityClasses33(ownerState);
  return (0, import_jsx_runtime56.jsxs)(SvgIconRoot, {
    as: component,
    className: clsx_default(classes2.root, className),
    focusable: "false",
    color: htmlColor,
    "aria-hidden": titleAccess ? void 0 : true,
    role: titleAccess ? "img" : void 0,
    ref,
    ...more,
    ...other,
    ...hasSvgAsChild && children2.props,
    ownerState,
    children: [hasSvgAsChild ? children2.props.children : children2, titleAccess ? (0, import_jsx_runtime56.jsx)("title", {
      children: titleAccess
    }) : null]
  });
});
true ? SvgIcon.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Node passed into the SVG element.
   */
  children: import_prop_types32.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types32.default.object,
  /**
   * @ignore
   */
  className: import_prop_types32.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * You can use the `htmlColor` prop to apply a color attribute to the SVG element.
   * @default 'inherit'
   */
  color: import_prop_types32.default.oneOfType([import_prop_types32.default.oneOf(["inherit", "action", "disabled", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types32.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types32.default.elementType,
  /**
   * The fontSize applied to the icon. Defaults to 24px, but can be configure to inherit font size.
   * @default 'medium'
   */
  fontSize: import_prop_types32.default.oneOfType([import_prop_types32.default.oneOf(["inherit", "large", "medium", "small"]), import_prop_types32.default.string]),
  /**
   * Applies a color attribute to the SVG element.
   */
  htmlColor: import_prop_types32.default.string,
  /**
   * If `true`, the root node will inherit the custom `component`'s viewBox and the `viewBox`
   * prop will be ignored.
   * Useful when you want to reference a custom `component` and have `SvgIcon` pass that
   * `component`'s viewBox to the root node.
   * @default false
   */
  inheritViewBox: import_prop_types32.default.bool,
  /**
   * The shape-rendering attribute. The behavior of the different options is described on the
   * [MDN Web Docs](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Attribute/shape-rendering).
   * If you are having issues with blurry icons you should investigate this prop.
   */
  shapeRendering: import_prop_types32.default.string,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types32.default.oneOfType([import_prop_types32.default.arrayOf(import_prop_types32.default.oneOfType([import_prop_types32.default.func, import_prop_types32.default.object, import_prop_types32.default.bool])), import_prop_types32.default.func, import_prop_types32.default.object]),
  /**
   * Provides a human-readable title for the element that contains it.
   * https://www.w3.org/TR/SVG-access/#Equivalent
   */
  titleAccess: import_prop_types32.default.string,
  /**
   * Allows you to redefine what the coordinates without units mean inside an SVG element.
   * For example, if the SVG element is 500 (width) by 200 (height),
   * and you pass viewBox="0 0 50 20",
   * this means that the coordinates inside the SVG will go from the top left corner (0,0)
   * to bottom right (50,20) and each unit will be worth 10px.
   * @default '0 0 24 24'
   */
  viewBox: import_prop_types32.default.string
} : void 0;
SvgIcon.muiName = "SvgIcon";
var SvgIcon_default = SvgIcon;

// node_modules/@mui/material/esm/utils/createSvgIcon.js
var import_jsx_runtime57 = __toESM(require_jsx_runtime(), 1);
function createSvgIcon(path, displayName) {
  function Component(props, ref) {
    return (0, import_jsx_runtime57.jsx)(SvgIcon_default, {
      "data-testid": true ? `${displayName}Icon` : void 0,
      ref,
      ...props,
      children: path
    });
  }
  if (true) {
    Component.displayName = `${displayName}Icon`;
  }
  Component.muiName = SvgIcon_default.muiName;
  return React83.memo(React83.forwardRef(Component));
}

// node_modules/@mui/utils/esm/debounce/debounce.js
function debounce(func, wait = 166) {
  let timeout2;
  function debounced(...args) {
    const later = () => {
      func.apply(this, args);
    };
    clearTimeout(timeout2);
    timeout2 = setTimeout(later, wait);
  }
  debounced.clear = () => {
    clearTimeout(timeout2);
  };
  return debounced;
}

// node_modules/@mui/material/esm/utils/debounce.js
var debounce_default = debounce;

// node_modules/@mui/material/esm/utils/isMuiElement.js
var isMuiElement_default = isMuiElement;

// node_modules/@mui/material/esm/utils/ownerDocument.js
var ownerDocument_default = ownerDocument;

// node_modules/@mui/utils/esm/ownerWindow/ownerWindow.js
function ownerWindow(node) {
  const doc = ownerDocument(node);
  return doc.defaultView || window;
}

// node_modules/@mui/material/esm/utils/ownerWindow.js
var ownerWindow_default = ownerWindow;

// node_modules/@mui/utils/esm/setRef/setRef.js
function setRef(ref, value) {
  if (typeof ref === "function") {
    ref(value);
  } else if (ref) {
    ref.current = value;
  }
}

// node_modules/@mui/material/esm/utils/useEnhancedEffect.js
var useEnhancedEffect_default2 = useEnhancedEffect_default;

// node_modules/@mui/material/esm/utils/useId.js
var useId_default = useId;

// node_modules/@mui/utils/esm/unsupportedProp/unsupportedProp.js
function unsupportedProp(props, propName, componentName, location, propFullName) {
  if (false) {
    return null;
  }
  const propFullNameSafe = propFullName || propName;
  if (typeof props[propName] !== "undefined") {
    return new Error(`The prop \`${propFullNameSafe}\` is not supported. Please remove it.`);
  }
  return null;
}

// node_modules/@mui/material/esm/utils/unsupportedProp.js
var unsupportedProp_default = unsupportedProp;

// node_modules/@mui/utils/esm/useControlled/useControlled.js
var React84 = __toESM(require_react(), 1);
function useControlled(props) {
  const {
    controlled,
    default: defaultProp,
    name,
    state = "value"
  } = props;
  const {
    current: isControlled
  } = React84.useRef(controlled !== void 0);
  const [valueState, setValue] = React84.useState(defaultProp);
  const value = isControlled ? controlled : valueState;
  if (true) {
    React84.useEffect(() => {
      if (isControlled !== (controlled !== void 0)) {
        console.error([`MUI: A component is changing the ${isControlled ? "" : "un"}controlled ${state} state of ${name} to be ${isControlled ? "un" : ""}controlled.`, "Elements should not switch from uncontrolled to controlled (or vice versa).", `Decide between using a controlled or uncontrolled ${name} element for the lifetime of the component.`, "The nature of the state is determined during the first render. It's considered controlled if the value is not `undefined`.", "More info: https://fb.me/react-controlled-components"].join("\n"));
      }
    }, [state, name, controlled]);
    const {
      current: defaultValue
    } = React84.useRef(defaultProp);
    React84.useEffect(() => {
      if (!isControlled && JSON.stringify(defaultProp) !== JSON.stringify(defaultValue)) {
        console.error([`MUI: A component is changing the default ${state} state of an uncontrolled ${name} after being initialized. To suppress this warning opt to use a controlled ${name}.`].join("\n"));
      }
    }, [JSON.stringify(defaultProp)]);
  }
  const setValueIfUncontrolled = React84.useCallback((newValue) => {
    if (!isControlled) {
      setValue(newValue);
    }
  }, []);
  return [value, setValueIfUncontrolled];
}

// node_modules/@mui/material/esm/utils/useControlled.js
var useControlled_default = useControlled;

// node_modules/@mui/material/esm/utils/useEventCallback.js
var useEventCallback_default2 = useEventCallback_default;

// node_modules/@mui/material/esm/utils/useForkRef.js
var useForkRef_default = useForkRef;

// node_modules/@mui/material/esm/utils/mergeSlotProps.js
function isEventHandler(key, value) {
  const thirdCharCode = key.charCodeAt(2);
  return key[0] === "o" && key[1] === "n" && thirdCharCode >= 65 && thirdCharCode <= 90 && typeof value === "function";
}
function mergeSlotProps(externalSlotProps, defaultSlotProps) {
  if (!externalSlotProps) {
    return defaultSlotProps;
  }
  function extractHandlers(externalSlotPropsValue, defaultSlotPropsValue) {
    const handlers2 = {};
    Object.keys(defaultSlotPropsValue).forEach((key) => {
      if (isEventHandler(key, defaultSlotPropsValue[key]) && typeof externalSlotPropsValue[key] === "function") {
        handlers2[key] = (...args) => {
          externalSlotPropsValue[key](...args);
          defaultSlotPropsValue[key](...args);
        };
      }
    });
    return handlers2;
  }
  if (typeof externalSlotProps === "function" || typeof defaultSlotProps === "function") {
    return (ownerState) => {
      const defaultSlotPropsValue = typeof defaultSlotProps === "function" ? defaultSlotProps(ownerState) : defaultSlotProps;
      const externalSlotPropsValue = typeof externalSlotProps === "function" ? externalSlotProps({
        ...ownerState,
        ...defaultSlotPropsValue
      }) : externalSlotProps;
      const className2 = clsx_default(ownerState == null ? void 0 : ownerState.className, defaultSlotPropsValue == null ? void 0 : defaultSlotPropsValue.className, externalSlotPropsValue == null ? void 0 : externalSlotPropsValue.className);
      const handlers2 = extractHandlers(externalSlotPropsValue, defaultSlotPropsValue);
      return {
        ...defaultSlotPropsValue,
        ...externalSlotPropsValue,
        ...handlers2,
        ...!!className2 && {
          className: className2
        },
        ...(defaultSlotPropsValue == null ? void 0 : defaultSlotPropsValue.style) && (externalSlotPropsValue == null ? void 0 : externalSlotPropsValue.style) && {
          style: {
            ...defaultSlotPropsValue.style,
            ...externalSlotPropsValue.style
          }
        },
        ...(defaultSlotPropsValue == null ? void 0 : defaultSlotPropsValue.sx) && (externalSlotPropsValue == null ? void 0 : externalSlotPropsValue.sx) && {
          sx: [...Array.isArray(defaultSlotPropsValue.sx) ? defaultSlotPropsValue.sx : [defaultSlotPropsValue.sx], ...Array.isArray(externalSlotPropsValue.sx) ? externalSlotPropsValue.sx : [externalSlotPropsValue.sx]]
        }
      };
    };
  }
  const typedDefaultSlotProps = defaultSlotProps;
  const handlers = extractHandlers(externalSlotProps, typedDefaultSlotProps);
  const className = clsx_default(typedDefaultSlotProps == null ? void 0 : typedDefaultSlotProps.className, externalSlotProps == null ? void 0 : externalSlotProps.className);
  return {
    ...defaultSlotProps,
    ...externalSlotProps,
    ...handlers,
    ...!!className && {
      className
    },
    ...(typedDefaultSlotProps == null ? void 0 : typedDefaultSlotProps.style) && (externalSlotProps == null ? void 0 : externalSlotProps.style) && {
      style: {
        ...typedDefaultSlotProps.style,
        ...externalSlotProps.style
      }
    },
    ...(typedDefaultSlotProps == null ? void 0 : typedDefaultSlotProps.sx) && (externalSlotProps == null ? void 0 : externalSlotProps.sx) && {
      sx: [...Array.isArray(typedDefaultSlotProps.sx) ? typedDefaultSlotProps.sx : [typedDefaultSlotProps.sx], ...Array.isArray(externalSlotProps.sx) ? externalSlotProps.sx : [externalSlotProps.sx]]
    }
  };
}

// node_modules/@mui/x-data-grid/esm/material/icons/createSvgIcon.js
var createSvgIcon2 = createSvgIcon;

// node_modules/@mui/x-data-grid/esm/material/icons/index.js
var import_jsx_runtime58 = __toESM(require_jsx_runtime(), 1);
var GridArrowUpwardIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M4 12l1.41 1.41L11 7.83V20h2V7.83l5.58 5.59L20 12l-8-8-8 8z"
}), "ArrowUpward");
var GridArrowDownwardIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M20 12l-1.41-1.41L13 16.17V4h-2v12.17l-5.58-5.59L4 12l8 8 8-8z"
}), "ArrowDownward");
var GridKeyboardArrowRight = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M8.59 16.59 13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.41z"
}), "KeyboardArrowRight");
var GridExpandMoreIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M16.59 8.59 12 13.17 7.41 8.59 6 10l6 6 6-6z"
}), "ExpandMore");
var GridFilterListIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M10 18h4v-2h-4v2zM3 6v2h18V6H3zm3 7h12v-2H6v2z"
}), "FilterList");
var GridFilterAltIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M4.25 5.61C6.27 8.2 10 13 10 13v6c0 .55.45 1 1 1h2c.55 0 1-.45 1-1v-6s3.72-4.8 5.74-7.39c.51-.66.04-1.61-.79-1.61H5.04c-.83 0-1.3.95-.79 1.61z"
}), "FilterAlt");
var GridSearchIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"
}), "Search");
var GridMenuIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z"
}), "Menu");
var GridCheckCircleIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"
}), "CheckCircle");
var GridUndoIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12.5 8c-2.65 0-5.05.99-6.9 2.6L2 7v9h9l-3.62-3.62c1.39-1.16 3.16-1.88 5.12-1.88 3.54 0 6.55 2.31 7.6 5.5l2.37-.78C21.08 11.03 17.15 8 12.5 8"
}), "Undo");
var GridRedoIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M18.4 10.6C16.55 8.99 14.15 8 11.5 8c-4.65 0-8.58 3.03-9.96 7.22L3.9 16c1.05-3.19 4.05-5.5 7.6-5.5 1.95 0 3.73.72 5.12 1.88L13 16h9V7z"
}), "Redo");
var GridColumnIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M14.67 5v14H9.33V5zm1 14H21V5h-5.33zm-7.34 0V5H3v14z"
}), "ColumnIcon");
var GridSeparatorIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("rect", {
  width: "1",
  height: "24",
  x: "11.5",
  rx: "0.5"
}), "Separator");
var GridViewHeadlineIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M4 15h16v-2H4v2zm0 4h16v-2H4v2zm0-8h16V9H4v2zm0-6v2h16V5H4z"
}), "ViewHeadline");
var GridTableRowsIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M21,8H3V4h18V8z M21,10H3v4h18V10z M21,16H3v4h18V16z"
}), "TableRows");
var GridViewStreamIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M4 18h17v-6H4v6zM4 5v6h17V5H4z"
}), "ViewStream");
var GridTripleDotsVerticalIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 8c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0 6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z"
}), "TripleDotsVertical");
var GridCloseIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
}), "Close");
var GridAddIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"
}), "Add");
var GridRemoveIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M19 13H5v-2h14v2z"
}), "Remove");
var GridLoadIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 4V1L8 5l4 4V6c3.31 0 6 2.69 6 6 0 1.01-.25 1.97-.7 2.8l1.46 1.46C19.54 15.03 20 13.57 20 12c0-4.42-3.58-8-8-8zm0 14c-3.31 0-6-2.69-6-6 0-1.01.25-1.97.7-2.8L5.24 7.74C4.46 8.97 4 10.43 4 12c0 4.42 3.58 8 8 8v3l4-4-4-4v3z"
}), "Load");
var GridDragIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M11 18c0 1.1-.9 2-2 2s-2-.9-2-2 .9-2 2-2 2 .9 2 2zm-2-8c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0-6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm6 4c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0 6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z"
}), "Drag");
var GridCheckIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"
}), "Check");
var GridMoreVertIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 8c1.1 0 2-.9 2-2s-.9-2-2-2-2 .9-2 2 .9 2 2 2zm0 2c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm0 6c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2z"
}), "MoreVert");
var GridVisibilityOffIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 7c2.76 0 5 2.24 5 5 0 .65-.13 1.26-.36 1.83l2.92 2.92c1.51-1.26 2.7-2.89 3.43-4.75-1.73-4.39-6-7.5-11-7.5-1.4 0-2.74.25-3.98.7l2.16 2.16C10.74 7.13 11.35 7 12 7zM2 4.27l2.28 2.28.46.46C3.08 8.3 1.78 10.02 1 12c1.73 4.39 6 7.5 11 7.5 1.55 0 3.03-.3 4.38-.84l.42.42L19.73 22 21 20.73 3.27 3 2 4.27zM7.53 9.8l1.55 1.55c-.05.21-.08.43-.08.65 0 1.66 1.34 3 3 3 .22 0 .44-.03.65-.08l1.55 1.55c-.67.33-1.41.53-2.2.53-2.76 0-5-2.24-5-5 0-.79.2-1.53.53-2.2zm4.31-.78l3.15 3.15.02-.16c0-1.66-1.34-3-3-3l-.17.01z"
}), "VisibilityOff");
var GridViewColumnIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("g", {
  children: (0, import_jsx_runtime58.jsx)("path", {
    d: "M14.67,5v14H9.33V5H14.67z M15.67,19H21V5h-5.33V19z M8.33,19V5H3v14H8.33z"
  })
}), "ViewColumn");
var GridClearIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M12 2C6.47 2 2 6.47 2 12s4.47 10 10 10 10-4.47 10-10S17.53 2 12 2m5 13.59L15.59 17 12 13.41 8.41 17 7 15.59 10.59 12 7 8.41 8.41 7 12 10.59 15.59 7 17 8.41 13.41 12z"
}), "Clear");
var GridDeleteIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zM19 4h-3.5l-1-1h-5l-1 1H5v2h14V4z"
}), "Delete");
var GridDeleteForeverIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M6 19c0 1.1.9 2 2 2h8c1.1 0 2-.9 2-2V7H6v12zm2.46-7.12l1.41-1.41L12 12.59l2.12-2.12 1.41 1.41L13.41 14l2.12 2.12-1.41 1.41L12 15.41l-2.12 2.12-1.41-1.41L10.59 14l-2.13-2.12zM15.5 4l-1-1h-5l-1 1H5v2h14V4z"
}), "Delete");
var GridDownloadIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M5 20h14v-2H5zM19 9h-4V3H9v6H5l7 7z"
}), "Download");
var GridLongTextCellExpandIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M21 11V3h-8l3.29 3.29-10 10L3 13v8h8l-3.29-3.29 10-10z"
}), "OpenInFull");
var GridLongTextCellCollapseIcon = createSvgIcon2((0, import_jsx_runtime58.jsx)("path", {
  d: "M5 15h2V8.41L18.59 20 20 18.59 8.41 7H15V5H5z"
}), "NorthWest");

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/GridColumnMenuContainer.js
var import_prop_types33 = __toESM(require_prop_types(), 1);
var React85 = __toESM(require_react(), 1);
var import_jsx_runtime59 = __toESM(require_jsx_runtime(), 1);
var _excluded33 = ["hideMenu", "colDef", "id", "labelledby", "className", "children", "open"];
var StyledMenuList = styled_default2(NotRendered, {
  slot: "internal"
})(() => ({
  minWidth: 248
}));
function handleMenuScrollCapture(event) {
  if (!event.currentTarget.contains(event.target)) {
    return;
  }
  event.stopPropagation();
}
var GridColumnMenuContainer = forwardRef2(function GridColumnMenuContainer2(props, ref) {
  const {
    hideMenu,
    id,
    labelledby,
    className,
    children: children2,
    open
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded33);
  const rootProps = useGridRootProps();
  const handleListKeyDown = React85.useCallback((event) => {
    if (event.key === "Tab") {
      event.preventDefault();
    }
    if (isHideMenuKey(event.key)) {
      hideMenu(event);
    }
  }, [hideMenu]);
  return (0, import_jsx_runtime59.jsx)(StyledMenuList, _extends({
    as: rootProps.slots.baseMenuList,
    id,
    className: clsx_default(gridClasses.menuList, className),
    "aria-labelledby": labelledby,
    onKeyDown: handleListKeyDown,
    onWheel: handleMenuScrollCapture,
    onTouchMove: handleMenuScrollCapture,
    autoFocus: open
  }, other, {
    ref,
    children: children2
  }));
});
if (true) GridColumnMenuContainer.displayName = "GridColumnMenuContainer";
true ? GridColumnMenuContainer.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types33.default.object.isRequired,
  hideMenu: import_prop_types33.default.func.isRequired,
  id: import_prop_types33.default.string,
  labelledby: import_prop_types33.default.string,
  open: import_prop_types33.default.bool.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/GridColumnMenu.js
var import_prop_types39 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnMenu/useGridColumnMenuSlots.js
var React86 = __toESM(require_react(), 1);
var _excluded34 = ["displayOrder"];
var useGridColumnMenuSlots = (props) => {
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const {
    defaultSlots: defaultSlots4,
    defaultSlotProps,
    slots = {},
    slotProps = {},
    hideMenu,
    colDef,
    addDividers = true
  } = props;
  const processedComponents = React86.useMemo(() => _extends({}, defaultSlots4, slots), [defaultSlots4, slots]);
  const processedSlotProps = React86.useMemo(() => {
    if (!slotProps || Object.keys(slotProps).length === 0) {
      return defaultSlotProps;
    }
    const mergedProps = _extends({}, slotProps);
    Object.entries(defaultSlotProps).forEach(([key, currentSlotProps]) => {
      mergedProps[key] = _extends({}, currentSlotProps, slotProps[key] || {});
    });
    return mergedProps;
  }, [defaultSlotProps, slotProps]);
  return React86.useMemo(() => {
    const sortedKeys = getColumnMenuItemKeys({
      apiRef,
      colDef,
      defaultSlots: defaultSlots4,
      defaultSlotProps,
      slots,
      slotProps
    });
    return sortedKeys.reduce((acc, key, index) => {
      let itemProps = {
        colDef,
        onClick: hideMenu
      };
      const processedComponentProps = processedSlotProps[key];
      if (processedComponentProps) {
        const customProps = _objectWithoutPropertiesLoose(processedComponentProps, _excluded34);
        itemProps = _extends({}, itemProps, customProps);
      }
      return addDividers && index !== sortedKeys.length - 1 ? [...acc, [processedComponents[key], itemProps], [rootProps.slots.baseDivider, {}]] : [...acc, [processedComponents[key], itemProps]];
    }, []);
  }, [addDividers, apiRef, colDef, defaultSlotProps, defaultSlots4, hideMenu, processedComponents, processedSlotProps, slotProps, slots, rootProps.slots.baseDivider]);
};

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuColumnsItem.js
var React89 = __toESM(require_react(), 1);
var import_prop_types36 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuHideItem.js
var React87 = __toESM(require_react(), 1);
var import_prop_types34 = __toESM(require_prop_types(), 1);
var import_jsx_runtime60 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuHideItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
  const columnsWithMenu = visibleColumns.filter((col) => col.disableColumnMenu !== true);
  const disabled = columnsWithMenu.length === 1;
  const toggleColumn = React87.useCallback((event) => {
    if (disabled) {
      return;
    }
    apiRef.current.setColumnVisibility(colDef.field, false);
    onClick(event);
  }, [apiRef, colDef.field, onClick, disabled]);
  if (rootProps.disableColumnSelector) {
    return null;
  }
  if (colDef.hideable === false) {
    return null;
  }
  return (0, import_jsx_runtime60.jsx)(rootProps.slots.baseMenuItem, {
    onClick: toggleColumn,
    disabled,
    iconStart: (0, import_jsx_runtime60.jsx)(rootProps.slots.columnMenuHideIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("columnMenuHideColumn")
  });
}
true ? GridColumnMenuHideItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types34.default.object.isRequired,
  onClick: import_prop_types34.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuManageItem.js
var React88 = __toESM(require_react(), 1);
var import_prop_types35 = __toESM(require_prop_types(), 1);
var import_jsx_runtime61 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuManageItem(props) {
  const {
    onClick
  } = props;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const showColumns = React88.useCallback((event) => {
    onClick(event);
    apiRef.current.showPreferences(GridPreferencePanelsValue.columns);
  }, [apiRef, onClick]);
  if (rootProps.disableColumnSelector) {
    return null;
  }
  return (0, import_jsx_runtime61.jsx)(rootProps.slots.baseMenuItem, {
    onClick: showColumns,
    iconStart: (0, import_jsx_runtime61.jsx)(rootProps.slots.columnMenuManageColumnsIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("columnMenuManageColumns")
  });
}
true ? GridColumnMenuManageItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types35.default.object.isRequired,
  onClick: import_prop_types35.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuColumnsItem.js
var import_jsx_runtime62 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuColumnsItem(props) {
  return (0, import_jsx_runtime62.jsxs)(React89.Fragment, {
    children: [(0, import_jsx_runtime62.jsx)(GridColumnMenuHideItem, _extends({}, props)), (0, import_jsx_runtime62.jsx)(GridColumnMenuManageItem, _extends({}, props))]
  });
}
true ? GridColumnMenuColumnsItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types36.default.object.isRequired,
  onClick: import_prop_types36.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuFilterItem.js
var React90 = __toESM(require_react(), 1);
var import_prop_types37 = __toESM(require_prop_types(), 1);
var import_jsx_runtime63 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuFilterItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const showFilter = React90.useCallback((event) => {
    onClick(event);
    apiRef.current.showFilterPanel(colDef.field);
  }, [apiRef, colDef.field, onClick]);
  if (rootProps.disableColumnFilter || !colDef.filterable) {
    return null;
  }
  return (0, import_jsx_runtime63.jsx)(rootProps.slots.baseMenuItem, {
    onClick: showFilter,
    iconStart: (0, import_jsx_runtime63.jsx)(rootProps.slots.columnMenuFilterIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("columnMenuFilter")
  });
}
true ? GridColumnMenuFilterItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types37.default.object.isRequired,
  onClick: import_prop_types37.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/menuItems/GridColumnMenuSortItem.js
var React91 = __toESM(require_react(), 1);
var import_prop_types38 = __toESM(require_prop_types(), 1);
var import_jsx_runtime64 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuSortItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext();
  const sortModel = useGridSelector(apiRef, gridSortModelSelector);
  const rootProps = useGridRootProps();
  const sortDirection = React91.useMemo(() => {
    if (!colDef) {
      return null;
    }
    const sortItem = sortModel.find((item) => item.field === colDef.field);
    return sortItem == null ? void 0 : sortItem.sort;
  }, [colDef, sortModel]);
  const sortingOrder = colDef.sortingOrder ?? rootProps.sortingOrder;
  const onSortMenuItemClick = React91.useCallback((event) => {
    onClick(event);
    const direction = event.currentTarget.getAttribute("data-value") || null;
    const allowMultipleSorting = rootProps.multipleColumnsSortingMode === "always";
    apiRef.current.sortColumn(colDef.field, direction === sortDirection ? null : direction, allowMultipleSorting);
  }, [apiRef, colDef, onClick, sortDirection, rootProps.multipleColumnsSortingMode]);
  if (rootProps.disableColumnSorting || !colDef || !colDef.sortable || !sortingOrder.some((item) => !!item)) {
    return null;
  }
  const getLabel = (key) => {
    const label = apiRef.current.getLocaleText(key);
    return typeof label === "function" ? label(colDef) : label;
  };
  return (0, import_jsx_runtime64.jsxs)(React91.Fragment, {
    children: [sortingOrder.includes("asc") && sortDirection !== "asc" ? (0, import_jsx_runtime64.jsx)(rootProps.slots.baseMenuItem, {
      onClick: onSortMenuItemClick,
      "data-value": "asc",
      iconStart: (0, import_jsx_runtime64.jsx)(rootProps.slots.columnMenuSortAscendingIcon, {
        fontSize: "small"
      }),
      children: getLabel("columnMenuSortAsc")
    }) : null, sortingOrder.includes("desc") && sortDirection !== "desc" ? (0, import_jsx_runtime64.jsx)(rootProps.slots.baseMenuItem, {
      onClick: onSortMenuItemClick,
      "data-value": "desc",
      iconStart: (0, import_jsx_runtime64.jsx)(rootProps.slots.columnMenuSortDescendingIcon, {
        fontSize: "small"
      }),
      children: getLabel("columnMenuSortDesc")
    }) : null, sortingOrder.includes(null) && sortDirection != null ? (0, import_jsx_runtime64.jsx)(rootProps.slots.baseMenuItem, {
      onClick: onSortMenuItemClick,
      iconStart: rootProps.slots.columnMenuUnsortIcon ? (0, import_jsx_runtime64.jsx)(rootProps.slots.columnMenuUnsortIcon, {
        fontSize: "small"
      }) : (0, import_jsx_runtime64.jsx)("span", {}),
      children: apiRef.current.getLocaleText("columnMenuUnsort")
    }) : null]
  });
}
true ? GridColumnMenuSortItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types38.default.object.isRequired,
  onClick: import_prop_types38.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/menu/columnMenu/GridColumnMenu.js
var import_jsx_runtime65 = __toESM(require_jsx_runtime(), 1);
var _excluded35 = ["defaultSlots", "defaultSlotProps", "slots", "slotProps"];
var GRID_COLUMN_MENU_SLOTS = {
  columnMenuSortItem: GridColumnMenuSortItem,
  columnMenuFilterItem: GridColumnMenuFilterItem,
  columnMenuColumnsItem: GridColumnMenuColumnsItem
};
var GRID_COLUMN_MENU_SLOT_PROPS = {
  columnMenuSortItem: {
    displayOrder: 10
  },
  columnMenuFilterItem: {
    displayOrder: 20
  },
  columnMenuColumnsItem: {
    displayOrder: 30
  }
};
var GridGenericColumnMenu = forwardRef2(function GridGenericColumnMenu2(props, ref) {
  const {
    defaultSlots: defaultSlots4,
    defaultSlotProps,
    slots,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded35);
  const orderedSlots = useGridColumnMenuSlots(_extends({}, other, {
    defaultSlots: defaultSlots4,
    defaultSlotProps,
    slots,
    slotProps
  }));
  return (0, import_jsx_runtime65.jsx)(GridColumnMenuContainer, _extends({}, other, {
    ref,
    children: orderedSlots.map(([Component, otherProps], index) => (0, import_jsx_runtime65.jsx)(Component, _extends({}, otherProps), index))
  }));
});
if (true) GridGenericColumnMenu.displayName = "GridGenericColumnMenu";
true ? GridGenericColumnMenu.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types39.default.object.isRequired,
  /**
   * Initial `slotProps` - it is internal, to be overrriden by Pro or Premium packages
   * @ignore - do not document.
   */
  defaultSlotProps: import_prop_types39.default.object.isRequired,
  /**
   * Initial `slots` - it is internal, to be overrriden by Pro or Premium packages
   * @ignore - do not document.
   */
  defaultSlots: import_prop_types39.default.object.isRequired,
  hideMenu: import_prop_types39.default.func.isRequired,
  id: import_prop_types39.default.string,
  labelledby: import_prop_types39.default.string,
  open: import_prop_types39.default.bool.isRequired,
  /**
   * Could be used to pass new props or override props specific to a column menu component
   * e.g. `displayOrder`
   */
  slotProps: import_prop_types39.default.object,
  /**
   * `slots` could be used to add new and (or) override default column menu items
   * If you register a nee component you must pass it's `displayOrder` in `slotProps`
   * or it will be placed in the end of the list
   */
  slots: import_prop_types39.default.object
} : void 0;
var GridColumnMenu = forwardRef2(function GridColumnMenu2(props, ref) {
  return (0, import_jsx_runtime65.jsx)(GridGenericColumnMenu, _extends({}, props, {
    ref,
    defaultSlots: GRID_COLUMN_MENU_SLOTS,
    defaultSlotProps: GRID_COLUMN_MENU_SLOT_PROPS
  }));
});
if (true) GridColumnMenu.displayName = "GridColumnMenu";
GridColumnMenu.defaultSlots = GRID_COLUMN_MENU_SLOTS;
GridColumnMenu.defaultSlotProps = GRID_COLUMN_MENU_SLOT_PROPS;
true ? GridColumnMenu.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types39.default.object.isRequired,
  hideMenu: import_prop_types39.default.func.isRequired,
  id: import_prop_types39.default.string,
  labelledby: import_prop_types39.default.string,
  open: import_prop_types39.default.bool.isRequired,
  /**
   * Could be used to pass new props or override props specific to a column menu component
   * e.g. `displayOrder`
   */
  slotProps: import_prop_types39.default.object,
  /**
   * `slots` could be used to add new and (or) override default column menu items
   * If you register a nee component you must pass it's `displayOrder` in `slotProps`
   * or it will be placed in the end of the list
   */
  slots: import_prop_types39.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanelWrapper.js
var React92 = __toESM(require_react(), 1);
var import_jsx_runtime66 = __toESM(require_jsx_runtime(), 1);
var _excluded36 = ["className"];
var useUtilityClasses34 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["panelWrapper"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPanelWrapperRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PanelWrapper"
})({
  display: "flex",
  flexDirection: "column",
  flex: 1,
  "&:focus": {
    outline: 0
  }
});
var GridPanelWrapper = forwardRef2(function GridPanelWrapper2(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded36);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses34(rootProps);
  return (0, import_jsx_runtime66.jsx)(GridPanelWrapperRoot, _extends({
    tabIndex: -1,
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    ref
  }));
});
if (true) GridPanelWrapper.displayName = "GridPanelWrapper";

// node_modules/@mui/x-data-grid/esm/components/panel/GridColumnsPanel.js
var import_jsx_runtime67 = __toESM(require_jsx_runtime(), 1);
function GridColumnsPanel(props) {
  var _a;
  const rootProps = useGridRootProps();
  return (0, import_jsx_runtime67.jsx)(GridPanelWrapper, _extends({}, props, {
    children: (0, import_jsx_runtime67.jsx)(rootProps.slots.columnsManagement, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.columnsManagement))
  }));
}

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanel.js
var React93 = __toESM(require_react(), 1);
var import_prop_types40 = __toESM(require_prop_types(), 1);
var import_jsx_runtime68 = __toESM(require_jsx_runtime(), 1);
var _excluded37 = ["children", "className", "classes", "onClose"];
var gridPanelClasses = generateUtilityClasses("MuiDataGrid", ["panel", "paper"]);
var GridPanelRoot = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "panel"
})({
  zIndex: vars.zIndex.panel
});
var GridPanelContent = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "panelContent"
})({
  backgroundColor: vars.colors.background.overlay,
  borderRadius: vars.radius.base,
  boxShadow: vars.shadows.overlay,
  display: "flex",
  maxWidth: `calc(100vw - ${vars.spacing(2)})`,
  overflow: "auto"
});
var GridPanel = forwardRef2((props, ref) => {
  var _a;
  const {
    children: children2,
    className,
    onClose
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded37);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const classes2 = gridPanelClasses;
  const [isPlaced, setIsPlaced] = React93.useState(false);
  const variablesClass = useCSSVariablesClass();
  const onDidShow = useEventCallback_default(() => setIsPlaced(true));
  const onDidHide = useEventCallback_default(() => setIsPlaced(false));
  const handleClickAway = useEventCallback_default(() => {
    onClose == null ? void 0 : onClose();
  });
  const handleKeyDown = useEventCallback_default((event) => {
    if (event.key === "Escape") {
      onClose == null ? void 0 : onClose();
    }
  });
  const [fallbackTarget, setFallbackTarget] = React93.useState(null);
  React93.useEffect(() => {
    var _a2, _b;
    const panelAnchor = (_b = (_a2 = apiRef.current.rootElementRef) == null ? void 0 : _a2.current) == null ? void 0 : _b.querySelector('[data-id="gridPanelAnchor"]');
    if (panelAnchor) {
      setFallbackTarget(panelAnchor);
    }
  }, [apiRef]);
  if (!fallbackTarget) {
    return null;
  }
  return (0, import_jsx_runtime68.jsx)(GridPanelRoot, _extends({
    as: rootProps.slots.basePopper,
    ownerState: rootProps,
    placement: "bottom-end",
    className: clsx_default(classes2.panel, className, variablesClass),
    flip: true,
    onDidShow,
    onDidHide,
    onClickAway: handleClickAway,
    clickAwayMouseEvent: "onPointerUp",
    clickAwayTouchEvent: false,
    focusTrap: true
  }, other, (_a = rootProps.slotProps) == null ? void 0 : _a.basePopper, {
    target: props.target ?? fallbackTarget,
    ref,
    children: (0, import_jsx_runtime68.jsx)(GridPanelContent, {
      className: classes2.paper,
      ownerState: rootProps,
      onKeyDown: handleKeyDown,
      children: isPlaced && children2
    })
  }));
});
if (true) GridPanel.displayName = "GridPanel";
true ? GridPanel.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  children: import_prop_types40.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types40.default.object,
  className: import_prop_types40.default.string,
  flip: import_prop_types40.default.bool,
  id: import_prop_types40.default.string,
  onClose: import_prop_types40.default.func,
  open: import_prop_types40.default.bool.isRequired,
  target: import_prop_types40.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanelContent.js
var React94 = __toESM(require_react(), 1);
var import_prop_types41 = __toESM(require_prop_types(), 1);
var import_jsx_runtime69 = __toESM(require_jsx_runtime(), 1);
var _excluded38 = ["className"];
var useUtilityClasses35 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["panelContent"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPanelContentRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PanelContent"
})({
  display: "flex",
  flexDirection: "column",
  overflow: "auto",
  flex: "1 1",
  maxHeight: 400,
  padding: vars.spacing(2.5, 1.5, 2, 1),
  gap: vars.spacing(2.5)
});
function GridPanelContent2(props) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded38);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses35(rootProps);
  return (0, import_jsx_runtime69.jsx)(GridPanelContentRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other));
}
true ? GridPanelContent2.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types41.default.oneOfType([import_prop_types41.default.arrayOf(import_prop_types41.default.oneOfType([import_prop_types41.default.func, import_prop_types41.default.object, import_prop_types41.default.bool])), import_prop_types41.default.func, import_prop_types41.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanelFooter.js
var React95 = __toESM(require_react(), 1);
var import_prop_types42 = __toESM(require_prop_types(), 1);
var import_jsx_runtime70 = __toESM(require_jsx_runtime(), 1);
var _excluded39 = ["className"];
var useUtilityClasses36 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["panelFooter"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPanelFooterRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PanelFooter"
})({
  padding: vars.spacing(1),
  display: "flex",
  justifyContent: "space-between",
  borderTop: `1px solid ${vars.colors.border.base}`
});
function GridPanelFooter(props) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded39);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses36(rootProps);
  return (0, import_jsx_runtime70.jsx)(GridPanelFooterRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other));
}
true ? GridPanelFooter.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types42.default.oneOfType([import_prop_types42.default.arrayOf(import_prop_types42.default.oneOfType([import_prop_types42.default.func, import_prop_types42.default.object, import_prop_types42.default.bool])), import_prop_types42.default.func, import_prop_types42.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/GridPanelHeader.js
var React96 = __toESM(require_react(), 1);
var import_prop_types43 = __toESM(require_prop_types(), 1);
var import_jsx_runtime71 = __toESM(require_jsx_runtime(), 1);
var _excluded40 = ["className"];
var useUtilityClasses37 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["panelHeader"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPanelHeaderRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PanelHeader"
})({
  padding: vars.spacing(1)
});
function GridPanelHeader(props) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded40);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses37(rootProps);
  return (0, import_jsx_runtime71.jsx)(GridPanelHeaderRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other));
}
true ? GridPanelHeader.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types43.default.oneOfType([import_prop_types43.default.arrayOf(import_prop_types43.default.oneOfType([import_prop_types43.default.func, import_prop_types43.default.object, import_prop_types43.default.bool])), import_prop_types43.default.func, import_prop_types43.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterForm.js
var React97 = __toESM(require_react(), 1);
var import_prop_types44 = __toESM(require_prop_types(), 1);
var import_jsx_runtime72 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);
var _excluded41 = ["item", "hasMultipleFilters", "deleteFilter", "applyFilterChanges", "showMultiFilterOperators", "disableMultiFilterOperator", "applyMultiFilterOperatorChanges", "focusElementRef", "logicOperators", "columnsSort", "filterColumns", "deleteIconProps", "logicOperatorInputProps", "operatorInputProps", "columnInputProps", "valueInputProps", "readOnly", "children"];
var _excluded210 = ["InputComponentProps"];
var useUtilityClasses38 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["filterForm"],
    deleteIcon: ["filterFormDeleteIcon"],
    logicOperatorInput: ["filterFormLogicOperatorInput"],
    columnInput: ["filterFormColumnInput"],
    operatorInput: ["filterFormOperatorInput"],
    valueInput: ["filterFormValueInput"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridFilterFormRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterForm"
})({
  display: "flex",
  gap: vars.spacing(1.5)
});
var FilterFormDeleteIcon = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterFormDeleteIcon"
})({
  flexShrink: 0,
  display: "flex",
  justifyContent: "center",
  alignItems: "center"
});
var FilterFormLogicOperatorInput = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterFormLogicOperatorInput"
})({
  minWidth: 75,
  justifyContent: "end"
});
var FilterFormColumnInput = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterFormColumnInput"
})({
  width: 150
});
var FilterFormOperatorInput = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterFormOperatorInput"
})({
  width: 150
});
var FilterFormValueInput = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "FilterFormValueInput"
})({
  width: 190
});
var getLogicOperatorLocaleKey = (logicOperator) => {
  switch (logicOperator) {
    case GridLogicOperator.And:
      return "filterPanelOperatorAnd";
    case GridLogicOperator.Or:
      return "filterPanelOperatorOr";
    default:
      throw new Error("MUI X: Invalid `logicOperator` property in the `GridFilterPanel`.");
  }
};
var getColumnLabel = (col) => col.headerName || col.field;
var collator2 = new Intl.Collator();
var GridFilterForm = forwardRef2(function GridFilterForm2(props, ref) {
  var _a, _b, _c, _d, _e, _f, _g;
  const {
    item,
    hasMultipleFilters,
    deleteFilter,
    applyFilterChanges,
    showMultiFilterOperators,
    disableMultiFilterOperator,
    applyMultiFilterOperatorChanges,
    focusElementRef,
    logicOperators = [GridLogicOperator.And, GridLogicOperator.Or],
    columnsSort,
    filterColumns,
    deleteIconProps = {},
    logicOperatorInputProps = {},
    operatorInputProps = {},
    columnInputProps = {},
    valueInputProps = {},
    readOnly
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded41);
  const apiRef = useGridApiContext();
  const columnLookup = useGridSelector(apiRef, gridColumnLookupSelector);
  const filterableColumns = useGridSelector(apiRef, gridFilterableColumnDefinitionsSelector);
  const filterModel = useGridSelector(apiRef, gridFilterModelSelector);
  const columnSelectId = useId();
  const columnSelectLabelId = useId();
  const operatorSelectId = useId();
  const operatorSelectLabelId = useId();
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses38(rootProps);
  const valueRef = React97.useRef(null);
  const filterSelectorRef = React97.useRef(null);
  const multiFilterOperator = filterModel.logicOperator ?? GridLogicOperator.And;
  const hasLogicOperatorColumn = hasMultipleFilters && logicOperators.length > 0;
  const baseSelectProps = ((_a = rootProps.slotProps) == null ? void 0 : _a.baseSelect) || {};
  const isBaseSelectNative = baseSelectProps.native ?? false;
  const baseSelectOptionProps = ((_b = rootProps.slotProps) == null ? void 0 : _b.baseSelectOption) || {};
  const {
    InputComponentProps
  } = valueInputProps, valueInputPropsOther = _objectWithoutPropertiesLoose(valueInputProps, _excluded210);
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const initialColumns = useGridSelector(apiRef, gridPivotInitialColumnsSelector);
  const {
    filteredColumns,
    selectedField
  } = React97.useMemo(() => {
    let itemField = item.field;
    const selectedNonFilterableColumn = columnLookup[item.field].filterable === false ? columnLookup[item.field] : null;
    if (selectedNonFilterableColumn) {
      return {
        filteredColumns: [selectedNonFilterableColumn],
        selectedField: itemField
      };
    }
    if (pivotActive) {
      return {
        filteredColumns: filterableColumns.filter((column) => initialColumns.get(column.field) !== void 0),
        selectedField: itemField
      };
    }
    if (filterColumns === void 0 || typeof filterColumns !== "function") {
      return {
        filteredColumns: filterableColumns,
        selectedField: itemField
      };
    }
    const filteredFields = filterColumns({
      field: item.field,
      columns: filterableColumns,
      currentFilters: (filterModel == null ? void 0 : filterModel.items) || []
    });
    return {
      filteredColumns: filterableColumns.filter((column) => {
        const isFieldIncluded = filteredFields.includes(column.field);
        if (column.field === item.field && !isFieldIncluded) {
          itemField = void 0;
        }
        return isFieldIncluded;
      }),
      selectedField: itemField
    };
  }, [item.field, columnLookup, pivotActive, filterColumns, filterableColumns, filterModel == null ? void 0 : filterModel.items, initialColumns]);
  const sortedFilteredColumns = React97.useMemo(() => {
    switch (columnsSort) {
      case "asc":
        return filteredColumns.sort((a, b) => collator2.compare(getColumnLabel(a), getColumnLabel(b)));
      case "desc":
        return filteredColumns.sort((a, b) => -collator2.compare(getColumnLabel(a), getColumnLabel(b)));
      default:
        return filteredColumns;
    }
  }, [filteredColumns, columnsSort]);
  const currentColumn = item.field ? apiRef.current.getColumn(item.field) : null;
  const currentOperator = React97.useMemo(() => {
    var _a2;
    if (!item.operator || !currentColumn) {
      return null;
    }
    return (_a2 = currentColumn.filterOperators) == null ? void 0 : _a2.find((operator) => operator.value === item.operator);
  }, [item, currentColumn]);
  const changeColumn = React97.useCallback((event) => {
    const field = event.target.value;
    const column = apiRef.current.getColumn(field);
    if (column.field === currentColumn.field) {
      return;
    }
    const newOperator = column.filterOperators.find((operator) => operator.value === item.operator) || column.filterOperators[0];
    const eraseFilterValue = !newOperator.InputComponent || newOperator.InputComponent !== (currentOperator == null ? void 0 : currentOperator.InputComponent) || column.type !== currentColumn.type;
    let filterValue = eraseFilterValue ? void 0 : item.value;
    if (column.type === "singleSelect" && filterValue !== void 0) {
      const colDef = column;
      const valueOptions = getValueOptions(colDef);
      if (Array.isArray(filterValue)) {
        filterValue = filterValue.filter((val) => {
          return (
            // Only keep values that are in the new value options
            getValueFromValueOptions(val, valueOptions, colDef == null ? void 0 : colDef.getOptionValue) !== void 0
          );
        });
      } else if (getValueFromValueOptions(item.value, valueOptions, colDef == null ? void 0 : colDef.getOptionValue) === void 0) {
        filterValue = void 0;
      }
    }
    applyFilterChanges(_extends({}, item, {
      field,
      operator: newOperator.value,
      value: filterValue
    }));
  }, [apiRef, applyFilterChanges, item, currentColumn, currentOperator]);
  const changeOperator = React97.useCallback((event) => {
    const operator = event.target.value;
    const newOperator = currentColumn == null ? void 0 : currentColumn.filterOperators.find((op) => op.value === operator);
    const eraseItemValue = !(newOperator == null ? void 0 : newOperator.InputComponent) || (newOperator == null ? void 0 : newOperator.InputComponent) !== (currentOperator == null ? void 0 : currentOperator.InputComponent);
    applyFilterChanges(_extends({}, item, {
      operator,
      value: eraseItemValue ? void 0 : item.value
    }));
  }, [applyFilterChanges, item, currentColumn, currentOperator]);
  const changeLogicOperator = React97.useCallback((event) => {
    const logicOperator = event.target.value === GridLogicOperator.And.toString() ? GridLogicOperator.And : GridLogicOperator.Or;
    applyMultiFilterOperatorChanges(logicOperator);
  }, [applyMultiFilterOperatorChanges]);
  const handleDeleteFilter = () => {
    deleteFilter(item);
  };
  React97.useImperativeHandle(focusElementRef, () => ({
    focus: () => {
      var _a2;
      if (currentOperator == null ? void 0 : currentOperator.InputComponent) {
        (_a2 = valueRef == null ? void 0 : valueRef.current) == null ? void 0 : _a2.focus();
      } else {
        filterSelectorRef.current.focus();
      }
    }
  }), [currentOperator]);
  return (0, import_jsx_runtime72.jsxs)(GridFilterFormRoot, _extends({
    className: classes2.root,
    "data-id": item.id,
    ownerState: rootProps
  }, other, {
    ref,
    children: [(0, import_jsx_runtime72.jsx)(FilterFormDeleteIcon, _extends({}, deleteIconProps, {
      className: clsx_default(classes2.deleteIcon, deleteIconProps.className),
      ownerState: rootProps,
      children: (0, import_jsx_runtime72.jsx)(rootProps.slots.baseIconButton, _extends({
        "aria-label": apiRef.current.getLocaleText("filterPanelDeleteIconLabel"),
        title: apiRef.current.getLocaleText("filterPanelDeleteIconLabel"),
        onClick: handleDeleteFilter,
        size: "small",
        disabled: readOnly
      }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseIconButton, {
        children: (0, import_jsx_runtime72.jsx)(rootProps.slots.filterPanelDeleteIcon, {
          fontSize: "small"
        })
      }))
    })), (0, import_jsx_runtime72.jsx)(FilterFormLogicOperatorInput, _extends({
      as: rootProps.slots.baseSelect,
      sx: [hasLogicOperatorColumn ? {
        display: "flex"
      } : {
        display: "none"
      }, showMultiFilterOperators ? {
        visibility: "visible"
      } : {
        visibility: "hidden"
      }, logicOperatorInputProps.sx],
      className: clsx_default(classes2.logicOperatorInput, logicOperatorInputProps.className),
      ownerState: rootProps
    }, logicOperatorInputProps, {
      size: "small",
      slotProps: {
        htmlInput: {
          "aria-label": apiRef.current.getLocaleText("filterPanelLogicOperator")
        }
      },
      value: multiFilterOperator ?? "",
      onChange: changeLogicOperator,
      disabled: !!disableMultiFilterOperator || logicOperators.length === 1,
      native: isBaseSelectNative
    }, (_d = rootProps.slotProps) == null ? void 0 : _d.baseSelect, {
      children: logicOperators.map((logicOperator) => (0, import_react3.createElement)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isBaseSelectNative,
        key: logicOperator.toString(),
        value: logicOperator.toString()
      }), apiRef.current.getLocaleText(getLogicOperatorLocaleKey(logicOperator))))
    })), (0, import_jsx_runtime72.jsx)(FilterFormColumnInput, _extends({
      as: rootProps.slots.baseSelect
    }, columnInputProps, {
      className: clsx_default(classes2.columnInput, columnInputProps.className),
      ownerState: rootProps,
      size: "small",
      labelId: columnSelectLabelId,
      id: columnSelectId,
      label: apiRef.current.getLocaleText("filterPanelColumns"),
      value: selectedField ?? "",
      onChange: changeColumn,
      native: isBaseSelectNative,
      disabled: readOnly
    }, (_e = rootProps.slotProps) == null ? void 0 : _e.baseSelect, {
      children: sortedFilteredColumns.map((col) => (0, import_react3.createElement)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isBaseSelectNative,
        key: col.field,
        value: col.field
      }), getColumnLabel(col)))
    })), (0, import_jsx_runtime72.jsx)(FilterFormOperatorInput, _extends({
      as: rootProps.slots.baseSelect,
      size: "small"
    }, operatorInputProps, {
      className: clsx_default(classes2.operatorInput, operatorInputProps.className),
      ownerState: rootProps,
      labelId: operatorSelectLabelId,
      label: apiRef.current.getLocaleText("filterPanelOperator"),
      id: operatorSelectId,
      value: item.operator,
      onChange: changeOperator,
      native: isBaseSelectNative,
      inputRef: filterSelectorRef,
      disabled: readOnly
    }, (_f = rootProps.slotProps) == null ? void 0 : _f.baseSelect, {
      children: (_g = currentColumn == null ? void 0 : currentColumn.filterOperators) == null ? void 0 : _g.map((operator) => (0, import_react3.createElement)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isBaseSelectNative,
        key: operator.value,
        value: operator.value
      }), operator.label || apiRef.current.getLocaleText(`filterOperator${capitalize(operator.value)}`)))
    })), (0, import_jsx_runtime72.jsx)(FilterFormValueInput, _extends({}, valueInputPropsOther, {
      className: clsx_default(classes2.valueInput, valueInputPropsOther.className),
      ownerState: rootProps,
      children: (currentOperator == null ? void 0 : currentOperator.InputComponent) ? (0, import_jsx_runtime72.jsx)(currentOperator.InputComponent, _extends({
        apiRef,
        item,
        applyValue: applyFilterChanges,
        focusElementRef: valueRef,
        disabled: readOnly,
        slotProps: {
          root: {
            size: "small"
          }
        }
      }, currentOperator.InputComponentProps, InputComponentProps), item.field) : null
    }))]
  }));
});
if (true) GridFilterForm.displayName = "GridFilterForm";
true ? GridFilterForm.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Callback called when the operator, column field or value is changed.
   * @param {GridFilterItem} item The updated [[GridFilterItem]].
   */
  applyFilterChanges: import_prop_types44.default.func.isRequired,
  /**
   * Callback called when the logic operator is changed.
   * @param {GridLogicOperator} operator The new logic operator.
   */
  applyMultiFilterOperatorChanges: import_prop_types44.default.func.isRequired,
  /**
   * @ignore - do not document.
   */
  children: import_prop_types44.default.node,
  /**
   * Props passed to the column input component.
   * @default {}
   */
  columnInputProps: import_prop_types44.default.any,
  /**
   * Changes how the options in the columns selector should be ordered.
   * If not specified, the order is derived from the `columns` prop.
   */
  columnsSort: import_prop_types44.default.oneOf(["asc", "desc"]),
  /**
   * Callback called when the delete button is clicked.
   * @param {GridFilterItem} item The deleted [[GridFilterItem]].
   */
  deleteFilter: import_prop_types44.default.func.isRequired,
  /**
   * Props passed to the delete icon.
   * @default {}
   */
  deleteIconProps: import_prop_types44.default.any,
  /**
   * If `true`, disables the logic operator field but still renders it.
   */
  disableMultiFilterOperator: import_prop_types44.default.bool,
  /**
   * Allows to filter the columns displayed in the filter form.
   * @param {FilterColumnsArgs} args The columns of the grid and name of field.
   * @returns {GridColDef['field'][]} The filtered fields array.
   */
  filterColumns: import_prop_types44.default.func,
  /**
   * A ref allowing to set imperative focus.
   * It can be passed to the el
   */
  focusElementRef: import_prop_types44.default.oneOfType([import_prop_types44.default.func, import_prop_types44.default.object]),
  /**
   * If `true`, the logic operator field is rendered.
   * The field will be invisible if `showMultiFilterOperators` is also `true`.
   */
  hasMultipleFilters: import_prop_types44.default.bool.isRequired,
  /**
   * The [[GridFilterItem]] representing this form.
   */
  item: import_prop_types44.default.shape({
    field: import_prop_types44.default.string.isRequired,
    id: import_prop_types44.default.oneOfType([import_prop_types44.default.number, import_prop_types44.default.string]),
    operator: import_prop_types44.default.string.isRequired,
    value: import_prop_types44.default.any
  }).isRequired,
  /**
   * Props passed to the logic operator input component.
   * @default {}
   */
  logicOperatorInputProps: import_prop_types44.default.any,
  /**
   * Sets the available logic operators.
   * @default [GridLogicOperator.And, GridLogicOperator.Or]
   */
  logicOperators: import_prop_types44.default.arrayOf(import_prop_types44.default.oneOf(["and", "or"]).isRequired),
  /**
   * Props passed to the operator input component.
   * @default {}
   */
  operatorInputProps: import_prop_types44.default.any,
  /**
   * `true` if the filter is disabled/read only.
   * i.e. `colDef.fiterable = false` but passed in `filterModel`
   * @default false
   */
  readOnly: import_prop_types44.default.bool,
  /**
   * If `true`, the logic operator field is visible.
   */
  showMultiFilterOperators: import_prop_types44.default.bool,
  /**
   * Props passed to the value input component.
   * @default {}
   */
  valueInputProps: import_prop_types44.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/panel/filterPanel/GridFilterPanel.js
var React98 = __toESM(require_react(), 1);
var import_prop_types45 = __toESM(require_prop_types(), 1);
var import_jsx_runtime73 = __toESM(require_jsx_runtime(), 1);
var _excluded42 = ["logicOperators", "columnsSort", "filterFormProps", "getColumnForNewFilter", "children", "disableAddFilterButton", "disableRemoveAllButton"];
var getGridFilter = (col) => ({
  field: col.field,
  operator: col.filterOperators[0].value,
  id: Math.round(Math.random() * 1e5)
});
var GridFilterPanel = forwardRef2(function GridFilterPanel2(props, ref) {
  var _a, _b;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const filterModel = useGridSelector(apiRef, gridFilterModelSelector);
  const filterableColumns = useGridSelector(apiRef, gridFilterableColumnDefinitionsSelector);
  const filterableColumnsLookup = useGridSelector(apiRef, gridFilterableColumnLookupSelector);
  const lastFilterRef = React98.useRef(null);
  const placeholderFilter = React98.useRef(null);
  const {
    logicOperators = [GridLogicOperator.And, GridLogicOperator.Or],
    columnsSort,
    filterFormProps,
    getColumnForNewFilter,
    disableAddFilterButton = false,
    disableRemoveAllButton = false
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded42);
  const applyFilter = apiRef.current.upsertFilterItem;
  const applyFilterLogicOperator = React98.useCallback((operator) => {
    apiRef.current.setFilterLogicOperator(operator);
  }, [apiRef]);
  const getDefaultFilter = React98.useCallback(() => {
    let nextColumnWithOperator;
    if (getColumnForNewFilter && typeof getColumnForNewFilter === "function") {
      const nextFieldName = getColumnForNewFilter({
        currentFilters: (filterModel == null ? void 0 : filterModel.items) || [],
        columns: filterableColumns
      });
      if (nextFieldName === null) {
        return null;
      }
      nextColumnWithOperator = filterableColumns.find(({
        field
      }) => field === nextFieldName);
    } else {
      nextColumnWithOperator = filterableColumns.find((colDef) => {
        var _a2;
        return (_a2 = colDef.filterOperators) == null ? void 0 : _a2.length;
      });
    }
    if (!nextColumnWithOperator) {
      return null;
    }
    return getGridFilter(nextColumnWithOperator);
  }, [filterModel == null ? void 0 : filterModel.items, filterableColumns, getColumnForNewFilter]);
  const getNewFilter = React98.useCallback(() => {
    if (getColumnForNewFilter === void 0 || typeof getColumnForNewFilter !== "function") {
      return getDefaultFilter();
    }
    const currentFilters = filterModel.items.length ? filterModel.items : [getDefaultFilter()].filter(Boolean);
    const nextColumnFieldName = getColumnForNewFilter({
      currentFilters,
      columns: filterableColumns
    });
    if (nextColumnFieldName === null) {
      return null;
    }
    const nextColumnWithOperator = filterableColumns.find(({
      field
    }) => field === nextColumnFieldName);
    if (!nextColumnWithOperator) {
      return null;
    }
    return getGridFilter(nextColumnWithOperator);
  }, [filterModel.items, filterableColumns, getColumnForNewFilter, getDefaultFilter]);
  const items = React98.useMemo(() => {
    if (filterModel.items.length) {
      return filterModel.items;
    }
    if (!placeholderFilter.current) {
      placeholderFilter.current = getDefaultFilter();
    }
    return placeholderFilter.current ? [placeholderFilter.current] : [];
  }, [filterModel.items, getDefaultFilter]);
  const hasMultipleFilters = items.length > 1;
  const {
    readOnlyFilters,
    validFilters
  } = React98.useMemo(() => items.reduce((acc, item) => {
    if (filterableColumnsLookup[item.field]) {
      acc.validFilters.push(item);
    } else {
      acc.readOnlyFilters.push(item);
    }
    return acc;
  }, {
    readOnlyFilters: [],
    validFilters: []
  }), [items, filterableColumnsLookup]);
  const addNewFilter = React98.useCallback(() => {
    const newFilter = getNewFilter();
    if (!newFilter) {
      return;
    }
    apiRef.current.upsertFilterItems([...items, newFilter]);
  }, [apiRef, getNewFilter, items]);
  const deleteFilter = React98.useCallback((item) => {
    const shouldCloseFilterPanel = validFilters.length === 1;
    apiRef.current.deleteFilterItem(item);
    if (shouldCloseFilterPanel) {
      apiRef.current.hideFilterPanel();
    }
  }, [apiRef, validFilters.length]);
  const handleRemoveAll = React98.useCallback(() => {
    if (validFilters.length === 1 && validFilters[0].value === void 0) {
      apiRef.current.deleteFilterItem(validFilters[0]);
      return apiRef.current.hideFilterPanel();
    }
    return apiRef.current.setFilterModel(_extends({}, filterModel, {
      items: readOnlyFilters
    }), "removeAllFilterItems");
  }, [apiRef, readOnlyFilters, filterModel, validFilters]);
  React98.useEffect(() => {
    if (logicOperators.length > 0 && filterModel.logicOperator && !logicOperators.includes(filterModel.logicOperator)) {
      applyFilterLogicOperator(logicOperators[0]);
    }
  }, [logicOperators, applyFilterLogicOperator, filterModel.logicOperator]);
  React98.useEffect(() => {
    if (validFilters.length > 0) {
      lastFilterRef.current.focus();
    }
  }, [validFilters.length]);
  return (0, import_jsx_runtime73.jsxs)(GridPanelWrapper, _extends({}, other, {
    ref,
    children: [(0, import_jsx_runtime73.jsxs)(GridPanelContent2, {
      children: [readOnlyFilters.map((item, index) => (0, import_jsx_runtime73.jsx)(GridFilterForm, _extends({
        item,
        applyFilterChanges: applyFilter,
        deleteFilter,
        hasMultipleFilters,
        showMultiFilterOperators: index > 0,
        disableMultiFilterOperator: index !== 1,
        applyMultiFilterOperatorChanges: applyFilterLogicOperator,
        focusElementRef: null,
        readOnly: true,
        logicOperators,
        columnsSort
      }, filterFormProps), item.id == null ? index : item.id)), validFilters.map((item, index) => (0, import_jsx_runtime73.jsx)(GridFilterForm, _extends({
        item,
        applyFilterChanges: applyFilter,
        deleteFilter,
        hasMultipleFilters,
        showMultiFilterOperators: readOnlyFilters.length + index > 0,
        disableMultiFilterOperator: readOnlyFilters.length + index !== 1,
        applyMultiFilterOperatorChanges: applyFilterLogicOperator,
        focusElementRef: index === validFilters.length - 1 ? lastFilterRef : null,
        logicOperators,
        columnsSort
      }, filterFormProps), item.id == null ? index + readOnlyFilters.length : item.id))]
    }), !rootProps.disableMultipleColumnsFiltering && !(disableAddFilterButton && disableRemoveAllButton) ? (0, import_jsx_runtime73.jsxs)(GridPanelFooter, {
      children: [!disableAddFilterButton ? (0, import_jsx_runtime73.jsx)(rootProps.slots.baseButton, _extends({
        onClick: addNewFilter,
        startIcon: (0, import_jsx_runtime73.jsx)(rootProps.slots.filterPanelAddIcon, {})
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
        children: apiRef.current.getLocaleText("filterPanelAddFilter")
      })) : (0, import_jsx_runtime73.jsx)("span", {}), !disableRemoveAllButton && validFilters.length > 0 ? (0, import_jsx_runtime73.jsx)(rootProps.slots.baseButton, _extends({
        onClick: handleRemoveAll,
        startIcon: (0, import_jsx_runtime73.jsx)(rootProps.slots.filterPanelRemoveAllIcon, {})
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseButton, {
        children: apiRef.current.getLocaleText("filterPanelRemoveAll")
      })) : null]
    }) : null]
  }));
});
if (true) GridFilterPanel.displayName = "GridFilterPanel";
true ? GridFilterPanel.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * @ignore - do not document.
   */
  children: import_prop_types45.default.node,
  /**
   * Changes how the options in the columns selector should be ordered.
   * If not specified, the order is derived from the `columns` prop.
   */
  columnsSort: import_prop_types45.default.oneOf(["asc", "desc"]),
  /**
   * If `true`, the `Add filter` button will not be displayed.
   * @default false
   */
  disableAddFilterButton: import_prop_types45.default.bool,
  /**
   * If `true`, the `Remove all` button will be disabled
   * @default false
   */
  disableRemoveAllButton: import_prop_types45.default.bool,
  /**
   * Props passed to each filter form.
   */
  filterFormProps: import_prop_types45.default.shape({
    columnInputProps: import_prop_types45.default.any,
    columnsSort: import_prop_types45.default.oneOf(["asc", "desc"]),
    deleteIconProps: import_prop_types45.default.any,
    filterColumns: import_prop_types45.default.func,
    logicOperatorInputProps: import_prop_types45.default.any,
    operatorInputProps: import_prop_types45.default.any,
    valueInputProps: import_prop_types45.default.any
  }),
  /**
   * Function that returns the next filter item to be picked as default filter.
   * @param {GetColumnForNewFilterArgs} args Currently configured filters and columns.
   * @returns {GridColDef['field']} The field to be used for the next filter or `null` to prevent adding a filter.
   */
  getColumnForNewFilter: import_prop_types45.default.func,
  /**
   * Sets the available logic operators.
   * @default [GridLogicOperator.And, GridLogicOperator.Or]
   */
  logicOperators: import_prop_types45.default.arrayOf(import_prop_types45.default.oneOf(["and", "or"]).isRequired),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types45.default.oneOfType([import_prop_types45.default.arrayOf(import_prop_types45.default.oneOfType([import_prop_types45.default.func, import_prop_types45.default.object, import_prop_types45.default.bool])), import_prop_types45.default.func, import_prop_types45.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnsManagement/GridColumnsManagement.js
var React105 = __toESM(require_react(), 1);
var import_prop_types50 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/InputBase/InputBase.js
var React103 = __toESM(require_react(), 1);
var import_prop_types48 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/elementTypeAcceptingRef/elementTypeAcceptingRef.js
var import_prop_types46 = __toESM(require_prop_types(), 1);
var React99 = __toESM(require_react(), 1);

// node_modules/@mui/utils/esm/chainPropTypes/chainPropTypes.js
function chainPropTypes(propType1, propType2) {
  if (false) {
    return () => null;
  }
  return function validate(...args) {
    return propType1(...args) || propType2(...args);
  };
}

// node_modules/@mui/utils/esm/elementTypeAcceptingRef/elementTypeAcceptingRef.js
function isClassComponent(elementType) {
  const {
    prototype = {}
  } = elementType;
  return Boolean(prototype.isReactComponent);
}
function elementTypeAcceptingRef(props, propName, componentName, location, propFullName) {
  const propValue = props[propName];
  const safePropName = propFullName || propName;
  if (propValue == null || // When server-side rendering React doesn't warn either.
  // This is not an accurate check for SSR.
  // This is only in place for emotion compat.
  // TODO: Revisit once https://github.com/facebook/react/issues/20047 is resolved.
  typeof window === "undefined") {
    return null;
  }
  let warningHint;
  if (typeof propValue === "function" && !isClassComponent(propValue)) {
    warningHint = "Did you accidentally provide a plain function component instead?";
  }
  if (propValue === React99.Fragment) {
    warningHint = "Did you accidentally provide a React.Fragment instead?";
  }
  if (warningHint !== void 0) {
    return new Error(`Invalid ${location} \`${safePropName}\` supplied to \`${componentName}\`. Expected an element type that can hold a ref. ${warningHint} For more information see https://mui.com/r/caveat-with-refs-guide`);
  }
  return null;
}
var elementTypeAcceptingRef_default = chainPropTypes(import_prop_types46.default.elementType, elementTypeAcceptingRef);

// node_modules/@mui/utils/esm/isHostComponent/isHostComponent.js
function isHostComponent(element) {
  return typeof element === "string";
}
var isHostComponent_default = isHostComponent;

// node_modules/@mui/material/esm/TextareaAutosize/TextareaAutosize.js
var React100 = __toESM(require_react(), 1);
var import_prop_types47 = __toESM(require_prop_types(), 1);
var import_jsx_runtime74 = __toESM(require_jsx_runtime(), 1);
function getStyleValue(value) {
  return parseInt(value, 10) || 0;
}
var styles = {
  shadow: {
    // Visibility needed to hide the extra text area on iPads
    visibility: "hidden",
    // Remove from the content flow
    position: "absolute",
    // Ignore the scrollbar width
    overflow: "hidden",
    height: 0,
    top: 0,
    left: 0,
    // Create a new layer, increase the isolation of the computed values
    transform: "translateZ(0)"
  }
};
function isObjectEmpty2(object) {
  for (const _ in object) {
    return false;
  }
  return true;
}
function isEmpty(obj) {
  return isObjectEmpty2(obj) || obj.outerHeightStyle === 0 && !obj.overflowing;
}
var TextareaAutosize = React100.forwardRef(function TextareaAutosize2(props, forwardedRef) {
  const {
    onChange,
    maxRows,
    minRows = 1,
    style,
    value,
    ...other
  } = props;
  const {
    current: isControlled
  } = React100.useRef(value != null);
  const textareaRef = React100.useRef(null);
  const handleRef = useForkRef(forwardedRef, textareaRef);
  const heightRef = React100.useRef(null);
  const hiddenTextareaRef = React100.useRef(null);
  const calculateTextareaStyles = React100.useCallback(() => {
    const textarea = textareaRef.current;
    const hiddenTextarea = hiddenTextareaRef.current;
    if (!textarea || !hiddenTextarea) {
      return void 0;
    }
    const containerWindow = ownerWindow(textarea);
    const computedStyle = containerWindow.getComputedStyle(textarea);
    if (computedStyle.width === "0px") {
      return {
        outerHeightStyle: 0,
        overflowing: false
      };
    }
    hiddenTextarea.style.width = computedStyle.width;
    hiddenTextarea.value = textarea.value || props.placeholder || "x";
    if (hiddenTextarea.value.slice(-1) === "\n") {
      hiddenTextarea.value += " ";
    }
    const boxSizing = computedStyle.boxSizing;
    const padding = getStyleValue(computedStyle.paddingBottom) + getStyleValue(computedStyle.paddingTop);
    const border = getStyleValue(computedStyle.borderBottomWidth) + getStyleValue(computedStyle.borderTopWidth);
    const innerHeight = hiddenTextarea.scrollHeight;
    hiddenTextarea.value = "x";
    const singleRowHeight = hiddenTextarea.scrollHeight;
    let outerHeight = innerHeight;
    if (minRows) {
      outerHeight = Math.max(Number(minRows) * singleRowHeight, outerHeight);
    }
    if (maxRows) {
      outerHeight = Math.min(Number(maxRows) * singleRowHeight, outerHeight);
    }
    outerHeight = Math.max(outerHeight, singleRowHeight);
    const outerHeightStyle = outerHeight + (boxSizing === "border-box" ? padding + border : 0);
    const overflowing = Math.abs(outerHeight - innerHeight) <= 1;
    return {
      outerHeightStyle,
      overflowing
    };
  }, [maxRows, minRows, props.placeholder]);
  const didHeightChange = useEventCallback_default(() => {
    const textarea = textareaRef.current;
    const textareaStyles = calculateTextareaStyles();
    if (!textarea || !textareaStyles || isEmpty(textareaStyles)) {
      return false;
    }
    const outerHeightStyle = textareaStyles.outerHeightStyle;
    return heightRef.current != null && heightRef.current !== outerHeightStyle;
  });
  const syncHeight = React100.useCallback(() => {
    const textarea = textareaRef.current;
    const textareaStyles = calculateTextareaStyles();
    if (!textarea || !textareaStyles || isEmpty(textareaStyles)) {
      return;
    }
    const outerHeightStyle = textareaStyles.outerHeightStyle;
    if (heightRef.current !== outerHeightStyle) {
      heightRef.current = outerHeightStyle;
      textarea.style.height = `${outerHeightStyle}px`;
    }
    textarea.style.overflow = textareaStyles.overflowing ? "hidden" : "";
  }, [calculateTextareaStyles]);
  const frameRef = React100.useRef(-1);
  useEnhancedEffect_default(() => {
    const debouncedHandleResize = debounce(syncHeight);
    const textarea = textareaRef == null ? void 0 : textareaRef.current;
    if (!textarea) {
      return void 0;
    }
    const containerWindow = ownerWindow(textarea);
    containerWindow.addEventListener("resize", debouncedHandleResize);
    let resizeObserver;
    if (typeof ResizeObserver !== "undefined") {
      resizeObserver = new ResizeObserver(() => {
        if (didHeightChange()) {
          resizeObserver.unobserve(textarea);
          cancelAnimationFrame(frameRef.current);
          syncHeight();
          frameRef.current = requestAnimationFrame(() => {
            resizeObserver.observe(textarea);
          });
        }
      });
      resizeObserver.observe(textarea);
    }
    return () => {
      debouncedHandleResize.clear();
      cancelAnimationFrame(frameRef.current);
      containerWindow.removeEventListener("resize", debouncedHandleResize);
      if (resizeObserver) {
        resizeObserver.disconnect();
      }
    };
  }, [calculateTextareaStyles, syncHeight, didHeightChange]);
  useEnhancedEffect_default(() => {
    syncHeight();
  });
  const handleChange = (event) => {
    if (!isControlled) {
      syncHeight();
    }
    const textarea = event.target;
    const countOfCharacters = textarea.value.length;
    const isLastCharacterNewLine = textarea.value.endsWith("\n");
    const isEndOfTheLine = textarea.selectionStart === countOfCharacters;
    if (isLastCharacterNewLine && isEndOfTheLine) {
      textarea.setSelectionRange(countOfCharacters, countOfCharacters);
    }
    if (onChange) {
      onChange(event);
    }
  };
  return (0, import_jsx_runtime74.jsxs)(React100.Fragment, {
    children: [(0, import_jsx_runtime74.jsx)("textarea", {
      value,
      onChange: handleChange,
      ref: handleRef,
      rows: minRows,
      style,
      ...other
    }), (0, import_jsx_runtime74.jsx)("textarea", {
      "aria-hidden": true,
      className: props.className,
      readOnly: true,
      ref: hiddenTextareaRef,
      tabIndex: -1,
      style: {
        ...styles.shadow,
        ...style,
        paddingTop: 0,
        paddingBottom: 0
      }
    })]
  });
});
true ? TextareaAutosize.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * @ignore
   */
  className: import_prop_types47.default.string,
  /**
   * Maximum number of rows to display.
   */
  maxRows: import_prop_types47.default.oneOfType([import_prop_types47.default.number, import_prop_types47.default.string]),
  /**
   * Minimum number of rows to display.
   * @default 1
   */
  minRows: import_prop_types47.default.oneOfType([import_prop_types47.default.number, import_prop_types47.default.string]),
  /**
   * @ignore
   */
  onChange: import_prop_types47.default.func,
  /**
   * @ignore
   */
  placeholder: import_prop_types47.default.string,
  /**
   * @ignore
   */
  style: import_prop_types47.default.object,
  /**
   * @ignore
   */
  value: import_prop_types47.default.oneOfType([import_prop_types47.default.arrayOf(import_prop_types47.default.string), import_prop_types47.default.number, import_prop_types47.default.string])
} : void 0;
var TextareaAutosize_default = TextareaAutosize;

// node_modules/@mui/material/esm/FormControl/formControlState.js
function formControlState({
  props,
  states,
  muiFormControl
}) {
  return states.reduce((acc, state) => {
    acc[state] = props[state];
    if (muiFormControl) {
      if (typeof props[state] === "undefined") {
        acc[state] = muiFormControl[state];
      }
    }
    return acc;
  }, {});
}

// node_modules/@mui/material/esm/FormControl/FormControlContext.js
var React101 = __toESM(require_react(), 1);
var FormControlContext = React101.createContext(void 0);
if (true) {
  FormControlContext.displayName = "FormControlContext";
}
var FormControlContext_default = FormControlContext;

// node_modules/@mui/material/esm/FormControl/useFormControl.js
var React102 = __toESM(require_react(), 1);
function useFormControl() {
  return React102.useContext(FormControlContext_default);
}

// node_modules/@mui/material/esm/InputBase/utils.js
function hasValue(value) {
  return value != null && !(Array.isArray(value) && value.length === 0);
}
function isFilled(obj, SSR = false) {
  return obj && (hasValue(obj.value) && obj.value !== "" || SSR && hasValue(obj.defaultValue) && obj.defaultValue !== "");
}
function isAdornedStart(obj) {
  return obj.startAdornment;
}

// node_modules/@mui/material/esm/InputBase/inputBaseClasses.js
function getInputBaseUtilityClass(slot) {
  return generateUtilityClass("MuiInputBase", slot);
}
var inputBaseClasses = generateUtilityClasses("MuiInputBase", ["root", "formControl", "focused", "disabled", "adornedStart", "adornedEnd", "error", "sizeSmall", "multiline", "colorSecondary", "fullWidth", "hiddenLabel", "readOnly", "input", "inputSizeSmall", "inputMultiline", "inputTypeSearch", "inputAdornedStart", "inputAdornedEnd", "inputHiddenLabel"]);
var inputBaseClasses_default = inputBaseClasses;

// node_modules/@mui/material/esm/InputBase/InputBase.js
var import_jsx_runtime75 = __toESM(require_jsx_runtime(), 1);
var _InputGlobalStyles;
var rootOverridesResolver = (props, styles5) => {
  const {
    ownerState
  } = props;
  return [styles5.root, ownerState.formControl && styles5.formControl, ownerState.startAdornment && styles5.adornedStart, ownerState.endAdornment && styles5.adornedEnd, ownerState.error && styles5.error, ownerState.size === "small" && styles5.sizeSmall, ownerState.multiline && styles5.multiline, ownerState.color && styles5[`color${capitalize_default(ownerState.color)}`], ownerState.fullWidth && styles5.fullWidth, ownerState.hiddenLabel && styles5.hiddenLabel];
};
var inputOverridesResolver = (props, styles5) => {
  const {
    ownerState
  } = props;
  return [styles5.input, ownerState.size === "small" && styles5.inputSizeSmall, ownerState.multiline && styles5.inputMultiline, ownerState.type === "search" && styles5.inputTypeSearch, ownerState.startAdornment && styles5.inputAdornedStart, ownerState.endAdornment && styles5.inputAdornedEnd, ownerState.hiddenLabel && styles5.inputHiddenLabel];
};
var useUtilityClasses39 = (ownerState) => {
  const {
    classes: classes2,
    color,
    disabled,
    error,
    endAdornment,
    focused,
    formControl,
    fullWidth,
    hiddenLabel,
    multiline,
    readOnly,
    size,
    startAdornment,
    type
  } = ownerState;
  const slots = {
    root: ["root", `color${capitalize_default(color)}`, disabled && "disabled", error && "error", fullWidth && "fullWidth", focused && "focused", formControl && "formControl", size && size !== "medium" && `size${capitalize_default(size)}`, multiline && "multiline", startAdornment && "adornedStart", endAdornment && "adornedEnd", hiddenLabel && "hiddenLabel", readOnly && "readOnly"],
    input: ["input", disabled && "disabled", type === "search" && "inputTypeSearch", multiline && "inputMultiline", size === "small" && "inputSizeSmall", hiddenLabel && "inputHiddenLabel", startAdornment && "inputAdornedStart", endAdornment && "inputAdornedEnd", readOnly && "readOnly"]
  };
  return composeClasses(slots, getInputBaseUtilityClass, classes2);
};
var InputBaseRoot = styled_default2("div", {
  name: "MuiInputBase",
  slot: "Root",
  overridesResolver: rootOverridesResolver
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body1,
  color: (theme.vars || theme).palette.text.primary,
  lineHeight: "1.4375em",
  // 23px
  boxSizing: "border-box",
  // Prevent padding issue with fullWidth.
  position: "relative",
  cursor: "text",
  display: "inline-flex",
  alignItems: "center",
  [`&.${inputBaseClasses_default.disabled}`]: {
    color: (theme.vars || theme).palette.text.disabled,
    cursor: "default"
  },
  variants: [{
    props: ({
      ownerState
    }) => ownerState.multiline,
    style: {
      padding: "4px 0 5px"
    }
  }, {
    props: ({
      ownerState,
      size
    }) => ownerState.multiline && size === "small",
    style: {
      paddingTop: 1
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.fullWidth,
    style: {
      width: "100%"
    }
  }]
})));
var InputBaseInput = styled_default2("input", {
  name: "MuiInputBase",
  slot: "Input",
  overridesResolver: inputOverridesResolver
})(memoTheme_default(({
  theme
}) => {
  const light = theme.palette.mode === "light";
  const placeholder = {
    color: "currentColor",
    ...theme.vars ? {
      opacity: theme.vars.opacity.inputPlaceholder
    } : {
      opacity: light ? 0.42 : 0.5
    },
    transition: theme.transitions.create("opacity", {
      duration: theme.transitions.duration.shorter
    })
  };
  const placeholderHidden = {
    opacity: "0 !important"
  };
  const placeholderVisible = theme.vars ? {
    opacity: theme.vars.opacity.inputPlaceholder
  } : {
    opacity: light ? 0.42 : 0.5
  };
  return {
    font: "inherit",
    letterSpacing: "inherit",
    color: "currentColor",
    padding: "4px 0 5px",
    border: 0,
    boxSizing: "content-box",
    background: "none",
    height: "1.4375em",
    // Reset 23pxthe native input line-height
    margin: 0,
    // Reset for Safari
    WebkitTapHighlightColor: "transparent",
    display: "block",
    // Make the flex item shrink with Firefox
    minWidth: 0,
    width: "100%",
    "&::-webkit-input-placeholder": placeholder,
    "&::-moz-placeholder": placeholder,
    // Firefox 19+
    "&::-ms-input-placeholder": placeholder,
    // Edge
    "&:focus": {
      outline: 0
    },
    // Reset Firefox invalid required input style
    "&:invalid": {
      boxShadow: "none"
    },
    "&::-webkit-search-decoration": {
      // Remove the padding when type=search.
      WebkitAppearance: "none"
    },
    // Show and hide the placeholder logic
    [`label[data-shrink=false] + .${inputBaseClasses_default.formControl} &`]: {
      "&::-webkit-input-placeholder": placeholderHidden,
      "&::-moz-placeholder": placeholderHidden,
      // Firefox 19+
      "&::-ms-input-placeholder": placeholderHidden,
      // Edge
      "&:focus::-webkit-input-placeholder": placeholderVisible,
      "&:focus::-moz-placeholder": placeholderVisible,
      // Firefox 19+
      "&:focus::-ms-input-placeholder": placeholderVisible
      // Edge
    },
    [`&.${inputBaseClasses_default.disabled}`]: {
      opacity: 1,
      // Reset iOS opacity
      WebkitTextFillColor: (theme.vars || theme).palette.text.disabled
      // Fix opacity Safari bug
    },
    variants: [{
      props: ({
        ownerState
      }) => !ownerState.disableInjectingGlobalStyles,
      style: {
        animationName: "mui-auto-fill-cancel",
        animationDuration: "10ms",
        "&:-webkit-autofill": {
          animationDuration: "5000s",
          animationName: "mui-auto-fill"
        }
      }
    }, {
      props: {
        size: "small"
      },
      style: {
        paddingTop: 1
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.multiline,
      style: {
        height: "auto",
        resize: "none",
        padding: 0,
        paddingTop: 0
      }
    }, {
      props: {
        type: "search"
      },
      style: {
        MozAppearance: "textfield"
        // Improve type search style.
      }
    }]
  };
}));
var InputGlobalStyles = globalCss({
  "@keyframes mui-auto-fill": {
    from: {
      display: "block"
    }
  },
  "@keyframes mui-auto-fill-cancel": {
    from: {
      display: "block"
    }
  }
});
var InputBase = React103.forwardRef(function InputBase2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiInputBase"
  });
  const {
    "aria-describedby": ariaDescribedby,
    autoComplete,
    autoFocus,
    className,
    color,
    components = {},
    componentsProps = {},
    defaultValue,
    disabled,
    disableInjectingGlobalStyles,
    endAdornment,
    error,
    fullWidth = false,
    id,
    inputComponent = "input",
    inputProps: inputPropsProp = {},
    inputRef: inputRefProp,
    margin,
    maxRows,
    minRows,
    multiline = false,
    name,
    onBlur,
    onChange,
    onClick,
    onFocus,
    onKeyDown,
    onKeyUp,
    placeholder,
    readOnly,
    renderSuffix,
    rows,
    size,
    slotProps = {},
    slots = {},
    startAdornment,
    type = "text",
    value: valueProp,
    ...other
  } = props;
  const value = inputPropsProp.value != null ? inputPropsProp.value : valueProp;
  const {
    current: isControlled
  } = React103.useRef(value != null);
  const inputRef = React103.useRef();
  const handleInputRefWarning = React103.useCallback((instance) => {
    if (true) {
      if (instance && instance.nodeName !== "INPUT" && !instance.focus) {
        console.error(["MUI: You have provided a `inputComponent` to the input component", "that does not correctly handle the `ref` prop.", "Make sure the `ref` prop is called with a HTMLInputElement."].join("\n"));
      }
    }
  }, []);
  const handleInputRef = useForkRef_default(inputRef, inputRefProp, inputPropsProp.ref, handleInputRefWarning);
  const [focused, setFocused] = React103.useState(false);
  const muiFormControl = useFormControl();
  if (true) {
    React103.useEffect(() => {
      if (muiFormControl) {
        return muiFormControl.registerEffect();
      }
      return void 0;
    }, [muiFormControl]);
  }
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["color", "disabled", "error", "hiddenLabel", "size", "required", "filled"]
  });
  fcs.focused = muiFormControl ? muiFormControl.focused : focused;
  React103.useEffect(() => {
    if (!muiFormControl && disabled && focused) {
      setFocused(false);
      if (onBlur) {
        onBlur();
      }
    }
  }, [muiFormControl, disabled, focused, onBlur]);
  const onFilled = muiFormControl && muiFormControl.onFilled;
  const onEmpty = muiFormControl && muiFormControl.onEmpty;
  const checkDirty = React103.useCallback((obj) => {
    if (isFilled(obj)) {
      if (onFilled) {
        onFilled();
      }
    } else if (onEmpty) {
      onEmpty();
    }
  }, [onFilled, onEmpty]);
  useEnhancedEffect_default2(() => {
    if (isControlled) {
      checkDirty({
        value
      });
    }
  }, [value, checkDirty, isControlled]);
  const handleFocus = (event) => {
    if (onFocus) {
      onFocus(event);
    }
    if (inputPropsProp.onFocus) {
      inputPropsProp.onFocus(event);
    }
    if (muiFormControl && muiFormControl.onFocus) {
      muiFormControl.onFocus(event);
    } else {
      setFocused(true);
    }
  };
  const handleBlur = (event) => {
    if (onBlur) {
      onBlur(event);
    }
    if (inputPropsProp.onBlur) {
      inputPropsProp.onBlur(event);
    }
    if (muiFormControl && muiFormControl.onBlur) {
      muiFormControl.onBlur(event);
    } else {
      setFocused(false);
    }
  };
  const handleChange = (event, ...args) => {
    if (!isControlled) {
      const element = event.target || inputRef.current;
      if (element == null) {
        throw new Error(true ? "MUI: Expected valid input target. Did you use a custom `inputComponent` and forget to forward refs? See https://mui.com/r/input-component-ref-interface for more info." : formatMuiErrorMessage(1));
      }
      checkDirty({
        value: element.value
      });
    }
    if (inputPropsProp.onChange) {
      inputPropsProp.onChange(event, ...args);
    }
    if (onChange) {
      onChange(event, ...args);
    }
  };
  React103.useEffect(() => {
    checkDirty(inputRef.current);
  }, []);
  const handleClick = (event) => {
    if (inputRef.current && event.currentTarget === event.target) {
      inputRef.current.focus();
    }
    if (onClick) {
      onClick(event);
    }
  };
  let InputComponent = inputComponent;
  let inputProps = inputPropsProp;
  if (multiline && InputComponent === "input") {
    if (rows) {
      if (true) {
        if (minRows || maxRows) {
          console.warn("MUI: You can not use the `minRows` or `maxRows` props when the input `rows` prop is set.");
        }
      }
      inputProps = {
        type: void 0,
        minRows: rows,
        maxRows: rows,
        ...inputProps
      };
    } else {
      inputProps = {
        type: void 0,
        maxRows,
        minRows,
        ...inputProps
      };
    }
    InputComponent = TextareaAutosize_default;
  }
  const handleAutoFill = (event) => {
    checkDirty(event.animationName === "mui-auto-fill-cancel" ? inputRef.current : {
      value: "x"
    });
  };
  React103.useEffect(() => {
    if (muiFormControl) {
      muiFormControl.setAdornedStart(Boolean(startAdornment));
    }
  }, [muiFormControl, startAdornment]);
  const ownerState = {
    ...props,
    color: fcs.color || "primary",
    disabled: fcs.disabled,
    endAdornment,
    error: fcs.error,
    focused: fcs.focused,
    formControl: muiFormControl,
    fullWidth,
    hiddenLabel: fcs.hiddenLabel,
    multiline,
    size: fcs.size,
    startAdornment,
    type
  };
  const classes2 = useUtilityClasses39(ownerState);
  const Root = slots.root || components.Root || InputBaseRoot;
  const rootProps = slotProps.root || componentsProps.root || {};
  const Input3 = slots.input || components.Input || InputBaseInput;
  inputProps = {
    ...inputProps,
    ...slotProps.input ?? componentsProps.input
  };
  return (0, import_jsx_runtime75.jsxs)(React103.Fragment, {
    children: [!disableInjectingGlobalStyles && typeof InputGlobalStyles === "function" && // For Emotion/Styled-components, InputGlobalStyles will be a function
    // For Pigment CSS, this has no effect because the InputGlobalStyles will be null.
    (_InputGlobalStyles || (_InputGlobalStyles = (0, import_jsx_runtime75.jsx)(InputGlobalStyles, {}))), (0, import_jsx_runtime75.jsxs)(Root, {
      ...rootProps,
      ref,
      onClick: handleClick,
      ...other,
      ...!isHostComponent_default(Root) && {
        ownerState: {
          ...ownerState,
          ...rootProps.ownerState
        }
      },
      className: clsx_default(classes2.root, rootProps.className, className, readOnly && "MuiInputBase-readOnly"),
      children: [startAdornment, (0, import_jsx_runtime75.jsx)(FormControlContext_default.Provider, {
        value: null,
        children: (0, import_jsx_runtime75.jsx)(Input3, {
          "aria-invalid": fcs.error,
          "aria-describedby": ariaDescribedby,
          autoComplete,
          autoFocus,
          defaultValue,
          disabled: fcs.disabled,
          id,
          onAnimationStart: handleAutoFill,
          name,
          placeholder,
          readOnly,
          required: fcs.required,
          rows,
          value,
          onKeyDown,
          onKeyUp,
          type,
          ...inputProps,
          ...!isHostComponent_default(Input3) && {
            as: InputComponent,
            ownerState: {
              ...ownerState,
              ...inputProps.ownerState
            }
          },
          ref: handleInputRef,
          className: clsx_default(classes2.input, inputProps.className, readOnly && "MuiInputBase-readOnly"),
          onBlur: handleBlur,
          onChange: handleChange,
          onFocus: handleFocus
        })
      }), endAdornment, renderSuffix ? renderSuffix({
        ...fcs,
        startAdornment
      }) : null]
    })]
  });
});
true ? InputBase.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * @ignore
   */
  "aria-describedby": import_prop_types48.default.string,
  /**
   * This prop helps users to fill forms faster, especially on mobile devices.
   * The name can be confusing, as it's more like an autofill.
   * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
   */
  autoComplete: import_prop_types48.default.string,
  /**
   * If `true`, the `input` element is focused during the first mount.
   */
  autoFocus: import_prop_types48.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types48.default.object,
  /**
   * @ignore
   */
  className: import_prop_types48.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
   */
  color: import_prop_types48.default.oneOfType([import_prop_types48.default.oneOf(["primary", "secondary", "error", "info", "success", "warning"]), import_prop_types48.default.string]),
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types48.default.shape({
    Input: import_prop_types48.default.elementType,
    Root: import_prop_types48.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types48.default.shape({
    input: import_prop_types48.default.object,
    root: import_prop_types48.default.object
  }),
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types48.default.any,
  /**
   * If `true`, the component is disabled.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  disabled: import_prop_types48.default.bool,
  /**
   * If `true`, GlobalStyles for the auto-fill keyframes will not be injected/removed on mount/unmount. Make sure to inject them at the top of your application.
   * This option is intended to help with boosting the initial rendering performance if you are loading a big amount of Input components at once.
   * @default false
   */
  disableInjectingGlobalStyles: import_prop_types48.default.bool,
  /**
   * End `InputAdornment` for this component.
   */
  endAdornment: import_prop_types48.default.node,
  /**
   * If `true`, the `input` will indicate an error.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  error: import_prop_types48.default.bool,
  /**
   * If `true`, the `input` will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types48.default.bool,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types48.default.string,
  /**
   * The component used for the `input` element.
   * Either a string to use a HTML element or a component.
   * @default 'input'
   */
  inputComponent: elementTypeAcceptingRef_default,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @default {}
   */
  inputProps: import_prop_types48.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
   */
  margin: import_prop_types48.default.oneOf(["dense", "none"]),
  /**
   * Maximum number of rows to display when multiline option is set to true.
   */
  maxRows: import_prop_types48.default.oneOfType([import_prop_types48.default.number, import_prop_types48.default.string]),
  /**
   * Minimum number of rows to display when multiline option is set to true.
   */
  minRows: import_prop_types48.default.oneOfType([import_prop_types48.default.number, import_prop_types48.default.string]),
  /**
   * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
   * @default false
   */
  multiline: import_prop_types48.default.bool,
  /**
   * Name attribute of the `input` element.
   */
  name: import_prop_types48.default.string,
  /**
   * Callback fired when the `input` is blurred.
   *
   * Notice that the first argument (event) might be undefined.
   */
  onBlur: import_prop_types48.default.func,
  /**
   * Callback fired when the value is changed.
   *
   * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types48.default.func,
  /**
   * @ignore
   */
  onClick: import_prop_types48.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types48.default.func,
  /**
   * Callback fired when the `input` doesn't satisfy its constraints.
   */
  onInvalid: import_prop_types48.default.func,
  /**
   * @ignore
   */
  onKeyDown: import_prop_types48.default.func,
  /**
   * @ignore
   */
  onKeyUp: import_prop_types48.default.func,
  /**
   * The short hint displayed in the `input` before the user enters a value.
   */
  placeholder: import_prop_types48.default.string,
  /**
   * It prevents the user from changing the value of the field
   * (not from interacting with the field).
   */
  readOnly: import_prop_types48.default.bool,
  /**
   * @ignore
   */
  renderSuffix: import_prop_types48.default.func,
  /**
   * If `true`, the `input` element is required.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  required: import_prop_types48.default.bool,
  /**
   * Number of rows to display when multiline option is set to true.
   */
  rows: import_prop_types48.default.oneOfType([import_prop_types48.default.number, import_prop_types48.default.string]),
  /**
   * The size of the component.
   */
  size: import_prop_types48.default.oneOfType([import_prop_types48.default.oneOf(["medium", "small"]), import_prop_types48.default.string]),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slotProps: import_prop_types48.default.shape({
    input: import_prop_types48.default.object,
    root: import_prop_types48.default.object
  }),
  /**
   * The components used for each slot inside.
   *
   * This prop is an alias for the `components` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slots: import_prop_types48.default.shape({
    input: import_prop_types48.default.elementType,
    root: import_prop_types48.default.elementType
  }),
  /**
   * Start `InputAdornment` for this component.
   */
  startAdornment: import_prop_types48.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types48.default.oneOfType([import_prop_types48.default.arrayOf(import_prop_types48.default.oneOfType([import_prop_types48.default.func, import_prop_types48.default.object, import_prop_types48.default.bool])), import_prop_types48.default.func, import_prop_types48.default.object]),
  /**
   * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
   * @default 'text'
   */
  type: import_prop_types48.default.string,
  /**
   * The value of the `input` element, required for a controlled component.
   */
  value: import_prop_types48.default.any
} : void 0;
var InputBase_default = InputBase;

// node_modules/@mui/x-data-grid/esm/components/columnsManagement/utils.js
var checkColumnVisibilityModelsSame = (a, b) => {
  const aFalseValues = new Set(Object.keys(a).filter((key) => a[key] === false));
  const bFalseValues = new Set(Object.keys(b).filter((key) => b[key] === false));
  if (aFalseValues.size !== bFalseValues.size) {
    return false;
  }
  let result = true;
  aFalseValues.forEach((key) => {
    if (!bFalseValues.has(key)) {
      result = false;
    }
  });
  return result;
};
var defaultSearchPredicate = (column, searchValue) => (column.headerName || column.field).toLowerCase().indexOf(searchValue) > -1;

// node_modules/@mui/x-data-grid/esm/components/GridShadowScrollArea.js
var React104 = __toESM(require_react(), 1);
var import_prop_types49 = __toESM(require_prop_types(), 1);
var import_jsx_runtime76 = __toESM(require_jsx_runtime(), 1);
var _excluded43 = ["children"];
var reveal = keyframes({
  from: {
    opacity: 0
  },
  to: {
    opacity: 1
  }
});
var detectScroll = keyframes({
  "from, to": {
    "--scrollable": '" "'
  }
});
var ShadowScrollArea = styled_default("div", {
  name: "MuiDataGrid",
  slot: "ShadowScrollArea"
})`
  flex: 1;
  display: flex;
  flex-direction: column;
  animation: ${detectScroll};
  animation-timeline: --scroll-timeline;
  animation-fill-mode: none;
  box-sizing: border-box;
  overflow: auto;
  scrollbar-width: thin;
  scroll-timeline: --scroll-timeline block;

  &::before,
  &::after {
    content: '';
    flex-shrink: 0;
    display: block;
    position: sticky;
    left: 0;
    width: 100%;
    height: 4px;
    animation: ${reveal} linear both;
    animation-timeline: --scroll-timeline;

    // Custom property toggle trick:
    // - Detects if the element is scrollable
    // - https://css-tricks.com/the-css-custom-property-toggle-trick/
    --visibility-scrollable: var(--scrollable) visible;
    --visibility-not-scrollable: hidden;
    visibility: var(--visibility-scrollable, var(--visibility-not-scrollable));
  }

  &::before {
    top: 0;
    background: linear-gradient(to bottom, rgba(0, 0, 0, 0.05) 0, transparent 100%);
    animation-range: 0 4px;
  }

  &::after {
    bottom: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.05) 0, transparent 100%);
    animation-direction: reverse;
    animation-range: calc(100% - 4px) 100%;
  }
`;
var GridShadowScrollArea = forwardRef2(function GridShadowScrollArea2(props, ref) {
  const {
    children: children2
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded43);
  return (0, import_jsx_runtime76.jsx)(ShadowScrollArea, _extends({}, other, {
    ref,
    children: children2
  }));
});
if (true) GridShadowScrollArea.displayName = "GridShadowScrollArea";
true ? GridShadowScrollArea.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  children: import_prop_types49.default.node
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnsManagement/GridColumnsManagement.js
var import_jsx_runtime77 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses40 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["columnsManagement"],
    header: ["columnsManagementHeader"],
    searchInput: ["columnsManagementSearchInput"],
    footer: ["columnsManagementFooter"],
    row: ["columnsManagementRow"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var collator3 = new Intl.Collator();
function GridColumnsManagement(props) {
  var _a, _b, _c, _d;
  const apiRef = useGridApiContext();
  const searchInputRef = React105.useRef(null);
  const initialColumnVisibilityModel = useGridSelector(apiRef, gridInitialColumnVisibilityModelSelector);
  const columnVisibilityModel = useGridSelector(apiRef, gridColumnVisibilityModelSelector);
  const rootProps = useGridRootProps();
  const [searchValue, setSearchValue] = React105.useState("");
  const classes2 = useUtilityClasses40(rootProps);
  const columnDefinitions = useGridSelector(apiRef, gridColumnDefinitionsSelector);
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const pivotInitialColumns = useGridSelector(apiRef, gridPivotInitialColumnsSelector);
  const columns = React105.useMemo(() => pivotActive ? Array.from(pivotInitialColumns.values()) : columnDefinitions, [pivotActive, pivotInitialColumns, columnDefinitions]);
  const {
    sort,
    searchPredicate = defaultSearchPredicate,
    autoFocusSearchField = true,
    disableShowHideToggle = false,
    disableResetButton = false,
    toggleAllMode = "all",
    getTogglableColumns,
    searchInputProps,
    searchDebounceMs = rootProps.columnFilterDebounceMs
  } = props;
  const debouncedFilter = React105.useMemo(() => debounce((value) => {
    setSearchValue(value);
  }, searchDebounceMs ?? 150), [searchDebounceMs]);
  const isResetDisabled = React105.useMemo(() => checkColumnVisibilityModelsSame(columnVisibilityModel, initialColumnVisibilityModel), [columnVisibilityModel, initialColumnVisibilityModel]);
  const sortedColumns = React105.useMemo(() => {
    switch (sort) {
      case "asc":
        return [...columns].sort((a, b) => collator3.compare(a.headerName || a.field, b.headerName || b.field));
      case "desc":
        return [...columns].sort((a, b) => -collator3.compare(a.headerName || a.field, b.headerName || b.field));
      default:
        return columns;
    }
  }, [columns, sort]);
  const toggleColumn = (event) => {
    const {
      name: field
    } = event.target;
    apiRef.current.setColumnVisibility(field, columnVisibilityModel[field] === false);
  };
  const currentColumns = React105.useMemo(() => {
    const togglableColumns = getTogglableColumns ? getTogglableColumns(sortedColumns) : null;
    const togglableSortedColumns = togglableColumns ? sortedColumns.filter(({
      field
    }) => togglableColumns.includes(field)) : sortedColumns;
    if (!searchValue) {
      return togglableSortedColumns;
    }
    return togglableSortedColumns.filter((column) => searchPredicate(column, searchValue.toLowerCase()));
  }, [sortedColumns, searchValue, searchPredicate, getTogglableColumns]);
  const toggleAllColumns = React105.useCallback((isVisible) => {
    const currentModel = gridColumnVisibilityModelSelector(apiRef);
    const newModel = _extends({}, currentModel);
    const togglableColumns = getTogglableColumns ? getTogglableColumns(columns) : null;
    (toggleAllMode === "filteredOnly" ? currentColumns : columns).forEach((col) => {
      if (col.hideable && (togglableColumns == null || togglableColumns.includes(col.field))) {
        if (isVisible) {
          delete newModel[col.field];
        } else {
          newModel[col.field] = false;
        }
      }
    });
    return apiRef.current.setColumnVisibilityModel(newModel);
  }, [apiRef, columns, getTogglableColumns, toggleAllMode, currentColumns]);
  const handleSearchValueChange = React105.useCallback((event) => {
    debouncedFilter(event.target.value);
  }, [debouncedFilter]);
  const hideableColumns = React105.useMemo(() => currentColumns.filter((col) => col.hideable), [currentColumns]);
  const allHideableColumnsVisible = React105.useMemo(() => hideableColumns.every((column) => columnVisibilityModel[column.field] == null || columnVisibilityModel[column.field] !== false), [columnVisibilityModel, hideableColumns]);
  const allHideableColumnsHidden = React105.useMemo(() => hideableColumns.every((column) => columnVisibilityModel[column.field] === false), [columnVisibilityModel, hideableColumns]);
  const firstSwitchRef = React105.useRef(null);
  React105.useEffect(() => {
    var _a2;
    if (autoFocusSearchField) {
      (_a2 = searchInputRef.current) == null ? void 0 : _a2.focus();
    } else if (firstSwitchRef.current && typeof firstSwitchRef.current.focus === "function") {
      firstSwitchRef.current.focus();
    }
  }, [autoFocusSearchField]);
  let firstHideableColumnFound = false;
  const isFirstHideableColumn = (column) => {
    if (firstHideableColumnFound === false && column.hideable !== false) {
      firstHideableColumnFound = true;
      return true;
    }
    return false;
  };
  const handleSearchReset = React105.useCallback(() => {
    setSearchValue("");
    if (searchInputRef.current) {
      searchInputRef.current.value = "";
      searchInputRef.current.focus();
    }
  }, []);
  return (0, import_jsx_runtime77.jsxs)(React105.Fragment, {
    children: [(0, import_jsx_runtime77.jsx)(GridColumnsManagementHeader, {
      className: classes2.header,
      ownerState: rootProps,
      children: (0, import_jsx_runtime77.jsx)(SearchInput, _extends({
        as: rootProps.slots.baseTextField,
        ownerState: rootProps,
        placeholder: apiRef.current.getLocaleText("columnsManagementSearchTitle"),
        inputRef: searchInputRef,
        className: classes2.searchInput,
        onChange: handleSearchValueChange,
        size: "small",
        type: "search",
        slotProps: {
          input: {
            startAdornment: (0, import_jsx_runtime77.jsx)(rootProps.slots.quickFilterIcon, {
              fontSize: "small"
            }),
            endAdornment: (0, import_jsx_runtime77.jsx)(rootProps.slots.baseIconButton, _extends({
              size: "small",
              "aria-label": apiRef.current.getLocaleText("columnsManagementDeleteIconLabel"),
              style: searchValue ? {
                visibility: "visible"
              } : {
                visibility: "hidden"
              },
              tabIndex: -1,
              onClick: handleSearchReset,
              edge: "end"
            }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
              children: (0, import_jsx_runtime77.jsx)(rootProps.slots.quickFilterClearIcon, {
                fontSize: "small"
              })
            }))
          },
          htmlInput: {
            "aria-label": apiRef.current.getLocaleText("columnsManagementSearchTitle")
          }
        },
        autoComplete: "off",
        fullWidth: true
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseTextField, searchInputProps))
    }), (0, import_jsx_runtime77.jsx)(GridColumnsManagementScrollArea, {
      ownerState: rootProps,
      children: (0, import_jsx_runtime77.jsxs)(GridColumnsManagementBody, {
        className: classes2.root,
        ownerState: rootProps,
        children: [currentColumns.map((column) => {
          var _a2;
          return (0, import_jsx_runtime77.jsx)(GridColumnsManagementRow, _extends({
            as: rootProps.slots.baseCheckbox,
            className: classes2.row,
            disabled: column.hideable === false || pivotActive,
            checked: columnVisibilityModel[column.field] !== false,
            onChange: toggleColumn,
            name: column.field,
            inputRef: isFirstHideableColumn(column) ? firstSwitchRef : void 0,
            label: column.headerName || column.field,
            density: "compact",
            fullWidth: true
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseCheckbox), column.field);
        }), currentColumns.length === 0 && (0, import_jsx_runtime77.jsx)(GridColumnsManagementEmptyText, {
          ownerState: rootProps,
          children: apiRef.current.getLocaleText("columnsManagementNoColumns")
        })]
      })
    }), !disableShowHideToggle || !disableResetButton ? (0, import_jsx_runtime77.jsxs)(GridColumnsManagementFooter, {
      ownerState: rootProps,
      className: classes2.footer,
      children: [!disableShowHideToggle ? (0, import_jsx_runtime77.jsx)(rootProps.slots.baseCheckbox, _extends({
        disabled: hideableColumns.length === 0 || pivotActive,
        checked: allHideableColumnsVisible,
        indeterminate: !allHideableColumnsVisible && !allHideableColumnsHidden,
        onChange: () => toggleAllColumns(!allHideableColumnsVisible),
        name: apiRef.current.getLocaleText("columnsManagementShowHideAllText"),
        label: apiRef.current.getLocaleText("columnsManagementShowHideAllText"),
        density: "compact"
      }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseCheckbox)) : (0, import_jsx_runtime77.jsx)("span", {}), !disableResetButton ? (0, import_jsx_runtime77.jsx)(rootProps.slots.baseButton, _extends({
        onClick: () => apiRef.current.setColumnVisibilityModel(initialColumnVisibilityModel),
        disabled: isResetDisabled || pivotActive
      }, (_d = rootProps.slotProps) == null ? void 0 : _d.baseButton, {
        children: apiRef.current.getLocaleText("columnsManagementReset")
      })) : null]
    }) : null]
  });
}
true ? GridColumnsManagement.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * If `true`, the column search field will be focused automatically.
   * If `false`, the first column switch input will be focused automatically.
   * This helps to avoid input keyboard panel to popup automatically on touch devices.
   * @default true
   */
  autoFocusSearchField: import_prop_types50.default.bool,
  /**
   * If `true`, the `Reset` button will not be disabled
   * @default false
   */
  disableResetButton: import_prop_types50.default.bool,
  /**
   * If `true`, the `Show/Hide all` toggle checkbox will not be displayed.
   * @default false
   */
  disableShowHideToggle: import_prop_types50.default.bool,
  /**
   * Returns the list of togglable columns.
   * If used, only those columns will be displayed in the panel
   * which are passed as the return value of the function.
   * @param {GridColDef[]} columns The `ColDef` list of all columns.
   * @returns {GridColDef['field'][]} The list of togglable columns' field names.
   */
  getTogglableColumns: import_prop_types50.default.func,
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering in the columns menu.
   * @default 150
   */
  searchDebounceMs: import_prop_types50.default.number,
  searchInputProps: import_prop_types50.default.object,
  searchPredicate: import_prop_types50.default.func,
  sort: import_prop_types50.default.oneOf(["asc", "desc"]),
  /**
   * Changes the behavior of the `Show/Hide All` toggle when the search field is used:
   * - `all`: Will toggle all columns.
   * - `filteredOnly`: Will only toggle columns that match the search criteria.
   * @default 'all'
   */
  toggleAllMode: import_prop_types50.default.oneOf(["all", "filteredOnly"])
} : void 0;
var GridColumnsManagementBody = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnsManagement"
})({
  display: "flex",
  flexDirection: "column",
  padding: vars.spacing(0.5, 1.5)
});
var GridColumnsManagementScrollArea = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "ColumnsManagementScrollArea"
})({
  maxHeight: 300
});
var GridColumnsManagementHeader = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnsManagementHeader"
})({
  padding: vars.spacing(1.5, 2),
  borderBottom: `1px solid ${vars.colors.border.base}`
});
var SearchInput = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "ColumnsManagementSearchInput"
})({
  [`& .${inputBaseClasses_default.input}::-webkit-search-decoration,
      & .${inputBaseClasses_default.input}::-webkit-search-cancel-button,
      & .${inputBaseClasses_default.input}::-webkit-search-results-button,
      & .${inputBaseClasses_default.input}::-webkit-search-results-decoration`]: {
    /* clears the 'X' icon from Chrome */
    display: "none"
  }
});
var GridColumnsManagementFooter = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnsManagementFooter"
})({
  padding: vars.spacing(1, 1, 1, 1.5),
  display: "flex",
  justifyContent: "space-between",
  borderTop: `1px solid ${vars.colors.border.base}`
});
var GridColumnsManagementEmptyText = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnsManagementEmptyText"
})({
  padding: vars.spacing(1, 0),
  alignSelf: "center",
  font: vars.typography.font.body
});
var GridColumnsManagementRow = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "ColumnsManagementRow"
})({});

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbar.js
var import_prop_types66 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarColumnsButton.js
var React106 = __toESM(require_react(), 1);
var import_prop_types51 = __toESM(require_prop_types(), 1);
var import_jsx_runtime78 = __toESM(require_jsx_runtime(), 1);
var GridToolbarColumnsButton = forwardRef2(function GridToolbarColumnsButton2(props, ref) {
  var _a, _b;
  const {
    slotProps = {}
  } = props;
  const buttonProps = slotProps.button || {};
  const tooltipProps = slotProps.tooltip || {};
  const columnButtonId = useId();
  const columnPanelId = useId();
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const {
    columnsPanelTriggerRef
  } = useGridPanelContext();
  const preferencePanel = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const handleRef = useForkRef(ref, columnsPanelTriggerRef);
  const showColumns = (event) => {
    var _a2;
    if (preferencePanel.open && preferencePanel.openedPanelValue === GridPreferencePanelsValue.columns) {
      apiRef.current.hidePreferences();
    } else {
      apiRef.current.showPreferences(GridPreferencePanelsValue.columns, columnPanelId, columnButtonId);
    }
    (_a2 = buttonProps.onClick) == null ? void 0 : _a2.call(buttonProps, event);
  };
  if (rootProps.disableColumnSelector) {
    return null;
  }
  const isOpen = preferencePanel.open && preferencePanel.panelId === columnPanelId;
  return (0, import_jsx_runtime78.jsx)(rootProps.slots.baseTooltip, _extends({
    title: apiRef.current.getLocaleText("toolbarColumnsLabel"),
    enterDelay: 1e3
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, tooltipProps, {
    children: (0, import_jsx_runtime78.jsx)(rootProps.slots.baseButton, _extends({
      id: columnButtonId,
      size: "small",
      "aria-label": apiRef.current.getLocaleText("toolbarColumnsLabel"),
      "aria-haspopup": "menu",
      "aria-expanded": isOpen,
      "aria-controls": isOpen ? columnPanelId : void 0,
      startIcon: (0, import_jsx_runtime78.jsx)(rootProps.slots.columnSelectorIcon, {})
    }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseButton, buttonProps, {
      onPointerUp: (event) => {
        var _a2;
        if (preferencePanel.open) {
          event.stopPropagation();
        }
        (_a2 = buttonProps.onPointerUp) == null ? void 0 : _a2.call(buttonProps, event);
      },
      onClick: showColumns,
      ref: handleRef,
      children: apiRef.current.getLocaleText("toolbarColumns")
    }))
  }));
});
if (true) GridToolbarColumnsButton.displayName = "GridToolbarColumnsButton";
true ? GridToolbarColumnsButton.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types51.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarDensitySelector.js
var React107 = __toESM(require_react(), 1);
var import_prop_types52 = __toESM(require_prop_types(), 1);
var import_jsx_runtime79 = __toESM(require_jsx_runtime(), 1);
var GridToolbarDensitySelector = forwardRef2(function GridToolbarDensitySelector2(props, ref) {
  var _a, _b;
  const {
    slotProps = {}
  } = props;
  const buttonProps = slotProps.button || {};
  const tooltipProps = slotProps.tooltip || {};
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const density = useGridSelector(apiRef, gridDensitySelector);
  const densityButtonId = useId();
  const densityMenuId = useId();
  const [open, setOpen] = React107.useState(false);
  const buttonRef = React107.useRef(null);
  const handleRef = useForkRef(ref, buttonRef);
  const densityOptions = [{
    icon: (0, import_jsx_runtime79.jsx)(rootProps.slots.densityCompactIcon, {}),
    label: apiRef.current.getLocaleText("toolbarDensityCompact"),
    value: "compact"
  }, {
    icon: (0, import_jsx_runtime79.jsx)(rootProps.slots.densityStandardIcon, {}),
    label: apiRef.current.getLocaleText("toolbarDensityStandard"),
    value: "standard"
  }, {
    icon: (0, import_jsx_runtime79.jsx)(rootProps.slots.densityComfortableIcon, {}),
    label: apiRef.current.getLocaleText("toolbarDensityComfortable"),
    value: "comfortable"
  }];
  const startIcon = React107.useMemo(() => {
    switch (density) {
      case "compact":
        return (0, import_jsx_runtime79.jsx)(rootProps.slots.densityCompactIcon, {});
      case "comfortable":
        return (0, import_jsx_runtime79.jsx)(rootProps.slots.densityComfortableIcon, {});
      default:
        return (0, import_jsx_runtime79.jsx)(rootProps.slots.densityStandardIcon, {});
    }
  }, [density, rootProps]);
  const handleDensitySelectorOpen = (event) => {
    var _a2;
    setOpen((prevOpen) => !prevOpen);
    (_a2 = buttonProps.onClick) == null ? void 0 : _a2.call(buttonProps, event);
  };
  const handleDensitySelectorClose = () => {
    setOpen(false);
  };
  const handleDensityUpdate = (newDensity) => {
    apiRef.current.setDensity(newDensity);
    setOpen(false);
  };
  if (rootProps.disableDensitySelector) {
    return null;
  }
  const densityElements = densityOptions.map((option, index) => (0, import_jsx_runtime79.jsx)(rootProps.slots.baseMenuItem, {
    onClick: () => handleDensityUpdate(option.value),
    selected: option.value === density,
    iconStart: option.icon,
    children: option.label
  }, index));
  return (0, import_jsx_runtime79.jsxs)(React107.Fragment, {
    children: [(0, import_jsx_runtime79.jsx)(rootProps.slots.baseTooltip, _extends({
      title: apiRef.current.getLocaleText("toolbarDensityLabel"),
      enterDelay: 1e3
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, tooltipProps, {
      children: (0, import_jsx_runtime79.jsx)(rootProps.slots.baseButton, _extends({
        size: "small",
        startIcon,
        "aria-label": apiRef.current.getLocaleText("toolbarDensityLabel"),
        "aria-haspopup": "menu",
        "aria-expanded": open,
        "aria-controls": open ? densityMenuId : void 0,
        id: densityButtonId
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseButton, buttonProps, {
        onClick: handleDensitySelectorOpen,
        ref: handleRef,
        children: apiRef.current.getLocaleText("toolbarDensity")
      }))
    })), (0, import_jsx_runtime79.jsx)(GridMenu, {
      open,
      target: buttonRef.current,
      onClose: handleDensitySelectorClose,
      position: "bottom-end",
      children: (0, import_jsx_runtime79.jsx)(rootProps.slots.baseMenuList, {
        id: densityMenuId,
        className: gridClasses.menuList,
        "aria-labelledby": densityButtonId,
        autoFocusItem: open,
        children: densityElements
      })
    })]
  });
});
if (true) GridToolbarDensitySelector.displayName = "GridToolbarDensitySelector";
true ? GridToolbarDensitySelector.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types52.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarFilterButton.js
var React108 = __toESM(require_react(), 1);
var import_prop_types53 = __toESM(require_prop_types(), 1);
var import_jsx_runtime80 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses41 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["toolbarFilterList"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridToolbarFilterListRoot = styled_default2("ul", {
  name: "MuiDataGrid",
  slot: "ToolbarFilterList"
})({
  margin: vars.spacing(1, 1, 0.5),
  padding: vars.spacing(0, 1)
});
var GridToolbarFilterButton = forwardRef2(function GridToolbarFilterButton2(props, ref) {
  var _a, _b, _c;
  const {
    slotProps = {}
  } = props;
  const buttonProps = slotProps.button || {};
  const tooltipProps = slotProps.tooltip || {};
  const badgeProps = slotProps.badge || {};
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const activeFilters = useGridSelector(apiRef, gridFilterActiveItemsSelector);
  const lookup = useGridSelector(apiRef, gridColumnLookupSelector);
  const preferencePanel = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const classes2 = useUtilityClasses41(rootProps);
  const filterButtonId = useId();
  const filterPanelId = useId();
  const {
    filterPanelTriggerRef
  } = useGridPanelContext();
  const handleRef = useForkRef(ref, filterPanelTriggerRef);
  const tooltipContentNode = React108.useMemo(() => {
    if (preferencePanel.open) {
      return apiRef.current.getLocaleText("toolbarFiltersTooltipHide");
    }
    if (activeFilters.length === 0) {
      return apiRef.current.getLocaleText("toolbarFiltersTooltipShow");
    }
    const getOperatorLabel = (item) => lookup[item.field].filterOperators.find((operator) => operator.value === item.operator).label || apiRef.current.getLocaleText(`filterOperator${capitalize(item.operator)}`).toString();
    const getFilterItemValue = (item) => {
      const {
        getValueAsString
      } = lookup[item.field].filterOperators.find((operator) => operator.value === item.operator);
      return getValueAsString ? getValueAsString(item.value) : item.value;
    };
    return (0, import_jsx_runtime80.jsxs)("div", {
      children: [apiRef.current.getLocaleText("toolbarFiltersTooltipActive")(activeFilters.length), (0, import_jsx_runtime80.jsx)(GridToolbarFilterListRoot, {
        className: classes2.root,
        ownerState: rootProps,
        children: activeFilters.map((item, index) => _extends({}, lookup[item.field] && (0, import_jsx_runtime80.jsx)("li", {
          children: `${lookup[item.field].headerName || item.field}
                  ${getOperatorLabel(item)}
                  ${// implicit check for null and undefined
          item.value != null ? getFilterItemValue(item) : ""}`
        }, index)))
      })]
    });
  }, [apiRef, rootProps, preferencePanel.open, activeFilters, lookup, classes2]);
  const toggleFilter = (event) => {
    var _a2;
    const {
      open,
      openedPanelValue
    } = preferencePanel;
    if (open && openedPanelValue === GridPreferencePanelsValue.filters) {
      apiRef.current.hidePreferences();
    } else {
      apiRef.current.showPreferences(GridPreferencePanelsValue.filters, filterPanelId, filterButtonId);
    }
    (_a2 = buttonProps.onClick) == null ? void 0 : _a2.call(buttonProps, event);
  };
  if (rootProps.disableColumnFilter) {
    return null;
  }
  const isOpen = preferencePanel.open && preferencePanel.panelId === filterPanelId;
  return (0, import_jsx_runtime80.jsx)(rootProps.slots.baseTooltip, _extends({
    title: tooltipContentNode,
    enterDelay: 1e3
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, tooltipProps, {
    children: (0, import_jsx_runtime80.jsx)(rootProps.slots.baseButton, _extends({
      id: filterButtonId,
      size: "small",
      "aria-label": apiRef.current.getLocaleText("toolbarFiltersLabel"),
      "aria-controls": isOpen ? filterPanelId : void 0,
      "aria-expanded": isOpen,
      "aria-haspopup": true,
      startIcon: (0, import_jsx_runtime80.jsx)(rootProps.slots.baseBadge, _extends({
        badgeContent: activeFilters.length,
        color: "primary"
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseBadge, badgeProps, {
        children: (0, import_jsx_runtime80.jsx)(rootProps.slots.openFilterButtonIcon, {})
      }))
    }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseButton, buttonProps, {
      onClick: toggleFilter,
      onPointerUp: (event) => {
        var _a2;
        if (preferencePanel.open) {
          event.stopPropagation();
        }
        (_a2 = buttonProps.onPointerUp) == null ? void 0 : _a2.call(buttonProps, event);
      },
      ref: handleRef,
      children: apiRef.current.getLocaleText("toolbarFilters")
    }))
  }));
});
if (true) GridToolbarFilterButton.displayName = "GridToolbarFilterButton";
true ? GridToolbarFilterButton.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types53.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarExport.js
var React110 = __toESM(require_react(), 1);
var import_prop_types55 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarExportContainer.js
var React109 = __toESM(require_react(), 1);
var import_prop_types54 = __toESM(require_prop_types(), 1);
var import_jsx_runtime81 = __toESM(require_jsx_runtime(), 1);
var GridToolbarExportContainer = forwardRef2(function GridToolbarExportContainer2(props, ref) {
  var _a, _b;
  const {
    children: children2,
    slotProps = {}
  } = props;
  const buttonProps = slotProps.button || {};
  const tooltipProps = slotProps.tooltip || {};
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const exportButtonId = useId();
  const exportMenuId = useId();
  const [open, setOpen] = React109.useState(false);
  const buttonRef = React109.useRef(null);
  const handleRef = useForkRef(ref, buttonRef);
  const handleMenuOpen = (event) => {
    var _a2;
    setOpen((prevOpen) => !prevOpen);
    (_a2 = buttonProps.onClick) == null ? void 0 : _a2.call(buttonProps, event);
  };
  const handleMenuClose = () => setOpen(false);
  if (children2 == null) {
    return null;
  }
  return (0, import_jsx_runtime81.jsxs)(React109.Fragment, {
    children: [(0, import_jsx_runtime81.jsx)(rootProps.slots.baseTooltip, _extends({
      title: apiRef.current.getLocaleText("toolbarExportLabel"),
      enterDelay: 1e3
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, tooltipProps, {
      children: (0, import_jsx_runtime81.jsx)(rootProps.slots.baseButton, _extends({
        size: "small",
        startIcon: (0, import_jsx_runtime81.jsx)(rootProps.slots.exportIcon, {}),
        "aria-expanded": open,
        "aria-label": apiRef.current.getLocaleText("toolbarExportLabel"),
        "aria-haspopup": "menu",
        "aria-controls": open ? exportMenuId : void 0,
        id: exportButtonId
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseButton, buttonProps, {
        onClick: handleMenuOpen,
        ref: handleRef,
        children: apiRef.current.getLocaleText("toolbarExport")
      }))
    })), (0, import_jsx_runtime81.jsx)(GridMenu, {
      open,
      target: buttonRef.current,
      onClose: handleMenuClose,
      position: "bottom-end",
      children: (0, import_jsx_runtime81.jsx)(rootProps.slots.baseMenuList, {
        id: exportMenuId,
        className: gridClasses.menuList,
        "aria-labelledby": exportButtonId,
        autoFocusItem: open,
        children: React109.Children.map(children2, (child) => {
          if (!React109.isValidElement(child)) {
            return child;
          }
          return React109.cloneElement(child, {
            hideMenu: handleMenuClose
          });
        })
      })
    })]
  });
});
if (true) GridToolbarExportContainer.displayName = "GridToolbarExportContainer";
true ? GridToolbarExportContainer.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types54.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarExport.js
var import_jsx_runtime82 = __toESM(require_jsx_runtime(), 1);
var _excluded44 = ["hideMenu", "options"];
var _excluded211 = ["hideMenu", "options"];
var _excluded310 = ["csvOptions", "printOptions", "excelOptions"];
function GridCsvExportMenuItem(props) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const {
    hideMenu,
    options: options3
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded44);
  return (0, import_jsx_runtime82.jsx)(rootProps.slots.baseMenuItem, _extends({
    onClick: () => {
      apiRef.current.exportDataAsCsv(options3);
      hideMenu == null ? void 0 : hideMenu();
    }
  }, other, {
    children: apiRef.current.getLocaleText("toolbarExportCSV")
  }));
}
true ? GridCsvExportMenuItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  hideMenu: import_prop_types55.default.func,
  options: import_prop_types55.default.shape({
    allColumns: import_prop_types55.default.bool,
    delimiter: import_prop_types55.default.string,
    disableToolbarButton: import_prop_types55.default.bool,
    escapeFormulas: import_prop_types55.default.bool,
    fields: import_prop_types55.default.arrayOf(import_prop_types55.default.string),
    fileName: import_prop_types55.default.string,
    getRowsToExport: import_prop_types55.default.func,
    includeColumnGroupsHeaders: import_prop_types55.default.bool,
    includeHeaders: import_prop_types55.default.bool,
    shouldAppendQuotes: import_prop_types55.default.bool,
    utf8WithBom: import_prop_types55.default.bool
  })
} : void 0;
function GridPrintExportMenuItem(props) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const {
    hideMenu,
    options: options3
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded211);
  return (0, import_jsx_runtime82.jsx)(rootProps.slots.baseMenuItem, _extends({
    onClick: () => {
      apiRef.current.exportDataAsPrint(options3);
      hideMenu == null ? void 0 : hideMenu();
    }
  }, other, {
    children: apiRef.current.getLocaleText("toolbarExportPrint")
  }));
}
true ? GridPrintExportMenuItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  hideMenu: import_prop_types55.default.func,
  options: import_prop_types55.default.shape({
    allColumns: import_prop_types55.default.bool,
    bodyClassName: import_prop_types55.default.string,
    copyStyles: import_prop_types55.default.bool,
    disableToolbarButton: import_prop_types55.default.bool,
    fields: import_prop_types55.default.arrayOf(import_prop_types55.default.string),
    fileName: import_prop_types55.default.string,
    getRowsToExport: import_prop_types55.default.func,
    hideFooter: import_prop_types55.default.bool,
    hideToolbar: import_prop_types55.default.bool,
    includeCheckboxes: import_prop_types55.default.bool,
    pageStyle: import_prop_types55.default.oneOfType([import_prop_types55.default.func, import_prop_types55.default.string])
  })
} : void 0;
var GridToolbarExport = forwardRef2(function GridToolbarExport2(props, ref) {
  const _ref = props, {
    csvOptions = {},
    printOptions = {},
    excelOptions
  } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded310);
  const apiRef = useGridApiContext();
  const preProcessedButtons = apiRef.current.unstable_applyPipeProcessors("exportMenu", [], {
    excelOptions,
    csvOptions,
    printOptions
  }).sort((a, b) => a.componentName > b.componentName ? 1 : -1);
  if (preProcessedButtons.length === 0) {
    return null;
  }
  return (0, import_jsx_runtime82.jsx)(GridToolbarExportContainer, _extends({}, other, {
    ref,
    children: preProcessedButtons.map((button, index) => React110.cloneElement(button.component, {
      key: index
    }))
  }));
});
if (true) GridToolbarExport.displayName = "GridToolbarExport";
true ? GridToolbarExport.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  csvOptions: import_prop_types55.default.object,
  printOptions: import_prop_types55.default.object,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types55.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarQuickFilter.js
var import_prop_types60 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilter.js
var React112 = __toESM(require_react(), 1);
var import_prop_types56 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilterContext.js
var React111 = __toESM(require_react(), 1);
var QuickFilterContext = React111.createContext(void 0);
if (true) QuickFilterContext.displayName = "QuickFilterContext";
function useQuickFilterContext() {
  const context = React111.useContext(QuickFilterContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context. Quick Filter subcomponents must be placed within a <QuickFilter /> component.");
  }
  return context;
}

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilter.js
var import_jsx_runtime83 = __toESM(require_jsx_runtime(), 1);
var _excluded45 = ["render", "className", "parser", "formatter", "debounceMs", "defaultExpanded", "expanded", "onExpandedChange"];
var DEFAULT_PARSER = (searchText) => searchText.split(" ").filter((word) => word !== "");
var DEFAULT_FORMATTER = (values3) => values3.join(" ");
function QuickFilter(props) {
  const rootProps = useGridRootProps();
  const {
    render,
    className,
    parser = DEFAULT_PARSER,
    formatter = DEFAULT_FORMATTER,
    debounceMs = rootProps.filterDebounceMs,
    defaultExpanded,
    expanded,
    onExpandedChange
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded45);
  const apiRef = useGridApiContext();
  const controlRef = React112.useRef(null);
  const triggerRef = React112.useRef(null);
  const quickFilterValues = useGridSelector(apiRef, gridQuickFilterValuesSelector);
  const [value, setValue] = React112.useState(formatter(quickFilterValues ?? []));
  const [internalExpanded, setInternalExpanded] = React112.useState(defaultExpanded ?? value.length > 0);
  const expandedValue = expanded ?? internalExpanded;
  const state = React112.useMemo(() => ({
    value,
    expanded: expandedValue
  }), [value, expandedValue]);
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const ref = React112.useRef(null);
  const controlId = useId();
  const handleExpandedChange = React112.useCallback((newExpanded) => {
    if (onExpandedChange) {
      onExpandedChange(newExpanded);
    }
    if (expanded === void 0) {
      setInternalExpanded(newExpanded);
    }
  }, [onExpandedChange, expanded]);
  const prevQuickFilterValuesRef = React112.useRef(quickFilterValues);
  React112.useEffect(() => {
    if (!isDeepEqual(prevQuickFilterValuesRef.current, quickFilterValues)) {
      prevQuickFilterValuesRef.current = quickFilterValues;
      setValue((prevSearchValue) => isDeepEqual(parser(prevSearchValue), quickFilterValues) ? prevSearchValue : formatter(quickFilterValues ?? []));
    }
  }, [quickFilterValues, formatter, parser]);
  const isFirstRender = React112.useRef(true);
  const previousExpandedValue = React112.useRef(expandedValue);
  React112.useEffect(() => {
    var _a;
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    if (previousExpandedValue.current !== expandedValue) {
      if (expandedValue) {
        requestAnimationFrame(() => {
          var _a2;
          (_a2 = controlRef.current) == null ? void 0 : _a2.focus({
            preventScroll: true
          });
        });
      } else {
        (_a = triggerRef.current) == null ? void 0 : _a.focus({
          preventScroll: true
        });
      }
      previousExpandedValue.current = expandedValue;
    }
  }, [expandedValue]);
  const setQuickFilterValueDebounced = React112.useMemo(() => debounce((newValue) => {
    const newQuickFilterValues = parser(newValue);
    prevQuickFilterValuesRef.current = newQuickFilterValues;
    apiRef.current.setQuickFilterValues(newQuickFilterValues);
  }, debounceMs), [apiRef, debounceMs, parser]);
  React112.useEffect(() => setQuickFilterValueDebounced.clear, [setQuickFilterValueDebounced]);
  const handleValueChange = React112.useCallback((event) => {
    const newValue = event.target.value;
    setValue(newValue);
    setQuickFilterValueDebounced(newValue);
  }, [setQuickFilterValueDebounced]);
  const handleClearValue = React112.useCallback(() => {
    var _a;
    setValue("");
    apiRef.current.setQuickFilterValues([]);
    (_a = controlRef.current) == null ? void 0 : _a.focus();
  }, [apiRef, controlRef]);
  const contextValue = React112.useMemo(() => ({
    controlRef,
    triggerRef,
    state,
    controlId,
    clearValue: handleClearValue,
    onValueChange: handleValueChange,
    onExpandedChange: handleExpandedChange
  }), [controlId, state, handleValueChange, handleClearValue, handleExpandedChange]);
  useEnhancedEffect_default(() => {
    var _a;
    if (ref.current && triggerRef.current) {
      ref.current.style.setProperty("--trigger-width", `${(_a = triggerRef.current) == null ? void 0 : _a.offsetWidth}px`);
    }
  }, []);
  const element = useComponentRenderer("div", render, _extends({
    className: resolvedClassName
  }, other, {
    ref
  }), state);
  return (0, import_jsx_runtime83.jsx)(QuickFilterContext.Provider, {
    value: contextValue,
    children: element
  });
}
true ? QuickFilter.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types56.default.oneOfType([import_prop_types56.default.func, import_prop_types56.default.string]),
  /**
   * The debounce time in milliseconds.
   * @default 150
   */
  debounceMs: import_prop_types56.default.number,
  /**
   * The default expanded state of the quick filter control.
   * @default false
   */
  defaultExpanded: import_prop_types56.default.bool,
  /**
   * The expanded state of the quick filter control.
   */
  expanded: import_prop_types56.default.bool,
  /**
   * Function responsible for formatting values of quick filter in a string when the model is modified
   * @param {any[]} values The new values passed to the quick filter model
   * @returns {string} The string to display in the text field
   * @default (values: string[]) => values.join(' ')
   */
  formatter: import_prop_types56.default.func,
  /**
   * Callback function that is called when the quick filter input is expanded or collapsed.
   * @param {boolean} expanded The new expanded state of the quick filter control
   */
  onExpandedChange: import_prop_types56.default.func,
  /**
   * Function responsible for parsing text input in an array of independent values for quick filtering.
   * @param {string} input The value entered by the user
   * @returns {any[]} The array of value on which quick filter is applied
   * @default (searchText: string) => searchText.split(' ').filter((word) => word !== '')
   */
  parser: import_prop_types56.default.func,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types56.default.oneOfType([import_prop_types56.default.element, import_prop_types56.default.func])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilterControl.js
var React113 = __toESM(require_react(), 1);
var import_prop_types57 = __toESM(require_prop_types(), 1);
var import_jsx_runtime84 = __toESM(require_jsx_runtime(), 1);
var _excluded46 = ["render", "className", "slotProps", "onKeyDown", "onChange"];
var QuickFilterControl = forwardRef2(function QuickFilterControl2(props, ref) {
  var _a;
  const {
    render,
    className,
    slotProps,
    onKeyDown,
    onChange
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded46);
  const rootProps = useGridRootProps();
  const {
    state,
    controlId,
    controlRef,
    onValueChange,
    onExpandedChange,
    clearValue
  } = useQuickFilterContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleRef = useForkRef(controlRef, ref);
  const handleKeyDown = (event) => {
    if (event.key === "Escape") {
      if (state.value === "") {
        onExpandedChange(false);
      } else {
        clearValue();
      }
    }
    onKeyDown == null ? void 0 : onKeyDown(event);
  };
  const handleBlur = (event) => {
    var _a2, _b;
    if (state.value === "") {
      onExpandedChange(false);
    }
    (_b = (_a2 = slotProps == null ? void 0 : slotProps.htmlInput) == null ? void 0 : _a2.onBlur) == null ? void 0 : _b.call(_a2, event);
  };
  const handleChange = (event) => {
    if (!state.expanded) {
      onExpandedChange(true);
    }
    onValueChange(event);
    onChange == null ? void 0 : onChange(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseTextField, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, {
    slotProps: _extends({
      htmlInput: _extends({
        role: "searchbox",
        id: controlId,
        tabIndex: state.expanded ? void 0 : -1
      }, slotProps == null ? void 0 : slotProps.htmlInput, {
        onBlur: handleBlur
      })
    }, slotProps),
    value: state.value,
    className: resolvedClassName
  }, other, {
    onChange: handleChange,
    onKeyDown: handleKeyDown,
    ref: handleRef
  }), state);
  return (0, import_jsx_runtime84.jsx)(React113.Fragment, {
    children: element
  });
});
if (true) QuickFilterControl.displayName = "QuickFilterControl";
true ? QuickFilterControl.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  autoComplete: import_prop_types57.default.string,
  autoFocus: import_prop_types57.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types57.default.oneOfType([import_prop_types57.default.func, import_prop_types57.default.string]),
  color: import_prop_types57.default.oneOf(["error", "primary"]),
  disabled: import_prop_types57.default.bool,
  error: import_prop_types57.default.bool,
  fullWidth: import_prop_types57.default.bool,
  helperText: import_prop_types57.default.string,
  id: import_prop_types57.default.string,
  inputRef: import_prop_types57.default.oneOfType([import_prop_types57.default.func, import_prop_types57.default.shape({
    current: import_prop_types57.default.object
  })]),
  label: import_prop_types57.default.node,
  multiline: import_prop_types57.default.bool,
  placeholder: import_prop_types57.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types57.default.oneOfType([import_prop_types57.default.element, import_prop_types57.default.func]),
  role: import_prop_types57.default.string,
  size: import_prop_types57.default.oneOf(["medium", "small"]),
  slotProps: import_prop_types57.default.object,
  style: import_prop_types57.default.object,
  tabIndex: import_prop_types57.default.number,
  type: import_prop_types57.default.string,
  value: import_prop_types57.default.string
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilterClear.js
var React114 = __toESM(require_react(), 1);
var import_prop_types58 = __toESM(require_prop_types(), 1);
var import_jsx_runtime85 = __toESM(require_jsx_runtime(), 1);
var _excluded47 = ["render", "className", "onClick"];
var QuickFilterClear = forwardRef2(function QuickFilterClear2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded47);
  const rootProps = useGridRootProps();
  const {
    state,
    clearValue
  } = useQuickFilterContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleClick = (event) => {
    clearValue();
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseIconButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    className: resolvedClassName,
    tabIndex: -1
  }, other, {
    onClick: handleClick,
    ref
  }), state);
  return (0, import_jsx_runtime85.jsx)(React114.Fragment, {
    children: element
  });
});
if (true) QuickFilterClear.displayName = "QuickFilterClear";
true ? QuickFilterClear.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types58.default.oneOfType([import_prop_types58.default.func, import_prop_types58.default.string]),
  color: import_prop_types58.default.oneOf(["default", "inherit", "primary"]),
  disabled: import_prop_types58.default.bool,
  edge: import_prop_types58.default.oneOf(["end", "start", false]),
  id: import_prop_types58.default.string,
  label: import_prop_types58.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types58.default.oneOfType([import_prop_types58.default.element, import_prop_types58.default.func]),
  role: import_prop_types58.default.string,
  size: import_prop_types58.default.oneOf(["large", "medium", "small"]),
  style: import_prop_types58.default.object,
  tabIndex: import_prop_types58.default.number,
  title: import_prop_types58.default.string,
  touchRippleRef: import_prop_types58.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/quickFilter/QuickFilterTrigger.js
var React115 = __toESM(require_react(), 1);
var import_prop_types59 = __toESM(require_prop_types(), 1);
var import_jsx_runtime86 = __toESM(require_jsx_runtime(), 1);
var _excluded48 = ["render", "className", "onClick"];
var QuickFilterTrigger = forwardRef2(function QuickFilterTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded48);
  const rootProps = useGridRootProps();
  const {
    state,
    controlId,
    onExpandedChange,
    triggerRef
  } = useQuickFilterContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleRef = useForkRef(triggerRef, ref);
  const handleClick = (event) => {
    onExpandedChange(!state.expanded);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    className: resolvedClassName,
    "aria-controls": controlId,
    "aria-expanded": state.expanded
  }, other, {
    onClick: handleClick,
    ref: handleRef
  }), state);
  return (0, import_jsx_runtime86.jsx)(React115.Fragment, {
    children: element
  });
});
if (true) QuickFilterTrigger.displayName = "QuickFilterTrigger";
true ? QuickFilterTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types59.default.oneOfType([import_prop_types59.default.func, import_prop_types59.default.string]),
  disabled: import_prop_types59.default.bool,
  id: import_prop_types59.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types59.default.oneOfType([import_prop_types59.default.element, import_prop_types59.default.func]),
  role: import_prop_types59.default.string,
  size: import_prop_types59.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types59.default.node,
  style: import_prop_types59.default.object,
  tabIndex: import_prop_types59.default.number,
  title: import_prop_types59.default.string,
  touchRippleRef: import_prop_types59.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbarQuickFilter.js
var import_jsx_runtime87 = __toESM(require_jsx_runtime(), 1);
var _excluded49 = ["quickFilterParser", "quickFilterFormatter", "debounceMs", "className", "slotProps"];
var _excluded212 = ["ref", "slotProps"];
var useUtilityClasses42 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["toolbarQuickFilter"],
    trigger: ["toolbarQuickFilterTrigger"],
    control: ["toolbarQuickFilterControl"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridQuickFilterRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ToolbarQuickFilter"
})({
  display: "grid",
  alignItems: "center"
});
var GridQuickFilterTrigger = styled_default2(ToolbarButton, {
  name: "MuiDataGrid",
  slot: "ToolbarQuickFilterTrigger"
})(({
  ownerState
}) => ({
  gridArea: "1 / 1",
  width: "min-content",
  height: "min-content",
  zIndex: 1,
  opacity: ownerState.expanded ? 0 : 1,
  pointerEvents: ownerState.expanded ? "none" : "auto",
  transition: vars.transition(["opacity"])
}));
var GridQuickFilterTextField = styled_default2((_props) => {
  throw new Error("Failed assertion: should not be rendered");
}, {
  name: "MuiDataGrid",
  slot: "ToolbarQuickFilterControl"
})(({
  ownerState
}) => ({
  gridArea: "1 / 1",
  overflowX: "clip",
  width: ownerState.expanded ? 260 : "var(--trigger-width)",
  opacity: ownerState.expanded ? 1 : 0,
  transition: vars.transition(["width", "opacity"])
}));
function GridToolbarQuickFilter(props) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const ownerState = {
    classes: rootProps.classes,
    expanded: false
  };
  const classes2 = useUtilityClasses42(ownerState);
  const {
    quickFilterParser,
    quickFilterFormatter,
    debounceMs,
    className,
    slotProps
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded49);
  return (0, import_jsx_runtime87.jsx)(QuickFilter, {
    parser: quickFilterParser,
    formatter: quickFilterFormatter,
    debounceMs,
    render: (quickFilterProps, state) => {
      const currentOwnerState = _extends({}, ownerState, {
        expanded: state.expanded
      });
      return (0, import_jsx_runtime87.jsxs)(GridQuickFilterRoot, _extends({}, quickFilterProps, {
        className: clsx_default(classes2.root, className),
        children: [(0, import_jsx_runtime87.jsx)(QuickFilterTrigger, {
          render: (triggerProps) => (0, import_jsx_runtime87.jsx)(rootProps.slots.baseTooltip, {
            title: apiRef.current.getLocaleText("toolbarQuickFilterLabel"),
            enterDelay: 0,
            children: (0, import_jsx_runtime87.jsx)(GridQuickFilterTrigger, _extends({
              className: classes2.trigger
            }, triggerProps, {
              ownerState: currentOwnerState,
              color: "default",
              "aria-disabled": state.expanded,
              children: (0, import_jsx_runtime87.jsx)(rootProps.slots.quickFilterIcon, {
                fontSize: "small"
              })
            }))
          })
        }), (0, import_jsx_runtime87.jsx)(QuickFilterControl, {
          render: (_ref) => {
            var _a;
            let {
              ref,
              slotProps: controlSlotProps
            } = _ref, controlProps = _objectWithoutPropertiesLoose(_ref, _excluded212);
            return (0, import_jsx_runtime87.jsx)(GridQuickFilterTextField, _extends({
              as: rootProps.slots.baseTextField,
              className: classes2.control,
              ownerState: currentOwnerState,
              inputRef: ref,
              "aria-label": apiRef.current.getLocaleText("toolbarQuickFilterLabel"),
              placeholder: apiRef.current.getLocaleText("toolbarQuickFilterPlaceholder"),
              size: "small",
              slotProps: _extends({
                input: _extends({
                  startAdornment: (0, import_jsx_runtime87.jsx)(rootProps.slots.quickFilterIcon, {
                    fontSize: "small"
                  }),
                  endAdornment: controlProps.value ? (0, import_jsx_runtime87.jsx)(QuickFilterClear, {
                    render: (0, import_jsx_runtime87.jsx)(rootProps.slots.baseIconButton, {
                      size: "small",
                      edge: "end",
                      "aria-label": apiRef.current.getLocaleText("toolbarQuickFilterDeleteIconLabel"),
                      children: (0, import_jsx_runtime87.jsx)(rootProps.slots.quickFilterClearIcon, {
                        fontSize: "small"
                      })
                    })
                  }) : null
                }, controlSlotProps == null ? void 0 : controlSlotProps.input)
              }, controlSlotProps)
            }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, controlProps, slotProps == null ? void 0 : slotProps.root, other));
          }
        })]
      }));
    }
  });
}
true ? GridToolbarQuickFilter.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types60.default.string,
  /**
   * The debounce time in milliseconds.
   * @default 150
   */
  debounceMs: import_prop_types60.default.number,
  /**
   * Function responsible for formatting values of quick filter in a string when the model is modified
   * @param {any[]} values The new values passed to the quick filter model
   * @returns {string} The string to display in the text field
   * @default (values: string[]) => values.join(' ')
   */
  quickFilterFormatter: import_prop_types60.default.func,
  /**
   * Function responsible for parsing text input in an array of independent values for quick filtering.
   * @param {string} input The value entered by the user
   * @returns {any[]} The array of value on which quick filter is applied
   * @default (searchText: string) => searchText
   *   .split(' ')
   *   .filter((word) => word !== '')
   */
  quickFilterParser: import_prop_types60.default.func,
  slotProps: import_prop_types60.default.object
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/GridToolbar.js
var React120 = __toESM(require_react(), 1);
var import_prop_types65 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/filterPanel/FilterPanelTrigger.js
var React116 = __toESM(require_react(), 1);
var import_prop_types61 = __toESM(require_prop_types(), 1);
var import_jsx_runtime88 = __toESM(require_jsx_runtime(), 1);
var _excluded50 = ["render", "className", "onClick", "onPointerUp"];
var FilterPanelTrigger = forwardRef2(function FilterPanelTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick,
    onPointerUp
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded50);
  const rootProps = useGridRootProps();
  const buttonId = useId();
  const panelId = useId();
  const apiRef = useGridApiContext();
  const panelState = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const open = panelState.open && panelState.openedPanelValue === GridPreferencePanelsValue.filters;
  const activeFilters = useGridSelector(apiRef, gridFilterActiveItemsSelector);
  const filterCount = activeFilters.length;
  const state = {
    open,
    filterCount
  };
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const {
    filterPanelTriggerRef
  } = useGridPanelContext();
  const handleRef = useForkRef(ref, filterPanelTriggerRef);
  const handleClick = (event) => {
    if (open) {
      apiRef.current.hidePreferences();
    } else {
      apiRef.current.showPreferences(GridPreferencePanelsValue.filters, panelId, buttonId);
    }
    onClick == null ? void 0 : onClick(event);
  };
  const handlePointerUp = (event) => {
    if (open) {
      event.stopPropagation();
    }
    onPointerUp == null ? void 0 : onPointerUp(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    id: buttonId,
    "aria-haspopup": "true",
    "aria-expanded": open ? "true" : void 0,
    "aria-controls": open ? panelId : void 0,
    onClick: handleClick,
    onPointerUp: handlePointerUp,
    className: resolvedClassName
  }, other, {
    ref: handleRef
  }), state);
  return (0, import_jsx_runtime88.jsx)(React116.Fragment, {
    children: element
  });
});
if (true) FilterPanelTrigger.displayName = "FilterPanelTrigger";
true ? FilterPanelTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * A function to customize rendering of the component.
   */
  className: import_prop_types61.default.oneOfType([import_prop_types61.default.func, import_prop_types61.default.string]),
  disabled: import_prop_types61.default.bool,
  id: import_prop_types61.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types61.default.oneOfType([import_prop_types61.default.element, import_prop_types61.default.func]),
  role: import_prop_types61.default.string,
  size: import_prop_types61.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types61.default.node,
  style: import_prop_types61.default.object,
  tabIndex: import_prop_types61.default.number,
  title: import_prop_types61.default.string,
  touchRippleRef: import_prop_types61.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/columnsPanel/ColumnsPanelTrigger.js
var React117 = __toESM(require_react(), 1);
var import_prop_types62 = __toESM(require_prop_types(), 1);
var import_jsx_runtime89 = __toESM(require_jsx_runtime(), 1);
var _excluded51 = ["render", "className", "onClick", "onPointerUp"];
var ColumnsPanelTrigger = forwardRef2(function ColumnsPanelTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick,
    onPointerUp
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded51);
  const rootProps = useGridRootProps();
  const buttonId = useId();
  const panelId = useId();
  const apiRef = useGridApiContext();
  const panelState = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const open = panelState.open && panelState.openedPanelValue === GridPreferencePanelsValue.columns;
  const state = {
    open
  };
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const {
    columnsPanelTriggerRef
  } = useGridPanelContext();
  const handleRef = useForkRef(ref, columnsPanelTriggerRef);
  const handleClick = (event) => {
    if (open) {
      apiRef.current.hidePreferences();
    } else {
      apiRef.current.showPreferences(GridPreferencePanelsValue.columns, panelId, buttonId);
    }
    onClick == null ? void 0 : onClick(event);
  };
  const handlePointerUp = (event) => {
    if (open) {
      event.stopPropagation();
    }
    onPointerUp == null ? void 0 : onPointerUp(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    id: buttonId,
    "aria-haspopup": "true",
    "aria-expanded": open ? "true" : void 0,
    "aria-controls": open ? panelId : void 0,
    className: resolvedClassName
  }, other, {
    onPointerUp: handlePointerUp,
    onClick: handleClick,
    ref: handleRef
  }), state);
  return (0, import_jsx_runtime89.jsx)(React117.Fragment, {
    children: element
  });
});
if (true) ColumnsPanelTrigger.displayName = "ColumnsPanelTrigger";
true ? ColumnsPanelTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types62.default.oneOfType([import_prop_types62.default.func, import_prop_types62.default.string]),
  disabled: import_prop_types62.default.bool,
  id: import_prop_types62.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types62.default.oneOfType([import_prop_types62.default.element, import_prop_types62.default.func]),
  role: import_prop_types62.default.string,
  size: import_prop_types62.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types62.default.node,
  style: import_prop_types62.default.object,
  tabIndex: import_prop_types62.default.number,
  title: import_prop_types62.default.string,
  touchRippleRef: import_prop_types62.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/export/ExportCsv.js
var React118 = __toESM(require_react(), 1);
var import_prop_types63 = __toESM(require_prop_types(), 1);
var import_jsx_runtime90 = __toESM(require_jsx_runtime(), 1);
var _excluded52 = ["render", "options", "onClick"];
var ExportCsv = forwardRef2(function ExportCsv2(props, ref) {
  var _a;
  const {
    render,
    options: options3,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded52);
  const rootProps = useGridRootProps();
  const apiRef = useGridApiContext();
  const handleClick = (event) => {
    apiRef.current.exportDataAsCsv(options3);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    onClick: handleClick
  }, other, {
    ref
  }));
  return (0, import_jsx_runtime90.jsx)(React118.Fragment, {
    children: element
  });
});
if (true) ExportCsv.displayName = "ExportCsv";
true ? ExportCsv.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types63.default.string,
  disabled: import_prop_types63.default.bool,
  id: import_prop_types63.default.string,
  /**
   * The options to apply on the CSV export.
   * @demos
   *   - [CSV export](/x/react-data-grid/export/#csv-export)
   */
  options: import_prop_types63.default.shape({
    allColumns: import_prop_types63.default.bool,
    delimiter: import_prop_types63.default.string,
    escapeFormulas: import_prop_types63.default.bool,
    fields: import_prop_types63.default.arrayOf(import_prop_types63.default.string),
    fileName: import_prop_types63.default.string,
    getRowsToExport: import_prop_types63.default.func,
    includeColumnGroupsHeaders: import_prop_types63.default.bool,
    includeHeaders: import_prop_types63.default.bool,
    shouldAppendQuotes: import_prop_types63.default.bool,
    utf8WithBom: import_prop_types63.default.bool
  }),
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types63.default.oneOfType([import_prop_types63.default.element, import_prop_types63.default.func]),
  role: import_prop_types63.default.string,
  size: import_prop_types63.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types63.default.node,
  style: import_prop_types63.default.object,
  tabIndex: import_prop_types63.default.number,
  title: import_prop_types63.default.string,
  touchRippleRef: import_prop_types63.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/export/ExportPrint.js
var React119 = __toESM(require_react(), 1);
var import_prop_types64 = __toESM(require_prop_types(), 1);
var import_jsx_runtime91 = __toESM(require_jsx_runtime(), 1);
var _excluded53 = ["render", "options", "onClick"];
var ExportPrint = forwardRef2(function ExportPrint2(props, ref) {
  var _a;
  const {
    render,
    options: options3,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded53);
  const rootProps = useGridRootProps();
  const apiRef = useGridApiContext();
  const handleClick = (event) => {
    apiRef.current.exportDataAsPrint(options3);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    onClick: handleClick
  }, other, {
    ref
  }));
  return (0, import_jsx_runtime91.jsx)(React119.Fragment, {
    children: element
  });
});
if (true) ExportPrint.displayName = "ExportPrint";
true ? ExportPrint.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types64.default.string,
  disabled: import_prop_types64.default.bool,
  id: import_prop_types64.default.string,
  /**
   * The options to apply on the Print export.
   * @demos
   *   - [Print export](/x/react-data-grid/export/#print-export)
   */
  options: import_prop_types64.default.shape({
    allColumns: import_prop_types64.default.bool,
    bodyClassName: import_prop_types64.default.string,
    copyStyles: import_prop_types64.default.bool,
    fields: import_prop_types64.default.arrayOf(import_prop_types64.default.string),
    fileName: import_prop_types64.default.string,
    getRowsToExport: import_prop_types64.default.func,
    hideFooter: import_prop_types64.default.bool,
    hideToolbar: import_prop_types64.default.bool,
    includeCheckboxes: import_prop_types64.default.bool,
    pageStyle: import_prop_types64.default.oneOfType([import_prop_types64.default.func, import_prop_types64.default.string])
  }),
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types64.default.oneOfType([import_prop_types64.default.element, import_prop_types64.default.func]),
  role: import_prop_types64.default.string,
  size: import_prop_types64.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types64.default.node,
  style: import_prop_types64.default.object,
  tabIndex: import_prop_types64.default.number,
  title: import_prop_types64.default.string,
  touchRippleRef: import_prop_types64.default.any
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbarV8/GridToolbar.js
var import_jsx_runtime92 = __toESM(require_jsx_runtime(), 1);
var _excluded54 = ["className"];
var _excluded213 = ["className"];
var _excluded311 = ["showQuickFilter", "quickFilterProps", "csvOptions", "printOptions", "mainControls", "additionalExportMenuItems"];
var useUtilityClasses43 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    divider: ["toolbarDivider"],
    label: ["toolbarLabel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var Divider = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "ToolbarDivider"
})({
  height: "50%",
  margin: vars.spacing(0, 0.5)
});
var Label = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "ToolbarLabel"
})({
  flex: 1,
  font: vars.typography.font.large,
  fontWeight: vars.typography.fontWeight.medium,
  margin: vars.spacing(0, 0.5),
  textOverflow: "ellipsis",
  overflow: "hidden",
  whiteSpace: "nowrap"
});
function GridToolbarDivider(props) {
  const other = _objectWithoutPropertiesLoose(props, _excluded54);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses43(rootProps);
  return (0, import_jsx_runtime92.jsx)(Divider, _extends({
    as: rootProps.slots.baseDivider,
    orientation: "vertical",
    className: classes2.divider
  }, other));
}
true ? GridToolbarDivider.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types65.default.string,
  orientation: import_prop_types65.default.oneOf(["horizontal", "vertical"])
} : void 0;
function GridToolbarLabel(props) {
  const other = _objectWithoutPropertiesLoose(props, _excluded213);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses43(rootProps);
  return (0, import_jsx_runtime92.jsx)(Label, _extends({
    className: classes2.label
  }, other));
}
function GridToolbar(props) {
  var _a, _b, _c;
  const {
    showQuickFilter = true,
    quickFilterProps,
    csvOptions,
    printOptions,
    mainControls,
    additionalExportMenuItems
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded311);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const [exportMenuOpen, setExportMenuOpen] = React120.useState(false);
  const exportMenuTriggerRef = React120.useRef(null);
  const exportMenuId = useId();
  const exportMenuTriggerId = useId();
  const showExportMenu = !(csvOptions == null ? void 0 : csvOptions.disableToolbarButton) || !(printOptions == null ? void 0 : printOptions.disableToolbarButton) || additionalExportMenuItems;
  const closeExportMenu = () => setExportMenuOpen(false);
  return (0, import_jsx_runtime92.jsxs)(Toolbar, _extends({}, other, {
    children: [rootProps.label && (0, import_jsx_runtime92.jsx)(GridToolbarLabel, {
      children: rootProps.label
    }), mainControls || (0, import_jsx_runtime92.jsxs)(React120.Fragment, {
      children: [!rootProps.disableColumnSelector && (0, import_jsx_runtime92.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarColumns"),
        children: (0, import_jsx_runtime92.jsx)(ColumnsPanelTrigger, {
          render: (0, import_jsx_runtime92.jsx)(ToolbarButton, {}),
          children: (0, import_jsx_runtime92.jsx)(rootProps.slots.columnSelectorIcon, {
            fontSize: "small"
          })
        })
      }), !rootProps.disableColumnFilter && (0, import_jsx_runtime92.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarFilters"),
        children: (0, import_jsx_runtime92.jsx)(FilterPanelTrigger, {
          render: (triggerProps, state) => (0, import_jsx_runtime92.jsx)(ToolbarButton, _extends({}, triggerProps, {
            color: state.filterCount > 0 ? "primary" : "default",
            children: (0, import_jsx_runtime92.jsx)(rootProps.slots.baseBadge, {
              badgeContent: state.filterCount,
              color: "primary",
              variant: "dot",
              children: (0, import_jsx_runtime92.jsx)(rootProps.slots.openFilterButtonIcon, {
                fontSize: "small"
              })
            })
          }))
        })
      })]
    }), showExportMenu && (!rootProps.disableColumnFilter || !rootProps.disableColumnSelector) && (0, import_jsx_runtime92.jsx)(GridToolbarDivider, {}), showExportMenu && (0, import_jsx_runtime92.jsxs)(React120.Fragment, {
      children: [(0, import_jsx_runtime92.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarExport"),
        disableInteractive: exportMenuOpen,
        children: (0, import_jsx_runtime92.jsx)(ToolbarButton, {
          ref: exportMenuTriggerRef,
          id: exportMenuTriggerId,
          "aria-controls": exportMenuId,
          "aria-haspopup": "true",
          "aria-expanded": exportMenuOpen ? "true" : void 0,
          onClick: () => setExportMenuOpen(!exportMenuOpen),
          children: (0, import_jsx_runtime92.jsx)(rootProps.slots.exportIcon, {
            fontSize: "small"
          })
        })
      }), (0, import_jsx_runtime92.jsx)(GridMenu, {
        target: exportMenuTriggerRef.current,
        open: exportMenuOpen,
        onClose: closeExportMenu,
        position: "bottom-end",
        children: (0, import_jsx_runtime92.jsxs)(rootProps.slots.baseMenuList, _extends({
          id: exportMenuId,
          "aria-labelledby": exportMenuTriggerId,
          autoFocusItem: true
        }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseMenuList, {
          children: [!(printOptions == null ? void 0 : printOptions.disableToolbarButton) && (0, import_jsx_runtime92.jsx)(ExportPrint, {
            render: (0, import_jsx_runtime92.jsx)(rootProps.slots.baseMenuItem, _extends({}, (_b = rootProps.slotProps) == null ? void 0 : _b.baseMenuItem)),
            options: printOptions,
            onClick: closeExportMenu,
            children: apiRef.current.getLocaleText("toolbarExportPrint")
          }), !(csvOptions == null ? void 0 : csvOptions.disableToolbarButton) && (0, import_jsx_runtime92.jsx)(ExportCsv, {
            render: (0, import_jsx_runtime92.jsx)(rootProps.slots.baseMenuItem, _extends({}, (_c = rootProps.slotProps) == null ? void 0 : _c.baseMenuItem)),
            options: csvOptions,
            onClick: closeExportMenu,
            children: apiRef.current.getLocaleText("toolbarExportCSV")
          }), additionalExportMenuItems == null ? void 0 : additionalExportMenuItems(closeExportMenu)]
        }))
      })]
    }), showQuickFilter && (0, import_jsx_runtime92.jsxs)(React120.Fragment, {
      children: [(0, import_jsx_runtime92.jsx)(GridToolbarDivider, {}), (0, import_jsx_runtime92.jsx)(GridToolbarQuickFilter, _extends({}, quickFilterProps))]
    })]
  }));
}
true ? GridToolbar.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  additionalExportMenuItems: import_prop_types65.default.func,
  additionalItems: import_prop_types65.default.node,
  csvOptions: import_prop_types65.default.object,
  printOptions: import_prop_types65.default.object,
  /**
   * Props passed to the quick filter component.
   */
  quickFilterProps: import_prop_types65.default.shape({
    className: import_prop_types65.default.string,
    debounceMs: import_prop_types65.default.number,
    quickFilterFormatter: import_prop_types65.default.func,
    quickFilterParser: import_prop_types65.default.func,
    slotProps: import_prop_types65.default.object
  }),
  /**
   * Show the quick filter component.
   * @default true
   */
  showQuickFilter: import_prop_types65.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types65.default.object,
  sx: import_prop_types65.default.oneOfType([import_prop_types65.default.arrayOf(import_prop_types65.default.oneOfType([import_prop_types65.default.func, import_prop_types65.default.object, import_prop_types65.default.bool])), import_prop_types65.default.func, import_prop_types65.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/toolbar/GridToolbar.js
var import_jsx_runtime93 = __toESM(require_jsx_runtime(), 1);
var _excluded55 = ["className", "csvOptions", "printOptions", "excelOptions", "showQuickFilter", "quickFilterProps"];
var GridToolbar2 = forwardRef2(function GridToolbar3(props, ref) {
  const _ref = props, {
    csvOptions,
    printOptions,
    excelOptions,
    showQuickFilter = true,
    quickFilterProps = {}
  } = _ref, other = _objectWithoutPropertiesLoose(_ref, _excluded55);
  const rootProps = useGridRootProps();
  if (rootProps.disableColumnFilter && rootProps.disableColumnSelector && rootProps.disableDensitySelector && !showQuickFilter) {
    return null;
  }
  return (0, import_jsx_runtime93.jsxs)(GridToolbarContainer, _extends({}, other, {
    ref,
    children: [rootProps.label && (0, import_jsx_runtime93.jsx)(GridToolbarLabel, {
      children: rootProps.label
    }), (0, import_jsx_runtime93.jsx)(GridToolbarColumnsButton, {}), (0, import_jsx_runtime93.jsx)(GridToolbarFilterButton, {}), (0, import_jsx_runtime93.jsx)(GridToolbarDensitySelector, {}), (0, import_jsx_runtime93.jsx)(GridToolbarExport, {
      csvOptions,
      printOptions,
      excelOptions
    }), (0, import_jsx_runtime93.jsx)("div", {
      style: {
        flex: 1
      }
    }), showQuickFilter && (0, import_jsx_runtime93.jsx)(GridToolbarQuickFilter, _extends({}, quickFilterProps))]
  }));
});
if (true) GridToolbar2.displayName = "GridToolbar";
true ? GridToolbar2.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  csvOptions: import_prop_types66.default.object,
  printOptions: import_prop_types66.default.object,
  /**
   * Props passed to the quick filter component.
   */
  quickFilterProps: import_prop_types66.default.shape({
    className: import_prop_types66.default.string,
    debounceMs: import_prop_types66.default.number,
    quickFilterFormatter: import_prop_types66.default.func,
    quickFilterParser: import_prop_types66.default.func,
    slotProps: import_prop_types66.default.object
  }),
  /**
   * Show the history controls (undo/redo buttons).
   * @default true
   */
  showHistoryControls: import_prop_types66.default.bool,
  /**
   * Show the quick filter component.
   * @default true
   */
  showQuickFilter: import_prop_types66.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types66.default.object,
  sx: import_prop_types66.default.oneOfType([import_prop_types66.default.arrayOf(import_prop_types66.default.oneOfType([import_prop_types66.default.func, import_prop_types66.default.object, import_prop_types66.default.bool])), import_prop_types66.default.func, import_prop_types66.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridFooter.js
var import_prop_types68 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/GridSelectedRowCount.js
var React121 = __toESM(require_react(), 1);
var import_prop_types67 = __toESM(require_prop_types(), 1);
var import_jsx_runtime94 = __toESM(require_jsx_runtime(), 1);
var _excluded56 = ["className", "selectedRowCount"];
var useUtilityClasses44 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["selectedRowCount"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridSelectedRowCountRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "SelectedRowCount"
})({
  alignItems: "center",
  display: "flex",
  margin: vars.spacing(0, 2),
  visibility: "hidden",
  width: 0,
  height: 0,
  [vars.breakpoints.up("sm")]: {
    visibility: "visible",
    width: "auto",
    height: "auto"
  }
});
var GridSelectedRowCount = forwardRef2(function GridSelectedRowCount2(props, ref) {
  const {
    className,
    selectedRowCount
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded56);
  const apiRef = useGridApiContext();
  const ownerState = useGridRootProps();
  const classes2 = useUtilityClasses44(ownerState);
  const rowSelectedText = apiRef.current.getLocaleText("footerRowSelected")(selectedRowCount);
  return (0, import_jsx_runtime94.jsx)(GridSelectedRowCountRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState
  }, other, {
    ref,
    children: rowSelectedText
  }));
});
if (true) GridSelectedRowCount.displayName = "GridSelectedRowCount";
true ? GridSelectedRowCount.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  selectedRowCount: import_prop_types67.default.number.isRequired,
  sx: import_prop_types67.default.oneOfType([import_prop_types67.default.arrayOf(import_prop_types67.default.oneOfType([import_prop_types67.default.func, import_prop_types67.default.object, import_prop_types67.default.bool])), import_prop_types67.default.func, import_prop_types67.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridFooter.js
var import_jsx_runtime95 = __toESM(require_jsx_runtime(), 1);
var GridFooter = forwardRef2(function GridFooter2(props, ref) {
  var _a, _b;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const totalTopLevelRowCount = useGridSelector(apiRef, gridTopLevelRowCountSelector);
  const selectedRowCount = useGridSelector(apiRef, gridRowSelectionCountSelector);
  const visibleTopLevelRowCount = useGridSelector(apiRef, gridFilteredTopLevelRowCountSelector);
  const selectedRowCountElement = !rootProps.hideFooterSelectedRowCount && selectedRowCount > 0 ? (0, import_jsx_runtime95.jsx)(GridSelectedRowCount, {
    selectedRowCount
  }) : (0, import_jsx_runtime95.jsx)("div", {});
  const rowCountElement = !rootProps.hideFooterRowCount && !rootProps.pagination ? (0, import_jsx_runtime95.jsx)(rootProps.slots.footerRowCount, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.footerRowCount, {
    rowCount: totalTopLevelRowCount,
    visibleRowCount: visibleTopLevelRowCount
  })) : null;
  const paginationElement = rootProps.pagination && !rootProps.hideFooterPagination && rootProps.slots.pagination && (0, import_jsx_runtime95.jsx)(rootProps.slots.pagination, _extends({}, (_b = rootProps.slotProps) == null ? void 0 : _b.pagination));
  return (0, import_jsx_runtime95.jsxs)(GridFooterContainer, _extends({}, props, {
    ref,
    children: [selectedRowCountElement, rowCountElement, paginationElement]
  }));
});
if (true) GridFooter.displayName = "GridFooter";
true ? GridFooter.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types68.default.oneOfType([import_prop_types68.default.arrayOf(import_prop_types68.default.oneOfType([import_prop_types68.default.func, import_prop_types68.default.object, import_prop_types68.default.bool])), import_prop_types68.default.func, import_prop_types68.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridLoadingOverlay.js
var React123 = __toESM(require_react(), 1);
var import_prop_types69 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/components/GridSkeletonLoadingOverlay.js
var React122 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/internals/utils/getPinnedCellOffset.js
var getPinnedCellOffset = (pinnedPosition, computedWidth, columnIndex, columnPositions, columnsTotalWidth, scrollbarWidth) => {
  let pinnedOffset;
  switch (pinnedPosition) {
    case PinnedColumnPosition.LEFT:
      pinnedOffset = columnPositions[columnIndex];
      break;
    case PinnedColumnPosition.RIGHT:
      pinnedOffset = columnsTotalWidth - columnPositions[columnIndex] - computedWidth + scrollbarWidth;
      break;
    default:
      pinnedOffset = void 0;
      break;
  }
  if (Number.isNaN(pinnedOffset)) {
    pinnedOffset = void 0;
  }
  return pinnedOffset;
};

// node_modules/@mui/x-data-grid/esm/utils/cellBorderUtils.js
var shouldCellShowRightBorder = (pinnedPosition, indexInSection, sectionLength, showCellVerticalBorderRootProp, gridHasFiller, pinnedColumnsSectionSeparatorRootProp) => {
  const isSectionLastCell = indexInSection === sectionLength - 1;
  if ((pinnedColumnsSectionSeparatorRootProp == null ? void 0 : pinnedColumnsSectionSeparatorRootProp.startsWith("border")) && pinnedPosition === PinnedColumnPosition.LEFT && isSectionLastCell) {
    return true;
  }
  if (showCellVerticalBorderRootProp) {
    if (pinnedPosition === PinnedColumnPosition.LEFT) {
      return true;
    }
    if (pinnedPosition === PinnedColumnPosition.RIGHT) {
      return !isSectionLastCell;
    }
    return !isSectionLastCell || gridHasFiller;
  }
  return false;
};
var shouldCellShowLeftBorder = (pinnedPosition, indexInSection, showCellVerticalBorderRootProp, pinnedColumnsSectionSeparatorRootProp) => {
  if (pinnedColumnsSectionSeparatorRootProp == null ? void 0 : pinnedColumnsSectionSeparatorRootProp.startsWith("border")) {
    return pinnedPosition === PinnedColumnPosition.RIGHT && indexInSection === 0;
  }
  return showCellVerticalBorderRootProp && pinnedPosition === PinnedColumnPosition.RIGHT && indexInSection === 0;
};

// node_modules/@mui/x-data-grid/esm/components/GridSkeletonLoadingOverlay.js
var import_jsx_runtime96 = __toESM(require_jsx_runtime(), 1);
var _excluded57 = ["skeletonRowsCount", "visibleColumns", "showFirstRowBorder"];
var SkeletonOverlay = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "SkeletonLoadingOverlay"
})({
  minWidth: "100%",
  width: "max-content",
  // prevents overflow: clip; cutting off the x axis
  height: "100%",
  overflow: "clip"
  // y axis is hidden while the x axis is allowed to overflow
});
var useUtilityClasses45 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["skeletonLoadingOverlay"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var getColIndex = (el) => parseInt(el.getAttribute("data-colindex"), 10);
var GridSkeletonLoadingOverlayInner = forwardRef2(function GridSkeletonLoadingOverlayInner2(props, forwardedRef) {
  const rootProps = useGridRootProps();
  const {
    slots
  } = rootProps;
  const isRtl = useRtl();
  const classes2 = useUtilityClasses45({
    classes: rootProps.classes
  });
  const ref = React122.useRef(null);
  const handleRef = useForkRef(ref, forwardedRef);
  const apiRef = useGridApiContext();
  const dimensions = useGridSelector(apiRef, gridDimensionsSelector);
  const totalWidth = useGridSelector(apiRef, gridColumnsTotalWidthSelector);
  const positions = useGridSelector(apiRef, gridColumnPositionsSelector);
  const inViewportCount = React122.useMemo(() => positions.filter((value) => value <= totalWidth).length, [totalWidth, positions]);
  const {
    skeletonRowsCount,
    visibleColumns,
    showFirstRowBorder
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded57);
  const allVisibleColumns = useGridSelector(apiRef, gridVisibleColumnDefinitionsSelector);
  const columns = React122.useMemo(() => allVisibleColumns.slice(0, inViewportCount), [allVisibleColumns, inViewportCount]);
  const pinnedColumns = useGridSelector(apiRef, gridVisiblePinnedColumnDefinitionsSelector);
  const getPinnedPosition = React122.useCallback((field) => {
    if (pinnedColumns.left.findIndex((col) => col.field === field) !== -1) {
      return PinnedColumnPosition.LEFT;
    }
    if (pinnedColumns.right.findIndex((col) => col.field === field) !== -1) {
      return PinnedColumnPosition.RIGHT;
    }
    return void 0;
  }, [pinnedColumns.left, pinnedColumns.right]);
  const children2 = React122.useMemo(() => {
    const array = [];
    for (let i2 = 0; i2 < skeletonRowsCount; i2 += 1) {
      const rowCells = [];
      for (let colIndex = 0; colIndex < columns.length; colIndex += 1) {
        const column = columns[colIndex];
        const pinnedPosition = getPinnedPosition(column.field);
        const isPinnedLeft = pinnedPosition === PinnedColumnPosition.LEFT;
        const isPinnedRight = pinnedPosition === PinnedColumnPosition.RIGHT;
        const pinnedSide = rtlFlipSide(pinnedPosition, isRtl);
        const sectionLength = pinnedSide ? pinnedColumns[pinnedSide].length : columns.length - pinnedColumns.left.length - pinnedColumns.right.length;
        const sectionIndex = pinnedSide ? pinnedColumns[pinnedSide].findIndex((col) => col.field === column.field) : colIndex - pinnedColumns.left.length;
        const scrollbarWidth = dimensions.hasScrollY ? dimensions.scrollbarSize : 0;
        const pinnedStyle = attachPinnedStyle({}, isRtl, pinnedPosition, getPinnedCellOffset(pinnedPosition, column.computedWidth, colIndex, positions, dimensions.columnsTotalWidth, scrollbarWidth));
        const gridHasFiller = dimensions.columnsTotalWidth < dimensions.viewportOuterSize.width;
        const showRightBorder = shouldCellShowRightBorder(pinnedPosition, sectionIndex, sectionLength, rootProps.showCellVerticalBorder, gridHasFiller, rootProps.pinnedColumnsSectionSeparator);
        const showLeftBorder = shouldCellShowLeftBorder(pinnedPosition, sectionIndex, rootProps.showCellVerticalBorder, rootProps.pinnedColumnsSectionSeparator);
        const isLastColumn = colIndex === columns.length - 1;
        const isFirstPinnedRight = isPinnedRight && sectionIndex === 0;
        const hasFillerBefore = isFirstPinnedRight && gridHasFiller;
        const hasFillerAfter = isLastColumn && !isFirstPinnedRight && gridHasFiller;
        const expandedWidth = dimensions.viewportOuterSize.width - dimensions.columnsTotalWidth;
        const emptyCellWidth = Math.max(0, expandedWidth);
        const emptyCell = (0, import_jsx_runtime96.jsx)(slots.skeletonCell, {
          width: emptyCellWidth,
          empty: true
        }, `skeleton-filler-column-${i2}`);
        if (hasFillerBefore) {
          rowCells.push(emptyCell);
        }
        rowCells.push((0, import_jsx_runtime96.jsx)(slots.skeletonCell, {
          field: column.field,
          type: column.type,
          align: column.align,
          width: "var(--width)",
          height: dimensions.rowHeight,
          "data-colindex": colIndex,
          empty: visibleColumns && !visibleColumns.has(column.field),
          className: clsx_default(isPinnedLeft && gridClasses["cell--pinnedLeft"], isPinnedRight && gridClasses["cell--pinnedRight"], showRightBorder && gridClasses["cell--withRightBorder"], showLeftBorder && gridClasses["cell--withLeftBorder"]),
          style: _extends({
            "--width": `${column.computedWidth}px`
          }, pinnedStyle)
        }, `skeleton-column-${i2}-${column.field}`));
        if (hasFillerAfter) {
          rowCells.push(emptyCell);
        }
      }
      array.push((0, import_jsx_runtime96.jsx)("div", {
        className: clsx_default(gridClasses.row, gridClasses.rowSkeleton, i2 === 0 && !showFirstRowBorder && gridClasses["row--firstVisible"]),
        children: rowCells
      }, `skeleton-row-${i2}`));
    }
    return array;
  }, [skeletonRowsCount, columns, getPinnedPosition, isRtl, pinnedColumns, dimensions.hasScrollY, dimensions.scrollbarSize, dimensions.columnsTotalWidth, dimensions.viewportOuterSize.width, dimensions.rowHeight, positions, rootProps.showCellVerticalBorder, rootProps.pinnedColumnsSectionSeparator, slots, visibleColumns, showFirstRowBorder]);
  const handleColumnResize = (params) => {
    var _a, _b, _c;
    const {
      colDef,
      width
    } = params;
    const cells = (_a = ref.current) == null ? void 0 : _a.querySelectorAll(`[data-field="${escapeOperandAttributeSelector(colDef.field)}"]`);
    if (!cells) {
      throw new Error("MUI X: Expected skeleton cells to be defined with `data-field` attribute.");
    }
    const resizedColIndex = columns.findIndex((col) => col.field === colDef.field);
    const pinnedPosition = getPinnedPosition(colDef.field);
    const isPinnedLeft = pinnedPosition === PinnedColumnPosition.LEFT;
    const isPinnedRight = pinnedPosition === PinnedColumnPosition.RIGHT;
    const currentWidth = getComputedStyle(cells[0]).getPropertyValue("--width");
    const delta = parseInt(currentWidth, 10) - width;
    if (cells) {
      cells.forEach((element) => {
        element.style.setProperty("--width", `${width}px`);
      });
    }
    if (isPinnedLeft) {
      const pinnedCells = (_b = ref.current) == null ? void 0 : _b.querySelectorAll(`.${gridClasses["cell--pinnedLeft"]}`);
      pinnedCells == null ? void 0 : pinnedCells.forEach((element) => {
        const colIndex = getColIndex(element);
        if (colIndex > resizedColIndex) {
          element.style.left = `${parseInt(getComputedStyle(element).left, 10) - delta}px`;
        }
      });
    }
    if (isPinnedRight) {
      const pinnedCells = (_c = ref.current) == null ? void 0 : _c.querySelectorAll(`.${gridClasses["cell--pinnedRight"]}`);
      pinnedCells == null ? void 0 : pinnedCells.forEach((element) => {
        const colIndex = getColIndex(element);
        if (colIndex < resizedColIndex) {
          element.style.right = `${parseInt(getComputedStyle(element).right, 10) + delta}px`;
        }
      });
    }
  };
  useGridEvent(apiRef, "columnResize", handleColumnResize);
  return (0, import_jsx_runtime96.jsx)(SkeletonOverlay, _extends({
    className: classes2.root
  }, other, {
    ref: handleRef,
    children: children2
  }));
});
if (true) GridSkeletonLoadingOverlayInner.displayName = "GridSkeletonLoadingOverlayInner";
var GridSkeletonLoadingOverlay = forwardRef2(function GridSkeletonLoadingOverlay2(props, forwardedRef) {
  const apiRef = useGridApiContext();
  const dimensions = useGridSelector(apiRef, gridDimensionsSelector);
  const viewportHeight = (dimensions == null ? void 0 : dimensions.viewportInnerSize.height) ?? 0;
  const skeletonRowsCount = Math.ceil(viewportHeight / dimensions.rowHeight);
  return (0, import_jsx_runtime96.jsx)(GridSkeletonLoadingOverlayInner, _extends({}, props, {
    skeletonRowsCount,
    ref: forwardedRef
  }));
});
if (true) GridSkeletonLoadingOverlay.displayName = "GridSkeletonLoadingOverlay";

// node_modules/@mui/x-data-grid/esm/components/GridLoadingOverlay.js
var import_jsx_runtime97 = __toESM(require_jsx_runtime(), 1);
var _excluded58 = ["variant", "noRowsVariant", "style"];
var LOADING_VARIANTS = {
  "circular-progress": {
    component: (rootProps) => rootProps.slots.baseCircularProgress,
    style: {}
  },
  "linear-progress": {
    component: (rootProps) => rootProps.slots.baseLinearProgress,
    style: {
      display: "block"
    }
  },
  skeleton: {
    component: () => GridSkeletonLoadingOverlay,
    style: {
      display: "block"
    }
  }
};
var GridLoadingOverlay = forwardRef2(function GridLoadingOverlay2(props, ref) {
  const {
    variant = "linear-progress",
    noRowsVariant = "skeleton",
    style
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded58);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const rowsCount = useGridSelector(apiRef, gridRowCountSelector);
  const activeVariant = LOADING_VARIANTS[rowsCount === 0 ? noRowsVariant : variant];
  const Component = activeVariant.component(rootProps);
  return (0, import_jsx_runtime97.jsx)(GridOverlay, _extends({
    style: _extends({}, activeVariant.style, style)
  }, other, {
    ref,
    children: (0, import_jsx_runtime97.jsx)(Component, {})
  }));
});
if (true) GridLoadingOverlay.displayName = "GridLoadingOverlay";
true ? GridLoadingOverlay.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The variant of the overlay when no rows are displayed.
   * @default 'skeleton'
   */
  noRowsVariant: import_prop_types69.default.oneOf(["circular-progress", "linear-progress", "skeleton"]),
  sx: import_prop_types69.default.oneOfType([import_prop_types69.default.arrayOf(import_prop_types69.default.oneOfType([import_prop_types69.default.func, import_prop_types69.default.object, import_prop_types69.default.bool])), import_prop_types69.default.func, import_prop_types69.default.object]),
  /**
   * The variant of the overlay.
   * @default 'linear-progress'
   */
  variant: import_prop_types69.default.oneOf(["circular-progress", "linear-progress", "skeleton"])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridNoRowsOverlay.js
var import_prop_types70 = __toESM(require_prop_types(), 1);
var import_jsx_runtime98 = __toESM(require_jsx_runtime(), 1);
var GridNoRowsOverlay = forwardRef2(function GridNoRowsOverlay2(props, ref) {
  const apiRef = useGridApiContext();
  const noRowsLabel = apiRef.current.getLocaleText("noRowsLabel");
  return (0, import_jsx_runtime98.jsx)(GridOverlay, _extends({}, props, {
    ref,
    children: noRowsLabel
  }));
});
if (true) GridNoRowsOverlay.displayName = "GridNoRowsOverlay";
true ? GridNoRowsOverlay.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types70.default.oneOfType([import_prop_types70.default.arrayOf(import_prop_types70.default.oneOfType([import_prop_types70.default.func, import_prop_types70.default.object, import_prop_types70.default.bool])), import_prop_types70.default.func, import_prop_types70.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridNoColumnsOverlay.js
var import_prop_types71 = __toESM(require_prop_types(), 1);
var import_jsx_runtime99 = __toESM(require_jsx_runtime(), 1);
var GridNoColumnsOverlay = forwardRef2(function GridNoColumnsOverlay2(props, ref) {
  var _a;
  const rootProps = useGridRootProps();
  const apiRef = useGridApiContext();
  const columns = useGridSelector(apiRef, gridColumnFieldsSelector);
  const handleOpenManageColumns = () => {
    apiRef.current.showPreferences(GridPreferencePanelsValue.columns);
  };
  const showManageColumnsButton = !rootProps.disableColumnSelector && columns.length > 0;
  return (0, import_jsx_runtime99.jsxs)(GridOverlay, _extends({}, props, {
    ref,
    children: [apiRef.current.getLocaleText("noColumnsOverlayLabel"), showManageColumnsButton && (0, import_jsx_runtime99.jsx)(rootProps.slots.baseButton, _extends({
      size: "small"
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
      onClick: handleOpenManageColumns,
      children: apiRef.current.getLocaleText("noColumnsOverlayManageColumns")
    }))]
  }));
});
if (true) GridNoColumnsOverlay.displayName = "GridNoColumnsOverlay";
true ? GridNoColumnsOverlay.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types71.default.oneOfType([import_prop_types71.default.arrayOf(import_prop_types71.default.oneOfType([import_prop_types71.default.func, import_prop_types71.default.object, import_prop_types71.default.bool])), import_prop_types71.default.func, import_prop_types71.default.object])
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridPagination.js
var React124 = __toESM(require_react(), 1);
var import_prop_types72 = __toESM(require_prop_types(), 1);
var import_jsx_runtime100 = __toESM(require_jsx_runtime(), 1);
var GridPaginationRoot = styled_default2(NotRendered, {
  slot: "internal"
})({
  maxHeight: "calc(100% + 1px)",
  // border width
  flexGrow: 1
});
function GridPagination() {
  var _a, _b;
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const paginationModel = useGridSelector(apiRef, gridPaginationModelSelector);
  const rowCount = useGridSelector(apiRef, gridPaginationRowCountSelector);
  const pageCount = useGridSelector(apiRef, gridPageCountSelector);
  const {
    paginationMode,
    loading
  } = rootProps;
  const disabled = rowCount === -1 && paginationMode === "server" && loading;
  const lastPage = React124.useMemo(() => Math.max(0, pageCount - 1), [pageCount]);
  const computedPage = React124.useMemo(() => {
    if (rowCount === -1) {
      return paginationModel.page;
    }
    return paginationModel.page <= lastPage ? paginationModel.page : lastPage;
  }, [lastPage, paginationModel.page, rowCount]);
  const handlePageSizeChange = React124.useCallback((pageSize2) => {
    apiRef.current.setPageSize(pageSize2);
  }, [apiRef]);
  const handlePageChange = React124.useCallback((_, page) => {
    apiRef.current.setPage(page);
  }, [apiRef]);
  const isPageSizeIncludedInPageSizeOptions = (pageSize2) => {
    for (let i2 = 0; i2 < rootProps.pageSizeOptions.length; i2 += 1) {
      const option = rootProps.pageSizeOptions[i2];
      if (typeof option === "number") {
        if (option === pageSize2) {
          return true;
        }
      } else if (option.value === pageSize2) {
        return true;
      }
    }
    return false;
  };
  if (true) {
    const warnedOnceMissingInPageSizeOptions = React124.useRef(false);
    const pageSize2 = ((_a = rootProps.paginationModel) == null ? void 0 : _a.pageSize) ?? paginationModel.pageSize;
    if (!warnedOnceMissingInPageSizeOptions.current && !rootProps.autoPageSize && !isPageSizeIncludedInPageSizeOptions(pageSize2)) {
      console.warn([`MUI X: The page size \`${paginationModel.pageSize}\` is not present in the \`pageSizeOptions\`.`, `Add it to show the pagination select.`].join("\n"));
      warnedOnceMissingInPageSizeOptions.current = true;
    }
  }
  const pageSizeOptions = isPageSizeIncludedInPageSizeOptions(paginationModel.pageSize) ? rootProps.pageSizeOptions : [];
  return (0, import_jsx_runtime100.jsx)(GridPaginationRoot, _extends({
    as: rootProps.slots.basePagination,
    count: rowCount,
    page: computedPage,
    rowsPerPageOptions: pageSizeOptions,
    rowsPerPage: paginationModel.pageSize,
    onPageChange: handlePageChange,
    onRowsPerPageChange: handlePageSizeChange,
    disabled
  }, (_b = rootProps.slotProps) == null ? void 0 : _b.basePagination));
}
true ? GridPagination.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  component: import_prop_types72.default.elementType
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridRowCount.js
var React125 = __toESM(require_react(), 1);
var import_prop_types73 = __toESM(require_prop_types(), 1);
var import_jsx_runtime101 = __toESM(require_jsx_runtime(), 1);
var _excluded59 = ["className", "rowCount", "visibleRowCount"];
var useUtilityClasses46 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["rowCount"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridRowCountRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "RowCount"
})({
  alignItems: "center",
  display: "flex",
  margin: vars.spacing(0, 2)
});
var GridRowCount = forwardRef2(function GridRowCount2(props, ref) {
  const {
    className,
    rowCount,
    visibleRowCount
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded59);
  const apiRef = useGridApiContext();
  const ownerState = useGridRootProps();
  const classes2 = useUtilityClasses46(ownerState);
  if (rowCount === 0) {
    return null;
  }
  const text = visibleRowCount < rowCount ? apiRef.current.getLocaleText("footerTotalVisibleRows")(visibleRowCount, rowCount) : rowCount.toLocaleString();
  return (0, import_jsx_runtime101.jsxs)(GridRowCountRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState
  }, other, {
    ref,
    children: [apiRef.current.getLocaleText("footerTotalRows"), " ", text]
  }));
});
if (true) GridRowCount.displayName = "GridRowCount";
true ? GridRowCount.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  rowCount: import_prop_types73.default.number.isRequired,
  sx: import_prop_types73.default.oneOfType([import_prop_types73.default.arrayOf(import_prop_types73.default.oneOfType([import_prop_types73.default.func, import_prop_types73.default.object, import_prop_types73.default.bool])), import_prop_types73.default.func, import_prop_types73.default.object]),
  visibleRowCount: import_prop_types73.default.number.isRequired
} : void 0;

// node_modules/@mui/x-data-grid/esm/components/GridRow.js
var React127 = __toESM(require_react(), 1);
var import_prop_types74 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/utils/composeGridClasses.js
function composeGridClasses(classes2, slots) {
  return composeClasses(slots, getDataGridUtilityClass, classes2);
}

// node_modules/@mui/x-data-grid/esm/hooks/features/rowReorder/gridRowReorderSelector.js
var gridRowReorderStateSelector = createRootSelector((state) => state.rowReorder);
var gridIsRowDragActiveSelector = createSelector3(gridRowReorderStateSelector, (rowReorder) => (rowReorder == null ? void 0 : rowReorder.isActive) ?? false);
var gridRowDropTargetSelector = createSelector3(gridRowReorderStateSelector, (rowReorder) => (rowReorder == null ? void 0 : rowReorder.dropTarget) ?? {
  rowId: null,
  position: null
});
var gridRowDropTargetRowIdSelector = createSelector3(gridRowDropTargetSelector, (dropTarget) => dropTarget.rowId ?? null);
var gridRowDropPositionSelector = createSelector3(gridRowDropTargetSelector, (dropTarget, rowId) => {
  if (dropTarget.rowId === rowId) {
    return dropTarget.position;
  }
  return null;
});
var gridDraggedRowIdSelector = createSelector3(gridRowReorderStateSelector, (rowReorder) => (rowReorder == null ? void 0 : rowReorder.draggedRowId) ?? null);

// node_modules/@mui/x-data-grid/esm/components/GridRowDragAndDropOverlay.js
var React126 = __toESM(require_react(), 1);
var import_jsx_runtime102 = __toESM(require_jsx_runtime(), 1);
var GridRowDragAndDropOverlayRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "RowDragOverlay",
  shouldForwardProp: (prop) => shouldForwardProp(prop) && prop !== "action"
})(({
  theme,
  action
}) => _extends({
  position: "absolute",
  left: 0,
  right: 0,
  top: 0,
  bottom: 0,
  pointerEvents: "none",
  zIndex: 1
}, action === "above" && {
  "&::before": {
    pointerEvents: "none",
    content: '""',
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    height: "2px",
    backgroundColor: (theme.vars || theme).palette.primary.main
  }
}, action === "below" && {
  "&::after": {
    pointerEvents: "none",
    content: '""',
    position: "absolute",
    bottom: "-2px",
    left: 0,
    right: 0,
    height: "2px",
    backgroundColor: (theme.vars || theme).palette.primary.main
  }
}, action === "inside" && {
  backgroundColor: theme.vars ? `rgba(${theme.vars.palette.primary.mainChannel} / 0.1)` : alpha(theme.palette.primary.main, 0.1)
}));
var GridRowDragAndDropOverlay = React126.memo(function GridRowDragAndDropOverlay2(props) {
  const {
    rowId,
    className
  } = props;
  const apiRef = useGridPrivateApiContext();
  const dropPosition = useGridSelector(apiRef, gridRowDropPositionSelector, rowId);
  if (!dropPosition) {
    return null;
  }
  return (0, import_jsx_runtime102.jsx)(GridRowDragAndDropOverlayRoot, {
    action: dropPosition,
    className
  });
});
if (true) GridRowDragAndDropOverlay.displayName = "GridRowDragAndDropOverlay";

// node_modules/@mui/x-data-grid/esm/components/GridRow.js
var import_jsx_runtime103 = __toESM(require_jsx_runtime(), 1);
var _excluded60 = ["selected", "rowId", "row", "index", "style", "rowHeight", "className", "visibleColumns", "pinnedColumns", "offsetLeft", "columnsTotalWidth", "firstColumnIndex", "lastColumnIndex", "focusedColumnIndex", "isFirstVisible", "isLastVisible", "isNotVisible", "showBottomBorder", "scrollbarWidth", "gridHasFiller", "onClick", "onDoubleClick", "onMouseEnter", "onMouseLeave", "onMouseOut", "onMouseOver"];
var isRowReorderingEnabledSelector = createSelector3(gridEditRowsStateSelector, (editRows, {
  rowReordering,
  treeData
}) => {
  if (!rowReordering || treeData) {
    return false;
  }
  const isEditingRows = !isObjectEmpty(editRows);
  return !isEditingRows;
});
var GridRow = forwardRef2(function GridRow2(props, refProp) {
  var _a;
  const {
    selected,
    rowId,
    row,
    index,
    style: styleProp,
    rowHeight,
    className,
    visibleColumns,
    pinnedColumns,
    offsetLeft,
    columnsTotalWidth,
    firstColumnIndex,
    lastColumnIndex,
    focusedColumnIndex,
    isFirstVisible,
    isLastVisible,
    isNotVisible,
    showBottomBorder,
    scrollbarWidth,
    gridHasFiller,
    onClick,
    onDoubleClick,
    onMouseEnter,
    onMouseLeave,
    onMouseOut,
    onMouseOver
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded60);
  const apiRef = useGridPrivateApiContext();
  const configuration4 = useGridConfiguration();
  const ref = React127.useRef(null);
  const rootProps = useGridRootProps();
  const currentPage = useGridVisibleRows(apiRef, rootProps);
  const sortModel = useGridSelector(apiRef, gridSortModelSelector);
  const columnPositions = useGridSelector(apiRef, gridColumnPositionsSelector);
  const rowReordering = rootProps.rowReordering;
  const treeData = rootProps.treeData;
  const isRowReorderingEnabled = useGridSelector(apiRef, isRowReorderingEnabledSelector, {
    rowReordering,
    treeData
  });
  const isRowDragActive = useGridSelector(apiRef, gridIsRowDragActiveSelector);
  const handleRef = useForkRef(ref, refProp);
  const rowNode = gridRowNodeSelector(apiRef, rowId);
  const editing = useGridSelector(apiRef, gridRowIsEditingSelector, {
    rowId,
    editMode: rootProps.editMode
  });
  const editable = rootProps.editMode === GridEditModes.Row;
  const hasFocusCell = focusedColumnIndex !== void 0;
  const hasVirtualFocusCellLeft = hasFocusCell && focusedColumnIndex >= pinnedColumns.left.length && focusedColumnIndex < firstColumnIndex;
  const hasVirtualFocusCellRight = hasFocusCell && focusedColumnIndex < visibleColumns.length - pinnedColumns.right.length && focusedColumnIndex >= lastColumnIndex;
  const classes2 = composeGridClasses(rootProps.classes, {
    root: ["row", selected && "selected", editable && "row--editable", editing && "row--editing", isFirstVisible && "row--firstVisible", isLastVisible && "row--lastVisible", showBottomBorder && "row--borderBottom", rowHeight === "auto" && "row--dynamicHeight"]
  });
  const getRowAriaAttributes = configuration4.hooks.useGridRowAriaAttributes();
  React127.useLayoutEffect(() => {
    if (currentPage.range) {
      const rowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(rowId);
      if (rowIndex !== void 0) {
        apiRef.current.unstable_setLastMeasuredRowIndex(rowIndex);
      }
    }
    if (ref.current && rowHeight === "auto") {
      return apiRef.current.observeRowHeight(ref.current, rowId);
    }
    return void 0;
  }, [apiRef, currentPage.range, rowHeight, rowId]);
  const publish = React127.useCallback((eventName, propHandler) => (event) => {
    if (isEventTargetInPortal(event)) {
      return;
    }
    if (!apiRef.current.getRow(rowId)) {
      return;
    }
    apiRef.current.publishEvent(eventName, apiRef.current.getRowParams(rowId), event);
    if (propHandler) {
      propHandler(event);
    }
  }, [apiRef, rowId]);
  const publishClick = React127.useCallback((event) => {
    const cell = findParentElementFromClassName(event.target, gridClasses.cell);
    const field = cell == null ? void 0 : cell.getAttribute("data-field");
    if (field) {
      if (field === GRID_CHECKBOX_SELECTION_COL_DEF.field) {
        return;
      }
      if (field === GRID_DETAIL_PANEL_TOGGLE_FIELD) {
        return;
      }
      if (field === "__reorder__") {
        return;
      }
      if (apiRef.current.getCellMode(rowId, field) === GridCellModes.Edit) {
        return;
      }
      const column = apiRef.current.getColumn(field);
      if ((column == null ? void 0 : column.type) === GRID_ACTIONS_COLUMN_TYPE) {
        return;
      }
    }
    publish("rowClick", onClick)(event);
  }, [apiRef, onClick, publish, rowId]);
  const {
    slots,
    slotProps,
    disableColumnReorder
  } = rootProps;
  const heightEntry = useGridSelector(apiRef, () => _extends({}, apiRef.current.getRowHeightEntry(rowId)), void 0, objectShallowCompare);
  const style = React127.useMemo(() => {
    if (isNotVisible) {
      return {
        opacity: 0,
        width: 0,
        height: 0
      };
    }
    const rowStyle = _extends({}, styleProp, {
      maxHeight: rowHeight === "auto" ? "none" : rowHeight,
      // max-height doesn't support "auto"
      minHeight: rowHeight,
      "--height": typeof rowHeight === "number" ? `${rowHeight}px` : rowHeight
    });
    if (heightEntry.spacingTop) {
      const property = rootProps.rowSpacingType === "border" ? "borderTopWidth" : "marginTop";
      rowStyle[property] = heightEntry.spacingTop;
    }
    if (heightEntry.spacingBottom) {
      const property = rootProps.rowSpacingType === "border" ? "borderBottomWidth" : "marginBottom";
      let propertyValue = rowStyle[property];
      if (typeof propertyValue !== "number") {
        propertyValue = parseInt(propertyValue || "0", 10);
      }
      propertyValue += heightEntry.spacingBottom;
      rowStyle[property] = propertyValue;
    }
    return rowStyle;
  }, [isNotVisible, rowHeight, styleProp, heightEntry.spacingTop, heightEntry.spacingBottom, rootProps.rowSpacingType]);
  if (!rowNode) {
    return null;
  }
  const rowClassNames = apiRef.current.unstable_applyPipeProcessors("rowClassName", [], rowId);
  const ariaAttributes = getRowAriaAttributes(rowNode, index);
  if (typeof rootProps.getRowClassName === "function") {
    const indexRelativeToCurrentPage = index - (((_a = currentPage.range) == null ? void 0 : _a.firstRowIndex) || 0);
    const rowParams = _extends({}, apiRef.current.getRowParams(rowId), {
      isFirstVisible: indexRelativeToCurrentPage === 0,
      isLastVisible: indexRelativeToCurrentPage === currentPage.rows.length - 1,
      indexRelativeToCurrentPage
    });
    rowClassNames.push(rootProps.getRowClassName(rowParams));
  }
  const getCell = (column, indexInSection, indexRelativeToAllColumns, sectionLength, pinnedPosition = PinnedColumnPosition.NONE) => {
    const cellColSpanInfo = apiRef.current.unstable_getCellColSpanInfo(rowId, indexRelativeToAllColumns);
    if (cellColSpanInfo == null ? void 0 : cellColSpanInfo.spannedByColSpan) {
      return null;
    }
    const width = (cellColSpanInfo == null ? void 0 : cellColSpanInfo.cellProps.width) ?? column.computedWidth;
    const colSpan = (cellColSpanInfo == null ? void 0 : cellColSpanInfo.cellProps.colSpan) ?? 1;
    const pinnedOffset = getPinnedCellOffset(pinnedPosition, column.computedWidth, indexRelativeToAllColumns, columnPositions, columnsTotalWidth, scrollbarWidth);
    if (rowNode.type === "skeletonRow") {
      return (0, import_jsx_runtime103.jsx)(slots.skeletonCell, {
        type: column.type,
        width,
        height: rowHeight,
        field: column.field,
        align: column.align
      }, column.field);
    }
    const isReorderCell = column.field === "__reorder__";
    const canReorderColumn = !(disableColumnReorder || column.disableReorder);
    const canReorderRow = isRowReorderingEnabled && !sortModel.length;
    const disableDragEvents = !(canReorderColumn || isReorderCell && canReorderRow || isRowDragActive);
    const cellIsNotVisible = pinnedPosition === PinnedColumnPosition.VIRTUAL;
    const showLeftBorder = shouldCellShowLeftBorder(pinnedPosition, indexInSection, rootProps.showCellVerticalBorder, rootProps.pinnedColumnsSectionSeparator);
    const showRightBorder = shouldCellShowRightBorder(pinnedPosition, indexInSection, sectionLength, rootProps.showCellVerticalBorder, gridHasFiller, rootProps.pinnedColumnsSectionSeparator);
    return (0, import_jsx_runtime103.jsx)(slots.cell, _extends({
      column,
      width,
      rowId,
      align: column.align || "left",
      colIndex: indexRelativeToAllColumns,
      colSpan,
      disableDragEvents,
      isNotVisible: cellIsNotVisible,
      pinnedOffset,
      pinnedPosition,
      showLeftBorder,
      showRightBorder,
      row,
      rowNode
    }, slotProps == null ? void 0 : slotProps.cell), column.field);
  };
  if (true) getCell.displayName = "getCell";
  const leftCells = pinnedColumns.left.map((column, i2) => {
    const indexRelativeToAllColumns = i2;
    return getCell(column, i2, indexRelativeToAllColumns, pinnedColumns.left.length, PinnedColumnPosition.LEFT);
  });
  const rightCells = pinnedColumns.right.map((column, i2) => {
    const indexRelativeToAllColumns = visibleColumns.length - pinnedColumns.right.length + i2;
    return getCell(column, i2, indexRelativeToAllColumns, pinnedColumns.right.length, PinnedColumnPosition.RIGHT);
  });
  const middleColumnsLength = visibleColumns.length - pinnedColumns.left.length - pinnedColumns.right.length;
  const cells = [];
  if (hasVirtualFocusCellLeft) {
    cells.push(getCell(visibleColumns[focusedColumnIndex], focusedColumnIndex - pinnedColumns.left.length, focusedColumnIndex, middleColumnsLength, PinnedColumnPosition.VIRTUAL));
  }
  for (let i2 = firstColumnIndex; i2 < lastColumnIndex; i2 += 1) {
    const column = visibleColumns[i2];
    const indexInSection = i2 - pinnedColumns.left.length;
    if (!column) {
      continue;
    }
    cells.push(getCell(column, indexInSection, i2, middleColumnsLength));
  }
  if (hasVirtualFocusCellRight) {
    cells.push(getCell(visibleColumns[focusedColumnIndex], focusedColumnIndex - pinnedColumns.left.length, focusedColumnIndex, middleColumnsLength, PinnedColumnPosition.VIRTUAL));
  }
  const eventHandlers = row ? {
    onClick: publishClick,
    onDoubleClick: publish("rowDoubleClick", onDoubleClick),
    onMouseEnter: publish("rowMouseEnter", onMouseEnter),
    onMouseLeave: publish("rowMouseLeave", onMouseLeave),
    onMouseOut: publish("rowMouseOut", onMouseOut),
    onMouseOver: publish("rowMouseOver", onMouseOver)
  } : null;
  return (0, import_jsx_runtime103.jsxs)("div", _extends({
    "data-id": rowId,
    "data-rowindex": index,
    role: "row",
    className: clsx_default(...rowClassNames, classes2.root, className),
    style
  }, ariaAttributes, eventHandlers, other, {
    ref: handleRef,
    children: [leftCells, (0, import_jsx_runtime103.jsx)("div", {
      role: "presentation",
      className: gridClasses.cellOffsetLeft,
      style: {
        width: offsetLeft
      }
    }), cells, (0, import_jsx_runtime103.jsx)("div", {
      role: "presentation",
      className: clsx_default(gridClasses.cell, gridClasses.cellEmpty)
    }), rightCells, (0, import_jsx_runtime103.jsx)(GridRowDragAndDropOverlay, {
      rowId
    })]
  }));
});
if (true) GridRow.displayName = "GridRow";
true ? GridRow.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  columnsTotalWidth: import_prop_types74.default.number.isRequired,
  firstColumnIndex: import_prop_types74.default.number.isRequired,
  /**
   * Determines which cell has focus.
   * If `null`, no cell in this row has focus.
   */
  focusedColumnIndex: import_prop_types74.default.number,
  gridHasFiller: import_prop_types74.default.bool.isRequired,
  /**
   * Index of the row in the whole sorted and filtered dataset.
   * If some rows above have expanded children, this index also take those children into account.
   */
  index: import_prop_types74.default.number.isRequired,
  isFirstVisible: import_prop_types74.default.bool.isRequired,
  isLastVisible: import_prop_types74.default.bool.isRequired,
  isNotVisible: import_prop_types74.default.bool.isRequired,
  lastColumnIndex: import_prop_types74.default.number.isRequired,
  offsetLeft: import_prop_types74.default.number.isRequired,
  onClick: import_prop_types74.default.func,
  onDoubleClick: import_prop_types74.default.func,
  onMouseEnter: import_prop_types74.default.func,
  onMouseLeave: import_prop_types74.default.func,
  pinnedColumns: import_prop_types74.default.object.isRequired,
  row: import_prop_types74.default.object.isRequired,
  rowHeight: import_prop_types74.default.oneOfType([import_prop_types74.default.oneOf(["auto"]), import_prop_types74.default.number]).isRequired,
  rowId: import_prop_types74.default.oneOfType([import_prop_types74.default.number, import_prop_types74.default.string]).isRequired,
  scrollbarWidth: import_prop_types74.default.number.isRequired,
  selected: import_prop_types74.default.bool.isRequired,
  showBottomBorder: import_prop_types74.default.bool.isRequired,
  visibleColumns: import_prop_types74.default.arrayOf(import_prop_types74.default.object).isRequired
} : void 0;
var MemoizedGridRow = fastMemo(GridRow);

// node_modules/@mui/x-data-grid-pro/esm/components/GridTreeDataGroupingCell.js
var React261 = __toESM(require_react(), 1);
var import_prop_types141 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/material/index.js
var React204 = __toESM(require_react(), 1);

// node_modules/@mui/material/esm/Autocomplete/Autocomplete.js
var React149 = __toESM(require_react(), 1);
var import_prop_types92 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/integerPropType/integerPropType.js
function getTypeByValue(value) {
  const valueType = typeof value;
  switch (valueType) {
    case "number":
      if (Number.isNaN(value)) {
        return "NaN";
      }
      if (!Number.isFinite(value)) {
        return "Infinity";
      }
      if (value !== Math.floor(value)) {
        return "float";
      }
      return "number";
    case "object":
      if (value === null) {
        return "null";
      }
      return value.constructor.name;
    default:
      return valueType;
  }
}
function requiredInteger(props, propName, componentName, location) {
  const propValue = props[propName];
  if (propValue == null || !Number.isInteger(propValue)) {
    const propType = getTypeByValue(propValue);
    return new RangeError(`Invalid ${location} \`${propName}\` of type \`${propType}\` supplied to \`${componentName}\`, expected \`integer\`.`);
  }
  return null;
}
function validator(props, propName, componentName, location) {
  const propValue = props[propName];
  if (propValue === void 0) {
    return null;
  }
  return requiredInteger(props, propName, componentName, location);
}
function validatorNoop() {
  return null;
}
validator.isRequired = requiredInteger;
validatorNoop.isRequired = validatorNoop;
var integerPropType = false ? validatorNoop : validator;
var integerPropType_default = integerPropType;

// node_modules/@mui/material/esm/useAutocomplete/useAutocomplete.js
var React129 = __toESM(require_react(), 1);

// node_modules/@mui/utils/esm/usePreviousProps/usePreviousProps.js
var React128 = __toESM(require_react(), 1);
function usePreviousProps(value) {
  const ref = React128.useRef({});
  React128.useEffect(() => {
    ref.current = value;
  });
  return ref.current;
}
var usePreviousProps_default = usePreviousProps;

// node_modules/@mui/material/esm/useAutocomplete/useAutocomplete.js
function areArraysSame({
  array1,
  array2,
  parser = (value) => value
}) {
  return array1 && array2 && array1.length === array2.length && array1.every((prevOption, index) => parser(prevOption) === parser(array2[index]));
}
function stripDiacritics(string) {
  return string.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
}
function createFilterOptions(config = {}) {
  const {
    ignoreAccents = true,
    ignoreCase = true,
    limit,
    matchFrom = "any",
    stringify,
    trim = false
  } = config;
  return (options3, {
    inputValue,
    getOptionLabel
  }) => {
    let input = trim ? inputValue.trim() : inputValue;
    if (ignoreCase) {
      input = input.toLowerCase();
    }
    if (ignoreAccents) {
      input = stripDiacritics(input);
    }
    const filteredOptions = !input ? options3 : options3.filter((option) => {
      let candidate = (stringify || getOptionLabel)(option);
      if (ignoreCase) {
        candidate = candidate.toLowerCase();
      }
      if (ignoreAccents) {
        candidate = stripDiacritics(candidate);
      }
      return matchFrom === "start" ? candidate.startsWith(input) : candidate.includes(input);
    });
    return typeof limit === "number" ? filteredOptions.slice(0, limit) : filteredOptions;
  };
}
var defaultFilterOptions = createFilterOptions();
var pageSize = 5;
var defaultIsActiveElementInListbox = (listboxRef) => {
  var _a;
  return listboxRef.current !== null && ((_a = listboxRef.current.parentElement) == null ? void 0 : _a.contains(document.activeElement));
};
var MULTIPLE_DEFAULT_VALUE = [];
function getInputValue(value, multiple, getOptionLabel, renderValue) {
  if (multiple || value == null || renderValue) {
    return "";
  }
  const optionLabel = getOptionLabel(value);
  return typeof optionLabel === "string" ? optionLabel : "";
}
function useAutocomplete(props) {
  const {
    // eslint-disable-next-line @typescript-eslint/naming-convention
    unstable_isActiveElementInListbox = defaultIsActiveElementInListbox,
    // eslint-disable-next-line @typescript-eslint/naming-convention
    unstable_classNamePrefix = "Mui",
    autoComplete = false,
    autoHighlight = false,
    autoSelect = false,
    blurOnSelect = false,
    clearOnBlur = !props.freeSolo,
    clearOnEscape = false,
    componentName = "useAutocomplete",
    defaultValue = props.multiple ? MULTIPLE_DEFAULT_VALUE : null,
    disableClearable = false,
    disableCloseOnSelect = false,
    disabled: disabledProp,
    disabledItemsFocusable = false,
    disableListWrap = false,
    filterOptions = defaultFilterOptions,
    filterSelectedOptions = false,
    freeSolo = false,
    getOptionDisabled,
    getOptionKey,
    getOptionLabel: getOptionLabelProp = (option) => option.label ?? option,
    groupBy,
    handleHomeEndKeys = !props.freeSolo,
    id: idProp,
    includeInputInList = false,
    inputValue: inputValueProp,
    isOptionEqualToValue = (option, value2) => option === value2,
    multiple = false,
    onChange,
    onClose,
    onHighlightChange,
    onInputChange,
    onOpen,
    open: openProp,
    openOnFocus = false,
    options: options3,
    readOnly = false,
    renderValue,
    selectOnFocus = !props.freeSolo,
    value: valueProp
  } = props;
  const id = useId(idProp);
  let getOptionLabel = getOptionLabelProp;
  getOptionLabel = (option) => {
    const optionLabel = getOptionLabelProp(option);
    if (typeof optionLabel !== "string") {
      if (true) {
        const erroneousReturn = optionLabel === void 0 ? "undefined" : `${typeof optionLabel} (${optionLabel})`;
        console.error(`MUI: The \`getOptionLabel\` method of ${componentName} returned ${erroneousReturn} instead of a string for ${JSON.stringify(option)}.`);
      }
      return String(optionLabel);
    }
    return optionLabel;
  };
  const ignoreFocus = React129.useRef(false);
  const firstFocus = React129.useRef(true);
  const inputRef = React129.useRef(null);
  const listboxRef = React129.useRef(null);
  const [anchorEl, setAnchorEl] = React129.useState(null);
  const [focusedItem, setFocusedItem] = React129.useState(-1);
  const defaultHighlighted = autoHighlight ? 0 : -1;
  const highlightedIndexRef = React129.useRef(defaultHighlighted);
  const initialInputValue = React129.useRef(getInputValue(defaultValue ?? valueProp, multiple, getOptionLabel)).current;
  const [value, setValueState] = useControlled({
    controlled: valueProp,
    default: defaultValue,
    name: componentName
  });
  const [inputValue, setInputValueState] = useControlled({
    controlled: inputValueProp,
    default: initialInputValue,
    name: componentName,
    state: "inputValue"
  });
  const [focused, setFocused] = React129.useState(false);
  const resetInputValue = React129.useCallback((event, newValue, reason) => {
    const isOptionSelected = multiple ? value.length < newValue.length : newValue !== null;
    if (!isOptionSelected && !clearOnBlur) {
      return;
    }
    const newInputValue = getInputValue(newValue, multiple, getOptionLabel, renderValue);
    if (inputValue === newInputValue) {
      return;
    }
    setInputValueState(newInputValue);
    if (onInputChange) {
      onInputChange(event, newInputValue, reason);
    }
  }, [getOptionLabel, inputValue, multiple, onInputChange, setInputValueState, clearOnBlur, value, renderValue]);
  const [open, setOpenState] = useControlled({
    controlled: openProp,
    default: false,
    name: componentName,
    state: "open"
  });
  const [inputPristine, setInputPristine] = React129.useState(true);
  const inputValueIsSelectedValue = !multiple && value != null && inputValue === getOptionLabel(value);
  const popupOpen = open && !readOnly;
  const filteredOptions = popupOpen ? filterOptions(
    options3.filter((option) => {
      if (filterSelectedOptions && (multiple ? value : [value]).some((value2) => value2 !== null && isOptionEqualToValue(option, value2))) {
        return false;
      }
      return true;
    }),
    // we use the empty string to manipulate `filterOptions` to not filter any options
    // i.e. the filter predicate always returns true
    {
      inputValue: inputValueIsSelectedValue && inputPristine ? "" : inputValue,
      getOptionLabel
    }
  ) : [];
  const previousProps = usePreviousProps_default({
    filteredOptions,
    value,
    inputValue
  });
  React129.useEffect(() => {
    const valueChange = value !== previousProps.value;
    if (focused && !valueChange) {
      return;
    }
    if (freeSolo && !valueChange) {
      return;
    }
    resetInputValue(null, value, "reset");
  }, [value, resetInputValue, focused, previousProps.value, freeSolo]);
  const listboxAvailable = open && filteredOptions.length > 0 && !readOnly;
  const focusItem = useEventCallback_default((itemToFocus) => {
    if (itemToFocus === -1) {
      inputRef.current.focus();
    } else {
      const indexType = renderValue ? "data-item-index" : "data-tag-index";
      anchorEl.querySelector(`[${indexType}="${itemToFocus}"]`).focus();
    }
  });
  React129.useEffect(() => {
    if (multiple && focusedItem > value.length - 1) {
      setFocusedItem(-1);
      focusItem(-1);
    }
  }, [value, multiple, focusedItem, focusItem]);
  function validOptionIndex(index, direction) {
    if (!listboxRef.current || index < 0 || index >= filteredOptions.length) {
      return -1;
    }
    let nextFocus = index;
    while (true) {
      const option = listboxRef.current.querySelector(`[data-option-index="${nextFocus}"]`);
      const nextFocusDisabled = disabledItemsFocusable ? false : !option || option.disabled || option.getAttribute("aria-disabled") === "true";
      if (option && option.hasAttribute("tabindex") && !nextFocusDisabled) {
        return nextFocus;
      }
      if (direction === "next") {
        nextFocus = (nextFocus + 1) % filteredOptions.length;
      } else {
        nextFocus = (nextFocus - 1 + filteredOptions.length) % filteredOptions.length;
      }
      if (nextFocus === index) {
        return -1;
      }
    }
  }
  const setHighlightedIndex = useEventCallback_default(({
    event,
    index,
    reason
  }) => {
    highlightedIndexRef.current = index;
    if (index === -1) {
      inputRef.current.removeAttribute("aria-activedescendant");
    } else {
      inputRef.current.setAttribute("aria-activedescendant", `${id}-option-${index}`);
    }
    if (onHighlightChange && ["mouse", "keyboard", "touch"].includes(reason)) {
      onHighlightChange(event, index === -1 ? null : filteredOptions[index], reason);
    }
    if (!listboxRef.current) {
      return;
    }
    const prev = listboxRef.current.querySelector(`[role="option"].${unstable_classNamePrefix}-focused`);
    if (prev) {
      prev.classList.remove(`${unstable_classNamePrefix}-focused`);
      prev.classList.remove(`${unstable_classNamePrefix}-focusVisible`);
    }
    let listboxNode = listboxRef.current;
    if (listboxRef.current.getAttribute("role") !== "listbox") {
      listboxNode = listboxRef.current.parentElement.querySelector('[role="listbox"]');
    }
    if (!listboxNode) {
      return;
    }
    if (index === -1) {
      listboxNode.scrollTop = 0;
      return;
    }
    const option = listboxRef.current.querySelector(`[data-option-index="${index}"]`);
    if (!option) {
      return;
    }
    option.classList.add(`${unstable_classNamePrefix}-focused`);
    if (reason === "keyboard") {
      option.classList.add(`${unstable_classNamePrefix}-focusVisible`);
    }
    if (listboxNode.scrollHeight > listboxNode.clientHeight && reason !== "mouse" && reason !== "touch") {
      const element = option;
      const scrollBottom = listboxNode.clientHeight + listboxNode.scrollTop;
      const elementBottom = element.offsetTop + element.offsetHeight;
      if (elementBottom > scrollBottom) {
        listboxNode.scrollTop = elementBottom - listboxNode.clientHeight;
      } else if (element.offsetTop - element.offsetHeight * (groupBy ? 1.3 : 0) < listboxNode.scrollTop) {
        listboxNode.scrollTop = element.offsetTop - element.offsetHeight * (groupBy ? 1.3 : 0);
      }
    }
  });
  const changeHighlightedIndex = useEventCallback_default(({
    event,
    diff,
    direction = "next",
    reason
  }) => {
    if (!popupOpen) {
      return;
    }
    const getNextIndex = () => {
      const maxIndex = filteredOptions.length - 1;
      if (diff === "reset") {
        return defaultHighlighted;
      }
      if (diff === "start") {
        return 0;
      }
      if (diff === "end") {
        return maxIndex;
      }
      const newIndex = highlightedIndexRef.current + diff;
      if (newIndex < 0) {
        if (newIndex === -1 && includeInputInList) {
          return -1;
        }
        if (disableListWrap && highlightedIndexRef.current !== -1 || Math.abs(diff) > 1) {
          return 0;
        }
        return maxIndex;
      }
      if (newIndex > maxIndex) {
        if (newIndex === maxIndex + 1 && includeInputInList) {
          return -1;
        }
        if (disableListWrap || Math.abs(diff) > 1) {
          return maxIndex;
        }
        return 0;
      }
      return newIndex;
    };
    const nextIndex = validOptionIndex(getNextIndex(), direction);
    setHighlightedIndex({
      index: nextIndex,
      reason,
      event
    });
    if (autoComplete && diff !== "reset") {
      if (nextIndex === -1) {
        inputRef.current.value = inputValue;
      } else {
        const option = getOptionLabel(filteredOptions[nextIndex]);
        inputRef.current.value = option;
        const index = option.toLowerCase().indexOf(inputValue.toLowerCase());
        if (index === 0 && inputValue.length > 0) {
          inputRef.current.setSelectionRange(inputValue.length, option.length);
        }
      }
    }
  });
  const filteredOptionsChanged = !areArraysSame({
    array1: previousProps.filteredOptions,
    array2: filteredOptions,
    parser: getOptionLabel
  });
  const getPreviousHighlightedOptionIndex = () => {
    const isSameValue = (value1, value2) => {
      const label1 = value1 ? getOptionLabel(value1) : "";
      const label2 = value2 ? getOptionLabel(value2) : "";
      return label1 === label2;
    };
    if (highlightedIndexRef.current !== -1 && !areArraysSame({
      array1: previousProps.filteredOptions,
      array2: filteredOptions,
      parser: getOptionLabel
    }) && previousProps.inputValue === inputValue && (multiple ? value.length === previousProps.value.length && previousProps.value.every((val, i2) => getOptionLabel(value[i2]) === getOptionLabel(val)) : isSameValue(previousProps.value, value))) {
      const previousHighlightedOption = previousProps.filteredOptions[highlightedIndexRef.current];
      if (previousHighlightedOption) {
        return filteredOptions.findIndex((option) => {
          return getOptionLabel(option) === getOptionLabel(previousHighlightedOption);
        });
      }
    }
    return -1;
  };
  const syncHighlightedIndex = React129.useCallback(() => {
    if (!popupOpen) {
      return;
    }
    const previousHighlightedOptionIndex = getPreviousHighlightedOptionIndex();
    if (previousHighlightedOptionIndex !== -1) {
      highlightedIndexRef.current = previousHighlightedOptionIndex;
      return;
    }
    const valueItem = multiple ? value[0] : value;
    if (filteredOptions.length === 0 || valueItem == null) {
      changeHighlightedIndex({
        diff: "reset"
      });
      return;
    }
    if (!listboxRef.current) {
      return;
    }
    if (valueItem != null) {
      const currentOption = filteredOptions[highlightedIndexRef.current];
      if (multiple && currentOption && value.findIndex((val) => isOptionEqualToValue(currentOption, val)) !== -1) {
        return;
      }
      const itemIndex = filteredOptions.findIndex((optionItem) => isOptionEqualToValue(optionItem, valueItem));
      if (itemIndex === -1) {
        changeHighlightedIndex({
          diff: "reset"
        });
      } else {
        setHighlightedIndex({
          index: itemIndex
        });
      }
      return;
    }
    if (highlightedIndexRef.current >= filteredOptions.length - 1) {
      setHighlightedIndex({
        index: filteredOptions.length - 1
      });
      return;
    }
    setHighlightedIndex({
      index: highlightedIndexRef.current
    });
  }, [
    // Only sync the highlighted index when the option switch between empty and not
    filteredOptions.length,
    // Don't sync the highlighted index with the value when multiple
    // eslint-disable-next-line react-hooks/exhaustive-deps
    multiple ? false : value,
    changeHighlightedIndex,
    setHighlightedIndex,
    popupOpen,
    inputValue,
    multiple
  ]);
  const handleListboxRef = useEventCallback_default((node) => {
    setRef(listboxRef, node);
    if (!node) {
      return;
    }
    syncHighlightedIndex();
  });
  if (true) {
    React129.useEffect(() => {
      if (!inputRef.current || inputRef.current.nodeName !== "INPUT") {
        if (inputRef.current && inputRef.current.nodeName === "TEXTAREA") {
          console.warn([`A textarea element was provided to ${componentName} where input was expected.`, `This is not a supported scenario but it may work under certain conditions.`, `A textarea keyboard navigation may conflict with Autocomplete controls (for example enter and arrow keys).`, `Make sure to test keyboard navigation and add custom event handlers if necessary.`].join("\n"));
        } else {
          console.error([`MUI: Unable to find the input element. It was resolved to ${inputRef.current} while an HTMLInputElement was expected.`, `Instead, ${componentName} expects an input element.`, "", componentName === "useAutocomplete" ? "Make sure you have bound getInputProps correctly and that the normal ref/effect resolutions order is guaranteed." : "Make sure you have customized the input component correctly."].join("\n"));
        }
      }
    }, [componentName]);
  }
  React129.useEffect(() => {
    if (filteredOptionsChanged || popupOpen && !disableCloseOnSelect) {
      syncHighlightedIndex();
    }
  }, [syncHighlightedIndex, filteredOptionsChanged, popupOpen, disableCloseOnSelect]);
  const handleOpen = (event) => {
    if (open) {
      return;
    }
    setOpenState(true);
    setInputPristine(true);
    if (onOpen) {
      onOpen(event);
    }
  };
  const handleClose = (event, reason) => {
    if (!open) {
      return;
    }
    setOpenState(false);
    if (onClose) {
      onClose(event, reason);
    }
  };
  const handleValue = (event, newValue, reason, details) => {
    if (multiple) {
      if (value.length === newValue.length && value.every((val, i2) => val === newValue[i2])) {
        return;
      }
    } else if (value === newValue) {
      return;
    }
    if (onChange) {
      onChange(event, newValue, reason, details);
    }
    setValueState(newValue);
  };
  const isTouch = React129.useRef(false);
  const selectNewValue = (event, option, reasonProp = "selectOption", origin = "options") => {
    let reason = reasonProp;
    let newValue = option;
    if (multiple) {
      newValue = Array.isArray(value) ? value.slice() : [];
      if (true) {
        const matches = newValue.filter((val) => isOptionEqualToValue(option, val));
        if (matches.length > 1) {
          console.error([`MUI: The \`isOptionEqualToValue\` method of ${componentName} does not handle the arguments correctly.`, `The component expects a single value to match a given option but found ${matches.length} matches.`].join("\n"));
        }
      }
      const itemIndex = newValue.findIndex((valueItem) => isOptionEqualToValue(option, valueItem));
      if (itemIndex === -1) {
        newValue.push(option);
      } else if (origin !== "freeSolo") {
        newValue.splice(itemIndex, 1);
        reason = "removeOption";
      }
    }
    resetInputValue(event, newValue, reason);
    handleValue(event, newValue, reason, {
      option
    });
    if (!disableCloseOnSelect && (!event || !event.ctrlKey && !event.metaKey)) {
      handleClose(event, reason);
    }
    if (blurOnSelect === true || blurOnSelect === "touch" && isTouch.current || blurOnSelect === "mouse" && !isTouch.current) {
      inputRef.current.blur();
    }
  };
  function validItemIndex(index, direction) {
    if (index === -1) {
      return -1;
    }
    let nextFocus = index;
    while (true) {
      if (direction === "next" && nextFocus === value.length || direction === "previous" && nextFocus === -1) {
        return -1;
      }
      const indexType = renderValue ? "data-item-index" : "data-tag-index";
      const option = anchorEl.querySelector(`[${indexType}="${nextFocus}"]`);
      if (!option || !option.hasAttribute("tabindex") || option.disabled || option.getAttribute("aria-disabled") === "true") {
        nextFocus += direction === "next" ? 1 : -1;
      } else {
        return nextFocus;
      }
    }
  }
  const handleFocusItem = (event, direction) => {
    if (!multiple) {
      return;
    }
    if (inputValue === "") {
      handleClose(event, "toggleInput");
    }
    let nextItem3 = focusedItem;
    if (focusedItem === -1 && direction === "previous") {
      nextItem3 = value.length - 1;
      if (freeSolo && inputValue !== "") {
        setInputValueState("");
        if (onInputChange) {
          onInputChange(event, "", "reset");
        }
      }
    } else {
      nextItem3 += direction === "next" ? 1 : -1;
      if (nextItem3 < 0) {
        nextItem3 = 0;
      }
      if (nextItem3 === value.length) {
        nextItem3 = -1;
      }
    }
    nextItem3 = validItemIndex(nextItem3, direction);
    setFocusedItem(nextItem3);
    focusItem(nextItem3);
  };
  const handleClear = (event) => {
    ignoreFocus.current = true;
    setInputValueState("");
    if (onInputChange) {
      onInputChange(event, "", "clear");
    }
    handleValue(event, multiple ? [] : null, "clear");
  };
  const handleKeyDown = (other) => (event) => {
    if (other.onKeyDown) {
      other.onKeyDown(event);
    }
    if (event.defaultMuiPrevented) {
      return;
    }
    if (focusedItem !== -1 && !["ArrowLeft", "ArrowRight"].includes(event.key)) {
      setFocusedItem(-1);
      focusItem(-1);
    }
    if (event.which !== 229) {
      switch (event.key) {
        case "Home":
          if (popupOpen && handleHomeEndKeys) {
            event.preventDefault();
            changeHighlightedIndex({
              diff: "start",
              direction: "next",
              reason: "keyboard",
              event
            });
          }
          break;
        case "End":
          if (popupOpen && handleHomeEndKeys) {
            event.preventDefault();
            changeHighlightedIndex({
              diff: "end",
              direction: "previous",
              reason: "keyboard",
              event
            });
          }
          break;
        case "PageUp":
          event.preventDefault();
          changeHighlightedIndex({
            diff: -pageSize,
            direction: "previous",
            reason: "keyboard",
            event
          });
          handleOpen(event);
          break;
        case "PageDown":
          event.preventDefault();
          changeHighlightedIndex({
            diff: pageSize,
            direction: "next",
            reason: "keyboard",
            event
          });
          handleOpen(event);
          break;
        case "ArrowDown":
          event.preventDefault();
          changeHighlightedIndex({
            diff: 1,
            direction: "next",
            reason: "keyboard",
            event
          });
          handleOpen(event);
          break;
        case "ArrowUp":
          event.preventDefault();
          changeHighlightedIndex({
            diff: -1,
            direction: "previous",
            reason: "keyboard",
            event
          });
          handleOpen(event);
          break;
        case "ArrowLeft": {
          const input = inputRef.current;
          const caretAtStart = input && input.selectionStart === 0 && input.selectionEnd === 0;
          if (!caretAtStart) {
            return;
          }
          if (!multiple && renderValue && value != null) {
            if (freeSolo && inputValue !== "") {
              setInputValueState("");
              if (onInputChange) {
                onInputChange(event, "", "reset");
              }
            }
            setFocusedItem(0);
            focusItem(0);
          } else {
            handleFocusItem(event, "previous");
          }
          break;
        }
        case "ArrowRight":
          if (!multiple && renderValue) {
            setFocusedItem(-1);
            focusItem(-1);
          } else {
            handleFocusItem(event, "next");
          }
          break;
        case "Enter":
          if (highlightedIndexRef.current !== -1 && popupOpen) {
            const option = filteredOptions[highlightedIndexRef.current];
            const disabled = getOptionDisabled ? getOptionDisabled(option) : false;
            event.preventDefault();
            if (disabled) {
              return;
            }
            selectNewValue(event, option, "selectOption");
            if (autoComplete) {
              inputRef.current.setSelectionRange(inputRef.current.value.length, inputRef.current.value.length);
            }
          } else if (freeSolo && inputValue !== "" && inputValueIsSelectedValue === false) {
            if (multiple) {
              event.preventDefault();
            }
            selectNewValue(event, inputValue, "createOption", "freeSolo");
          }
          break;
        case "Escape":
          if (popupOpen) {
            event.preventDefault();
            event.stopPropagation();
            handleClose(event, "escape");
          } else if (clearOnEscape && (inputValue !== "" || multiple && value.length > 0 || renderValue)) {
            event.preventDefault();
            event.stopPropagation();
            handleClear(event);
          }
          break;
        case "Backspace":
          if (multiple && !readOnly && inputValue === "" && value.length > 0) {
            const index = focusedItem === -1 ? value.length - 1 : focusedItem;
            const newValue = value.slice();
            newValue.splice(index, 1);
            handleValue(event, newValue, "removeOption", {
              option: value[index]
            });
          }
          if (!multiple && renderValue && !readOnly && inputValue === "") {
            handleValue(event, null, "removeOption", {
              option: value
            });
          }
          break;
        case "Delete":
          if (multiple && !readOnly && inputValue === "" && value.length > 0 && focusedItem !== -1) {
            const index = focusedItem;
            const newValue = value.slice();
            newValue.splice(index, 1);
            handleValue(event, newValue, "removeOption", {
              option: value[index]
            });
          }
          if (!multiple && renderValue && !readOnly && inputValue === "") {
            handleValue(event, null, "removeOption", {
              option: value
            });
          }
          break;
        default:
      }
    }
  };
  const handleFocus = (event) => {
    setFocused(true);
    if (focusedItem !== -1) {
      setFocusedItem(-1);
      focusItem(-1);
    }
    if (openOnFocus && !ignoreFocus.current) {
      handleOpen(event);
    }
  };
  const handleBlur = (event) => {
    if (unstable_isActiveElementInListbox(listboxRef)) {
      inputRef.current.focus();
      return;
    }
    setFocused(false);
    firstFocus.current = true;
    ignoreFocus.current = false;
    if (autoSelect && highlightedIndexRef.current !== -1 && popupOpen) {
      selectNewValue(event, filteredOptions[highlightedIndexRef.current], "blur");
    } else if (autoSelect && freeSolo && inputValue !== "") {
      selectNewValue(event, inputValue, "blur", "freeSolo");
    } else if (clearOnBlur) {
      resetInputValue(event, value, "blur");
    }
    handleClose(event, "blur");
  };
  const handleInputChange = (event) => {
    const newValue = event.target.value;
    if (inputValue !== newValue) {
      setInputValueState(newValue);
      setInputPristine(false);
      if (onInputChange) {
        onInputChange(event, newValue, "input");
      }
    }
    if (newValue === "") {
      if (!disableClearable && !multiple && !renderValue) {
        handleValue(event, null, "clear");
      }
    } else {
      handleOpen(event);
    }
  };
  const handleOptionMouseMove = (event) => {
    const index = Number(event.currentTarget.getAttribute("data-option-index"));
    if (highlightedIndexRef.current !== index) {
      setHighlightedIndex({
        event,
        index,
        reason: "mouse"
      });
    }
  };
  const handleOptionTouchStart = (event) => {
    setHighlightedIndex({
      event,
      index: Number(event.currentTarget.getAttribute("data-option-index")),
      reason: "touch"
    });
    isTouch.current = true;
  };
  const handleOptionClick = (event) => {
    const index = Number(event.currentTarget.getAttribute("data-option-index"));
    selectNewValue(event, filteredOptions[index], "selectOption");
    isTouch.current = false;
  };
  const handleItemDelete = (index) => (event) => {
    const newValue = value.slice();
    newValue.splice(index, 1);
    handleValue(event, newValue, "removeOption", {
      option: value[index]
    });
  };
  const handleSingleItemDelete = (event) => {
    handleValue(event, null, "removeOption", {
      option: value
    });
  };
  const handlePopupIndicator = (event) => {
    if (open) {
      handleClose(event, "toggleInput");
    } else {
      handleOpen(event);
    }
  };
  const handleMouseDown = (event) => {
    if (!event.currentTarget.contains(event.target)) {
      return;
    }
    if (event.target.getAttribute("id") !== id) {
      event.preventDefault();
    }
  };
  const handleClick = (event) => {
    if (!event.currentTarget.contains(event.target)) {
      return;
    }
    inputRef.current.focus();
    if (selectOnFocus && firstFocus.current && inputRef.current.selectionEnd - inputRef.current.selectionStart === 0) {
      inputRef.current.select();
    }
    firstFocus.current = false;
  };
  const handleInputMouseDown = (event) => {
    if (!disabledProp && (inputValue === "" || !open)) {
      handlePopupIndicator(event);
    }
  };
  let dirty = freeSolo && inputValue.length > 0;
  dirty = dirty || (multiple ? value.length > 0 : value !== null);
  let groupedOptions = filteredOptions;
  if (groupBy) {
    const indexBy = /* @__PURE__ */ new Map();
    let warn = false;
    groupedOptions = filteredOptions.reduce((acc, option, index) => {
      const group = groupBy(option);
      if (acc.length > 0 && acc[acc.length - 1].group === group) {
        acc[acc.length - 1].options.push(option);
      } else {
        if (true) {
          if (indexBy.get(group) && !warn) {
            console.warn(`MUI: The options provided combined with the \`groupBy\` method of ${componentName} returns duplicated headers.`, "You can solve the issue by sorting the options with the output of `groupBy`.");
            warn = true;
          }
          indexBy.set(group, true);
        }
        acc.push({
          key: index,
          index,
          group,
          options: [option]
        });
      }
      return acc;
    }, []);
  }
  if (disabledProp && focused) {
    handleBlur();
  }
  return {
    getRootProps: (other = {}) => ({
      ...other,
      onKeyDown: handleKeyDown(other),
      onMouseDown: handleMouseDown,
      onClick: handleClick
    }),
    getInputLabelProps: () => ({
      id: `${id}-label`,
      htmlFor: id
    }),
    getInputProps: () => ({
      id,
      value: inputValue,
      onBlur: handleBlur,
      onFocus: handleFocus,
      onChange: handleInputChange,
      onMouseDown: handleInputMouseDown,
      // if open then this is handled imperatively so don't let react override
      // only have an opinion about this when closed
      "aria-activedescendant": popupOpen ? "" : null,
      "aria-autocomplete": autoComplete ? "both" : "list",
      "aria-controls": listboxAvailable ? `${id}-listbox` : void 0,
      "aria-expanded": listboxAvailable,
      // Disable browser's suggestion that might overlap with the popup.
      // Handle autocomplete but not autofill.
      autoComplete: "off",
      ref: inputRef,
      autoCapitalize: "none",
      spellCheck: "false",
      role: "combobox",
      disabled: disabledProp
    }),
    getClearProps: () => ({
      tabIndex: -1,
      type: "button",
      onClick: handleClear
    }),
    getItemProps: ({
      index = 0
    } = {}) => ({
      ...multiple && {
        key: index
      },
      ...renderValue ? {
        "data-item-index": index
      } : {
        "data-tag-index": index
      },
      tabIndex: -1,
      ...!readOnly && {
        onDelete: multiple ? handleItemDelete(index) : handleSingleItemDelete
      }
    }),
    getPopupIndicatorProps: () => ({
      tabIndex: -1,
      type: "button",
      onClick: handlePopupIndicator
    }),
    // deprecated
    getTagProps: ({
      index
    }) => ({
      key: index,
      "data-tag-index": index,
      tabIndex: -1,
      ...!readOnly && {
        onDelete: handleItemDelete(index)
      }
    }),
    getListboxProps: () => ({
      role: "listbox",
      id: `${id}-listbox`,
      "aria-labelledby": `${id}-label`,
      "aria-multiselectable": multiple || void 0,
      ref: handleListboxRef,
      onMouseDown: (event) => {
        event.preventDefault();
      }
    }),
    getOptionProps: ({
      index,
      option
    }) => {
      const selected = (multiple ? value : [value]).some((value2) => value2 != null && isOptionEqualToValue(option, value2));
      const disabled = getOptionDisabled ? getOptionDisabled(option) : false;
      return {
        key: (getOptionKey == null ? void 0 : getOptionKey(option)) ?? getOptionLabel(option),
        tabIndex: -1,
        role: "option",
        id: `${id}-option-${index}`,
        onMouseMove: handleOptionMouseMove,
        onClick: handleOptionClick,
        onTouchStart: handleOptionTouchStart,
        "data-option-index": index,
        "aria-disabled": disabled,
        "aria-selected": selected
      };
    },
    id,
    inputValue,
    value,
    dirty,
    expanded: popupOpen && anchorEl,
    popupOpen,
    focused: focused || focusedItem !== -1,
    anchorEl,
    setAnchorEl,
    focusedItem,
    // deprecated
    focusedTag: focusedItem,
    groupedOptions
  };
}
var useAutocomplete_default = useAutocomplete;

// node_modules/@mui/material/esm/Popper/Popper.js
var import_prop_types77 = __toESM(require_prop_types(), 1);
var React133 = __toESM(require_react(), 1);

// node_modules/@mui/material/esm/Popper/BasePopper.js
var React132 = __toESM(require_react(), 1);

// node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];

// node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument2 = node.ownerDocument;
    return ownerDocument2 ? ownerDocument2.defaultView || window : window;
  }
  return node;
}

// node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement(node) {
  var OwnElement = getWindow(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement(node) {
  var OwnElement = getWindow(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}

// node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function(name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
      var style = styleProperties.reduce(function(style2, property) {
        style2[property] = "";
        return style2;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect,
  requires: ["computeStyles"]
};

// node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/@popperjs/core/lib/utils/math.js
var max = Math.max;
var min = Math.min;
var round = Math.round;

// node_modules/@popperjs/core/lib/utils/userAgent.js
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands && Array.isArray(uaData.brands)) {
    return uaData.brands.map(function(item) {
      return item.brand + "/" + item.version;
    }).join(" ");
  }
  return navigator.userAgent;
}

// node_modules/@popperjs/core/lib/dom-utils/isLayoutViewport.js
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}

// node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement(element)) {
    scaleX = element.offsetWidth > 0 ? round(clientRect.width) / element.offsetWidth || 1 : 1;
    scaleY = element.offsetHeight > 0 ? round(clientRect.height) / element.offsetHeight || 1 : 1;
  }
  var _ref = isElement(element) ? getWindow(element) : window, visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
  var y = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
  var width = clientRect.width / scaleX;
  var height = clientRect.height / scaleY;
  return {
    width,
    height,
    top: y,
    right: x + width,
    bottom: y + height,
    left: x,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}

// node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next = child;
    do {
      if (next && parent.isSameNode(next)) {
        return true;
      }
      next = next.parentNode || next.host;
    } while (next);
  }
  return false;
}

// node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}

// node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement(element) {
  return ((isElement(element) ? element.ownerDocument : (
    // $FlowFixMe[prop-missing]
    element.document
  )) || window.document).documentElement;
}

// node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement(element)
  );
}

// node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent(element) {
  if (!isHTMLElement(element) || // https://github.com/popperjs/popper-core/issues/837
  getComputedStyle2(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox2 = /firefox/i.test(getUAString());
  var isIE = /Trident/i.test(getUAString());
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle2(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css2 = getComputedStyle2(currentNode);
    if (css2.transform !== "none" || css2.perspective !== "none" || css2.contain === "paint" || ["transform", "perspective"].indexOf(css2.willChange) !== -1 || isFirefox2 && css2.willChange === "filter" || isFirefox2 && css2.filter && css2.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle2(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle2(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}

// node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/@popperjs/core/lib/utils/within.js
function within(min2, value, max2) {
  return max(min2, min(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}

// node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

// node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys2) {
  return keys2.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding, state) {
  padding = typeof padding === "function" ? padding(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding;
  return mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name = _ref.name, options3 = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options3.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset2, _state$modifiersData$.centerOffset = offset2 - center, _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state, options3 = _ref2.options;
  var _options$element = options3.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (!contains(state.elements.popper, arrowElement)) {
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};

// node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref, win) {
  var x = _ref.x, y = _ref.y;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(x * dpr) / dpr || 0,
    y: round(y * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x, x = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x,
    y
  }) : {
    x,
    y
  };
  x = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper2)) {
      offsetParent = getDocumentElement(popper2);
      if (getComputedStyle2(offsetParent).position !== "static" && position === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        offsetParent[heightProp]
      );
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        offsetParent[widthProp]
      );
      x -= offsetX - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x,
    y
  }, getWindow(popper2)) : {
    x,
    y
  };
  x = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options3 = _ref5.options;
  var _options$gpuAccelerat = options3.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options3.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options3.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};

// node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true
};
function effect3(_ref) {
  var state = _ref.state, instance = _ref.instance, options3 = _ref.options;
  var _options$scroll = options3.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options3.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect: effect3,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash[matched];
  });
}

// node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash2 = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash2[matched];
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node) {
  var win = getWindow(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}

// node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect(element, strategy) {
  var win = getWindow(element);
  var html = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x = 0;
  var y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || !layoutViewport && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x + getWindowScrollBarX(element),
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle2(body || html).direction === "rtl") {
    x += max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}

// node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle2(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent(getParentNode(node));
}

// node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    updatedList.concat(listScrollParents(getParentNode(target)))
  );
}

// node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

// node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect(element, strategy) {
  var rect = getBoundingClientRect(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport ? rectToClientRect(getViewportRect(element, strategy)) : isElement(clippingParent) ? getInnerBoundingClientRect(clippingParent, strategy) : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle2(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement(element) ? getOffsetParent(element) : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement(clippingParent) && contains(clippingParent, clipperElement) && getNodeName(clippingParent) !== "body";
  });
}
function getClippingRect(element, boundary, rootBoundary, strategy) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent, strategy));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow(state, options3) {
  if (options3 === void 0) {
    options3 = {};
  }
  var _options = options3, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$strategy = _options.strategy, strategy = _options$strategy === void 0 ? state.strategy : _options$strategy, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(isElement(element) ? element : element.contextElement || getDocumentElement(state.elements.popper), boundary, rootBoundary, strategy);
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement
  });
  var popperClientRect = rectToClientRect(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options3) {
  if (options3 === void 0) {
    options3 = {};
  }
  var _options = options3, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements2 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements2.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements2;
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a, b) {
    return overflows[a] - overflows[b];
  });
}

// node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip(_ref) {
  var state = _ref.state, options3 = _ref.options, name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options3.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options3.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options3.fallbackPlacements, padding = options3.padding, boundary = options3.boundary, rootBoundary = options3.rootBoundary, altBoundary = options3.altBoundary, _options$flipVariatio = options3.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options3.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i2 = 0; i2 < placements2.length; i2++) {
    var placement = placements2[i2];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements2.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break") break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};

// node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state, name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide
};

// node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset(_ref2) {
  var state = _ref2.state, options3 = _ref2.options, name = _ref2.name;
  var _options$offset = options3.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset
};

// node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state, name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};

// node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state, options3 = _ref.options, name = _ref.name;
  var _options$mainAxis = options3.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options3.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options3.boundary, rootBoundary = options3.rootBoundary, altBoundary = options3.altBoundary, padding = options3.padding, _options$tether = options3.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options3.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min(min2, tetherMin) : min2, offset2, tether ? max(max2, tetherMax) : max2);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};

// node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

// node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}

// node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round(rect.width) / element.offsetWidth || 1;
  var scaleY = round(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var offsetParentIsScaled = isHTMLElement(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled, isFixed);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
    isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

// node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

// node_modules/@popperjs/core/lib/utils/debounce.js
function debounce2(fn2) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn2());
        });
      });
    }
    return pending;
  };
}

// node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}

// node_modules/@popperjs/core/lib/createPopper.js
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers3 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper4(reference2, popper2, options3) {
    if (options3 === void 0) {
      options3 = defaultOptions;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options4 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions, state.options, options4);
        state.scrollParents = {
          reference: isElement(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers3, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index], fn2 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
          if (typeof fn2 === "function") {
            state = fn2({
              state,
              options: _options,
              name,
              instance
            }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce2(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      return instance;
    }
    instance.setOptions(options3).then(function(state2) {
      if (!isDestroyed && options3.onFirstUpdate) {
        options3.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref) {
        var name = _ref.name, _ref$options = _ref.options, options4 = _ref$options === void 0 ? {} : _ref$options, effect4 = _ref.effect;
        if (typeof effect4 === "function") {
          var cleanupFn = effect4({
            state,
            name,
            instance,
            options: options4
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn2) {
        return fn2();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}
var createPopper = popperGenerator();

// node_modules/@popperjs/core/lib/popper-lite.js
var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default];
var createPopper2 = popperGenerator({
  defaultModifiers
});

// node_modules/@popperjs/core/lib/popper.js
var defaultModifiers2 = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default, offset_default, flip_default, preventOverflow_default, arrow_default, hide_default];
var createPopper3 = popperGenerator({
  defaultModifiers: defaultModifiers2
});

// node_modules/@mui/material/esm/Popper/BasePopper.js
var import_prop_types76 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/appendOwnerState/appendOwnerState.js
function appendOwnerState(elementType, otherProps, ownerState) {
  if (elementType === void 0 || isHostComponent_default(elementType)) {
    return otherProps;
  }
  return {
    ...otherProps,
    ownerState: {
      ...otherProps.ownerState,
      ...ownerState
    }
  };
}
var appendOwnerState_default = appendOwnerState;

// node_modules/@mui/utils/esm/extractEventHandlers/extractEventHandlers.js
function extractEventHandlers(object, excludeKeys = []) {
  if (object === void 0) {
    return {};
  }
  const result = {};
  Object.keys(object).filter((prop) => prop.match(/^on[A-Z]/) && typeof object[prop] === "function" && !excludeKeys.includes(prop)).forEach((prop) => {
    result[prop] = object[prop];
  });
  return result;
}
var extractEventHandlers_default = extractEventHandlers;

// node_modules/@mui/utils/esm/omitEventHandlers/omitEventHandlers.js
function omitEventHandlers(object) {
  if (object === void 0) {
    return {};
  }
  const result = {};
  Object.keys(object).filter((prop) => !(prop.match(/^on[A-Z]/) && typeof object[prop] === "function")).forEach((prop) => {
    result[prop] = object[prop];
  });
  return result;
}
var omitEventHandlers_default = omitEventHandlers;

// node_modules/@mui/utils/esm/mergeSlotProps/mergeSlotProps.js
function mergeSlotProps2(parameters) {
  const {
    getSlotProps,
    additionalProps,
    externalSlotProps,
    externalForwardedProps,
    className
  } = parameters;
  if (!getSlotProps) {
    const joinedClasses2 = clsx_default(additionalProps == null ? void 0 : additionalProps.className, className, externalForwardedProps == null ? void 0 : externalForwardedProps.className, externalSlotProps == null ? void 0 : externalSlotProps.className);
    const mergedStyle2 = {
      ...additionalProps == null ? void 0 : additionalProps.style,
      ...externalForwardedProps == null ? void 0 : externalForwardedProps.style,
      ...externalSlotProps == null ? void 0 : externalSlotProps.style
    };
    const props2 = {
      ...additionalProps,
      ...externalForwardedProps,
      ...externalSlotProps
    };
    if (joinedClasses2.length > 0) {
      props2.className = joinedClasses2;
    }
    if (Object.keys(mergedStyle2).length > 0) {
      props2.style = mergedStyle2;
    }
    return {
      props: props2,
      internalRef: void 0
    };
  }
  const eventHandlers = extractEventHandlers_default({
    ...externalForwardedProps,
    ...externalSlotProps
  });
  const componentsPropsWithoutEventHandlers = omitEventHandlers_default(externalSlotProps);
  const otherPropsWithoutEventHandlers = omitEventHandlers_default(externalForwardedProps);
  const internalSlotProps = getSlotProps(eventHandlers);
  const joinedClasses = clsx_default(internalSlotProps == null ? void 0 : internalSlotProps.className, additionalProps == null ? void 0 : additionalProps.className, className, externalForwardedProps == null ? void 0 : externalForwardedProps.className, externalSlotProps == null ? void 0 : externalSlotProps.className);
  const mergedStyle = {
    ...internalSlotProps == null ? void 0 : internalSlotProps.style,
    ...additionalProps == null ? void 0 : additionalProps.style,
    ...externalForwardedProps == null ? void 0 : externalForwardedProps.style,
    ...externalSlotProps == null ? void 0 : externalSlotProps.style
  };
  const props = {
    ...internalSlotProps,
    ...additionalProps,
    ...otherPropsWithoutEventHandlers,
    ...componentsPropsWithoutEventHandlers
  };
  if (joinedClasses.length > 0) {
    props.className = joinedClasses;
  }
  if (Object.keys(mergedStyle).length > 0) {
    props.style = mergedStyle;
  }
  return {
    props,
    internalRef: internalSlotProps.ref
  };
}
var mergeSlotProps_default = mergeSlotProps2;

// node_modules/@mui/utils/esm/resolveComponentProps/resolveComponentProps.js
function resolveComponentProps(componentProps, ownerState, slotState) {
  if (typeof componentProps === "function") {
    return componentProps(ownerState, slotState);
  }
  return componentProps;
}
var resolveComponentProps_default = resolveComponentProps;

// node_modules/@mui/utils/esm/useSlotProps/useSlotProps.js
function useSlotProps(parameters) {
  var _a;
  const {
    elementType,
    externalSlotProps,
    ownerState,
    skipResolvingSlotProps = false,
    ...other
  } = parameters;
  const resolvedComponentsProps = skipResolvingSlotProps ? {} : resolveComponentProps_default(externalSlotProps, ownerState);
  const {
    props: mergedProps,
    internalRef
  } = mergeSlotProps_default({
    ...other,
    externalSlotProps: resolvedComponentsProps
  });
  const ref = useForkRef(internalRef, resolvedComponentsProps == null ? void 0 : resolvedComponentsProps.ref, (_a = parameters.additionalProps) == null ? void 0 : _a.ref);
  const props = appendOwnerState_default(elementType, {
    ...mergedProps,
    ref
  }, ownerState);
  return props;
}
var useSlotProps_default = useSlotProps;

// node_modules/@mui/material/esm/utils/isLayoutSupported.js
function isLayoutSupported() {
  return !(/jsdom|HappyDOM/.test(window.navigator.userAgent) || // TODO(v9): Remove the test environment check
  // eslint-disable-next-line mui/consistent-production-guard
  false);
}

// node_modules/@mui/material/esm/Portal/Portal.js
var React131 = __toESM(require_react(), 1);
var ReactDOM2 = __toESM(require_react_dom(), 1);
var import_prop_types75 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/getReactElementRef/getReactElementRef.js
var React130 = __toESM(require_react(), 1);
function getReactElementRef(element) {
  var _a;
  if (parseInt(React130.version, 10) >= 19) {
    return ((_a = element == null ? void 0 : element.props) == null ? void 0 : _a.ref) || null;
  }
  return (element == null ? void 0 : element.ref) || null;
}

// node_modules/@mui/material/esm/Portal/Portal.js
function getContainer(container) {
  return typeof container === "function" ? container() : container;
}
var Portal = React131.forwardRef(function Portal2(props, forwardedRef) {
  const {
    children: children2,
    container,
    disablePortal = false
  } = props;
  const [mountNode, setMountNode] = React131.useState(null);
  const handleRef = useForkRef(React131.isValidElement(children2) ? getReactElementRef(children2) : null, forwardedRef);
  useEnhancedEffect_default(() => {
    if (!disablePortal) {
      setMountNode(getContainer(container) || document.body);
    }
  }, [container, disablePortal]);
  useEnhancedEffect_default(() => {
    if (mountNode && !disablePortal) {
      setRef(forwardedRef, mountNode);
      return () => {
        setRef(forwardedRef, null);
      };
    }
    return void 0;
  }, [forwardedRef, mountNode, disablePortal]);
  if (disablePortal) {
    if (React131.isValidElement(children2)) {
      const newProps = {
        ref: handleRef
      };
      return React131.cloneElement(children2, newProps);
    }
    return children2;
  }
  return mountNode ? ReactDOM2.createPortal(children2, mountNode) : mountNode;
});
true ? Portal.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The children to render into the `container`.
   */
  children: import_prop_types75.default.node,
  /**
   * An HTML element or function that returns one.
   * The `container` will have the portal children appended to it.
   *
   * You can also provide a callback, which is called in a React layout effect.
   * This lets you set the container from a ref, and also makes server-side rendering possible.
   *
   * By default, it uses the body of the top-level document object,
   * so it's simply `document.body` most of the time.
   */
  container: import_prop_types75.default.oneOfType([HTMLElementType, import_prop_types75.default.func]),
  /**
   * The `children` will be under the DOM hierarchy of the parent component.
   * @default false
   */
  disablePortal: import_prop_types75.default.bool
} : void 0;
if (true) {
  Portal["propTypes"] = exactProp(Portal.propTypes);
}
var Portal_default = Portal;

// node_modules/@mui/material/esm/Popper/popperClasses.js
function getPopperUtilityClass(slot) {
  return generateUtilityClass("MuiPopper", slot);
}
var popperClasses = generateUtilityClasses("MuiPopper", ["root"]);

// node_modules/@mui/material/esm/Popper/BasePopper.js
var import_jsx_runtime104 = __toESM(require_jsx_runtime(), 1);
function flipPlacement(placement, direction) {
  if (direction === "ltr") {
    return placement;
  }
  switch (placement) {
    case "bottom-end":
      return "bottom-start";
    case "bottom-start":
      return "bottom-end";
    case "top-end":
      return "top-start";
    case "top-start":
      return "top-end";
    default:
      return placement;
  }
}
function resolveAnchorEl(anchorEl) {
  return typeof anchorEl === "function" ? anchorEl() : anchorEl;
}
function isHTMLElement2(element) {
  return element.nodeType !== void 0;
}
function isVirtualElement(element) {
  return !isHTMLElement2(element);
}
var useUtilityClasses47 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"]
  };
  return composeClasses(slots, getPopperUtilityClass, classes2);
};
var defaultPopperOptions = {};
var PopperTooltip = React132.forwardRef(function PopperTooltip2(props, forwardedRef) {
  const {
    anchorEl,
    children: children2,
    direction,
    disablePortal,
    modifiers,
    open,
    placement: initialPlacement,
    popperOptions,
    popperRef: popperRefProp,
    slotProps = {},
    slots = {},
    TransitionProps,
    // @ts-ignore internal logic
    ownerState: ownerStateProp,
    // prevent from spreading to DOM, it can come from the parent component e.g. Select.
    ...other
  } = props;
  const tooltipRef = React132.useRef(null);
  const ownRef = useForkRef(tooltipRef, forwardedRef);
  const popperRef = React132.useRef(null);
  const handlePopperRef = useForkRef(popperRef, popperRefProp);
  const handlePopperRefRef = React132.useRef(handlePopperRef);
  useEnhancedEffect_default(() => {
    handlePopperRefRef.current = handlePopperRef;
  }, [handlePopperRef]);
  React132.useImperativeHandle(popperRefProp, () => popperRef.current, []);
  const rtlPlacement = flipPlacement(initialPlacement, direction);
  const [placement, setPlacement] = React132.useState(rtlPlacement);
  const [resolvedAnchorElement, setResolvedAnchorElement] = React132.useState(resolveAnchorEl(anchorEl));
  React132.useEffect(() => {
    if (popperRef.current) {
      popperRef.current.forceUpdate();
    }
  });
  React132.useEffect(() => {
    if (anchorEl) {
      setResolvedAnchorElement(resolveAnchorEl(anchorEl));
    }
  }, [anchorEl]);
  useEnhancedEffect_default(() => {
    if (!resolvedAnchorElement || !open) {
      return void 0;
    }
    const handlePopperUpdate = (data) => {
      setPlacement(data.placement);
    };
    if (true) {
      if (resolvedAnchorElement && isHTMLElement2(resolvedAnchorElement) && resolvedAnchorElement.nodeType === 1) {
        const box = resolvedAnchorElement.getBoundingClientRect();
        if (isLayoutSupported() && box.top === 0 && box.left === 0 && box.right === 0 && box.bottom === 0) {
          console.warn(["MUI: The `anchorEl` prop provided to the component is invalid.", "The anchor element should be part of the document layout.", "Make sure the element is present in the document or that it's not display none."].join("\n"));
        }
      }
    }
    let popperModifiers = [{
      name: "preventOverflow",
      options: {
        altBoundary: disablePortal
      }
    }, {
      name: "flip",
      options: {
        altBoundary: disablePortal
      }
    }, {
      name: "onUpdate",
      enabled: true,
      phase: "afterWrite",
      fn: ({
        state
      }) => {
        handlePopperUpdate(state);
      }
    }];
    if (modifiers != null) {
      popperModifiers = popperModifiers.concat(modifiers);
    }
    if (popperOptions && popperOptions.modifiers != null) {
      popperModifiers = popperModifiers.concat(popperOptions.modifiers);
    }
    const popper2 = createPopper3(resolvedAnchorElement, tooltipRef.current, {
      placement: rtlPlacement,
      ...popperOptions,
      modifiers: popperModifiers
    });
    handlePopperRefRef.current(popper2);
    return () => {
      popper2.destroy();
      handlePopperRefRef.current(null);
    };
  }, [resolvedAnchorElement, disablePortal, modifiers, open, popperOptions, rtlPlacement]);
  const childProps = {
    placement
  };
  if (TransitionProps !== null) {
    childProps.TransitionProps = TransitionProps;
  }
  const classes2 = useUtilityClasses47(props);
  const Root = slots.root ?? "div";
  const rootProps = useSlotProps_default({
    elementType: Root,
    externalSlotProps: slotProps.root,
    externalForwardedProps: other,
    additionalProps: {
      role: "tooltip",
      ref: ownRef
    },
    ownerState: props,
    className: classes2.root
  });
  return (0, import_jsx_runtime104.jsx)(Root, {
    ...rootProps,
    children: typeof children2 === "function" ? children2(childProps) : children2
  });
});
var Popper = React132.forwardRef(function Popper2(props, forwardedRef) {
  const {
    anchorEl,
    children: children2,
    container: containerProp,
    direction = "ltr",
    disablePortal = false,
    keepMounted = false,
    modifiers,
    open,
    placement = "bottom",
    popperOptions = defaultPopperOptions,
    popperRef,
    style,
    transition: transition2 = false,
    slotProps = {},
    slots = {},
    ...other
  } = props;
  const [exited, setExited] = React132.useState(true);
  const handleEnter = () => {
    setExited(false);
  };
  const handleExited = () => {
    setExited(true);
  };
  if (!keepMounted && !open && (!transition2 || exited)) {
    return null;
  }
  let container;
  if (containerProp) {
    container = containerProp;
  } else if (anchorEl) {
    const resolvedAnchorEl = resolveAnchorEl(anchorEl);
    container = resolvedAnchorEl && isHTMLElement2(resolvedAnchorEl) ? ownerDocument(resolvedAnchorEl).body : ownerDocument(null).body;
  }
  const display = !open && keepMounted && (!transition2 || exited) ? "none" : void 0;
  const transitionProps = transition2 ? {
    in: open,
    onEnter: handleEnter,
    onExited: handleExited
  } : void 0;
  return (0, import_jsx_runtime104.jsx)(Portal_default, {
    disablePortal,
    container,
    children: (0, import_jsx_runtime104.jsx)(PopperTooltip, {
      anchorEl,
      direction,
      disablePortal,
      modifiers,
      ref: forwardedRef,
      open: transition2 ? !exited : open,
      placement,
      popperOptions,
      popperRef,
      slotProps,
      slots,
      ...other,
      style: {
        // Prevents scroll issue, waiting for Popper.js to add this style once initiated.
        position: "fixed",
        // Fix Popper.js display issue
        top: 0,
        left: 0,
        display,
        ...style
      },
      TransitionProps: transitionProps,
      children: children2
    })
  });
});
true ? Popper.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * An HTML element, [virtualElement](https://popper.js.org/docs/v2/virtual-elements/),
   * or a function that returns either.
   * It's used to set the position of the popper.
   * The return value will passed as the reference object of the Popper instance.
   */
  anchorEl: chainPropTypes(import_prop_types76.default.oneOfType([HTMLElementType, import_prop_types76.default.object, import_prop_types76.default.func]), (props) => {
    if (props.open) {
      const resolvedAnchorEl = resolveAnchorEl(props.anchorEl);
      if (resolvedAnchorEl && isHTMLElement2(resolvedAnchorEl) && resolvedAnchorEl.nodeType === 1) {
        const box = resolvedAnchorEl.getBoundingClientRect();
        if (true) {
          if (isLayoutSupported() && box.top === 0 && box.left === 0 && box.right === 0 && box.bottom === 0) {
            return new Error(["MUI: The `anchorEl` prop provided to the component is invalid.", "The anchor element should be part of the document layout.", "Make sure the element is present in the document or that it's not display none."].join("\n"));
          }
        }
      } else if (!resolvedAnchorEl || typeof resolvedAnchorEl.getBoundingClientRect !== "function" || isVirtualElement(resolvedAnchorEl) && resolvedAnchorEl.contextElement != null && resolvedAnchorEl.contextElement.nodeType !== 1) {
        return new Error(["MUI: The `anchorEl` prop provided to the component is invalid.", "It should be an HTML element instance or a virtualElement ", "(https://popper.js.org/docs/v2/virtual-elements/)."].join("\n"));
      }
    }
    return null;
  }),
  /**
   * Popper render function or node.
   */
  children: import_prop_types76.default.oneOfType([import_prop_types76.default.node, import_prop_types76.default.func]),
  /**
   * An HTML element or function that returns one.
   * The `container` will have the portal children appended to it.
   *
   * You can also provide a callback, which is called in a React layout effect.
   * This lets you set the container from a ref, and also makes server-side rendering possible.
   *
   * By default, it uses the body of the top-level document object,
   * so it's simply `document.body` most of the time.
   */
  container: import_prop_types76.default.oneOfType([HTMLElementType, import_prop_types76.default.func]),
  /**
   * Direction of the text.
   * @default 'ltr'
   */
  direction: import_prop_types76.default.oneOf(["ltr", "rtl"]),
  /**
   * The `children` will be under the DOM hierarchy of the parent component.
   * @default false
   */
  disablePortal: import_prop_types76.default.bool,
  /**
   * Always keep the children in the DOM.
   * This prop can be useful in SEO situation or
   * when you want to maximize the responsiveness of the Popper.
   * @default false
   */
  keepMounted: import_prop_types76.default.bool,
  /**
   * Popper.js is based on a "plugin-like" architecture,
   * most of its features are fully encapsulated "modifiers".
   *
   * A modifier is a function that is called each time Popper.js needs to
   * compute the position of the popper.
   * For this reason, modifiers should be very performant to avoid bottlenecks.
   * To learn how to create a modifier, [read the modifiers documentation](https://popper.js.org/docs/v2/modifiers/).
   */
  modifiers: import_prop_types76.default.arrayOf(import_prop_types76.default.shape({
    data: import_prop_types76.default.object,
    effect: import_prop_types76.default.func,
    enabled: import_prop_types76.default.bool,
    fn: import_prop_types76.default.func,
    name: import_prop_types76.default.any,
    options: import_prop_types76.default.object,
    phase: import_prop_types76.default.oneOf(["afterMain", "afterRead", "afterWrite", "beforeMain", "beforeRead", "beforeWrite", "main", "read", "write"]),
    requires: import_prop_types76.default.arrayOf(import_prop_types76.default.string),
    requiresIfExists: import_prop_types76.default.arrayOf(import_prop_types76.default.string)
  })),
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types76.default.bool.isRequired,
  /**
   * Popper placement.
   * @default 'bottom'
   */
  placement: import_prop_types76.default.oneOf(["auto-end", "auto-start", "auto", "bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
  /**
   * Options provided to the [`Popper.js`](https://popper.js.org/docs/v2/constructors/#options) instance.
   * @default {}
   */
  popperOptions: import_prop_types76.default.shape({
    modifiers: import_prop_types76.default.array,
    onFirstUpdate: import_prop_types76.default.func,
    placement: import_prop_types76.default.oneOf(["auto-end", "auto-start", "auto", "bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
    strategy: import_prop_types76.default.oneOf(["absolute", "fixed"])
  }),
  /**
   * A ref that points to the used popper instance.
   */
  popperRef: refType_default,
  /**
   * The props used for each slot inside the Popper.
   * @default {}
   */
  slotProps: import_prop_types76.default.shape({
    root: import_prop_types76.default.oneOfType([import_prop_types76.default.func, import_prop_types76.default.object])
  }),
  /**
   * The components used for each slot inside the Popper.
   * Either a string to use a HTML element or a component.
   * @default {}
   */
  slots: import_prop_types76.default.shape({
    root: import_prop_types76.default.elementType
  }),
  /**
   * Help supporting a react-transition-group/Transition component.
   * @default false
   */
  transition: import_prop_types76.default.bool
} : void 0;
var BasePopper_default = Popper;

// node_modules/@mui/material/esm/Popper/Popper.js
var import_jsx_runtime105 = __toESM(require_jsx_runtime(), 1);
var PopperRoot = styled_default2(BasePopper_default, {
  name: "MuiPopper",
  slot: "Root"
})({});
var Popper3 = React133.forwardRef(function Popper4(inProps, ref) {
  const isRtl = useRtl();
  const props = useDefaultProps({
    props: inProps,
    name: "MuiPopper"
  });
  const {
    anchorEl,
    component,
    components,
    componentsProps,
    container,
    disablePortal,
    keepMounted,
    modifiers,
    open,
    placement,
    popperOptions,
    popperRef,
    transition: transition2,
    slots,
    slotProps,
    ...other
  } = props;
  const RootComponent = (slots == null ? void 0 : slots.root) ?? (components == null ? void 0 : components.Root);
  const otherProps = {
    anchorEl,
    container,
    disablePortal,
    keepMounted,
    modifiers,
    open,
    placement,
    popperOptions,
    popperRef,
    transition: transition2,
    ...other
  };
  return (0, import_jsx_runtime105.jsx)(PopperRoot, {
    as: component,
    direction: isRtl ? "rtl" : "ltr",
    slots: {
      root: RootComponent
    },
    slotProps: slotProps ?? componentsProps,
    ...otherProps,
    ref
  });
});
true ? Popper3.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * An HTML element, [virtualElement](https://popper.js.org/docs/v2/virtual-elements/),
   * or a function that returns either.
   * It's used to set the position of the popper.
   * The return value will passed as the reference object of the Popper instance.
   */
  anchorEl: import_prop_types77.default.oneOfType([HTMLElementType, import_prop_types77.default.object, import_prop_types77.default.func]),
  /**
   * Popper render function or node.
   */
  children: import_prop_types77.default.oneOfType([import_prop_types77.default.node, import_prop_types77.default.func]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types77.default.elementType,
  /**
   * The components used for each slot inside the Popper.
   * Either a string to use a HTML element or a component.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. [How to migrate](/material-ui/migration/migrating-from-deprecated-apis/).
   * @default {}
   */
  components: import_prop_types77.default.shape({
    Root: import_prop_types77.default.elementType
  }),
  /**
   * The props used for each slot inside the Popper.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. [How to migrate](/material-ui/migration/migrating-from-deprecated-apis/).
   * @default {}
   */
  componentsProps: import_prop_types77.default.shape({
    root: import_prop_types77.default.oneOfType([import_prop_types77.default.func, import_prop_types77.default.object])
  }),
  /**
   * An HTML element or function that returns one.
   * The `container` will have the portal children appended to it.
   *
   * You can also provide a callback, which is called in a React layout effect.
   * This lets you set the container from a ref, and also makes server-side rendering possible.
   *
   * By default, it uses the body of the top-level document object,
   * so it's simply `document.body` most of the time.
   */
  container: import_prop_types77.default.oneOfType([HTMLElementType, import_prop_types77.default.func]),
  /**
   * The `children` will be under the DOM hierarchy of the parent component.
   * @default false
   */
  disablePortal: import_prop_types77.default.bool,
  /**
   * Always keep the children in the DOM.
   * This prop can be useful in SEO situation or
   * when you want to maximize the responsiveness of the Popper.
   * @default false
   */
  keepMounted: import_prop_types77.default.bool,
  /**
   * Popper.js is based on a "plugin-like" architecture,
   * most of its features are fully encapsulated "modifiers".
   *
   * A modifier is a function that is called each time Popper.js needs to
   * compute the position of the popper.
   * For this reason, modifiers should be very performant to avoid bottlenecks.
   * To learn how to create a modifier, [read the modifiers documentation](https://popper.js.org/docs/v2/modifiers/).
   */
  modifiers: import_prop_types77.default.arrayOf(import_prop_types77.default.shape({
    data: import_prop_types77.default.object,
    effect: import_prop_types77.default.func,
    enabled: import_prop_types77.default.bool,
    fn: import_prop_types77.default.func,
    name: import_prop_types77.default.any,
    options: import_prop_types77.default.object,
    phase: import_prop_types77.default.oneOf(["afterMain", "afterRead", "afterWrite", "beforeMain", "beforeRead", "beforeWrite", "main", "read", "write"]),
    requires: import_prop_types77.default.arrayOf(import_prop_types77.default.string),
    requiresIfExists: import_prop_types77.default.arrayOf(import_prop_types77.default.string)
  })),
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types77.default.bool.isRequired,
  /**
   * Popper placement.
   * @default 'bottom'
   */
  placement: import_prop_types77.default.oneOf(["auto-end", "auto-start", "auto", "bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
  /**
   * Options provided to the [`Popper.js`](https://popper.js.org/docs/v2/constructors/#options) instance.
   * @default {}
   */
  popperOptions: import_prop_types77.default.shape({
    modifiers: import_prop_types77.default.array,
    onFirstUpdate: import_prop_types77.default.func,
    placement: import_prop_types77.default.oneOf(["auto-end", "auto-start", "auto", "bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
    strategy: import_prop_types77.default.oneOf(["absolute", "fixed"])
  }),
  /**
   * A ref that points to the used popper instance.
   */
  popperRef: refType_default,
  /**
   * The props used for each slot inside the Popper.
   * @default {}
   */
  slotProps: import_prop_types77.default.shape({
    root: import_prop_types77.default.oneOfType([import_prop_types77.default.func, import_prop_types77.default.object])
  }),
  /**
   * The components used for each slot inside the Popper.
   * Either a string to use a HTML element or a component.
   * @default {}
   */
  slots: import_prop_types77.default.shape({
    root: import_prop_types77.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types77.default.oneOfType([import_prop_types77.default.arrayOf(import_prop_types77.default.oneOfType([import_prop_types77.default.func, import_prop_types77.default.object, import_prop_types77.default.bool])), import_prop_types77.default.func, import_prop_types77.default.object]),
  /**
   * Help supporting a react-transition-group/Transition component.
   * @default false
   */
  transition: import_prop_types77.default.bool
} : void 0;
var Popper_default = Popper3;

// node_modules/@mui/material/esm/ListSubheader/ListSubheader.js
var React134 = __toESM(require_react(), 1);
var import_prop_types78 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/ListSubheader/listSubheaderClasses.js
function getListSubheaderUtilityClass(slot) {
  return generateUtilityClass("MuiListSubheader", slot);
}
var listSubheaderClasses = generateUtilityClasses("MuiListSubheader", ["root", "colorPrimary", "colorInherit", "gutters", "inset", "sticky"]);

// node_modules/@mui/material/esm/ListSubheader/ListSubheader.js
var import_jsx_runtime106 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses48 = (ownerState) => {
  const {
    classes: classes2,
    color,
    disableGutters,
    inset,
    disableSticky
  } = ownerState;
  const slots = {
    root: ["root", color !== "default" && `color${capitalize_default(color)}`, !disableGutters && "gutters", inset && "inset", !disableSticky && "sticky"]
  };
  return composeClasses(slots, getListSubheaderUtilityClass, classes2);
};
var ListSubheaderRoot = styled_default2("li", {
  name: "MuiListSubheader",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.color !== "default" && styles5[`color${capitalize_default(ownerState.color)}`], !ownerState.disableGutters && styles5.gutters, ownerState.inset && styles5.inset, !ownerState.disableSticky && styles5.sticky];
  }
})(memoTheme_default(({
  theme
}) => ({
  boxSizing: "border-box",
  lineHeight: "48px",
  listStyle: "none",
  color: (theme.vars || theme).palette.text.secondary,
  fontFamily: theme.typography.fontFamily,
  fontWeight: theme.typography.fontWeightMedium,
  fontSize: theme.typography.pxToRem(14),
  variants: [{
    props: {
      color: "primary"
    },
    style: {
      color: (theme.vars || theme).palette.primary.main
    }
  }, {
    props: {
      color: "inherit"
    },
    style: {
      color: "inherit"
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.disableGutters,
    style: {
      paddingLeft: 16,
      paddingRight: 16
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.inset,
    style: {
      paddingLeft: 72
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.disableSticky,
    style: {
      position: "sticky",
      top: 0,
      zIndex: 1,
      backgroundColor: (theme.vars || theme).palette.background.paper
    }
  }]
})));
var ListSubheader = React134.forwardRef(function ListSubheader2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiListSubheader"
  });
  const {
    className,
    color = "default",
    component = "li",
    disableGutters = false,
    disableSticky = false,
    inset = false,
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    component,
    disableGutters,
    disableSticky,
    inset
  };
  const classes2 = useUtilityClasses48(ownerState);
  return (0, import_jsx_runtime106.jsx)(ListSubheaderRoot, {
    as: component,
    className: clsx_default(classes2.root, className),
    ref,
    ownerState,
    ...other
  });
});
if (ListSubheader) {
  ListSubheader.muiSkipListHighlight = true;
}
true ? ListSubheader.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types78.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types78.default.object,
  /**
   * @ignore
   */
  className: import_prop_types78.default.string,
  /**
   * The color of the component. It supports those theme colors that make sense for this component.
   * @default 'default'
   */
  color: import_prop_types78.default.oneOf(["default", "inherit", "primary"]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types78.default.elementType,
  /**
   * If `true`, the List Subheader will not have gutters.
   * @default false
   */
  disableGutters: import_prop_types78.default.bool,
  /**
   * If `true`, the List Subheader will not stick to the top during scroll.
   * @default false
   */
  disableSticky: import_prop_types78.default.bool,
  /**
   * If `true`, the List Subheader is indented.
   * @default false
   */
  inset: import_prop_types78.default.bool,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types78.default.oneOfType([import_prop_types78.default.arrayOf(import_prop_types78.default.oneOfType([import_prop_types78.default.func, import_prop_types78.default.object, import_prop_types78.default.bool])), import_prop_types78.default.func, import_prop_types78.default.object])
} : void 0;
var ListSubheader_default = ListSubheader;

// node_modules/@mui/material/esm/Paper/Paper.js
var React135 = __toESM(require_react(), 1);
var import_prop_types79 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Paper/paperClasses.js
function getPaperUtilityClass(slot) {
  return generateUtilityClass("MuiPaper", slot);
}
var paperClasses = generateUtilityClasses("MuiPaper", ["root", "rounded", "outlined", "elevation", "elevation0", "elevation1", "elevation2", "elevation3", "elevation4", "elevation5", "elevation6", "elevation7", "elevation8", "elevation9", "elevation10", "elevation11", "elevation12", "elevation13", "elevation14", "elevation15", "elevation16", "elevation17", "elevation18", "elevation19", "elevation20", "elevation21", "elevation22", "elevation23", "elevation24"]);

// node_modules/@mui/material/esm/Paper/Paper.js
var import_jsx_runtime107 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses49 = (ownerState) => {
  const {
    square,
    elevation,
    variant,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", variant, !square && "rounded", variant === "elevation" && `elevation${elevation}`]
  };
  return composeClasses(slots, getPaperUtilityClass, classes2);
};
var PaperRoot = styled_default2("div", {
  name: "MuiPaper",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[ownerState.variant], !ownerState.square && styles5.rounded, ownerState.variant === "elevation" && styles5[`elevation${ownerState.elevation}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  backgroundColor: (theme.vars || theme).palette.background.paper,
  color: (theme.vars || theme).palette.text.primary,
  transition: theme.transitions.create("box-shadow"),
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.square,
    style: {
      borderRadius: theme.shape.borderRadius
    }
  }, {
    props: {
      variant: "outlined"
    },
    style: {
      border: `1px solid ${(theme.vars || theme).palette.divider}`
    }
  }, {
    props: {
      variant: "elevation"
    },
    style: {
      boxShadow: "var(--Paper-shadow)",
      backgroundImage: "var(--Paper-overlay)"
    }
  }]
})));
var Paper = React135.forwardRef(function Paper2(inProps, ref) {
  var _a;
  const props = useDefaultProps({
    props: inProps,
    name: "MuiPaper"
  });
  const theme = useTheme();
  const {
    className,
    component = "div",
    elevation = 1,
    square = false,
    variant = "elevation",
    ...other
  } = props;
  const ownerState = {
    ...props,
    component,
    elevation,
    square,
    variant
  };
  const classes2 = useUtilityClasses49(ownerState);
  if (true) {
    if (theme.shadows[elevation] === void 0) {
      console.error([`MUI: The elevation provided <Paper elevation={${elevation}}> is not available in the theme.`, `Please make sure that \`theme.shadows[${elevation}]\` is defined.`].join("\n"));
    }
  }
  return (0, import_jsx_runtime107.jsx)(PaperRoot, {
    as: component,
    ownerState,
    className: clsx_default(classes2.root, className),
    ref,
    ...other,
    style: {
      ...variant === "elevation" && {
        "--Paper-shadow": (theme.vars || theme).shadows[elevation],
        ...theme.vars && {
          "--Paper-overlay": (_a = theme.vars.overlays) == null ? void 0 : _a[elevation]
        },
        ...!theme.vars && theme.palette.mode === "dark" && {
          "--Paper-overlay": `linear-gradient(${alpha("#fff", getOverlayAlpha(elevation))}, ${alpha("#fff", getOverlayAlpha(elevation))})`
        }
      },
      ...other.style
    }
  });
});
true ? Paper.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types79.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types79.default.object,
  /**
   * @ignore
   */
  className: import_prop_types79.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types79.default.elementType,
  /**
   * Shadow depth, corresponds to `dp` in the spec.
   * It accepts values between 0 and 24 inclusive.
   * @default 1
   */
  elevation: chainPropTypes(integerPropType_default, (props) => {
    const {
      elevation,
      variant
    } = props;
    if (elevation > 0 && variant === "outlined") {
      return new Error(`MUI: Combining \`elevation={${elevation}}\` with \`variant="${variant}"\` has no effect. Either use \`elevation={0}\` or use a different \`variant\`.`);
    }
    return null;
  }),
  /**
   * If `true`, rounded corners are disabled.
   * @default false
   */
  square: import_prop_types79.default.bool,
  /**
   * @ignore
   */
  style: import_prop_types79.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types79.default.oneOfType([import_prop_types79.default.arrayOf(import_prop_types79.default.oneOfType([import_prop_types79.default.func, import_prop_types79.default.object, import_prop_types79.default.bool])), import_prop_types79.default.func, import_prop_types79.default.object]),
  /**
   * The variant to use.
   * @default 'elevation'
   */
  variant: import_prop_types79.default.oneOfType([import_prop_types79.default.oneOf(["elevation", "outlined"]), import_prop_types79.default.string])
} : void 0;
var Paper_default = Paper;

// node_modules/@mui/material/esm/IconButton/IconButton.js
var React147 = __toESM(require_react(), 1);
var import_prop_types90 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/ButtonBase/ButtonBase.js
var React145 = __toESM(require_react(), 1);
var import_prop_types88 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/isFocusVisible/isFocusVisible.js
function isFocusVisible(element) {
  try {
    return element.matches(":focus-visible");
  } catch (error) {
    if (!window.navigator.userAgent.includes("jsdom")) {
      console.warn(["MUI: The `:focus-visible` pseudo class is not supported in this browser.", "Some components rely on this feature to work properly."].join("\n"));
    }
  }
  return false;
}

// node_modules/@mui/material/esm/useLazyRipple/useLazyRipple.js
var React136 = __toESM(require_react(), 1);
var LazyRipple = class _LazyRipple {
  constructor() {
    __publicField(this, "mountEffect", () => {
      if (this.shouldMount && !this.didMount) {
        if (this.ref.current !== null) {
          this.didMount = true;
          this.mounted.resolve();
        }
      }
    });
    this.ref = {
      current: null
    };
    this.mounted = null;
    this.didMount = false;
    this.shouldMount = false;
    this.setShouldMount = null;
  }
  /** React ref to the ripple instance */
  /** If the ripple component should be mounted */
  /** Promise that resolves when the ripple component is mounted */
  /** If the ripple component has been mounted */
  /** React state hook setter */
  static create() {
    return new _LazyRipple();
  }
  static use() {
    const ripple = useLazyRef(_LazyRipple.create).current;
    const [shouldMount, setShouldMount] = React136.useState(false);
    ripple.shouldMount = shouldMount;
    ripple.setShouldMount = setShouldMount;
    React136.useEffect(ripple.mountEffect, [shouldMount]);
    return ripple;
  }
  mount() {
    if (!this.mounted) {
      this.mounted = createControlledPromise();
      this.shouldMount = true;
      this.setShouldMount(this.shouldMount);
    }
    return this.mounted;
  }
  /* Ripple API */
  start(...args) {
    this.mount().then(() => {
      var _a;
      return (_a = this.ref.current) == null ? void 0 : _a.start(...args);
    });
  }
  stop(...args) {
    this.mount().then(() => {
      var _a;
      return (_a = this.ref.current) == null ? void 0 : _a.stop(...args);
    });
  }
  pulsate(...args) {
    this.mount().then(() => {
      var _a;
      return (_a = this.ref.current) == null ? void 0 : _a.pulsate(...args);
    });
  }
};
function useLazyRipple() {
  return LazyRipple.use();
}
function createControlledPromise() {
  let resolve;
  let reject;
  const p = new Promise((resolveFn, rejectFn) => {
    resolve = resolveFn;
    reject = rejectFn;
  });
  p.resolve = resolve;
  p.reject = reject;
  return p;
}

// node_modules/@mui/material/esm/ButtonBase/TouchRipple.js
var React144 = __toESM(require_react(), 1);
var import_prop_types87 = __toESM(require_prop_types(), 1);

// node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(t, e) {
  return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t2, e2) {
    return t2.__proto__ = e2, t2;
  }, _setPrototypeOf(t, e);
}

// node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(t, o) {
  t.prototype = Object.create(o.prototype), t.prototype.constructor = t, _setPrototypeOf(t, o);
}

// node_modules/react-transition-group/esm/CSSTransition.js
var import_prop_types82 = __toESM(require_prop_types());

// node_modules/dom-helpers/esm/hasClass.js
function hasClass(element, className) {
  if (element.classList) return !!className && element.classList.contains(className);
  return (" " + (element.className.baseVal || element.className) + " ").indexOf(" " + className + " ") !== -1;
}

// node_modules/dom-helpers/esm/addClass.js
function addClass(element, className) {
  if (element.classList) element.classList.add(className);
  else if (!hasClass(element, className)) if (typeof element.className === "string") element.className = element.className + " " + className;
  else element.setAttribute("class", (element.className && element.className.baseVal || "") + " " + className);
}

// node_modules/dom-helpers/esm/removeClass.js
function replaceClassName(origClass, classToRemove) {
  return origClass.replace(new RegExp("(^|\\s)" + classToRemove + "(?:\\s|$)", "g"), "$1").replace(/\s+/g, " ").replace(/^\s*|\s*$/g, "");
}
function removeClass(element, className) {
  if (element.classList) {
    element.classList.remove(className);
  } else if (typeof element.className === "string") {
    element.className = replaceClassName(element.className, className);
  } else {
    element.setAttribute("class", replaceClassName(element.className && element.className.baseVal || "", className));
  }
}

// node_modules/react-transition-group/esm/CSSTransition.js
var import_react6 = __toESM(require_react());

// node_modules/react-transition-group/esm/Transition.js
var import_prop_types81 = __toESM(require_prop_types());
var import_react5 = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());

// node_modules/react-transition-group/esm/config.js
var config_default = {
  disabled: false
};

// node_modules/react-transition-group/esm/utils/PropTypes.js
var import_prop_types80 = __toESM(require_prop_types());
var timeoutsShape = true ? import_prop_types80.default.oneOfType([import_prop_types80.default.number, import_prop_types80.default.shape({
  enter: import_prop_types80.default.number,
  exit: import_prop_types80.default.number,
  appear: import_prop_types80.default.number
}).isRequired]) : null;
var classNamesShape = true ? import_prop_types80.default.oneOfType([import_prop_types80.default.string, import_prop_types80.default.shape({
  enter: import_prop_types80.default.string,
  exit: import_prop_types80.default.string,
  active: import_prop_types80.default.string
}), import_prop_types80.default.shape({
  enter: import_prop_types80.default.string,
  enterDone: import_prop_types80.default.string,
  enterActive: import_prop_types80.default.string,
  exit: import_prop_types80.default.string,
  exitDone: import_prop_types80.default.string,
  exitActive: import_prop_types80.default.string
})]) : null;

// node_modules/react-transition-group/esm/TransitionGroupContext.js
var import_react4 = __toESM(require_react());
var TransitionGroupContext_default = import_react4.default.createContext(null);

// node_modules/react-transition-group/esm/utils/reflow.js
var forceReflow = function forceReflow2(node) {
  return node.scrollTop;
};

// node_modules/react-transition-group/esm/Transition.js
var UNMOUNTED = "unmounted";
var EXITED = "exited";
var ENTERING = "entering";
var ENTERED = "entered";
var EXITING = "exiting";
var Transition = (function(_React$Component) {
  _inheritsLoose(Transition2, _React$Component);
  function Transition2(props, context) {
    var _this;
    _this = _React$Component.call(this, props, context) || this;
    var parentGroup = context;
    var appear = parentGroup && !parentGroup.isMounting ? props.enter : props.appear;
    var initialStatus;
    _this.appearStatus = null;
    if (props.in) {
      if (appear) {
        initialStatus = EXITED;
        _this.appearStatus = ENTERING;
      } else {
        initialStatus = ENTERED;
      }
    } else {
      if (props.unmountOnExit || props.mountOnEnter) {
        initialStatus = UNMOUNTED;
      } else {
        initialStatus = EXITED;
      }
    }
    _this.state = {
      status: initialStatus
    };
    _this.nextCallback = null;
    return _this;
  }
  Transition2.getDerivedStateFromProps = function getDerivedStateFromProps(_ref, prevState) {
    var nextIn = _ref.in;
    if (nextIn && prevState.status === UNMOUNTED) {
      return {
        status: EXITED
      };
    }
    return null;
  };
  var _proto = Transition2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.updateStatus(true, this.appearStatus);
  };
  _proto.componentDidUpdate = function componentDidUpdate(prevProps) {
    var nextStatus = null;
    if (prevProps !== this.props) {
      var status = this.state.status;
      if (this.props.in) {
        if (status !== ENTERING && status !== ENTERED) {
          nextStatus = ENTERING;
        }
      } else {
        if (status === ENTERING || status === ENTERED) {
          nextStatus = EXITING;
        }
      }
    }
    this.updateStatus(false, nextStatus);
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    this.cancelNextCallback();
  };
  _proto.getTimeouts = function getTimeouts() {
    var timeout2 = this.props.timeout;
    var exit, enter, appear;
    exit = enter = appear = timeout2;
    if (timeout2 != null && typeof timeout2 !== "number") {
      exit = timeout2.exit;
      enter = timeout2.enter;
      appear = timeout2.appear !== void 0 ? timeout2.appear : enter;
    }
    return {
      exit,
      enter,
      appear
    };
  };
  _proto.updateStatus = function updateStatus(mounting, nextStatus) {
    if (mounting === void 0) {
      mounting = false;
    }
    if (nextStatus !== null) {
      this.cancelNextCallback();
      if (nextStatus === ENTERING) {
        if (this.props.unmountOnExit || this.props.mountOnEnter) {
          var node = this.props.nodeRef ? this.props.nodeRef.current : import_react_dom.default.findDOMNode(this);
          if (node) forceReflow(node);
        }
        this.performEnter(mounting);
      } else {
        this.performExit();
      }
    } else if (this.props.unmountOnExit && this.state.status === EXITED) {
      this.setState({
        status: UNMOUNTED
      });
    }
  };
  _proto.performEnter = function performEnter(mounting) {
    var _this2 = this;
    var enter = this.props.enter;
    var appearing = this.context ? this.context.isMounting : mounting;
    var _ref2 = this.props.nodeRef ? [appearing] : [import_react_dom.default.findDOMNode(this), appearing], maybeNode = _ref2[0], maybeAppearing = _ref2[1];
    var timeouts = this.getTimeouts();
    var enterTimeout = appearing ? timeouts.appear : timeouts.enter;
    if (!mounting && !enter || config_default.disabled) {
      this.safeSetState({
        status: ENTERED
      }, function() {
        _this2.props.onEntered(maybeNode);
      });
      return;
    }
    this.props.onEnter(maybeNode, maybeAppearing);
    this.safeSetState({
      status: ENTERING
    }, function() {
      _this2.props.onEntering(maybeNode, maybeAppearing);
      _this2.onTransitionEnd(enterTimeout, function() {
        _this2.safeSetState({
          status: ENTERED
        }, function() {
          _this2.props.onEntered(maybeNode, maybeAppearing);
        });
      });
    });
  };
  _proto.performExit = function performExit() {
    var _this3 = this;
    var exit = this.props.exit;
    var timeouts = this.getTimeouts();
    var maybeNode = this.props.nodeRef ? void 0 : import_react_dom.default.findDOMNode(this);
    if (!exit || config_default.disabled) {
      this.safeSetState({
        status: EXITED
      }, function() {
        _this3.props.onExited(maybeNode);
      });
      return;
    }
    this.props.onExit(maybeNode);
    this.safeSetState({
      status: EXITING
    }, function() {
      _this3.props.onExiting(maybeNode);
      _this3.onTransitionEnd(timeouts.exit, function() {
        _this3.safeSetState({
          status: EXITED
        }, function() {
          _this3.props.onExited(maybeNode);
        });
      });
    });
  };
  _proto.cancelNextCallback = function cancelNextCallback() {
    if (this.nextCallback !== null) {
      this.nextCallback.cancel();
      this.nextCallback = null;
    }
  };
  _proto.safeSetState = function safeSetState(nextState, callback) {
    callback = this.setNextCallback(callback);
    this.setState(nextState, callback);
  };
  _proto.setNextCallback = function setNextCallback(callback) {
    var _this4 = this;
    var active = true;
    this.nextCallback = function(event) {
      if (active) {
        active = false;
        _this4.nextCallback = null;
        callback(event);
      }
    };
    this.nextCallback.cancel = function() {
      active = false;
    };
    return this.nextCallback;
  };
  _proto.onTransitionEnd = function onTransitionEnd(timeout2, handler) {
    this.setNextCallback(handler);
    var node = this.props.nodeRef ? this.props.nodeRef.current : import_react_dom.default.findDOMNode(this);
    var doesNotHaveTimeoutOrListener = timeout2 == null && !this.props.addEndListener;
    if (!node || doesNotHaveTimeoutOrListener) {
      setTimeout(this.nextCallback, 0);
      return;
    }
    if (this.props.addEndListener) {
      var _ref3 = this.props.nodeRef ? [this.nextCallback] : [node, this.nextCallback], maybeNode = _ref3[0], maybeNextCallback = _ref3[1];
      this.props.addEndListener(maybeNode, maybeNextCallback);
    }
    if (timeout2 != null) {
      setTimeout(this.nextCallback, timeout2);
    }
  };
  _proto.render = function render() {
    var status = this.state.status;
    if (status === UNMOUNTED) {
      return null;
    }
    var _this$props = this.props, children2 = _this$props.children, _in = _this$props.in, _mountOnEnter = _this$props.mountOnEnter, _unmountOnExit = _this$props.unmountOnExit, _appear = _this$props.appear, _enter = _this$props.enter, _exit = _this$props.exit, _timeout = _this$props.timeout, _addEndListener = _this$props.addEndListener, _onEnter = _this$props.onEnter, _onEntering = _this$props.onEntering, _onEntered = _this$props.onEntered, _onExit = _this$props.onExit, _onExiting = _this$props.onExiting, _onExited = _this$props.onExited, _nodeRef = _this$props.nodeRef, childProps = _objectWithoutPropertiesLoose(_this$props, ["children", "in", "mountOnEnter", "unmountOnExit", "appear", "enter", "exit", "timeout", "addEndListener", "onEnter", "onEntering", "onEntered", "onExit", "onExiting", "onExited", "nodeRef"]);
    return (
      // allows for nested Transitions
      import_react5.default.createElement(TransitionGroupContext_default.Provider, {
        value: null
      }, typeof children2 === "function" ? children2(status, childProps) : import_react5.default.cloneElement(import_react5.default.Children.only(children2), childProps))
    );
  };
  return Transition2;
})(import_react5.default.Component);
Transition.contextType = TransitionGroupContext_default;
Transition.propTypes = true ? {
  /**
   * A React reference to DOM element that need to transition:
   * https://stackoverflow.com/a/51127130/4671932
   *
   *   - When `nodeRef` prop is used, `node` is not passed to callback functions
   *      (e.g. `onEnter`) because user already has direct access to the node.
   *   - When changing `key` prop of `Transition` in a `TransitionGroup` a new
   *     `nodeRef` need to be provided to `Transition` with changed `key` prop
   *     (see
   *     [test/CSSTransition-test.js](https://github.com/reactjs/react-transition-group/blob/13435f897b3ab71f6e19d724f145596f5910581c/test/CSSTransition-test.js#L362-L437)).
   */
  nodeRef: import_prop_types81.default.shape({
    current: typeof Element === "undefined" ? import_prop_types81.default.any : function(propValue, key, componentName, location, propFullName, secret) {
      var value = propValue[key];
      return import_prop_types81.default.instanceOf(value && "ownerDocument" in value ? value.ownerDocument.defaultView.Element : Element)(propValue, key, componentName, location, propFullName, secret);
    }
  }),
  /**
   * A `function` child can be used instead of a React element. This function is
   * called with the current transition status (`'entering'`, `'entered'`,
   * `'exiting'`, `'exited'`), which can be used to apply context
   * specific props to a component.
   *
   * ```jsx
   * <Transition in={this.state.in} timeout={150}>
   *   {state => (
   *     <MyComponent className={`fade fade-${state}`} />
   *   )}
   * </Transition>
   * ```
   */
  children: import_prop_types81.default.oneOfType([import_prop_types81.default.func.isRequired, import_prop_types81.default.element.isRequired]).isRequired,
  /**
   * Show the component; triggers the enter or exit states
   */
  in: import_prop_types81.default.bool,
  /**
   * By default the child component is mounted immediately along with
   * the parent `Transition` component. If you want to "lazy mount" the component on the
   * first `in={true}` you can set `mountOnEnter`. After the first enter transition the component will stay
   * mounted, even on "exited", unless you also specify `unmountOnExit`.
   */
  mountOnEnter: import_prop_types81.default.bool,
  /**
   * By default the child component stays mounted after it reaches the `'exited'` state.
   * Set `unmountOnExit` if you'd prefer to unmount the component after it finishes exiting.
   */
  unmountOnExit: import_prop_types81.default.bool,
  /**
   * By default the child component does not perform the enter transition when
   * it first mounts, regardless of the value of `in`. If you want this
   * behavior, set both `appear` and `in` to `true`.
   *
   * > **Note**: there are no special appear states like `appearing`/`appeared`, this prop
   * > only adds an additional enter transition. However, in the
   * > `<CSSTransition>` component that first enter transition does result in
   * > additional `.appear-*` classes, that way you can choose to style it
   * > differently.
   */
  appear: import_prop_types81.default.bool,
  /**
   * Enable or disable enter transitions.
   */
  enter: import_prop_types81.default.bool,
  /**
   * Enable or disable exit transitions.
   */
  exit: import_prop_types81.default.bool,
  /**
   * The duration of the transition, in milliseconds.
   * Required unless `addEndListener` is provided.
   *
   * You may specify a single timeout for all transitions:
   *
   * ```jsx
   * timeout={500}
   * ```
   *
   * or individually:
   *
   * ```jsx
   * timeout={{
   *  appear: 500,
   *  enter: 300,
   *  exit: 500,
   * }}
   * ```
   *
   * - `appear` defaults to the value of `enter`
   * - `enter` defaults to `0`
   * - `exit` defaults to `0`
   *
   * @type {number | { enter?: number, exit?: number, appear?: number }}
   */
  timeout: function timeout(props) {
    var pt = timeoutsShape;
    if (!props.addEndListener) pt = pt.isRequired;
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }
    return pt.apply(void 0, [props].concat(args));
  },
  /**
   * Add a custom transition end trigger. Called with the transitioning
   * DOM node and a `done` callback. Allows for more fine grained transition end
   * logic. Timeouts are still used as a fallback if provided.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * ```jsx
   * addEndListener={(node, done) => {
   *   // use the css transitionend event to mark the finish of a transition
   *   node.addEventListener('transitionend', done, false);
   * }}
   * ```
   */
  addEndListener: import_prop_types81.default.func,
  /**
   * Callback fired before the "entering" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool) -> void
   */
  onEnter: import_prop_types81.default.func,
  /**
   * Callback fired after the "entering" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntering: import_prop_types81.default.func,
  /**
   * Callback fired after the "entered" status is applied. An extra parameter
   * `isAppearing` is supplied to indicate if the enter stage is occurring on the initial mount
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool) -> void
   */
  onEntered: import_prop_types81.default.func,
  /**
   * Callback fired before the "exiting" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExit: import_prop_types81.default.func,
  /**
   * Callback fired after the "exiting" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExiting: import_prop_types81.default.func,
  /**
   * Callback fired after the "exited" status is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement) -> void
   */
  onExited: import_prop_types81.default.func
} : {};
function noop3() {
}
Transition.defaultProps = {
  in: false,
  mountOnEnter: false,
  unmountOnExit: false,
  appear: false,
  enter: true,
  exit: true,
  onEnter: noop3,
  onEntering: noop3,
  onEntered: noop3,
  onExit: noop3,
  onExiting: noop3,
  onExited: noop3
};
Transition.UNMOUNTED = UNMOUNTED;
Transition.EXITED = EXITED;
Transition.ENTERING = ENTERING;
Transition.ENTERED = ENTERED;
Transition.EXITING = EXITING;
var Transition_default = Transition;

// node_modules/react-transition-group/esm/CSSTransition.js
var _addClass = function addClass2(node, classes2) {
  return node && classes2 && classes2.split(" ").forEach(function(c) {
    return addClass(node, c);
  });
};
var removeClass2 = function removeClass3(node, classes2) {
  return node && classes2 && classes2.split(" ").forEach(function(c) {
    return removeClass(node, c);
  });
};
var CSSTransition = (function(_React$Component) {
  _inheritsLoose(CSSTransition2, _React$Component);
  function CSSTransition2() {
    var _this;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
    _this.appliedClasses = {
      appear: {},
      enter: {},
      exit: {}
    };
    _this.onEnter = function(maybeNode, maybeAppearing) {
      var _this$resolveArgument = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument[0], appearing = _this$resolveArgument[1];
      _this.removeClasses(node, "exit");
      _this.addClass(node, appearing ? "appear" : "enter", "base");
      if (_this.props.onEnter) {
        _this.props.onEnter(maybeNode, maybeAppearing);
      }
    };
    _this.onEntering = function(maybeNode, maybeAppearing) {
      var _this$resolveArgument2 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument2[0], appearing = _this$resolveArgument2[1];
      var type = appearing ? "appear" : "enter";
      _this.addClass(node, type, "active");
      if (_this.props.onEntering) {
        _this.props.onEntering(maybeNode, maybeAppearing);
      }
    };
    _this.onEntered = function(maybeNode, maybeAppearing) {
      var _this$resolveArgument3 = _this.resolveArguments(maybeNode, maybeAppearing), node = _this$resolveArgument3[0], appearing = _this$resolveArgument3[1];
      var type = appearing ? "appear" : "enter";
      _this.removeClasses(node, type);
      _this.addClass(node, type, "done");
      if (_this.props.onEntered) {
        _this.props.onEntered(maybeNode, maybeAppearing);
      }
    };
    _this.onExit = function(maybeNode) {
      var _this$resolveArgument4 = _this.resolveArguments(maybeNode), node = _this$resolveArgument4[0];
      _this.removeClasses(node, "appear");
      _this.removeClasses(node, "enter");
      _this.addClass(node, "exit", "base");
      if (_this.props.onExit) {
        _this.props.onExit(maybeNode);
      }
    };
    _this.onExiting = function(maybeNode) {
      var _this$resolveArgument5 = _this.resolveArguments(maybeNode), node = _this$resolveArgument5[0];
      _this.addClass(node, "exit", "active");
      if (_this.props.onExiting) {
        _this.props.onExiting(maybeNode);
      }
    };
    _this.onExited = function(maybeNode) {
      var _this$resolveArgument6 = _this.resolveArguments(maybeNode), node = _this$resolveArgument6[0];
      _this.removeClasses(node, "exit");
      _this.addClass(node, "exit", "done");
      if (_this.props.onExited) {
        _this.props.onExited(maybeNode);
      }
    };
    _this.resolveArguments = function(maybeNode, maybeAppearing) {
      return _this.props.nodeRef ? [_this.props.nodeRef.current, maybeNode] : [maybeNode, maybeAppearing];
    };
    _this.getClassNames = function(type) {
      var classNames = _this.props.classNames;
      var isStringClassNames = typeof classNames === "string";
      var prefix = isStringClassNames && classNames ? classNames + "-" : "";
      var baseClassName = isStringClassNames ? "" + prefix + type : classNames[type];
      var activeClassName = isStringClassNames ? baseClassName + "-active" : classNames[type + "Active"];
      var doneClassName = isStringClassNames ? baseClassName + "-done" : classNames[type + "Done"];
      return {
        baseClassName,
        activeClassName,
        doneClassName
      };
    };
    return _this;
  }
  var _proto = CSSTransition2.prototype;
  _proto.addClass = function addClass3(node, type, phase) {
    var className = this.getClassNames(type)[phase + "ClassName"];
    var _this$getClassNames = this.getClassNames("enter"), doneClassName = _this$getClassNames.doneClassName;
    if (type === "appear" && phase === "done" && doneClassName) {
      className += " " + doneClassName;
    }
    if (phase === "active") {
      if (node) forceReflow(node);
    }
    if (className) {
      this.appliedClasses[type][phase] = className;
      _addClass(node, className);
    }
  };
  _proto.removeClasses = function removeClasses(node, type) {
    var _this$appliedClasses$ = this.appliedClasses[type], baseClassName = _this$appliedClasses$.base, activeClassName = _this$appliedClasses$.active, doneClassName = _this$appliedClasses$.done;
    this.appliedClasses[type] = {};
    if (baseClassName) {
      removeClass2(node, baseClassName);
    }
    if (activeClassName) {
      removeClass2(node, activeClassName);
    }
    if (doneClassName) {
      removeClass2(node, doneClassName);
    }
  };
  _proto.render = function render() {
    var _this$props = this.props, _ = _this$props.classNames, props = _objectWithoutPropertiesLoose(_this$props, ["classNames"]);
    return import_react6.default.createElement(Transition_default, _extends({}, props, {
      onEnter: this.onEnter,
      onEntered: this.onEntered,
      onEntering: this.onEntering,
      onExit: this.onExit,
      onExiting: this.onExiting,
      onExited: this.onExited
    }));
  };
  return CSSTransition2;
})(import_react6.default.Component);
CSSTransition.defaultProps = {
  classNames: ""
};
CSSTransition.propTypes = true ? _extends({}, Transition_default.propTypes, {
  /**
   * The animation classNames applied to the component as it appears, enters,
   * exits or has finished the transition. A single name can be provided, which
   * will be suffixed for each stage, e.g. `classNames="fade"` applies:
   *
   * - `fade-appear`, `fade-appear-active`, `fade-appear-done`
   * - `fade-enter`, `fade-enter-active`, `fade-enter-done`
   * - `fade-exit`, `fade-exit-active`, `fade-exit-done`
   *
   * A few details to note about how these classes are applied:
   *
   * 1. They are _joined_ with the ones that are already defined on the child
   *    component, so if you want to add some base styles, you can use
   *    `className` without worrying that it will be overridden.
   *
   * 2. If the transition component mounts with `in={false}`, no classes are
   *    applied yet. You might be expecting `*-exit-done`, but if you think
   *    about it, a component cannot finish exiting if it hasn't entered yet.
   *
   * 2. `fade-appear-done` and `fade-enter-done` will _both_ be applied. This
   *    allows you to define different behavior for when appearing is done and
   *    when regular entering is done, using selectors like
   *    `.fade-enter-done:not(.fade-appear-done)`. For example, you could apply
   *    an epic entrance animation when element first appears in the DOM using
   *    [Animate.css](https://daneden.github.io/animate.css/). Otherwise you can
   *    simply use `fade-enter-done` for defining both cases.
   *
   * Each individual classNames can also be specified independently like:
   *
   * ```js
   * classNames={{
   *  appear: 'my-appear',
   *  appearActive: 'my-active-appear',
   *  appearDone: 'my-done-appear',
   *  enter: 'my-enter',
   *  enterActive: 'my-active-enter',
   *  enterDone: 'my-done-enter',
   *  exit: 'my-exit',
   *  exitActive: 'my-active-exit',
   *  exitDone: 'my-done-exit',
   * }}
   * ```
   *
   * If you want to set these classes using CSS Modules:
   *
   * ```js
   * import styles from './styles.css';
   * ```
   *
   * you might want to use camelCase in your CSS file, that way could simply
   * spread them instead of listing them one by one:
   *
   * ```js
   * classNames={{ ...styles }}
   * ```
   *
   * @type {string | {
   *  appear?: string,
   *  appearActive?: string,
   *  appearDone?: string,
   *  enter?: string,
   *  enterActive?: string,
   *  enterDone?: string,
   *  exit?: string,
   *  exitActive?: string,
   *  exitDone?: string,
   * }}
   */
  classNames: classNamesShape,
  /**
   * A `<Transition>` callback fired immediately after the 'enter' or 'appear' class is
   * applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEnter: import_prop_types82.default.func,
  /**
   * A `<Transition>` callback fired immediately after the 'enter-active' or
   * 'appear-active' class is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntering: import_prop_types82.default.func,
  /**
   * A `<Transition>` callback fired immediately after the 'enter' or
   * 'appear' classes are **removed** and the `done` class is added to the DOM node.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed.
   *
   * @type Function(node: HtmlElement, isAppearing: bool)
   */
  onEntered: import_prop_types82.default.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit' class is
   * applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExit: import_prop_types82.default.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit-active' is applied.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExiting: import_prop_types82.default.func,
  /**
   * A `<Transition>` callback fired immediately after the 'exit' classes
   * are **removed** and the `exit-done` class is added to the DOM node.
   *
   * **Note**: when `nodeRef` prop is passed, `node` is not passed
   *
   * @type Function(node: HtmlElement)
   */
  onExited: import_prop_types82.default.func
}) : {};

// node_modules/react-transition-group/esm/ReplaceTransition.js
var import_prop_types84 = __toESM(require_prop_types());
var import_react9 = __toESM(require_react());
var import_react_dom2 = __toESM(require_react_dom());

// node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(e) {
  if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}

// node_modules/react-transition-group/esm/TransitionGroup.js
var import_prop_types83 = __toESM(require_prop_types());
var import_react8 = __toESM(require_react());

// node_modules/react-transition-group/esm/utils/ChildMapping.js
var import_react7 = __toESM(require_react());
function getChildMapping(children2, mapFn) {
  var mapper = function mapper2(child) {
    return mapFn && (0, import_react7.isValidElement)(child) ? mapFn(child) : child;
  };
  var result = /* @__PURE__ */ Object.create(null);
  if (children2) import_react7.Children.map(children2, function(c) {
    return c;
  }).forEach(function(child) {
    result[child.key] = mapper(child);
  });
  return result;
}
function mergeChildMappings(prev, next) {
  prev = prev || {};
  next = next || {};
  function getValueForKey(key) {
    return key in next ? next[key] : prev[key];
  }
  var nextKeysPending = /* @__PURE__ */ Object.create(null);
  var pendingKeys = [];
  for (var prevKey in prev) {
    if (prevKey in next) {
      if (pendingKeys.length) {
        nextKeysPending[prevKey] = pendingKeys;
        pendingKeys = [];
      }
    } else {
      pendingKeys.push(prevKey);
    }
  }
  var i2;
  var childMapping = {};
  for (var nextKey in next) {
    if (nextKeysPending[nextKey]) {
      for (i2 = 0; i2 < nextKeysPending[nextKey].length; i2++) {
        var pendingNextKey = nextKeysPending[nextKey][i2];
        childMapping[nextKeysPending[nextKey][i2]] = getValueForKey(pendingNextKey);
      }
    }
    childMapping[nextKey] = getValueForKey(nextKey);
  }
  for (i2 = 0; i2 < pendingKeys.length; i2++) {
    childMapping[pendingKeys[i2]] = getValueForKey(pendingKeys[i2]);
  }
  return childMapping;
}
function getProp(child, prop, props) {
  return props[prop] != null ? props[prop] : child.props[prop];
}
function getInitialChildMapping(props, onExited) {
  return getChildMapping(props.children, function(child) {
    return (0, import_react7.cloneElement)(child, {
      onExited: onExited.bind(null, child),
      in: true,
      appear: getProp(child, "appear", props),
      enter: getProp(child, "enter", props),
      exit: getProp(child, "exit", props)
    });
  });
}
function getNextChildMapping(nextProps, prevChildMapping, onExited) {
  var nextChildMapping = getChildMapping(nextProps.children);
  var children2 = mergeChildMappings(prevChildMapping, nextChildMapping);
  Object.keys(children2).forEach(function(key) {
    var child = children2[key];
    if (!(0, import_react7.isValidElement)(child)) return;
    var hasPrev = key in prevChildMapping;
    var hasNext = key in nextChildMapping;
    var prevChild = prevChildMapping[key];
    var isLeaving = (0, import_react7.isValidElement)(prevChild) && !prevChild.props.in;
    if (hasNext && (!hasPrev || isLeaving)) {
      children2[key] = (0, import_react7.cloneElement)(child, {
        onExited: onExited.bind(null, child),
        in: true,
        exit: getProp(child, "exit", nextProps),
        enter: getProp(child, "enter", nextProps)
      });
    } else if (!hasNext && hasPrev && !isLeaving) {
      children2[key] = (0, import_react7.cloneElement)(child, {
        in: false
      });
    } else if (hasNext && hasPrev && (0, import_react7.isValidElement)(prevChild)) {
      children2[key] = (0, import_react7.cloneElement)(child, {
        onExited: onExited.bind(null, child),
        in: prevChild.props.in,
        exit: getProp(child, "exit", nextProps),
        enter: getProp(child, "enter", nextProps)
      });
    }
  });
  return children2;
}

// node_modules/react-transition-group/esm/TransitionGroup.js
var values2 = Object.values || function(obj) {
  return Object.keys(obj).map(function(k2) {
    return obj[k2];
  });
};
var defaultProps = {
  component: "div",
  childFactory: function childFactory(child) {
    return child;
  }
};
var TransitionGroup = (function(_React$Component) {
  _inheritsLoose(TransitionGroup2, _React$Component);
  function TransitionGroup2(props, context) {
    var _this;
    _this = _React$Component.call(this, props, context) || this;
    var handleExited = _this.handleExited.bind(_assertThisInitialized(_this));
    _this.state = {
      contextValue: {
        isMounting: true
      },
      handleExited,
      firstRender: true
    };
    return _this;
  }
  var _proto = TransitionGroup2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.mounted = true;
    this.setState({
      contextValue: {
        isMounting: false
      }
    });
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    this.mounted = false;
  };
  TransitionGroup2.getDerivedStateFromProps = function getDerivedStateFromProps(nextProps, _ref) {
    var prevChildMapping = _ref.children, handleExited = _ref.handleExited, firstRender = _ref.firstRender;
    return {
      children: firstRender ? getInitialChildMapping(nextProps, handleExited) : getNextChildMapping(nextProps, prevChildMapping, handleExited),
      firstRender: false
    };
  };
  _proto.handleExited = function handleExited(child, node) {
    var currentChildMapping = getChildMapping(this.props.children);
    if (child.key in currentChildMapping) return;
    if (child.props.onExited) {
      child.props.onExited(node);
    }
    if (this.mounted) {
      this.setState(function(state) {
        var children2 = _extends({}, state.children);
        delete children2[child.key];
        return {
          children: children2
        };
      });
    }
  };
  _proto.render = function render() {
    var _this$props = this.props, Component = _this$props.component, childFactory2 = _this$props.childFactory, props = _objectWithoutPropertiesLoose(_this$props, ["component", "childFactory"]);
    var contextValue = this.state.contextValue;
    var children2 = values2(this.state.children).map(childFactory2);
    delete props.appear;
    delete props.enter;
    delete props.exit;
    if (Component === null) {
      return import_react8.default.createElement(TransitionGroupContext_default.Provider, {
        value: contextValue
      }, children2);
    }
    return import_react8.default.createElement(TransitionGroupContext_default.Provider, {
      value: contextValue
    }, import_react8.default.createElement(Component, props, children2));
  };
  return TransitionGroup2;
})(import_react8.default.Component);
TransitionGroup.propTypes = true ? {
  /**
   * `<TransitionGroup>` renders a `<div>` by default. You can change this
   * behavior by providing a `component` prop.
   * If you use React v16+ and would like to avoid a wrapping `<div>` element
   * you can pass in `component={null}`. This is useful if the wrapping div
   * borks your css styles.
   */
  component: import_prop_types83.default.any,
  /**
   * A set of `<Transition>` components, that are toggled `in` and out as they
   * leave. the `<TransitionGroup>` will inject specific transition props, so
   * remember to spread them through if you are wrapping the `<Transition>` as
   * with our `<Fade>` example.
   *
   * While this component is meant for multiple `Transition` or `CSSTransition`
   * children, sometimes you may want to have a single transition child with
   * content that you want to be transitioned out and in when you change it
   * (e.g. routes, images etc.) In that case you can change the `key` prop of
   * the transition child as you change its content, this will cause
   * `TransitionGroup` to transition the child out and back in.
   */
  children: import_prop_types83.default.node,
  /**
   * A convenience prop that enables or disables appear animations
   * for all children. Note that specifying this will override any defaults set
   * on individual children Transitions.
   */
  appear: import_prop_types83.default.bool,
  /**
   * A convenience prop that enables or disables enter animations
   * for all children. Note that specifying this will override any defaults set
   * on individual children Transitions.
   */
  enter: import_prop_types83.default.bool,
  /**
   * A convenience prop that enables or disables exit animations
   * for all children. Note that specifying this will override any defaults set
   * on individual children Transitions.
   */
  exit: import_prop_types83.default.bool,
  /**
   * You may need to apply reactive updates to a child as it is exiting.
   * This is generally done by using `cloneElement` however in the case of an exiting
   * child the element has already been removed and not accessible to the consumer.
   *
   * If you do need to update a child as it leaves you can provide a `childFactory`
   * to wrap every child, even the ones that are leaving.
   *
   * @type Function(child: ReactElement) -> ReactElement
   */
  childFactory: import_prop_types83.default.func
} : {};
TransitionGroup.defaultProps = defaultProps;
var TransitionGroup_default = TransitionGroup;

// node_modules/react-transition-group/esm/ReplaceTransition.js
var ReplaceTransition = (function(_React$Component) {
  _inheritsLoose(ReplaceTransition2, _React$Component);
  function ReplaceTransition2() {
    var _this;
    for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
      _args[_key] = arguments[_key];
    }
    _this = _React$Component.call.apply(_React$Component, [this].concat(_args)) || this;
    _this.handleEnter = function() {
      for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      return _this.handleLifecycle("onEnter", 0, args);
    };
    _this.handleEntering = function() {
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      return _this.handleLifecycle("onEntering", 0, args);
    };
    _this.handleEntered = function() {
      for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        args[_key4] = arguments[_key4];
      }
      return _this.handleLifecycle("onEntered", 0, args);
    };
    _this.handleExit = function() {
      for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
        args[_key5] = arguments[_key5];
      }
      return _this.handleLifecycle("onExit", 1, args);
    };
    _this.handleExiting = function() {
      for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
        args[_key6] = arguments[_key6];
      }
      return _this.handleLifecycle("onExiting", 1, args);
    };
    _this.handleExited = function() {
      for (var _len7 = arguments.length, args = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) {
        args[_key7] = arguments[_key7];
      }
      return _this.handleLifecycle("onExited", 1, args);
    };
    return _this;
  }
  var _proto = ReplaceTransition2.prototype;
  _proto.handleLifecycle = function handleLifecycle(handler, idx, originalArgs) {
    var _child$props;
    var children2 = this.props.children;
    var child = import_react9.default.Children.toArray(children2)[idx];
    if (child.props[handler]) (_child$props = child.props)[handler].apply(_child$props, originalArgs);
    if (this.props[handler]) {
      var maybeNode = child.props.nodeRef ? void 0 : import_react_dom2.default.findDOMNode(this);
      this.props[handler](maybeNode);
    }
  };
  _proto.render = function render() {
    var _this$props = this.props, children2 = _this$props.children, inProp = _this$props.in, props = _objectWithoutPropertiesLoose(_this$props, ["children", "in"]);
    var _React$Children$toArr = import_react9.default.Children.toArray(children2), first = _React$Children$toArr[0], second = _React$Children$toArr[1];
    delete props.onEnter;
    delete props.onEntering;
    delete props.onEntered;
    delete props.onExit;
    delete props.onExiting;
    delete props.onExited;
    return import_react9.default.createElement(TransitionGroup_default, props, inProp ? import_react9.default.cloneElement(first, {
      key: "first",
      onEnter: this.handleEnter,
      onEntering: this.handleEntering,
      onEntered: this.handleEntered
    }) : import_react9.default.cloneElement(second, {
      key: "second",
      onEnter: this.handleExit,
      onEntering: this.handleExiting,
      onEntered: this.handleExited
    }));
  };
  return ReplaceTransition2;
})(import_react9.default.Component);
ReplaceTransition.propTypes = true ? {
  in: import_prop_types84.default.bool.isRequired,
  children: function children(props, propName) {
    if (import_react9.default.Children.count(props[propName]) !== 2) return new Error('"' + propName + '" must be exactly two transition components.');
    return null;
  }
} : {};

// node_modules/react-transition-group/esm/SwitchTransition.js
var import_react10 = __toESM(require_react());
var import_prop_types85 = __toESM(require_prop_types());
var _leaveRenders;
var _enterRenders;
function areChildrenDifferent(oldChildren, newChildren) {
  if (oldChildren === newChildren) return false;
  if (import_react10.default.isValidElement(oldChildren) && import_react10.default.isValidElement(newChildren) && oldChildren.key != null && oldChildren.key === newChildren.key) {
    return false;
  }
  return true;
}
var modes = {
  out: "out-in",
  in: "in-out"
};
var callHook = function callHook2(element, name, cb) {
  return function() {
    var _element$props;
    element.props[name] && (_element$props = element.props)[name].apply(_element$props, arguments);
    cb();
  };
};
var leaveRenders = (_leaveRenders = {}, _leaveRenders[modes.out] = function(_ref) {
  var current = _ref.current, changeState = _ref.changeState;
  return import_react10.default.cloneElement(current, {
    in: false,
    onExited: callHook(current, "onExited", function() {
      changeState(ENTERING, null);
    })
  });
}, _leaveRenders[modes.in] = function(_ref2) {
  var current = _ref2.current, changeState = _ref2.changeState, children2 = _ref2.children;
  return [current, import_react10.default.cloneElement(children2, {
    in: true,
    onEntered: callHook(children2, "onEntered", function() {
      changeState(ENTERING);
    })
  })];
}, _leaveRenders);
var enterRenders = (_enterRenders = {}, _enterRenders[modes.out] = function(_ref3) {
  var children2 = _ref3.children, changeState = _ref3.changeState;
  return import_react10.default.cloneElement(children2, {
    in: true,
    onEntered: callHook(children2, "onEntered", function() {
      changeState(ENTERED, import_react10.default.cloneElement(children2, {
        in: true
      }));
    })
  });
}, _enterRenders[modes.in] = function(_ref4) {
  var current = _ref4.current, children2 = _ref4.children, changeState = _ref4.changeState;
  return [import_react10.default.cloneElement(current, {
    in: false,
    onExited: callHook(current, "onExited", function() {
      changeState(ENTERED, import_react10.default.cloneElement(children2, {
        in: true
      }));
    })
  }), import_react10.default.cloneElement(children2, {
    in: true
  })];
}, _enterRenders);
var SwitchTransition = (function(_React$Component) {
  _inheritsLoose(SwitchTransition2, _React$Component);
  function SwitchTransition2() {
    var _this;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
    _this.state = {
      status: ENTERED,
      current: null
    };
    _this.appeared = false;
    _this.changeState = function(status, current) {
      if (current === void 0) {
        current = _this.state.current;
      }
      _this.setState({
        status,
        current
      });
    };
    return _this;
  }
  var _proto = SwitchTransition2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.appeared = true;
  };
  SwitchTransition2.getDerivedStateFromProps = function getDerivedStateFromProps(props, state) {
    if (props.children == null) {
      return {
        current: null
      };
    }
    if (state.status === ENTERING && props.mode === modes.in) {
      return {
        status: ENTERING
      };
    }
    if (state.current && areChildrenDifferent(state.current, props.children)) {
      return {
        status: EXITING
      };
    }
    return {
      current: import_react10.default.cloneElement(props.children, {
        in: true
      })
    };
  };
  _proto.render = function render() {
    var _this$props = this.props, children2 = _this$props.children, mode = _this$props.mode, _this$state = this.state, status = _this$state.status, current = _this$state.current;
    var data = {
      children: children2,
      current,
      changeState: this.changeState,
      status
    };
    var component;
    switch (status) {
      case ENTERING:
        component = enterRenders[mode](data);
        break;
      case EXITING:
        component = leaveRenders[mode](data);
        break;
      case ENTERED:
        component = current;
    }
    return import_react10.default.createElement(TransitionGroupContext_default.Provider, {
      value: {
        isMounting: !this.appeared
      }
    }, component);
  };
  return SwitchTransition2;
})(import_react10.default.Component);
SwitchTransition.propTypes = true ? {
  /**
   * Transition modes.
   * `out-in`: Current element transitions out first, then when complete, the new element transitions in.
   * `in-out`: New element transitions in first, then when complete, the current element transitions out.
   *
   * @type {'out-in'|'in-out'}
   */
  mode: import_prop_types85.default.oneOf([modes.in, modes.out]),
  /**
   * Any `Transition` or `CSSTransition` component.
   */
  children: import_prop_types85.default.oneOfType([import_prop_types85.default.element.isRequired])
} : {};
SwitchTransition.defaultProps = {
  mode: modes.out
};

// node_modules/@mui/material/esm/ButtonBase/Ripple.js
var React143 = __toESM(require_react(), 1);
var import_prop_types86 = __toESM(require_prop_types(), 1);
var import_jsx_runtime108 = __toESM(require_jsx_runtime(), 1);
function Ripple(props) {
  const {
    className,
    classes: classes2,
    pulsate = false,
    rippleX,
    rippleY,
    rippleSize,
    in: inProp,
    onExited,
    timeout: timeout2
  } = props;
  const [leaving, setLeaving] = React143.useState(false);
  const rippleClassName = clsx_default(className, classes2.ripple, classes2.rippleVisible, pulsate && classes2.ripplePulsate);
  const rippleStyles = {
    width: rippleSize,
    height: rippleSize,
    top: -(rippleSize / 2) + rippleY,
    left: -(rippleSize / 2) + rippleX
  };
  const childClassName = clsx_default(classes2.child, leaving && classes2.childLeaving, pulsate && classes2.childPulsate);
  if (!inProp && !leaving) {
    setLeaving(true);
  }
  React143.useEffect(() => {
    if (!inProp && onExited != null) {
      const timeoutId = setTimeout(onExited, timeout2);
      return () => {
        clearTimeout(timeoutId);
      };
    }
    return void 0;
  }, [onExited, inProp, timeout2]);
  return (0, import_jsx_runtime108.jsx)("span", {
    className: rippleClassName,
    style: rippleStyles,
    children: (0, import_jsx_runtime108.jsx)("span", {
      className: childClassName
    })
  });
}
true ? Ripple.propTypes = {
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types86.default.object.isRequired,
  className: import_prop_types86.default.string,
  /**
   * @ignore - injected from TransitionGroup
   */
  in: import_prop_types86.default.bool,
  /**
   * @ignore - injected from TransitionGroup
   */
  onExited: import_prop_types86.default.func,
  /**
   * If `true`, the ripple pulsates, typically indicating the keyboard focus state of an element.
   */
  pulsate: import_prop_types86.default.bool,
  /**
   * Diameter of the ripple.
   */
  rippleSize: import_prop_types86.default.number,
  /**
   * Horizontal position of the ripple center.
   */
  rippleX: import_prop_types86.default.number,
  /**
   * Vertical position of the ripple center.
   */
  rippleY: import_prop_types86.default.number,
  /**
   * exit delay
   */
  timeout: import_prop_types86.default.number.isRequired
} : void 0;
var Ripple_default = Ripple;

// node_modules/@mui/material/esm/ButtonBase/touchRippleClasses.js
var touchRippleClasses = generateUtilityClasses("MuiTouchRipple", ["root", "ripple", "rippleVisible", "ripplePulsate", "child", "childLeaving", "childPulsate"]);
var touchRippleClasses_default = touchRippleClasses;

// node_modules/@mui/material/esm/ButtonBase/TouchRipple.js
var import_jsx_runtime109 = __toESM(require_jsx_runtime(), 1);
var DURATION = 550;
var DELAY_RIPPLE = 80;
var enterKeyframe = keyframes`
  0% {
    transform: scale(0);
    opacity: 0.1;
  }

  100% {
    transform: scale(1);
    opacity: 0.3;
  }
`;
var exitKeyframe = keyframes`
  0% {
    opacity: 1;
  }

  100% {
    opacity: 0;
  }
`;
var pulsateKeyframe = keyframes`
  0% {
    transform: scale(1);
  }

  50% {
    transform: scale(0.92);
  }

  100% {
    transform: scale(1);
  }
`;
var TouchRippleRoot = styled_default2("span", {
  name: "MuiTouchRipple",
  slot: "Root"
})({
  overflow: "hidden",
  pointerEvents: "none",
  position: "absolute",
  zIndex: 0,
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
  borderRadius: "inherit"
});
var TouchRippleRipple = styled_default2(Ripple_default, {
  name: "MuiTouchRipple",
  slot: "Ripple"
})`
  opacity: 0;
  position: absolute;

  &.${touchRippleClasses_default.rippleVisible} {
    opacity: 0.3;
    transform: scale(1);
    animation-name: ${enterKeyframe};
    animation-duration: ${DURATION}ms;
    animation-timing-function: ${({
  theme
}) => theme.transitions.easing.easeInOut};
  }

  &.${touchRippleClasses_default.ripplePulsate} {
    animation-duration: ${({
  theme
}) => theme.transitions.duration.shorter}ms;
  }

  & .${touchRippleClasses_default.child} {
    opacity: 1;
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background-color: currentColor;
  }

  & .${touchRippleClasses_default.childLeaving} {
    opacity: 0;
    animation-name: ${exitKeyframe};
    animation-duration: ${DURATION}ms;
    animation-timing-function: ${({
  theme
}) => theme.transitions.easing.easeInOut};
  }

  & .${touchRippleClasses_default.childPulsate} {
    position: absolute;
    /* @noflip */
    left: 0px;
    top: 0;
    animation-name: ${pulsateKeyframe};
    animation-duration: 2500ms;
    animation-timing-function: ${({
  theme
}) => theme.transitions.easing.easeInOut};
    animation-iteration-count: infinite;
    animation-delay: 200ms;
  }
`;
var TouchRipple = React144.forwardRef(function TouchRipple2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTouchRipple"
  });
  const {
    center: centerProp = false,
    classes: classes2 = {},
    className,
    ...other
  } = props;
  const [ripples, setRipples] = React144.useState([]);
  const nextKey = React144.useRef(0);
  const rippleCallback = React144.useRef(null);
  React144.useEffect(() => {
    if (rippleCallback.current) {
      rippleCallback.current();
      rippleCallback.current = null;
    }
  }, [ripples]);
  const ignoringMouseDown = React144.useRef(false);
  const startTimer = useTimeout();
  const startTimerCommit = React144.useRef(null);
  const container = React144.useRef(null);
  const startCommit = React144.useCallback((params) => {
    const {
      pulsate: pulsate2,
      rippleX,
      rippleY,
      rippleSize,
      cb
    } = params;
    setRipples((oldRipples) => [...oldRipples, (0, import_jsx_runtime109.jsx)(TouchRippleRipple, {
      classes: {
        ripple: clsx_default(classes2.ripple, touchRippleClasses_default.ripple),
        rippleVisible: clsx_default(classes2.rippleVisible, touchRippleClasses_default.rippleVisible),
        ripplePulsate: clsx_default(classes2.ripplePulsate, touchRippleClasses_default.ripplePulsate),
        child: clsx_default(classes2.child, touchRippleClasses_default.child),
        childLeaving: clsx_default(classes2.childLeaving, touchRippleClasses_default.childLeaving),
        childPulsate: clsx_default(classes2.childPulsate, touchRippleClasses_default.childPulsate)
      },
      timeout: DURATION,
      pulsate: pulsate2,
      rippleX,
      rippleY,
      rippleSize
    }, nextKey.current)]);
    nextKey.current += 1;
    rippleCallback.current = cb;
  }, [classes2]);
  const start2 = React144.useCallback((event = {}, options3 = {}, cb = () => {
  }) => {
    const {
      pulsate: pulsate2 = false,
      center = centerProp || options3.pulsate,
      fakeElement = false
      // For test purposes
    } = options3;
    if ((event == null ? void 0 : event.type) === "mousedown" && ignoringMouseDown.current) {
      ignoringMouseDown.current = false;
      return;
    }
    if ((event == null ? void 0 : event.type) === "touchstart") {
      ignoringMouseDown.current = true;
    }
    const element = fakeElement ? null : container.current;
    const rect = element ? element.getBoundingClientRect() : {
      width: 0,
      height: 0,
      left: 0,
      top: 0
    };
    let rippleX;
    let rippleY;
    let rippleSize;
    if (center || event === void 0 || event.clientX === 0 && event.clientY === 0 || !event.clientX && !event.touches) {
      rippleX = Math.round(rect.width / 2);
      rippleY = Math.round(rect.height / 2);
    } else {
      const {
        clientX,
        clientY
      } = event.touches && event.touches.length > 0 ? event.touches[0] : event;
      rippleX = Math.round(clientX - rect.left);
      rippleY = Math.round(clientY - rect.top);
    }
    if (center) {
      rippleSize = Math.sqrt((2 * rect.width ** 2 + rect.height ** 2) / 3);
      if (rippleSize % 2 === 0) {
        rippleSize += 1;
      }
    } else {
      const sizeX = Math.max(Math.abs((element ? element.clientWidth : 0) - rippleX), rippleX) * 2 + 2;
      const sizeY = Math.max(Math.abs((element ? element.clientHeight : 0) - rippleY), rippleY) * 2 + 2;
      rippleSize = Math.sqrt(sizeX ** 2 + sizeY ** 2);
    }
    if (event == null ? void 0 : event.touches) {
      if (startTimerCommit.current === null) {
        startTimerCommit.current = () => {
          startCommit({
            pulsate: pulsate2,
            rippleX,
            rippleY,
            rippleSize,
            cb
          });
        };
        startTimer.start(DELAY_RIPPLE, () => {
          if (startTimerCommit.current) {
            startTimerCommit.current();
            startTimerCommit.current = null;
          }
        });
      }
    } else {
      startCommit({
        pulsate: pulsate2,
        rippleX,
        rippleY,
        rippleSize,
        cb
      });
    }
  }, [centerProp, startCommit, startTimer]);
  const pulsate = React144.useCallback(() => {
    start2({}, {
      pulsate: true
    });
  }, [start2]);
  const stop = React144.useCallback((event, cb) => {
    startTimer.clear();
    if ((event == null ? void 0 : event.type) === "touchend" && startTimerCommit.current) {
      startTimerCommit.current();
      startTimerCommit.current = null;
      startTimer.start(0, () => {
        stop(event, cb);
      });
      return;
    }
    startTimerCommit.current = null;
    setRipples((oldRipples) => {
      if (oldRipples.length > 0) {
        return oldRipples.slice(1);
      }
      return oldRipples;
    });
    rippleCallback.current = cb;
  }, [startTimer]);
  React144.useImperativeHandle(ref, () => ({
    pulsate,
    start: start2,
    stop
  }), [pulsate, start2, stop]);
  return (0, import_jsx_runtime109.jsx)(TouchRippleRoot, {
    className: clsx_default(touchRippleClasses_default.root, classes2.root, className),
    ref: container,
    ...other,
    children: (0, import_jsx_runtime109.jsx)(TransitionGroup_default, {
      component: null,
      exit: true,
      children: ripples
    })
  });
});
true ? TouchRipple.propTypes = {
  /**
   * If `true`, the ripple starts at the center of the component
   * rather than at the point of interaction.
   */
  center: import_prop_types87.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types87.default.object,
  /**
   * @ignore
   */
  className: import_prop_types87.default.string
} : void 0;
var TouchRipple_default = TouchRipple;

// node_modules/@mui/material/esm/ButtonBase/buttonBaseClasses.js
function getButtonBaseUtilityClass(slot) {
  return generateUtilityClass("MuiButtonBase", slot);
}
var buttonBaseClasses = generateUtilityClasses("MuiButtonBase", ["root", "disabled", "focusVisible"]);
var buttonBaseClasses_default = buttonBaseClasses;

// node_modules/@mui/material/esm/ButtonBase/ButtonBase.js
var import_jsx_runtime110 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses50 = (ownerState) => {
  const {
    disabled,
    focusVisible,
    focusVisibleClassName,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", disabled && "disabled", focusVisible && "focusVisible"]
  };
  const composedClasses = composeClasses(slots, getButtonBaseUtilityClass, classes2);
  if (focusVisible && focusVisibleClassName) {
    composedClasses.root += ` ${focusVisibleClassName}`;
  }
  return composedClasses;
};
var ButtonBaseRoot = styled_default2("button", {
  name: "MuiButtonBase",
  slot: "Root"
})({
  display: "inline-flex",
  alignItems: "center",
  justifyContent: "center",
  position: "relative",
  boxSizing: "border-box",
  WebkitTapHighlightColor: "transparent",
  backgroundColor: "transparent",
  // Reset default value
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0,
  border: 0,
  margin: 0,
  // Remove the margin in Safari
  borderRadius: 0,
  padding: 0,
  // Remove the padding in Firefox
  cursor: "pointer",
  userSelect: "none",
  verticalAlign: "middle",
  MozAppearance: "none",
  // Reset
  WebkitAppearance: "none",
  // Reset
  textDecoration: "none",
  // So we take precedent over the style of a native <a /> element.
  color: "inherit",
  "&::-moz-focus-inner": {
    borderStyle: "none"
    // Remove Firefox dotted outline.
  },
  [`&.${buttonBaseClasses_default.disabled}`]: {
    pointerEvents: "none",
    // Disable link interactions
    cursor: "default"
  },
  "@media print": {
    colorAdjust: "exact"
  }
});
var ButtonBase = React145.forwardRef(function ButtonBase2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiButtonBase"
  });
  const {
    action,
    centerRipple = false,
    children: children2,
    className,
    component = "button",
    disabled = false,
    disableRipple = false,
    disableTouchRipple = false,
    focusRipple = false,
    focusVisibleClassName,
    LinkComponent = "a",
    onBlur,
    onClick,
    onContextMenu,
    onDragLeave,
    onFocus,
    onFocusVisible,
    onKeyDown,
    onKeyUp,
    onMouseDown,
    onMouseLeave,
    onMouseUp,
    onTouchEnd,
    onTouchMove,
    onTouchStart,
    tabIndex = 0,
    TouchRippleProps,
    touchRippleRef,
    type,
    ...other
  } = props;
  const buttonRef = React145.useRef(null);
  const ripple = useLazyRipple();
  const handleRippleRef = useForkRef_default(ripple.ref, touchRippleRef);
  const [focusVisible, setFocusVisible] = React145.useState(false);
  if (disabled && focusVisible) {
    setFocusVisible(false);
  }
  React145.useImperativeHandle(action, () => ({
    focusVisible: () => {
      setFocusVisible(true);
      buttonRef.current.focus();
    }
  }), []);
  const enableTouchRipple = ripple.shouldMount && !disableRipple && !disabled;
  React145.useEffect(() => {
    if (focusVisible && focusRipple && !disableRipple) {
      ripple.pulsate();
    }
  }, [disableRipple, focusRipple, focusVisible, ripple]);
  const handleMouseDown = useRippleHandler(ripple, "start", onMouseDown, disableTouchRipple);
  const handleContextMenu = useRippleHandler(ripple, "stop", onContextMenu, disableTouchRipple);
  const handleDragLeave = useRippleHandler(ripple, "stop", onDragLeave, disableTouchRipple);
  const handleMouseUp = useRippleHandler(ripple, "stop", onMouseUp, disableTouchRipple);
  const handleMouseLeave = useRippleHandler(ripple, "stop", (event) => {
    if (focusVisible) {
      event.preventDefault();
    }
    if (onMouseLeave) {
      onMouseLeave(event);
    }
  }, disableTouchRipple);
  const handleTouchStart = useRippleHandler(ripple, "start", onTouchStart, disableTouchRipple);
  const handleTouchEnd = useRippleHandler(ripple, "stop", onTouchEnd, disableTouchRipple);
  const handleTouchMove = useRippleHandler(ripple, "stop", onTouchMove, disableTouchRipple);
  const handleBlur = useRippleHandler(ripple, "stop", (event) => {
    if (!isFocusVisible(event.target)) {
      setFocusVisible(false);
    }
    if (onBlur) {
      onBlur(event);
    }
  }, false);
  const handleFocus = useEventCallback_default2((event) => {
    if (!buttonRef.current) {
      buttonRef.current = event.currentTarget;
    }
    if (isFocusVisible(event.target)) {
      setFocusVisible(true);
      if (onFocusVisible) {
        onFocusVisible(event);
      }
    }
    if (onFocus) {
      onFocus(event);
    }
  });
  const isNonNativeButton = () => {
    const button = buttonRef.current;
    return component && component !== "button" && !(button.tagName === "A" && button.href);
  };
  const handleKeyDown = useEventCallback_default2((event) => {
    if (focusRipple && !event.repeat && focusVisible && event.key === " ") {
      ripple.stop(event, () => {
        ripple.start(event);
      });
    }
    if (event.target === event.currentTarget && isNonNativeButton() && event.key === " ") {
      event.preventDefault();
    }
    if (onKeyDown) {
      onKeyDown(event);
    }
    if (event.target === event.currentTarget && isNonNativeButton() && event.key === "Enter" && !disabled) {
      event.preventDefault();
      if (onClick) {
        onClick(event);
      }
    }
  });
  const handleKeyUp = useEventCallback_default2((event) => {
    if (focusRipple && event.key === " " && focusVisible && !event.defaultPrevented) {
      ripple.stop(event, () => {
        ripple.pulsate(event);
      });
    }
    if (onKeyUp) {
      onKeyUp(event);
    }
    if (onClick && event.target === event.currentTarget && isNonNativeButton() && event.key === " " && !event.defaultPrevented) {
      onClick(event);
    }
  });
  let ComponentProp = component;
  if (ComponentProp === "button" && (other.href || other.to)) {
    ComponentProp = LinkComponent;
  }
  const buttonProps = {};
  if (ComponentProp === "button") {
    const hasFormAttributes = !!other.formAction;
    buttonProps.type = type === void 0 && !hasFormAttributes ? "button" : type;
    buttonProps.disabled = disabled;
  } else {
    if (!other.href && !other.to) {
      buttonProps.role = "button";
    }
    if (disabled) {
      buttonProps["aria-disabled"] = disabled;
    }
  }
  const handleRef = useForkRef_default(ref, buttonRef);
  const ownerState = {
    ...props,
    centerRipple,
    component,
    disabled,
    disableRipple,
    disableTouchRipple,
    focusRipple,
    tabIndex,
    focusVisible
  };
  const classes2 = useUtilityClasses50(ownerState);
  return (0, import_jsx_runtime110.jsxs)(ButtonBaseRoot, {
    as: ComponentProp,
    className: clsx_default(classes2.root, className),
    ownerState,
    onBlur: handleBlur,
    onClick,
    onContextMenu: handleContextMenu,
    onFocus: handleFocus,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    onMouseDown: handleMouseDown,
    onMouseLeave: handleMouseLeave,
    onMouseUp: handleMouseUp,
    onDragLeave: handleDragLeave,
    onTouchEnd: handleTouchEnd,
    onTouchMove: handleTouchMove,
    onTouchStart: handleTouchStart,
    ref: handleRef,
    tabIndex: disabled ? -1 : tabIndex,
    type,
    ...buttonProps,
    ...other,
    children: [children2, enableTouchRipple ? (0, import_jsx_runtime110.jsx)(TouchRipple_default, {
      ref: handleRippleRef,
      center: centerRipple,
      ...TouchRippleProps
    }) : null]
  });
});
function useRippleHandler(ripple, rippleAction, eventCallback, skipRippleAction = false) {
  return useEventCallback_default2((event) => {
    if (eventCallback) {
      eventCallback(event);
    }
    if (!skipRippleAction) {
      ripple[rippleAction](event);
    }
    return true;
  });
}
true ? ButtonBase.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * A ref for imperative actions.
   * It currently only supports `focusVisible()` action.
   */
  action: refType_default,
  /**
   * If `true`, the ripples are centered.
   * They won't start at the cursor interaction position.
   * @default false
   */
  centerRipple: import_prop_types88.default.bool,
  /**
   * The content of the component.
   */
  children: import_prop_types88.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types88.default.object,
  /**
   * @ignore
   */
  className: import_prop_types88.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: elementTypeAcceptingRef_default,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types88.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   *
   * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
   * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
   * @default false
   */
  disableRipple: import_prop_types88.default.bool,
  /**
   * If `true`, the touch ripple effect is disabled.
   * @default false
   */
  disableTouchRipple: import_prop_types88.default.bool,
  /**
   * If `true`, the base button will have a keyboard focus ripple.
   * @default false
   */
  focusRipple: import_prop_types88.default.bool,
  /**
   * This prop can help identify which element has keyboard focus.
   * The class name will be applied when the element gains the focus through keyboard interaction.
   * It's a polyfill for the [CSS :focus-visible selector](https://drafts.csswg.org/selectors-4/#the-focus-visible-pseudo).
   * The rationale for using this feature [is explained here](https://github.com/WICG/focus-visible/blob/HEAD/explainer.md).
   * A [polyfill can be used](https://github.com/WICG/focus-visible) to apply a `focus-visible` class to other components
   * if needed.
   */
  focusVisibleClassName: import_prop_types88.default.string,
  /**
   * @ignore
   */
  formAction: import_prop_types88.default.oneOfType([import_prop_types88.default.func, import_prop_types88.default.string]),
  /**
   * @ignore
   */
  href: import_prop_types88.default.any,
  /**
   * The component used to render a link when the `href` prop is provided.
   * @default 'a'
   */
  LinkComponent: import_prop_types88.default.elementType,
  /**
   * @ignore
   */
  onBlur: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onClick: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onContextMenu: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onDragLeave: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types88.default.func,
  /**
   * Callback fired when the component is focused with a keyboard.
   * We trigger a `onFocus` callback too.
   */
  onFocusVisible: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onKeyDown: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onKeyUp: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onMouseDown: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onMouseLeave: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onMouseUp: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onTouchEnd: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onTouchMove: import_prop_types88.default.func,
  /**
   * @ignore
   */
  onTouchStart: import_prop_types88.default.func,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types88.default.oneOfType([import_prop_types88.default.arrayOf(import_prop_types88.default.oneOfType([import_prop_types88.default.func, import_prop_types88.default.object, import_prop_types88.default.bool])), import_prop_types88.default.func, import_prop_types88.default.object]),
  /**
   * @default 0
   */
  tabIndex: import_prop_types88.default.number,
  /**
   * Props applied to the `TouchRipple` element.
   */
  TouchRippleProps: import_prop_types88.default.object,
  /**
   * A ref that points to the `TouchRipple` element.
   */
  touchRippleRef: import_prop_types88.default.oneOfType([import_prop_types88.default.func, import_prop_types88.default.shape({
    current: import_prop_types88.default.shape({
      pulsate: import_prop_types88.default.func.isRequired,
      start: import_prop_types88.default.func.isRequired,
      stop: import_prop_types88.default.func.isRequired
    })
  })]),
  /**
   * @ignore
   */
  type: import_prop_types88.default.oneOfType([import_prop_types88.default.oneOf(["button", "reset", "submit"]), import_prop_types88.default.string])
} : void 0;
var ButtonBase_default = ButtonBase;

// node_modules/@mui/material/esm/CircularProgress/CircularProgress.js
var React146 = __toESM(require_react(), 1);
var import_prop_types89 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/CircularProgress/circularProgressClasses.js
function getCircularProgressUtilityClass(slot) {
  return generateUtilityClass("MuiCircularProgress", slot);
}
var circularProgressClasses = generateUtilityClasses("MuiCircularProgress", ["root", "determinate", "indeterminate", "colorPrimary", "colorSecondary", "svg", "track", "circle", "circleDeterminate", "circleIndeterminate", "circleDisableShrink"]);

// node_modules/@mui/material/esm/CircularProgress/CircularProgress.js
var import_jsx_runtime111 = __toESM(require_jsx_runtime(), 1);
var SIZE = 44;
var circularRotateKeyframe = keyframes`
  0% {
    transform: rotate(0deg);
  }

  100% {
    transform: rotate(360deg);
  }
`;
var circularDashKeyframe = keyframes`
  0% {
    stroke-dasharray: 1px, 200px;
    stroke-dashoffset: 0;
  }

  50% {
    stroke-dasharray: 100px, 200px;
    stroke-dashoffset: -15px;
  }

  100% {
    stroke-dasharray: 1px, 200px;
    stroke-dashoffset: -126px;
  }
`;
var rotateAnimation = typeof circularRotateKeyframe !== "string" ? css`
        animation: ${circularRotateKeyframe} 1.4s linear infinite;
      ` : null;
var dashAnimation = typeof circularDashKeyframe !== "string" ? css`
        animation: ${circularDashKeyframe} 1.4s ease-in-out infinite;
      ` : null;
var useUtilityClasses51 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    color,
    disableShrink
  } = ownerState;
  const slots = {
    root: ["root", variant, `color${capitalize_default(color)}`],
    svg: ["svg"],
    track: ["track"],
    circle: ["circle", `circle${capitalize_default(variant)}`, disableShrink && "circleDisableShrink"]
  };
  return composeClasses(slots, getCircularProgressUtilityClass, classes2);
};
var CircularProgressRoot = styled_default2("span", {
  name: "MuiCircularProgress",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[ownerState.variant], styles5[`color${capitalize_default(ownerState.color)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  display: "inline-block",
  variants: [{
    props: {
      variant: "determinate"
    },
    style: {
      transition: theme.transitions.create("transform")
    }
  }, {
    props: {
      variant: "indeterminate"
    },
    style: rotateAnimation || {
      animation: `${circularRotateKeyframe} 1.4s linear infinite`
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      color: (theme.vars || theme).palette[color].main
    }
  }))]
})));
var CircularProgressSVG = styled_default2("svg", {
  name: "MuiCircularProgress",
  slot: "Svg"
})({
  display: "block"
  // Keeps the progress centered
});
var CircularProgressCircle = styled_default2("circle", {
  name: "MuiCircularProgress",
  slot: "Circle",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.circle, styles5[`circle${capitalize_default(ownerState.variant)}`], ownerState.disableShrink && styles5.circleDisableShrink];
  }
})(memoTheme_default(({
  theme
}) => ({
  stroke: "currentColor",
  variants: [{
    props: {
      variant: "determinate"
    },
    style: {
      transition: theme.transitions.create("stroke-dashoffset")
    }
  }, {
    props: {
      variant: "indeterminate"
    },
    style: {
      // Some default value that looks fine waiting for the animation to kicks in.
      strokeDasharray: "80px, 200px",
      strokeDashoffset: 0
      // Add the unit to fix a Edge 16 and below bug.
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.variant === "indeterminate" && !ownerState.disableShrink,
    style: dashAnimation || {
      // At runtime for Pigment CSS, `bufferAnimation` will be null and the generated keyframe will be used.
      animation: `${circularDashKeyframe} 1.4s ease-in-out infinite`
    }
  }]
})));
var CircularProgressTrack = styled_default2("circle", {
  name: "MuiCircularProgress",
  slot: "Track"
})(memoTheme_default(({
  theme
}) => ({
  stroke: "currentColor",
  opacity: (theme.vars || theme).palette.action.activatedOpacity
})));
var CircularProgress = React146.forwardRef(function CircularProgress2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiCircularProgress"
  });
  const {
    className,
    color = "primary",
    disableShrink = false,
    enableTrackSlot = false,
    size = 40,
    style,
    thickness = 3.6,
    value = 0,
    variant = "indeterminate",
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    disableShrink,
    size,
    thickness,
    value,
    variant,
    enableTrackSlot
  };
  const classes2 = useUtilityClasses51(ownerState);
  const circleStyle = {};
  const rootStyle = {};
  const rootProps = {};
  if (variant === "determinate") {
    const circumference = 2 * Math.PI * ((SIZE - thickness) / 2);
    circleStyle.strokeDasharray = circumference.toFixed(3);
    rootProps["aria-valuenow"] = Math.round(value);
    circleStyle.strokeDashoffset = `${((100 - value) / 100 * circumference).toFixed(3)}px`;
    rootStyle.transform = "rotate(-90deg)";
  }
  return (0, import_jsx_runtime111.jsx)(CircularProgressRoot, {
    className: clsx_default(classes2.root, className),
    style: {
      width: size,
      height: size,
      ...rootStyle,
      ...style
    },
    ownerState,
    ref,
    role: "progressbar",
    ...rootProps,
    ...other,
    children: (0, import_jsx_runtime111.jsxs)(CircularProgressSVG, {
      className: classes2.svg,
      ownerState,
      viewBox: `${SIZE / 2} ${SIZE / 2} ${SIZE} ${SIZE}`,
      children: [enableTrackSlot ? (0, import_jsx_runtime111.jsx)(CircularProgressTrack, {
        className: classes2.track,
        ownerState,
        cx: SIZE,
        cy: SIZE,
        r: (SIZE - thickness) / 2,
        fill: "none",
        strokeWidth: thickness,
        "aria-hidden": "true"
      }) : null, (0, import_jsx_runtime111.jsx)(CircularProgressCircle, {
        className: classes2.circle,
        style: circleStyle,
        ownerState,
        cx: SIZE,
        cy: SIZE,
        r: (SIZE - thickness) / 2,
        fill: "none",
        strokeWidth: thickness
      })]
    })
  });
});
true ? CircularProgress.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types89.default.object,
  /**
   * @ignore
   */
  className: import_prop_types89.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types89.default.oneOfType([import_prop_types89.default.oneOf(["inherit", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types89.default.string]),
  /**
   * If `true`, the shrink animation is disabled.
   * This only works if variant is `indeterminate`.
   * @default false
   */
  disableShrink: chainPropTypes(import_prop_types89.default.bool, (props) => {
    if (props.disableShrink && props.variant && props.variant !== "indeterminate") {
      return new Error("MUI: You have provided the `disableShrink` prop with a variant other than `indeterminate`. This will have no effect.");
    }
    return null;
  }),
  /**
   * If `true`, a track circle slot is mounted to show a subtle background for the progress.
   * The `size` and `thickness` apply to the track slot to be consistent with the progress circle.
   * @default false
   */
  enableTrackSlot: import_prop_types89.default.bool,
  /**
   * The size of the component.
   * If using a number, the pixel unit is assumed.
   * If using a string, you need to provide the CSS unit, for example '3rem'.
   * @default 40
   */
  size: import_prop_types89.default.oneOfType([import_prop_types89.default.number, import_prop_types89.default.string]),
  /**
   * @ignore
   */
  style: import_prop_types89.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types89.default.oneOfType([import_prop_types89.default.arrayOf(import_prop_types89.default.oneOfType([import_prop_types89.default.func, import_prop_types89.default.object, import_prop_types89.default.bool])), import_prop_types89.default.func, import_prop_types89.default.object]),
  /**
   * The thickness of the circle.
   * @default 3.6
   */
  thickness: import_prop_types89.default.number,
  /**
   * The value of the progress indicator for the determinate variant.
   * Value between 0 and 100.
   * @default 0
   */
  value: import_prop_types89.default.number,
  /**
   * The variant to use.
   * Use indeterminate when there is no progress value.
   * @default 'indeterminate'
   */
  variant: import_prop_types89.default.oneOf(["determinate", "indeterminate"])
} : void 0;
var CircularProgress_default = CircularProgress;

// node_modules/@mui/material/esm/IconButton/iconButtonClasses.js
function getIconButtonUtilityClass(slot) {
  return generateUtilityClass("MuiIconButton", slot);
}
var iconButtonClasses = generateUtilityClasses("MuiIconButton", ["root", "disabled", "colorInherit", "colorPrimary", "colorSecondary", "colorError", "colorInfo", "colorSuccess", "colorWarning", "edgeStart", "edgeEnd", "sizeSmall", "sizeMedium", "sizeLarge", "loading", "loadingIndicator", "loadingWrapper"]);
var iconButtonClasses_default = iconButtonClasses;

// node_modules/@mui/material/esm/IconButton/IconButton.js
var import_jsx_runtime112 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses52 = (ownerState) => {
  const {
    classes: classes2,
    disabled,
    color,
    edge,
    size,
    loading
  } = ownerState;
  const slots = {
    root: ["root", loading && "loading", disabled && "disabled", color !== "default" && `color${capitalize_default(color)}`, edge && `edge${capitalize_default(edge)}`, `size${capitalize_default(size)}`],
    loadingIndicator: ["loadingIndicator"],
    loadingWrapper: ["loadingWrapper"]
  };
  return composeClasses(slots, getIconButtonUtilityClass, classes2);
};
var IconButtonRoot = styled_default2(ButtonBase_default, {
  name: "MuiIconButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.loading && styles5.loading, ownerState.color !== "default" && styles5[`color${capitalize_default(ownerState.color)}`], ownerState.edge && styles5[`edge${capitalize_default(ownerState.edge)}`], styles5[`size${capitalize_default(ownerState.size)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  textAlign: "center",
  flex: "0 0 auto",
  fontSize: theme.typography.pxToRem(24),
  padding: 8,
  borderRadius: "50%",
  color: (theme.vars || theme).palette.action.active,
  transition: theme.transitions.create("background-color", {
    duration: theme.transitions.duration.shortest
  }),
  variants: [{
    props: (props) => !props.disableRipple,
    style: {
      "--IconButton-hoverBg": theme.alpha((theme.vars || theme).palette.action.active, (theme.vars || theme).palette.action.hoverOpacity),
      "&:hover": {
        backgroundColor: "var(--IconButton-hoverBg)",
        // Reset on touch devices, it doesn't add specificity
        "@media (hover: none)": {
          backgroundColor: "transparent"
        }
      }
    }
  }, {
    props: {
      edge: "start"
    },
    style: {
      marginLeft: -12
    }
  }, {
    props: {
      edge: "start",
      size: "small"
    },
    style: {
      marginLeft: -3
    }
  }, {
    props: {
      edge: "end"
    },
    style: {
      marginRight: -12
    }
  }, {
    props: {
      edge: "end",
      size: "small"
    },
    style: {
      marginRight: -3
    }
  }]
})), memoTheme_default(({
  theme
}) => ({
  variants: [{
    props: {
      color: "inherit"
    },
    style: {
      color: "inherit"
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      color: (theme.vars || theme).palette[color].main
    }
  })), ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      "--IconButton-hoverBg": theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity)
    }
  })), {
    props: {
      size: "small"
    },
    style: {
      padding: 5,
      fontSize: theme.typography.pxToRem(18)
    }
  }, {
    props: {
      size: "large"
    },
    style: {
      padding: 12,
      fontSize: theme.typography.pxToRem(28)
    }
  }],
  [`&.${iconButtonClasses_default.disabled}`]: {
    backgroundColor: "transparent",
    color: (theme.vars || theme).palette.action.disabled
  },
  [`&.${iconButtonClasses_default.loading}`]: {
    color: "transparent"
  }
})));
var IconButtonLoadingIndicator = styled_default2("span", {
  name: "MuiIconButton",
  slot: "LoadingIndicator"
})(({
  theme
}) => ({
  display: "none",
  position: "absolute",
  visibility: "visible",
  top: "50%",
  left: "50%",
  transform: "translate(-50%, -50%)",
  color: (theme.vars || theme).palette.action.disabled,
  variants: [{
    props: {
      loading: true
    },
    style: {
      display: "flex"
    }
  }]
}));
var IconButton = React147.forwardRef(function IconButton2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiIconButton"
  });
  const {
    edge = false,
    children: children2,
    className,
    color = "default",
    disabled = false,
    disableFocusRipple = false,
    size = "medium",
    id: idProp,
    loading = null,
    loadingIndicator: loadingIndicatorProp,
    ...other
  } = props;
  const loadingId = useId_default(idProp);
  const loadingIndicator = loadingIndicatorProp ?? (0, import_jsx_runtime112.jsx)(CircularProgress_default, {
    "aria-labelledby": loadingId,
    color: "inherit",
    size: 16
  });
  const ownerState = {
    ...props,
    edge,
    color,
    disabled,
    disableFocusRipple,
    loading,
    loadingIndicator,
    size
  };
  const classes2 = useUtilityClasses52(ownerState);
  return (0, import_jsx_runtime112.jsxs)(IconButtonRoot, {
    id: loading ? loadingId : idProp,
    className: clsx_default(classes2.root, className),
    centerRipple: true,
    focusRipple: !disableFocusRipple,
    disabled: disabled || loading,
    ref,
    ...other,
    ownerState,
    children: [typeof loading === "boolean" && // use plain HTML span to minimize the runtime overhead
    (0, import_jsx_runtime112.jsx)("span", {
      className: classes2.loadingWrapper,
      style: {
        display: "contents"
      },
      children: (0, import_jsx_runtime112.jsx)(IconButtonLoadingIndicator, {
        className: classes2.loadingIndicator,
        ownerState,
        children: loading && loadingIndicator
      })
    }), children2]
  });
});
true ? IconButton.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The icon to display.
   */
  children: chainPropTypes(import_prop_types90.default.node, (props) => {
    const found = React147.Children.toArray(props.children).some((child) => React147.isValidElement(child) && child.props.onClick);
    if (found) {
      return new Error(["MUI: You are providing an onClick event listener to a child of a button element.", "Prefer applying it to the IconButton directly.", "This guarantees that the whole <button> will be responsive to click events."].join("\n"));
    }
    return null;
  }),
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types90.default.object,
  /**
   * @ignore
   */
  className: import_prop_types90.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'default'
   */
  color: import_prop_types90.default.oneOfType([import_prop_types90.default.oneOf(["inherit", "default", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types90.default.string]),
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types90.default.bool,
  /**
   * If `true`, the  keyboard focus ripple is disabled.
   * @default false
   */
  disableFocusRipple: import_prop_types90.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   *
   * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
   * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
   * @default false
   */
  disableRipple: import_prop_types90.default.bool,
  /**
   * If given, uses a negative margin to counteract the padding on one
   * side (this is often helpful for aligning the left or right
   * side of the icon with content above or below, without ruining the border
   * size and shape).
   * @default false
   */
  edge: import_prop_types90.default.oneOf(["end", "start", false]),
  /**
   * @ignore
   */
  id: import_prop_types90.default.string,
  /**
   * If `true`, the loading indicator is visible and the button is disabled.
   * If `true | false`, the loading wrapper is always rendered before the children to prevent [Google Translation Crash](https://github.com/mui/material-ui/issues/27853).
   * @default null
   */
  loading: import_prop_types90.default.bool,
  /**
   * Element placed before the children if the button is in loading state.
   * The node should contain an element with `role="progressbar"` with an accessible name.
   * By default, it renders a `CircularProgress` that is labeled by the button itself.
   * @default <CircularProgress color="inherit" size={16} />
   */
  loadingIndicator: import_prop_types90.default.node,
  /**
   * The size of the component.
   * `small` is equivalent to the dense button styling.
   * @default 'medium'
   */
  size: import_prop_types90.default.oneOfType([import_prop_types90.default.oneOf(["small", "medium", "large"]), import_prop_types90.default.string]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types90.default.oneOfType([import_prop_types90.default.arrayOf(import_prop_types90.default.oneOfType([import_prop_types90.default.func, import_prop_types90.default.object, import_prop_types90.default.bool])), import_prop_types90.default.func, import_prop_types90.default.object])
} : void 0;
var IconButton_default = IconButton;

// node_modules/@mui/material/esm/Chip/Chip.js
var React148 = __toESM(require_react(), 1);
var import_prop_types91 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/internal/svg-icons/Cancel.js
var import_jsx_runtime113 = __toESM(require_jsx_runtime(), 1);
var Cancel_default = createSvgIcon((0, import_jsx_runtime113.jsx)("path", {
  d: "M12 2C6.47 2 2 6.47 2 12s4.47 10 10 10 10-4.47 10-10S17.53 2 12 2zm5 13.59L15.59 17 12 13.41 8.41 17 7 15.59 10.59 12 7 8.41 8.41 7 12 10.59 15.59 7 17 8.41 13.41 12 17 15.59z"
}), "Cancel");

// node_modules/@mui/material/esm/Chip/chipClasses.js
function getChipUtilityClass(slot) {
  return generateUtilityClass("MuiChip", slot);
}
var chipClasses = generateUtilityClasses("MuiChip", ["root", "sizeSmall", "sizeMedium", "colorDefault", "colorError", "colorInfo", "colorPrimary", "colorSecondary", "colorSuccess", "colorWarning", "disabled", "clickable", "clickableColorPrimary", "clickableColorSecondary", "deletable", "deletableColorPrimary", "deletableColorSecondary", "outlined", "filled", "outlinedPrimary", "outlinedSecondary", "filledPrimary", "filledSecondary", "avatar", "avatarSmall", "avatarMedium", "avatarColorPrimary", "avatarColorSecondary", "icon", "iconSmall", "iconMedium", "iconColorPrimary", "iconColorSecondary", "label", "labelSmall", "labelMedium", "deleteIcon", "deleteIconSmall", "deleteIconMedium", "deleteIconColorPrimary", "deleteIconColorSecondary", "deleteIconOutlinedColorPrimary", "deleteIconOutlinedColorSecondary", "deleteIconFilledColorPrimary", "deleteIconFilledColorSecondary", "focusVisible"]);
var chipClasses_default = chipClasses;

// node_modules/@mui/material/esm/utils/useSlot.js
function useSlot(name, parameters) {
  const {
    className,
    elementType: initialElementType,
    ownerState,
    externalForwardedProps,
    internalForwardedProps,
    shouldForwardComponentProp = false,
    ...useSlotPropsParams
  } = parameters;
  const {
    component: rootComponent,
    slots = {
      [name]: void 0
    },
    slotProps = {
      [name]: void 0
    },
    ...other
  } = externalForwardedProps;
  const elementType = slots[name] || initialElementType;
  const resolvedComponentsProps = resolveComponentProps_default(slotProps[name], ownerState);
  const {
    props: {
      component: slotComponent,
      ...mergedProps
    },
    internalRef
  } = mergeSlotProps_default({
    className,
    ...useSlotPropsParams,
    externalForwardedProps: name === "root" ? other : void 0,
    externalSlotProps: resolvedComponentsProps
  });
  const ref = useForkRef(internalRef, resolvedComponentsProps == null ? void 0 : resolvedComponentsProps.ref, parameters.ref);
  const LeafComponent = name === "root" ? slotComponent || rootComponent : slotComponent;
  const props = appendOwnerState_default(elementType, {
    ...name === "root" && !rootComponent && !slots[name] && internalForwardedProps,
    ...name !== "root" && !slots[name] && internalForwardedProps,
    ...mergedProps,
    ...LeafComponent && !shouldForwardComponentProp && {
      as: LeafComponent
    },
    ...LeafComponent && shouldForwardComponentProp && {
      component: LeafComponent
    },
    ref
  }, ownerState);
  return [elementType, props];
}

// node_modules/@mui/material/esm/Chip/Chip.js
var import_jsx_runtime114 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses53 = (ownerState) => {
  const {
    classes: classes2,
    disabled,
    size,
    color,
    iconColor,
    onDelete,
    clickable,
    variant
  } = ownerState;
  const slots = {
    root: ["root", variant, disabled && "disabled", `size${capitalize_default(size)}`, `color${capitalize_default(color)}`, clickable && "clickable", clickable && `clickableColor${capitalize_default(color)}`, onDelete && "deletable", onDelete && `deletableColor${capitalize_default(color)}`, `${variant}${capitalize_default(color)}`],
    label: ["label", `label${capitalize_default(size)}`],
    avatar: ["avatar", `avatar${capitalize_default(size)}`, `avatarColor${capitalize_default(color)}`],
    icon: ["icon", `icon${capitalize_default(size)}`, `iconColor${capitalize_default(iconColor)}`],
    deleteIcon: ["deleteIcon", `deleteIcon${capitalize_default(size)}`, `deleteIconColor${capitalize_default(color)}`, `deleteIcon${capitalize_default(variant)}Color${capitalize_default(color)}`]
  };
  return composeClasses(slots, getChipUtilityClass, classes2);
};
var ChipRoot = styled_default2("div", {
  name: "MuiChip",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    const {
      color,
      iconColor,
      clickable,
      onDelete,
      size,
      variant
    } = ownerState;
    return [{
      [`& .${chipClasses_default.avatar}`]: styles5.avatar
    }, {
      [`& .${chipClasses_default.avatar}`]: styles5[`avatar${capitalize_default(size)}`]
    }, {
      [`& .${chipClasses_default.avatar}`]: styles5[`avatarColor${capitalize_default(color)}`]
    }, {
      [`& .${chipClasses_default.icon}`]: styles5.icon
    }, {
      [`& .${chipClasses_default.icon}`]: styles5[`icon${capitalize_default(size)}`]
    }, {
      [`& .${chipClasses_default.icon}`]: styles5[`iconColor${capitalize_default(iconColor)}`]
    }, {
      [`& .${chipClasses_default.deleteIcon}`]: styles5.deleteIcon
    }, {
      [`& .${chipClasses_default.deleteIcon}`]: styles5[`deleteIcon${capitalize_default(size)}`]
    }, {
      [`& .${chipClasses_default.deleteIcon}`]: styles5[`deleteIconColor${capitalize_default(color)}`]
    }, {
      [`& .${chipClasses_default.deleteIcon}`]: styles5[`deleteIcon${capitalize_default(variant)}Color${capitalize_default(color)}`]
    }, styles5.root, styles5[`size${capitalize_default(size)}`], styles5[`color${capitalize_default(color)}`], clickable && styles5.clickable, clickable && color !== "default" && styles5[`clickableColor${capitalize_default(color)}`], onDelete && styles5.deletable, onDelete && color !== "default" && styles5[`deletableColor${capitalize_default(color)}`], styles5[variant], styles5[`${variant}${capitalize_default(color)}`]];
  }
})(memoTheme_default(({
  theme
}) => {
  const textColor = theme.palette.mode === "light" ? theme.palette.grey[700] : theme.palette.grey[300];
  return {
    maxWidth: "100%",
    fontFamily: theme.typography.fontFamily,
    fontSize: theme.typography.pxToRem(13),
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    height: 32,
    lineHeight: 1.5,
    color: (theme.vars || theme).palette.text.primary,
    backgroundColor: (theme.vars || theme).palette.action.selected,
    borderRadius: 32 / 2,
    whiteSpace: "nowrap",
    transition: theme.transitions.create(["background-color", "box-shadow"]),
    // reset cursor explicitly in case ButtonBase is used
    cursor: "unset",
    // We disable the focus ring for mouse, touch and keyboard users.
    outline: 0,
    textDecoration: "none",
    border: 0,
    // Remove `button` border
    padding: 0,
    // Remove `button` padding
    verticalAlign: "middle",
    boxSizing: "border-box",
    [`&.${chipClasses_default.disabled}`]: {
      opacity: (theme.vars || theme).palette.action.disabledOpacity,
      pointerEvents: "none"
    },
    [`& .${chipClasses_default.avatar}`]: {
      marginLeft: 5,
      marginRight: -6,
      width: 24,
      height: 24,
      color: theme.vars ? theme.vars.palette.Chip.defaultAvatarColor : textColor,
      fontSize: theme.typography.pxToRem(12)
    },
    [`& .${chipClasses_default.avatarColorPrimary}`]: {
      color: (theme.vars || theme).palette.primary.contrastText,
      backgroundColor: (theme.vars || theme).palette.primary.dark
    },
    [`& .${chipClasses_default.avatarColorSecondary}`]: {
      color: (theme.vars || theme).palette.secondary.contrastText,
      backgroundColor: (theme.vars || theme).palette.secondary.dark
    },
    [`& .${chipClasses_default.avatarSmall}`]: {
      marginLeft: 4,
      marginRight: -4,
      width: 18,
      height: 18,
      fontSize: theme.typography.pxToRem(10)
    },
    [`& .${chipClasses_default.icon}`]: {
      marginLeft: 5,
      marginRight: -6
    },
    [`& .${chipClasses_default.deleteIcon}`]: {
      WebkitTapHighlightColor: "transparent",
      color: theme.alpha((theme.vars || theme).palette.text.primary, 0.26),
      fontSize: 22,
      cursor: "pointer",
      margin: "0 5px 0 -6px",
      "&:hover": {
        color: theme.alpha((theme.vars || theme).palette.text.primary, 0.4)
      }
    },
    variants: [{
      props: {
        size: "small"
      },
      style: {
        height: 24,
        [`& .${chipClasses_default.icon}`]: {
          fontSize: 18,
          marginLeft: 4,
          marginRight: -4
        },
        [`& .${chipClasses_default.deleteIcon}`]: {
          fontSize: 16,
          marginRight: 4,
          marginLeft: -4
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter(["contrastText"])).map(([color]) => {
      return {
        props: {
          color
        },
        style: {
          backgroundColor: (theme.vars || theme).palette[color].main,
          color: (theme.vars || theme).palette[color].contrastText,
          [`& .${chipClasses_default.deleteIcon}`]: {
            color: theme.alpha((theme.vars || theme).palette[color].contrastText, 0.7),
            "&:hover, &:active": {
              color: (theme.vars || theme).palette[color].contrastText
            }
          }
        }
      };
    }), {
      props: (props) => props.iconColor === props.color,
      style: {
        [`& .${chipClasses_default.icon}`]: {
          color: theme.vars ? theme.vars.palette.Chip.defaultIconColor : textColor
        }
      }
    }, {
      props: (props) => props.iconColor === props.color && props.color !== "default",
      style: {
        [`& .${chipClasses_default.icon}`]: {
          color: "inherit"
        }
      }
    }, {
      props: {
        onDelete: true
      },
      style: {
        [`&.${chipClasses_default.focusVisible}`]: {
          backgroundColor: theme.alpha((theme.vars || theme).palette.action.selected, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.focusOpacity}`)
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter(["dark"])).map(([color]) => {
      return {
        props: {
          color,
          onDelete: true
        },
        style: {
          [`&.${chipClasses_default.focusVisible}`]: {
            background: (theme.vars || theme).palette[color].dark
          }
        }
      };
    }), {
      props: {
        clickable: true
      },
      style: {
        userSelect: "none",
        WebkitTapHighlightColor: "transparent",
        cursor: "pointer",
        "&:hover": {
          backgroundColor: theme.alpha((theme.vars || theme).palette.action.selected, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`)
        },
        [`&.${chipClasses_default.focusVisible}`]: {
          backgroundColor: theme.alpha((theme.vars || theme).palette.action.selected, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.focusOpacity}`)
        },
        "&:active": {
          boxShadow: (theme.vars || theme).shadows[1]
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter(["dark"])).map(([color]) => ({
      props: {
        color,
        clickable: true
      },
      style: {
        [`&:hover, &.${chipClasses_default.focusVisible}`]: {
          backgroundColor: (theme.vars || theme).palette[color].dark
        }
      }
    })), {
      props: {
        variant: "outlined"
      },
      style: {
        backgroundColor: "transparent",
        border: theme.vars ? `1px solid ${theme.vars.palette.Chip.defaultBorder}` : `1px solid ${theme.palette.mode === "light" ? theme.palette.grey[400] : theme.palette.grey[700]}`,
        [`&.${chipClasses_default.clickable}:hover`]: {
          backgroundColor: (theme.vars || theme).palette.action.hover
        },
        [`&.${chipClasses_default.focusVisible}`]: {
          backgroundColor: (theme.vars || theme).palette.action.focus
        },
        [`& .${chipClasses_default.avatar}`]: {
          marginLeft: 4
        },
        [`& .${chipClasses_default.avatarSmall}`]: {
          marginLeft: 2
        },
        [`& .${chipClasses_default.icon}`]: {
          marginLeft: 4
        },
        [`& .${chipClasses_default.iconSmall}`]: {
          marginLeft: 2
        },
        [`& .${chipClasses_default.deleteIcon}`]: {
          marginRight: 5
        },
        [`& .${chipClasses_default.deleteIconSmall}`]: {
          marginRight: 3
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
      props: {
        variant: "outlined",
        color
      },
      style: {
        color: (theme.vars || theme).palette[color].main,
        border: `1px solid ${theme.alpha((theme.vars || theme).palette[color].main, 0.7)}`,
        [`&.${chipClasses_default.clickable}:hover`]: {
          backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity)
        },
        [`&.${chipClasses_default.focusVisible}`]: {
          backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.focusOpacity)
        },
        [`& .${chipClasses_default.deleteIcon}`]: {
          color: theme.alpha((theme.vars || theme).palette[color].main, 0.7),
          "&:hover, &:active": {
            color: (theme.vars || theme).palette[color].main
          }
        }
      }
    }))]
  };
}));
var ChipLabel = styled_default2("span", {
  name: "MuiChip",
  slot: "Label",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    const {
      size
    } = ownerState;
    return [styles5.label, styles5[`label${capitalize_default(size)}`]];
  }
})({
  overflow: "hidden",
  textOverflow: "ellipsis",
  paddingLeft: 12,
  paddingRight: 12,
  whiteSpace: "nowrap",
  variants: [{
    props: {
      variant: "outlined"
    },
    style: {
      paddingLeft: 11,
      paddingRight: 11
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      paddingLeft: 8,
      paddingRight: 8
    }
  }, {
    props: {
      size: "small",
      variant: "outlined"
    },
    style: {
      paddingLeft: 7,
      paddingRight: 7
    }
  }]
});
function isDeleteKeyboardEvent(keyboardEvent) {
  return keyboardEvent.key === "Backspace" || keyboardEvent.key === "Delete";
}
var Chip = React148.forwardRef(function Chip2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiChip"
  });
  const {
    avatar: avatarProp,
    className,
    clickable: clickableProp,
    color = "default",
    component: ComponentProp,
    deleteIcon: deleteIconProp,
    disabled = false,
    icon: iconProp,
    label,
    onClick,
    onDelete,
    onKeyDown,
    onKeyUp,
    size = "medium",
    variant = "filled",
    tabIndex,
    skipFocusWhenDisabled = false,
    // TODO v6: Rename to `focusableWhenDisabled`.
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const chipRef = React148.useRef(null);
  const handleRef = useForkRef_default(chipRef, ref);
  const handleDeleteIconClick = (event) => {
    event.stopPropagation();
    onDelete(event);
  };
  const handleKeyDown = (event) => {
    if (event.currentTarget === event.target && isDeleteKeyboardEvent(event)) {
      event.preventDefault();
    }
    if (onKeyDown) {
      onKeyDown(event);
    }
  };
  const handleKeyUp = (event) => {
    if (event.currentTarget === event.target) {
      if (onDelete && isDeleteKeyboardEvent(event)) {
        onDelete(event);
      }
    }
    if (onKeyUp) {
      onKeyUp(event);
    }
  };
  const clickable = clickableProp !== false && onClick ? true : clickableProp;
  const component = clickable || onDelete ? ButtonBase_default : ComponentProp || "div";
  const ownerState = {
    ...props,
    component,
    disabled,
    size,
    color,
    iconColor: React148.isValidElement(iconProp) ? iconProp.props.color || color : color,
    onDelete: !!onDelete,
    clickable,
    variant
  };
  const classes2 = useUtilityClasses53(ownerState);
  const moreProps = component === ButtonBase_default ? {
    component: ComponentProp || "div",
    focusVisibleClassName: classes2.focusVisible,
    ...onDelete && {
      disableRipple: true
    }
  } : {};
  let deleteIcon = null;
  if (onDelete) {
    deleteIcon = deleteIconProp && React148.isValidElement(deleteIconProp) ? React148.cloneElement(deleteIconProp, {
      className: clsx_default(deleteIconProp.props.className, classes2.deleteIcon),
      onClick: handleDeleteIconClick
    }) : (0, import_jsx_runtime114.jsx)(Cancel_default, {
      className: classes2.deleteIcon,
      onClick: handleDeleteIconClick
    });
  }
  let avatar = null;
  if (avatarProp && React148.isValidElement(avatarProp)) {
    avatar = React148.cloneElement(avatarProp, {
      className: clsx_default(classes2.avatar, avatarProp.props.className)
    });
  }
  let icon = null;
  if (iconProp && React148.isValidElement(iconProp)) {
    icon = React148.cloneElement(iconProp, {
      className: clsx_default(classes2.icon, iconProp.props.className)
    });
  }
  if (true) {
    if (avatar && icon) {
      console.error("MUI: The Chip component can not handle the avatar and the icon prop at the same time. Pick one.");
    }
  }
  const externalForwardedProps = {
    slots,
    slotProps
  };
  const [RootSlot, rootProps] = useSlot("root", {
    elementType: ChipRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other
    },
    ownerState,
    // The `component` prop is preserved because `Chip` relies on it for internal logic. If `shouldForwardComponentProp` were `false`, `useSlot` would remove the `component` prop, potentially breaking the component's behavior.
    shouldForwardComponentProp: true,
    ref: handleRef,
    className: clsx_default(classes2.root, className),
    additionalProps: {
      disabled: clickable && disabled ? true : void 0,
      tabIndex: skipFocusWhenDisabled && disabled ? -1 : tabIndex,
      ...moreProps
    },
    getSlotProps: (handlers) => ({
      ...handlers,
      onClick: (event) => {
        var _a;
        (_a = handlers.onClick) == null ? void 0 : _a.call(handlers, event);
        onClick == null ? void 0 : onClick(event);
      },
      onKeyDown: (event) => {
        var _a;
        (_a = handlers.onKeyDown) == null ? void 0 : _a.call(handlers, event);
        handleKeyDown(event);
      },
      onKeyUp: (event) => {
        var _a;
        (_a = handlers.onKeyUp) == null ? void 0 : _a.call(handlers, event);
        handleKeyUp(event);
      }
    })
  });
  const [LabelSlot, labelProps] = useSlot("label", {
    elementType: ChipLabel,
    externalForwardedProps,
    ownerState,
    className: classes2.label
  });
  return (0, import_jsx_runtime114.jsxs)(RootSlot, {
    as: component,
    ...rootProps,
    children: [avatar || icon, (0, import_jsx_runtime114.jsx)(LabelSlot, {
      ...labelProps,
      children: label
    }), deleteIcon]
  });
});
true ? Chip.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The Avatar element to display.
   */
  avatar: import_prop_types91.default.element,
  /**
   * This prop isn't supported.
   * Use the `component` prop if you need to change the children structure.
   */
  children: unsupportedProp_default,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types91.default.object,
  /**
   * @ignore
   */
  className: import_prop_types91.default.string,
  /**
   * If `true`, the chip will appear clickable, and will raise when pressed,
   * even if the onClick prop is not defined.
   * If `false`, the chip will not appear clickable, even if onClick prop is defined.
   * This can be used, for example,
   * along with the component prop to indicate an anchor Chip is clickable.
   * Note: this controls the UI and does not affect the onClick event.
   */
  clickable: import_prop_types91.default.bool,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'default'
   */
  color: import_prop_types91.default.oneOfType([import_prop_types91.default.oneOf(["default", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types91.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types91.default.elementType,
  /**
   * Override the default delete icon element. Shown only if `onDelete` is set.
   */
  deleteIcon: import_prop_types91.default.element,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types91.default.bool,
  /**
   * Icon element.
   */
  icon: import_prop_types91.default.element,
  /**
   * The content of the component.
   */
  label: import_prop_types91.default.node,
  /**
   * @ignore
   */
  onClick: import_prop_types91.default.func,
  /**
   * Callback fired when the delete icon is clicked.
   * If set, the delete icon will be shown.
   */
  onDelete: import_prop_types91.default.func,
  /**
   * @ignore
   */
  onKeyDown: import_prop_types91.default.func,
  /**
   * @ignore
   */
  onKeyUp: import_prop_types91.default.func,
  /**
   * The size of the component.
   * @default 'medium'
   */
  size: import_prop_types91.default.oneOfType([import_prop_types91.default.oneOf(["medium", "small"]), import_prop_types91.default.string]),
  /**
   * If `true`, allows the disabled chip to escape focus.
   * If `false`, allows the disabled chip to receive focus.
   * @default false
   */
  skipFocusWhenDisabled: import_prop_types91.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types91.default.shape({
    label: import_prop_types91.default.oneOfType([import_prop_types91.default.func, import_prop_types91.default.object]),
    root: import_prop_types91.default.oneOfType([import_prop_types91.default.func, import_prop_types91.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types91.default.shape({
    label: import_prop_types91.default.elementType,
    root: import_prop_types91.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types91.default.oneOfType([import_prop_types91.default.arrayOf(import_prop_types91.default.oneOfType([import_prop_types91.default.func, import_prop_types91.default.object, import_prop_types91.default.bool])), import_prop_types91.default.func, import_prop_types91.default.object]),
  /**
   * @ignore
   */
  tabIndex: import_prop_types91.default.number,
  /**
   * The variant to use.
   * @default 'filled'
   */
  variant: import_prop_types91.default.oneOfType([import_prop_types91.default.oneOf(["filled", "outlined"]), import_prop_types91.default.string])
} : void 0;
var Chip_default = Chip;

// node_modules/@mui/material/esm/Input/inputClasses.js
function getInputUtilityClass(slot) {
  return generateUtilityClass("MuiInput", slot);
}
var inputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiInput", ["root", "underline", "input"])
};
var inputClasses_default = inputClasses;

// node_modules/@mui/material/esm/OutlinedInput/outlinedInputClasses.js
function getOutlinedInputUtilityClass(slot) {
  return generateUtilityClass("MuiOutlinedInput", slot);
}
var outlinedInputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiOutlinedInput", ["root", "notchedOutline", "input"])
};
var outlinedInputClasses_default = outlinedInputClasses;

// node_modules/@mui/material/esm/FilledInput/filledInputClasses.js
function getFilledInputUtilityClass(slot) {
  return generateUtilityClass("MuiFilledInput", slot);
}
var filledInputClasses = {
  ...inputBaseClasses_default,
  ...generateUtilityClasses("MuiFilledInput", ["root", "underline", "input", "adornedStart", "adornedEnd", "sizeSmall", "multiline", "hiddenLabel"])
};
var filledInputClasses_default = filledInputClasses;

// node_modules/@mui/material/esm/internal/svg-icons/Close.js
var import_jsx_runtime115 = __toESM(require_jsx_runtime(), 1);
var Close_default = createSvgIcon((0, import_jsx_runtime115.jsx)("path", {
  d: "M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"
}), "Close");

// node_modules/@mui/material/esm/internal/svg-icons/ArrowDropDown.js
var import_jsx_runtime116 = __toESM(require_jsx_runtime(), 1);
var ArrowDropDown_default = createSvgIcon((0, import_jsx_runtime116.jsx)("path", {
  d: "M7 10l5 5 5-5z"
}), "ArrowDropDown");

// node_modules/@mui/material/esm/Autocomplete/autocompleteClasses.js
function getAutocompleteUtilityClass(slot) {
  return generateUtilityClass("MuiAutocomplete", slot);
}
var autocompleteClasses = generateUtilityClasses("MuiAutocomplete", ["root", "expanded", "fullWidth", "focused", "focusVisible", "tag", "tagSizeSmall", "tagSizeMedium", "hasPopupIcon", "hasClearIcon", "inputRoot", "input", "inputFocused", "endAdornment", "clearIndicator", "popupIndicator", "popupIndicatorOpen", "popper", "popperDisablePortal", "paper", "listbox", "loading", "noOptions", "option", "groupLabel", "groupUl"]);
var autocompleteClasses_default = autocompleteClasses;

// node_modules/@mui/material/esm/Autocomplete/Autocomplete.js
var import_jsx_runtime117 = __toESM(require_jsx_runtime(), 1);
var _ClearIcon;
var _ArrowDropDownIcon;
var useUtilityClasses54 = (ownerState) => {
  const {
    classes: classes2,
    disablePortal,
    expanded,
    focused,
    fullWidth,
    hasClearIcon,
    hasPopupIcon,
    inputFocused,
    popupOpen,
    size
  } = ownerState;
  const slots = {
    root: ["root", expanded && "expanded", focused && "focused", fullWidth && "fullWidth", hasClearIcon && "hasClearIcon", hasPopupIcon && "hasPopupIcon"],
    inputRoot: ["inputRoot"],
    input: ["input", inputFocused && "inputFocused"],
    tag: ["tag", `tagSize${capitalize_default(size)}`],
    endAdornment: ["endAdornment"],
    clearIndicator: ["clearIndicator"],
    popupIndicator: ["popupIndicator", popupOpen && "popupIndicatorOpen"],
    popper: ["popper", disablePortal && "popperDisablePortal"],
    paper: ["paper"],
    listbox: ["listbox"],
    loading: ["loading"],
    noOptions: ["noOptions"],
    option: ["option"],
    groupLabel: ["groupLabel"],
    groupUl: ["groupUl"]
  };
  return composeClasses(slots, getAutocompleteUtilityClass, classes2);
};
var AutocompleteRoot = styled_default2("div", {
  name: "MuiAutocomplete",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    const {
      fullWidth,
      hasClearIcon,
      hasPopupIcon,
      inputFocused,
      size
    } = ownerState;
    return [{
      [`& .${autocompleteClasses_default.tag}`]: styles5.tag
    }, {
      [`& .${autocompleteClasses_default.tag}`]: styles5[`tagSize${capitalize_default(size)}`]
    }, {
      [`& .${autocompleteClasses_default.inputRoot}`]: styles5.inputRoot
    }, {
      [`& .${autocompleteClasses_default.input}`]: styles5.input
    }, {
      [`& .${autocompleteClasses_default.input}`]: inputFocused && styles5.inputFocused
    }, styles5.root, fullWidth && styles5.fullWidth, hasPopupIcon && styles5.hasPopupIcon, hasClearIcon && styles5.hasClearIcon];
  }
})({
  [`&.${autocompleteClasses_default.focused} .${autocompleteClasses_default.clearIndicator}`]: {
    visibility: "visible"
  },
  /* Avoid double tap issue on iOS */
  "@media (pointer: fine)": {
    [`&:hover .${autocompleteClasses_default.clearIndicator}`]: {
      visibility: "visible"
    }
  },
  [`& .${autocompleteClasses_default.tag}`]: {
    margin: 3,
    maxWidth: "calc(100% - 6px)"
  },
  [`& .${autocompleteClasses_default.inputRoot}`]: {
    [`.${autocompleteClasses_default.hasPopupIcon}&, .${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 26 + 4
    },
    [`.${autocompleteClasses_default.hasPopupIcon}.${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 52 + 4
    },
    [`& .${autocompleteClasses_default.input}`]: {
      width: 0,
      minWidth: 30
    }
  },
  [`& .${inputClasses_default.root}`]: {
    paddingBottom: 1,
    "& .MuiInput-input": {
      padding: "4px 4px 4px 0px"
    }
  },
  [`& .${inputClasses_default.root}.${inputBaseClasses_default.sizeSmall}`]: {
    [`& .${inputClasses_default.input}`]: {
      padding: "2px 4px 3px 0"
    }
  },
  [`& .${outlinedInputClasses_default.root}`]: {
    padding: 9,
    [`.${autocompleteClasses_default.hasPopupIcon}&, .${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 26 + 4 + 9
    },
    [`.${autocompleteClasses_default.hasPopupIcon}.${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 52 + 4 + 9
    },
    [`& .${autocompleteClasses_default.input}`]: {
      padding: "7.5px 4px 7.5px 5px"
    },
    [`& .${autocompleteClasses_default.endAdornment}`]: {
      right: 9
    }
  },
  [`& .${outlinedInputClasses_default.root}.${inputBaseClasses_default.sizeSmall}`]: {
    // Don't specify paddingRight, as it overrides the default value set when there is only
    // one of the popup or clear icon as the specificity is equal so the latter one wins
    paddingTop: 6,
    paddingBottom: 6,
    paddingLeft: 6,
    [`& .${autocompleteClasses_default.input}`]: {
      padding: "2.5px 4px 2.5px 8px"
    }
  },
  [`& .${filledInputClasses_default.root}`]: {
    paddingTop: 19,
    paddingLeft: 8,
    [`.${autocompleteClasses_default.hasPopupIcon}&, .${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 26 + 4 + 9
    },
    [`.${autocompleteClasses_default.hasPopupIcon}.${autocompleteClasses_default.hasClearIcon}&`]: {
      paddingRight: 52 + 4 + 9
    },
    [`& .${filledInputClasses_default.input}`]: {
      padding: "7px 4px"
    },
    [`& .${autocompleteClasses_default.endAdornment}`]: {
      right: 9
    }
  },
  [`& .${filledInputClasses_default.root}.${inputBaseClasses_default.sizeSmall}`]: {
    paddingBottom: 1,
    [`& .${filledInputClasses_default.input}`]: {
      padding: "2.5px 4px"
    }
  },
  [`& .${inputBaseClasses_default.hiddenLabel}`]: {
    paddingTop: 8
  },
  [`& .${filledInputClasses_default.root}.${inputBaseClasses_default.hiddenLabel}`]: {
    paddingTop: 0,
    paddingBottom: 0,
    [`& .${autocompleteClasses_default.input}`]: {
      paddingTop: 16,
      paddingBottom: 17
    }
  },
  [`& .${filledInputClasses_default.root}.${inputBaseClasses_default.hiddenLabel}.${inputBaseClasses_default.sizeSmall}`]: {
    [`& .${autocompleteClasses_default.input}`]: {
      paddingTop: 8,
      paddingBottom: 9
    }
  },
  [`& .${autocompleteClasses_default.input}`]: {
    flexGrow: 1,
    textOverflow: "ellipsis",
    opacity: 0
  },
  variants: [{
    props: {
      fullWidth: true
    },
    style: {
      width: "100%"
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      [`& .${autocompleteClasses_default.tag}`]: {
        margin: 2,
        maxWidth: "calc(100% - 4px)"
      }
    }
  }, {
    props: {
      inputFocused: true
    },
    style: {
      [`& .${autocompleteClasses_default.input}`]: {
        opacity: 1
      }
    }
  }, {
    props: {
      multiple: true
    },
    style: {
      [`& .${autocompleteClasses_default.inputRoot}`]: {
        flexWrap: "wrap"
      }
    }
  }]
});
var AutocompleteEndAdornment = styled_default2("div", {
  name: "MuiAutocomplete",
  slot: "EndAdornment"
})({
  // We use a position absolute to support wrapping tags.
  position: "absolute",
  right: 0,
  top: "50%",
  transform: "translate(0, -50%)"
});
var AutocompleteClearIndicator = styled_default2(IconButton_default, {
  name: "MuiAutocomplete",
  slot: "ClearIndicator"
})({
  marginRight: -2,
  padding: 4,
  visibility: "hidden"
});
var AutocompletePopupIndicator = styled_default2(IconButton_default, {
  name: "MuiAutocomplete",
  slot: "PopupIndicator",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.popupIndicator, ownerState.popupOpen && styles5.popupIndicatorOpen];
  }
})({
  padding: 2,
  marginRight: -2,
  variants: [{
    props: {
      popupOpen: true
    },
    style: {
      transform: "rotate(180deg)"
    }
  }]
});
var AutocompletePopper = styled_default2(Popper_default, {
  name: "MuiAutocomplete",
  slot: "Popper",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [{
      [`& .${autocompleteClasses_default.option}`]: styles5.option
    }, styles5.popper, ownerState.disablePortal && styles5.popperDisablePortal];
  }
})(memoTheme_default(({
  theme
}) => ({
  zIndex: (theme.vars || theme).zIndex.modal,
  variants: [{
    props: {
      disablePortal: true
    },
    style: {
      position: "absolute"
    }
  }]
})));
var AutocompletePaper = styled_default2(Paper_default, {
  name: "MuiAutocomplete",
  slot: "Paper"
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body1,
  overflow: "auto"
})));
var AutocompleteLoading = styled_default2("div", {
  name: "MuiAutocomplete",
  slot: "Loading"
})(memoTheme_default(({
  theme
}) => ({
  color: (theme.vars || theme).palette.text.secondary,
  padding: "14px 16px"
})));
var AutocompleteNoOptions = styled_default2("div", {
  name: "MuiAutocomplete",
  slot: "NoOptions"
})(memoTheme_default(({
  theme
}) => ({
  color: (theme.vars || theme).palette.text.secondary,
  padding: "14px 16px"
})));
var AutocompleteListbox = styled_default2("ul", {
  name: "MuiAutocomplete",
  slot: "Listbox"
})(memoTheme_default(({
  theme
}) => ({
  listStyle: "none",
  margin: 0,
  padding: "8px 0",
  maxHeight: "40vh",
  overflow: "auto",
  position: "relative",
  [`& .${autocompleteClasses_default.option}`]: {
    minHeight: 48,
    display: "flex",
    overflow: "hidden",
    justifyContent: "flex-start",
    alignItems: "center",
    cursor: "pointer",
    paddingTop: 6,
    boxSizing: "border-box",
    outline: "0",
    WebkitTapHighlightColor: "transparent",
    paddingBottom: 6,
    paddingLeft: 16,
    paddingRight: 16,
    [theme.breakpoints.up("sm")]: {
      minHeight: "auto"
    },
    [`&.${autocompleteClasses_default.focused}`]: {
      backgroundColor: (theme.vars || theme).palette.action.hover,
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        backgroundColor: "transparent"
      }
    },
    '&[aria-disabled="true"]': {
      opacity: (theme.vars || theme).palette.action.disabledOpacity,
      pointerEvents: "none"
    },
    [`&.${autocompleteClasses_default.focusVisible}`]: {
      backgroundColor: (theme.vars || theme).palette.action.focus
    },
    '&[aria-selected="true"]': {
      backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, (theme.vars || theme).palette.action.selectedOpacity),
      [`&.${autocompleteClasses_default.focused}`]: {
        backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`),
        // Reset on touch devices, it doesn't add specificity
        "@media (hover: none)": {
          backgroundColor: (theme.vars || theme).palette.action.selected
        }
      },
      [`&.${autocompleteClasses_default.focusVisible}`]: {
        backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.focusOpacity}`)
      }
    }
  }
})));
var AutocompleteGroupLabel = styled_default2(ListSubheader_default, {
  name: "MuiAutocomplete",
  slot: "GroupLabel"
})(memoTheme_default(({
  theme
}) => ({
  backgroundColor: (theme.vars || theme).palette.background.paper,
  top: -8
})));
var AutocompleteGroupUl = styled_default2("ul", {
  name: "MuiAutocomplete",
  slot: "GroupUl"
})({
  padding: 0,
  [`& .${autocompleteClasses_default.option}`]: {
    paddingLeft: 24
  }
});
var Autocomplete = React149.forwardRef(function Autocomplete2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiAutocomplete"
  });
  const {
    autoComplete = false,
    autoHighlight = false,
    autoSelect = false,
    blurOnSelect = false,
    ChipProps: ChipPropsProp,
    className,
    clearIcon = _ClearIcon || (_ClearIcon = (0, import_jsx_runtime117.jsx)(Close_default, {
      fontSize: "small"
    })),
    clearOnBlur = !props.freeSolo,
    clearOnEscape = false,
    clearText = "Clear",
    closeText = "Close",
    componentsProps,
    defaultValue = props.multiple ? [] : null,
    disableClearable = false,
    disableCloseOnSelect = false,
    disabled = false,
    disabledItemsFocusable = false,
    disableListWrap = false,
    disablePortal = false,
    filterOptions,
    filterSelectedOptions = false,
    forcePopupIcon = "auto",
    freeSolo = false,
    fullWidth = false,
    getLimitTagsText = (more) => `+${more}`,
    getOptionDisabled,
    getOptionKey,
    getOptionLabel: getOptionLabelProp,
    isOptionEqualToValue,
    groupBy,
    handleHomeEndKeys = !props.freeSolo,
    id: idProp,
    includeInputInList = false,
    inputValue: inputValueProp,
    limitTags = -1,
    ListboxComponent: ListboxComponentProp,
    ListboxProps: ListboxPropsProp,
    loading = false,
    loadingText = "Loading…",
    multiple = false,
    noOptionsText = "No options",
    onChange,
    onClose,
    onHighlightChange,
    onInputChange,
    onOpen,
    open,
    openOnFocus = false,
    openText = "Open",
    options: options3,
    PaperComponent: PaperComponentProp,
    PopperComponent: PopperComponentProp,
    popupIcon = _ArrowDropDownIcon || (_ArrowDropDownIcon = (0, import_jsx_runtime117.jsx)(ArrowDropDown_default, {})),
    readOnly = false,
    renderGroup: renderGroupProp,
    renderInput,
    renderOption: renderOptionProp,
    renderTags,
    renderValue,
    selectOnFocus = !props.freeSolo,
    size = "medium",
    slots = {},
    slotProps = {},
    value: valueProp,
    ...other
  } = props;
  const {
    getRootProps,
    getInputProps,
    getInputLabelProps,
    getPopupIndicatorProps,
    getClearProps,
    getItemProps,
    getListboxProps,
    getOptionProps,
    value,
    dirty,
    expanded,
    id,
    popupOpen,
    focused,
    focusedItem,
    anchorEl,
    setAnchorEl,
    inputValue,
    groupedOptions
  } = useAutocomplete_default({
    ...props,
    componentName: "Autocomplete"
  });
  const hasClearIcon = !disableClearable && !disabled && dirty && !readOnly;
  const hasPopupIcon = (!freeSolo || forcePopupIcon === true) && forcePopupIcon !== false;
  const {
    onMouseDown: handleInputMouseDown
  } = getInputProps();
  const {
    ref: listboxRef,
    ...otherListboxProps
  } = getListboxProps();
  const defaultGetOptionLabel2 = (option) => option.label ?? option;
  const getOptionLabel = getOptionLabelProp || defaultGetOptionLabel2;
  const ownerState = {
    ...props,
    disablePortal,
    expanded,
    focused,
    fullWidth,
    getOptionLabel,
    hasClearIcon,
    hasPopupIcon,
    inputFocused: focusedItem === -1,
    popupOpen,
    size
  };
  const classes2 = useUtilityClasses54(ownerState);
  const externalForwardedProps = {
    slots: {
      paper: PaperComponentProp,
      popper: PopperComponentProp,
      ...slots
    },
    slotProps: {
      chip: ChipPropsProp,
      listbox: ListboxPropsProp,
      ...componentsProps,
      ...slotProps
    }
  };
  const [ListboxSlot, listboxProps] = useSlot("listbox", {
    elementType: AutocompleteListbox,
    externalForwardedProps,
    ownerState,
    className: classes2.listbox,
    additionalProps: otherListboxProps,
    ref: listboxRef
  });
  const [PaperSlot, paperProps] = useSlot("paper", {
    elementType: Paper_default,
    externalForwardedProps,
    ownerState,
    className: classes2.paper
  });
  const [PopperSlot, popperProps] = useSlot("popper", {
    elementType: Popper_default,
    externalForwardedProps,
    ownerState,
    className: classes2.popper,
    additionalProps: {
      disablePortal,
      style: {
        width: anchorEl ? anchorEl.clientWidth : null
      },
      role: "presentation",
      anchorEl,
      open: popupOpen
    }
  });
  let startAdornment;
  const getCustomizedItemProps = (params) => ({
    className: classes2.tag,
    disabled,
    ...getItemProps(params)
  });
  if (multiple) {
    if (value.length > 0) {
      if (renderTags) {
        startAdornment = renderTags(value, getCustomizedItemProps, ownerState);
      } else if (renderValue) {
        startAdornment = renderValue(value, getCustomizedItemProps, ownerState);
      } else {
        startAdornment = value.map((option, index) => {
          const {
            key,
            ...customItemProps
          } = getCustomizedItemProps({
            index
          });
          return (0, import_jsx_runtime117.jsx)(Chip_default, {
            label: getOptionLabel(option),
            size,
            ...customItemProps,
            ...externalForwardedProps.slotProps.chip
          }, key);
        });
      }
    }
  } else if (renderValue && value != null) {
    startAdornment = renderValue(value, getCustomizedItemProps, ownerState);
  }
  if (limitTags > -1 && Array.isArray(startAdornment)) {
    const more = startAdornment.length - limitTags;
    if (!focused && more > 0) {
      startAdornment = startAdornment.splice(0, limitTags);
      startAdornment.push((0, import_jsx_runtime117.jsx)("span", {
        className: classes2.tag,
        children: getLimitTagsText(more)
      }, startAdornment.length));
    }
  }
  const defaultRenderGroup = (params) => (0, import_jsx_runtime117.jsxs)("li", {
    children: [(0, import_jsx_runtime117.jsx)(AutocompleteGroupLabel, {
      className: classes2.groupLabel,
      ownerState,
      component: "div",
      children: params.group
    }), (0, import_jsx_runtime117.jsx)(AutocompleteGroupUl, {
      className: classes2.groupUl,
      ownerState,
      children: params.children
    })]
  }, params.key);
  const renderGroup = renderGroupProp || defaultRenderGroup;
  const defaultRenderOption = (props2, option) => {
    const {
      key,
      ...otherProps
    } = props2;
    return (0, import_jsx_runtime117.jsx)("li", {
      ...otherProps,
      children: getOptionLabel(option)
    }, key);
  };
  const renderOption = renderOptionProp || defaultRenderOption;
  const renderListOption = (option, index) => {
    const optionProps = getOptionProps({
      option,
      index
    });
    return renderOption({
      ...optionProps,
      className: classes2.option
    }, option, {
      selected: optionProps["aria-selected"],
      index,
      inputValue
    }, ownerState);
  };
  const clearIndicatorSlotProps = externalForwardedProps.slotProps.clearIndicator;
  const popupIndicatorSlotProps = externalForwardedProps.slotProps.popupIndicator;
  return (0, import_jsx_runtime117.jsxs)(React149.Fragment, {
    children: [(0, import_jsx_runtime117.jsx)(AutocompleteRoot, {
      ref,
      className: clsx_default(classes2.root, className),
      ownerState,
      ...getRootProps(other),
      children: renderInput({
        id,
        disabled,
        fullWidth: props.fullWidth ?? true,
        size: size === "small" ? "small" : void 0,
        InputLabelProps: getInputLabelProps(),
        InputProps: {
          ref: setAnchorEl,
          className: classes2.inputRoot,
          startAdornment,
          onMouseDown: (event) => {
            if (event.target === event.currentTarget) {
              handleInputMouseDown(event);
            }
          },
          ...(hasClearIcon || hasPopupIcon) && {
            endAdornment: (0, import_jsx_runtime117.jsxs)(AutocompleteEndAdornment, {
              className: classes2.endAdornment,
              ownerState,
              children: [hasClearIcon ? (0, import_jsx_runtime117.jsx)(AutocompleteClearIndicator, {
                ...getClearProps(),
                "aria-label": clearText,
                title: clearText,
                ownerState,
                ...clearIndicatorSlotProps,
                className: clsx_default(classes2.clearIndicator, clearIndicatorSlotProps == null ? void 0 : clearIndicatorSlotProps.className),
                children: clearIcon
              }) : null, hasPopupIcon ? (0, import_jsx_runtime117.jsx)(AutocompletePopupIndicator, {
                ...getPopupIndicatorProps(),
                disabled,
                "aria-label": popupOpen ? closeText : openText,
                title: popupOpen ? closeText : openText,
                ownerState,
                ...popupIndicatorSlotProps,
                className: clsx_default(classes2.popupIndicator, popupIndicatorSlotProps == null ? void 0 : popupIndicatorSlotProps.className),
                children: popupIcon
              }) : null]
            })
          }
        },
        inputProps: {
          className: classes2.input,
          disabled,
          readOnly,
          ...getInputProps()
        }
      })
    }), anchorEl ? (0, import_jsx_runtime117.jsx)(AutocompletePopper, {
      as: PopperSlot,
      ...popperProps,
      children: (0, import_jsx_runtime117.jsxs)(AutocompletePaper, {
        as: PaperSlot,
        ...paperProps,
        children: [loading && groupedOptions.length === 0 ? (0, import_jsx_runtime117.jsx)(AutocompleteLoading, {
          className: classes2.loading,
          ownerState,
          children: loadingText
        }) : null, groupedOptions.length === 0 && !freeSolo && !loading ? (0, import_jsx_runtime117.jsx)(AutocompleteNoOptions, {
          className: classes2.noOptions,
          ownerState,
          role: "presentation",
          onMouseDown: (event) => {
            event.preventDefault();
          },
          children: noOptionsText
        }) : null, groupedOptions.length > 0 ? (0, import_jsx_runtime117.jsx)(ListboxSlot, {
          as: ListboxComponentProp,
          ...listboxProps,
          children: groupedOptions.map((option, index) => {
            if (groupBy) {
              return renderGroup({
                key: option.key,
                group: option.group,
                children: option.options.map((option2, index2) => renderListOption(option2, option.index + index2))
              });
            }
            return renderListOption(option, index);
          })
        }) : null]
      })
    }) : null]
  });
});
true ? Autocomplete.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the portion of the selected suggestion that the user hasn't typed,
   * known as the completion string, appears inline after the input cursor in the textbox.
   * The inline completion string is visually highlighted and has a selected state.
   * @default false
   */
  autoComplete: import_prop_types92.default.bool,
  /**
   * If `true`, the first option is automatically highlighted.
   * @default false
   */
  autoHighlight: import_prop_types92.default.bool,
  /**
   * If `true`, the selected option becomes the value of the input
   * when the Autocomplete loses focus unless the user chooses
   * a different option or changes the character string in the input.
   *
   * When using the `freeSolo` mode, the typed value will be the input value
   * if the Autocomplete loses focus without highlighting an option.
   * @default false
   */
  autoSelect: import_prop_types92.default.bool,
  /**
   * Control if the input should be blurred when an option is selected:
   *
   * - `false` the input is not blurred.
   * - `true` the input is always blurred.
   * - `touch` the input is blurred after a touch event.
   * - `mouse` the input is blurred after a mouse event.
   * @default false
   */
  blurOnSelect: import_prop_types92.default.oneOfType([import_prop_types92.default.oneOf(["mouse", "touch"]), import_prop_types92.default.bool]),
  /**
   * Props applied to the [`Chip`](https://mui.com/material-ui/api/chip/) element.
   * @deprecated Use `slotProps.chip` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  ChipProps: import_prop_types92.default.object,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types92.default.object,
  /**
   * @ignore
   */
  className: import_prop_types92.default.string,
  /**
   * The icon to display in place of the default clear icon.
   * @default <ClearIcon fontSize="small" />
   */
  clearIcon: import_prop_types92.default.node,
  /**
   * If `true`, the input's text is cleared on blur if no value is selected.
   *
   * Set it to `true` if you want to help the user enter a new value.
   * Set it to `false` if you want to help the user resume their search.
   * @default !props.freeSolo
   */
  clearOnBlur: import_prop_types92.default.bool,
  /**
   * If `true`, clear all values when the user presses escape and the popup is closed.
   * @default false
   */
  clearOnEscape: import_prop_types92.default.bool,
  /**
   * Override the default text for the *clear* icon button.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'Clear'
   */
  clearText: import_prop_types92.default.string,
  /**
   * Override the default text for the *close popup* icon button.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'Close'
   */
  closeText: import_prop_types92.default.string,
  /**
   * The props used for each slot inside.
   * @deprecated Use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  componentsProps: import_prop_types92.default.shape({
    clearIndicator: import_prop_types92.default.object,
    paper: import_prop_types92.default.object,
    popper: import_prop_types92.default.object,
    popupIndicator: import_prop_types92.default.object
  }),
  /**
   * The default value. Use when the component is not controlled.
   * @default props.multiple ? [] : null
   */
  defaultValue: chainPropTypes(import_prop_types92.default.any, (props) => {
    if (props.multiple && props.defaultValue !== void 0 && !Array.isArray(props.defaultValue)) {
      return new Error(["MUI: The Autocomplete expects the `defaultValue` prop to be an array when `multiple={true}` or undefined.", `However, ${props.defaultValue} was provided.`].join("\n"));
    }
    return null;
  }),
  /**
   * If `true`, the input can't be cleared.
   * @default false
   */
  disableClearable: import_prop_types92.default.bool,
  /**
   * If `true`, the popup won't close when a value is selected.
   * @default false
   */
  disableCloseOnSelect: import_prop_types92.default.bool,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types92.default.bool,
  /**
   * If `true`, will allow focus on disabled items.
   * @default false
   */
  disabledItemsFocusable: import_prop_types92.default.bool,
  /**
   * If `true`, the list box in the popup will not wrap focus.
   * @default false
   */
  disableListWrap: import_prop_types92.default.bool,
  /**
   * If `true`, the `Popper` content will be under the DOM hierarchy of the parent component.
   * @default false
   */
  disablePortal: import_prop_types92.default.bool,
  /**
   * A function that determines the filtered options to be rendered on search.
   *
   * @default createFilterOptions()
   * @param {Value[]} options The options to render.
   * @param {object} state The state of the component.
   * @returns {Value[]}
   */
  filterOptions: import_prop_types92.default.func,
  /**
   * If `true`, hide the selected options from the list box.
   * @default false
   */
  filterSelectedOptions: import_prop_types92.default.bool,
  /**
   * Force the visibility display of the popup icon.
   * @default 'auto'
   */
  forcePopupIcon: import_prop_types92.default.oneOfType([import_prop_types92.default.oneOf(["auto"]), import_prop_types92.default.bool]),
  /**
   * If `true`, the Autocomplete is free solo, meaning that the user input is not bound to provided options.
   * @default false
   */
  freeSolo: import_prop_types92.default.bool,
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types92.default.bool,
  /**
   * The label to display when the tags are truncated (`limitTags`).
   *
   * @param {number} more The number of truncated tags.
   * @returns {ReactNode}
   * @default (more) => `+${more}`
   */
  getLimitTagsText: import_prop_types92.default.func,
  /**
   * Used to determine the disabled state for a given option.
   *
   * @param {Value} option The option to test.
   * @template Value The option shape. Will be the same shape as an item of the options.
   * @returns {boolean}
   */
  getOptionDisabled: import_prop_types92.default.func,
  /**
   * Used to determine the key for a given option.
   * This can be useful when the labels of options are not unique (since labels are used as keys by default).
   *
   * @param {Value} option The option to get the key for.
   * @returns {string | number}
   */
  getOptionKey: import_prop_types92.default.func,
  /**
   * Used to determine the string value for a given option.
   * It's used to fill the input (and the list box options if `renderOption` is not provided).
   *
   * If used in free solo mode, it must accept both the type of the options and a string.
   *
   * @param {Value} option
   * @returns {string}
   * @default (option) => option.label ?? option
   */
  getOptionLabel: import_prop_types92.default.func,
  /**
   * If provided, the options will be grouped under the returned string.
   * The groupBy value is also used as the text for group headings when `renderGroup` is not provided.
   *
   * @param {Value} option The Autocomplete option.
   * @returns {string}
   */
  groupBy: import_prop_types92.default.func,
  /**
   * If `true`, the component handles the "Home" and "End" keys when the popup is open.
   * It should move focus to the first option and last option, respectively.
   * @default !props.freeSolo
   */
  handleHomeEndKeys: import_prop_types92.default.bool,
  /**
   * This prop is used to help implement the accessibility logic.
   * If you don't provide an id it will fall back to a randomly generated one.
   */
  id: import_prop_types92.default.string,
  /**
   * If `true`, the highlight can move to the input.
   * @default false
   */
  includeInputInList: import_prop_types92.default.bool,
  /**
   * The input value.
   */
  inputValue: import_prop_types92.default.string,
  /**
   * Used to determine if the option represents the given value.
   * Uses strict equality by default.
   * ⚠️ Both arguments need to be handled, an option can only match with one value.
   *
   * @param {Value} option The option to test.
   * @param {Value} value The value to test against.
   * @returns {boolean}
   */
  isOptionEqualToValue: import_prop_types92.default.func,
  /**
   * The maximum number of tags that will be visible when not focused.
   * Set `-1` to disable the limit.
   * @default -1
   */
  limitTags: integerPropType_default,
  /**
   * The component used to render the listbox.
   * @default 'ul'
   * @deprecated Use `slotProps.listbox.component` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  ListboxComponent: import_prop_types92.default.elementType,
  /**
   * Props applied to the Listbox element.
   * @deprecated Use `slotProps.listbox` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  ListboxProps: import_prop_types92.default.object,
  /**
   * If `true`, the component is in a loading state.
   * This shows the `loadingText` in place of suggestions (only if there are no suggestions to show, for example `options` are empty).
   * @default false
   */
  loading: import_prop_types92.default.bool,
  /**
   * Text to display when in a loading state.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'Loading…'
   */
  loadingText: import_prop_types92.default.node,
  /**
   * If `true`, `value` must be an array and the menu will support multiple selections.
   * @default false
   */
  multiple: import_prop_types92.default.bool,
  /**
   * Text to display when there are no options.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'No options'
   */
  noOptionsText: import_prop_types92.default.node,
  /**
   * Callback fired when the value changes.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   * @param {Value|Value[]} value The new value of the component.
   * @param {string} reason One of "createOption", "selectOption", "removeOption", "blur" or "clear".
   * @param {string} [details]
   */
  onChange: import_prop_types92.default.func,
  /**
   * Callback fired when the popup requests to be closed.
   * Use in controlled mode (see open).
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   * @param {string} reason Can be: `"toggleInput"`, `"escape"`, `"selectOption"`, `"removeOption"`, `"blur"`.
   */
  onClose: import_prop_types92.default.func,
  /**
   * Callback fired when the highlight option changes.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   * @param {Value} option The highlighted option.
   * @param {string} reason Can be: `"keyboard"`, `"mouse"`, `"touch"`.
   */
  onHighlightChange: import_prop_types92.default.func,
  /**
   * Callback fired when the input value changes.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   * @param {string} value The new value of the text input.
   * @param {string} reason Can be: `"input"` (user input), `"reset"` (programmatic change), `"clear"`, `"blur"`, `"selectOption"`, `"removeOption"`
   */
  onInputChange: import_prop_types92.default.func,
  /**
   * @ignore
   */
  onKeyDown: import_prop_types92.default.func,
  /**
   * Callback fired when the popup requests to be opened.
   * Use in controlled mode (see open).
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   */
  onOpen: import_prop_types92.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types92.default.bool,
  /**
   * If `true`, the popup will open on input focus.
   * @default false
   */
  openOnFocus: import_prop_types92.default.bool,
  /**
   * Override the default text for the *open popup* icon button.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'Open'
   */
  openText: import_prop_types92.default.string,
  /**
   * A list of options that will be shown in the Autocomplete.
   */
  options: import_prop_types92.default.array.isRequired,
  /**
   * The component used to render the body of the popup.
   * @default Paper
   * @deprecated Use `slots.paper` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  PaperComponent: import_prop_types92.default.elementType,
  /**
   * The component used to position the popup.
   * @default Popper
   * @deprecated Use `slots.popper` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  PopperComponent: import_prop_types92.default.elementType,
  /**
   * The icon to display in place of the default popup icon.
   * @default <ArrowDropDownIcon />
   */
  popupIcon: import_prop_types92.default.node,
  /**
   * If `true`, the component becomes readonly. It is also supported for multiple tags where the tag cannot be deleted.
   * @default false
   */
  readOnly: import_prop_types92.default.bool,
  /**
   * Render the group.
   *
   * @param {AutocompleteRenderGroupParams} params The group to render.
   * @returns {ReactNode}
   */
  renderGroup: import_prop_types92.default.func,
  /**
   * Render the input.
   *
   * **Note:** The `renderInput` prop must return a `TextField` component or a compatible custom component
   * that correctly forwards `InputProps.ref` and spreads `inputProps`. This ensures proper integration
   * with the Autocomplete's internal logic (e.g., focus management and keyboard navigation).
   *
   * Avoid using components like `DatePicker` or `Select` directly, as they may not forward the required props,
   * leading to runtime errors or unexpected behavior.
   *
   * @param {object} params
   * @returns {ReactNode}
   */
  renderInput: import_prop_types92.default.func.isRequired,
  /**
   * Render the option, use `getOptionLabel` by default.
   *
   * @param {object} props The props to apply on the li element.
   * @param {Value} option The option to render.
   * @param {object} state The state of each option.
   * @param {object} ownerState The state of the Autocomplete component.
   * @returns {ReactNode}
   */
  renderOption: import_prop_types92.default.func,
  /**
   * Render the selected value when doing multiple selections.
   *
   * @deprecated Use `renderValue` prop instead
   *
   * @param {Value[]} value The `value` provided to the component.
   * @param {function} getTagProps A tag props getter.
   * @param {object} ownerState The state of the Autocomplete component.
   * @returns {ReactNode}
   */
  renderTags: import_prop_types92.default.func,
  /**
   * Renders the selected value(s) as rich content in the input for both single and multiple selections.
   *
   * @param {AutocompleteRenderValue<Value, Multiple, FreeSolo>} value The `value` provided to the component.
   * @param {function} getItemProps The value item props.
   * @param {object} ownerState The state of the Autocomplete component.
   * @returns {ReactNode}
   */
  renderValue: import_prop_types92.default.func,
  /**
   * If `true`, the input's text is selected on focus.
   * It helps the user clear the selected value.
   * @default !props.freeSolo
   */
  selectOnFocus: import_prop_types92.default.bool,
  /**
   * The size of the component.
   * @default 'medium'
   */
  size: import_prop_types92.default.oneOfType([import_prop_types92.default.oneOf(["small", "medium"]), import_prop_types92.default.string]),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types92.default.shape({
    chip: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object]),
    clearIndicator: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object]),
    listbox: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object]),
    paper: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object]),
    popper: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object]),
    popupIndicator: import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types92.default.shape({
    listbox: import_prop_types92.default.elementType,
    paper: import_prop_types92.default.elementType,
    popper: import_prop_types92.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types92.default.oneOfType([import_prop_types92.default.arrayOf(import_prop_types92.default.oneOfType([import_prop_types92.default.func, import_prop_types92.default.object, import_prop_types92.default.bool])), import_prop_types92.default.func, import_prop_types92.default.object]),
  /**
   * The value of the autocomplete.
   *
   * The value must have reference equality with the option in order to be selected.
   * You can customize the equality behavior with the `isOptionEqualToValue` prop.
   */
  value: chainPropTypes(import_prop_types92.default.any, (props) => {
    if (props.multiple && props.value !== void 0 && !Array.isArray(props.value)) {
      return new Error(["MUI: The Autocomplete expects the `value` prop to be an array when `multiple={true}` or undefined.", `However, ${props.value} was provided.`].join("\n"));
    }
    return null;
  })
} : void 0;
var Autocomplete_default = Autocomplete;

// node_modules/@mui/material/esm/Badge/Badge.js
var React150 = __toESM(require_react(), 1);
var import_prop_types93 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Badge/useBadge.js
function useBadge(parameters) {
  const {
    badgeContent: badgeContentProp,
    invisible: invisibleProp = false,
    max: maxProp = 99,
    showZero = false
  } = parameters;
  const prevProps = usePreviousProps_default({
    badgeContent: badgeContentProp,
    max: maxProp
  });
  let invisible = invisibleProp;
  if (invisibleProp === false && badgeContentProp === 0 && !showZero) {
    invisible = true;
  }
  const {
    badgeContent,
    max: max2 = maxProp
  } = invisible ? prevProps : parameters;
  const displayValue = badgeContent && Number(badgeContent) > max2 ? `${max2}+` : badgeContent;
  return {
    badgeContent,
    invisible,
    max: max2,
    displayValue
  };
}
var useBadge_default = useBadge;

// node_modules/@mui/material/esm/Badge/badgeClasses.js
function getBadgeUtilityClass(slot) {
  return generateUtilityClass("MuiBadge", slot);
}
var badgeClasses = generateUtilityClasses("MuiBadge", [
  "root",
  "badge",
  "dot",
  "standard",
  "anchorOriginTopRight",
  "anchorOriginBottomRight",
  "anchorOriginTopLeft",
  "anchorOriginBottomLeft",
  "invisible",
  "colorError",
  "colorInfo",
  "colorPrimary",
  "colorSecondary",
  "colorSuccess",
  "colorWarning",
  "overlapRectangular",
  "overlapCircular",
  // TODO: v6 remove the overlap value from these class keys
  "anchorOriginTopLeftCircular",
  "anchorOriginTopLeftRectangular",
  "anchorOriginTopRightCircular",
  "anchorOriginTopRightRectangular",
  "anchorOriginBottomLeftCircular",
  "anchorOriginBottomLeftRectangular",
  "anchorOriginBottomRightCircular",
  "anchorOriginBottomRightRectangular"
]);
var badgeClasses_default = badgeClasses;

// node_modules/@mui/material/esm/Badge/Badge.js
var import_jsx_runtime118 = __toESM(require_jsx_runtime(), 1);
var RADIUS_STANDARD = 10;
var RADIUS_DOT = 4;
var useUtilityClasses55 = (ownerState) => {
  const {
    color,
    anchorOrigin,
    invisible,
    overlap,
    variant,
    classes: classes2 = {}
  } = ownerState;
  const slots = {
    root: ["root"],
    badge: ["badge", variant, invisible && "invisible", `anchorOrigin${capitalize_default(anchorOrigin.vertical)}${capitalize_default(anchorOrigin.horizontal)}`, `anchorOrigin${capitalize_default(anchorOrigin.vertical)}${capitalize_default(anchorOrigin.horizontal)}${capitalize_default(overlap)}`, `overlap${capitalize_default(overlap)}`, color !== "default" && `color${capitalize_default(color)}`]
  };
  return composeClasses(slots, getBadgeUtilityClass, classes2);
};
var BadgeRoot = styled_default2("span", {
  name: "MuiBadge",
  slot: "Root"
})({
  position: "relative",
  display: "inline-flex",
  // For correct alignment with the text.
  verticalAlign: "middle",
  flexShrink: 0
});
var BadgeBadge = styled_default2("span", {
  name: "MuiBadge",
  slot: "Badge",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.badge, styles5[ownerState.variant], styles5[`anchorOrigin${capitalize_default(ownerState.anchorOrigin.vertical)}${capitalize_default(ownerState.anchorOrigin.horizontal)}${capitalize_default(ownerState.overlap)}`], ownerState.color !== "default" && styles5[`color${capitalize_default(ownerState.color)}`], ownerState.invisible && styles5.invisible];
  }
})(memoTheme_default(({
  theme
}) => ({
  display: "flex",
  flexDirection: "row",
  flexWrap: "wrap",
  justifyContent: "center",
  alignContent: "center",
  alignItems: "center",
  position: "absolute",
  boxSizing: "border-box",
  fontFamily: theme.typography.fontFamily,
  fontWeight: theme.typography.fontWeightMedium,
  fontSize: theme.typography.pxToRem(12),
  minWidth: RADIUS_STANDARD * 2,
  lineHeight: 1,
  padding: "0 6px",
  height: RADIUS_STANDARD * 2,
  borderRadius: RADIUS_STANDARD,
  zIndex: 1,
  // Render the badge on top of potential ripples.
  transition: theme.transitions.create("transform", {
    easing: theme.transitions.easing.easeInOut,
    duration: theme.transitions.duration.enteringScreen
  }),
  variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter(["contrastText"])).map(([color]) => ({
    props: {
      color
    },
    style: {
      backgroundColor: (theme.vars || theme).palette[color].main,
      color: (theme.vars || theme).palette[color].contrastText
    }
  })), {
    props: {
      variant: "dot"
    },
    style: {
      borderRadius: RADIUS_DOT,
      height: RADIUS_DOT * 2,
      minWidth: RADIUS_DOT * 2,
      padding: 0
    }
  }, {
    props: {
      invisible: true
    },
    style: {
      transition: theme.transitions.create("transform", {
        easing: theme.transitions.easing.easeInOut,
        duration: theme.transitions.duration.leavingScreen
      })
    }
  }, {
    style: ({
      ownerState
    }) => {
      const {
        vertical,
        horizontal
      } = ownerState.anchorOrigin;
      const offset2 = ownerState.overlap === "circular" ? "14%" : 0;
      return {
        "--Badge-translateX": horizontal === "right" ? "50%" : "-50%",
        "--Badge-translateY": vertical === "top" ? "-50%" : "50%",
        top: vertical === "top" ? offset2 : "initial",
        bottom: vertical === "bottom" ? offset2 : "initial",
        right: horizontal === "right" ? offset2 : "initial",
        left: horizontal === "left" ? offset2 : "initial",
        transform: "scale(1) translate(var(--Badge-translateX), var(--Badge-translateY))",
        transformOrigin: `${horizontal === "right" ? "100%" : "0%"} ${vertical === "top" ? "0%" : "100%"}`,
        [`&.${badgeClasses_default.invisible}`]: {
          transform: "scale(0) translate(var(--Badge-translateX), var(--Badge-translateY))"
        }
      };
    }
  }]
})));
function getAnchorOrigin(anchorOrigin) {
  return {
    vertical: (anchorOrigin == null ? void 0 : anchorOrigin.vertical) ?? "top",
    horizontal: (anchorOrigin == null ? void 0 : anchorOrigin.horizontal) ?? "right"
  };
}
var Badge = React150.forwardRef(function Badge2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiBadge"
  });
  const {
    anchorOrigin: anchorOriginProp,
    className,
    classes: classesProp,
    component,
    components = {},
    componentsProps = {},
    children: children2,
    overlap: overlapProp = "rectangular",
    color: colorProp = "default",
    invisible: invisibleProp = false,
    max: maxProp = 99,
    badgeContent: badgeContentProp,
    slots,
    slotProps,
    showZero = false,
    variant: variantProp = "standard",
    ...other
  } = props;
  const {
    badgeContent,
    invisible: invisibleFromHook,
    max: max2,
    displayValue: displayValueFromHook
  } = useBadge_default({
    max: maxProp,
    invisible: invisibleProp,
    badgeContent: badgeContentProp,
    showZero
  });
  const prevProps = usePreviousProps_default({
    anchorOrigin: getAnchorOrigin(anchorOriginProp),
    color: colorProp,
    overlap: overlapProp,
    variant: variantProp,
    badgeContent: badgeContentProp
  });
  const invisible = invisibleFromHook || badgeContent == null && variantProp !== "dot";
  const {
    color = colorProp,
    overlap = overlapProp,
    anchorOrigin: anchorOriginPropProp,
    variant = variantProp
  } = invisible ? prevProps : props;
  const anchorOrigin = getAnchorOrigin(anchorOriginPropProp);
  const displayValue = variant !== "dot" ? displayValueFromHook : void 0;
  const ownerState = {
    ...props,
    badgeContent,
    invisible,
    max: max2,
    displayValue,
    showZero,
    anchorOrigin,
    color,
    overlap,
    variant
  };
  const classes2 = useUtilityClasses55(ownerState);
  const externalForwardedProps = {
    slots: {
      root: (slots == null ? void 0 : slots.root) ?? components.Root,
      badge: (slots == null ? void 0 : slots.badge) ?? components.Badge
    },
    slotProps: {
      root: (slotProps == null ? void 0 : slotProps.root) ?? componentsProps.root,
      badge: (slotProps == null ? void 0 : slotProps.badge) ?? componentsProps.badge
    }
  };
  const [RootSlot, rootProps] = useSlot("root", {
    elementType: BadgeRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other
    },
    ownerState,
    className: clsx_default(classes2.root, className),
    ref,
    additionalProps: {
      as: component
    }
  });
  const [BadgeSlot, badgeProps] = useSlot("badge", {
    elementType: BadgeBadge,
    externalForwardedProps,
    ownerState,
    className: classes2.badge
  });
  return (0, import_jsx_runtime118.jsxs)(RootSlot, {
    ...rootProps,
    children: [children2, (0, import_jsx_runtime118.jsx)(BadgeSlot, {
      ...badgeProps,
      children: displayValue
    })]
  });
});
true ? Badge.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The anchor of the badge.
   * @default {
   *   vertical: 'top',
   *   horizontal: 'right',
   * }
   */
  anchorOrigin: import_prop_types93.default.shape({
    horizontal: import_prop_types93.default.oneOf(["left", "right"]),
    vertical: import_prop_types93.default.oneOf(["bottom", "top"])
  }),
  /**
   * The content rendered within the badge.
   */
  badgeContent: import_prop_types93.default.node,
  /**
   * The badge will be added relative to this node.
   */
  children: import_prop_types93.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types93.default.object,
  /**
   * @ignore
   */
  className: import_prop_types93.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'default'
   */
  color: import_prop_types93.default.oneOfType([import_prop_types93.default.oneOf(["default", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types93.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types93.default.elementType,
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types93.default.shape({
    Badge: import_prop_types93.default.elementType,
    Root: import_prop_types93.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types93.default.shape({
    badge: import_prop_types93.default.oneOfType([import_prop_types93.default.func, import_prop_types93.default.object]),
    root: import_prop_types93.default.oneOfType([import_prop_types93.default.func, import_prop_types93.default.object])
  }),
  /**
   * If `true`, the badge is invisible.
   * @default false
   */
  invisible: import_prop_types93.default.bool,
  /**
   * Max count to show.
   * @default 99
   */
  max: import_prop_types93.default.number,
  /**
   * Wrapped shape the badge should overlap.
   * @default 'rectangular'
   */
  overlap: import_prop_types93.default.oneOf(["circular", "rectangular"]),
  /**
   * Controls whether the badge is hidden when `badgeContent` is zero.
   * @default false
   */
  showZero: import_prop_types93.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types93.default.shape({
    badge: import_prop_types93.default.oneOfType([import_prop_types93.default.func, import_prop_types93.default.object]),
    root: import_prop_types93.default.oneOfType([import_prop_types93.default.func, import_prop_types93.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types93.default.shape({
    badge: import_prop_types93.default.elementType,
    root: import_prop_types93.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types93.default.oneOfType([import_prop_types93.default.arrayOf(import_prop_types93.default.oneOfType([import_prop_types93.default.func, import_prop_types93.default.object, import_prop_types93.default.bool])), import_prop_types93.default.func, import_prop_types93.default.object]),
  /**
   * The variant to use.
   * @default 'standard'
   */
  variant: import_prop_types93.default.oneOfType([import_prop_types93.default.oneOf(["dot", "standard"]), import_prop_types93.default.string])
} : void 0;
var Badge_default = Badge;

// node_modules/@mui/material/esm/Checkbox/Checkbox.js
var React152 = __toESM(require_react(), 1);
var import_prop_types95 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/internal/SwitchBase.js
var React151 = __toESM(require_react(), 1);
var import_prop_types94 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/internal/switchBaseClasses.js
function getSwitchBaseUtilityClass(slot) {
  return generateUtilityClass("PrivateSwitchBase", slot);
}
var switchBaseClasses = generateUtilityClasses("PrivateSwitchBase", ["root", "checked", "disabled", "input", "edgeStart", "edgeEnd"]);

// node_modules/@mui/material/esm/internal/SwitchBase.js
var import_jsx_runtime119 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses56 = (ownerState) => {
  const {
    classes: classes2,
    checked,
    disabled,
    edge
  } = ownerState;
  const slots = {
    root: ["root", checked && "checked", disabled && "disabled", edge && `edge${capitalize_default(edge)}`],
    input: ["input"]
  };
  return composeClasses(slots, getSwitchBaseUtilityClass, classes2);
};
var SwitchBaseRoot = styled_default2(ButtonBase_default, {
  name: "MuiSwitchBase"
})({
  padding: 9,
  borderRadius: "50%",
  variants: [{
    props: {
      edge: "start",
      size: "small"
    },
    style: {
      marginLeft: -3
    }
  }, {
    props: ({
      edge,
      ownerState
    }) => edge === "start" && ownerState.size !== "small",
    style: {
      marginLeft: -12
    }
  }, {
    props: {
      edge: "end",
      size: "small"
    },
    style: {
      marginRight: -3
    }
  }, {
    props: ({
      edge,
      ownerState
    }) => edge === "end" && ownerState.size !== "small",
    style: {
      marginRight: -12
    }
  }]
});
var SwitchBaseInput = styled_default2("input", {
  name: "MuiSwitchBase",
  shouldForwardProp: rootShouldForwardProp_default
})({
  cursor: "inherit",
  position: "absolute",
  opacity: 0,
  width: "100%",
  height: "100%",
  top: 0,
  left: 0,
  margin: 0,
  padding: 0,
  zIndex: 1
});
var SwitchBase = React151.forwardRef(function SwitchBase2(props, ref) {
  const {
    autoFocus,
    checked: checkedProp,
    checkedIcon,
    defaultChecked,
    disabled: disabledProp,
    disableFocusRipple = false,
    edge = false,
    icon,
    id,
    inputProps,
    inputRef,
    name,
    onBlur,
    onChange,
    onFocus,
    readOnly,
    required = false,
    tabIndex,
    type,
    value,
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const [checked, setCheckedState] = useControlled_default({
    controlled: checkedProp,
    default: Boolean(defaultChecked),
    name: "SwitchBase",
    state: "checked"
  });
  const muiFormControl = useFormControl();
  const handleFocus = (event) => {
    if (onFocus) {
      onFocus(event);
    }
    if (muiFormControl && muiFormControl.onFocus) {
      muiFormControl.onFocus(event);
    }
  };
  const handleBlur = (event) => {
    if (onBlur) {
      onBlur(event);
    }
    if (muiFormControl && muiFormControl.onBlur) {
      muiFormControl.onBlur(event);
    }
  };
  const handleInputChange = (event) => {
    if (event.nativeEvent.defaultPrevented || readOnly) {
      return;
    }
    const newChecked = event.target.checked;
    setCheckedState(newChecked);
    if (onChange) {
      onChange(event, newChecked);
    }
  };
  let disabled = disabledProp;
  if (muiFormControl) {
    if (typeof disabled === "undefined") {
      disabled = muiFormControl.disabled;
    }
  }
  const hasLabelFor = type === "checkbox" || type === "radio";
  const ownerState = {
    ...props,
    checked,
    disabled,
    disableFocusRipple,
    edge
  };
  const classes2 = useUtilityClasses56(ownerState);
  const externalForwardedProps = {
    slots,
    slotProps: {
      input: inputProps,
      ...slotProps
    }
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref,
    elementType: SwitchBaseRoot,
    className: classes2.root,
    shouldForwardComponentProp: true,
    externalForwardedProps: {
      ...externalForwardedProps,
      component: "span",
      ...other
    },
    getSlotProps: (handlers) => ({
      ...handlers,
      onFocus: (event) => {
        var _a;
        (_a = handlers.onFocus) == null ? void 0 : _a.call(handlers, event);
        handleFocus(event);
      },
      onBlur: (event) => {
        var _a;
        (_a = handlers.onBlur) == null ? void 0 : _a.call(handlers, event);
        handleBlur(event);
      }
    }),
    ownerState,
    additionalProps: {
      centerRipple: true,
      focusRipple: !disableFocusRipple,
      role: void 0,
      tabIndex: null
    }
  });
  const [InputSlot, inputSlotProps] = useSlot("input", {
    ref: inputRef,
    elementType: SwitchBaseInput,
    className: classes2.input,
    externalForwardedProps,
    getSlotProps: (handlers) => ({
      ...handlers,
      onChange: (event) => {
        var _a;
        (_a = handlers.onChange) == null ? void 0 : _a.call(handlers, event);
        handleInputChange(event);
      }
    }),
    ownerState,
    additionalProps: {
      autoFocus,
      checked: checkedProp,
      defaultChecked,
      disabled,
      id: hasLabelFor ? id : void 0,
      name,
      readOnly,
      required,
      tabIndex,
      type,
      ...type === "checkbox" && value === void 0 ? {} : {
        value
      }
    }
  });
  return (0, import_jsx_runtime119.jsxs)(RootSlot, {
    ...rootSlotProps,
    children: [(0, import_jsx_runtime119.jsx)(InputSlot, {
      ...inputSlotProps
    }), checked ? checkedIcon : icon]
  });
});
true ? SwitchBase.propTypes = {
  /**
   * If `true`, the `input` element is focused during the first mount.
   */
  autoFocus: import_prop_types94.default.bool,
  /**
   * If `true`, the component is checked.
   */
  checked: import_prop_types94.default.bool,
  /**
   * The icon to display when the component is checked.
   */
  checkedIcon: import_prop_types94.default.node.isRequired,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types94.default.object,
  /**
   * @ignore
   */
  className: import_prop_types94.default.string,
  /**
   * @ignore
   */
  defaultChecked: import_prop_types94.default.bool,
  /**
   * If `true`, the component is disabled.
   */
  disabled: import_prop_types94.default.bool,
  /**
   * If `true`, the  keyboard focus ripple is disabled.
   * @default false
   */
  disableFocusRipple: import_prop_types94.default.bool,
  /**
   * If given, uses a negative margin to counteract the padding on one
   * side (this is often helpful for aligning the left or right
   * side of the icon with content above or below, without ruining the border
   * size and shape).
   * @default false
   */
  edge: import_prop_types94.default.oneOf(["end", "start", false]),
  /**
   * The icon to display when the component is unchecked.
   */
  icon: import_prop_types94.default.node.isRequired,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types94.default.string,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   */
  inputProps: import_prop_types94.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /*
   * @ignore
   */
  name: import_prop_types94.default.string,
  /**
   * @ignore
   */
  onBlur: import_prop_types94.default.func,
  /**
   * Callback fired when the state is changed.
   *
   * @param {object} event The event source of the callback.
   * You can pull out the new checked state by accessing `event.target.checked` (boolean).
   */
  onChange: import_prop_types94.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types94.default.func,
  /**
   * It prevents the user from changing the value of the field
   * (not from interacting with the field).
   */
  readOnly: import_prop_types94.default.bool,
  /**
   * If `true`, the `input` element is required.
   */
  required: import_prop_types94.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types94.default.shape({
    input: import_prop_types94.default.oneOfType([import_prop_types94.default.func, import_prop_types94.default.object]),
    root: import_prop_types94.default.oneOfType([import_prop_types94.default.func, import_prop_types94.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types94.default.shape({
    input: import_prop_types94.default.elementType,
    root: import_prop_types94.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types94.default.object,
  /**
   * @ignore
   */
  tabIndex: import_prop_types94.default.oneOfType([import_prop_types94.default.number, import_prop_types94.default.string]),
  /**
   * The input component prop `type`.
   */
  type: import_prop_types94.default.string.isRequired,
  /**
   * The value of the component.
   */
  value: import_prop_types94.default.any
} : void 0;
var SwitchBase_default = SwitchBase;

// node_modules/@mui/material/esm/internal/svg-icons/CheckBoxOutlineBlank.js
var import_jsx_runtime120 = __toESM(require_jsx_runtime(), 1);
var CheckBoxOutlineBlank_default = createSvgIcon((0, import_jsx_runtime120.jsx)("path", {
  d: "M19 5v14H5V5h14m0-2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2z"
}), "CheckBoxOutlineBlank");

// node_modules/@mui/material/esm/internal/svg-icons/CheckBox.js
var import_jsx_runtime121 = __toESM(require_jsx_runtime(), 1);
var CheckBox_default = createSvgIcon((0, import_jsx_runtime121.jsx)("path", {
  d: "M19 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.11 0 2-.9 2-2V5c0-1.1-.89-2-2-2zm-9 14l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"
}), "CheckBox");

// node_modules/@mui/material/esm/internal/svg-icons/IndeterminateCheckBox.js
var import_jsx_runtime122 = __toESM(require_jsx_runtime(), 1);
var IndeterminateCheckBox_default = createSvgIcon((0, import_jsx_runtime122.jsx)("path", {
  d: "M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-2 10H7v-2h10v2z"
}), "IndeterminateCheckBox");

// node_modules/@mui/material/esm/Checkbox/checkboxClasses.js
function getCheckboxUtilityClass(slot) {
  return generateUtilityClass("MuiCheckbox", slot);
}
var checkboxClasses = generateUtilityClasses("MuiCheckbox", ["root", "checked", "disabled", "indeterminate", "colorPrimary", "colorSecondary", "sizeSmall", "sizeMedium"]);
var checkboxClasses_default = checkboxClasses;

// node_modules/@mui/material/esm/Checkbox/Checkbox.js
var import_jsx_runtime123 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses57 = (ownerState) => {
  const {
    classes: classes2,
    indeterminate,
    color,
    size
  } = ownerState;
  const slots = {
    root: ["root", indeterminate && "indeterminate", `color${capitalize_default(color)}`, `size${capitalize_default(size)}`]
  };
  const composedClasses = composeClasses(slots, getCheckboxUtilityClass, classes2);
  return {
    ...classes2,
    // forward the disabled and checked classes to the SwitchBase
    ...composedClasses
  };
};
var CheckboxRoot = styled_default2(SwitchBase_default, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiCheckbox",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.indeterminate && styles5.indeterminate, styles5[`size${capitalize_default(ownerState.size)}`], ownerState.color !== "default" && styles5[`color${capitalize_default(ownerState.color)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  color: (theme.vars || theme).palette.text.secondary,
  variants: [{
    props: {
      color: "default",
      disableRipple: false
    },
    style: {
      "&:hover": {
        backgroundColor: theme.alpha((theme.vars || theme).palette.action.active, (theme.vars || theme).palette.action.hoverOpacity)
      }
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color,
      disableRipple: false
    },
    style: {
      "&:hover": {
        backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity)
      }
    }
  })), ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      [`&.${checkboxClasses_default.checked}, &.${checkboxClasses_default.indeterminate}`]: {
        color: (theme.vars || theme).palette[color].main
      },
      [`&.${checkboxClasses_default.disabled}`]: {
        color: (theme.vars || theme).palette.action.disabled
      }
    }
  })), {
    // Should be last to override other colors
    props: {
      disableRipple: false
    },
    style: {
      // Reset on touch devices, it doesn't add specificity
      "&:hover": {
        "@media (hover: none)": {
          backgroundColor: "transparent"
        }
      }
    }
  }]
})));
var defaultCheckedIcon = (0, import_jsx_runtime123.jsx)(CheckBox_default, {});
var defaultIcon = (0, import_jsx_runtime123.jsx)(CheckBoxOutlineBlank_default, {});
var defaultIndeterminateIcon = (0, import_jsx_runtime123.jsx)(IndeterminateCheckBox_default, {});
var Checkbox = React152.forwardRef(function Checkbox2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiCheckbox"
  });
  const {
    checkedIcon = defaultCheckedIcon,
    color = "primary",
    icon: iconProp = defaultIcon,
    indeterminate = false,
    indeterminateIcon: indeterminateIconProp = defaultIndeterminateIcon,
    inputProps,
    size = "medium",
    disableRipple = false,
    className,
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const icon = indeterminate ? indeterminateIconProp : iconProp;
  const indeterminateIcon = indeterminate ? indeterminateIconProp : checkedIcon;
  const ownerState = {
    ...props,
    disableRipple,
    color,
    indeterminate,
    size
  };
  const classes2 = useUtilityClasses57(ownerState);
  const externalInputProps = slotProps.input ?? inputProps;
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref,
    elementType: CheckboxRoot,
    className: clsx_default(classes2.root, className),
    shouldForwardComponentProp: true,
    externalForwardedProps: {
      slots,
      slotProps,
      ...other
    },
    ownerState,
    additionalProps: {
      type: "checkbox",
      icon: React152.cloneElement(icon, {
        fontSize: icon.props.fontSize ?? size
      }),
      checkedIcon: React152.cloneElement(indeterminateIcon, {
        fontSize: indeterminateIcon.props.fontSize ?? size
      }),
      disableRipple,
      slots,
      slotProps: {
        input: mergeSlotProps(typeof externalInputProps === "function" ? externalInputProps(ownerState) : externalInputProps, {
          "data-indeterminate": indeterminate
        })
      }
    }
  });
  return (0, import_jsx_runtime123.jsx)(RootSlot, {
    ...rootSlotProps,
    classes: classes2
  });
});
true ? Checkbox.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the component is checked.
   */
  checked: import_prop_types95.default.bool,
  /**
   * The icon to display when the component is checked.
   * @default <CheckBoxIcon />
   */
  checkedIcon: import_prop_types95.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types95.default.object,
  /**
   * @ignore
   */
  className: import_prop_types95.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types95.default.oneOfType([import_prop_types95.default.oneOf(["default", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types95.default.string]),
  /**
   * The default checked state. Use when the component is not controlled.
   */
  defaultChecked: import_prop_types95.default.bool,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types95.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   * @default false
   */
  disableRipple: import_prop_types95.default.bool,
  /**
   * The icon to display when the component is unchecked.
   * @default <CheckBoxOutlineBlankIcon />
   */
  icon: import_prop_types95.default.node,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types95.default.string,
  /**
   * If `true`, the component appears indeterminate.
   * This does not set the native input element to indeterminate due
   * to inconsistent behavior across browsers.
   * However, we set a `data-indeterminate` attribute on the `input`.
   * @default false
   */
  indeterminate: import_prop_types95.default.bool,
  /**
   * The icon to display when the component is indeterminate.
   * @default <IndeterminateCheckBoxIcon />
   */
  indeterminateIcon: import_prop_types95.default.node,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @deprecated Use `slotProps.input` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  inputProps: import_prop_types95.default.object,
  /**
   * Callback fired when the state is changed.
   *
   * @param {React.ChangeEvent<HTMLInputElement>} event The event source of the callback.
   * You can pull out the new checked state by accessing `event.target.checked` (boolean).
   */
  onChange: import_prop_types95.default.func,
  /**
   * If `true`, the `input` element is required.
   * @default false
   */
  required: import_prop_types95.default.bool,
  /**
   * The size of the component.
   * `small` is equivalent to the dense checkbox styling.
   * @default 'medium'
   */
  size: import_prop_types95.default.oneOfType([import_prop_types95.default.oneOf(["medium", "small"]), import_prop_types95.default.string]),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types95.default.shape({
    input: import_prop_types95.default.oneOfType([import_prop_types95.default.func, import_prop_types95.default.object]),
    root: import_prop_types95.default.oneOfType([import_prop_types95.default.func, import_prop_types95.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types95.default.shape({
    input: import_prop_types95.default.elementType,
    root: import_prop_types95.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types95.default.oneOfType([import_prop_types95.default.arrayOf(import_prop_types95.default.oneOfType([import_prop_types95.default.func, import_prop_types95.default.object, import_prop_types95.default.bool])), import_prop_types95.default.func, import_prop_types95.default.object]),
  /**
   * The value of the component. The DOM API casts this to a string.
   * The browser uses "on" as the default value.
   */
  value: import_prop_types95.default.any
} : void 0;
var Checkbox_default = Checkbox;

// node_modules/@mui/material/esm/Divider/Divider.js
var React153 = __toESM(require_react(), 1);
var import_prop_types96 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Divider/dividerClasses.js
function getDividerUtilityClass(slot) {
  return generateUtilityClass("MuiDivider", slot);
}
var dividerClasses = generateUtilityClasses("MuiDivider", ["root", "absolute", "fullWidth", "inset", "middle", "flexItem", "light", "vertical", "withChildren", "withChildrenVertical", "textAlignRight", "textAlignLeft", "wrapper", "wrapperVertical"]);
var dividerClasses_default = dividerClasses;

// node_modules/@mui/material/esm/Divider/Divider.js
var import_jsx_runtime124 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses58 = (ownerState) => {
  const {
    absolute,
    children: children2,
    classes: classes2,
    flexItem,
    light,
    orientation,
    textAlign,
    variant
  } = ownerState;
  const slots = {
    root: ["root", absolute && "absolute", variant, light && "light", orientation === "vertical" && "vertical", flexItem && "flexItem", children2 && "withChildren", children2 && orientation === "vertical" && "withChildrenVertical", textAlign === "right" && orientation !== "vertical" && "textAlignRight", textAlign === "left" && orientation !== "vertical" && "textAlignLeft"],
    wrapper: ["wrapper", orientation === "vertical" && "wrapperVertical"]
  };
  return composeClasses(slots, getDividerUtilityClass, classes2);
};
var DividerRoot = styled_default2("div", {
  name: "MuiDivider",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.absolute && styles5.absolute, styles5[ownerState.variant], ownerState.light && styles5.light, ownerState.orientation === "vertical" && styles5.vertical, ownerState.flexItem && styles5.flexItem, ownerState.children && styles5.withChildren, ownerState.children && ownerState.orientation === "vertical" && styles5.withChildrenVertical, ownerState.textAlign === "right" && ownerState.orientation !== "vertical" && styles5.textAlignRight, ownerState.textAlign === "left" && ownerState.orientation !== "vertical" && styles5.textAlignLeft];
  }
})(memoTheme_default(({
  theme
}) => ({
  margin: 0,
  // Reset browser default style.
  flexShrink: 0,
  borderWidth: 0,
  borderStyle: "solid",
  borderColor: (theme.vars || theme).palette.divider,
  borderBottomWidth: "thin",
  variants: [{
    props: {
      absolute: true
    },
    style: {
      position: "absolute",
      bottom: 0,
      left: 0,
      width: "100%"
    }
  }, {
    props: {
      light: true
    },
    style: {
      borderColor: theme.alpha((theme.vars || theme).palette.divider, 0.08)
    }
  }, {
    props: {
      variant: "inset"
    },
    style: {
      marginLeft: 72
    }
  }, {
    props: {
      variant: "middle",
      orientation: "horizontal"
    },
    style: {
      marginLeft: theme.spacing(2),
      marginRight: theme.spacing(2)
    }
  }, {
    props: {
      variant: "middle",
      orientation: "vertical"
    },
    style: {
      marginTop: theme.spacing(1),
      marginBottom: theme.spacing(1)
    }
  }, {
    props: {
      orientation: "vertical"
    },
    style: {
      height: "100%",
      borderBottomWidth: 0,
      borderRightWidth: "thin"
    }
  }, {
    props: {
      flexItem: true
    },
    style: {
      alignSelf: "stretch",
      height: "auto"
    }
  }, {
    props: ({
      ownerState
    }) => !!ownerState.children,
    style: {
      display: "flex",
      textAlign: "center",
      border: 0,
      borderTopStyle: "solid",
      borderLeftStyle: "solid",
      "&::before, &::after": {
        content: '""',
        alignSelf: "center"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.children && ownerState.orientation !== "vertical",
    style: {
      "&::before, &::after": {
        width: "100%",
        borderTop: `thin solid ${(theme.vars || theme).palette.divider}`,
        borderTopStyle: "inherit"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.orientation === "vertical" && ownerState.children,
    style: {
      flexDirection: "column",
      "&::before, &::after": {
        height: "100%",
        borderLeft: `thin solid ${(theme.vars || theme).palette.divider}`,
        borderLeftStyle: "inherit"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.textAlign === "right" && ownerState.orientation !== "vertical",
    style: {
      "&::before": {
        width: "90%"
      },
      "&::after": {
        width: "10%"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.textAlign === "left" && ownerState.orientation !== "vertical",
    style: {
      "&::before": {
        width: "10%"
      },
      "&::after": {
        width: "90%"
      }
    }
  }]
})));
var DividerWrapper = styled_default2("span", {
  name: "MuiDivider",
  slot: "Wrapper",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.wrapper, ownerState.orientation === "vertical" && styles5.wrapperVertical];
  }
})(memoTheme_default(({
  theme
}) => ({
  display: "inline-block",
  paddingLeft: `calc(${theme.spacing(1)} * 1.2)`,
  paddingRight: `calc(${theme.spacing(1)} * 1.2)`,
  whiteSpace: "nowrap",
  variants: [{
    props: {
      orientation: "vertical"
    },
    style: {
      paddingTop: `calc(${theme.spacing(1)} * 1.2)`,
      paddingBottom: `calc(${theme.spacing(1)} * 1.2)`
    }
  }]
})));
var Divider2 = React153.forwardRef(function Divider3(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiDivider"
  });
  const {
    absolute = false,
    children: children2,
    className,
    orientation = "horizontal",
    component = children2 || orientation === "vertical" ? "div" : "hr",
    flexItem = false,
    light = false,
    role = component !== "hr" ? "separator" : void 0,
    textAlign = "center",
    variant = "fullWidth",
    ...other
  } = props;
  const ownerState = {
    ...props,
    absolute,
    component,
    flexItem,
    light,
    orientation,
    role,
    textAlign,
    variant
  };
  const classes2 = useUtilityClasses58(ownerState);
  return (0, import_jsx_runtime124.jsx)(DividerRoot, {
    as: component,
    className: clsx_default(classes2.root, className),
    role,
    ref,
    ownerState,
    "aria-orientation": role === "separator" && (component !== "hr" || orientation === "vertical") ? orientation : void 0,
    ...other,
    children: children2 ? (0, import_jsx_runtime124.jsx)(DividerWrapper, {
      className: classes2.wrapper,
      ownerState,
      children: children2
    }) : null
  });
});
if (Divider2) {
  Divider2.muiSkipListHighlight = true;
}
true ? Divider2.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Absolutely position the element.
   * @default false
   */
  absolute: import_prop_types96.default.bool,
  /**
   * The content of the component.
   */
  children: import_prop_types96.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types96.default.object,
  /**
   * @ignore
   */
  className: import_prop_types96.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types96.default.elementType,
  /**
   * If `true`, a vertical divider will have the correct height when used in flex container.
   * (By default, a vertical divider will have a calculated height of `0px` if it is the child of a flex container.)
   * @default false
   */
  flexItem: import_prop_types96.default.bool,
  /**
   * If `true`, the divider will have a lighter color.
   * @default false
   * @deprecated Use <Divider sx={{ opacity: 0.6 }} /> (or any opacity or color) instead. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  light: import_prop_types96.default.bool,
  /**
   * The component orientation.
   * @default 'horizontal'
   */
  orientation: import_prop_types96.default.oneOf(["horizontal", "vertical"]),
  /**
   * @ignore
   */
  role: import_prop_types96.default.string,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types96.default.oneOfType([import_prop_types96.default.arrayOf(import_prop_types96.default.oneOfType([import_prop_types96.default.func, import_prop_types96.default.object, import_prop_types96.default.bool])), import_prop_types96.default.func, import_prop_types96.default.object]),
  /**
   * The text alignment.
   * @default 'center'
   */
  textAlign: import_prop_types96.default.oneOf(["center", "left", "right"]),
  /**
   * The variant to use.
   * @default 'fullWidth'
   */
  variant: import_prop_types96.default.oneOfType([import_prop_types96.default.oneOf(["fullWidth", "inset", "middle"]), import_prop_types96.default.string])
} : void 0;
var Divider_default = Divider2;

// node_modules/@mui/material/esm/Unstable_TrapFocus/FocusTrap.js
var React154 = __toESM(require_react(), 1);
var import_prop_types98 = __toESM(require_prop_types(), 1);

// node_modules/@mui/utils/esm/elementAcceptingRef/elementAcceptingRef.js
var import_prop_types97 = __toESM(require_prop_types(), 1);
function isClassComponent2(elementType) {
  const {
    prototype = {}
  } = elementType;
  return Boolean(prototype.isReactComponent);
}
function acceptingRef(props, propName, componentName, location, propFullName) {
  const element = props[propName];
  const safePropName = propFullName || propName;
  if (element == null || // When server-side rendering React doesn't warn either.
  // This is not an accurate check for SSR.
  // This is only in place for Emotion compat.
  // TODO: Revisit once https://github.com/facebook/react/issues/20047 is resolved.
  typeof window === "undefined") {
    return null;
  }
  let warningHint;
  const elementType = element.type;
  if (typeof elementType === "function" && !isClassComponent2(elementType)) {
    warningHint = "Did you accidentally use a plain function component for an element instead?";
  }
  if (warningHint !== void 0) {
    return new Error(`Invalid ${location} \`${safePropName}\` supplied to \`${componentName}\`. Expected an element that can hold a ref. ${warningHint} For more information see https://mui.com/r/caveat-with-refs-guide`);
  }
  return null;
}
var elementAcceptingRef = chainPropTypes(import_prop_types97.default.element, acceptingRef);
elementAcceptingRef.isRequired = chainPropTypes(import_prop_types97.default.element.isRequired, acceptingRef);
var elementAcceptingRef_default = elementAcceptingRef;

// node_modules/@mui/utils/esm/getActiveElement/getActiveElement.js
function activeElement(doc) {
  var _a;
  let element = doc.activeElement;
  while (((_a = element == null ? void 0 : element.shadowRoot) == null ? void 0 : _a.activeElement) != null) {
    element = element.shadowRoot.activeElement;
  }
  return element;
}

// node_modules/@mui/material/esm/utils/getActiveElement.js
var getActiveElement_default = activeElement;

// node_modules/@mui/material/esm/Unstable_TrapFocus/FocusTrap.js
var import_jsx_runtime125 = __toESM(require_jsx_runtime(), 1);
var candidatesSelector = ["input", "select", "textarea", "a[href]", "button", "[tabindex]", "audio[controls]", "video[controls]", '[contenteditable]:not([contenteditable="false"])'].join(",");
function getTabIndex(node) {
  const tabindexAttr = parseInt(node.getAttribute("tabindex") || "", 10);
  if (!Number.isNaN(tabindexAttr)) {
    return tabindexAttr;
  }
  if (node.contentEditable === "true" || (node.nodeName === "AUDIO" || node.nodeName === "VIDEO" || node.nodeName === "DETAILS") && node.getAttribute("tabindex") === null) {
    return 0;
  }
  return node.tabIndex;
}
function isNonTabbableRadio(node) {
  if (node.tagName !== "INPUT" || node.type !== "radio") {
    return false;
  }
  if (!node.name) {
    return false;
  }
  const getRadio = (selector) => node.ownerDocument.querySelector(`input[type="radio"]${selector}`);
  let roving = getRadio(`[name="${node.name}"]:checked`);
  if (!roving) {
    roving = getRadio(`[name="${node.name}"]`);
  }
  return roving !== node;
}
function isNodeMatchingSelectorFocusable(node) {
  if (node.disabled || node.tagName === "INPUT" && node.type === "hidden" || isNonTabbableRadio(node)) {
    return false;
  }
  return true;
}
function defaultGetTabbable(root) {
  const regularTabNodes = [];
  const orderedTabNodes = [];
  Array.from(root.querySelectorAll(candidatesSelector)).forEach((node, i2) => {
    const nodeTabIndex = getTabIndex(node);
    if (nodeTabIndex === -1 || !isNodeMatchingSelectorFocusable(node)) {
      return;
    }
    if (nodeTabIndex === 0) {
      regularTabNodes.push(node);
    } else {
      orderedTabNodes.push({
        documentOrder: i2,
        tabIndex: nodeTabIndex,
        node
      });
    }
  });
  return orderedTabNodes.sort((a, b) => a.tabIndex === b.tabIndex ? a.documentOrder - b.documentOrder : a.tabIndex - b.tabIndex).map((a) => a.node).concat(regularTabNodes);
}
function defaultIsEnabled() {
  return true;
}
function FocusTrap(props) {
  const {
    children: children2,
    disableAutoFocus = false,
    disableEnforceFocus = false,
    disableRestoreFocus = false,
    getTabbable = defaultGetTabbable,
    isEnabled = defaultIsEnabled,
    open
  } = props;
  const ignoreNextEnforceFocus = React154.useRef(false);
  const sentinelStart = React154.useRef(null);
  const sentinelEnd = React154.useRef(null);
  const nodeToRestore = React154.useRef(null);
  const reactFocusEventTarget = React154.useRef(null);
  const activated = React154.useRef(false);
  const rootRef = React154.useRef(null);
  const handleRef = useForkRef(getReactElementRef(children2), rootRef);
  const lastKeydown = React154.useRef(null);
  React154.useEffect(() => {
    if (!open || !rootRef.current) {
      return;
    }
    activated.current = !disableAutoFocus;
  }, [disableAutoFocus, open]);
  React154.useEffect(() => {
    if (!open || !rootRef.current) {
      return;
    }
    const doc = ownerDocument(rootRef.current);
    const activeElement2 = getActiveElement_default(doc);
    if (!rootRef.current.contains(activeElement2)) {
      if (!rootRef.current.hasAttribute("tabIndex")) {
        if (true) {
          console.error(["MUI: The modal content node does not accept focus.", 'For the benefit of assistive technologies, the tabIndex of the node is being set to "-1".'].join("\n"));
        }
        rootRef.current.setAttribute("tabIndex", "-1");
      }
      if (activated.current) {
        rootRef.current.focus();
      }
    }
    return () => {
      if (!disableRestoreFocus) {
        if (nodeToRestore.current && nodeToRestore.current.focus) {
          ignoreNextEnforceFocus.current = true;
          nodeToRestore.current.focus();
        }
        nodeToRestore.current = null;
      }
    };
  }, [open]);
  React154.useEffect(() => {
    if (!open || !rootRef.current) {
      return;
    }
    const doc = ownerDocument(rootRef.current);
    const loopFocus = (nativeEvent) => {
      lastKeydown.current = nativeEvent;
      if (disableEnforceFocus || !isEnabled() || nativeEvent.key !== "Tab") {
        return;
      }
      const activeElement2 = getActiveElement_default(doc);
      if (activeElement2 === rootRef.current && nativeEvent.shiftKey) {
        ignoreNextEnforceFocus.current = true;
        if (sentinelEnd.current) {
          sentinelEnd.current.focus();
        }
      }
    };
    const contain = () => {
      var _a, _b;
      const rootElement = rootRef.current;
      if (rootElement === null) {
        return;
      }
      const activeEl = getActiveElement_default(doc);
      if (!doc.hasFocus() || !isEnabled() || ignoreNextEnforceFocus.current) {
        ignoreNextEnforceFocus.current = false;
        return;
      }
      if (rootElement.contains(activeEl)) {
        return;
      }
      if (disableEnforceFocus && activeEl !== sentinelStart.current && activeEl !== sentinelEnd.current) {
        return;
      }
      if (activeEl !== reactFocusEventTarget.current) {
        reactFocusEventTarget.current = null;
      } else if (reactFocusEventTarget.current !== null) {
        return;
      }
      if (!activated.current) {
        return;
      }
      let tabbable = [];
      if (activeEl === sentinelStart.current || activeEl === sentinelEnd.current) {
        tabbable = getTabbable(rootRef.current);
      }
      if (tabbable.length > 0) {
        const isShiftTab = Boolean(((_a = lastKeydown.current) == null ? void 0 : _a.shiftKey) && ((_b = lastKeydown.current) == null ? void 0 : _b.key) === "Tab");
        const focusNext = tabbable[0];
        const focusPrevious = tabbable[tabbable.length - 1];
        if (typeof focusNext !== "string" && typeof focusPrevious !== "string") {
          if (isShiftTab) {
            focusPrevious.focus();
          } else {
            focusNext.focus();
          }
        }
      } else {
        rootElement.focus();
      }
    };
    doc.addEventListener("focusin", contain);
    doc.addEventListener("keydown", loopFocus, true);
    const interval = setInterval(() => {
      const activeEl = getActiveElement_default(doc);
      if (activeEl && activeEl.tagName === "BODY") {
        contain();
      }
    }, 50);
    return () => {
      clearInterval(interval);
      doc.removeEventListener("focusin", contain);
      doc.removeEventListener("keydown", loopFocus, true);
    };
  }, [disableAutoFocus, disableEnforceFocus, disableRestoreFocus, isEnabled, open, getTabbable]);
  const onFocus = (event) => {
    if (nodeToRestore.current === null) {
      nodeToRestore.current = event.relatedTarget;
    }
    activated.current = true;
    reactFocusEventTarget.current = event.target;
    const childrenPropsHandler = children2.props.onFocus;
    if (childrenPropsHandler) {
      childrenPropsHandler(event);
    }
  };
  const handleFocusSentinel = (event) => {
    if (nodeToRestore.current === null) {
      nodeToRestore.current = event.relatedTarget;
    }
    activated.current = true;
  };
  return (0, import_jsx_runtime125.jsxs)(React154.Fragment, {
    children: [(0, import_jsx_runtime125.jsx)("div", {
      tabIndex: open ? 0 : -1,
      onFocus: handleFocusSentinel,
      ref: sentinelStart,
      "data-testid": "sentinelStart"
    }), React154.cloneElement(children2, {
      ref: handleRef,
      onFocus
    }), (0, import_jsx_runtime125.jsx)("div", {
      tabIndex: open ? 0 : -1,
      onFocus: handleFocusSentinel,
      ref: sentinelEnd,
      "data-testid": "sentinelEnd"
    })]
  });
}
true ? FocusTrap.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * A single child content element.
   */
  children: elementAcceptingRef_default,
  /**
   * If `true`, the focus trap will not automatically shift focus to itself when it opens, and
   * replace it to the last focused element when it closes.
   * This also works correctly with any focus trap children that have the `disableAutoFocus` prop.
   *
   * Generally this should never be set to `true` as it makes the focus trap less
   * accessible to assistive technologies, like screen readers.
   * @default false
   */
  disableAutoFocus: import_prop_types98.default.bool,
  /**
   * If `true`, the focus trap will not prevent focus from leaving the focus trap while open.
   *
   * Generally this should never be set to `true` as it makes the focus trap less
   * accessible to assistive technologies, like screen readers.
   * @default false
   */
  disableEnforceFocus: import_prop_types98.default.bool,
  /**
   * If `true`, the focus trap will not restore focus to previously focused element once
   * focus trap is hidden or unmounted.
   * @default false
   */
  disableRestoreFocus: import_prop_types98.default.bool,
  /**
   * Returns an array of ordered tabbable nodes (i.e. in tab order) within the root.
   * For instance, you can provide the "tabbable" npm dependency.
   * @param {HTMLElement} root
   */
  getTabbable: import_prop_types98.default.func,
  /**
   * This prop extends the `open` prop.
   * It allows to toggle the open state without having to wait for a rerender when changing the `open` prop.
   * This prop should be memoized.
   * It can be used to support multiple focus trap mounted at the same time.
   * @default function defaultIsEnabled(): boolean {
   *   return true;
   * }
   */
  isEnabled: import_prop_types98.default.func,
  /**
   * If `true`, focus is locked.
   */
  open: import_prop_types98.default.bool.isRequired
} : void 0;
if (true) {
  FocusTrap["propTypes"] = exactProp(FocusTrap.propTypes);
}
var FocusTrap_default = FocusTrap;

// node_modules/@mui/material/esm/LinearProgress/LinearProgress.js
var React155 = __toESM(require_react(), 1);
var import_prop_types99 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/LinearProgress/linearProgressClasses.js
function getLinearProgressUtilityClass(slot) {
  return generateUtilityClass("MuiLinearProgress", slot);
}
var linearProgressClasses = generateUtilityClasses("MuiLinearProgress", ["root", "colorPrimary", "colorSecondary", "determinate", "indeterminate", "buffer", "query", "dashed", "dashedColorPrimary", "dashedColorSecondary", "bar", "bar1", "bar2", "barColorPrimary", "barColorSecondary", "bar1Indeterminate", "bar1Determinate", "bar1Buffer", "bar2Indeterminate", "bar2Buffer"]);

// node_modules/@mui/material/esm/LinearProgress/LinearProgress.js
var import_jsx_runtime126 = __toESM(require_jsx_runtime(), 1);
var TRANSITION_DURATION = 4;
var indeterminate1Keyframe = keyframes`
  0% {
    left: -35%;
    right: 100%;
  }

  60% {
    left: 100%;
    right: -90%;
  }

  100% {
    left: 100%;
    right: -90%;
  }
`;
var indeterminate1Animation = typeof indeterminate1Keyframe !== "string" ? css`
        animation: ${indeterminate1Keyframe} 2.1s cubic-bezier(0.65, 0.815, 0.735, 0.395) infinite;
      ` : null;
var indeterminate2Keyframe = keyframes`
  0% {
    left: -200%;
    right: 100%;
  }

  60% {
    left: 107%;
    right: -8%;
  }

  100% {
    left: 107%;
    right: -8%;
  }
`;
var indeterminate2Animation = typeof indeterminate2Keyframe !== "string" ? css`
        animation: ${indeterminate2Keyframe} 2.1s cubic-bezier(0.165, 0.84, 0.44, 1) 1.15s infinite;
      ` : null;
var bufferKeyframe = keyframes`
  0% {
    opacity: 1;
    background-position: 0 -23px;
  }

  60% {
    opacity: 0;
    background-position: 0 -23px;
  }

  100% {
    opacity: 1;
    background-position: -200px -23px;
  }
`;
var bufferAnimation = typeof bufferKeyframe !== "string" ? css`
        animation: ${bufferKeyframe} 3s infinite linear;
      ` : null;
var useUtilityClasses59 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    color
  } = ownerState;
  const slots = {
    root: ["root", `color${capitalize_default(color)}`, variant],
    dashed: ["dashed", `dashedColor${capitalize_default(color)}`],
    bar1: ["bar", "bar1", `barColor${capitalize_default(color)}`, (variant === "indeterminate" || variant === "query") && "bar1Indeterminate", variant === "determinate" && "bar1Determinate", variant === "buffer" && "bar1Buffer"],
    bar2: ["bar", "bar2", variant !== "buffer" && `barColor${capitalize_default(color)}`, variant === "buffer" && `color${capitalize_default(color)}`, (variant === "indeterminate" || variant === "query") && "bar2Indeterminate", variant === "buffer" && "bar2Buffer"]
  };
  return composeClasses(slots, getLinearProgressUtilityClass, classes2);
};
var getColorShade = (theme, color) => {
  if (theme.vars) {
    return theme.vars.palette.LinearProgress[`${color}Bg`];
  }
  return theme.palette.mode === "light" ? theme.lighten(theme.palette[color].main, 0.62) : theme.darken(theme.palette[color].main, 0.5);
};
var LinearProgressRoot = styled_default2("span", {
  name: "MuiLinearProgress",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[`color${capitalize_default(ownerState.color)}`], styles5[ownerState.variant]];
  }
})(memoTheme_default(({
  theme
}) => ({
  position: "relative",
  overflow: "hidden",
  display: "block",
  height: 4,
  // Fix Safari's bug during composition of different paint.
  zIndex: 0,
  "@media print": {
    colorAdjust: "exact"
  },
  variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      backgroundColor: getColorShade(theme, color)
    }
  })), {
    props: ({
      ownerState
    }) => ownerState.color === "inherit" && ownerState.variant !== "buffer",
    style: {
      "&::before": {
        content: '""',
        position: "absolute",
        left: 0,
        top: 0,
        right: 0,
        bottom: 0,
        backgroundColor: "currentColor",
        opacity: 0.3
      }
    }
  }, {
    props: {
      variant: "buffer"
    },
    style: {
      backgroundColor: "transparent"
    }
  }, {
    props: {
      variant: "query"
    },
    style: {
      transform: "rotate(180deg)"
    }
  }]
})));
var LinearProgressDashed = styled_default2("span", {
  name: "MuiLinearProgress",
  slot: "Dashed",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.dashed, styles5[`dashedColor${capitalize_default(ownerState.color)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  position: "absolute",
  marginTop: 0,
  height: "100%",
  width: "100%",
  backgroundSize: "10px 10px",
  backgroundPosition: "0 -23px",
  variants: [{
    props: {
      color: "inherit"
    },
    style: {
      opacity: 0.3,
      backgroundImage: `radial-gradient(currentColor 0%, currentColor 16%, transparent 42%)`
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => {
    const backgroundColor = getColorShade(theme, color);
    return {
      props: {
        color
      },
      style: {
        backgroundImage: `radial-gradient(${backgroundColor} 0%, ${backgroundColor} 16%, transparent 42%)`
      }
    };
  })]
})), bufferAnimation || {
  // At runtime for Pigment CSS, `bufferAnimation` will be null and the generated keyframe will be used.
  animation: `${bufferKeyframe} 3s infinite linear`
});
var LinearProgressBar1 = styled_default2("span", {
  name: "MuiLinearProgress",
  slot: "Bar1",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.bar, styles5.bar1, styles5[`barColor${capitalize_default(ownerState.color)}`], (ownerState.variant === "indeterminate" || ownerState.variant === "query") && styles5.bar1Indeterminate, ownerState.variant === "determinate" && styles5.bar1Determinate, ownerState.variant === "buffer" && styles5.bar1Buffer];
  }
})(memoTheme_default(({
  theme
}) => ({
  width: "100%",
  position: "absolute",
  left: 0,
  bottom: 0,
  top: 0,
  transition: "transform 0.2s linear",
  transformOrigin: "left",
  variants: [{
    props: {
      color: "inherit"
    },
    style: {
      backgroundColor: "currentColor"
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      backgroundColor: (theme.vars || theme).palette[color].main
    }
  })), {
    props: {
      variant: "determinate"
    },
    style: {
      transition: `transform .${TRANSITION_DURATION}s linear`
    }
  }, {
    props: {
      variant: "buffer"
    },
    style: {
      zIndex: 1,
      transition: `transform .${TRANSITION_DURATION}s linear`
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.variant === "indeterminate" || ownerState.variant === "query",
    style: {
      width: "auto"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.variant === "indeterminate" || ownerState.variant === "query",
    style: indeterminate1Animation || {
      animation: `${indeterminate1Keyframe} 2.1s cubic-bezier(0.65, 0.815, 0.735, 0.395) infinite`
    }
  }]
})));
var LinearProgressBar2 = styled_default2("span", {
  name: "MuiLinearProgress",
  slot: "Bar2",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.bar, styles5.bar2, styles5[`barColor${capitalize_default(ownerState.color)}`], (ownerState.variant === "indeterminate" || ownerState.variant === "query") && styles5.bar2Indeterminate, ownerState.variant === "buffer" && styles5.bar2Buffer];
  }
})(memoTheme_default(({
  theme
}) => ({
  width: "100%",
  position: "absolute",
  left: 0,
  bottom: 0,
  top: 0,
  transition: "transform 0.2s linear",
  transformOrigin: "left",
  variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      "--LinearProgressBar2-barColor": (theme.vars || theme).palette[color].main
    }
  })), {
    props: ({
      ownerState
    }) => ownerState.variant !== "buffer" && ownerState.color !== "inherit",
    style: {
      backgroundColor: "var(--LinearProgressBar2-barColor, currentColor)"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.variant !== "buffer" && ownerState.color === "inherit",
    style: {
      backgroundColor: "currentColor"
    }
  }, {
    props: {
      color: "inherit"
    },
    style: {
      opacity: 0.3
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color,
      variant: "buffer"
    },
    style: {
      backgroundColor: getColorShade(theme, color),
      transition: `transform .${TRANSITION_DURATION}s linear`
    }
  })), {
    props: ({
      ownerState
    }) => ownerState.variant === "indeterminate" || ownerState.variant === "query",
    style: {
      width: "auto"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.variant === "indeterminate" || ownerState.variant === "query",
    style: indeterminate2Animation || {
      animation: `${indeterminate2Keyframe} 2.1s cubic-bezier(0.165, 0.84, 0.44, 1) 1.15s infinite`
    }
  }]
})));
var LinearProgress = React155.forwardRef(function LinearProgress2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiLinearProgress"
  });
  const {
    className,
    color = "primary",
    value,
    valueBuffer,
    variant = "indeterminate",
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    variant
  };
  const classes2 = useUtilityClasses59(ownerState);
  const isRtl = useRtl();
  const rootProps = {};
  const inlineStyles = {
    bar1: {},
    bar2: {}
  };
  if (variant === "determinate" || variant === "buffer") {
    if (value !== void 0) {
      rootProps["aria-valuenow"] = Math.round(value);
      rootProps["aria-valuemin"] = 0;
      rootProps["aria-valuemax"] = 100;
      let transform = value - 100;
      if (isRtl) {
        transform = -transform;
      }
      inlineStyles.bar1.transform = `translateX(${transform}%)`;
    } else if (true) {
      console.error("MUI: You need to provide a value prop when using the determinate or buffer variant of LinearProgress .");
    }
  }
  if (variant === "buffer") {
    if (valueBuffer !== void 0) {
      let transform = (valueBuffer || 0) - 100;
      if (isRtl) {
        transform = -transform;
      }
      inlineStyles.bar2.transform = `translateX(${transform}%)`;
    } else if (true) {
      console.error("MUI: You need to provide a valueBuffer prop when using the buffer variant of LinearProgress.");
    }
  }
  return (0, import_jsx_runtime126.jsxs)(LinearProgressRoot, {
    className: clsx_default(classes2.root, className),
    ownerState,
    role: "progressbar",
    ...rootProps,
    ref,
    ...other,
    children: [variant === "buffer" ? (0, import_jsx_runtime126.jsx)(LinearProgressDashed, {
      className: classes2.dashed,
      ownerState
    }) : null, (0, import_jsx_runtime126.jsx)(LinearProgressBar1, {
      className: classes2.bar1,
      ownerState,
      style: inlineStyles.bar1
    }), variant === "determinate" ? null : (0, import_jsx_runtime126.jsx)(LinearProgressBar2, {
      className: classes2.bar2,
      ownerState,
      style: inlineStyles.bar2
    })]
  });
});
true ? LinearProgress.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types99.default.object,
  /**
   * @ignore
   */
  className: import_prop_types99.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types99.default.oneOfType([import_prop_types99.default.oneOf(["inherit", "primary", "secondary"]), import_prop_types99.default.string]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types99.default.oneOfType([import_prop_types99.default.arrayOf(import_prop_types99.default.oneOfType([import_prop_types99.default.func, import_prop_types99.default.object, import_prop_types99.default.bool])), import_prop_types99.default.func, import_prop_types99.default.object]),
  /**
   * The value of the progress indicator for the determinate and buffer variants.
   * Value between 0 and 100.
   */
  value: import_prop_types99.default.number,
  /**
   * The value for the buffer variant.
   * Value between 0 and 100.
   */
  valueBuffer: import_prop_types99.default.number,
  /**
   * The variant to use.
   * Use indeterminate or query when there is no progress value.
   * @default 'indeterminate'
   */
  variant: import_prop_types99.default.oneOf(["buffer", "determinate", "indeterminate", "query"])
} : void 0;
var LinearProgress_default = LinearProgress;

// node_modules/@mui/material/esm/ListItemIcon/ListItemIcon.js
var React157 = __toESM(require_react(), 1);
var import_prop_types100 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/ListItemIcon/listItemIconClasses.js
function getListItemIconUtilityClass(slot) {
  return generateUtilityClass("MuiListItemIcon", slot);
}
var listItemIconClasses = generateUtilityClasses("MuiListItemIcon", ["root", "alignItemsFlexStart"]);
var listItemIconClasses_default = listItemIconClasses;

// node_modules/@mui/material/esm/List/ListContext.js
var React156 = __toESM(require_react(), 1);
var ListContext = React156.createContext({});
if (true) {
  ListContext.displayName = "ListContext";
}
var ListContext_default = ListContext;

// node_modules/@mui/material/esm/ListItemIcon/ListItemIcon.js
var import_jsx_runtime127 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses60 = (ownerState) => {
  const {
    alignItems,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", alignItems === "flex-start" && "alignItemsFlexStart"]
  };
  return composeClasses(slots, getListItemIconUtilityClass, classes2);
};
var ListItemIconRoot = styled_default2("div", {
  name: "MuiListItemIcon",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.alignItems === "flex-start" && styles5.alignItemsFlexStart];
  }
})(memoTheme_default(({
  theme
}) => ({
  minWidth: 56,
  color: (theme.vars || theme).palette.action.active,
  flexShrink: 0,
  display: "inline-flex",
  variants: [{
    props: {
      alignItems: "flex-start"
    },
    style: {
      marginTop: 8
    }
  }]
})));
var ListItemIcon = React157.forwardRef(function ListItemIcon2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiListItemIcon"
  });
  const {
    className,
    ...other
  } = props;
  const context = React157.useContext(ListContext_default);
  const ownerState = {
    ...props,
    alignItems: context.alignItems
  };
  const classes2 = useUtilityClasses60(ownerState);
  return (0, import_jsx_runtime127.jsx)(ListItemIconRoot, {
    className: clsx_default(classes2.root, className),
    ownerState,
    ref,
    ...other
  });
});
true ? ListItemIcon.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component, normally `Icon`, `SvgIcon`,
   * or a `@mui/icons-material` SVG icon element.
   */
  children: import_prop_types100.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types100.default.object,
  /**
   * @ignore
   */
  className: import_prop_types100.default.string,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types100.default.oneOfType([import_prop_types100.default.arrayOf(import_prop_types100.default.oneOfType([import_prop_types100.default.func, import_prop_types100.default.object, import_prop_types100.default.bool])), import_prop_types100.default.func, import_prop_types100.default.object])
} : void 0;
var ListItemIcon_default = ListItemIcon;

// node_modules/@mui/material/esm/ListItemText/ListItemText.js
var React158 = __toESM(require_react(), 1);
var import_prop_types101 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/ListItemText/listItemTextClasses.js
function getListItemTextUtilityClass(slot) {
  return generateUtilityClass("MuiListItemText", slot);
}
var listItemTextClasses = generateUtilityClasses("MuiListItemText", ["root", "multiline", "dense", "inset", "primary", "secondary"]);
var listItemTextClasses_default = listItemTextClasses;

// node_modules/@mui/material/esm/ListItemText/ListItemText.js
var import_jsx_runtime128 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses61 = (ownerState) => {
  const {
    classes: classes2,
    inset,
    primary,
    secondary,
    dense
  } = ownerState;
  const slots = {
    root: ["root", inset && "inset", dense && "dense", primary && secondary && "multiline"],
    primary: ["primary"],
    secondary: ["secondary"]
  };
  return composeClasses(slots, getListItemTextUtilityClass, classes2);
};
var ListItemTextRoot = styled_default2("div", {
  name: "MuiListItemText",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [{
      [`& .${listItemTextClasses_default.primary}`]: styles5.primary
    }, {
      [`& .${listItemTextClasses_default.secondary}`]: styles5.secondary
    }, styles5.root, ownerState.inset && styles5.inset, ownerState.primary && ownerState.secondary && styles5.multiline, ownerState.dense && styles5.dense];
  }
})({
  flex: "1 1 auto",
  minWidth: 0,
  marginTop: 4,
  marginBottom: 4,
  [`.${typographyClasses_default.root}:where(& .${listItemTextClasses_default.primary}), .${typographyClasses_default.root}:where(& .${listItemTextClasses_default.secondary})`]: {
    display: "block"
  },
  variants: [{
    props: ({
      ownerState
    }) => ownerState.primary && ownerState.secondary,
    style: {
      marginTop: 6,
      marginBottom: 6
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.inset,
    style: {
      paddingLeft: 56
    }
  }]
});
var ListItemText = React158.forwardRef(function ListItemText2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiListItemText"
  });
  const {
    children: children2,
    className,
    disableTypography = false,
    inset = false,
    primary: primaryProp,
    primaryTypographyProps,
    secondary: secondaryProp,
    secondaryTypographyProps,
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const {
    dense
  } = React158.useContext(ListContext_default);
  let primary = primaryProp != null ? primaryProp : children2;
  let secondary = secondaryProp;
  const ownerState = {
    ...props,
    disableTypography,
    inset,
    primary: !!primary,
    secondary: !!secondary,
    dense
  };
  const classes2 = useUtilityClasses61(ownerState);
  const externalForwardedProps = {
    slots,
    slotProps: {
      primary: primaryTypographyProps,
      secondary: secondaryTypographyProps,
      ...slotProps
    }
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    className: clsx_default(classes2.root, className),
    elementType: ListItemTextRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other
    },
    ownerState,
    ref
  });
  const [PrimarySlot, primarySlotProps] = useSlot("primary", {
    className: classes2.primary,
    elementType: Typography_default,
    externalForwardedProps,
    ownerState
  });
  const [SecondarySlot, secondarySlotProps] = useSlot("secondary", {
    className: classes2.secondary,
    elementType: Typography_default,
    externalForwardedProps,
    ownerState
  });
  if (primary != null && primary.type !== Typography_default && !disableTypography) {
    primary = (0, import_jsx_runtime128.jsx)(PrimarySlot, {
      variant: dense ? "body2" : "body1",
      component: (primarySlotProps == null ? void 0 : primarySlotProps.variant) ? void 0 : "span",
      ...primarySlotProps,
      children: primary
    });
  }
  if (secondary != null && secondary.type !== Typography_default && !disableTypography) {
    secondary = (0, import_jsx_runtime128.jsx)(SecondarySlot, {
      variant: "body2",
      color: "textSecondary",
      ...secondarySlotProps,
      children: secondary
    });
  }
  return (0, import_jsx_runtime128.jsxs)(RootSlot, {
    ...rootSlotProps,
    children: [primary, secondary]
  });
});
true ? ListItemText.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Alias for the `primary` prop.
   */
  children: import_prop_types101.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types101.default.object,
  /**
   * @ignore
   */
  className: import_prop_types101.default.string,
  /**
   * If `true`, the children won't be wrapped by a Typography component.
   * This can be useful to render an alternative Typography variant by wrapping
   * the `children` (or `primary`) text, and optional `secondary` text
   * with the Typography component.
   * @default false
   */
  disableTypography: import_prop_types101.default.bool,
  /**
   * If `true`, the children are indented.
   * This should be used if there is no left avatar or left icon.
   * @default false
   */
  inset: import_prop_types101.default.bool,
  /**
   * The main content element.
   */
  primary: import_prop_types101.default.node,
  /**
   * These props will be forwarded to the primary typography component
   * (as long as disableTypography is not `true`).
   * @deprecated Use `slotProps.primary` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  primaryTypographyProps: import_prop_types101.default.object,
  /**
   * The secondary content element.
   */
  secondary: import_prop_types101.default.node,
  /**
   * These props will be forwarded to the secondary typography component
   * (as long as disableTypography is not `true`).
   * @deprecated Use `slotProps.secondary` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  secondaryTypographyProps: import_prop_types101.default.object,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types101.default.shape({
    primary: import_prop_types101.default.oneOfType([import_prop_types101.default.func, import_prop_types101.default.object]),
    root: import_prop_types101.default.oneOfType([import_prop_types101.default.func, import_prop_types101.default.object]),
    secondary: import_prop_types101.default.oneOfType([import_prop_types101.default.func, import_prop_types101.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types101.default.shape({
    primary: import_prop_types101.default.elementType,
    root: import_prop_types101.default.elementType,
    secondary: import_prop_types101.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types101.default.oneOfType([import_prop_types101.default.arrayOf(import_prop_types101.default.oneOfType([import_prop_types101.default.func, import_prop_types101.default.object, import_prop_types101.default.bool])), import_prop_types101.default.func, import_prop_types101.default.object])
} : void 0;
var ListItemText_default = ListItemText;

// node_modules/@mui/material/esm/MenuList/MenuList.js
var React160 = __toESM(require_react(), 1);
var import_react_is = __toESM(require_react_is(), 1);
var import_prop_types103 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/List/List.js
var React159 = __toESM(require_react(), 1);
var import_prop_types102 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/List/listClasses.js
function getListUtilityClass(slot) {
  return generateUtilityClass("MuiList", slot);
}
var listClasses = generateUtilityClasses("MuiList", ["root", "padding", "dense", "subheader"]);

// node_modules/@mui/material/esm/List/List.js
var import_jsx_runtime129 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses62 = (ownerState) => {
  const {
    classes: classes2,
    disablePadding,
    dense,
    subheader
  } = ownerState;
  const slots = {
    root: ["root", !disablePadding && "padding", dense && "dense", subheader && "subheader"]
  };
  return composeClasses(slots, getListUtilityClass, classes2);
};
var ListRoot = styled_default2("ul", {
  name: "MuiList",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, !ownerState.disablePadding && styles5.padding, ownerState.dense && styles5.dense, ownerState.subheader && styles5.subheader];
  }
})({
  listStyle: "none",
  margin: 0,
  padding: 0,
  position: "relative",
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.disablePadding,
    style: {
      paddingTop: 8,
      paddingBottom: 8
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.subheader,
    style: {
      paddingTop: 0
    }
  }]
});
var List = React159.forwardRef(function List2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiList"
  });
  const {
    children: children2,
    className,
    component = "ul",
    dense = false,
    disablePadding = false,
    subheader,
    ...other
  } = props;
  const context = React159.useMemo(() => ({
    dense
  }), [dense]);
  const ownerState = {
    ...props,
    component,
    dense,
    disablePadding
  };
  const classes2 = useUtilityClasses62(ownerState);
  return (0, import_jsx_runtime129.jsx)(ListContext_default.Provider, {
    value: context,
    children: (0, import_jsx_runtime129.jsxs)(ListRoot, {
      as: component,
      className: clsx_default(classes2.root, className),
      ref,
      ownerState,
      ...other,
      children: [subheader, children2]
    })
  });
});
true ? List.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types102.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types102.default.object,
  /**
   * @ignore
   */
  className: import_prop_types102.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types102.default.elementType,
  /**
   * If `true`, compact vertical padding designed for keyboard and mouse input is used for
   * the list and list items.
   * The prop is available to descendant components as the `dense` context.
   * @default false
   */
  dense: import_prop_types102.default.bool,
  /**
   * If `true`, vertical padding is removed from the list.
   * @default false
   */
  disablePadding: import_prop_types102.default.bool,
  /**
   * The content of the subheader, normally `ListSubheader`.
   */
  subheader: import_prop_types102.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types102.default.oneOfType([import_prop_types102.default.arrayOf(import_prop_types102.default.oneOfType([import_prop_types102.default.func, import_prop_types102.default.object, import_prop_types102.default.bool])), import_prop_types102.default.func, import_prop_types102.default.object])
} : void 0;
var List_default = List;

// node_modules/@mui/utils/esm/getScrollbarSize/getScrollbarSize.js
function getScrollbarSize(win = window) {
  const documentWidth = win.document.documentElement.clientWidth;
  return win.innerWidth - documentWidth;
}

// node_modules/@mui/material/esm/utils/getScrollbarSize.js
var getScrollbarSize_default = getScrollbarSize;

// node_modules/@mui/material/esm/MenuList/MenuList.js
var import_jsx_runtime130 = __toESM(require_jsx_runtime(), 1);
function nextItem(list, item, disableListWrap) {
  if (list === item) {
    return list.firstChild;
  }
  if (item && item.nextElementSibling) {
    return item.nextElementSibling;
  }
  return disableListWrap ? null : list.firstChild;
}
function previousItem(list, item, disableListWrap) {
  if (list === item) {
    return disableListWrap ? list.firstChild : list.lastChild;
  }
  if (item && item.previousElementSibling) {
    return item.previousElementSibling;
  }
  return disableListWrap ? null : list.lastChild;
}
function textCriteriaMatches(nextFocus, textCriteria) {
  if (textCriteria === void 0) {
    return true;
  }
  let text = nextFocus.innerText;
  if (text === void 0) {
    text = nextFocus.textContent;
  }
  text = text.trim().toLowerCase();
  if (text.length === 0) {
    return false;
  }
  if (textCriteria.repeating) {
    return text[0] === textCriteria.keys[0];
  }
  return text.startsWith(textCriteria.keys.join(""));
}
function moveFocus(list, currentFocus, disableListWrap, disabledItemsFocusable, traversalFunction, textCriteria) {
  let wrappedOnce = false;
  let nextFocus = traversalFunction(list, currentFocus, currentFocus ? disableListWrap : false);
  while (nextFocus) {
    if (nextFocus === list.firstChild) {
      if (wrappedOnce) {
        return false;
      }
      wrappedOnce = true;
    }
    const nextFocusDisabled = disabledItemsFocusable ? false : nextFocus.disabled || nextFocus.getAttribute("aria-disabled") === "true";
    if (!nextFocus.hasAttribute("tabindex") || !textCriteriaMatches(nextFocus, textCriteria) || nextFocusDisabled) {
      nextFocus = traversalFunction(list, nextFocus, disableListWrap);
    } else {
      nextFocus.focus();
      return true;
    }
  }
  return false;
}
var MenuList = React160.forwardRef(function MenuList2(props, ref) {
  const {
    // private
    // eslint-disable-next-line react/prop-types
    actions,
    autoFocus = false,
    autoFocusItem = false,
    children: children2,
    className,
    disabledItemsFocusable = false,
    disableListWrap = false,
    onKeyDown,
    variant = "selectedMenu",
    ...other
  } = props;
  const listRef = React160.useRef(null);
  const textCriteriaRef = React160.useRef({
    keys: [],
    repeating: true,
    previousKeyMatched: true,
    lastTime: null
  });
  useEnhancedEffect_default2(() => {
    if (autoFocus) {
      listRef.current.focus();
    }
  }, [autoFocus]);
  React160.useImperativeHandle(actions, () => ({
    adjustStyleForScrollbar: (containerElement, {
      direction
    }) => {
      const noExplicitWidth = !listRef.current.style.width;
      if (containerElement.clientHeight < listRef.current.clientHeight && noExplicitWidth) {
        const scrollbarSize = `${getScrollbarSize_default(ownerWindow_default(containerElement))}px`;
        listRef.current.style[direction === "rtl" ? "paddingLeft" : "paddingRight"] = scrollbarSize;
        listRef.current.style.width = `calc(100% + ${scrollbarSize})`;
      }
      return listRef.current;
    }
  }), []);
  const handleKeyDown = (event) => {
    const list = listRef.current;
    const key = event.key;
    const isModifierKeyPressed = event.ctrlKey || event.metaKey || event.altKey;
    if (isModifierKeyPressed) {
      if (onKeyDown) {
        onKeyDown(event);
      }
      return;
    }
    const currentFocus = getActiveElement_default(ownerDocument_default(list));
    if (key === "ArrowDown") {
      event.preventDefault();
      moveFocus(list, currentFocus, disableListWrap, disabledItemsFocusable, nextItem);
    } else if (key === "ArrowUp") {
      event.preventDefault();
      moveFocus(list, currentFocus, disableListWrap, disabledItemsFocusable, previousItem);
    } else if (key === "Home") {
      event.preventDefault();
      moveFocus(list, null, disableListWrap, disabledItemsFocusable, nextItem);
    } else if (key === "End") {
      event.preventDefault();
      moveFocus(list, null, disableListWrap, disabledItemsFocusable, previousItem);
    } else if (key.length === 1) {
      const criteria = textCriteriaRef.current;
      const lowerKey = key.toLowerCase();
      const currTime = performance.now();
      if (criteria.keys.length > 0) {
        if (currTime - criteria.lastTime > 500) {
          criteria.keys = [];
          criteria.repeating = true;
          criteria.previousKeyMatched = true;
        } else if (criteria.repeating && lowerKey !== criteria.keys[0]) {
          criteria.repeating = false;
        }
      }
      criteria.lastTime = currTime;
      criteria.keys.push(lowerKey);
      const keepFocusOnCurrent = currentFocus && !criteria.repeating && textCriteriaMatches(currentFocus, criteria);
      if (criteria.previousKeyMatched && (keepFocusOnCurrent || moveFocus(list, currentFocus, false, disabledItemsFocusable, nextItem, criteria))) {
        event.preventDefault();
      } else {
        criteria.previousKeyMatched = false;
      }
    }
    if (onKeyDown) {
      onKeyDown(event);
    }
  };
  const handleRef = useForkRef_default(listRef, ref);
  let activeItemIndex = -1;
  React160.Children.forEach(children2, (child, index) => {
    if (!React160.isValidElement(child)) {
      if (activeItemIndex === index) {
        activeItemIndex += 1;
        if (activeItemIndex >= children2.length) {
          activeItemIndex = -1;
        }
      }
      return;
    }
    if (true) {
      if ((0, import_react_is.isFragment)(child)) {
        console.error(["MUI: The Menu component doesn't accept a Fragment as a child.", "Consider providing an array instead."].join("\n"));
      }
    }
    if (!child.props.disabled) {
      if (variant === "selectedMenu" && child.props.selected) {
        activeItemIndex = index;
      } else if (activeItemIndex === -1) {
        activeItemIndex = index;
      }
    }
    if (activeItemIndex === index && (child.props.disabled || child.props.muiSkipListHighlight || child.type.muiSkipListHighlight)) {
      activeItemIndex += 1;
      if (activeItemIndex >= children2.length) {
        activeItemIndex = -1;
      }
    }
  });
  const items = React160.Children.map(children2, (child, index) => {
    if (index === activeItemIndex) {
      const newChildProps = {};
      if (autoFocusItem) {
        newChildProps.autoFocus = true;
      }
      if (child.props.tabIndex === void 0 && variant === "selectedMenu") {
        newChildProps.tabIndex = 0;
      }
      return React160.cloneElement(child, newChildProps);
    }
    return child;
  });
  return (0, import_jsx_runtime130.jsx)(List_default, {
    role: "menu",
    ref: handleRef,
    className,
    onKeyDown: handleKeyDown,
    tabIndex: autoFocus ? 0 : -1,
    ...other,
    children: items
  });
});
true ? MenuList.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, will focus the `[role="menu"]` container and move into tab order.
   * @default false
   */
  autoFocus: import_prop_types103.default.bool,
  /**
   * If `true`, will focus the first menuitem if `variant="menu"` or selected item
   * if `variant="selectedMenu"`.
   * @default false
   */
  autoFocusItem: import_prop_types103.default.bool,
  /**
   * MenuList contents, normally `MenuItem`s.
   */
  children: import_prop_types103.default.node,
  /**
   * @ignore
   */
  className: import_prop_types103.default.string,
  /**
   * If `true`, will allow focus on disabled items.
   * @default false
   */
  disabledItemsFocusable: import_prop_types103.default.bool,
  /**
   * If `true`, the menu items will not wrap focus.
   * @default false
   */
  disableListWrap: import_prop_types103.default.bool,
  /**
   * @ignore
   */
  onKeyDown: import_prop_types103.default.func,
  /**
   * The variant to use. Use `menu` to prevent selected items from impacting the initial focus
   * and the vertical alignment relative to the anchor element.
   * @default 'selectedMenu'
   */
  variant: import_prop_types103.default.oneOf(["menu", "selectedMenu"])
} : void 0;
var MenuList_default = MenuList;

// node_modules/@mui/material/esm/MenuItem/MenuItem.js
var React161 = __toESM(require_react(), 1);
var import_prop_types104 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/MenuItem/menuItemClasses.js
function getMenuItemUtilityClass(slot) {
  return generateUtilityClass("MuiMenuItem", slot);
}
var menuItemClasses = generateUtilityClasses("MuiMenuItem", ["root", "focusVisible", "dense", "disabled", "divider", "gutters", "selected"]);
var menuItemClasses_default = menuItemClasses;

// node_modules/@mui/material/esm/MenuItem/MenuItem.js
var import_jsx_runtime131 = __toESM(require_jsx_runtime(), 1);
var overridesResolver = (props, styles5) => {
  const {
    ownerState
  } = props;
  return [styles5.root, ownerState.dense && styles5.dense, ownerState.divider && styles5.divider, !ownerState.disableGutters && styles5.gutters];
};
var useUtilityClasses63 = (ownerState) => {
  const {
    disabled,
    dense,
    divider,
    disableGutters,
    selected,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", dense && "dense", disabled && "disabled", !disableGutters && "gutters", divider && "divider", selected && "selected"]
  };
  const composedClasses = composeClasses(slots, getMenuItemUtilityClass, classes2);
  return {
    ...classes2,
    ...composedClasses
  };
};
var MenuItemRoot = styled_default2(ButtonBase_default, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiMenuItem",
  slot: "Root",
  overridesResolver
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body1,
  display: "flex",
  justifyContent: "flex-start",
  alignItems: "center",
  position: "relative",
  textDecoration: "none",
  minHeight: 48,
  paddingTop: 6,
  paddingBottom: 6,
  boxSizing: "border-box",
  whiteSpace: "nowrap",
  "&:hover": {
    textDecoration: "none",
    backgroundColor: (theme.vars || theme).palette.action.hover,
    // Reset on touch devices, it doesn't add specificity
    "@media (hover: none)": {
      backgroundColor: "transparent"
    }
  },
  [`&.${menuItemClasses_default.selected}`]: {
    backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, (theme.vars || theme).palette.action.selectedOpacity),
    [`&.${menuItemClasses_default.focusVisible}`]: {
      backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.focusOpacity}`)
    }
  },
  [`&.${menuItemClasses_default.selected}:hover`]: {
    backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`),
    // Reset on touch devices, it doesn't add specificity
    "@media (hover: none)": {
      backgroundColor: theme.alpha((theme.vars || theme).palette.primary.main, (theme.vars || theme).palette.action.selectedOpacity)
    }
  },
  [`&.${menuItemClasses_default.focusVisible}`]: {
    backgroundColor: (theme.vars || theme).palette.action.focus
  },
  [`&.${menuItemClasses_default.disabled}`]: {
    opacity: (theme.vars || theme).palette.action.disabledOpacity
  },
  [`& + .${dividerClasses_default.root}`]: {
    marginTop: theme.spacing(1),
    marginBottom: theme.spacing(1)
  },
  [`& + .${dividerClasses_default.inset}`]: {
    marginLeft: 52
  },
  [`& .${listItemTextClasses_default.root}`]: {
    marginTop: 0,
    marginBottom: 0
  },
  [`& .${listItemTextClasses_default.inset}`]: {
    paddingLeft: 36
  },
  [`& .${listItemIconClasses_default.root}`]: {
    minWidth: 36
  },
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.disableGutters,
    style: {
      paddingLeft: 16,
      paddingRight: 16
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.divider,
    style: {
      borderBottom: `1px solid ${(theme.vars || theme).palette.divider}`,
      backgroundClip: "padding-box"
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.dense,
    style: {
      [theme.breakpoints.up("sm")]: {
        minHeight: "auto"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.dense,
    style: {
      minHeight: 32,
      // https://m2.material.io/components/menus#specs > Dense
      paddingTop: 4,
      paddingBottom: 4,
      ...theme.typography.body2,
      [`& .${listItemIconClasses_default.root} svg`]: {
        fontSize: "1.25rem"
      }
    }
  }]
})));
var MenuItem = React161.forwardRef(function MenuItem2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiMenuItem"
  });
  const {
    autoFocus = false,
    component = "li",
    dense = false,
    divider = false,
    disableGutters = false,
    focusVisibleClassName,
    role = "menuitem",
    tabIndex: tabIndexProp,
    className,
    ...other
  } = props;
  const context = React161.useContext(ListContext_default);
  const childContext = React161.useMemo(() => ({
    dense: dense || context.dense || false,
    disableGutters
  }), [context.dense, dense, disableGutters]);
  const menuItemRef = React161.useRef(null);
  useEnhancedEffect_default2(() => {
    if (autoFocus) {
      if (menuItemRef.current) {
        menuItemRef.current.focus();
      } else if (true) {
        console.error("MUI: Unable to set focus to a MenuItem whose component has not been rendered.");
      }
    }
  }, [autoFocus]);
  const ownerState = {
    ...props,
    dense: childContext.dense,
    divider,
    disableGutters
  };
  const classes2 = useUtilityClasses63(props);
  const handleRef = useForkRef_default(menuItemRef, ref);
  let tabIndex;
  if (!props.disabled) {
    tabIndex = tabIndexProp !== void 0 ? tabIndexProp : -1;
  }
  return (0, import_jsx_runtime131.jsx)(ListContext_default.Provider, {
    value: childContext,
    children: (0, import_jsx_runtime131.jsx)(MenuItemRoot, {
      ref: handleRef,
      role,
      tabIndex,
      component,
      focusVisibleClassName: clsx_default(classes2.focusVisible, focusVisibleClassName),
      className: clsx_default(classes2.root, className),
      ...other,
      ownerState,
      classes: classes2
    })
  });
});
true ? MenuItem.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the list item is focused during the first mount.
   * Focus will also be triggered if the value changes from false to true.
   * @default false
   */
  autoFocus: import_prop_types104.default.bool,
  /**
   * The content of the component.
   */
  children: import_prop_types104.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types104.default.object,
  /**
   * @ignore
   */
  className: import_prop_types104.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types104.default.elementType,
  /**
   * If `true`, compact vertical padding designed for keyboard and mouse input is used.
   * The prop defaults to the value inherited from the parent Menu component.
   * @default false
   */
  dense: import_prop_types104.default.bool,
  /**
   * @ignore
   */
  disabled: import_prop_types104.default.bool,
  /**
   * If `true`, the left and right padding is removed.
   * @default false
   */
  disableGutters: import_prop_types104.default.bool,
  /**
   * If `true`, a 1px light border is added to the bottom of the menu item.
   * @default false
   */
  divider: import_prop_types104.default.bool,
  /**
   * This prop can help identify which element has keyboard focus.
   * The class name will be applied when the element gains the focus through keyboard interaction.
   * It's a polyfill for the [CSS :focus-visible selector](https://drafts.csswg.org/selectors-4/#the-focus-visible-pseudo).
   * The rationale for using this feature [is explained here](https://github.com/WICG/focus-visible/blob/HEAD/explainer.md).
   * A [polyfill can be used](https://github.com/WICG/focus-visible) to apply a `focus-visible` class to other components
   * if needed.
   */
  focusVisibleClassName: import_prop_types104.default.string,
  /**
   * @ignore
   */
  role: import_prop_types104.default.string,
  /**
   * If `true`, the component is selected.
   * @default false
   */
  selected: import_prop_types104.default.bool,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types104.default.oneOfType([import_prop_types104.default.arrayOf(import_prop_types104.default.oneOfType([import_prop_types104.default.func, import_prop_types104.default.object, import_prop_types104.default.bool])), import_prop_types104.default.func, import_prop_types104.default.object]),
  /**
   * @default 0
   */
  tabIndex: import_prop_types104.default.number
} : void 0;
var MenuItem_default = MenuItem;

// node_modules/@mui/material/esm/TextField/TextField.js
var React179 = __toESM(require_react(), 1);
var import_prop_types122 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Input/Input.js
var React162 = __toESM(require_react(), 1);
var import_prop_types105 = __toESM(require_prop_types(), 1);
var import_jsx_runtime132 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses64 = (ownerState) => {
  const {
    classes: classes2,
    disableUnderline
  } = ownerState;
  const slots = {
    root: ["root", !disableUnderline && "underline"],
    input: ["input"]
  };
  const composedClasses = composeClasses(slots, getInputUtilityClass, classes2);
  return {
    ...classes2,
    // forward classes to the InputBase
    ...composedClasses
  };
};
var InputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiInput",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [...rootOverridesResolver(props, styles5), !ownerState.disableUnderline && styles5.underline];
  }
})(memoTheme_default(({
  theme
}) => {
  const light = theme.palette.mode === "light";
  let bottomLineColor = light ? "rgba(0, 0, 0, 0.42)" : "rgba(255, 255, 255, 0.7)";
  if (theme.vars) {
    bottomLineColor = theme.alpha(theme.vars.palette.common.onBackground, theme.vars.opacity.inputUnderline);
  }
  return {
    position: "relative",
    variants: [{
      props: ({
        ownerState
      }) => ownerState.formControl,
      style: {
        "label + &": {
          marginTop: 16
        }
      }
    }, {
      props: ({
        ownerState
      }) => !ownerState.disableUnderline,
      style: {
        "&::after": {
          left: 0,
          bottom: 0,
          content: '""',
          position: "absolute",
          right: 0,
          transform: "scaleX(0)",
          transition: theme.transitions.create("transform", {
            duration: theme.transitions.duration.shorter,
            easing: theme.transitions.easing.easeOut
          }),
          pointerEvents: "none"
          // Transparent to the hover style.
        },
        [`&.${inputClasses_default.focused}:after`]: {
          // translateX(0) is a workaround for Safari transform scale bug
          // See https://github.com/mui/material-ui/issues/31766
          transform: "scaleX(1) translateX(0)"
        },
        [`&.${inputClasses_default.error}`]: {
          "&::before, &::after": {
            borderBottomColor: (theme.vars || theme).palette.error.main
          }
        },
        "&::before": {
          borderBottom: `1px solid ${bottomLineColor}`,
          left: 0,
          bottom: 0,
          content: '"\\00a0"',
          position: "absolute",
          right: 0,
          transition: theme.transitions.create("border-bottom-color", {
            duration: theme.transitions.duration.shorter
          }),
          pointerEvents: "none"
          // Transparent to the hover style.
        },
        [`&:hover:not(.${inputClasses_default.disabled}, .${inputClasses_default.error}):before`]: {
          borderBottom: `2px solid ${(theme.vars || theme).palette.text.primary}`,
          // Reset on touch devices, it doesn't add specificity
          "@media (hover: none)": {
            borderBottom: `1px solid ${bottomLineColor}`
          }
        },
        [`&.${inputClasses_default.disabled}:before`]: {
          borderBottomStyle: "dotted"
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
      props: {
        color,
        disableUnderline: false
      },
      style: {
        "&::after": {
          borderBottom: `2px solid ${(theme.vars || theme).palette[color].main}`
        }
      }
    }))]
  };
}));
var InputInput = styled_default2(InputBaseInput, {
  name: "MuiInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver
})({});
var Input = React162.forwardRef(function Input2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiInput"
  });
  const {
    disableUnderline = false,
    components = {},
    componentsProps: componentsPropsProp,
    fullWidth = false,
    inputComponent = "input",
    multiline = false,
    slotProps,
    slots = {},
    type = "text",
    ...other
  } = props;
  const classes2 = useUtilityClasses64(props);
  const ownerState = {
    disableUnderline
  };
  const inputComponentsProps = {
    root: {
      ownerState
    }
  };
  const componentsProps = slotProps ?? componentsPropsProp ? deepmerge(slotProps ?? componentsPropsProp, inputComponentsProps) : inputComponentsProps;
  const RootSlot = slots.root ?? components.Root ?? InputRoot;
  const InputSlot = slots.input ?? components.Input ?? InputInput;
  return (0, import_jsx_runtime132.jsx)(InputBase_default, {
    slots: {
      root: RootSlot,
      input: InputSlot
    },
    slotProps: componentsProps,
    fullWidth,
    inputComponent,
    multiline,
    ref,
    type,
    ...other,
    classes: classes2
  });
});
true ? Input.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop helps users to fill forms faster, especially on mobile devices.
   * The name can be confusing, as it's more like an autofill.
   * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
   */
  autoComplete: import_prop_types105.default.string,
  /**
   * If `true`, the `input` element is focused during the first mount.
   */
  autoFocus: import_prop_types105.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types105.default.object,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
   */
  color: import_prop_types105.default.oneOfType([import_prop_types105.default.oneOf(["primary", "secondary"]), import_prop_types105.default.string]),
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types105.default.shape({
    Input: import_prop_types105.default.elementType,
    Root: import_prop_types105.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types105.default.shape({
    input: import_prop_types105.default.object,
    root: import_prop_types105.default.object
  }),
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types105.default.any,
  /**
   * If `true`, the component is disabled.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  disabled: import_prop_types105.default.bool,
  /**
   * If `true`, the `input` will not have an underline.
   * @default false
   */
  disableUnderline: import_prop_types105.default.bool,
  /**
   * End `InputAdornment` for this component.
   */
  endAdornment: import_prop_types105.default.node,
  /**
   * If `true`, the `input` will indicate an error.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  error: import_prop_types105.default.bool,
  /**
   * If `true`, the `input` will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types105.default.bool,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types105.default.string,
  /**
   * The component used for the `input` element.
   * Either a string to use a HTML element or a component.
   * @default 'input'
   */
  inputComponent: import_prop_types105.default.elementType,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @default {}
   */
  inputProps: import_prop_types105.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
   */
  margin: import_prop_types105.default.oneOf(["dense", "none"]),
  /**
   * Maximum number of rows to display when multiline option is set to true.
   */
  maxRows: import_prop_types105.default.oneOfType([import_prop_types105.default.number, import_prop_types105.default.string]),
  /**
   * Minimum number of rows to display when multiline option is set to true.
   */
  minRows: import_prop_types105.default.oneOfType([import_prop_types105.default.number, import_prop_types105.default.string]),
  /**
   * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
   * @default false
   */
  multiline: import_prop_types105.default.bool,
  /**
   * Name attribute of the `input` element.
   */
  name: import_prop_types105.default.string,
  /**
   * Callback fired when the value is changed.
   *
   * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types105.default.func,
  /**
   * The short hint displayed in the `input` before the user enters a value.
   */
  placeholder: import_prop_types105.default.string,
  /**
   * It prevents the user from changing the value of the field
   * (not from interacting with the field).
   */
  readOnly: import_prop_types105.default.bool,
  /**
   * If `true`, the `input` element is required.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  required: import_prop_types105.default.bool,
  /**
   * Number of rows to display when multiline option is set to true.
   */
  rows: import_prop_types105.default.oneOfType([import_prop_types105.default.number, import_prop_types105.default.string]),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slotProps: import_prop_types105.default.shape({
    input: import_prop_types105.default.object,
    root: import_prop_types105.default.object
  }),
  /**
   * The components used for each slot inside.
   *
   * This prop is an alias for the `components` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slots: import_prop_types105.default.shape({
    input: import_prop_types105.default.elementType,
    root: import_prop_types105.default.elementType
  }),
  /**
   * Start `InputAdornment` for this component.
   */
  startAdornment: import_prop_types105.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types105.default.oneOfType([import_prop_types105.default.arrayOf(import_prop_types105.default.oneOfType([import_prop_types105.default.func, import_prop_types105.default.object, import_prop_types105.default.bool])), import_prop_types105.default.func, import_prop_types105.default.object]),
  /**
   * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
   * @default 'text'
   */
  type: import_prop_types105.default.string,
  /**
   * The value of the `input` element, required for a controlled component.
   */
  value: import_prop_types105.default.any
} : void 0;
Input.muiName = "Input";
var Input_default = Input;

// node_modules/@mui/material/esm/FilledInput/FilledInput.js
var React163 = __toESM(require_react(), 1);
var import_prop_types106 = __toESM(require_prop_types(), 1);
var import_jsx_runtime133 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses65 = (ownerState) => {
  const {
    classes: classes2,
    disableUnderline,
    startAdornment,
    endAdornment,
    size,
    hiddenLabel,
    multiline
  } = ownerState;
  const slots = {
    root: ["root", !disableUnderline && "underline", startAdornment && "adornedStart", endAdornment && "adornedEnd", size === "small" && `size${capitalize_default(size)}`, hiddenLabel && "hiddenLabel", multiline && "multiline"],
    input: ["input"]
  };
  const composedClasses = composeClasses(slots, getFilledInputUtilityClass, classes2);
  return {
    ...classes2,
    // forward classes to the InputBase
    ...composedClasses
  };
};
var FilledInputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiFilledInput",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [...rootOverridesResolver(props, styles5), !ownerState.disableUnderline && styles5.underline];
  }
})(memoTheme_default(({
  theme
}) => {
  const light = theme.palette.mode === "light";
  const bottomLineColor = light ? "rgba(0, 0, 0, 0.42)" : "rgba(255, 255, 255, 0.7)";
  const backgroundColor = light ? "rgba(0, 0, 0, 0.06)" : "rgba(255, 255, 255, 0.09)";
  const hoverBackground = light ? "rgba(0, 0, 0, 0.09)" : "rgba(255, 255, 255, 0.13)";
  const disabledBackground = light ? "rgba(0, 0, 0, 0.12)" : "rgba(255, 255, 255, 0.12)";
  return {
    position: "relative",
    backgroundColor: theme.vars ? theme.vars.palette.FilledInput.bg : backgroundColor,
    borderTopLeftRadius: (theme.vars || theme).shape.borderRadius,
    borderTopRightRadius: (theme.vars || theme).shape.borderRadius,
    transition: theme.transitions.create("background-color", {
      duration: theme.transitions.duration.shorter,
      easing: theme.transitions.easing.easeOut
    }),
    "&:hover": {
      backgroundColor: theme.vars ? theme.vars.palette.FilledInput.hoverBg : hoverBackground,
      // Reset on touch devices, it doesn't add specificity
      "@media (hover: none)": {
        backgroundColor: theme.vars ? theme.vars.palette.FilledInput.bg : backgroundColor
      }
    },
    [`&.${filledInputClasses_default.focused}`]: {
      backgroundColor: theme.vars ? theme.vars.palette.FilledInput.bg : backgroundColor
    },
    [`&.${filledInputClasses_default.disabled}`]: {
      backgroundColor: theme.vars ? theme.vars.palette.FilledInput.disabledBg : disabledBackground
    },
    variants: [{
      props: ({
        ownerState
      }) => !ownerState.disableUnderline,
      style: {
        "&::after": {
          left: 0,
          bottom: 0,
          content: '""',
          position: "absolute",
          right: 0,
          transform: "scaleX(0)",
          transition: theme.transitions.create("transform", {
            duration: theme.transitions.duration.shorter,
            easing: theme.transitions.easing.easeOut
          }),
          pointerEvents: "none"
          // Transparent to the hover style.
        },
        [`&.${filledInputClasses_default.focused}:after`]: {
          // translateX(0) is a workaround for Safari transform scale bug
          // See https://github.com/mui/material-ui/issues/31766
          transform: "scaleX(1) translateX(0)"
        },
        [`&.${filledInputClasses_default.error}`]: {
          "&::before, &::after": {
            borderBottomColor: (theme.vars || theme).palette.error.main
          }
        },
        "&::before": {
          borderBottom: `1px solid ${theme.vars ? theme.alpha(theme.vars.palette.common.onBackground, theme.vars.opacity.inputUnderline) : bottomLineColor}`,
          left: 0,
          bottom: 0,
          content: '"\\00a0"',
          position: "absolute",
          right: 0,
          transition: theme.transitions.create("border-bottom-color", {
            duration: theme.transitions.duration.shorter
          }),
          pointerEvents: "none"
          // Transparent to the hover style.
        },
        [`&:hover:not(.${filledInputClasses_default.disabled}, .${filledInputClasses_default.error}):before`]: {
          borderBottom: `1px solid ${(theme.vars || theme).palette.text.primary}`
        },
        [`&.${filledInputClasses_default.disabled}:before`]: {
          borderBottomStyle: "dotted"
        }
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => {
      var _a;
      return {
        props: {
          disableUnderline: false,
          color
        },
        style: {
          "&::after": {
            borderBottom: `2px solid ${(_a = (theme.vars || theme).palette[color]) == null ? void 0 : _a.main}`
          }
        }
      };
    }), {
      props: ({
        ownerState
      }) => ownerState.startAdornment,
      style: {
        paddingLeft: 12
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.endAdornment,
      style: {
        paddingRight: 12
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.multiline,
      style: {
        padding: "25px 12px 8px"
      }
    }, {
      props: ({
        ownerState,
        size
      }) => ownerState.multiline && size === "small",
      style: {
        paddingTop: 21,
        paddingBottom: 4
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.multiline && ownerState.hiddenLabel,
      style: {
        paddingTop: 16,
        paddingBottom: 17
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.multiline && ownerState.hiddenLabel && ownerState.size === "small",
      style: {
        paddingTop: 8,
        paddingBottom: 9
      }
    }]
  };
}));
var FilledInputInput = styled_default2(InputBaseInput, {
  name: "MuiFilledInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver
})(memoTheme_default(({
  theme
}) => ({
  paddingTop: 25,
  paddingRight: 12,
  paddingBottom: 8,
  paddingLeft: 12,
  ...!theme.vars && {
    "&:-webkit-autofill": {
      WebkitBoxShadow: theme.palette.mode === "light" ? null : "0 0 0 100px #266798 inset",
      WebkitTextFillColor: theme.palette.mode === "light" ? null : "#fff",
      caretColor: theme.palette.mode === "light" ? null : "#fff",
      borderTopLeftRadius: "inherit",
      borderTopRightRadius: "inherit"
    }
  },
  ...theme.vars && {
    "&:-webkit-autofill": {
      borderTopLeftRadius: "inherit",
      borderTopRightRadius: "inherit"
    },
    [theme.getColorSchemeSelector("dark")]: {
      "&:-webkit-autofill": {
        WebkitBoxShadow: "0 0 0 100px #266798 inset",
        WebkitTextFillColor: "#fff",
        caretColor: "#fff"
      }
    }
  },
  variants: [{
    props: {
      size: "small"
    },
    style: {
      paddingTop: 21,
      paddingBottom: 4
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.hiddenLabel,
    style: {
      paddingTop: 16,
      paddingBottom: 17
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.startAdornment,
    style: {
      paddingLeft: 0
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.endAdornment,
    style: {
      paddingRight: 0
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.hiddenLabel && ownerState.size === "small",
    style: {
      paddingTop: 8,
      paddingBottom: 9
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.multiline,
    style: {
      paddingTop: 0,
      paddingBottom: 0,
      paddingLeft: 0,
      paddingRight: 0
    }
  }]
})));
var FilledInput = React163.forwardRef(function FilledInput2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiFilledInput"
  });
  const {
    disableUnderline = false,
    components = {},
    componentsProps: componentsPropsProp,
    fullWidth = false,
    hiddenLabel,
    // declare here to prevent spreading to DOM
    inputComponent = "input",
    multiline = false,
    slotProps,
    slots = {},
    type = "text",
    ...other
  } = props;
  const ownerState = {
    ...props,
    disableUnderline,
    fullWidth,
    inputComponent,
    multiline,
    type
  };
  const classes2 = useUtilityClasses65(props);
  const filledInputComponentsProps = {
    root: {
      ownerState
    },
    input: {
      ownerState
    }
  };
  const componentsProps = slotProps ?? componentsPropsProp ? deepmerge(filledInputComponentsProps, slotProps ?? componentsPropsProp) : filledInputComponentsProps;
  const RootSlot = slots.root ?? components.Root ?? FilledInputRoot;
  const InputSlot = slots.input ?? components.Input ?? FilledInputInput;
  return (0, import_jsx_runtime133.jsx)(InputBase_default, {
    slots: {
      root: RootSlot,
      input: InputSlot
    },
    slotProps: componentsProps,
    fullWidth,
    inputComponent,
    multiline,
    ref,
    type,
    ...other,
    classes: classes2
  });
});
true ? FilledInput.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop helps users to fill forms faster, especially on mobile devices.
   * The name can be confusing, as it's more like an autofill.
   * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
   */
  autoComplete: import_prop_types106.default.string,
  /**
   * If `true`, the `input` element is focused during the first mount.
   */
  autoFocus: import_prop_types106.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types106.default.object,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
   */
  color: import_prop_types106.default.oneOfType([import_prop_types106.default.oneOf(["primary", "secondary"]), import_prop_types106.default.string]),
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types106.default.shape({
    Input: import_prop_types106.default.elementType,
    Root: import_prop_types106.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types106.default.shape({
    input: import_prop_types106.default.object,
    root: import_prop_types106.default.object
  }),
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types106.default.any,
  /**
   * If `true`, the component is disabled.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  disabled: import_prop_types106.default.bool,
  /**
   * If `true`, the input will not have an underline.
   * @default false
   */
  disableUnderline: import_prop_types106.default.bool,
  /**
   * End `InputAdornment` for this component.
   */
  endAdornment: import_prop_types106.default.node,
  /**
   * If `true`, the `input` will indicate an error.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  error: import_prop_types106.default.bool,
  /**
   * If `true`, the `input` will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types106.default.bool,
  /**
   * If `true`, the label is hidden.
   * This is used to increase density for a `FilledInput`.
   * Be sure to add `aria-label` to the `input` element.
   * @default false
   */
  hiddenLabel: import_prop_types106.default.bool,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types106.default.string,
  /**
   * The component used for the `input` element.
   * Either a string to use a HTML element or a component.
   * @default 'input'
   */
  inputComponent: import_prop_types106.default.elementType,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @default {}
   */
  inputProps: import_prop_types106.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
   */
  margin: import_prop_types106.default.oneOf(["dense", "none"]),
  /**
   * Maximum number of rows to display when multiline option is set to true.
   */
  maxRows: import_prop_types106.default.oneOfType([import_prop_types106.default.number, import_prop_types106.default.string]),
  /**
   * Minimum number of rows to display when multiline option is set to true.
   */
  minRows: import_prop_types106.default.oneOfType([import_prop_types106.default.number, import_prop_types106.default.string]),
  /**
   * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
   * @default false
   */
  multiline: import_prop_types106.default.bool,
  /**
   * Name attribute of the `input` element.
   */
  name: import_prop_types106.default.string,
  /**
   * Callback fired when the value is changed.
   *
   * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types106.default.func,
  /**
   * The short hint displayed in the `input` before the user enters a value.
   */
  placeholder: import_prop_types106.default.string,
  /**
   * It prevents the user from changing the value of the field
   * (not from interacting with the field).
   */
  readOnly: import_prop_types106.default.bool,
  /**
   * If `true`, the `input` element is required.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  required: import_prop_types106.default.bool,
  /**
   * Number of rows to display when multiline option is set to true.
   */
  rows: import_prop_types106.default.oneOfType([import_prop_types106.default.number, import_prop_types106.default.string]),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * This prop is an alias for the `componentsProps` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slotProps: import_prop_types106.default.shape({
    input: import_prop_types106.default.object,
    root: import_prop_types106.default.object
  }),
  /**
   * The components used for each slot inside.
   *
   * This prop is an alias for the `components` prop, which will be deprecated in the future.
   *
   * @default {}
   */
  slots: import_prop_types106.default.shape({
    input: import_prop_types106.default.elementType,
    root: import_prop_types106.default.elementType
  }),
  /**
   * Start `InputAdornment` for this component.
   */
  startAdornment: import_prop_types106.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types106.default.oneOfType([import_prop_types106.default.arrayOf(import_prop_types106.default.oneOfType([import_prop_types106.default.func, import_prop_types106.default.object, import_prop_types106.default.bool])), import_prop_types106.default.func, import_prop_types106.default.object]),
  /**
   * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
   * @default 'text'
   */
  type: import_prop_types106.default.string,
  /**
   * The value of the `input` element, required for a controlled component.
   */
  value: import_prop_types106.default.any
} : void 0;
FilledInput.muiName = "Input";
var FilledInput_default = FilledInput;

// node_modules/@mui/material/esm/OutlinedInput/OutlinedInput.js
var React164 = __toESM(require_react(), 1);
var import_prop_types108 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/OutlinedInput/NotchedOutline.js
var import_prop_types107 = __toESM(require_prop_types(), 1);
var import_jsx_runtime134 = __toESM(require_jsx_runtime(), 1);
var _span;
var NotchedOutlineRoot = styled_default2("fieldset", {
  name: "MuiNotchedOutlined",
  shouldForwardProp: rootShouldForwardProp_default
})({
  textAlign: "left",
  position: "absolute",
  bottom: 0,
  right: 0,
  top: -5,
  left: 0,
  margin: 0,
  padding: "0 8px",
  pointerEvents: "none",
  borderRadius: "inherit",
  borderStyle: "solid",
  borderWidth: 1,
  overflow: "hidden",
  minWidth: "0%"
});
var NotchedOutlineLegend = styled_default2("legend", {
  name: "MuiNotchedOutlined",
  shouldForwardProp: rootShouldForwardProp_default
})(memoTheme_default(({
  theme
}) => ({
  float: "unset",
  // Fix conflict with bootstrap
  width: "auto",
  // Fix conflict with bootstrap
  overflow: "hidden",
  // Fix Horizontal scroll when label too long
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.withLabel,
    style: {
      padding: 0,
      lineHeight: "11px",
      // sync with `height` in `legend` styles
      transition: theme.transitions.create("width", {
        duration: 150,
        easing: theme.transitions.easing.easeOut
      })
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.withLabel,
    style: {
      display: "block",
      // Fix conflict with normalize.css and sanitize.css
      padding: 0,
      height: 11,
      // sync with `lineHeight` in `legend` styles
      fontSize: "0.75em",
      visibility: "hidden",
      maxWidth: 0.01,
      transition: theme.transitions.create("max-width", {
        duration: 50,
        easing: theme.transitions.easing.easeOut
      }),
      whiteSpace: "nowrap",
      "& > span": {
        paddingLeft: 5,
        paddingRight: 5,
        display: "inline-block",
        opacity: 0,
        visibility: "visible"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.withLabel && ownerState.notched,
    style: {
      maxWidth: "100%",
      transition: theme.transitions.create("max-width", {
        duration: 100,
        easing: theme.transitions.easing.easeOut,
        delay: 50
      })
    }
  }]
})));
function NotchedOutline(props) {
  const {
    children: children2,
    classes: classes2,
    className,
    label,
    notched,
    ...other
  } = props;
  const withLabel = label != null && label !== "";
  const ownerState = {
    ...props,
    notched,
    withLabel
  };
  return (0, import_jsx_runtime134.jsx)(NotchedOutlineRoot, {
    "aria-hidden": true,
    className,
    ownerState,
    ...other,
    children: (0, import_jsx_runtime134.jsx)(NotchedOutlineLegend, {
      ownerState,
      children: withLabel ? (0, import_jsx_runtime134.jsx)("span", {
        children: label
      }) : (
        // notranslate needed while Google Translate will not fix zero-width space issue
        _span || (_span = (0, import_jsx_runtime134.jsx)("span", {
          className: "notranslate",
          "aria-hidden": true,
          children: "​"
        }))
      )
    })
  });
}
true ? NotchedOutline.propTypes = {
  /**
   * The content of the component.
   */
  children: import_prop_types107.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types107.default.object,
  /**
   * @ignore
   */
  className: import_prop_types107.default.string,
  /**
   * The label.
   */
  label: import_prop_types107.default.node,
  /**
   * If `true`, the outline is notched to accommodate the label.
   */
  notched: import_prop_types107.default.bool.isRequired,
  /**
   * @ignore
   */
  style: import_prop_types107.default.object
} : void 0;

// node_modules/@mui/material/esm/OutlinedInput/OutlinedInput.js
var import_jsx_runtime135 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses66 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"],
    notchedOutline: ["notchedOutline"],
    input: ["input"]
  };
  const composedClasses = composeClasses(slots, getOutlinedInputUtilityClass, classes2);
  return {
    ...classes2,
    // forward classes to the InputBase
    ...composedClasses
  };
};
var OutlinedInputRoot = styled_default2(InputBaseRoot, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiOutlinedInput",
  slot: "Root",
  overridesResolver: rootOverridesResolver
})(memoTheme_default(({
  theme
}) => {
  const borderColor = theme.palette.mode === "light" ? "rgba(0, 0, 0, 0.23)" : "rgba(255, 255, 255, 0.23)";
  return {
    position: "relative",
    borderRadius: (theme.vars || theme).shape.borderRadius,
    [`&:hover .${outlinedInputClasses_default.notchedOutline}`]: {
      borderColor: (theme.vars || theme).palette.text.primary
    },
    // Reset on touch devices, it doesn't add specificity
    "@media (hover: none)": {
      [`&:hover .${outlinedInputClasses_default.notchedOutline}`]: {
        borderColor: theme.vars ? theme.alpha(theme.vars.palette.common.onBackground, 0.23) : borderColor
      }
    },
    [`&.${outlinedInputClasses_default.focused} .${outlinedInputClasses_default.notchedOutline}`]: {
      borderWidth: 2
    },
    variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
      props: {
        color
      },
      style: {
        [`&.${outlinedInputClasses_default.focused} .${outlinedInputClasses_default.notchedOutline}`]: {
          borderColor: (theme.vars || theme).palette[color].main
        }
      }
    })), {
      props: {},
      // to override the above style
      style: {
        [`&.${outlinedInputClasses_default.error} .${outlinedInputClasses_default.notchedOutline}`]: {
          borderColor: (theme.vars || theme).palette.error.main
        },
        [`&.${outlinedInputClasses_default.disabled} .${outlinedInputClasses_default.notchedOutline}`]: {
          borderColor: (theme.vars || theme).palette.action.disabled
        }
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.startAdornment,
      style: {
        paddingLeft: 14
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.endAdornment,
      style: {
        paddingRight: 14
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.multiline,
      style: {
        padding: "16.5px 14px"
      }
    }, {
      props: ({
        ownerState,
        size
      }) => ownerState.multiline && size === "small",
      style: {
        padding: "8.5px 14px"
      }
    }]
  };
}));
var NotchedOutlineRoot2 = styled_default2(NotchedOutline, {
  name: "MuiOutlinedInput",
  slot: "NotchedOutline"
})(memoTheme_default(({
  theme
}) => {
  const borderColor = theme.palette.mode === "light" ? "rgba(0, 0, 0, 0.23)" : "rgba(255, 255, 255, 0.23)";
  return {
    borderColor: theme.vars ? theme.alpha(theme.vars.palette.common.onBackground, 0.23) : borderColor
  };
}));
var OutlinedInputInput = styled_default2(InputBaseInput, {
  name: "MuiOutlinedInput",
  slot: "Input",
  overridesResolver: inputOverridesResolver
})(memoTheme_default(({
  theme
}) => ({
  padding: "16.5px 14px",
  ...!theme.vars && {
    "&:-webkit-autofill": {
      WebkitBoxShadow: theme.palette.mode === "light" ? null : "0 0 0 100px #266798 inset",
      WebkitTextFillColor: theme.palette.mode === "light" ? null : "#fff",
      caretColor: theme.palette.mode === "light" ? null : "#fff",
      borderRadius: "inherit"
    }
  },
  ...theme.vars && {
    "&:-webkit-autofill": {
      borderRadius: "inherit"
    },
    [theme.getColorSchemeSelector("dark")]: {
      "&:-webkit-autofill": {
        WebkitBoxShadow: "0 0 0 100px #266798 inset",
        WebkitTextFillColor: "#fff",
        caretColor: "#fff"
      }
    }
  },
  variants: [{
    props: {
      size: "small"
    },
    style: {
      padding: "8.5px 14px"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.multiline,
    style: {
      padding: 0
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.startAdornment,
    style: {
      paddingLeft: 0
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.endAdornment,
    style: {
      paddingRight: 0
    }
  }]
})));
var OutlinedInput = React164.forwardRef(function OutlinedInput2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiOutlinedInput"
  });
  const {
    components = {},
    fullWidth = false,
    inputComponent = "input",
    label,
    multiline = false,
    notched,
    slots = {},
    slotProps = {},
    type = "text",
    ...other
  } = props;
  const classes2 = useUtilityClasses66(props);
  const muiFormControl = useFormControl();
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["color", "disabled", "error", "focused", "hiddenLabel", "size", "required"]
  });
  const ownerState = {
    ...props,
    color: fcs.color || "primary",
    disabled: fcs.disabled,
    error: fcs.error,
    focused: fcs.focused,
    formControl: muiFormControl,
    fullWidth,
    hiddenLabel: fcs.hiddenLabel,
    multiline,
    size: fcs.size,
    type
  };
  const RootSlot = slots.root ?? components.Root ?? OutlinedInputRoot;
  const InputSlot = slots.input ?? components.Input ?? OutlinedInputInput;
  const [NotchedSlot, notchedProps] = useSlot("notchedOutline", {
    elementType: NotchedOutlineRoot2,
    className: classes2.notchedOutline,
    shouldForwardComponentProp: true,
    ownerState,
    externalForwardedProps: {
      slots,
      slotProps
    },
    additionalProps: {
      label: label != null && label !== "" && fcs.required ? (0, import_jsx_runtime135.jsxs)(React164.Fragment, {
        children: [label, " ", "*"]
      }) : label
    }
  });
  return (0, import_jsx_runtime135.jsx)(InputBase_default, {
    slots: {
      root: RootSlot,
      input: InputSlot
    },
    slotProps,
    renderSuffix: (state) => (0, import_jsx_runtime135.jsx)(NotchedSlot, {
      ...notchedProps,
      notched: typeof notched !== "undefined" ? notched : Boolean(state.startAdornment || state.filled || state.focused)
    }),
    fullWidth,
    inputComponent,
    multiline,
    ref,
    type,
    ...other,
    classes: {
      ...classes2,
      notchedOutline: null
    }
  });
});
true ? OutlinedInput.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop helps users to fill forms faster, especially on mobile devices.
   * The name can be confusing, as it's more like an autofill.
   * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
   */
  autoComplete: import_prop_types108.default.string,
  /**
   * If `true`, the `input` element is focused during the first mount.
   */
  autoFocus: import_prop_types108.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types108.default.object,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * The prop defaults to the value (`'primary'`) inherited from the parent FormControl component.
   */
  color: import_prop_types108.default.oneOfType([import_prop_types108.default.oneOf(["primary", "secondary"]), import_prop_types108.default.string]),
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types108.default.shape({
    Input: import_prop_types108.default.elementType,
    Root: import_prop_types108.default.elementType
  }),
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types108.default.any,
  /**
   * If `true`, the component is disabled.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  disabled: import_prop_types108.default.bool,
  /**
   * End `InputAdornment` for this component.
   */
  endAdornment: import_prop_types108.default.node,
  /**
   * If `true`, the `input` will indicate an error.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  error: import_prop_types108.default.bool,
  /**
   * If `true`, the `input` will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types108.default.bool,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types108.default.string,
  /**
   * The component used for the `input` element.
   * Either a string to use a HTML element or a component.
   * @default 'input'
   */
  inputComponent: import_prop_types108.default.elementType,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @default {}
   */
  inputProps: import_prop_types108.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * The label of the `input`. It is only used for layout. The actual labelling
   * is handled by `InputLabel`.
   */
  label: import_prop_types108.default.node,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   * The prop defaults to the value (`'none'`) inherited from the parent FormControl component.
   */
  margin: import_prop_types108.default.oneOf(["dense", "none"]),
  /**
   * Maximum number of rows to display when multiline option is set to true.
   */
  maxRows: import_prop_types108.default.oneOfType([import_prop_types108.default.number, import_prop_types108.default.string]),
  /**
   * Minimum number of rows to display when multiline option is set to true.
   */
  minRows: import_prop_types108.default.oneOfType([import_prop_types108.default.number, import_prop_types108.default.string]),
  /**
   * If `true`, a [TextareaAutosize](https://mui.com/material-ui/react-textarea-autosize/) element is rendered.
   * @default false
   */
  multiline: import_prop_types108.default.bool,
  /**
   * Name attribute of the `input` element.
   */
  name: import_prop_types108.default.string,
  /**
   * If `true`, the outline is notched to accommodate the label.
   */
  notched: import_prop_types108.default.bool,
  /**
   * Callback fired when the value is changed.
   *
   * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types108.default.func,
  /**
   * The short hint displayed in the `input` before the user enters a value.
   */
  placeholder: import_prop_types108.default.string,
  /**
   * It prevents the user from changing the value of the field
   * (not from interacting with the field).
   */
  readOnly: import_prop_types108.default.bool,
  /**
   * If `true`, the `input` element is required.
   * The prop defaults to the value (`false`) inherited from the parent FormControl component.
   */
  required: import_prop_types108.default.bool,
  /**
   * Number of rows to display when multiline option is set to true.
   */
  rows: import_prop_types108.default.oneOfType([import_prop_types108.default.number, import_prop_types108.default.string]),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types108.default.shape({
    input: import_prop_types108.default.object,
    notchedOutline: import_prop_types108.default.oneOfType([import_prop_types108.default.func, import_prop_types108.default.object]),
    root: import_prop_types108.default.object
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types108.default.shape({
    input: import_prop_types108.default.elementType,
    notchedOutline: import_prop_types108.default.elementType,
    root: import_prop_types108.default.elementType
  }),
  /**
   * Start `InputAdornment` for this component.
   */
  startAdornment: import_prop_types108.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types108.default.oneOfType([import_prop_types108.default.arrayOf(import_prop_types108.default.oneOfType([import_prop_types108.default.func, import_prop_types108.default.object, import_prop_types108.default.bool])), import_prop_types108.default.func, import_prop_types108.default.object]),
  /**
   * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
   * @default 'text'
   */
  type: import_prop_types108.default.string,
  /**
   * The value of the `input` element, required for a controlled component.
   */
  value: import_prop_types108.default.any
} : void 0;
OutlinedInput.muiName = "Input";
var OutlinedInput_default = OutlinedInput;

// node_modules/@mui/material/esm/InputLabel/InputLabel.js
var React166 = __toESM(require_react(), 1);
var import_prop_types110 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/FormLabel/FormLabel.js
var React165 = __toESM(require_react(), 1);
var import_prop_types109 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/FormLabel/formLabelClasses.js
function getFormLabelUtilityClasses(slot) {
  return generateUtilityClass("MuiFormLabel", slot);
}
var formLabelClasses = generateUtilityClasses("MuiFormLabel", ["root", "colorSecondary", "focused", "disabled", "error", "filled", "required", "asterisk"]);
var formLabelClasses_default = formLabelClasses;

// node_modules/@mui/material/esm/FormLabel/FormLabel.js
var import_jsx_runtime136 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses67 = (ownerState) => {
  const {
    classes: classes2,
    color,
    focused,
    disabled,
    error,
    filled,
    required
  } = ownerState;
  const slots = {
    root: ["root", `color${capitalize_default(color)}`, disabled && "disabled", error && "error", filled && "filled", focused && "focused", required && "required"],
    asterisk: ["asterisk", error && "error"]
  };
  return composeClasses(slots, getFormLabelUtilityClasses, classes2);
};
var FormLabelRoot = styled_default2("label", {
  name: "MuiFormLabel",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.color === "secondary" && styles5.colorSecondary, ownerState.filled && styles5.filled];
  }
})(memoTheme_default(({
  theme
}) => ({
  color: (theme.vars || theme).palette.text.secondary,
  ...theme.typography.body1,
  lineHeight: "1.4375em",
  padding: 0,
  position: "relative",
  variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      [`&.${formLabelClasses_default.focused}`]: {
        color: (theme.vars || theme).palette[color].main
      }
    }
  })), {
    props: {},
    style: {
      [`&.${formLabelClasses_default.disabled}`]: {
        color: (theme.vars || theme).palette.text.disabled
      },
      [`&.${formLabelClasses_default.error}`]: {
        color: (theme.vars || theme).palette.error.main
      }
    }
  }]
})));
var AsteriskComponent = styled_default2("span", {
  name: "MuiFormLabel",
  slot: "Asterisk"
})(memoTheme_default(({
  theme
}) => ({
  [`&.${formLabelClasses_default.error}`]: {
    color: (theme.vars || theme).palette.error.main
  }
})));
var FormLabel = React165.forwardRef(function FormLabel2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiFormLabel"
  });
  const {
    children: children2,
    className,
    color,
    component = "label",
    disabled,
    error,
    filled,
    focused,
    required,
    ...other
  } = props;
  const muiFormControl = useFormControl();
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["color", "required", "focused", "disabled", "error", "filled"]
  });
  const ownerState = {
    ...props,
    color: fcs.color || "primary",
    component,
    disabled: fcs.disabled,
    error: fcs.error,
    filled: fcs.filled,
    focused: fcs.focused,
    required: fcs.required
  };
  const classes2 = useUtilityClasses67(ownerState);
  return (0, import_jsx_runtime136.jsxs)(FormLabelRoot, {
    as: component,
    ownerState,
    className: clsx_default(classes2.root, className),
    ref,
    ...other,
    children: [children2, fcs.required && (0, import_jsx_runtime136.jsxs)(AsteriskComponent, {
      ownerState,
      "aria-hidden": true,
      className: classes2.asterisk,
      children: [" ", "*"]
    })]
  });
});
true ? FormLabel.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types109.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types109.default.object,
  /**
   * @ignore
   */
  className: import_prop_types109.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   */
  color: import_prop_types109.default.oneOfType([import_prop_types109.default.oneOf(["error", "info", "primary", "secondary", "success", "warning"]), import_prop_types109.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types109.default.elementType,
  /**
   * If `true`, the label should be displayed in a disabled state.
   */
  disabled: import_prop_types109.default.bool,
  /**
   * If `true`, the label is displayed in an error state.
   */
  error: import_prop_types109.default.bool,
  /**
   * If `true`, the label should use filled classes key.
   */
  filled: import_prop_types109.default.bool,
  /**
   * If `true`, the input of this label is focused (used by `FormGroup` components).
   */
  focused: import_prop_types109.default.bool,
  /**
   * If `true`, the label will indicate that the `input` is required.
   */
  required: import_prop_types109.default.bool,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types109.default.oneOfType([import_prop_types109.default.arrayOf(import_prop_types109.default.oneOfType([import_prop_types109.default.func, import_prop_types109.default.object, import_prop_types109.default.bool])), import_prop_types109.default.func, import_prop_types109.default.object])
} : void 0;
var FormLabel_default = FormLabel;

// node_modules/@mui/material/esm/InputLabel/inputLabelClasses.js
function getInputLabelUtilityClasses(slot) {
  return generateUtilityClass("MuiInputLabel", slot);
}
var inputLabelClasses = generateUtilityClasses("MuiInputLabel", ["root", "focused", "disabled", "error", "required", "asterisk", "formControl", "sizeSmall", "shrink", "animated", "standard", "filled", "outlined"]);

// node_modules/@mui/material/esm/InputLabel/InputLabel.js
var import_jsx_runtime137 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses68 = (ownerState) => {
  const {
    classes: classes2,
    formControl,
    size,
    shrink,
    disableAnimation,
    variant,
    required
  } = ownerState;
  const slots = {
    root: ["root", formControl && "formControl", !disableAnimation && "animated", shrink && "shrink", size && size !== "medium" && `size${capitalize_default(size)}`, variant],
    asterisk: [required && "asterisk"]
  };
  const composedClasses = composeClasses(slots, getInputLabelUtilityClasses, classes2);
  return {
    ...classes2,
    // forward the focused, disabled, etc. classes to the FormLabel
    ...composedClasses
  };
};
var InputLabelRoot = styled_default2(FormLabel_default, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiInputLabel",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [{
      [`& .${formLabelClasses_default.asterisk}`]: styles5.asterisk
    }, styles5.root, ownerState.formControl && styles5.formControl, ownerState.size === "small" && styles5.sizeSmall, ownerState.shrink && styles5.shrink, !ownerState.disableAnimation && styles5.animated, ownerState.focused && styles5.focused, styles5[ownerState.variant]];
  }
})(memoTheme_default(({
  theme
}) => ({
  display: "block",
  transformOrigin: "top left",
  whiteSpace: "nowrap",
  overflow: "hidden",
  textOverflow: "ellipsis",
  maxWidth: "100%",
  variants: [{
    props: ({
      ownerState
    }) => ownerState.formControl,
    style: {
      position: "absolute",
      left: 0,
      top: 0,
      // slight alteration to spec spacing to match visual spec result
      transform: "translate(0, 20px) scale(1)"
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      // Compensation for the `Input.inputSizeSmall` style.
      transform: "translate(0, 17px) scale(1)"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.shrink,
    style: {
      transform: "translate(0, -1.5px) scale(0.75)",
      transformOrigin: "top left",
      maxWidth: "133%"
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.disableAnimation,
    style: {
      transition: theme.transitions.create(["color", "transform", "max-width"], {
        duration: theme.transitions.duration.shorter,
        easing: theme.transitions.easing.easeOut
      })
    }
  }, {
    props: {
      variant: "filled"
    },
    style: {
      // Chrome's autofill feature gives the input field a yellow background.
      // Since the input field is behind the label in the HTML tree,
      // the input field is drawn last and hides the label with an opaque background color.
      // zIndex: 1 will raise the label above opaque background-colors of input.
      zIndex: 1,
      pointerEvents: "none",
      transform: "translate(12px, 16px) scale(1)",
      maxWidth: "calc(100% - 24px)"
    }
  }, {
    props: {
      variant: "filled",
      size: "small"
    },
    style: {
      transform: "translate(12px, 13px) scale(1)"
    }
  }, {
    props: ({
      variant,
      ownerState
    }) => variant === "filled" && ownerState.shrink,
    style: {
      userSelect: "none",
      pointerEvents: "auto",
      transform: "translate(12px, 7px) scale(0.75)",
      maxWidth: "calc(133% - 24px)"
    }
  }, {
    props: ({
      variant,
      ownerState,
      size
    }) => variant === "filled" && ownerState.shrink && size === "small",
    style: {
      transform: "translate(12px, 4px) scale(0.75)"
    }
  }, {
    props: {
      variant: "outlined"
    },
    style: {
      // see comment above on filled.zIndex
      zIndex: 1,
      pointerEvents: "none",
      transform: "translate(14px, 16px) scale(1)",
      maxWidth: "calc(100% - 24px)"
    }
  }, {
    props: {
      variant: "outlined",
      size: "small"
    },
    style: {
      transform: "translate(14px, 9px) scale(1)"
    }
  }, {
    props: ({
      variant,
      ownerState
    }) => variant === "outlined" && ownerState.shrink,
    style: {
      userSelect: "none",
      pointerEvents: "auto",
      // Theoretically, we should have (8+5)*2/0.75 = 34px
      // but it feels a better when it bleeds a bit on the left, so 32px.
      maxWidth: "calc(133% - 32px)",
      transform: "translate(14px, -9px) scale(0.75)"
    }
  }]
})));
var InputLabel = React166.forwardRef(function InputLabel2(inProps, ref) {
  const props = useDefaultProps({
    name: "MuiInputLabel",
    props: inProps
  });
  const {
    disableAnimation = false,
    margin,
    shrink: shrinkProp,
    variant,
    className,
    ...other
  } = props;
  const muiFormControl = useFormControl();
  let shrink = shrinkProp;
  if (typeof shrink === "undefined" && muiFormControl) {
    shrink = muiFormControl.filled || muiFormControl.focused || muiFormControl.adornedStart;
  }
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["size", "variant", "required", "focused"]
  });
  const ownerState = {
    ...props,
    disableAnimation,
    formControl: muiFormControl,
    shrink,
    size: fcs.size,
    variant: fcs.variant,
    required: fcs.required,
    focused: fcs.focused
  };
  const classes2 = useUtilityClasses68(ownerState);
  return (0, import_jsx_runtime137.jsx)(InputLabelRoot, {
    "data-shrink": shrink,
    ref,
    className: clsx_default(classes2.root, className),
    ...other,
    ownerState,
    classes: classes2
  });
});
true ? InputLabel.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types110.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types110.default.object,
  /**
   * @ignore
   */
  className: import_prop_types110.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   */
  color: import_prop_types110.default.oneOfType([import_prop_types110.default.oneOf(["error", "info", "primary", "secondary", "success", "warning"]), import_prop_types110.default.string]),
  /**
   * If `true`, the transition animation is disabled.
   * @default false
   */
  disableAnimation: import_prop_types110.default.bool,
  /**
   * If `true`, the component is disabled.
   */
  disabled: import_prop_types110.default.bool,
  /**
   * If `true`, the label is displayed in an error state.
   */
  error: import_prop_types110.default.bool,
  /**
   * If `true`, the `input` of this label is focused.
   */
  focused: import_prop_types110.default.bool,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   */
  margin: import_prop_types110.default.oneOf(["dense"]),
  /**
   * if `true`, the label will indicate that the `input` is required.
   */
  required: import_prop_types110.default.bool,
  /**
   * If `true`, the label is shrunk.
   */
  shrink: import_prop_types110.default.bool,
  /**
   * The size of the component.
   * @default 'medium'
   */
  size: import_prop_types110.default.oneOfType([import_prop_types110.default.oneOf(["medium", "small"]), import_prop_types110.default.string]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types110.default.oneOfType([import_prop_types110.default.arrayOf(import_prop_types110.default.oneOfType([import_prop_types110.default.func, import_prop_types110.default.object, import_prop_types110.default.bool])), import_prop_types110.default.func, import_prop_types110.default.object]),
  /**
   * The variant to use.
   */
  variant: import_prop_types110.default.oneOf(["filled", "outlined", "standard"])
} : void 0;
var InputLabel_default = InputLabel;

// node_modules/@mui/material/esm/FormControl/FormControl.js
var React167 = __toESM(require_react(), 1);
var import_prop_types111 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/FormControl/formControlClasses.js
function getFormControlUtilityClasses(slot) {
  return generateUtilityClass("MuiFormControl", slot);
}
var formControlClasses = generateUtilityClasses("MuiFormControl", ["root", "marginNone", "marginNormal", "marginDense", "fullWidth", "disabled"]);

// node_modules/@mui/material/esm/FormControl/FormControl.js
var import_jsx_runtime138 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses69 = (ownerState) => {
  const {
    classes: classes2,
    margin,
    fullWidth
  } = ownerState;
  const slots = {
    root: ["root", margin !== "none" && `margin${capitalize_default(margin)}`, fullWidth && "fullWidth"]
  };
  return composeClasses(slots, getFormControlUtilityClasses, classes2);
};
var FormControlRoot = styled_default2("div", {
  name: "MuiFormControl",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[`margin${capitalize_default(ownerState.margin)}`], ownerState.fullWidth && styles5.fullWidth];
  }
})({
  display: "inline-flex",
  flexDirection: "column",
  position: "relative",
  // Reset fieldset default style.
  minWidth: 0,
  padding: 0,
  margin: 0,
  border: 0,
  verticalAlign: "top",
  // Fix alignment issue on Safari.
  variants: [{
    props: {
      margin: "normal"
    },
    style: {
      marginTop: 16,
      marginBottom: 8
    }
  }, {
    props: {
      margin: "dense"
    },
    style: {
      marginTop: 8,
      marginBottom: 4
    }
  }, {
    props: {
      fullWidth: true
    },
    style: {
      width: "100%"
    }
  }]
});
var FormControl = React167.forwardRef(function FormControl2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiFormControl"
  });
  const {
    children: children2,
    className,
    color = "primary",
    component = "div",
    disabled = false,
    error = false,
    focused: visuallyFocused,
    fullWidth = false,
    hiddenLabel = false,
    margin = "none",
    required = false,
    size = "medium",
    variant = "outlined",
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    component,
    disabled,
    error,
    fullWidth,
    hiddenLabel,
    margin,
    required,
    size,
    variant
  };
  const classes2 = useUtilityClasses69(ownerState);
  const [adornedStart, setAdornedStart] = React167.useState(() => {
    let initialAdornedStart = false;
    if (children2) {
      React167.Children.forEach(children2, (child) => {
        if (!isMuiElement_default(child, ["Input", "Select"])) {
          return;
        }
        const input = isMuiElement_default(child, ["Select"]) ? child.props.input : child;
        if (input && isAdornedStart(input.props)) {
          initialAdornedStart = true;
        }
      });
    }
    return initialAdornedStart;
  });
  const [filled, setFilled] = React167.useState(() => {
    let initialFilled = false;
    if (children2) {
      React167.Children.forEach(children2, (child) => {
        if (!isMuiElement_default(child, ["Input", "Select"])) {
          return;
        }
        if (isFilled(child.props, true) || isFilled(child.props.inputProps, true)) {
          initialFilled = true;
        }
      });
    }
    return initialFilled;
  });
  const [focusedState, setFocused] = React167.useState(false);
  if (disabled && focusedState) {
    setFocused(false);
  }
  const focused = visuallyFocused !== void 0 && !disabled ? visuallyFocused : focusedState;
  let registerEffect;
  const registeredInput = React167.useRef(false);
  if (true) {
    registerEffect = () => {
      if (registeredInput.current) {
        console.error(["MUI: There are multiple `InputBase` components inside a FormControl.", "This creates visual inconsistencies, only use one `InputBase`."].join("\n"));
      }
      registeredInput.current = true;
      return () => {
        registeredInput.current = false;
      };
    };
  }
  const onFilled = React167.useCallback(() => {
    setFilled(true);
  }, []);
  const onEmpty = React167.useCallback(() => {
    setFilled(false);
  }, []);
  const childContext = React167.useMemo(() => {
    return {
      adornedStart,
      setAdornedStart,
      color,
      disabled,
      error,
      filled,
      focused,
      fullWidth,
      hiddenLabel,
      size,
      onBlur: () => {
        setFocused(false);
      },
      onFocus: () => {
        setFocused(true);
      },
      onEmpty,
      onFilled,
      registerEffect,
      required,
      variant
    };
  }, [adornedStart, color, disabled, error, filled, focused, fullWidth, hiddenLabel, registerEffect, onEmpty, onFilled, required, size, variant]);
  return (0, import_jsx_runtime138.jsx)(FormControlContext_default.Provider, {
    value: childContext,
    children: (0, import_jsx_runtime138.jsx)(FormControlRoot, {
      as: component,
      ownerState,
      className: clsx_default(classes2.root, className),
      ref,
      ...other,
      children: children2
    })
  });
});
true ? FormControl.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types111.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types111.default.object,
  /**
   * @ignore
   */
  className: import_prop_types111.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types111.default.oneOfType([import_prop_types111.default.oneOf(["primary", "secondary", "error", "info", "success", "warning"]), import_prop_types111.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types111.default.elementType,
  /**
   * If `true`, the label, input and helper text should be displayed in a disabled state.
   * @default false
   */
  disabled: import_prop_types111.default.bool,
  /**
   * If `true`, the label is displayed in an error state.
   * @default false
   */
  error: import_prop_types111.default.bool,
  /**
   * If `true`, the component is displayed in focused state.
   */
  focused: import_prop_types111.default.bool,
  /**
   * If `true`, the component will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types111.default.bool,
  /**
   * If `true`, the label is hidden.
   * This is used to increase density for a `FilledInput`.
   * Be sure to add `aria-label` to the `input` element.
   * @default false
   */
  hiddenLabel: import_prop_types111.default.bool,
  /**
   * If `dense` or `normal`, will adjust vertical spacing of this and contained components.
   * @default 'none'
   */
  margin: import_prop_types111.default.oneOf(["dense", "none", "normal"]),
  /**
   * If `true`, the label will indicate that the `input` is required.
   * @default false
   */
  required: import_prop_types111.default.bool,
  /**
   * The size of the component.
   * @default 'medium'
   */
  size: import_prop_types111.default.oneOfType([import_prop_types111.default.oneOf(["medium", "small"]), import_prop_types111.default.string]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types111.default.oneOfType([import_prop_types111.default.arrayOf(import_prop_types111.default.oneOfType([import_prop_types111.default.func, import_prop_types111.default.object, import_prop_types111.default.bool])), import_prop_types111.default.func, import_prop_types111.default.object]),
  /**
   * The variant to use.
   * @default 'outlined'
   */
  variant: import_prop_types111.default.oneOf(["filled", "outlined", "standard"])
} : void 0;
var FormControl_default = FormControl;

// node_modules/@mui/material/esm/FormHelperText/FormHelperText.js
var React168 = __toESM(require_react(), 1);
var import_prop_types112 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/FormHelperText/formHelperTextClasses.js
function getFormHelperTextUtilityClasses(slot) {
  return generateUtilityClass("MuiFormHelperText", slot);
}
var formHelperTextClasses = generateUtilityClasses("MuiFormHelperText", ["root", "error", "disabled", "sizeSmall", "sizeMedium", "contained", "focused", "filled", "required"]);
var formHelperTextClasses_default = formHelperTextClasses;

// node_modules/@mui/material/esm/FormHelperText/FormHelperText.js
var import_jsx_runtime139 = __toESM(require_jsx_runtime(), 1);
var _span2;
var useUtilityClasses70 = (ownerState) => {
  const {
    classes: classes2,
    contained,
    size,
    disabled,
    error,
    filled,
    focused,
    required
  } = ownerState;
  const slots = {
    root: ["root", disabled && "disabled", error && "error", size && `size${capitalize_default(size)}`, contained && "contained", focused && "focused", filled && "filled", required && "required"]
  };
  return composeClasses(slots, getFormHelperTextUtilityClasses, classes2);
};
var FormHelperTextRoot = styled_default2("p", {
  name: "MuiFormHelperText",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.size && styles5[`size${capitalize_default(ownerState.size)}`], ownerState.contained && styles5.contained, ownerState.filled && styles5.filled];
  }
})(memoTheme_default(({
  theme
}) => ({
  color: (theme.vars || theme).palette.text.secondary,
  ...theme.typography.caption,
  textAlign: "left",
  marginTop: 3,
  marginRight: 0,
  marginBottom: 0,
  marginLeft: 0,
  [`&.${formHelperTextClasses_default.disabled}`]: {
    color: (theme.vars || theme).palette.text.disabled
  },
  [`&.${formHelperTextClasses_default.error}`]: {
    color: (theme.vars || theme).palette.error.main
  },
  variants: [{
    props: {
      size: "small"
    },
    style: {
      marginTop: 4
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.contained,
    style: {
      marginLeft: 14,
      marginRight: 14
    }
  }]
})));
var FormHelperText = React168.forwardRef(function FormHelperText2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiFormHelperText"
  });
  const {
    children: children2,
    className,
    component = "p",
    disabled,
    error,
    filled,
    focused,
    margin,
    required,
    variant,
    ...other
  } = props;
  const muiFormControl = useFormControl();
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["variant", "size", "disabled", "error", "filled", "focused", "required"]
  });
  const ownerState = {
    ...props,
    component,
    contained: fcs.variant === "filled" || fcs.variant === "outlined",
    variant: fcs.variant,
    size: fcs.size,
    disabled: fcs.disabled,
    error: fcs.error,
    filled: fcs.filled,
    focused: fcs.focused,
    required: fcs.required
  };
  delete ownerState.ownerState;
  const classes2 = useUtilityClasses70(ownerState);
  return (0, import_jsx_runtime139.jsx)(FormHelperTextRoot, {
    as: component,
    className: clsx_default(classes2.root, className),
    ref,
    ...other,
    ownerState,
    children: children2 === " " ? (
      // notranslate needed while Google Translate will not fix zero-width space issue
      _span2 || (_span2 = (0, import_jsx_runtime139.jsx)("span", {
        className: "notranslate",
        "aria-hidden": true,
        children: "​"
      }))
    ) : children2
  });
});
true ? FormHelperText.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   *
   * If `' '` is provided, the component reserves one line height for displaying a future message.
   */
  children: import_prop_types112.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types112.default.object,
  /**
   * @ignore
   */
  className: import_prop_types112.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types112.default.elementType,
  /**
   * If `true`, the helper text should be displayed in a disabled state.
   */
  disabled: import_prop_types112.default.bool,
  /**
   * If `true`, helper text should be displayed in an error state.
   */
  error: import_prop_types112.default.bool,
  /**
   * If `true`, the helper text should use filled classes key.
   */
  filled: import_prop_types112.default.bool,
  /**
   * If `true`, the helper text should use focused classes key.
   */
  focused: import_prop_types112.default.bool,
  /**
   * If `dense`, will adjust vertical spacing. This is normally obtained via context from
   * FormControl.
   */
  margin: import_prop_types112.default.oneOf(["dense"]),
  /**
   * If `true`, the helper text should use required classes key.
   */
  required: import_prop_types112.default.bool,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types112.default.oneOfType([import_prop_types112.default.arrayOf(import_prop_types112.default.oneOfType([import_prop_types112.default.func, import_prop_types112.default.object, import_prop_types112.default.bool])), import_prop_types112.default.func, import_prop_types112.default.object]),
  /**
   * The variant to use.
   */
  variant: import_prop_types112.default.oneOfType([import_prop_types112.default.oneOf(["filled", "outlined", "standard"]), import_prop_types112.default.string])
} : void 0;
var FormHelperText_default = FormHelperText;

// node_modules/@mui/material/esm/Select/Select.js
var React178 = __toESM(require_react(), 1);
var import_prop_types121 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Select/SelectInput.js
var React177 = __toESM(require_react(), 1);
var import_react_is3 = __toESM(require_react_is(), 1);
var import_prop_types120 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Menu/Menu.js
var React175 = __toESM(require_react(), 1);
var import_react_is2 = __toESM(require_react_is(), 1);
var import_prop_types118 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Popover/Popover.js
var React174 = __toESM(require_react(), 1);
var import_prop_types117 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Grow/Grow.js
var React169 = __toESM(require_react(), 1);
var import_prop_types113 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/transitions/utils.js
var reflow = (node) => node.scrollTop;
function getTransitionProps(props, options3) {
  const {
    timeout: timeout2,
    easing,
    style = {}
  } = props;
  return {
    duration: style.transitionDuration ?? (typeof timeout2 === "number" ? timeout2 : timeout2[options3.mode] || 0),
    easing: style.transitionTimingFunction ?? (typeof easing === "object" ? easing[options3.mode] : easing),
    delay: style.transitionDelay
  };
}

// node_modules/@mui/material/esm/Grow/Grow.js
var import_jsx_runtime140 = __toESM(require_jsx_runtime(), 1);
function getScale(value) {
  return `scale(${value}, ${value ** 2})`;
}
var styles2 = {
  entering: {
    opacity: 1,
    transform: getScale(1)
  },
  entered: {
    opacity: 1,
    transform: "none"
  }
};
var isWebKit154 = typeof navigator !== "undefined" && /^((?!chrome|android).)*(safari|mobile)/i.test(navigator.userAgent) && /(os |version\/)15(.|_)4/i.test(navigator.userAgent);
var Grow = React169.forwardRef(function Grow2(props, ref) {
  const {
    addEndListener,
    appear = true,
    children: children2,
    easing,
    in: inProp,
    onEnter,
    onEntered,
    onEntering,
    onExit,
    onExited,
    onExiting,
    style,
    timeout: timeout2 = "auto",
    // eslint-disable-next-line react/prop-types
    TransitionComponent = Transition_default,
    ...other
  } = props;
  const timer = useTimeout();
  const autoTimeout = React169.useRef();
  const theme = useTheme();
  const nodeRef = React169.useRef(null);
  const handleRef = useForkRef_default(nodeRef, getReactElementRef(children2), ref);
  const normalizedTransitionCallback = (callback) => (maybeIsAppearing) => {
    if (callback) {
      const node = nodeRef.current;
      if (maybeIsAppearing === void 0) {
        callback(node);
      } else {
        callback(node, maybeIsAppearing);
      }
    }
  };
  const handleEntering = normalizedTransitionCallback(onEntering);
  const handleEnter = normalizedTransitionCallback((node, isAppearing) => {
    reflow(node);
    const {
      duration: transitionDuration,
      delay,
      easing: transitionTimingFunction
    } = getTransitionProps({
      style,
      timeout: timeout2,
      easing
    }, {
      mode: "enter"
    });
    let duration;
    if (timeout2 === "auto") {
      duration = theme.transitions.getAutoHeightDuration(node.clientHeight);
      autoTimeout.current = duration;
    } else {
      duration = transitionDuration;
    }
    node.style.transition = [theme.transitions.create("opacity", {
      duration,
      delay
    }), theme.transitions.create("transform", {
      duration: isWebKit154 ? duration : duration * 0.666,
      delay,
      easing: transitionTimingFunction
    })].join(",");
    if (onEnter) {
      onEnter(node, isAppearing);
    }
  });
  const handleEntered = normalizedTransitionCallback(onEntered);
  const handleExiting = normalizedTransitionCallback(onExiting);
  const handleExit = normalizedTransitionCallback((node) => {
    const {
      duration: transitionDuration,
      delay,
      easing: transitionTimingFunction
    } = getTransitionProps({
      style,
      timeout: timeout2,
      easing
    }, {
      mode: "exit"
    });
    let duration;
    if (timeout2 === "auto") {
      duration = theme.transitions.getAutoHeightDuration(node.clientHeight);
      autoTimeout.current = duration;
    } else {
      duration = transitionDuration;
    }
    node.style.transition = [theme.transitions.create("opacity", {
      duration,
      delay
    }), theme.transitions.create("transform", {
      duration: isWebKit154 ? duration : duration * 0.666,
      delay: isWebKit154 ? delay : delay || duration * 0.333,
      easing: transitionTimingFunction
    })].join(",");
    node.style.opacity = 0;
    node.style.transform = getScale(0.75);
    if (onExit) {
      onExit(node);
    }
  });
  const handleExited = normalizedTransitionCallback(onExited);
  const handleAddEndListener = (next) => {
    if (timeout2 === "auto") {
      timer.start(autoTimeout.current || 0, next);
    }
    if (addEndListener) {
      addEndListener(nodeRef.current, next);
    }
  };
  return (0, import_jsx_runtime140.jsx)(TransitionComponent, {
    appear,
    in: inProp,
    nodeRef,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    timeout: timeout2 === "auto" ? null : timeout2,
    ...other,
    children: (state, {
      ownerState,
      ...restChildProps
    }) => {
      return React169.cloneElement(children2, {
        style: {
          opacity: 0,
          transform: getScale(0.75),
          visibility: state === "exited" && !inProp ? "hidden" : void 0,
          ...styles2[state],
          ...style,
          ...children2.props.style
        },
        ref: handleRef,
        ...restChildProps
      });
    }
  });
});
true ? Grow.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Add a custom transition end trigger. Called with the transitioning DOM
   * node and a done callback. Allows for more fine grained transition end
   * logic. Note: Timeouts are still used as a fallback if provided.
   */
  addEndListener: import_prop_types113.default.func,
  /**
   * Perform the enter transition when it first mounts if `in` is also `true`.
   * Set this to `false` to disable this behavior.
   * @default true
   */
  appear: import_prop_types113.default.bool,
  /**
   * A single child content element.
   */
  children: elementAcceptingRef_default.isRequired,
  /**
   * The transition timing function.
   * You may specify a single easing or a object containing enter and exit values.
   */
  easing: import_prop_types113.default.oneOfType([import_prop_types113.default.shape({
    enter: import_prop_types113.default.string,
    exit: import_prop_types113.default.string
  }), import_prop_types113.default.string]),
  /**
   * If `true`, the component will transition in.
   */
  in: import_prop_types113.default.bool,
  /**
   * @ignore
   */
  onEnter: import_prop_types113.default.func,
  /**
   * @ignore
   */
  onEntered: import_prop_types113.default.func,
  /**
   * @ignore
   */
  onEntering: import_prop_types113.default.func,
  /**
   * @ignore
   */
  onExit: import_prop_types113.default.func,
  /**
   * @ignore
   */
  onExited: import_prop_types113.default.func,
  /**
   * @ignore
   */
  onExiting: import_prop_types113.default.func,
  /**
   * @ignore
   */
  style: import_prop_types113.default.object,
  /**
   * The duration for the transition, in milliseconds.
   * You may specify a single timeout for all transitions, or individually with an object.
   *
   * Set to 'auto' to automatically calculate transition time based on height.
   * @default 'auto'
   */
  timeout: import_prop_types113.default.oneOfType([import_prop_types113.default.oneOf(["auto"]), import_prop_types113.default.number, import_prop_types113.default.shape({
    appear: import_prop_types113.default.number,
    enter: import_prop_types113.default.number,
    exit: import_prop_types113.default.number
  })])
} : void 0;
if (Grow) {
  Grow.muiSupportAuto = true;
}
var Grow_default = Grow;

// node_modules/@mui/material/esm/Modal/ModalManager.js
function isOverflowing(container) {
  const doc = ownerDocument(container);
  if (doc.body === container) {
    return ownerWindow(container).innerWidth > doc.documentElement.clientWidth;
  }
  return container.scrollHeight > container.clientHeight;
}
function ariaHidden(element, hide2) {
  if (hide2) {
    element.setAttribute("aria-hidden", "true");
  } else {
    element.removeAttribute("aria-hidden");
  }
}
function getPaddingRight(element) {
  return parseFloat(ownerWindow(element).getComputedStyle(element).paddingRight) || 0;
}
function isAriaHiddenForbiddenOnElement(element) {
  const forbiddenTagNames = ["TEMPLATE", "SCRIPT", "STYLE", "LINK", "MAP", "META", "NOSCRIPT", "PICTURE", "COL", "COLGROUP", "PARAM", "SLOT", "SOURCE", "TRACK"];
  const isForbiddenTagName = forbiddenTagNames.includes(element.tagName);
  const isInputHidden = element.tagName === "INPUT" && element.getAttribute("type") === "hidden";
  return isForbiddenTagName || isInputHidden;
}
function ariaHiddenSiblings(container, mountElement, currentElement, elementsToExclude, hide2) {
  const blacklist = [mountElement, currentElement, ...elementsToExclude];
  [].forEach.call(container.children, (element) => {
    const isNotExcludedElement = !blacklist.includes(element);
    const isNotForbiddenElement = !isAriaHiddenForbiddenOnElement(element);
    if (isNotExcludedElement && isNotForbiddenElement) {
      ariaHidden(element, hide2);
    }
  });
}
function findIndexOf(items, callback) {
  let idx = -1;
  items.some((item, index) => {
    if (callback(item)) {
      idx = index;
      return true;
    }
    return false;
  });
  return idx;
}
function handleContainer(containerInfo, props) {
  const restoreStyle = [];
  const container = containerInfo.container;
  if (!props.disableScrollLock) {
    if (isOverflowing(container)) {
      const scrollbarSize = getScrollbarSize(ownerWindow(container));
      restoreStyle.push({
        value: container.style.paddingRight,
        property: "padding-right",
        el: container
      });
      container.style.paddingRight = `${getPaddingRight(container) + scrollbarSize}px`;
      const fixedElements = ownerDocument(container).querySelectorAll(".mui-fixed");
      [].forEach.call(fixedElements, (element) => {
        restoreStyle.push({
          value: element.style.paddingRight,
          property: "padding-right",
          el: element
        });
        element.style.paddingRight = `${getPaddingRight(element) + scrollbarSize}px`;
      });
    }
    let scrollContainer;
    if (container.parentNode instanceof DocumentFragment) {
      scrollContainer = ownerDocument(container).body;
    } else {
      const parent = container.parentElement;
      const containerWindow = ownerWindow(container);
      scrollContainer = (parent == null ? void 0 : parent.nodeName) === "HTML" && containerWindow.getComputedStyle(parent).overflowY === "scroll" ? parent : container;
    }
    restoreStyle.push({
      value: scrollContainer.style.overflow,
      property: "overflow",
      el: scrollContainer
    }, {
      value: scrollContainer.style.overflowX,
      property: "overflow-x",
      el: scrollContainer
    }, {
      value: scrollContainer.style.overflowY,
      property: "overflow-y",
      el: scrollContainer
    });
    scrollContainer.style.overflow = "hidden";
  }
  const restore = () => {
    restoreStyle.forEach(({
      value,
      el,
      property
    }) => {
      if (value) {
        el.style.setProperty(property, value);
      } else {
        el.style.removeProperty(property);
      }
    });
  };
  return restore;
}
function getHiddenSiblings(container) {
  const hiddenSiblings = [];
  [].forEach.call(container.children, (element) => {
    if (element.getAttribute("aria-hidden") === "true") {
      hiddenSiblings.push(element);
    }
  });
  return hiddenSiblings;
}
var ModalManager = class {
  constructor() {
    this.modals = [];
    this.containers = [];
  }
  add(modal, container) {
    let modalIndex = this.modals.indexOf(modal);
    if (modalIndex !== -1) {
      return modalIndex;
    }
    modalIndex = this.modals.length;
    this.modals.push(modal);
    if (modal.modalRef) {
      ariaHidden(modal.modalRef, false);
    }
    const hiddenSiblings = getHiddenSiblings(container);
    ariaHiddenSiblings(container, modal.mount, modal.modalRef, hiddenSiblings, true);
    const containerIndex = findIndexOf(this.containers, (item) => item.container === container);
    if (containerIndex !== -1) {
      this.containers[containerIndex].modals.push(modal);
      return modalIndex;
    }
    this.containers.push({
      modals: [modal],
      container,
      restore: null,
      hiddenSiblings
    });
    return modalIndex;
  }
  mount(modal, props) {
    const containerIndex = findIndexOf(this.containers, (item) => item.modals.includes(modal));
    const containerInfo = this.containers[containerIndex];
    if (!containerInfo.restore) {
      containerInfo.restore = handleContainer(containerInfo, props);
    }
  }
  remove(modal, ariaHiddenState = true) {
    const modalIndex = this.modals.indexOf(modal);
    if (modalIndex === -1) {
      return modalIndex;
    }
    const containerIndex = findIndexOf(this.containers, (item) => item.modals.includes(modal));
    const containerInfo = this.containers[containerIndex];
    containerInfo.modals.splice(containerInfo.modals.indexOf(modal), 1);
    this.modals.splice(modalIndex, 1);
    if (containerInfo.modals.length === 0) {
      if (containerInfo.restore) {
        containerInfo.restore();
      }
      if (modal.modalRef) {
        ariaHidden(modal.modalRef, ariaHiddenState);
      }
      ariaHiddenSiblings(containerInfo.container, modal.mount, modal.modalRef, containerInfo.hiddenSiblings, false);
      this.containers.splice(containerIndex, 1);
    } else {
      const nextTop = containerInfo.modals[containerInfo.modals.length - 1];
      if (nextTop.modalRef) {
        ariaHidden(nextTop.modalRef, false);
      }
    }
    return modalIndex;
  }
  isTopModal(modal) {
    return this.modals.length > 0 && this.modals[this.modals.length - 1] === modal;
  }
};

// node_modules/@mui/material/esm/Modal/Modal.js
var React173 = __toESM(require_react(), 1);
var import_prop_types116 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Backdrop/Backdrop.js
var React171 = __toESM(require_react(), 1);
var import_prop_types115 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Fade/Fade.js
var React170 = __toESM(require_react(), 1);
var import_prop_types114 = __toESM(require_prop_types(), 1);
var import_jsx_runtime141 = __toESM(require_jsx_runtime(), 1);
var styles3 = {
  entering: {
    opacity: 1
  },
  entered: {
    opacity: 1
  }
};
var Fade = React170.forwardRef(function Fade2(props, ref) {
  const theme = useTheme();
  const defaultTimeout = {
    enter: theme.transitions.duration.enteringScreen,
    exit: theme.transitions.duration.leavingScreen
  };
  const {
    addEndListener,
    appear = true,
    children: children2,
    easing,
    in: inProp,
    onEnter,
    onEntered,
    onEntering,
    onExit,
    onExited,
    onExiting,
    style,
    timeout: timeout2 = defaultTimeout,
    // eslint-disable-next-line react/prop-types
    TransitionComponent = Transition_default,
    ...other
  } = props;
  const enableStrictModeCompat = true;
  const nodeRef = React170.useRef(null);
  const handleRef = useForkRef_default(nodeRef, getReactElementRef(children2), ref);
  const normalizedTransitionCallback = (callback) => (maybeIsAppearing) => {
    if (callback) {
      const node = nodeRef.current;
      if (maybeIsAppearing === void 0) {
        callback(node);
      } else {
        callback(node, maybeIsAppearing);
      }
    }
  };
  const handleEntering = normalizedTransitionCallback(onEntering);
  const handleEnter = normalizedTransitionCallback((node, isAppearing) => {
    reflow(node);
    const transitionProps = getTransitionProps({
      style,
      timeout: timeout2,
      easing
    }, {
      mode: "enter"
    });
    node.style.webkitTransition = theme.transitions.create("opacity", transitionProps);
    node.style.transition = theme.transitions.create("opacity", transitionProps);
    if (onEnter) {
      onEnter(node, isAppearing);
    }
  });
  const handleEntered = normalizedTransitionCallback(onEntered);
  const handleExiting = normalizedTransitionCallback(onExiting);
  const handleExit = normalizedTransitionCallback((node) => {
    const transitionProps = getTransitionProps({
      style,
      timeout: timeout2,
      easing
    }, {
      mode: "exit"
    });
    node.style.webkitTransition = theme.transitions.create("opacity", transitionProps);
    node.style.transition = theme.transitions.create("opacity", transitionProps);
    if (onExit) {
      onExit(node);
    }
  });
  const handleExited = normalizedTransitionCallback(onExited);
  const handleAddEndListener = (next) => {
    if (addEndListener) {
      addEndListener(nodeRef.current, next);
    }
  };
  return (0, import_jsx_runtime141.jsx)(TransitionComponent, {
    appear,
    in: inProp,
    nodeRef: enableStrictModeCompat ? nodeRef : void 0,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    timeout: timeout2,
    ...other,
    children: (state, {
      ownerState,
      ...restChildProps
    }) => {
      return React170.cloneElement(children2, {
        style: {
          opacity: 0,
          visibility: state === "exited" && !inProp ? "hidden" : void 0,
          ...styles3[state],
          ...style,
          ...children2.props.style
        },
        ref: handleRef,
        ...restChildProps
      });
    }
  });
});
true ? Fade.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Add a custom transition end trigger. Called with the transitioning DOM
   * node and a done callback. Allows for more fine grained transition end
   * logic. Note: Timeouts are still used as a fallback if provided.
   */
  addEndListener: import_prop_types114.default.func,
  /**
   * Perform the enter transition when it first mounts if `in` is also `true`.
   * Set this to `false` to disable this behavior.
   * @default true
   */
  appear: import_prop_types114.default.bool,
  /**
   * A single child content element.
   */
  children: elementAcceptingRef_default.isRequired,
  /**
   * The transition timing function.
   * You may specify a single easing or a object containing enter and exit values.
   */
  easing: import_prop_types114.default.oneOfType([import_prop_types114.default.shape({
    enter: import_prop_types114.default.string,
    exit: import_prop_types114.default.string
  }), import_prop_types114.default.string]),
  /**
   * If `true`, the component will transition in.
   */
  in: import_prop_types114.default.bool,
  /**
   * @ignore
   */
  onEnter: import_prop_types114.default.func,
  /**
   * @ignore
   */
  onEntered: import_prop_types114.default.func,
  /**
   * @ignore
   */
  onEntering: import_prop_types114.default.func,
  /**
   * @ignore
   */
  onExit: import_prop_types114.default.func,
  /**
   * @ignore
   */
  onExited: import_prop_types114.default.func,
  /**
   * @ignore
   */
  onExiting: import_prop_types114.default.func,
  /**
   * @ignore
   */
  style: import_prop_types114.default.object,
  /**
   * The duration for the transition, in milliseconds.
   * You may specify a single timeout for all transitions, or individually with an object.
   * @default {
   *   enter: theme.transitions.duration.enteringScreen,
   *   exit: theme.transitions.duration.leavingScreen,
   * }
   */
  timeout: import_prop_types114.default.oneOfType([import_prop_types114.default.number, import_prop_types114.default.shape({
    appear: import_prop_types114.default.number,
    enter: import_prop_types114.default.number,
    exit: import_prop_types114.default.number
  })])
} : void 0;
var Fade_default = Fade;

// node_modules/@mui/material/esm/Backdrop/backdropClasses.js
function getBackdropUtilityClass(slot) {
  return generateUtilityClass("MuiBackdrop", slot);
}
var backdropClasses = generateUtilityClasses("MuiBackdrop", ["root", "invisible"]);

// node_modules/@mui/material/esm/Backdrop/Backdrop.js
var import_jsx_runtime142 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses71 = (ownerState) => {
  const {
    classes: classes2,
    invisible
  } = ownerState;
  const slots = {
    root: ["root", invisible && "invisible"]
  };
  return composeClasses(slots, getBackdropUtilityClass, classes2);
};
var BackdropRoot = styled_default2("div", {
  name: "MuiBackdrop",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.invisible && styles5.invisible];
  }
})({
  position: "fixed",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  right: 0,
  bottom: 0,
  top: 0,
  left: 0,
  backgroundColor: "rgba(0, 0, 0, 0.5)",
  WebkitTapHighlightColor: "transparent",
  variants: [{
    props: {
      invisible: true
    },
    style: {
      backgroundColor: "transparent"
    }
  }]
});
var Backdrop = React171.forwardRef(function Backdrop2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiBackdrop"
  });
  const {
    children: children2,
    className,
    component = "div",
    invisible = false,
    open,
    components = {},
    componentsProps = {},
    slotProps = {},
    slots = {},
    TransitionComponent: TransitionComponentProp,
    transitionDuration,
    ...other
  } = props;
  const ownerState = {
    ...props,
    component,
    invisible
  };
  const classes2 = useUtilityClasses71(ownerState);
  const backwardCompatibleSlots = {
    transition: TransitionComponentProp,
    root: components.Root,
    ...slots
  };
  const backwardCompatibleSlotProps = {
    ...componentsProps,
    ...slotProps
  };
  const externalForwardedProps = {
    component,
    slots: backwardCompatibleSlots,
    slotProps: backwardCompatibleSlotProps
  };
  const [RootSlot, rootProps] = useSlot("root", {
    elementType: BackdropRoot,
    externalForwardedProps,
    className: clsx_default(classes2.root, className),
    ownerState
  });
  const [TransitionSlot, transitionProps] = useSlot("transition", {
    elementType: Fade_default,
    externalForwardedProps,
    ownerState
  });
  return (0, import_jsx_runtime142.jsx)(TransitionSlot, {
    in: open,
    timeout: transitionDuration,
    ...other,
    ...transitionProps,
    children: (0, import_jsx_runtime142.jsx)(RootSlot, {
      "aria-hidden": true,
      ...rootProps,
      ref,
      children: children2
    })
  });
});
true ? Backdrop.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types115.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types115.default.object,
  /**
   * @ignore
   */
  className: import_prop_types115.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types115.default.elementType,
  /**
   * The components used for each slot inside.
   *
   * @deprecated Use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types115.default.shape({
    Root: import_prop_types115.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated Use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types115.default.shape({
    root: import_prop_types115.default.object
  }),
  /**
   * If `true`, the backdrop is invisible.
   * It can be used when rendering a popover or a custom select component.
   * @default false
   */
  invisible: import_prop_types115.default.bool,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types115.default.bool.isRequired,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types115.default.shape({
    root: import_prop_types115.default.oneOfType([import_prop_types115.default.func, import_prop_types115.default.object]),
    transition: import_prop_types115.default.oneOfType([import_prop_types115.default.func, import_prop_types115.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types115.default.shape({
    root: import_prop_types115.default.elementType,
    transition: import_prop_types115.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types115.default.oneOfType([import_prop_types115.default.arrayOf(import_prop_types115.default.oneOfType([import_prop_types115.default.func, import_prop_types115.default.object, import_prop_types115.default.bool])), import_prop_types115.default.func, import_prop_types115.default.object]),
  /**
   * The component used for the transition.
   * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
   * @default Fade
   * @deprecated Use `slots.transition` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  TransitionComponent: import_prop_types115.default.elementType,
  /**
   * The duration for the transition, in milliseconds.
   * You may specify a single timeout for all transitions, or individually with an object.
   */
  transitionDuration: import_prop_types115.default.oneOfType([import_prop_types115.default.number, import_prop_types115.default.shape({
    appear: import_prop_types115.default.number,
    enter: import_prop_types115.default.number,
    exit: import_prop_types115.default.number
  })])
} : void 0;
var Backdrop_default = Backdrop;

// node_modules/@mui/material/esm/Modal/useModal.js
var React172 = __toESM(require_react(), 1);
function getContainer2(container) {
  return typeof container === "function" ? container() : container;
}
function getHasTransition(children2) {
  return children2 ? children2.props.hasOwnProperty("in") : false;
}
var noop4 = () => {
};
var manager = new ModalManager();
function useModal(parameters) {
  const {
    container,
    disableEscapeKeyDown = false,
    disableScrollLock = false,
    closeAfterTransition = false,
    onTransitionEnter,
    onTransitionExited,
    children: children2,
    onClose,
    open,
    rootRef
  } = parameters;
  const modal = React172.useRef({});
  const mountNodeRef = React172.useRef(null);
  const modalRef = React172.useRef(null);
  const handleRef = useForkRef(modalRef, rootRef);
  const [exited, setExited] = React172.useState(!open);
  const hasTransition = getHasTransition(children2);
  let ariaHiddenProp = true;
  if (parameters["aria-hidden"] === "false" || parameters["aria-hidden"] === false) {
    ariaHiddenProp = false;
  }
  const getDoc = () => ownerDocument(mountNodeRef.current);
  const getModal = () => {
    modal.current.modalRef = modalRef.current;
    modal.current.mount = mountNodeRef.current;
    return modal.current;
  };
  const handleMounted = () => {
    manager.mount(getModal(), {
      disableScrollLock
    });
    if (modalRef.current) {
      modalRef.current.scrollTop = 0;
    }
  };
  const handleOpen = useEventCallback_default(() => {
    const resolvedContainer = getContainer2(container) || getDoc().body;
    manager.add(getModal(), resolvedContainer);
    if (modalRef.current) {
      handleMounted();
    }
  });
  const isTopModal = () => manager.isTopModal(getModal());
  const handlePortalRef = useEventCallback_default((node) => {
    mountNodeRef.current = node;
    if (!node) {
      return;
    }
    if (open && isTopModal()) {
      handleMounted();
    } else if (modalRef.current) {
      ariaHidden(modalRef.current, ariaHiddenProp);
    }
  });
  const handleClose = React172.useCallback(() => {
    manager.remove(getModal(), ariaHiddenProp);
  }, [ariaHiddenProp]);
  React172.useEffect(() => {
    return () => {
      handleClose();
    };
  }, [handleClose]);
  React172.useEffect(() => {
    if (open) {
      handleOpen();
    } else if (!hasTransition || !closeAfterTransition) {
      handleClose();
    }
  }, [open, handleClose, hasTransition, closeAfterTransition, handleOpen]);
  const createHandleKeyDown = (otherHandlers) => (event) => {
    var _a;
    (_a = otherHandlers.onKeyDown) == null ? void 0 : _a.call(otherHandlers, event);
    if (event.key !== "Escape" || event.which === 229 || // Wait until IME is settled.
    !isTopModal()) {
      return;
    }
    if (!disableEscapeKeyDown) {
      event.stopPropagation();
      if (onClose) {
        onClose(event, "escapeKeyDown");
      }
    }
  };
  const createHandleBackdropClick = (otherHandlers) => (event) => {
    var _a;
    (_a = otherHandlers.onClick) == null ? void 0 : _a.call(otherHandlers, event);
    if (event.target !== event.currentTarget) {
      return;
    }
    if (onClose) {
      onClose(event, "backdropClick");
    }
  };
  const getRootProps = (otherHandlers = {}) => {
    const propsEventHandlers = extractEventHandlers_default(parameters);
    delete propsEventHandlers.onTransitionEnter;
    delete propsEventHandlers.onTransitionExited;
    const externalEventHandlers = {
      ...propsEventHandlers,
      ...otherHandlers
    };
    return {
      /*
       * Marking an element with the role presentation indicates to assistive technology
       * that this element should be ignored; it exists to support the web application and
       * is not meant for humans to interact with directly.
       * https://github.com/evcohen/eslint-plugin-jsx-a11y/blob/master/docs/rules/no-static-element-interactions.md
       */
      role: "presentation",
      ...externalEventHandlers,
      onKeyDown: createHandleKeyDown(externalEventHandlers),
      ref: handleRef
    };
  };
  const getBackdropProps = (otherHandlers = {}) => {
    const externalEventHandlers = otherHandlers;
    return {
      "aria-hidden": true,
      ...externalEventHandlers,
      onClick: createHandleBackdropClick(externalEventHandlers),
      open
    };
  };
  const getTransitionProps2 = () => {
    const handleEnter = () => {
      setExited(false);
      if (onTransitionEnter) {
        onTransitionEnter();
      }
    };
    const handleExited = () => {
      setExited(true);
      if (onTransitionExited) {
        onTransitionExited();
      }
      if (closeAfterTransition) {
        handleClose();
      }
    };
    return {
      onEnter: createChainedFunction(handleEnter, (children2 == null ? void 0 : children2.props.onEnter) ?? noop4),
      onExited: createChainedFunction(handleExited, (children2 == null ? void 0 : children2.props.onExited) ?? noop4)
    };
  };
  return {
    getRootProps,
    getBackdropProps,
    getTransitionProps: getTransitionProps2,
    rootRef: handleRef,
    portalRef: handlePortalRef,
    isTopModal,
    exited,
    hasTransition
  };
}
var useModal_default = useModal;

// node_modules/@mui/material/esm/Modal/modalClasses.js
function getModalUtilityClass(slot) {
  return generateUtilityClass("MuiModal", slot);
}
var modalClasses = generateUtilityClasses("MuiModal", ["root", "hidden", "backdrop"]);

// node_modules/@mui/material/esm/Modal/Modal.js
var import_jsx_runtime143 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses72 = (ownerState) => {
  const {
    open,
    exited,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", !open && exited && "hidden"],
    backdrop: ["backdrop"]
  };
  return composeClasses(slots, getModalUtilityClass, classes2);
};
var ModalRoot = styled_default2("div", {
  name: "MuiModal",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, !ownerState.open && ownerState.exited && styles5.hidden];
  }
})(memoTheme_default(({
  theme
}) => ({
  position: "fixed",
  zIndex: (theme.vars || theme).zIndex.modal,
  right: 0,
  bottom: 0,
  top: 0,
  left: 0,
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.open && ownerState.exited,
    style: {
      visibility: "hidden"
    }
  }]
})));
var ModalBackdrop = styled_default2(Backdrop_default, {
  name: "MuiModal",
  slot: "Backdrop"
})({
  zIndex: -1
});
var Modal = React173.forwardRef(function Modal2(inProps, ref) {
  const props = useDefaultProps({
    name: "MuiModal",
    props: inProps
  });
  const {
    BackdropComponent = ModalBackdrop,
    BackdropProps,
    classes: classesProp,
    className,
    closeAfterTransition = false,
    children: children2,
    container,
    component,
    components = {},
    componentsProps = {},
    disableAutoFocus = false,
    disableEnforceFocus = false,
    disableEscapeKeyDown = false,
    disablePortal = false,
    disableRestoreFocus = false,
    disableScrollLock = false,
    hideBackdrop = false,
    keepMounted = false,
    onClose,
    onTransitionEnter,
    onTransitionExited,
    open,
    slotProps = {},
    slots = {},
    // eslint-disable-next-line react/prop-types
    theme,
    ...other
  } = props;
  const propsWithDefaults = {
    ...props,
    closeAfterTransition,
    disableAutoFocus,
    disableEnforceFocus,
    disableEscapeKeyDown,
    disablePortal,
    disableRestoreFocus,
    disableScrollLock,
    hideBackdrop,
    keepMounted
  };
  const {
    getRootProps,
    getBackdropProps,
    getTransitionProps: getTransitionProps2,
    portalRef,
    isTopModal,
    exited,
    hasTransition
  } = useModal_default({
    ...propsWithDefaults,
    rootRef: ref
  });
  const ownerState = {
    ...propsWithDefaults,
    exited
  };
  const classes2 = useUtilityClasses72(ownerState);
  const childProps = {};
  if (children2.props.tabIndex === void 0) {
    childProps.tabIndex = "-1";
  }
  if (hasTransition) {
    const {
      onEnter,
      onExited
    } = getTransitionProps2();
    childProps.onEnter = onEnter;
    childProps.onExited = onExited;
  }
  const externalForwardedProps = {
    slots: {
      root: components.Root,
      backdrop: components.Backdrop,
      ...slots
    },
    slotProps: {
      ...componentsProps,
      ...slotProps
    }
  };
  const [RootSlot, rootProps] = useSlot("root", {
    ref,
    elementType: ModalRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other,
      component
    },
    getSlotProps: getRootProps,
    ownerState,
    className: clsx_default(className, classes2 == null ? void 0 : classes2.root, !ownerState.open && ownerState.exited && (classes2 == null ? void 0 : classes2.hidden))
  });
  const [BackdropSlot, backdropProps] = useSlot("backdrop", {
    ref: BackdropProps == null ? void 0 : BackdropProps.ref,
    elementType: BackdropComponent,
    externalForwardedProps,
    shouldForwardComponentProp: true,
    additionalProps: BackdropProps,
    getSlotProps: (otherHandlers) => {
      return getBackdropProps({
        ...otherHandlers,
        onClick: (event) => {
          if (otherHandlers == null ? void 0 : otherHandlers.onClick) {
            otherHandlers.onClick(event);
          }
        }
      });
    },
    className: clsx_default(BackdropProps == null ? void 0 : BackdropProps.className, classes2 == null ? void 0 : classes2.backdrop),
    ownerState
  });
  if (!keepMounted && !open && (!hasTransition || exited)) {
    return null;
  }
  return (0, import_jsx_runtime143.jsx)(Portal_default, {
    ref: portalRef,
    container,
    disablePortal,
    children: (0, import_jsx_runtime143.jsxs)(RootSlot, {
      ...rootProps,
      children: [!hideBackdrop && BackdropComponent ? (0, import_jsx_runtime143.jsx)(BackdropSlot, {
        ...backdropProps
      }) : null, (0, import_jsx_runtime143.jsx)(FocusTrap_default, {
        disableEnforceFocus,
        disableAutoFocus,
        disableRestoreFocus,
        isEnabled: isTopModal,
        open,
        children: React173.cloneElement(children2, childProps)
      })]
    })
  });
});
true ? Modal.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * A backdrop component. This prop enables custom backdrop rendering.
   * @deprecated Use `slots.backdrop` instead. While this prop currently works, it will be removed in the next major version.
   * Use the `slots.backdrop` prop to make your application ready for the next version of Material UI.
   * @default styled(Backdrop, {
   *   name: 'MuiModal',
   *   slot: 'Backdrop',
   * })({
   *   zIndex: -1,
   * })
   */
  BackdropComponent: import_prop_types116.default.elementType,
  /**
   * Props applied to the [`Backdrop`](https://mui.com/material-ui/api/backdrop/) element.
   * @deprecated Use `slotProps.backdrop` instead.
   */
  BackdropProps: import_prop_types116.default.object,
  /**
   * A single child content element.
   */
  children: elementAcceptingRef_default.isRequired,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types116.default.object,
  /**
   * @ignore
   */
  className: import_prop_types116.default.string,
  /**
   * When set to true the Modal waits until a nested Transition is completed before closing.
   * @default false
   */
  closeAfterTransition: import_prop_types116.default.bool,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types116.default.elementType,
  /**
   * The components used for each slot inside.
   *
   * @deprecated Use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types116.default.shape({
    Backdrop: import_prop_types116.default.elementType,
    Root: import_prop_types116.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated Use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types116.default.shape({
    backdrop: import_prop_types116.default.oneOfType([import_prop_types116.default.func, import_prop_types116.default.object]),
    root: import_prop_types116.default.oneOfType([import_prop_types116.default.func, import_prop_types116.default.object])
  }),
  /**
   * An HTML element or function that returns one.
   * The `container` will have the portal children appended to it.
   *
   * You can also provide a callback, which is called in a React layout effect.
   * This lets you set the container from a ref, and also makes server-side rendering possible.
   *
   * By default, it uses the body of the top-level document object,
   * so it's simply `document.body` most of the time.
   */
  container: import_prop_types116.default.oneOfType([HTMLElementType, import_prop_types116.default.func]),
  /**
   * If `true`, the modal will not automatically shift focus to itself when it opens, and
   * replace it to the last focused element when it closes.
   * This also works correctly with any modal children that have the `disableAutoFocus` prop.
   *
   * Generally this should never be set to `true` as it makes the modal less
   * accessible to assistive technologies, like screen readers.
   * @default false
   */
  disableAutoFocus: import_prop_types116.default.bool,
  /**
   * If `true`, the modal will not prevent focus from leaving the modal while open.
   *
   * Generally this should never be set to `true` as it makes the modal less
   * accessible to assistive technologies, like screen readers.
   * @default false
   */
  disableEnforceFocus: import_prop_types116.default.bool,
  /**
   * If `true`, hitting escape will not fire the `onClose` callback.
   * @default false
   */
  disableEscapeKeyDown: import_prop_types116.default.bool,
  /**
   * The `children` will be under the DOM hierarchy of the parent component.
   * @default false
   */
  disablePortal: import_prop_types116.default.bool,
  /**
   * If `true`, the modal will not restore focus to previously focused element once
   * modal is hidden or unmounted.
   * @default false
   */
  disableRestoreFocus: import_prop_types116.default.bool,
  /**
   * Disable the scroll lock behavior.
   * @default false
   */
  disableScrollLock: import_prop_types116.default.bool,
  /**
   * If `true`, the backdrop is not rendered.
   * @default false
   */
  hideBackdrop: import_prop_types116.default.bool,
  /**
   * Always keep the children in the DOM.
   * This prop can be useful in SEO situation or
   * when you want to maximize the responsiveness of the Modal.
   * @default false
   */
  keepMounted: import_prop_types116.default.bool,
  /**
   * Callback fired when the component requests to be closed.
   * The `reason` parameter can optionally be used to control the response to `onClose`.
   *
   * @param {object} event The event source of the callback.
   * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`.
   */
  onClose: import_prop_types116.default.func,
  /**
   * A function called when a transition enters.
   */
  onTransitionEnter: import_prop_types116.default.func,
  /**
   * A function called when a transition has exited.
   */
  onTransitionExited: import_prop_types116.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types116.default.bool.isRequired,
  /**
   * The props used for each slot inside the Modal.
   * @default {}
   */
  slotProps: import_prop_types116.default.shape({
    backdrop: import_prop_types116.default.oneOfType([import_prop_types116.default.func, import_prop_types116.default.object]),
    root: import_prop_types116.default.oneOfType([import_prop_types116.default.func, import_prop_types116.default.object])
  }),
  /**
   * The components used for each slot inside the Modal.
   * Either a string to use a HTML element or a component.
   * @default {}
   */
  slots: import_prop_types116.default.shape({
    backdrop: import_prop_types116.default.elementType,
    root: import_prop_types116.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types116.default.oneOfType([import_prop_types116.default.arrayOf(import_prop_types116.default.oneOfType([import_prop_types116.default.func, import_prop_types116.default.object, import_prop_types116.default.bool])), import_prop_types116.default.func, import_prop_types116.default.object])
} : void 0;
var Modal_default = Modal;

// node_modules/@mui/material/esm/Popover/popoverClasses.js
function getPopoverUtilityClass(slot) {
  return generateUtilityClass("MuiPopover", slot);
}
var popoverClasses = generateUtilityClasses("MuiPopover", ["root", "paper"]);

// node_modules/@mui/material/esm/Popover/Popover.js
var import_jsx_runtime144 = __toESM(require_jsx_runtime(), 1);
function getOffsetTop(rect, vertical) {
  let offset2 = 0;
  if (typeof vertical === "number") {
    offset2 = vertical;
  } else if (vertical === "center") {
    offset2 = rect.height / 2;
  } else if (vertical === "bottom") {
    offset2 = rect.height;
  }
  return offset2;
}
function getOffsetLeft(rect, horizontal) {
  let offset2 = 0;
  if (typeof horizontal === "number") {
    offset2 = horizontal;
  } else if (horizontal === "center") {
    offset2 = rect.width / 2;
  } else if (horizontal === "right") {
    offset2 = rect.width;
  }
  return offset2;
}
function getTransformOriginValue(transformOrigin2) {
  return [transformOrigin2.horizontal, transformOrigin2.vertical].map((n) => typeof n === "number" ? `${n}px` : n).join(" ");
}
function resolveAnchorEl2(anchorEl) {
  return typeof anchorEl === "function" ? anchorEl() : anchorEl;
}
var useUtilityClasses73 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"],
    paper: ["paper"]
  };
  return composeClasses(slots, getPopoverUtilityClass, classes2);
};
var PopoverRoot = styled_default2(Modal_default, {
  name: "MuiPopover",
  slot: "Root"
})({});
var PopoverPaper = styled_default2(Paper_default, {
  name: "MuiPopover",
  slot: "Paper"
})({
  position: "absolute",
  overflowY: "auto",
  overflowX: "hidden",
  // So we see the popover when it's empty.
  // It's most likely on issue on userland.
  minWidth: 16,
  minHeight: 16,
  maxWidth: "calc(100% - 32px)",
  maxHeight: "calc(100% - 32px)",
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0
});
var Popover = React174.forwardRef(function Popover2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiPopover"
  });
  const {
    action,
    anchorEl,
    anchorOrigin = {
      vertical: "top",
      horizontal: "left"
    },
    anchorPosition,
    anchorReference = "anchorEl",
    children: children2,
    className,
    container: containerProp,
    elevation = 8,
    marginThreshold = 16,
    open,
    PaperProps: PaperPropsProp = {},
    // TODO: remove in v7
    slots = {},
    slotProps = {},
    transformOrigin: transformOrigin2 = {
      vertical: "top",
      horizontal: "left"
    },
    TransitionComponent,
    // TODO: remove in v7
    transitionDuration: transitionDurationProp = "auto",
    TransitionProps = {},
    // TODO: remove in v7
    disableScrollLock = false,
    ...other
  } = props;
  const paperRef = React174.useRef();
  const ownerState = {
    ...props,
    anchorOrigin,
    anchorReference,
    elevation,
    marginThreshold,
    transformOrigin: transformOrigin2,
    TransitionComponent,
    transitionDuration: transitionDurationProp,
    TransitionProps
  };
  const classes2 = useUtilityClasses73(ownerState);
  const getAnchorOffset = React174.useCallback(() => {
    if (anchorReference === "anchorPosition") {
      if (true) {
        if (!anchorPosition) {
          console.error('MUI: You need to provide a `anchorPosition` prop when using <Popover anchorReference="anchorPosition" />.');
        }
      }
      return anchorPosition;
    }
    const resolvedAnchorEl = resolveAnchorEl2(anchorEl);
    const anchorElement = resolvedAnchorEl && resolvedAnchorEl.nodeType === 1 ? resolvedAnchorEl : ownerDocument_default(paperRef.current).body;
    const anchorRect = anchorElement.getBoundingClientRect();
    if (true) {
      const box = anchorElement.getBoundingClientRect();
      if (isLayoutSupported() && box.top === 0 && box.left === 0 && box.right === 0 && box.bottom === 0) {
        console.warn(["MUI: The `anchorEl` prop provided to the component is invalid.", "The anchor element should be part of the document layout.", "Make sure the element is present in the document or that it's not display none."].join("\n"));
      }
    }
    return {
      top: anchorRect.top + getOffsetTop(anchorRect, anchorOrigin.vertical),
      left: anchorRect.left + getOffsetLeft(anchorRect, anchorOrigin.horizontal)
    };
  }, [anchorEl, anchorOrigin.horizontal, anchorOrigin.vertical, anchorPosition, anchorReference]);
  const getTransformOrigin = React174.useCallback((elemRect) => {
    return {
      vertical: getOffsetTop(elemRect, transformOrigin2.vertical),
      horizontal: getOffsetLeft(elemRect, transformOrigin2.horizontal)
    };
  }, [transformOrigin2.horizontal, transformOrigin2.vertical]);
  const getPositioningStyle = React174.useCallback((element) => {
    const elemRect = {
      width: element.offsetWidth,
      height: element.offsetHeight
    };
    const elemTransformOrigin = getTransformOrigin(elemRect);
    if (anchorReference === "none") {
      return {
        top: null,
        left: null,
        transformOrigin: getTransformOriginValue(elemTransformOrigin)
      };
    }
    const anchorOffset = getAnchorOffset();
    let top2 = anchorOffset.top - elemTransformOrigin.vertical;
    let left2 = anchorOffset.left - elemTransformOrigin.horizontal;
    const bottom2 = top2 + elemRect.height;
    const right2 = left2 + elemRect.width;
    const containerWindow = ownerWindow_default(resolveAnchorEl2(anchorEl));
    const heightThreshold = containerWindow.innerHeight - marginThreshold;
    const widthThreshold = containerWindow.innerWidth - marginThreshold;
    if (marginThreshold !== null && top2 < marginThreshold) {
      const diff = top2 - marginThreshold;
      top2 -= diff;
      elemTransformOrigin.vertical += diff;
    } else if (marginThreshold !== null && bottom2 > heightThreshold) {
      const diff = bottom2 - heightThreshold;
      top2 -= diff;
      elemTransformOrigin.vertical += diff;
    }
    if (true) {
      if (elemRect.height > heightThreshold && elemRect.height && heightThreshold) {
        console.error(["MUI: The popover component is too tall.", `Some part of it can not be seen on the screen (${elemRect.height - heightThreshold}px).`, "Please consider adding a `max-height` to improve the user-experience."].join("\n"));
      }
    }
    if (marginThreshold !== null && left2 < marginThreshold) {
      const diff = left2 - marginThreshold;
      left2 -= diff;
      elemTransformOrigin.horizontal += diff;
    } else if (right2 > widthThreshold) {
      const diff = right2 - widthThreshold;
      left2 -= diff;
      elemTransformOrigin.horizontal += diff;
    }
    return {
      top: `${Math.round(top2)}px`,
      left: `${Math.round(left2)}px`,
      transformOrigin: getTransformOriginValue(elemTransformOrigin)
    };
  }, [anchorEl, anchorReference, getAnchorOffset, getTransformOrigin, marginThreshold]);
  const [isPositioned, setIsPositioned] = React174.useState(open);
  const setPositioningStyles = React174.useCallback(() => {
    const element = paperRef.current;
    if (!element) {
      return;
    }
    const positioning = getPositioningStyle(element);
    if (positioning.top !== null) {
      element.style.setProperty("top", positioning.top);
    }
    if (positioning.left !== null) {
      element.style.left = positioning.left;
    }
    element.style.transformOrigin = positioning.transformOrigin;
    setIsPositioned(true);
  }, [getPositioningStyle]);
  React174.useEffect(() => {
    if (disableScrollLock) {
      window.addEventListener("scroll", setPositioningStyles);
    }
    return () => window.removeEventListener("scroll", setPositioningStyles);
  }, [anchorEl, disableScrollLock, setPositioningStyles]);
  const handleEntering = () => {
    setPositioningStyles();
  };
  const handleExited = () => {
    setIsPositioned(false);
  };
  React174.useEffect(() => {
    if (open) {
      setPositioningStyles();
    }
  });
  React174.useImperativeHandle(action, () => open ? {
    updatePosition: () => {
      setPositioningStyles();
    }
  } : null, [open, setPositioningStyles]);
  React174.useEffect(() => {
    if (!open) {
      return void 0;
    }
    const handleResize = debounce_default(() => {
      setPositioningStyles();
    });
    const containerWindow = ownerWindow_default(resolveAnchorEl2(anchorEl));
    containerWindow.addEventListener("resize", handleResize);
    return () => {
      handleResize.clear();
      containerWindow.removeEventListener("resize", handleResize);
    };
  }, [anchorEl, open, setPositioningStyles]);
  let transitionDuration = transitionDurationProp;
  const externalForwardedProps = {
    slots: {
      transition: TransitionComponent,
      ...slots
    },
    slotProps: {
      transition: TransitionProps,
      paper: PaperPropsProp,
      ...slotProps
    }
  };
  const [TransitionSlot, transitionSlotProps] = useSlot("transition", {
    elementType: Grow_default,
    externalForwardedProps,
    ownerState,
    getSlotProps: (handlers) => ({
      ...handlers,
      onEntering: (element, isAppearing) => {
        var _a;
        (_a = handlers.onEntering) == null ? void 0 : _a.call(handlers, element, isAppearing);
        handleEntering();
      },
      onExited: (element) => {
        var _a;
        (_a = handlers.onExited) == null ? void 0 : _a.call(handlers, element);
        handleExited();
      }
    }),
    additionalProps: {
      appear: true,
      in: open
    }
  });
  if (transitionDurationProp === "auto" && !TransitionSlot.muiSupportAuto) {
    transitionDuration = void 0;
  }
  const container = containerProp || (anchorEl ? ownerDocument_default(resolveAnchorEl2(anchorEl)).body : void 0);
  const [RootSlot, {
    slots: rootSlotsProp,
    slotProps: rootSlotPropsProp,
    ...rootProps
  }] = useSlot("root", {
    ref,
    elementType: PopoverRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other
    },
    shouldForwardComponentProp: true,
    additionalProps: {
      slots: {
        backdrop: slots.backdrop
      },
      slotProps: {
        backdrop: mergeSlotProps(typeof slotProps.backdrop === "function" ? slotProps.backdrop(ownerState) : slotProps.backdrop, {
          invisible: true
        })
      },
      container,
      open
    },
    ownerState,
    className: clsx_default(classes2.root, className)
  });
  const [PaperSlot, paperProps] = useSlot("paper", {
    ref: paperRef,
    className: classes2.paper,
    elementType: PopoverPaper,
    externalForwardedProps,
    shouldForwardComponentProp: true,
    additionalProps: {
      elevation,
      style: isPositioned ? void 0 : {
        opacity: 0
      }
    },
    ownerState
  });
  return (0, import_jsx_runtime144.jsx)(RootSlot, {
    ...rootProps,
    ...!isHostComponent_default(RootSlot) && {
      slots: rootSlotsProp,
      slotProps: rootSlotPropsProp,
      disableScrollLock
    },
    children: (0, import_jsx_runtime144.jsx)(TransitionSlot, {
      ...transitionSlotProps,
      timeout: transitionDuration,
      children: (0, import_jsx_runtime144.jsx)(PaperSlot, {
        ...paperProps,
        children: children2
      })
    })
  });
});
true ? Popover.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * A ref for imperative actions.
   * It currently only supports updatePosition() action.
   */
  action: refType_default,
  /**
   * An HTML element, [PopoverVirtualElement](https://mui.com/material-ui/react-popover/#virtual-element),
   * or a function that returns either.
   * It's used to set the position of the popover.
   */
  anchorEl: chainPropTypes(import_prop_types117.default.oneOfType([HTMLElementType, import_prop_types117.default.func]), (props) => {
    if (props.open && (!props.anchorReference || props.anchorReference === "anchorEl")) {
      const resolvedAnchorEl = resolveAnchorEl2(props.anchorEl);
      if (resolvedAnchorEl && resolvedAnchorEl.nodeType === 1) {
        const box = resolvedAnchorEl.getBoundingClientRect();
        if (true) {
          if (isLayoutSupported() && box.top === 0 && box.left === 0 && box.right === 0 && box.bottom === 0) {
            return new Error(["MUI: The `anchorEl` prop provided to the component is invalid.", "The anchor element should be part of the document layout.", "Make sure the element is present in the document or that it's not display none."].join("\n"));
          }
        }
      } else {
        return new Error(["MUI: The `anchorEl` prop provided to the component is invalid.", `It should be an Element or PopoverVirtualElement instance but it's \`${resolvedAnchorEl}\` instead.`].join("\n"));
      }
    }
    return null;
  }),
  /**
   * This is the point on the anchor where the popover's
   * `anchorEl` will attach to. This is not used when the
   * anchorReference is 'anchorPosition'.
   *
   * Options:
   * vertical: [top, center, bottom];
   * horizontal: [left, center, right].
   * @default {
   *   vertical: 'top',
   *   horizontal: 'left',
   * }
   */
  anchorOrigin: import_prop_types117.default.shape({
    horizontal: import_prop_types117.default.oneOfType([import_prop_types117.default.oneOf(["center", "left", "right"]), import_prop_types117.default.number]).isRequired,
    vertical: import_prop_types117.default.oneOfType([import_prop_types117.default.oneOf(["bottom", "center", "top"]), import_prop_types117.default.number]).isRequired
  }),
  /**
   * This is the position that may be used to set the position of the popover.
   * The coordinates are relative to the application's client area.
   */
  anchorPosition: import_prop_types117.default.shape({
    left: import_prop_types117.default.number.isRequired,
    top: import_prop_types117.default.number.isRequired
  }),
  /**
   * This determines which anchor prop to refer to when setting
   * the position of the popover.
   * @default 'anchorEl'
   */
  anchorReference: import_prop_types117.default.oneOf(["anchorEl", "anchorPosition", "none"]),
  /**
   * A backdrop component. This prop enables custom backdrop rendering.
   * @deprecated Use `slots.backdrop` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default styled(Backdrop, {
   *   name: 'MuiModal',
   *   slot: 'Backdrop',
   *   overridesResolver: (props, styles) => {
   *     return styles.backdrop;
   *   },
   * })({
   *   zIndex: -1,
   * })
   */
  BackdropComponent: import_prop_types117.default.elementType,
  /**
   * Props applied to the [`Backdrop`](/material-ui/api/backdrop/) element.
   * @deprecated Use `slotProps.backdrop` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  BackdropProps: import_prop_types117.default.object,
  /**
   * The content of the component.
   */
  children: import_prop_types117.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types117.default.object,
  /**
   * @ignore
   */
  className: import_prop_types117.default.string,
  /**
   * An HTML element, component instance, or function that returns either.
   * The `container` will passed to the Modal component.
   *
   * By default, it uses the body of the anchorEl's top-level document object,
   * so it's simply `document.body` most of the time.
   */
  container: import_prop_types117.default.oneOfType([HTMLElementType, import_prop_types117.default.func]),
  /**
   * Disable the scroll lock behavior.
   * @default false
   */
  disableScrollLock: import_prop_types117.default.bool,
  /**
   * The elevation of the popover.
   * @default 8
   */
  elevation: integerPropType_default,
  /**
   * Specifies how close to the edge of the window the popover can appear.
   * If null, the popover will not be constrained by the window.
   * @default 16
   */
  marginThreshold: import_prop_types117.default.number,
  /**
   * Callback fired when the component requests to be closed.
   * The `reason` parameter can optionally be used to control the response to `onClose`.
   */
  onClose: import_prop_types117.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types117.default.bool.isRequired,
  /**
   * Props applied to the [`Paper`](https://mui.com/material-ui/api/paper/) element.
   *
   * This prop is an alias for `slotProps.paper` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.paper` instead.
   *
   * @default {}
   */
  PaperProps: import_prop_types117.default.shape({
    component: elementTypeAcceptingRef_default
  }),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types117.default.shape({
    backdrop: import_prop_types117.default.oneOfType([import_prop_types117.default.func, import_prop_types117.default.object]),
    paper: import_prop_types117.default.oneOfType([import_prop_types117.default.func, import_prop_types117.default.object]),
    root: import_prop_types117.default.oneOfType([import_prop_types117.default.func, import_prop_types117.default.object]),
    transition: import_prop_types117.default.oneOfType([import_prop_types117.default.func, import_prop_types117.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types117.default.shape({
    backdrop: import_prop_types117.default.elementType,
    paper: import_prop_types117.default.elementType,
    root: import_prop_types117.default.elementType,
    transition: import_prop_types117.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types117.default.oneOfType([import_prop_types117.default.arrayOf(import_prop_types117.default.oneOfType([import_prop_types117.default.func, import_prop_types117.default.object, import_prop_types117.default.bool])), import_prop_types117.default.func, import_prop_types117.default.object]),
  /**
   * This is the point on the popover which
   * will attach to the anchor's origin.
   *
   * Options:
   * vertical: [top, center, bottom, x(px)];
   * horizontal: [left, center, right, x(px)].
   * @default {
   *   vertical: 'top',
   *   horizontal: 'left',
   * }
   */
  transformOrigin: import_prop_types117.default.shape({
    horizontal: import_prop_types117.default.oneOfType([import_prop_types117.default.oneOf(["center", "left", "right"]), import_prop_types117.default.number]).isRequired,
    vertical: import_prop_types117.default.oneOfType([import_prop_types117.default.oneOf(["bottom", "center", "top"]), import_prop_types117.default.number]).isRequired
  }),
  /**
   * The component used for the transition.
   * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
   * @deprecated use the `slots.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default Grow
   */
  TransitionComponent: import_prop_types117.default.elementType,
  /**
   * Set to 'auto' to automatically calculate transition time based on height.
   * @default 'auto'
   */
  transitionDuration: import_prop_types117.default.oneOfType([import_prop_types117.default.oneOf(["auto"]), import_prop_types117.default.number, import_prop_types117.default.shape({
    appear: import_prop_types117.default.number,
    enter: import_prop_types117.default.number,
    exit: import_prop_types117.default.number
  })]),
  /**
   * Props applied to the transition element.
   * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
   * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  TransitionProps: import_prop_types117.default.object
} : void 0;
var Popover_default = Popover;

// node_modules/@mui/material/esm/Menu/menuClasses.js
function getMenuUtilityClass(slot) {
  return generateUtilityClass("MuiMenu", slot);
}
var menuClasses = generateUtilityClasses("MuiMenu", ["root", "paper", "list"]);

// node_modules/@mui/material/esm/Menu/Menu.js
var import_jsx_runtime145 = __toESM(require_jsx_runtime(), 1);
var RTL_ORIGIN = {
  vertical: "top",
  horizontal: "right"
};
var LTR_ORIGIN = {
  vertical: "top",
  horizontal: "left"
};
var useUtilityClasses74 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"],
    paper: ["paper"],
    list: ["list"]
  };
  return composeClasses(slots, getMenuUtilityClass, classes2);
};
var MenuRoot = styled_default2(Popover_default, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiMenu",
  slot: "Root"
})({});
var MenuPaper = styled_default2(PopoverPaper, {
  name: "MuiMenu",
  slot: "Paper"
})({
  // specZ: The maximum height of a simple menu should be one or more rows less than the view
  // height. This ensures a tappable area outside of the simple menu with which to dismiss
  // the menu.
  maxHeight: "calc(100% - 96px)",
  // Add iOS momentum scrolling for iOS < 13.0
  WebkitOverflowScrolling: "touch"
});
var MenuMenuList = styled_default2(MenuList_default, {
  name: "MuiMenu",
  slot: "List"
})({
  // We disable the focus ring for mouse, touch and keyboard users.
  outline: 0
});
var Menu = React175.forwardRef(function Menu2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiMenu"
  });
  const {
    autoFocus = true,
    children: children2,
    className,
    disableAutoFocusItem = false,
    MenuListProps = {},
    onClose,
    open,
    PaperProps = {},
    PopoverClasses,
    transitionDuration = "auto",
    TransitionProps: {
      onEntering,
      ...TransitionProps
    } = {},
    variant = "selectedMenu",
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const isRtl = useRtl();
  const ownerState = {
    ...props,
    autoFocus,
    disableAutoFocusItem,
    MenuListProps,
    onEntering,
    PaperProps,
    transitionDuration,
    TransitionProps,
    variant
  };
  const classes2 = useUtilityClasses74(ownerState);
  const autoFocusItem = autoFocus && !disableAutoFocusItem && open;
  const menuListActionsRef = React175.useRef(null);
  const handleEntering = (element, isAppearing) => {
    if (menuListActionsRef.current) {
      menuListActionsRef.current.adjustStyleForScrollbar(element, {
        direction: isRtl ? "rtl" : "ltr"
      });
    }
    if (onEntering) {
      onEntering(element, isAppearing);
    }
  };
  const handleListKeyDown = (event) => {
    if (event.key === "Tab") {
      event.preventDefault();
      if (onClose) {
        onClose(event, "tabKeyDown");
      }
    }
  };
  let activeItemIndex = -1;
  React175.Children.map(children2, (child, index) => {
    if (!React175.isValidElement(child)) {
      return;
    }
    if (true) {
      if ((0, import_react_is2.isFragment)(child)) {
        console.error(["MUI: The Menu component doesn't accept a Fragment as a child.", "Consider providing an array instead."].join("\n"));
      }
    }
    if (!child.props.disabled) {
      if (variant === "selectedMenu" && child.props.selected) {
        activeItemIndex = index;
      } else if (activeItemIndex === -1) {
        activeItemIndex = index;
      }
    }
  });
  const externalForwardedProps = {
    slots,
    slotProps: {
      list: MenuListProps,
      transition: TransitionProps,
      paper: PaperProps,
      ...slotProps
    }
  };
  const rootSlotProps = useSlotProps_default({
    elementType: slots.root,
    externalSlotProps: slotProps.root,
    ownerState,
    className: [classes2.root, className]
  });
  const [PaperSlot, paperSlotProps] = useSlot("paper", {
    className: classes2.paper,
    elementType: MenuPaper,
    externalForwardedProps,
    shouldForwardComponentProp: true,
    ownerState
  });
  const [ListSlot, listSlotProps] = useSlot("list", {
    className: clsx_default(classes2.list, MenuListProps.className),
    elementType: MenuMenuList,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    getSlotProps: (handlers) => ({
      ...handlers,
      onKeyDown: (event) => {
        var _a;
        handleListKeyDown(event);
        (_a = handlers.onKeyDown) == null ? void 0 : _a.call(handlers, event);
      }
    }),
    ownerState
  });
  const resolvedTransitionProps = typeof externalForwardedProps.slotProps.transition === "function" ? externalForwardedProps.slotProps.transition(ownerState) : externalForwardedProps.slotProps.transition;
  return (0, import_jsx_runtime145.jsx)(MenuRoot, {
    onClose,
    anchorOrigin: {
      vertical: "bottom",
      horizontal: isRtl ? "right" : "left"
    },
    transformOrigin: isRtl ? RTL_ORIGIN : LTR_ORIGIN,
    slots: {
      root: slots.root,
      paper: PaperSlot,
      backdrop: slots.backdrop,
      ...slots.transition && {
        // TODO: pass `slots.transition` directly once `TransitionComponent` is removed from Popover
        transition: slots.transition
      }
    },
    slotProps: {
      root: rootSlotProps,
      paper: paperSlotProps,
      backdrop: typeof slotProps.backdrop === "function" ? slotProps.backdrop(ownerState) : slotProps.backdrop,
      transition: {
        ...resolvedTransitionProps,
        onEntering: (...args) => {
          var _a;
          handleEntering(...args);
          (_a = resolvedTransitionProps == null ? void 0 : resolvedTransitionProps.onEntering) == null ? void 0 : _a.call(resolvedTransitionProps, ...args);
        }
      }
    },
    open,
    ref,
    transitionDuration,
    ownerState,
    ...other,
    classes: PopoverClasses,
    children: (0, import_jsx_runtime145.jsx)(ListSlot, {
      actions: menuListActionsRef,
      autoFocus: autoFocus && (activeItemIndex === -1 || disableAutoFocusItem),
      autoFocusItem,
      variant,
      ...listSlotProps,
      children: children2
    })
  });
});
true ? Menu.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * An HTML element, or a function that returns one.
   * It's used to set the position of the menu.
   */
  anchorEl: import_prop_types118.default.oneOfType([HTMLElementType, import_prop_types118.default.func]),
  /**
   * If `true` (Default) will focus the `[role="menu"]` if no focusable child is found. Disabled
   * children are not focusable. If you set this prop to `false` focus will be placed
   * on the parent modal container. This has severe accessibility implications
   * and should only be considered if you manage focus otherwise.
   * @default true
   */
  autoFocus: import_prop_types118.default.bool,
  /**
   * Menu contents, normally `MenuItem`s.
   */
  children: import_prop_types118.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types118.default.object,
  /**
   * @ignore
   */
  className: import_prop_types118.default.string,
  /**
   * When opening the menu will not focus the active item but the `[role="menu"]`
   * unless `autoFocus` is also set to `false`. Not using the default means not
   * following WAI-ARIA authoring practices. Please be considerate about possible
   * accessibility implications.
   * @default false
   */
  disableAutoFocusItem: import_prop_types118.default.bool,
  /**
   * Props applied to the [`MenuList`](https://mui.com/material-ui/api/menu-list/) element.
   * @deprecated use the `slotProps.list` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  MenuListProps: import_prop_types118.default.object,
  /**
   * Callback fired when the component requests to be closed.
   *
   * @param {object} event The event source of the callback.
   * @param {string} reason Can be: `"escapeKeyDown"`, `"backdropClick"`, `"tabKeyDown"`.
   */
  onClose: import_prop_types118.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types118.default.bool.isRequired,
  /**
   * @ignore
   */
  PaperProps: import_prop_types118.default.object,
  /**
   * `classes` prop applied to the [`Popover`](https://mui.com/material-ui/api/popover/) element.
   */
  PopoverClasses: import_prop_types118.default.object,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types118.default.shape({
    backdrop: import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object]),
    list: import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object]),
    paper: import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object]),
    root: import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object]),
    transition: import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types118.default.shape({
    backdrop: import_prop_types118.default.elementType,
    list: import_prop_types118.default.elementType,
    paper: import_prop_types118.default.elementType,
    root: import_prop_types118.default.elementType,
    transition: import_prop_types118.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types118.default.oneOfType([import_prop_types118.default.arrayOf(import_prop_types118.default.oneOfType([import_prop_types118.default.func, import_prop_types118.default.object, import_prop_types118.default.bool])), import_prop_types118.default.func, import_prop_types118.default.object]),
  /**
   * The length of the transition in `ms`, or 'auto'
   * @default 'auto'
   */
  transitionDuration: import_prop_types118.default.oneOfType([import_prop_types118.default.oneOf(["auto"]), import_prop_types118.default.number, import_prop_types118.default.shape({
    appear: import_prop_types118.default.number,
    enter: import_prop_types118.default.number,
    exit: import_prop_types118.default.number
  })]),
  /**
   * Props applied to the transition element.
   * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
   * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  TransitionProps: import_prop_types118.default.object,
  /**
   * The variant to use. Use `menu` to prevent selected items from impacting the initial focus.
   * @default 'selectedMenu'
   */
  variant: import_prop_types118.default.oneOf(["menu", "selectedMenu"])
} : void 0;
var Menu_default = Menu;

// node_modules/@mui/material/esm/NativeSelect/NativeSelectInput.js
var React176 = __toESM(require_react(), 1);
var import_prop_types119 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/NativeSelect/nativeSelectClasses.js
function getNativeSelectUtilityClasses(slot) {
  return generateUtilityClass("MuiNativeSelect", slot);
}
var nativeSelectClasses = generateUtilityClasses("MuiNativeSelect", ["root", "select", "multiple", "filled", "outlined", "standard", "disabled", "icon", "iconOpen", "iconFilled", "iconOutlined", "iconStandard", "nativeInput", "error"]);
var nativeSelectClasses_default = nativeSelectClasses;

// node_modules/@mui/material/esm/NativeSelect/NativeSelectInput.js
var import_jsx_runtime146 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses75 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    disabled,
    multiple,
    open,
    error
  } = ownerState;
  const slots = {
    select: ["select", variant, disabled && "disabled", multiple && "multiple", error && "error"],
    icon: ["icon", `icon${capitalize_default(variant)}`, open && "iconOpen", disabled && "disabled"]
  };
  return composeClasses(slots, getNativeSelectUtilityClasses, classes2);
};
var StyledSelectSelect = styled_default2("select", {
  name: "MuiNativeSelect"
})(({
  theme
}) => ({
  // Reset
  MozAppearance: "none",
  // Reset
  WebkitAppearance: "none",
  // When interacting quickly, the text can end up selected.
  // Native select can't be selected either.
  userSelect: "none",
  // Reset
  borderRadius: 0,
  cursor: "pointer",
  "&:focus": {
    // Reset Chrome style
    borderRadius: 0
  },
  [`&.${nativeSelectClasses_default.disabled}`]: {
    cursor: "default"
  },
  "&[multiple]": {
    height: "auto"
  },
  "&:not([multiple]) option, &:not([multiple]) optgroup": {
    backgroundColor: (theme.vars || theme).palette.background.paper
  },
  variants: [{
    props: ({
      ownerState
    }) => ownerState.variant !== "filled" && ownerState.variant !== "outlined",
    style: {
      // Bump specificity to allow extending custom inputs
      "&&&": {
        paddingRight: 24,
        minWidth: 16
        // So it doesn't collapse.
      }
    }
  }, {
    props: {
      variant: "filled"
    },
    style: {
      "&&&": {
        paddingRight: 32
      }
    }
  }, {
    props: {
      variant: "outlined"
    },
    style: {
      borderRadius: (theme.vars || theme).shape.borderRadius,
      "&:focus": {
        borderRadius: (theme.vars || theme).shape.borderRadius
        // Reset the reset for Chrome style
      },
      "&&&": {
        paddingRight: 32
      }
    }
  }]
}));
var NativeSelectSelect = styled_default2(StyledSelectSelect, {
  name: "MuiNativeSelect",
  slot: "Select",
  shouldForwardProp: rootShouldForwardProp_default,
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.select, styles5[ownerState.variant], ownerState.error && styles5.error, {
      [`&.${nativeSelectClasses_default.multiple}`]: styles5.multiple
    }];
  }
})({});
var StyledSelectIcon = styled_default2("svg", {
  name: "MuiNativeSelect"
})(({
  theme
}) => ({
  // We use a position absolute over a flexbox in order to forward the pointer events
  // to the input and to support wrapping tags..
  position: "absolute",
  right: 0,
  // Center vertically, height is 1em
  top: "calc(50% - .5em)",
  // Don't block pointer events on the select under the icon.
  pointerEvents: "none",
  color: (theme.vars || theme).palette.action.active,
  [`&.${nativeSelectClasses_default.disabled}`]: {
    color: (theme.vars || theme).palette.action.disabled
  },
  variants: [{
    props: ({
      ownerState
    }) => ownerState.open,
    style: {
      transform: "rotate(180deg)"
    }
  }, {
    props: {
      variant: "filled"
    },
    style: {
      right: 7
    }
  }, {
    props: {
      variant: "outlined"
    },
    style: {
      right: 7
    }
  }]
}));
var NativeSelectIcon = styled_default2(StyledSelectIcon, {
  name: "MuiNativeSelect",
  slot: "Icon",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.icon, ownerState.variant && styles5[`icon${capitalize_default(ownerState.variant)}`], ownerState.open && styles5.iconOpen];
  }
})({});
var NativeSelectInput = React176.forwardRef(function NativeSelectInput2(props, ref) {
  const {
    className,
    disabled,
    error,
    IconComponent,
    inputRef,
    variant = "standard",
    ...other
  } = props;
  const ownerState = {
    ...props,
    disabled,
    variant,
    error
  };
  const classes2 = useUtilityClasses75(ownerState);
  return (0, import_jsx_runtime146.jsxs)(React176.Fragment, {
    children: [(0, import_jsx_runtime146.jsx)(NativeSelectSelect, {
      ownerState,
      className: clsx_default(classes2.select, className),
      disabled,
      ref: inputRef || ref,
      ...other
    }), props.multiple ? null : (0, import_jsx_runtime146.jsx)(NativeSelectIcon, {
      as: IconComponent,
      ownerState,
      className: classes2.icon
    })]
  });
});
true ? NativeSelectInput.propTypes = {
  /**
   * The option elements to populate the select with.
   * Can be some `<option>` elements.
   */
  children: import_prop_types119.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types119.default.object,
  /**
   * The CSS class name of the select element.
   */
  className: import_prop_types119.default.string,
  /**
   * If `true`, the select is disabled.
   */
  disabled: import_prop_types119.default.bool,
  /**
   * If `true`, the `select input` will indicate an error.
   */
  error: import_prop_types119.default.bool,
  /**
   * The icon that displays the arrow.
   */
  IconComponent: import_prop_types119.default.elementType.isRequired,
  /**
   * Use that prop to pass a ref to the native select element.
   * @deprecated
   */
  inputRef: refType_default,
  /**
   * @ignore
   */
  multiple: import_prop_types119.default.bool,
  /**
   * Name attribute of the `select` or hidden `input` element.
   */
  name: import_prop_types119.default.string,
  /**
   * Callback fired when a menu item is selected.
   *
   * @param {object} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types119.default.func,
  /**
   * The input value.
   */
  value: import_prop_types119.default.any,
  /**
   * The variant to use.
   */
  variant: import_prop_types119.default.oneOf(["standard", "outlined", "filled"])
} : void 0;
var NativeSelectInput_default = NativeSelectInput;

// node_modules/@mui/material/esm/Select/selectClasses.js
function getSelectUtilityClasses(slot) {
  return generateUtilityClass("MuiSelect", slot);
}
var selectClasses = generateUtilityClasses("MuiSelect", ["root", "select", "multiple", "filled", "outlined", "standard", "disabled", "focused", "icon", "iconOpen", "iconFilled", "iconOutlined", "iconStandard", "nativeInput", "error"]);
var selectClasses_default = selectClasses;

// node_modules/@mui/material/esm/Select/SelectInput.js
var import_jsx_runtime147 = __toESM(require_jsx_runtime(), 1);
var _span3;
var SelectSelect = styled_default2(StyledSelectSelect, {
  name: "MuiSelect",
  slot: "Select",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [
      // Win specificity over the input base
      {
        [`&.${selectClasses_default.select}`]: styles5.select
      },
      {
        [`&.${selectClasses_default.select}`]: styles5[ownerState.variant]
      },
      {
        [`&.${selectClasses_default.error}`]: styles5.error
      },
      {
        [`&.${selectClasses_default.multiple}`]: styles5.multiple
      }
    ];
  }
})({
  // Win specificity over the input base
  [`&.${selectClasses_default.select}`]: {
    height: "auto",
    // Resets for multiple select with chips
    minHeight: "1.4375em",
    // Required for select\text-field height consistency
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    overflow: "hidden"
  }
});
var SelectIcon = styled_default2(StyledSelectIcon, {
  name: "MuiSelect",
  slot: "Icon",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.icon, ownerState.variant && styles5[`icon${capitalize_default(ownerState.variant)}`], ownerState.open && styles5.iconOpen];
  }
})({});
var SelectNativeInput = styled_default2("input", {
  shouldForwardProp: (prop) => slotShouldForwardProp_default(prop) && prop !== "classes",
  name: "MuiSelect",
  slot: "NativeInput"
})({
  bottom: 0,
  left: 0,
  position: "absolute",
  opacity: 0,
  pointerEvents: "none",
  width: "100%",
  boxSizing: "border-box"
});
function areEqualValues(a, b) {
  if (typeof b === "object" && b !== null) {
    return a === b;
  }
  return String(a) === String(b);
}
function isEmpty2(display) {
  return display == null || typeof display === "string" && !display.trim();
}
var useUtilityClasses76 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    disabled,
    multiple,
    open,
    error
  } = ownerState;
  const slots = {
    select: ["select", variant, disabled && "disabled", multiple && "multiple", error && "error"],
    icon: ["icon", `icon${capitalize_default(variant)}`, open && "iconOpen", disabled && "disabled"],
    nativeInput: ["nativeInput"]
  };
  return composeClasses(slots, getSelectUtilityClasses, classes2);
};
var SelectInput = React177.forwardRef(function SelectInput2(props, ref) {
  var _a, _b, _c, _d;
  const {
    "aria-describedby": ariaDescribedby,
    "aria-label": ariaLabel,
    autoFocus,
    autoWidth,
    children: children2,
    className,
    defaultOpen,
    defaultValue,
    disabled,
    displayEmpty,
    error = false,
    IconComponent,
    inputRef: inputRefProp,
    labelId,
    MenuProps = {},
    multiple,
    name,
    onBlur,
    onChange,
    onClose,
    onFocus,
    // eslint-disable-next-line react/prop-types
    onKeyDown,
    // eslint-disable-next-line react/prop-types
    onMouseDown,
    onOpen,
    open: openProp,
    readOnly,
    renderValue,
    required,
    SelectDisplayProps = {},
    tabIndex: tabIndexProp,
    // catching `type` from Input which makes no sense for SelectInput
    type,
    value: valueProp,
    variant = "standard",
    ...other
  } = props;
  const [value, setValueState] = useControlled_default({
    controlled: valueProp,
    default: defaultValue,
    name: "Select"
  });
  const [openState, setOpenState] = useControlled_default({
    controlled: openProp,
    default: defaultOpen,
    name: "Select"
  });
  const inputRef = React177.useRef(null);
  const displayRef = React177.useRef(null);
  const [displayNode, setDisplayNode] = React177.useState(null);
  const {
    current: isOpenControlled
  } = React177.useRef(openProp != null);
  const [menuMinWidthState, setMenuMinWidthState] = React177.useState();
  const handleRef = useForkRef_default(ref, inputRefProp);
  const handleDisplayRef = React177.useCallback((node) => {
    displayRef.current = node;
    if (node) {
      setDisplayNode(node);
    }
  }, []);
  const anchorElement = displayNode == null ? void 0 : displayNode.parentNode;
  React177.useImperativeHandle(handleRef, () => ({
    focus: () => {
      displayRef.current.focus();
    },
    node: inputRef.current,
    value
  }), [value]);
  const open = displayNode !== null && openState;
  React177.useEffect(() => {
    if (!open || !anchorElement || autoWidth) {
      return void 0;
    }
    if (typeof ResizeObserver === "undefined") {
      return void 0;
    }
    const observer = new ResizeObserver(() => {
      setMenuMinWidthState(anchorElement.clientWidth);
    });
    observer.observe(anchorElement);
    return () => {
      observer.disconnect();
    };
  }, [open, anchorElement, autoWidth]);
  React177.useEffect(() => {
    if (defaultOpen && openState && displayNode && !isOpenControlled) {
      setMenuMinWidthState(autoWidth ? null : anchorElement.clientWidth);
      displayRef.current.focus();
    }
  }, [displayNode, autoWidth]);
  React177.useEffect(() => {
    if (autoFocus) {
      displayRef.current.focus();
    }
  }, [autoFocus]);
  React177.useEffect(() => {
    if (!labelId) {
      return void 0;
    }
    const label = ownerDocument_default(displayRef.current).getElementById(labelId);
    if (label) {
      const handler = () => {
        if (getSelection().isCollapsed) {
          displayRef.current.focus();
        }
      };
      label.addEventListener("click", handler);
      return () => {
        label.removeEventListener("click", handler);
      };
    }
    return void 0;
  }, [labelId]);
  const update = (openParam, event) => {
    if (openParam) {
      if (onOpen) {
        onOpen(event);
      }
    } else if (onClose) {
      onClose(event);
    }
    if (!isOpenControlled) {
      setMenuMinWidthState(autoWidth ? null : anchorElement.clientWidth);
      setOpenState(openParam);
    }
  };
  const handleMouseDown = (event) => {
    onMouseDown == null ? void 0 : onMouseDown(event);
    if (event.button !== 0) {
      return;
    }
    event.preventDefault();
    displayRef.current.focus();
    update(true, event);
  };
  const handleClose = (event) => {
    update(false, event);
  };
  const childrenArray = React177.Children.toArray(children2);
  const handleChange = (event) => {
    const child = childrenArray.find((childItem) => childItem.props.value === event.target.value);
    if (child === void 0) {
      return;
    }
    setValueState(child.props.value);
    if (onChange) {
      onChange(event, child);
    }
  };
  const handleItemClick = (child) => (event) => {
    let newValue;
    if (!event.currentTarget.hasAttribute("tabindex")) {
      return;
    }
    if (multiple) {
      newValue = Array.isArray(value) ? value.slice() : [];
      const itemIndex = value.indexOf(child.props.value);
      if (itemIndex === -1) {
        newValue.push(child.props.value);
      } else {
        newValue.splice(itemIndex, 1);
      }
    } else {
      newValue = child.props.value;
    }
    if (child.props.onClick) {
      child.props.onClick(event);
    }
    if (value !== newValue) {
      setValueState(newValue);
      if (onChange) {
        const nativeEvent = event.nativeEvent || event;
        const clonedEvent = new nativeEvent.constructor(nativeEvent.type, nativeEvent);
        Object.defineProperty(clonedEvent, "target", {
          writable: true,
          value: {
            value: newValue,
            name
          }
        });
        onChange(clonedEvent, child);
      }
    }
    if (!multiple) {
      update(false, event);
    }
  };
  const handleKeyDown = (event) => {
    if (!readOnly) {
      const validKeys = [
        " ",
        "ArrowUp",
        "ArrowDown",
        // The native select doesn't respond to enter on macOS, but it's recommended by
        // https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-select-only/
        "Enter"
      ];
      if (validKeys.includes(event.key)) {
        event.preventDefault();
        update(true, event);
      }
      onKeyDown == null ? void 0 : onKeyDown(event);
    }
  };
  const handleBlur = (event) => {
    if (!open && onBlur) {
      Object.defineProperty(event, "target", {
        writable: true,
        value: {
          value,
          name
        }
      });
      onBlur(event);
    }
  };
  delete other["aria-invalid"];
  let display;
  let displaySingle;
  const displayMultiple = [];
  let computeDisplay = false;
  let foundMatch = false;
  if (isFilled({
    value
  }) || displayEmpty) {
    if (renderValue) {
      display = renderValue(value);
    } else {
      computeDisplay = true;
    }
  }
  const items = childrenArray.map((child) => {
    if (!React177.isValidElement(child)) {
      return null;
    }
    if (true) {
      if ((0, import_react_is3.isFragment)(child)) {
        console.error(["MUI: The Select component doesn't accept a Fragment as a child.", "Consider providing an array instead."].join("\n"));
      }
    }
    let selected;
    if (multiple) {
      if (!Array.isArray(value)) {
        throw new Error(true ? "MUI: The `value` prop must be an array when using the `Select` component with `multiple`." : formatMuiErrorMessage(2));
      }
      selected = value.some((v) => areEqualValues(v, child.props.value));
      if (selected && computeDisplay) {
        displayMultiple.push(child.props.children);
      }
    } else {
      selected = areEqualValues(value, child.props.value);
      if (selected && computeDisplay) {
        displaySingle = child.props.children;
      }
    }
    if (selected) {
      foundMatch = true;
    }
    return React177.cloneElement(child, {
      "aria-selected": selected ? "true" : "false",
      onClick: handleItemClick(child),
      onKeyUp: (event) => {
        if (event.key === " ") {
          event.preventDefault();
        }
        if (child.props.onKeyUp) {
          child.props.onKeyUp(event);
        }
      },
      role: "option",
      selected,
      value: void 0,
      // The value is most likely not a valid HTML attribute.
      "data-value": child.props.value
      // Instead, we provide it as a data attribute.
    });
  });
  if (true) {
    React177.useEffect(() => {
      if (!foundMatch && !multiple && value !== "") {
        const values3 = childrenArray.map((child) => child.props.value);
        console.warn([`MUI: You have provided an out-of-range value \`${value}\` for the select ${name ? `(name="${name}") ` : ""}component.`, "Consider providing a value that matches one of the available options or ''.", `The available values are ${values3.filter((x) => x != null).map((x) => `\`${x}\``).join(", ") || '""'}.`].join("\n"));
      }
    }, [foundMatch, childrenArray, multiple, name, value]);
  }
  if (computeDisplay) {
    if (multiple) {
      if (displayMultiple.length === 0) {
        display = null;
      } else {
        display = displayMultiple.reduce((output, child, index) => {
          output.push(child);
          if (index < displayMultiple.length - 1) {
            output.push(", ");
          }
          return output;
        }, []);
      }
    } else {
      display = displaySingle;
    }
  }
  let menuMinWidth = menuMinWidthState;
  if (!autoWidth && isOpenControlled && displayNode) {
    menuMinWidth = anchorElement.clientWidth;
  }
  let tabIndex;
  if (typeof tabIndexProp !== "undefined") {
    tabIndex = tabIndexProp;
  } else {
    tabIndex = disabled ? null : 0;
  }
  const buttonId = SelectDisplayProps.id || (name ? `mui-component-select-${name}` : void 0);
  const ownerState = {
    ...props,
    variant,
    value,
    open,
    error
  };
  const classes2 = useUtilityClasses76(ownerState);
  const paperProps = {
    ...MenuProps.PaperProps,
    ...typeof ((_a = MenuProps.slotProps) == null ? void 0 : _a.paper) === "function" ? MenuProps.slotProps.paper(ownerState) : (_b = MenuProps.slotProps) == null ? void 0 : _b.paper
  };
  const listProps = {
    ...MenuProps.MenuListProps,
    ...typeof ((_c = MenuProps.slotProps) == null ? void 0 : _c.list) === "function" ? MenuProps.slotProps.list(ownerState) : (_d = MenuProps.slotProps) == null ? void 0 : _d.list
  };
  const listboxId = useId();
  return (0, import_jsx_runtime147.jsxs)(React177.Fragment, {
    children: [(0, import_jsx_runtime147.jsx)(SelectSelect, {
      as: "div",
      ref: handleDisplayRef,
      tabIndex,
      role: "combobox",
      "aria-controls": open ? listboxId : void 0,
      "aria-disabled": disabled ? "true" : void 0,
      "aria-expanded": open ? "true" : "false",
      "aria-haspopup": "listbox",
      "aria-label": ariaLabel,
      "aria-labelledby": [labelId, buttonId].filter(Boolean).join(" ") || void 0,
      "aria-describedby": ariaDescribedby,
      "aria-required": required ? "true" : void 0,
      "aria-invalid": error ? "true" : void 0,
      onKeyDown: handleKeyDown,
      onMouseDown: disabled || readOnly ? null : handleMouseDown,
      onBlur: handleBlur,
      onFocus,
      ...SelectDisplayProps,
      ownerState,
      className: clsx_default(SelectDisplayProps.className, classes2.select, className),
      id: buttonId,
      children: isEmpty2(display) ? (
        // notranslate needed while Google Translate will not fix zero-width space issue
        _span3 || (_span3 = (0, import_jsx_runtime147.jsx)("span", {
          className: "notranslate",
          "aria-hidden": true,
          children: "​"
        }))
      ) : display
    }), (0, import_jsx_runtime147.jsx)(SelectNativeInput, {
      "aria-invalid": error,
      value: Array.isArray(value) ? value.join(",") : value,
      name,
      ref: inputRef,
      "aria-hidden": true,
      onChange: handleChange,
      tabIndex: -1,
      disabled,
      className: classes2.nativeInput,
      autoFocus,
      required,
      ...other,
      ownerState
    }), (0, import_jsx_runtime147.jsx)(SelectIcon, {
      as: IconComponent,
      className: classes2.icon,
      ownerState
    }), (0, import_jsx_runtime147.jsx)(Menu_default, {
      id: `menu-${name || ""}`,
      anchorEl: anchorElement,
      open,
      onClose: handleClose,
      anchorOrigin: {
        vertical: "bottom",
        horizontal: "center"
      },
      transformOrigin: {
        vertical: "top",
        horizontal: "center"
      },
      ...MenuProps,
      slotProps: {
        ...MenuProps.slotProps,
        list: {
          "aria-labelledby": labelId,
          role: "listbox",
          "aria-multiselectable": multiple ? "true" : void 0,
          disableListWrap: true,
          id: listboxId,
          ...listProps
        },
        paper: {
          ...paperProps,
          style: {
            minWidth: menuMinWidth,
            ...paperProps != null ? paperProps.style : null
          }
        }
      },
      children: items
    })]
  });
});
true ? SelectInput.propTypes = {
  /**
   * @ignore
   */
  "aria-describedby": import_prop_types120.default.string,
  /**
   * @ignore
   */
  "aria-label": import_prop_types120.default.string,
  /**
   * @ignore
   */
  autoFocus: import_prop_types120.default.bool,
  /**
   * If `true`, the width of the popover will automatically be set according to the items inside the
   * menu, otherwise it will be at least the width of the select input.
   */
  autoWidth: import_prop_types120.default.bool,
  /**
   * The option elements to populate the select with.
   * Can be some `<MenuItem>` elements.
   */
  children: import_prop_types120.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types120.default.object,
  /**
   * The CSS class name of the select element.
   */
  className: import_prop_types120.default.string,
  /**
   * If `true`, the component is toggled on mount. Use when the component open state is not controlled.
   * You can only use it when the `native` prop is `false` (default).
   */
  defaultOpen: import_prop_types120.default.bool,
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types120.default.any,
  /**
   * If `true`, the select is disabled.
   */
  disabled: import_prop_types120.default.bool,
  /**
   * If `true`, the selected item is displayed even if its value is empty.
   */
  displayEmpty: import_prop_types120.default.bool,
  /**
   * If `true`, the `select input` will indicate an error.
   */
  error: import_prop_types120.default.bool,
  /**
   * The icon that displays the arrow.
   */
  IconComponent: import_prop_types120.default.elementType.isRequired,
  /**
   * Imperative handle implementing `{ value: T, node: HTMLElement, focus(): void }`
   * Equivalent to `ref`
   */
  inputRef: refType_default,
  /**
   * The ID of an element that acts as an additional label. The Select will
   * be labelled by the additional label and the selected value.
   */
  labelId: import_prop_types120.default.string,
  /**
   * Props applied to the [`Menu`](/material-ui/api/menu/) element.
   */
  MenuProps: import_prop_types120.default.object,
  /**
   * If `true`, `value` must be an array and the menu will support multiple selections.
   */
  multiple: import_prop_types120.default.bool,
  /**
   * Name attribute of the `select` or hidden `input` element.
   */
  name: import_prop_types120.default.string,
  /**
   * @ignore
   */
  onBlur: import_prop_types120.default.func,
  /**
   * Callback fired when a menu item is selected.
   *
   * @param {object} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (any).
   * @param {object} [child] The react element that was selected.
   */
  onChange: import_prop_types120.default.func,
  /**
   * Callback fired when the component requests to be closed.
   * Use in controlled mode (see open).
   *
   * @param {object} event The event source of the callback.
   */
  onClose: import_prop_types120.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types120.default.func,
  /**
   * Callback fired when the component requests to be opened.
   * Use in controlled mode (see open).
   *
   * @param {object} event The event source of the callback.
   */
  onOpen: import_prop_types120.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types120.default.bool,
  /**
   * @ignore
   */
  readOnly: import_prop_types120.default.bool,
  /**
   * Render the selected value.
   *
   * @param {any} value The `value` provided to the component.
   * @returns {ReactNode}
   */
  renderValue: import_prop_types120.default.func,
  /**
   * If `true`, the component is required.
   */
  required: import_prop_types120.default.bool,
  /**
   * Props applied to the clickable div element.
   */
  SelectDisplayProps: import_prop_types120.default.object,
  /**
   * @ignore
   */
  tabIndex: import_prop_types120.default.oneOfType([import_prop_types120.default.number, import_prop_types120.default.string]),
  /**
   * @ignore
   */
  type: import_prop_types120.default.any,
  /**
   * The input value.
   */
  value: import_prop_types120.default.any,
  /**
   * The variant to use.
   */
  variant: import_prop_types120.default.oneOf(["standard", "outlined", "filled"])
} : void 0;
var SelectInput_default = SelectInput;

// node_modules/@mui/material/esm/Select/Select.js
var import_jsx_runtime148 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses77 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"]
  };
  const composedClasses = composeClasses(slots, getSelectUtilityClasses, classes2);
  return {
    ...classes2,
    ...composedClasses
  };
};
var styledRootConfig = {
  name: "MuiSelect",
  slot: "Root",
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) && prop !== "variant"
};
var StyledInput = styled_default2(Input_default, styledRootConfig)("");
var StyledOutlinedInput = styled_default2(OutlinedInput_default, styledRootConfig)("");
var StyledFilledInput = styled_default2(FilledInput_default, styledRootConfig)("");
var Select = React178.forwardRef(function Select2(inProps, ref) {
  const props = useDefaultProps({
    name: "MuiSelect",
    props: inProps
  });
  const {
    autoWidth = false,
    children: children2,
    classes: classesProp = {},
    className,
    defaultOpen = false,
    displayEmpty = false,
    IconComponent = ArrowDropDown_default,
    id,
    input,
    inputProps,
    label,
    labelId,
    MenuProps,
    multiple = false,
    native = false,
    onClose,
    onOpen,
    open,
    renderValue,
    SelectDisplayProps,
    variant: variantProp = "outlined",
    ...other
  } = props;
  const inputComponent = native ? NativeSelectInput_default : SelectInput_default;
  const muiFormControl = useFormControl();
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["variant", "error"]
  });
  const variant = fcs.variant || variantProp;
  const ownerState = {
    ...props,
    variant,
    classes: classesProp
  };
  const classes2 = useUtilityClasses77(ownerState);
  const {
    root,
    ...restOfClasses
  } = classes2;
  const InputComponent = input || {
    standard: (0, import_jsx_runtime148.jsx)(StyledInput, {
      ownerState
    }),
    outlined: (0, import_jsx_runtime148.jsx)(StyledOutlinedInput, {
      label,
      ownerState
    }),
    filled: (0, import_jsx_runtime148.jsx)(StyledFilledInput, {
      ownerState
    })
  }[variant];
  const inputComponentRef = useForkRef_default(ref, getReactElementRef(InputComponent));
  return (0, import_jsx_runtime148.jsx)(React178.Fragment, {
    children: React178.cloneElement(InputComponent, {
      // Most of the logic is implemented in `SelectInput`.
      // The `Select` component is a simple API wrapper to expose something better to play with.
      inputComponent,
      inputProps: {
        children: children2,
        error: fcs.error,
        IconComponent,
        variant,
        type: void 0,
        // We render a select. We can ignore the type provided by the `Input`.
        multiple,
        ...native ? {
          id
        } : {
          autoWidth,
          defaultOpen,
          displayEmpty,
          labelId,
          MenuProps,
          onClose,
          onOpen,
          open,
          renderValue,
          SelectDisplayProps: {
            id,
            ...SelectDisplayProps
          }
        },
        ...inputProps,
        classes: inputProps ? deepmerge(restOfClasses, inputProps.classes) : restOfClasses,
        ...input ? input.props.inputProps : {}
      },
      ...(multiple && native || displayEmpty) && variant === "outlined" ? {
        notched: true
      } : {},
      ref: inputComponentRef,
      className: clsx_default(InputComponent.props.className, className, classes2.root),
      // If a custom input is provided via 'input' prop, do not allow 'variant' to be propagated to it's root element. See https://github.com/mui/material-ui/issues/33894.
      ...!input && {
        variant
      },
      ...other
    })
  });
});
true ? Select.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the width of the popover will automatically be set according to the items inside the
   * menu, otherwise it will be at least the width of the select input.
   * @default false
   */
  autoWidth: import_prop_types121.default.bool,
  /**
   * The option elements to populate the select with.
   * Can be some `MenuItem` when `native` is false and `option` when `native` is true.
   *
   * ⚠️The `MenuItem` elements **must** be direct descendants when `native` is false.
   */
  children: import_prop_types121.default.node,
  /**
   * Override or extend the styles applied to the component.
   * @default {}
   */
  classes: import_prop_types121.default.object,
  /**
   * @ignore
   */
  className: import_prop_types121.default.string,
  /**
   * If `true`, the component is initially open. Use when the component open state is not controlled (i.e. the `open` prop is not defined).
   * You can only use it when the `native` prop is `false` (default).
   * @default false
   */
  defaultOpen: import_prop_types121.default.bool,
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types121.default.any,
  /**
   * If `true`, a value is displayed even if no items are selected.
   *
   * In order to display a meaningful value, a function can be passed to the `renderValue` prop which
   * returns the value to be displayed when no items are selected.
   *
   * ⚠️ When using this prop, make sure the label doesn't overlap with the empty displayed value.
   * The label should either be hidden or forced to a shrunk state.
   * @default false
   */
  displayEmpty: import_prop_types121.default.bool,
  /**
   * The icon that displays the arrow.
   * @default ArrowDropDownIcon
   */
  IconComponent: import_prop_types121.default.elementType,
  /**
   * The `id` of the wrapper element or the `select` element when `native`.
   */
  id: import_prop_types121.default.string,
  /**
   * An `Input` element; does not have to be a material-ui specific `Input`.
   */
  input: import_prop_types121.default.element,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * When `native` is `true`, the attributes are applied on the `select` element.
   */
  inputProps: import_prop_types121.default.object,
  /**
   * See [OutlinedInput#label](https://mui.com/material-ui/api/outlined-input/#props)
   */
  label: import_prop_types121.default.node,
  /**
   * The ID of an element that acts as an additional label. The Select will
   * be labelled by the additional label and the selected value.
   */
  labelId: import_prop_types121.default.string,
  /**
   * Props applied to the [`Menu`](https://mui.com/material-ui/api/menu/) element.
   */
  MenuProps: import_prop_types121.default.object,
  /**
   * If `true`, `value` must be an array and the menu will support multiple selections.
   * @default false
   */
  multiple: import_prop_types121.default.bool,
  /**
   * If `true`, the component uses a native `select` element.
   * @default false
   */
  native: import_prop_types121.default.bool,
  /**
   * Callback fired when a menu item is selected.
   *
   * @param {SelectChangeEvent<Value>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (any).
   * **Warning**: This is a generic event, not a change event, unless the change event is caused by browser autofill.
   * @param {object} [child] The react element that was selected when `native` is `false` (default).
   */
  onChange: import_prop_types121.default.func,
  /**
   * Callback fired when the component requests to be closed.
   * Use it in either controlled (see the `open` prop), or uncontrolled mode (to detect when the Select collapses).
   *
   * @param {object} event The event source of the callback.
   */
  onClose: import_prop_types121.default.func,
  /**
   * Callback fired when the component requests to be opened.
   * Use it in either controlled (see the `open` prop), or uncontrolled mode (to detect when the Select expands).
   *
   * @param {object} event The event source of the callback.
   */
  onOpen: import_prop_types121.default.func,
  /**
   * If `true`, the component is shown.
   * You can only use it when the `native` prop is `false` (default).
   */
  open: import_prop_types121.default.bool,
  /**
   * Render the selected value.
   * You can only use it when the `native` prop is `false` (default).
   *
   * @param {any} value The `value` provided to the component.
   * @returns {ReactNode}
   */
  renderValue: import_prop_types121.default.func,
  /**
   * Props applied to the clickable div element.
   */
  SelectDisplayProps: import_prop_types121.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types121.default.oneOfType([import_prop_types121.default.arrayOf(import_prop_types121.default.oneOfType([import_prop_types121.default.func, import_prop_types121.default.object, import_prop_types121.default.bool])), import_prop_types121.default.func, import_prop_types121.default.object]),
  /**
   * The `input` value. Providing an empty string will select no options.
   * Set to an empty string `''` if you don't want any of the available options to be selected.
   *
   * If the value is an object it must have reference equality with the option in order to be selected.
   * If the value is not an object, the string representation must match with the string representation of the option in order to be selected.
   */
  value: import_prop_types121.default.oneOfType([import_prop_types121.default.oneOf([""]), import_prop_types121.default.any]),
  /**
   * The variant to use.
   * @default 'outlined'
   */
  variant: import_prop_types121.default.oneOf(["filled", "outlined", "standard"])
} : void 0;
Select.muiName = "Select";
var Select_default = Select;

// node_modules/@mui/material/esm/TextField/textFieldClasses.js
function getTextFieldUtilityClass(slot) {
  return generateUtilityClass("MuiTextField", slot);
}
var textFieldClasses = generateUtilityClasses("MuiTextField", ["root"]);

// node_modules/@mui/material/esm/TextField/TextField.js
var import_jsx_runtime149 = __toESM(require_jsx_runtime(), 1);
var variantComponent = {
  standard: Input_default,
  filled: FilledInput_default,
  outlined: OutlinedInput_default
};
var useUtilityClasses78 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"]
  };
  return composeClasses(slots, getTextFieldUtilityClass, classes2);
};
var TextFieldRoot = styled_default2(FormControl_default, {
  name: "MuiTextField",
  slot: "Root"
})({});
var TextField = React179.forwardRef(function TextField2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTextField"
  });
  const {
    autoComplete,
    autoFocus = false,
    children: children2,
    className,
    color = "primary",
    defaultValue,
    disabled = false,
    error = false,
    FormHelperTextProps: FormHelperTextPropsProp,
    fullWidth = false,
    helperText,
    id: idOverride,
    InputLabelProps: InputLabelPropsProp,
    inputProps: inputPropsProp,
    InputProps: InputPropsProp,
    inputRef,
    label,
    maxRows,
    minRows,
    multiline = false,
    name,
    onBlur,
    onChange,
    onFocus,
    placeholder,
    required = false,
    rows,
    select = false,
    SelectProps: SelectPropsProp,
    slots = {},
    slotProps = {},
    type,
    value,
    variant = "outlined",
    ...other
  } = props;
  const ownerState = {
    ...props,
    autoFocus,
    color,
    disabled,
    error,
    fullWidth,
    multiline,
    required,
    select,
    variant
  };
  const classes2 = useUtilityClasses78(ownerState);
  if (true) {
    if (select && !children2) {
      console.error("MUI: `children` must be passed when using the `TextField` component with `select`.");
    }
  }
  const id = useId(idOverride);
  const helperTextId = helperText && id ? `${id}-helper-text` : void 0;
  const inputLabelId = label && id ? `${id}-label` : void 0;
  const InputComponent = variantComponent[variant];
  const externalForwardedProps = {
    slots,
    slotProps: {
      input: InputPropsProp,
      inputLabel: InputLabelPropsProp,
      htmlInput: inputPropsProp,
      formHelperText: FormHelperTextPropsProp,
      select: SelectPropsProp,
      ...slotProps
    }
  };
  const inputAdditionalProps = {};
  const inputLabelSlotProps = externalForwardedProps.slotProps.inputLabel;
  if (variant === "outlined") {
    if (inputLabelSlotProps && typeof inputLabelSlotProps.shrink !== "undefined") {
      inputAdditionalProps.notched = inputLabelSlotProps.shrink;
    }
    inputAdditionalProps.label = label;
  }
  if (select) {
    if (!SelectPropsProp || !SelectPropsProp.native) {
      inputAdditionalProps.id = void 0;
    }
    inputAdditionalProps["aria-describedby"] = void 0;
  }
  const [RootSlot, rootProps] = useSlot("root", {
    elementType: TextFieldRoot,
    shouldForwardComponentProp: true,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other
    },
    ownerState,
    className: clsx_default(classes2.root, className),
    ref,
    additionalProps: {
      disabled,
      error,
      fullWidth,
      required,
      color,
      variant
    }
  });
  const [InputSlot, inputProps] = useSlot("input", {
    elementType: InputComponent,
    externalForwardedProps,
    additionalProps: inputAdditionalProps,
    ownerState
  });
  const [InputLabelSlot, inputLabelProps] = useSlot("inputLabel", {
    elementType: InputLabel_default,
    externalForwardedProps,
    ownerState
  });
  const [HtmlInputSlot, htmlInputProps] = useSlot("htmlInput", {
    elementType: "input",
    externalForwardedProps,
    ownerState
  });
  const [FormHelperTextSlot, formHelperTextProps] = useSlot("formHelperText", {
    elementType: FormHelperText_default,
    externalForwardedProps,
    ownerState
  });
  const [SelectSlot, selectProps] = useSlot("select", {
    elementType: Select_default,
    externalForwardedProps,
    ownerState
  });
  const InputElement = (0, import_jsx_runtime149.jsx)(InputSlot, {
    "aria-describedby": helperTextId,
    autoComplete,
    autoFocus,
    defaultValue,
    fullWidth,
    multiline,
    name,
    rows,
    maxRows,
    minRows,
    type,
    value,
    id,
    inputRef,
    onBlur,
    onChange,
    onFocus,
    placeholder,
    inputProps: htmlInputProps,
    slots: {
      input: slots.htmlInput ? HtmlInputSlot : void 0
    },
    ...inputProps
  });
  return (0, import_jsx_runtime149.jsxs)(RootSlot, {
    ...rootProps,
    children: [label != null && label !== "" && (0, import_jsx_runtime149.jsx)(InputLabelSlot, {
      htmlFor: id,
      id: inputLabelId,
      ...inputLabelProps,
      children: label
    }), select ? (0, import_jsx_runtime149.jsx)(SelectSlot, {
      "aria-describedby": helperTextId,
      id,
      labelId: inputLabelId,
      value,
      input: InputElement,
      ...selectProps,
      children: children2
    }) : InputElement, helperText && (0, import_jsx_runtime149.jsx)(FormHelperTextSlot, {
      id: helperTextId,
      ...formHelperTextProps,
      children: helperText
    })]
  });
});
true ? TextField.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop helps users to fill forms faster, especially on mobile devices.
   * The name can be confusing, as it's more like an autofill.
   * You can learn more about it [following the specification](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#autofill).
   */
  autoComplete: import_prop_types122.default.string,
  /**
   * If `true`, the `input` element is focused during the first mount.
   * @default false
   */
  autoFocus: import_prop_types122.default.bool,
  /**
   * @ignore
   */
  children: import_prop_types122.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types122.default.object,
  /**
   * @ignore
   */
  className: import_prop_types122.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types122.default.oneOfType([import_prop_types122.default.oneOf(["primary", "secondary", "error", "info", "success", "warning"]), import_prop_types122.default.string]),
  /**
   * The default value. Use when the component is not controlled.
   */
  defaultValue: import_prop_types122.default.any,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types122.default.bool,
  /**
   * If `true`, the label is displayed in an error state.
   * @default false
   */
  error: import_prop_types122.default.bool,
  /**
   * Props applied to the [`FormHelperText`](https://mui.com/material-ui/api/form-helper-text/) element.
   * @deprecated Use `slotProps.formHelperText` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  FormHelperTextProps: import_prop_types122.default.object,
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types122.default.bool,
  /**
   * The helper text content.
   */
  helperText: import_prop_types122.default.node,
  /**
   * The id of the `input` element.
   * Use this prop to make `label` and `helperText` accessible for screen readers.
   */
  id: import_prop_types122.default.string,
  /**
   * Props applied to the [`InputLabel`](https://mui.com/material-ui/api/input-label/) element.
   * Pointer events like `onClick` are enabled if and only if `shrink` is `true`.
   * @deprecated Use `slotProps.inputLabel` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  InputLabelProps: import_prop_types122.default.object,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @deprecated Use `slotProps.htmlInput` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  inputProps: import_prop_types122.default.object,
  /**
   * Props applied to the Input element.
   * It will be a [`FilledInput`](https://mui.com/material-ui/api/filled-input/),
   * [`OutlinedInput`](https://mui.com/material-ui/api/outlined-input/) or [`Input`](https://mui.com/material-ui/api/input/)
   * component depending on the `variant` prop value.
   * @deprecated Use `slotProps.input` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  InputProps: import_prop_types122.default.object,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * The label content.
   */
  label: import_prop_types122.default.node,
  /**
   * If `dense` or `normal`, will adjust vertical spacing of this and contained components.
   * @default 'none'
   */
  margin: import_prop_types122.default.oneOf(["dense", "none", "normal"]),
  /**
   * Maximum number of rows to display when multiline option is set to true.
   */
  maxRows: import_prop_types122.default.oneOfType([import_prop_types122.default.number, import_prop_types122.default.string]),
  /**
   * Minimum number of rows to display when multiline option is set to true.
   */
  minRows: import_prop_types122.default.oneOfType([import_prop_types122.default.number, import_prop_types122.default.string]),
  /**
   * If `true`, a `textarea` element is rendered instead of an input.
   * @default false
   */
  multiline: import_prop_types122.default.bool,
  /**
   * Name attribute of the `input` element.
   */
  name: import_prop_types122.default.string,
  /**
   * @ignore
   */
  onBlur: import_prop_types122.default.func,
  /**
   * Callback fired when the value is changed.
   *
   * @param {object} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   */
  onChange: import_prop_types122.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types122.default.func,
  /**
   * The short hint displayed in the `input` before the user enters a value.
   */
  placeholder: import_prop_types122.default.string,
  /**
   * If `true`, the label is displayed as required and the `input` element is required.
   * @default false
   */
  required: import_prop_types122.default.bool,
  /**
   * Number of rows to display when multiline option is set to true.
   */
  rows: import_prop_types122.default.oneOfType([import_prop_types122.default.number, import_prop_types122.default.string]),
  /**
   * Render a [`Select`](https://mui.com/material-ui/api/select/) element while passing the Input element to `Select` as `input` parameter.
   * If this option is set you must pass the options of the select as children.
   * @default false
   */
  select: import_prop_types122.default.bool,
  /**
   * Props applied to the [`Select`](https://mui.com/material-ui/api/select/) element.
   * @deprecated Use `slotProps.select` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  SelectProps: import_prop_types122.default.object,
  /**
   * The size of the component.
   * @default 'medium'
   */
  size: import_prop_types122.default.oneOfType([import_prop_types122.default.oneOf(["medium", "small"]), import_prop_types122.default.string]),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types122.default.shape({
    formHelperText: import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object]),
    htmlInput: import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object]),
    input: import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object]),
    inputLabel: import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object]),
    select: import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types122.default.shape({
    formHelperText: import_prop_types122.default.elementType,
    htmlInput: import_prop_types122.default.elementType,
    input: import_prop_types122.default.elementType,
    inputLabel: import_prop_types122.default.elementType,
    root: import_prop_types122.default.elementType,
    select: import_prop_types122.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types122.default.oneOfType([import_prop_types122.default.arrayOf(import_prop_types122.default.oneOfType([import_prop_types122.default.func, import_prop_types122.default.object, import_prop_types122.default.bool])), import_prop_types122.default.func, import_prop_types122.default.object]),
  /**
   * Type of the `input` element. It should be [a valid HTML5 input type](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#input_types).
   */
  type: import_prop_types122.default.string,
  /**
   * The value of the `input` element, required for a controlled component.
   */
  value: import_prop_types122.default.any,
  /**
   * The variant to use.
   * @default 'outlined'
   */
  variant: import_prop_types122.default.oneOf(["filled", "outlined", "standard"])
} : void 0;
var TextField_default = TextField;

// node_modules/@mui/material/esm/FormControlLabel/FormControlLabel.js
var React180 = __toESM(require_react(), 1);
var import_prop_types123 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/FormControlLabel/formControlLabelClasses.js
function getFormControlLabelUtilityClasses(slot) {
  return generateUtilityClass("MuiFormControlLabel", slot);
}
var formControlLabelClasses = generateUtilityClasses("MuiFormControlLabel", ["root", "labelPlacementStart", "labelPlacementTop", "labelPlacementBottom", "disabled", "label", "error", "required", "asterisk"]);
var formControlLabelClasses_default = formControlLabelClasses;

// node_modules/@mui/material/esm/FormControlLabel/FormControlLabel.js
var import_jsx_runtime150 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses79 = (ownerState) => {
  const {
    classes: classes2,
    disabled,
    labelPlacement,
    error,
    required
  } = ownerState;
  const slots = {
    root: ["root", disabled && "disabled", `labelPlacement${capitalize_default(labelPlacement)}`, error && "error", required && "required"],
    label: ["label", disabled && "disabled"],
    asterisk: ["asterisk", error && "error"]
  };
  return composeClasses(slots, getFormControlLabelUtilityClasses, classes2);
};
var FormControlLabelRoot = styled_default2("label", {
  name: "MuiFormControlLabel",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [{
      [`& .${formControlLabelClasses_default.label}`]: styles5.label
    }, styles5.root, styles5[`labelPlacement${capitalize_default(ownerState.labelPlacement)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  display: "inline-flex",
  alignItems: "center",
  cursor: "pointer",
  // For correct alignment with the text.
  verticalAlign: "middle",
  WebkitTapHighlightColor: "transparent",
  marginLeft: -11,
  marginRight: 16,
  // used for row presentation of radio/checkbox
  [`&.${formControlLabelClasses_default.disabled}`]: {
    cursor: "default"
  },
  [`& .${formControlLabelClasses_default.label}`]: {
    [`&.${formControlLabelClasses_default.disabled}`]: {
      color: (theme.vars || theme).palette.text.disabled
    }
  },
  variants: [{
    props: {
      labelPlacement: "start"
    },
    style: {
      flexDirection: "row-reverse",
      marginRight: -11
    }
  }, {
    props: {
      labelPlacement: "top"
    },
    style: {
      flexDirection: "column-reverse"
    }
  }, {
    props: {
      labelPlacement: "bottom"
    },
    style: {
      flexDirection: "column"
    }
  }, {
    props: ({
      labelPlacement
    }) => labelPlacement === "start" || labelPlacement === "top" || labelPlacement === "bottom",
    style: {
      marginLeft: 16
      // used for row presentation of radio/checkbox
    }
  }]
})));
var AsteriskComponent2 = styled_default2("span", {
  name: "MuiFormControlLabel",
  slot: "Asterisk"
})(memoTheme_default(({
  theme
}) => ({
  [`&.${formControlLabelClasses_default.error}`]: {
    color: (theme.vars || theme).palette.error.main
  }
})));
var FormControlLabel = React180.forwardRef(function FormControlLabel2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiFormControlLabel"
  });
  const {
    checked,
    className,
    componentsProps = {},
    control,
    disabled: disabledProp,
    disableTypography,
    inputRef,
    label: labelProp,
    labelPlacement = "end",
    name,
    onChange,
    required: requiredProp,
    slots = {},
    slotProps = {},
    value,
    ...other
  } = props;
  const muiFormControl = useFormControl();
  const disabled = disabledProp ?? control.props.disabled ?? (muiFormControl == null ? void 0 : muiFormControl.disabled);
  const required = requiredProp ?? control.props.required;
  const controlProps = {
    disabled,
    required
  };
  ["checked", "name", "onChange", "value", "inputRef"].forEach((key) => {
    if (typeof control.props[key] === "undefined" && typeof props[key] !== "undefined") {
      controlProps[key] = props[key];
    }
  });
  const fcs = formControlState({
    props,
    muiFormControl,
    states: ["error"]
  });
  const ownerState = {
    ...props,
    disabled,
    labelPlacement,
    required,
    error: fcs.error
  };
  const classes2 = useUtilityClasses79(ownerState);
  const externalForwardedProps = {
    slots,
    slotProps: {
      ...componentsProps,
      ...slotProps
    }
  };
  const [TypographySlot, typographySlotProps] = useSlot("typography", {
    elementType: Typography_default,
    externalForwardedProps,
    ownerState
  });
  let label = labelProp;
  if (label != null && label.type !== Typography_default && !disableTypography) {
    label = (0, import_jsx_runtime150.jsx)(TypographySlot, {
      component: "span",
      ...typographySlotProps,
      className: clsx_default(classes2.label, typographySlotProps == null ? void 0 : typographySlotProps.className),
      children: label
    });
  }
  return (0, import_jsx_runtime150.jsxs)(FormControlLabelRoot, {
    className: clsx_default(classes2.root, className),
    ownerState,
    ref,
    ...other,
    children: [React180.cloneElement(control, controlProps), required ? (0, import_jsx_runtime150.jsxs)("div", {
      children: [label, (0, import_jsx_runtime150.jsxs)(AsteriskComponent2, {
        ownerState,
        "aria-hidden": true,
        className: classes2.asterisk,
        children: [" ", "*"]
      })]
    }) : label]
  });
});
true ? FormControlLabel.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the component appears selected.
   */
  checked: import_prop_types123.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types123.default.object,
  /**
   * @ignore
   */
  className: import_prop_types123.default.string,
  /**
   * The props used for each slot inside.
   * @default {}
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  componentsProps: import_prop_types123.default.shape({
    typography: import_prop_types123.default.object
  }),
  /**
   * A control element. For instance, it can be a `Radio`, a `Switch` or a `Checkbox`.
   */
  control: import_prop_types123.default.element.isRequired,
  /**
   * If `true`, the control is disabled.
   */
  disabled: import_prop_types123.default.bool,
  /**
   * If `true`, the label is rendered as it is passed without an additional typography node.
   */
  disableTypography: import_prop_types123.default.bool,
  /**
   * Pass a ref to the `input` element.
   */
  inputRef: refType_default,
  /**
   * A text or an element to be used in an enclosing label element.
   */
  label: import_prop_types123.default.node,
  /**
   * The position of the label.
   * @default 'end'
   */
  labelPlacement: import_prop_types123.default.oneOf(["bottom", "end", "start", "top"]),
  /**
   * @ignore
   */
  name: import_prop_types123.default.string,
  /**
   * Callback fired when the state is changed.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   * You can pull out the new checked state by accessing `event.target.checked` (boolean).
   */
  onChange: import_prop_types123.default.func,
  /**
   * If `true`, the label will indicate that the `input` is required.
   */
  required: import_prop_types123.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types123.default.shape({
    typography: import_prop_types123.default.oneOfType([import_prop_types123.default.func, import_prop_types123.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types123.default.shape({
    typography: import_prop_types123.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types123.default.oneOfType([import_prop_types123.default.arrayOf(import_prop_types123.default.oneOfType([import_prop_types123.default.func, import_prop_types123.default.object, import_prop_types123.default.bool])), import_prop_types123.default.func, import_prop_types123.default.object]),
  /**
   * The value of the component.
   */
  value: import_prop_types123.default.any
} : void 0;
var FormControlLabel_default = FormControlLabel;

// node_modules/@mui/material/esm/Switch/Switch.js
var React181 = __toESM(require_react(), 1);
var import_prop_types124 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Switch/switchClasses.js
function getSwitchUtilityClass(slot) {
  return generateUtilityClass("MuiSwitch", slot);
}
var switchClasses = generateUtilityClasses("MuiSwitch", ["root", "edgeStart", "edgeEnd", "switchBase", "colorPrimary", "colorSecondary", "sizeSmall", "sizeMedium", "checked", "disabled", "input", "thumb", "track"]);
var switchClasses_default = switchClasses;

// node_modules/@mui/material/esm/Switch/Switch.js
var import_jsx_runtime151 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses80 = (ownerState) => {
  const {
    classes: classes2,
    edge,
    size,
    color,
    checked,
    disabled
  } = ownerState;
  const slots = {
    root: ["root", edge && `edge${capitalize_default(edge)}`, `size${capitalize_default(size)}`],
    switchBase: ["switchBase", `color${capitalize_default(color)}`, checked && "checked", disabled && "disabled"],
    thumb: ["thumb"],
    track: ["track"],
    input: ["input"]
  };
  const composedClasses = composeClasses(slots, getSwitchUtilityClass, classes2);
  return {
    ...classes2,
    // forward the disabled and checked classes to the SwitchBase
    ...composedClasses
  };
};
var SwitchRoot = styled_default2("span", {
  name: "MuiSwitch",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.edge && styles5[`edge${capitalize_default(ownerState.edge)}`], styles5[`size${capitalize_default(ownerState.size)}`]];
  }
})({
  display: "inline-flex",
  width: 34 + 12 * 2,
  height: 14 + 12 * 2,
  overflow: "hidden",
  padding: 12,
  boxSizing: "border-box",
  position: "relative",
  flexShrink: 0,
  zIndex: 0,
  // Reset the stacking context.
  verticalAlign: "middle",
  // For correct alignment with the text.
  "@media print": {
    colorAdjust: "exact"
  },
  variants: [{
    props: {
      edge: "start"
    },
    style: {
      marginLeft: -8
    }
  }, {
    props: {
      edge: "end"
    },
    style: {
      marginRight: -8
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      width: 40,
      height: 24,
      padding: 7,
      [`& .${switchClasses_default.thumb}`]: {
        width: 16,
        height: 16
      },
      [`& .${switchClasses_default.switchBase}`]: {
        padding: 4,
        [`&.${switchClasses_default.checked}`]: {
          transform: "translateX(16px)"
        }
      }
    }
  }]
});
var SwitchSwitchBase = styled_default2(SwitchBase_default, {
  name: "MuiSwitch",
  slot: "SwitchBase",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.switchBase, {
      [`& .${switchClasses_default.input}`]: styles5.input
    }, ownerState.color !== "default" && styles5[`color${capitalize_default(ownerState.color)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  position: "absolute",
  top: 0,
  left: 0,
  zIndex: 1,
  // Render above the focus ripple.
  color: theme.vars ? theme.vars.palette.Switch.defaultColor : `${theme.palette.mode === "light" ? theme.palette.common.white : theme.palette.grey[300]}`,
  transition: theme.transitions.create(["left", "transform"], {
    duration: theme.transitions.duration.shortest
  }),
  [`&.${switchClasses_default.checked}`]: {
    transform: "translateX(20px)"
  },
  [`&.${switchClasses_default.disabled}`]: {
    color: theme.vars ? theme.vars.palette.Switch.defaultDisabledColor : `${theme.palette.mode === "light" ? theme.palette.grey[100] : theme.palette.grey[600]}`
  },
  [`&.${switchClasses_default.checked} + .${switchClasses_default.track}`]: {
    opacity: 0.5
  },
  [`&.${switchClasses_default.disabled} + .${switchClasses_default.track}`]: {
    opacity: theme.vars ? theme.vars.opacity.switchTrackDisabled : `${theme.palette.mode === "light" ? 0.12 : 0.2}`
  },
  [`& .${switchClasses_default.input}`]: {
    left: "-100%",
    width: "300%"
  }
})), memoTheme_default(({
  theme
}) => ({
  "&:hover": {
    backgroundColor: theme.alpha((theme.vars || theme).palette.action.active, (theme.vars || theme).palette.action.hoverOpacity),
    // Reset on touch devices, it doesn't add specificity
    "@media (hover: none)": {
      backgroundColor: "transparent"
    }
  },
  variants: [...Object.entries(theme.palette).filter(createSimplePaletteValueFilter(["light"])).map(([color]) => ({
    props: {
      color
    },
    style: {
      [`&.${switchClasses_default.checked}`]: {
        color: (theme.vars || theme).palette[color].main,
        "&:hover": {
          backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity),
          "@media (hover: none)": {
            backgroundColor: "transparent"
          }
        },
        [`&.${switchClasses_default.disabled}`]: {
          color: theme.vars ? theme.vars.palette.Switch[`${color}DisabledColor`] : `${theme.palette.mode === "light" ? theme.lighten(theme.palette[color].main, 0.62) : theme.darken(theme.palette[color].main, 0.55)}`
        }
      },
      [`&.${switchClasses_default.checked} + .${switchClasses_default.track}`]: {
        backgroundColor: (theme.vars || theme).palette[color].main
      }
    }
  }))]
})));
var SwitchTrack = styled_default2("span", {
  name: "MuiSwitch",
  slot: "Track"
})(memoTheme_default(({
  theme
}) => ({
  height: "100%",
  width: "100%",
  borderRadius: 14 / 2,
  zIndex: -1,
  transition: theme.transitions.create(["opacity", "background-color"], {
    duration: theme.transitions.duration.shortest
  }),
  backgroundColor: theme.vars ? theme.vars.palette.common.onBackground : `${theme.palette.mode === "light" ? theme.palette.common.black : theme.palette.common.white}`,
  opacity: theme.vars ? theme.vars.opacity.switchTrack : `${theme.palette.mode === "light" ? 0.38 : 0.3}`
})));
var SwitchThumb = styled_default2("span", {
  name: "MuiSwitch",
  slot: "Thumb"
})(memoTheme_default(({
  theme
}) => ({
  boxShadow: (theme.vars || theme).shadows[1],
  backgroundColor: "currentColor",
  width: 20,
  height: 20,
  borderRadius: "50%"
})));
var Switch = React181.forwardRef(function Switch2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiSwitch"
  });
  const {
    className,
    color = "primary",
    edge = false,
    size = "medium",
    sx,
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    edge,
    size
  };
  const classes2 = useUtilityClasses80(ownerState);
  const externalForwardedProps = {
    slots,
    slotProps
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    className: clsx_default(classes2.root, className),
    elementType: SwitchRoot,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      sx
    }
  });
  const [ThumbSlot, thumbSlotProps] = useSlot("thumb", {
    className: classes2.thumb,
    elementType: SwitchThumb,
    externalForwardedProps,
    ownerState
  });
  const icon = (0, import_jsx_runtime151.jsx)(ThumbSlot, {
    ...thumbSlotProps
  });
  const [TrackSlot, trackSlotProps] = useSlot("track", {
    className: classes2.track,
    elementType: SwitchTrack,
    externalForwardedProps,
    ownerState
  });
  return (0, import_jsx_runtime151.jsxs)(RootSlot, {
    ...rootSlotProps,
    children: [(0, import_jsx_runtime151.jsx)(SwitchSwitchBase, {
      type: "checkbox",
      icon,
      checkedIcon: icon,
      ref,
      ownerState,
      ...other,
      classes: {
        ...classes2,
        root: classes2.switchBase
      },
      slots: {
        ...slots.switchBase && {
          root: slots.switchBase
        },
        ...slots.input && {
          input: slots.input
        }
      },
      slotProps: {
        ...slotProps.switchBase && {
          root: typeof slotProps.switchBase === "function" ? slotProps.switchBase(ownerState) : slotProps.switchBase
        },
        input: {
          role: "switch"
        },
        ...slotProps.input && {
          input: typeof slotProps.input === "function" ? slotProps.input(ownerState) : slotProps.input
        }
      }
    }), (0, import_jsx_runtime151.jsx)(TrackSlot, {
      ...trackSlotProps
    })]
  });
});
true ? Switch.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, the component is checked.
   */
  checked: import_prop_types124.default.bool,
  /**
   * The icon to display when the component is checked.
   */
  checkedIcon: import_prop_types124.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types124.default.object,
  /**
   * @ignore
   */
  className: import_prop_types124.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types124.default.oneOfType([import_prop_types124.default.oneOf(["default", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types124.default.string]),
  /**
   * The default checked state. Use when the component is not controlled.
   */
  defaultChecked: import_prop_types124.default.bool,
  /**
   * If `true`, the component is disabled.
   */
  disabled: import_prop_types124.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   * @default false
   */
  disableRipple: import_prop_types124.default.bool,
  /**
   * If given, uses a negative margin to counteract the padding on one
   * side (this is often helpful for aligning the left or right
   * side of the icon with content above or below, without ruining the border
   * size and shape).
   * @default false
   */
  edge: import_prop_types124.default.oneOf(["end", "start", false]),
  /**
   * The icon to display when the component is unchecked.
   */
  icon: import_prop_types124.default.node,
  /**
   * The id of the `input` element.
   */
  id: import_prop_types124.default.string,
  /**
   * [Attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input#attributes) applied to the `input` element.
   * @deprecated Use `slotProps.input` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  inputProps: import_prop_types124.default.object,
  /**
   * Pass a ref to the `input` element.
   * @deprecated Use `slotProps.input.ref` instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  inputRef: refType_default,
  /**
   * Callback fired when the state is changed.
   *
   * @param {React.ChangeEvent<HTMLInputElement>} event The event source of the callback.
   * You can pull out the new value by accessing `event.target.value` (string).
   * You can pull out the new checked state by accessing `event.target.checked` (boolean).
   */
  onChange: import_prop_types124.default.func,
  /**
   * If `true`, the `input` element is required.
   * @default false
   */
  required: import_prop_types124.default.bool,
  /**
   * The size of the component.
   * `small` is equivalent to the dense switch styling.
   * @default 'medium'
   */
  size: import_prop_types124.default.oneOfType([import_prop_types124.default.oneOf(["medium", "small"]), import_prop_types124.default.string]),
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types124.default.shape({
    input: import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object]),
    root: import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object]),
    switchBase: import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object]),
    thumb: import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object]),
    track: import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types124.default.shape({
    input: import_prop_types124.default.elementType,
    root: import_prop_types124.default.elementType,
    switchBase: import_prop_types124.default.elementType,
    thumb: import_prop_types124.default.elementType,
    track: import_prop_types124.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types124.default.oneOfType([import_prop_types124.default.arrayOf(import_prop_types124.default.oneOfType([import_prop_types124.default.func, import_prop_types124.default.object, import_prop_types124.default.bool])), import_prop_types124.default.func, import_prop_types124.default.object]),
  /**
   * The value of the component. The DOM API casts this to a string.
   * The browser uses "on" as the default value.
   */
  value: import_prop_types124.default.any
} : void 0;
var Switch_default = Switch;

// node_modules/@mui/material/esm/Button/Button.js
var React184 = __toESM(require_react(), 1);
var import_prop_types125 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Button/buttonClasses.js
function getButtonUtilityClass(slot) {
  return generateUtilityClass("MuiButton", slot);
}
var buttonClasses = generateUtilityClasses("MuiButton", ["root", "text", "textInherit", "textPrimary", "textSecondary", "textSuccess", "textError", "textInfo", "textWarning", "outlined", "outlinedInherit", "outlinedPrimary", "outlinedSecondary", "outlinedSuccess", "outlinedError", "outlinedInfo", "outlinedWarning", "contained", "containedInherit", "containedPrimary", "containedSecondary", "containedSuccess", "containedError", "containedInfo", "containedWarning", "disableElevation", "focusVisible", "disabled", "colorInherit", "colorPrimary", "colorSecondary", "colorSuccess", "colorError", "colorInfo", "colorWarning", "textSizeSmall", "textSizeMedium", "textSizeLarge", "outlinedSizeSmall", "outlinedSizeMedium", "outlinedSizeLarge", "containedSizeSmall", "containedSizeMedium", "containedSizeLarge", "sizeMedium", "sizeSmall", "sizeLarge", "fullWidth", "startIcon", "endIcon", "icon", "iconSizeSmall", "iconSizeMedium", "iconSizeLarge", "loading", "loadingWrapper", "loadingIconPlaceholder", "loadingIndicator", "loadingPositionCenter", "loadingPositionStart", "loadingPositionEnd"]);
var buttonClasses_default = buttonClasses;

// node_modules/@mui/material/esm/ButtonGroup/ButtonGroupContext.js
var React182 = __toESM(require_react(), 1);
var ButtonGroupContext = React182.createContext({});
if (true) {
  ButtonGroupContext.displayName = "ButtonGroupContext";
}
var ButtonGroupContext_default = ButtonGroupContext;

// node_modules/@mui/material/esm/ButtonGroup/ButtonGroupButtonContext.js
var React183 = __toESM(require_react(), 1);
var ButtonGroupButtonContext = React183.createContext(void 0);
if (true) {
  ButtonGroupButtonContext.displayName = "ButtonGroupButtonContext";
}
var ButtonGroupButtonContext_default = ButtonGroupButtonContext;

// node_modules/@mui/material/esm/Button/Button.js
var import_jsx_runtime152 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses81 = (ownerState) => {
  const {
    color,
    disableElevation,
    fullWidth,
    size,
    variant,
    loading,
    loadingPosition,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", loading && "loading", variant, `${variant}${capitalize_default(color)}`, `size${capitalize_default(size)}`, `${variant}Size${capitalize_default(size)}`, `color${capitalize_default(color)}`, disableElevation && "disableElevation", fullWidth && "fullWidth", loading && `loadingPosition${capitalize_default(loadingPosition)}`],
    startIcon: ["icon", "startIcon", `iconSize${capitalize_default(size)}`],
    endIcon: ["icon", "endIcon", `iconSize${capitalize_default(size)}`],
    loadingIndicator: ["loadingIndicator"],
    loadingWrapper: ["loadingWrapper"]
  };
  const composedClasses = composeClasses(slots, getButtonUtilityClass, classes2);
  return {
    ...classes2,
    // forward the focused, disabled, etc. classes to the ButtonBase
    ...composedClasses
  };
};
var commonIconStyles = [{
  props: {
    size: "small"
  },
  style: {
    "& > *:nth-of-type(1)": {
      fontSize: 18
    }
  }
}, {
  props: {
    size: "medium"
  },
  style: {
    "& > *:nth-of-type(1)": {
      fontSize: 20
    }
  }
}, {
  props: {
    size: "large"
  },
  style: {
    "& > *:nth-of-type(1)": {
      fontSize: 22
    }
  }
}];
var ButtonRoot = styled_default2(ButtonBase_default, {
  shouldForwardProp: (prop) => rootShouldForwardProp_default(prop) || prop === "classes",
  name: "MuiButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[ownerState.variant], styles5[`${ownerState.variant}${capitalize_default(ownerState.color)}`], styles5[`size${capitalize_default(ownerState.size)}`], styles5[`${ownerState.variant}Size${capitalize_default(ownerState.size)}`], ownerState.color === "inherit" && styles5.colorInherit, ownerState.disableElevation && styles5.disableElevation, ownerState.fullWidth && styles5.fullWidth, ownerState.loading && styles5.loading];
  }
})(memoTheme_default(({
  theme
}) => {
  const inheritContainedBackgroundColor = theme.palette.mode === "light" ? theme.palette.grey[300] : theme.palette.grey[800];
  const inheritContainedHoverBackgroundColor = theme.palette.mode === "light" ? theme.palette.grey.A100 : theme.palette.grey[700];
  return {
    ...theme.typography.button,
    minWidth: 64,
    padding: "6px 16px",
    border: 0,
    borderRadius: (theme.vars || theme).shape.borderRadius,
    transition: theme.transitions.create(["background-color", "box-shadow", "border-color", "color"], {
      duration: theme.transitions.duration.short
    }),
    "&:hover": {
      textDecoration: "none"
    },
    [`&.${buttonClasses_default.disabled}`]: {
      color: (theme.vars || theme).palette.action.disabled
    },
    variants: [{
      props: {
        variant: "contained"
      },
      style: {
        color: `var(--variant-containedColor)`,
        backgroundColor: `var(--variant-containedBg)`,
        boxShadow: (theme.vars || theme).shadows[2],
        "&:hover": {
          boxShadow: (theme.vars || theme).shadows[4],
          // Reset on touch devices, it doesn't add specificity
          "@media (hover: none)": {
            boxShadow: (theme.vars || theme).shadows[2]
          }
        },
        "&:active": {
          boxShadow: (theme.vars || theme).shadows[8]
        },
        [`&.${buttonClasses_default.focusVisible}`]: {
          boxShadow: (theme.vars || theme).shadows[6]
        },
        [`&.${buttonClasses_default.disabled}`]: {
          color: (theme.vars || theme).palette.action.disabled,
          boxShadow: (theme.vars || theme).shadows[0],
          backgroundColor: (theme.vars || theme).palette.action.disabledBackground
        }
      }
    }, {
      props: {
        variant: "outlined"
      },
      style: {
        padding: "5px 15px",
        border: "1px solid currentColor",
        borderColor: `var(--variant-outlinedBorder, currentColor)`,
        backgroundColor: `var(--variant-outlinedBg)`,
        color: `var(--variant-outlinedColor)`,
        [`&.${buttonClasses_default.disabled}`]: {
          border: `1px solid ${(theme.vars || theme).palette.action.disabledBackground}`
        }
      }
    }, {
      props: {
        variant: "text"
      },
      style: {
        padding: "6px 8px",
        color: `var(--variant-textColor)`,
        backgroundColor: `var(--variant-textBg)`
      }
    }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
      props: {
        color
      },
      style: {
        "--variant-textColor": (theme.vars || theme).palette[color].main,
        "--variant-outlinedColor": (theme.vars || theme).palette[color].main,
        "--variant-outlinedBorder": theme.alpha((theme.vars || theme).palette[color].main, 0.5),
        "--variant-containedColor": (theme.vars || theme).palette[color].contrastText,
        "--variant-containedBg": (theme.vars || theme).palette[color].main,
        "@media (hover: hover)": {
          "&:hover": {
            "--variant-containedBg": (theme.vars || theme).palette[color].dark,
            "--variant-textBg": theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity),
            "--variant-outlinedBorder": (theme.vars || theme).palette[color].main,
            "--variant-outlinedBg": theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.hoverOpacity)
          }
        }
      }
    })), {
      props: {
        color: "inherit"
      },
      style: {
        color: "inherit",
        borderColor: "currentColor",
        "--variant-containedBg": theme.vars ? theme.vars.palette.Button.inheritContainedBg : inheritContainedBackgroundColor,
        "@media (hover: hover)": {
          "&:hover": {
            "--variant-containedBg": theme.vars ? theme.vars.palette.Button.inheritContainedHoverBg : inheritContainedHoverBackgroundColor,
            "--variant-textBg": theme.alpha((theme.vars || theme).palette.text.primary, (theme.vars || theme).palette.action.hoverOpacity),
            "--variant-outlinedBg": theme.alpha((theme.vars || theme).palette.text.primary, (theme.vars || theme).palette.action.hoverOpacity)
          }
        }
      }
    }, {
      props: {
        size: "small",
        variant: "text"
      },
      style: {
        padding: "4px 5px",
        fontSize: theme.typography.pxToRem(13)
      }
    }, {
      props: {
        size: "large",
        variant: "text"
      },
      style: {
        padding: "8px 11px",
        fontSize: theme.typography.pxToRem(15)
      }
    }, {
      props: {
        size: "small",
        variant: "outlined"
      },
      style: {
        padding: "3px 9px",
        fontSize: theme.typography.pxToRem(13)
      }
    }, {
      props: {
        size: "large",
        variant: "outlined"
      },
      style: {
        padding: "7px 21px",
        fontSize: theme.typography.pxToRem(15)
      }
    }, {
      props: {
        size: "small",
        variant: "contained"
      },
      style: {
        padding: "4px 10px",
        fontSize: theme.typography.pxToRem(13)
      }
    }, {
      props: {
        size: "large",
        variant: "contained"
      },
      style: {
        padding: "8px 22px",
        fontSize: theme.typography.pxToRem(15)
      }
    }, {
      props: {
        disableElevation: true
      },
      style: {
        boxShadow: "none",
        "&:hover": {
          boxShadow: "none"
        },
        [`&.${buttonClasses_default.focusVisible}`]: {
          boxShadow: "none"
        },
        "&:active": {
          boxShadow: "none"
        },
        [`&.${buttonClasses_default.disabled}`]: {
          boxShadow: "none"
        }
      }
    }, {
      props: {
        fullWidth: true
      },
      style: {
        width: "100%"
      }
    }, {
      props: {
        loadingPosition: "center"
      },
      style: {
        transition: theme.transitions.create(["background-color", "box-shadow", "border-color"], {
          duration: theme.transitions.duration.short
        }),
        [`&.${buttonClasses_default.loading}`]: {
          color: "transparent"
        }
      }
    }]
  };
}));
var ButtonStartIcon = styled_default2("span", {
  name: "MuiButton",
  slot: "StartIcon",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.startIcon, ownerState.loading && styles5.startIconLoadingStart, styles5[`iconSize${capitalize_default(ownerState.size)}`]];
  }
})(({
  theme
}) => ({
  display: "inherit",
  marginRight: 8,
  marginLeft: -4,
  variants: [{
    props: {
      size: "small"
    },
    style: {
      marginLeft: -2
    }
  }, {
    props: {
      loadingPosition: "start",
      loading: true
    },
    style: {
      transition: theme.transitions.create(["opacity"], {
        duration: theme.transitions.duration.short
      }),
      opacity: 0
    }
  }, {
    props: {
      loadingPosition: "start",
      loading: true,
      fullWidth: true
    },
    style: {
      marginRight: -8
    }
  }, ...commonIconStyles]
}));
var ButtonEndIcon = styled_default2("span", {
  name: "MuiButton",
  slot: "EndIcon",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.endIcon, ownerState.loading && styles5.endIconLoadingEnd, styles5[`iconSize${capitalize_default(ownerState.size)}`]];
  }
})(({
  theme
}) => ({
  display: "inherit",
  marginRight: -4,
  marginLeft: 8,
  variants: [{
    props: {
      size: "small"
    },
    style: {
      marginRight: -2
    }
  }, {
    props: {
      loadingPosition: "end",
      loading: true
    },
    style: {
      transition: theme.transitions.create(["opacity"], {
        duration: theme.transitions.duration.short
      }),
      opacity: 0
    }
  }, {
    props: {
      loadingPosition: "end",
      loading: true,
      fullWidth: true
    },
    style: {
      marginLeft: -8
    }
  }, ...commonIconStyles]
}));
var ButtonLoadingIndicator = styled_default2("span", {
  name: "MuiButton",
  slot: "LoadingIndicator"
})(({
  theme
}) => ({
  display: "none",
  position: "absolute",
  visibility: "visible",
  variants: [{
    props: {
      loading: true
    },
    style: {
      display: "flex"
    }
  }, {
    props: {
      loadingPosition: "start"
    },
    style: {
      left: 14
    }
  }, {
    props: {
      loadingPosition: "start",
      size: "small"
    },
    style: {
      left: 10
    }
  }, {
    props: {
      variant: "text",
      loadingPosition: "start"
    },
    style: {
      left: 6
    }
  }, {
    props: {
      loadingPosition: "center"
    },
    style: {
      left: "50%",
      transform: "translate(-50%)",
      color: (theme.vars || theme).palette.action.disabled
    }
  }, {
    props: {
      loadingPosition: "end"
    },
    style: {
      right: 14
    }
  }, {
    props: {
      loadingPosition: "end",
      size: "small"
    },
    style: {
      right: 10
    }
  }, {
    props: {
      variant: "text",
      loadingPosition: "end"
    },
    style: {
      right: 6
    }
  }, {
    props: {
      loadingPosition: "start",
      fullWidth: true
    },
    style: {
      position: "relative",
      left: -10
    }
  }, {
    props: {
      loadingPosition: "end",
      fullWidth: true
    },
    style: {
      position: "relative",
      right: -10
    }
  }]
}));
var ButtonLoadingIconPlaceholder = styled_default2("span", {
  name: "MuiButton",
  slot: "LoadingIconPlaceholder"
})({
  display: "inline-block",
  width: "1em",
  height: "1em"
});
var Button = React184.forwardRef(function Button2(inProps, ref) {
  const contextProps = React184.useContext(ButtonGroupContext_default);
  const buttonGroupButtonContextPositionClassName = React184.useContext(ButtonGroupButtonContext_default);
  const resolvedProps = resolveProps(contextProps, inProps);
  const props = useDefaultProps({
    props: resolvedProps,
    name: "MuiButton"
  });
  const {
    children: children2,
    color = "primary",
    component = "button",
    className,
    disabled = false,
    disableElevation = false,
    disableFocusRipple = false,
    endIcon: endIconProp,
    focusVisibleClassName,
    fullWidth = false,
    id: idProp,
    loading = null,
    loadingIndicator: loadingIndicatorProp,
    loadingPosition = "center",
    size = "medium",
    startIcon: startIconProp,
    type,
    variant = "text",
    ...other
  } = props;
  const loadingId = useId_default(idProp);
  const loadingIndicator = loadingIndicatorProp ?? (0, import_jsx_runtime152.jsx)(CircularProgress_default, {
    "aria-labelledby": loadingId,
    color: "inherit",
    size: 16
  });
  const ownerState = {
    ...props,
    color,
    component,
    disabled,
    disableElevation,
    disableFocusRipple,
    fullWidth,
    loading,
    loadingIndicator,
    loadingPosition,
    size,
    type,
    variant
  };
  const classes2 = useUtilityClasses81(ownerState);
  const startIcon = (startIconProp || loading && loadingPosition === "start") && (0, import_jsx_runtime152.jsx)(ButtonStartIcon, {
    className: classes2.startIcon,
    ownerState,
    children: startIconProp || (0, import_jsx_runtime152.jsx)(ButtonLoadingIconPlaceholder, {
      className: classes2.loadingIconPlaceholder,
      ownerState
    })
  });
  const endIcon = (endIconProp || loading && loadingPosition === "end") && (0, import_jsx_runtime152.jsx)(ButtonEndIcon, {
    className: classes2.endIcon,
    ownerState,
    children: endIconProp || (0, import_jsx_runtime152.jsx)(ButtonLoadingIconPlaceholder, {
      className: classes2.loadingIconPlaceholder,
      ownerState
    })
  });
  const positionClassName = buttonGroupButtonContextPositionClassName || "";
  const loader = typeof loading === "boolean" ? (
    // use plain HTML span to minimize the runtime overhead
    (0, import_jsx_runtime152.jsx)("span", {
      className: classes2.loadingWrapper,
      style: {
        display: "contents"
      },
      children: loading && (0, import_jsx_runtime152.jsx)(ButtonLoadingIndicator, {
        className: classes2.loadingIndicator,
        ownerState,
        children: loadingIndicator
      })
    })
  ) : null;
  return (0, import_jsx_runtime152.jsxs)(ButtonRoot, {
    ownerState,
    className: clsx_default(contextProps.className, classes2.root, className, positionClassName),
    component,
    disabled: disabled || loading,
    focusRipple: !disableFocusRipple,
    focusVisibleClassName: clsx_default(classes2.focusVisible, focusVisibleClassName),
    ref,
    type,
    id: loading ? loadingId : idProp,
    ...other,
    classes: classes2,
    children: [startIcon, loadingPosition !== "end" && loader, children2, loadingPosition === "end" && loader, endIcon]
  });
});
true ? Button.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types125.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types125.default.object,
  /**
   * @ignore
   */
  className: import_prop_types125.default.string,
  /**
   * The color of the component.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'primary'
   */
  color: import_prop_types125.default.oneOfType([import_prop_types125.default.oneOf(["inherit", "primary", "secondary", "success", "error", "info", "warning"]), import_prop_types125.default.string]),
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types125.default.elementType,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types125.default.bool,
  /**
   * If `true`, no elevation is used.
   * @default false
   */
  disableElevation: import_prop_types125.default.bool,
  /**
   * If `true`, the  keyboard focus ripple is disabled.
   * @default false
   */
  disableFocusRipple: import_prop_types125.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   *
   * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
   * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
   * @default false
   */
  disableRipple: import_prop_types125.default.bool,
  /**
   * Element placed after the children.
   */
  endIcon: import_prop_types125.default.node,
  /**
   * @ignore
   */
  focusVisibleClassName: import_prop_types125.default.string,
  /**
   * If `true`, the button will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types125.default.bool,
  /**
   * The URL to link to when the button is clicked.
   * If defined, an `a` element will be used as the root node.
   */
  href: import_prop_types125.default.string,
  /**
   * @ignore
   */
  id: import_prop_types125.default.string,
  /**
   * If `true`, the loading indicator is visible and the button is disabled.
   * If `true | false`, the loading wrapper is always rendered before the children to prevent [Google Translation Crash](https://github.com/mui/material-ui/issues/27853).
   * @default null
   */
  loading: import_prop_types125.default.bool,
  /**
   * Element placed before the children if the button is in loading state.
   * The node should contain an element with `role="progressbar"` with an accessible name.
   * By default, it renders a `CircularProgress` that is labeled by the button itself.
   * @default <CircularProgress color="inherit" size={16} />
   */
  loadingIndicator: import_prop_types125.default.node,
  /**
   * The loading indicator can be positioned on the start, end, or the center of the button.
   * @default 'center'
   */
  loadingPosition: import_prop_types125.default.oneOf(["center", "end", "start"]),
  /**
   * The size of the component.
   * `small` is equivalent to the dense button styling.
   * @default 'medium'
   */
  size: import_prop_types125.default.oneOfType([import_prop_types125.default.oneOf(["small", "medium", "large"]), import_prop_types125.default.string]),
  /**
   * Element placed before the children.
   */
  startIcon: import_prop_types125.default.node,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types125.default.oneOfType([import_prop_types125.default.arrayOf(import_prop_types125.default.oneOfType([import_prop_types125.default.func, import_prop_types125.default.object, import_prop_types125.default.bool])), import_prop_types125.default.func, import_prop_types125.default.object]),
  /**
   * @ignore
   */
  type: import_prop_types125.default.oneOfType([import_prop_types125.default.oneOf(["button", "reset", "submit"]), import_prop_types125.default.string]),
  /**
   * The variant to use.
   * @default 'text'
   */
  variant: import_prop_types125.default.oneOfType([import_prop_types125.default.oneOf(["contained", "outlined", "text"]), import_prop_types125.default.string])
} : void 0;
var Button_default = Button;

// node_modules/@mui/material/esm/InputAdornment/InputAdornment.js
var React185 = __toESM(require_react(), 1);
var import_prop_types126 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/InputAdornment/inputAdornmentClasses.js
function getInputAdornmentUtilityClass(slot) {
  return generateUtilityClass("MuiInputAdornment", slot);
}
var inputAdornmentClasses = generateUtilityClasses("MuiInputAdornment", ["root", "filled", "standard", "outlined", "positionStart", "positionEnd", "disablePointerEvents", "hiddenLabel", "sizeSmall"]);
var inputAdornmentClasses_default = inputAdornmentClasses;

// node_modules/@mui/material/esm/InputAdornment/InputAdornment.js
var import_jsx_runtime153 = __toESM(require_jsx_runtime(), 1);
var _span4;
var overridesResolver2 = (props, styles5) => {
  const {
    ownerState
  } = props;
  return [styles5.root, styles5[`position${capitalize_default(ownerState.position)}`], ownerState.disablePointerEvents === true && styles5.disablePointerEvents, styles5[ownerState.variant]];
};
var useUtilityClasses82 = (ownerState) => {
  const {
    classes: classes2,
    disablePointerEvents,
    hiddenLabel,
    position,
    size,
    variant
  } = ownerState;
  const slots = {
    root: ["root", disablePointerEvents && "disablePointerEvents", position && `position${capitalize_default(position)}`, variant, hiddenLabel && "hiddenLabel", size && `size${capitalize_default(size)}`]
  };
  return composeClasses(slots, getInputAdornmentUtilityClass, classes2);
};
var InputAdornmentRoot = styled_default2("div", {
  name: "MuiInputAdornment",
  slot: "Root",
  overridesResolver: overridesResolver2
})(memoTheme_default(({
  theme
}) => ({
  display: "flex",
  maxHeight: "2em",
  alignItems: "center",
  whiteSpace: "nowrap",
  color: (theme.vars || theme).palette.action.active,
  variants: [{
    props: {
      variant: "filled"
    },
    style: {
      [`&.${inputAdornmentClasses_default.positionStart}&:not(.${inputAdornmentClasses_default.hiddenLabel})`]: {
        marginTop: 16
      }
    }
  }, {
    props: {
      position: "start"
    },
    style: {
      marginRight: 8
    }
  }, {
    props: {
      position: "end"
    },
    style: {
      marginLeft: 8
    }
  }, {
    props: {
      disablePointerEvents: true
    },
    style: {
      pointerEvents: "none"
    }
  }]
})));
var InputAdornment = React185.forwardRef(function InputAdornment2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiInputAdornment"
  });
  const {
    children: children2,
    className,
    component = "div",
    disablePointerEvents = false,
    disableTypography = false,
    position,
    variant: variantProp,
    ...other
  } = props;
  const muiFormControl = useFormControl() || {};
  let variant = variantProp;
  if (variantProp && muiFormControl.variant) {
    if (true) {
      if (variantProp === muiFormControl.variant) {
        console.error("MUI: The `InputAdornment` variant infers the variant prop you do not have to provide one.");
      }
    }
  }
  if (muiFormControl && !variant) {
    variant = muiFormControl.variant;
  }
  const ownerState = {
    ...props,
    hiddenLabel: muiFormControl.hiddenLabel,
    size: muiFormControl.size,
    disablePointerEvents,
    position,
    variant
  };
  const classes2 = useUtilityClasses82(ownerState);
  return (0, import_jsx_runtime153.jsx)(FormControlContext_default.Provider, {
    value: null,
    children: (0, import_jsx_runtime153.jsx)(InputAdornmentRoot, {
      as: component,
      ownerState,
      className: clsx_default(classes2.root, className),
      ref,
      ...other,
      children: typeof children2 === "string" && !disableTypography ? (0, import_jsx_runtime153.jsx)(Typography_default, {
        color: "textSecondary",
        children: children2
      }) : (0, import_jsx_runtime153.jsxs)(React185.Fragment, {
        children: [position === "start" ? (
          /* notranslate needed while Google Translate will not fix zero-width space issue */
          _span4 || (_span4 = (0, import_jsx_runtime153.jsx)("span", {
            className: "notranslate",
            "aria-hidden": true,
            children: "​"
          }))
        ) : null, children2]
      })
    })
  });
});
true ? InputAdornment.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component, normally an `IconButton` or string.
   */
  children: import_prop_types126.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types126.default.object,
  /**
   * @ignore
   */
  className: import_prop_types126.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types126.default.elementType,
  /**
   * Disable pointer events on the root.
   * This allows for the content of the adornment to focus the `input` on click.
   * @default false
   */
  disablePointerEvents: import_prop_types126.default.bool,
  /**
   * If children is a string then disable wrapping in a Typography component.
   * @default false
   */
  disableTypography: import_prop_types126.default.bool,
  /**
   * The position this adornment should appear relative to the `Input`.
   */
  position: import_prop_types126.default.oneOf(["end", "start"]).isRequired,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types126.default.oneOfType([import_prop_types126.default.arrayOf(import_prop_types126.default.oneOfType([import_prop_types126.default.func, import_prop_types126.default.object, import_prop_types126.default.bool])), import_prop_types126.default.func, import_prop_types126.default.object]),
  /**
   * The variant to use.
   * Note: If you are using the `TextField` component or the `FormControl` component
   * you do not have to set this manually.
   */
  variant: import_prop_types126.default.oneOf(["filled", "outlined", "standard"])
} : void 0;
var InputAdornment_default = InputAdornment;

// node_modules/@mui/material/esm/Tooltip/Tooltip.js
var React186 = __toESM(require_react(), 1);
var import_prop_types127 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Tooltip/tooltipClasses.js
function getTooltipUtilityClass(slot) {
  return generateUtilityClass("MuiTooltip", slot);
}
var tooltipClasses = generateUtilityClasses("MuiTooltip", ["popper", "popperInteractive", "popperArrow", "popperClose", "tooltip", "tooltipArrow", "touch", "tooltipPlacementLeft", "tooltipPlacementRight", "tooltipPlacementTop", "tooltipPlacementBottom", "arrow"]);
var tooltipClasses_default = tooltipClasses;

// node_modules/@mui/material/esm/Tooltip/Tooltip.js
var import_jsx_runtime154 = __toESM(require_jsx_runtime(), 1);
function round2(value) {
  return Math.round(value * 1e5) / 1e5;
}
var useUtilityClasses83 = (ownerState) => {
  const {
    classes: classes2,
    disableInteractive,
    arrow: arrow2,
    touch,
    placement
  } = ownerState;
  const slots = {
    popper: ["popper", !disableInteractive && "popperInteractive", arrow2 && "popperArrow"],
    tooltip: ["tooltip", arrow2 && "tooltipArrow", touch && "touch", `tooltipPlacement${capitalize_default(placement.split("-")[0])}`],
    arrow: ["arrow"]
  };
  return composeClasses(slots, getTooltipUtilityClass, classes2);
};
var TooltipPopper = styled_default2(Popper_default, {
  name: "MuiTooltip",
  slot: "Popper",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.popper, !ownerState.disableInteractive && styles5.popperInteractive, ownerState.arrow && styles5.popperArrow, !ownerState.open && styles5.popperClose];
  }
})(memoTheme_default(({
  theme
}) => ({
  zIndex: (theme.vars || theme).zIndex.tooltip,
  pointerEvents: "none",
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.disableInteractive,
    style: {
      pointerEvents: "auto"
    }
  }, {
    props: ({
      open
    }) => !open,
    style: {
      pointerEvents: "none"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.arrow,
    style: {
      [`&[data-popper-placement*="bottom"] .${tooltipClasses_default.arrow}`]: {
        top: 0,
        marginTop: "-0.71em",
        "&::before": {
          transformOrigin: "0 100%"
        }
      },
      [`&[data-popper-placement*="top"] .${tooltipClasses_default.arrow}`]: {
        bottom: 0,
        marginBottom: "-0.71em",
        "&::before": {
          transformOrigin: "100% 0"
        }
      },
      [`&[data-popper-placement*="right"] .${tooltipClasses_default.arrow}`]: {
        height: "1em",
        width: "0.71em",
        "&::before": {
          transformOrigin: "100% 100%"
        }
      },
      [`&[data-popper-placement*="left"] .${tooltipClasses_default.arrow}`]: {
        height: "1em",
        width: "0.71em",
        "&::before": {
          transformOrigin: "0 0"
        }
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.arrow && !ownerState.isRtl,
    style: {
      [`&[data-popper-placement*="right"] .${tooltipClasses_default.arrow}`]: {
        left: 0,
        marginLeft: "-0.71em"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.arrow && !!ownerState.isRtl,
    style: {
      [`&[data-popper-placement*="right"] .${tooltipClasses_default.arrow}`]: {
        right: 0,
        marginRight: "-0.71em"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.arrow && !ownerState.isRtl,
    style: {
      [`&[data-popper-placement*="left"] .${tooltipClasses_default.arrow}`]: {
        right: 0,
        marginRight: "-0.71em"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.arrow && !!ownerState.isRtl,
    style: {
      [`&[data-popper-placement*="left"] .${tooltipClasses_default.arrow}`]: {
        left: 0,
        marginLeft: "-0.71em"
      }
    }
  }]
})));
var TooltipTooltip = styled_default2("div", {
  name: "MuiTooltip",
  slot: "Tooltip",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.tooltip, ownerState.touch && styles5.touch, ownerState.arrow && styles5.tooltipArrow, styles5[`tooltipPlacement${capitalize_default(ownerState.placement.split("-")[0])}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  backgroundColor: theme.vars ? theme.vars.palette.Tooltip.bg : theme.alpha(theme.palette.grey[700], 0.92),
  borderRadius: (theme.vars || theme).shape.borderRadius,
  color: (theme.vars || theme).palette.common.white,
  fontFamily: theme.typography.fontFamily,
  padding: "4px 8px",
  fontSize: theme.typography.pxToRem(11),
  maxWidth: 300,
  margin: 2,
  wordWrap: "break-word",
  fontWeight: theme.typography.fontWeightMedium,
  [`.${tooltipClasses_default.popper}[data-popper-placement*="left"] &`]: {
    transformOrigin: "right center"
  },
  [`.${tooltipClasses_default.popper}[data-popper-placement*="right"] &`]: {
    transformOrigin: "left center"
  },
  [`.${tooltipClasses_default.popper}[data-popper-placement*="top"] &`]: {
    transformOrigin: "center bottom",
    marginBottom: "14px"
  },
  [`.${tooltipClasses_default.popper}[data-popper-placement*="bottom"] &`]: {
    transformOrigin: "center top",
    marginTop: "14px"
  },
  variants: [{
    props: ({
      ownerState
    }) => ownerState.arrow,
    style: {
      position: "relative",
      margin: 0
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.touch,
    style: {
      padding: "8px 16px",
      fontSize: theme.typography.pxToRem(14),
      lineHeight: `${round2(16 / 14)}em`,
      fontWeight: theme.typography.fontWeightRegular
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.isRtl,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="left"] &`]: {
        marginRight: "14px"
      },
      [`.${tooltipClasses_default.popper}[data-popper-placement*="right"] &`]: {
        marginLeft: "14px"
      }
    }
  }, {
    props: ({
      ownerState
    }) => !ownerState.isRtl && ownerState.touch,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="left"] &`]: {
        marginRight: "24px"
      },
      [`.${tooltipClasses_default.popper}[data-popper-placement*="right"] &`]: {
        marginLeft: "24px"
      }
    }
  }, {
    props: ({
      ownerState
    }) => !!ownerState.isRtl,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="left"] &`]: {
        marginLeft: "14px"
      },
      [`.${tooltipClasses_default.popper}[data-popper-placement*="right"] &`]: {
        marginRight: "14px"
      }
    }
  }, {
    props: ({
      ownerState
    }) => !!ownerState.isRtl && ownerState.touch,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="left"] &`]: {
        marginLeft: "24px"
      },
      [`.${tooltipClasses_default.popper}[data-popper-placement*="right"] &`]: {
        marginRight: "24px"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.touch,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="top"] &`]: {
        marginBottom: "24px"
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.touch,
    style: {
      [`.${tooltipClasses_default.popper}[data-popper-placement*="bottom"] &`]: {
        marginTop: "24px"
      }
    }
  }]
})));
var TooltipArrow = styled_default2("span", {
  name: "MuiTooltip",
  slot: "Arrow"
})(memoTheme_default(({
  theme
}) => ({
  overflow: "hidden",
  position: "absolute",
  width: "1em",
  height: "0.71em",
  boxSizing: "border-box",
  color: theme.vars ? theme.vars.palette.Tooltip.bg : theme.alpha(theme.palette.grey[700], 0.9),
  "&::before": {
    content: '""',
    margin: "auto",
    display: "block",
    width: "100%",
    height: "100%",
    backgroundColor: "currentColor",
    transform: "rotate(45deg)"
  }
})));
var hystersisOpen = false;
var hystersisTimer = new Timeout();
var cursorPosition = {
  x: 0,
  y: 0
};
function composeEventHandler(handler, eventHandler) {
  return (event, ...params) => {
    if (eventHandler) {
      eventHandler(event, ...params);
    }
    handler(event, ...params);
  };
}
var Tooltip = React186.forwardRef(function Tooltip2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTooltip"
  });
  const {
    arrow: arrow2 = false,
    children: childrenProp,
    classes: classesProp,
    components = {},
    componentsProps = {},
    describeChild = false,
    disableFocusListener = false,
    disableHoverListener = false,
    disableInteractive: disableInteractiveProp = false,
    disableTouchListener = false,
    enterDelay = 100,
    enterNextDelay = 0,
    enterTouchDelay = 700,
    followCursor = false,
    id: idProp,
    leaveDelay = 0,
    leaveTouchDelay = 1500,
    onClose,
    onOpen,
    open: openProp,
    placement = "bottom",
    PopperComponent: PopperComponentProp,
    PopperProps = {},
    slotProps = {},
    slots = {},
    title,
    TransitionComponent: TransitionComponentProp,
    TransitionProps,
    ...other
  } = props;
  const children2 = React186.isValidElement(childrenProp) ? childrenProp : (0, import_jsx_runtime154.jsx)("span", {
    children: childrenProp
  });
  const theme = useTheme();
  const isRtl = useRtl();
  const [childNode, setChildNode] = React186.useState();
  const [arrowRef, setArrowRef] = React186.useState(null);
  const ignoreNonTouchEvents = React186.useRef(false);
  const disableInteractive = disableInteractiveProp || followCursor;
  const closeTimer = useTimeout();
  const enterTimer = useTimeout();
  const leaveTimer = useTimeout();
  const touchTimer = useTimeout();
  const [openState, setOpenState] = useControlled_default({
    controlled: openProp,
    default: false,
    name: "Tooltip",
    state: "open"
  });
  let open = openState;
  if (true) {
    const {
      current: isControlled
    } = React186.useRef(openProp !== void 0);
    React186.useEffect(() => {
      if (childNode && childNode.disabled && !isControlled && title !== "" && childNode.tagName.toLowerCase() === "button") {
        console.warn(["MUI: You are providing a disabled `button` child to the Tooltip component.", "A disabled element does not fire events.", "Tooltip needs to listen to the child element's events to display the title.", "", "Add a simple wrapper element, such as a `span`."].join("\n"));
      }
    }, [title, childNode, isControlled]);
  }
  const id = useId_default(idProp);
  const prevUserSelect = React186.useRef();
  const stopTouchInteraction = useEventCallback_default2(() => {
    if (prevUserSelect.current !== void 0) {
      document.body.style.WebkitUserSelect = prevUserSelect.current;
      prevUserSelect.current = void 0;
    }
    touchTimer.clear();
  });
  React186.useEffect(() => stopTouchInteraction, [stopTouchInteraction]);
  const handleOpen = (event) => {
    hystersisTimer.clear();
    hystersisOpen = true;
    setOpenState(true);
    if (onOpen && !open) {
      onOpen(event);
    }
  };
  const handleClose = useEventCallback_default2(
    /**
     * @param {React.SyntheticEvent | Event} event
     */
    (event) => {
      hystersisTimer.start(800 + leaveDelay, () => {
        hystersisOpen = false;
      });
      setOpenState(false);
      if (onClose && open) {
        onClose(event);
      }
      closeTimer.start(theme.transitions.duration.shortest, () => {
        ignoreNonTouchEvents.current = false;
      });
    }
  );
  const handleMouseOver = (event) => {
    if (ignoreNonTouchEvents.current && event.type !== "touchstart") {
      return;
    }
    if (childNode) {
      childNode.removeAttribute("title");
    }
    enterTimer.clear();
    leaveTimer.clear();
    if (enterDelay || hystersisOpen && enterNextDelay) {
      enterTimer.start(hystersisOpen ? enterNextDelay : enterDelay, () => {
        handleOpen(event);
      });
    } else {
      handleOpen(event);
    }
  };
  const handleMouseLeave = (event) => {
    enterTimer.clear();
    leaveTimer.start(leaveDelay, () => {
      handleClose(event);
    });
  };
  const [, setChildIsFocusVisible] = React186.useState(false);
  const handleBlur = (event) => {
    const target = (event == null ? void 0 : event.target) ?? childNode;
    if (!target || !isFocusVisible(target)) {
      setChildIsFocusVisible(false);
      const closeEvent = event ?? new Event("blur");
      if (!event && target) {
        Object.defineProperty(closeEvent, "target", {
          value: target
        });
        Object.defineProperty(closeEvent, "currentTarget", {
          value: target
        });
      }
      handleMouseLeave(closeEvent);
    }
  };
  const handleFocus = (event) => {
    if (!childNode) {
      setChildNode(event.currentTarget);
    }
    if (isFocusVisible(event.target)) {
      setChildIsFocusVisible(true);
      handleMouseOver(event);
    }
  };
  const detectTouchStart = (event) => {
    ignoreNonTouchEvents.current = true;
    const childrenProps2 = children2.props;
    if (childrenProps2.onTouchStart) {
      childrenProps2.onTouchStart(event);
    }
  };
  const handleTouchStart = (event) => {
    detectTouchStart(event);
    leaveTimer.clear();
    closeTimer.clear();
    stopTouchInteraction();
    prevUserSelect.current = document.body.style.WebkitUserSelect;
    document.body.style.WebkitUserSelect = "none";
    touchTimer.start(enterTouchDelay, () => {
      document.body.style.WebkitUserSelect = prevUserSelect.current;
      handleMouseOver(event);
    });
  };
  const handleTouchEnd = (event) => {
    if (children2.props.onTouchEnd) {
      children2.props.onTouchEnd(event);
    }
    stopTouchInteraction();
    leaveTimer.start(leaveTouchDelay, () => {
      handleClose(event);
    });
  };
  React186.useEffect(() => {
    if (!open) {
      return void 0;
    }
    function handleKeyDown(nativeEvent) {
      if (nativeEvent.key === "Escape") {
        handleClose(nativeEvent);
      }
    }
    document.addEventListener("keydown", handleKeyDown);
    return () => {
      document.removeEventListener("keydown", handleKeyDown);
    };
  }, [handleClose, open]);
  const handleRef = useForkRef_default(getReactElementRef(children2), setChildNode, ref);
  if (!title && title !== 0) {
    open = false;
  }
  const popperRef = React186.useRef();
  const handleMouseMove = (event) => {
    const childrenProps2 = children2.props;
    if (childrenProps2.onMouseMove) {
      childrenProps2.onMouseMove(event);
    }
    cursorPosition = {
      x: event.clientX,
      y: event.clientY
    };
    if (popperRef.current) {
      popperRef.current.update();
    }
  };
  const nameOrDescProps = {};
  const titleIsString = typeof title === "string";
  if (describeChild) {
    nameOrDescProps.title = !open && titleIsString && !disableHoverListener ? title : null;
    nameOrDescProps["aria-describedby"] = open ? id : null;
  } else {
    nameOrDescProps["aria-label"] = titleIsString ? title : null;
    nameOrDescProps["aria-labelledby"] = open && !titleIsString ? id : null;
  }
  const childrenProps = {
    ...nameOrDescProps,
    ...other,
    ...children2.props,
    className: clsx_default(other.className, children2.props.className),
    onTouchStart: detectTouchStart,
    ref: handleRef,
    ...followCursor ? {
      onMouseMove: handleMouseMove
    } : {}
  };
  if (true) {
    childrenProps["data-mui-internal-clone-element"] = true;
    React186.useEffect(() => {
      if (childNode && !childNode.getAttribute("data-mui-internal-clone-element")) {
        console.error(["MUI: The `children` component of the Tooltip is not forwarding its props correctly.", "Please make sure that props are spread on the same element that the ref is applied to."].join("\n"));
      }
    }, [childNode]);
  }
  const interactiveWrapperListeners = {};
  if (!disableTouchListener) {
    childrenProps.onTouchStart = handleTouchStart;
    childrenProps.onTouchEnd = handleTouchEnd;
  }
  if (!disableHoverListener) {
    childrenProps.onMouseOver = composeEventHandler(handleMouseOver, childrenProps.onMouseOver);
    childrenProps.onMouseLeave = composeEventHandler(handleMouseLeave, childrenProps.onMouseLeave);
    if (!disableInteractive) {
      interactiveWrapperListeners.onMouseOver = handleMouseOver;
      interactiveWrapperListeners.onMouseLeave = handleMouseLeave;
    }
  }
  if (!disableFocusListener) {
    childrenProps.onFocus = composeEventHandler(handleFocus, childrenProps.onFocus);
    childrenProps.onBlur = composeEventHandler(handleBlur, childrenProps.onBlur);
    if (!disableInteractive) {
      interactiveWrapperListeners.onFocus = handleFocus;
      interactiveWrapperListeners.onBlur = handleBlur;
    }
  }
  if (true) {
    if (children2.props.title) {
      console.error(["MUI: You have provided a `title` prop to the child of <Tooltip />.", `Remove this title prop \`${children2.props.title}\` or the Tooltip component.`].join("\n"));
    }
  }
  const ownerState = {
    ...props,
    isRtl,
    arrow: arrow2,
    disableInteractive,
    placement,
    PopperComponentProp,
    touch: ignoreNonTouchEvents.current
  };
  const resolvedPopperProps = typeof slotProps.popper === "function" ? slotProps.popper(ownerState) : slotProps.popper;
  const popperOptions = React186.useMemo(() => {
    var _a, _b;
    let tooltipModifiers = [{
      name: "arrow",
      enabled: Boolean(arrowRef),
      options: {
        element: arrowRef,
        padding: 4
      }
    }];
    if ((_a = PopperProps.popperOptions) == null ? void 0 : _a.modifiers) {
      tooltipModifiers = tooltipModifiers.concat(PopperProps.popperOptions.modifiers);
    }
    if ((_b = resolvedPopperProps == null ? void 0 : resolvedPopperProps.popperOptions) == null ? void 0 : _b.modifiers) {
      tooltipModifiers = tooltipModifiers.concat(resolvedPopperProps.popperOptions.modifiers);
    }
    return {
      ...PopperProps.popperOptions,
      ...resolvedPopperProps == null ? void 0 : resolvedPopperProps.popperOptions,
      modifiers: tooltipModifiers
    };
  }, [arrowRef, PopperProps.popperOptions, resolvedPopperProps == null ? void 0 : resolvedPopperProps.popperOptions]);
  const classes2 = useUtilityClasses83(ownerState);
  const resolvedTransitionProps = typeof slotProps.transition === "function" ? slotProps.transition(ownerState) : slotProps.transition;
  const externalForwardedProps = {
    slots: {
      popper: components.Popper,
      transition: components.Transition ?? TransitionComponentProp,
      tooltip: components.Tooltip,
      arrow: components.Arrow,
      ...slots
    },
    slotProps: {
      arrow: slotProps.arrow ?? componentsProps.arrow,
      popper: {
        ...PopperProps,
        ...resolvedPopperProps ?? componentsProps.popper
      },
      // resolvedPopperProps can be spread because it's already an object
      tooltip: slotProps.tooltip ?? componentsProps.tooltip,
      transition: {
        ...TransitionProps,
        ...resolvedTransitionProps ?? componentsProps.transition
      }
    }
  };
  const [PopperSlot, popperSlotProps] = useSlot("popper", {
    elementType: TooltipPopper,
    externalForwardedProps,
    ownerState,
    className: clsx_default(classes2.popper, PopperProps == null ? void 0 : PopperProps.className)
  });
  const [TransitionSlot, transitionSlotProps] = useSlot("transition", {
    elementType: Grow_default,
    externalForwardedProps,
    ownerState
  });
  const [TooltipSlot, tooltipSlotProps] = useSlot("tooltip", {
    elementType: TooltipTooltip,
    className: classes2.tooltip,
    externalForwardedProps,
    ownerState
  });
  const [ArrowSlot, arrowSlotProps] = useSlot("arrow", {
    elementType: TooltipArrow,
    className: classes2.arrow,
    externalForwardedProps,
    ownerState,
    ref: setArrowRef
  });
  return (0, import_jsx_runtime154.jsxs)(React186.Fragment, {
    children: [React186.cloneElement(children2, childrenProps), (0, import_jsx_runtime154.jsx)(PopperSlot, {
      as: PopperComponentProp ?? Popper_default,
      placement,
      anchorEl: followCursor ? {
        getBoundingClientRect: () => ({
          top: cursorPosition.y,
          left: cursorPosition.x,
          right: cursorPosition.x,
          bottom: cursorPosition.y,
          width: 0,
          height: 0
        })
      } : childNode,
      popperRef,
      open: childNode ? open : false,
      id,
      transition: true,
      ...interactiveWrapperListeners,
      ...popperSlotProps,
      popperOptions,
      children: ({
        TransitionProps: TransitionPropsInner
      }) => (0, import_jsx_runtime154.jsx)(TransitionSlot, {
        timeout: theme.transitions.duration.shorter,
        ...TransitionPropsInner,
        ...transitionSlotProps,
        children: (0, import_jsx_runtime154.jsxs)(TooltipSlot, {
          ...tooltipSlotProps,
          children: [title, arrow2 ? (0, import_jsx_runtime154.jsx)(ArrowSlot, {
            ...arrowSlotProps
          }) : null]
        })
      })
    })]
  });
});
true ? Tooltip.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * If `true`, adds an arrow to the tooltip.
   * @default false
   */
  arrow: import_prop_types127.default.bool,
  /**
   * Tooltip reference element.
   */
  children: elementAcceptingRef_default.isRequired,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types127.default.object,
  /**
   * @ignore
   */
  className: import_prop_types127.default.string,
  /**
   * The components used for each slot inside.
   *
   * @deprecated use the `slots` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  components: import_prop_types127.default.shape({
    Arrow: import_prop_types127.default.elementType,
    Popper: import_prop_types127.default.elementType,
    Tooltip: import_prop_types127.default.elementType,
    Transition: import_prop_types127.default.elementType
  }),
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   *
   * @deprecated use the `slotProps` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   *
   * @default {}
   */
  componentsProps: import_prop_types127.default.shape({
    arrow: import_prop_types127.default.object,
    popper: import_prop_types127.default.object,
    tooltip: import_prop_types127.default.object,
    transition: import_prop_types127.default.object
  }),
  /**
   * Set to `true` if the `title` acts as an accessible description.
   * By default the `title` acts as an accessible label for the child.
   * @default false
   */
  describeChild: import_prop_types127.default.bool,
  /**
   * Do not respond to focus-visible events.
   * @default false
   */
  disableFocusListener: import_prop_types127.default.bool,
  /**
   * Do not respond to hover events.
   * @default false
   */
  disableHoverListener: import_prop_types127.default.bool,
  /**
   * Makes a tooltip not interactive, i.e. it will close when the user
   * hovers over the tooltip before the `leaveDelay` is expired.
   * @default false
   */
  disableInteractive: import_prop_types127.default.bool,
  /**
   * Do not respond to long press touch events.
   * @default false
   */
  disableTouchListener: import_prop_types127.default.bool,
  /**
   * The number of milliseconds to wait before showing the tooltip.
   * This prop won't impact the enter touch delay (`enterTouchDelay`).
   * @default 100
   */
  enterDelay: import_prop_types127.default.number,
  /**
   * The number of milliseconds to wait before showing the tooltip when one was already recently opened.
   * @default 0
   */
  enterNextDelay: import_prop_types127.default.number,
  /**
   * The number of milliseconds a user must touch the element before showing the tooltip.
   * @default 700
   */
  enterTouchDelay: import_prop_types127.default.number,
  /**
   * If `true`, the tooltip follow the cursor over the wrapped element.
   * @default false
   */
  followCursor: import_prop_types127.default.bool,
  /**
   * This prop is used to help implement the accessibility logic.
   * If you don't provide this prop. It falls back to a randomly generated id.
   */
  id: import_prop_types127.default.string,
  /**
   * The number of milliseconds to wait before hiding the tooltip.
   * This prop won't impact the leave touch delay (`leaveTouchDelay`).
   * @default 0
   */
  leaveDelay: import_prop_types127.default.number,
  /**
   * The number of milliseconds after the user stops touching an element before hiding the tooltip.
   * @default 1500
   */
  leaveTouchDelay: import_prop_types127.default.number,
  /**
   * Callback fired when the component requests to be closed.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   */
  onClose: import_prop_types127.default.func,
  /**
   * Callback fired when the component requests to be open.
   *
   * @param {React.SyntheticEvent} event The event source of the callback.
   */
  onOpen: import_prop_types127.default.func,
  /**
   * If `true`, the component is shown.
   */
  open: import_prop_types127.default.bool,
  /**
   * Tooltip placement.
   * @default 'bottom'
   */
  placement: import_prop_types127.default.oneOf(["auto-end", "auto-start", "auto", "bottom-end", "bottom-start", "bottom", "left-end", "left-start", "left", "right-end", "right-start", "right", "top-end", "top-start", "top"]),
  /**
   * The component used for the popper.
   * @deprecated use the `slots.popper` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  PopperComponent: import_prop_types127.default.elementType,
  /**
   * Props applied to the [`Popper`](https://mui.com/material-ui/api/popper/) element.
   * @deprecated use the `slotProps.popper` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  PopperProps: import_prop_types127.default.object,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types127.default.shape({
    arrow: import_prop_types127.default.oneOfType([import_prop_types127.default.func, import_prop_types127.default.object]),
    popper: import_prop_types127.default.oneOfType([import_prop_types127.default.func, import_prop_types127.default.object]),
    tooltip: import_prop_types127.default.oneOfType([import_prop_types127.default.func, import_prop_types127.default.object]),
    transition: import_prop_types127.default.oneOfType([import_prop_types127.default.func, import_prop_types127.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types127.default.shape({
    arrow: import_prop_types127.default.elementType,
    popper: import_prop_types127.default.elementType,
    tooltip: import_prop_types127.default.elementType,
    transition: import_prop_types127.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types127.default.oneOfType([import_prop_types127.default.arrayOf(import_prop_types127.default.oneOfType([import_prop_types127.default.func, import_prop_types127.default.object, import_prop_types127.default.bool])), import_prop_types127.default.func, import_prop_types127.default.object]),
  /**
   * Tooltip title. Zero-length titles string, undefined, null and false are never displayed.
   */
  title: import_prop_types127.default.node,
  /**
   * The component used for the transition.
   * [Follow this guide](https://mui.com/material-ui/transitions/#transitioncomponent-prop) to learn more about the requirements for this component.
   * @deprecated use the `slots.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   */
  TransitionComponent: import_prop_types127.default.elementType,
  /**
   * Props applied to the transition element.
   * By default, the element is based on this [`Transition`](https://reactcommunity.org/react-transition-group/transition/) component.
   * @deprecated use the `slotProps.transition` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  TransitionProps: import_prop_types127.default.object
} : void 0;
var Tooltip_default = Tooltip;

// node_modules/@mui/material/esm/TablePagination/TablePagination.js
var React192 = __toESM(require_react(), 1);
var import_prop_types131 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/TableCell/TableCell.js
var React189 = __toESM(require_react(), 1);
var import_prop_types128 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Table/TableContext.js
var React187 = __toESM(require_react(), 1);
var TableContext = React187.createContext();
if (true) {
  TableContext.displayName = "TableContext";
}
var TableContext_default = TableContext;

// node_modules/@mui/material/esm/Table/Tablelvl2Context.js
var React188 = __toESM(require_react(), 1);
var Tablelvl2Context = React188.createContext();
if (true) {
  Tablelvl2Context.displayName = "Tablelvl2Context";
}
var Tablelvl2Context_default = Tablelvl2Context;

// node_modules/@mui/material/esm/TableCell/tableCellClasses.js
function getTableCellUtilityClass(slot) {
  return generateUtilityClass("MuiTableCell", slot);
}
var tableCellClasses = generateUtilityClasses("MuiTableCell", ["root", "head", "body", "footer", "sizeSmall", "sizeMedium", "paddingCheckbox", "paddingNone", "alignLeft", "alignCenter", "alignRight", "alignJustify", "stickyHeader"]);
var tableCellClasses_default = tableCellClasses;

// node_modules/@mui/material/esm/TableCell/TableCell.js
var import_jsx_runtime155 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses84 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    align,
    padding,
    size,
    stickyHeader
  } = ownerState;
  const slots = {
    root: ["root", variant, stickyHeader && "stickyHeader", align !== "inherit" && `align${capitalize_default(align)}`, padding !== "normal" && `padding${capitalize_default(padding)}`, `size${capitalize_default(size)}`]
  };
  return composeClasses(slots, getTableCellUtilityClass, classes2);
};
var TableCellRoot = styled_default2("td", {
  name: "MuiTableCell",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[ownerState.variant], styles5[`size${capitalize_default(ownerState.size)}`], ownerState.padding !== "normal" && styles5[`padding${capitalize_default(ownerState.padding)}`], ownerState.align !== "inherit" && styles5[`align${capitalize_default(ownerState.align)}`], ownerState.stickyHeader && styles5.stickyHeader];
  }
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body2,
  display: "table-cell",
  verticalAlign: "inherit",
  // Workaround for a rendering bug with spanned columns in Chrome 62.0.
  // Removes the alpha (sets it to 1), and lightens or darkens the theme color.
  borderBottom: theme.vars ? `1px solid ${theme.vars.palette.TableCell.border}` : `1px solid
    ${theme.palette.mode === "light" ? theme.lighten(theme.alpha(theme.palette.divider, 1), 0.88) : theme.darken(theme.alpha(theme.palette.divider, 1), 0.68)}`,
  textAlign: "left",
  padding: 16,
  variants: [{
    props: {
      variant: "head"
    },
    style: {
      color: (theme.vars || theme).palette.text.primary,
      lineHeight: theme.typography.pxToRem(24),
      fontWeight: theme.typography.fontWeightMedium
    }
  }, {
    props: {
      variant: "body"
    },
    style: {
      color: (theme.vars || theme).palette.text.primary
    }
  }, {
    props: {
      variant: "footer"
    },
    style: {
      color: (theme.vars || theme).palette.text.secondary,
      lineHeight: theme.typography.pxToRem(21),
      fontSize: theme.typography.pxToRem(12)
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      padding: "6px 16px",
      [`&.${tableCellClasses_default.paddingCheckbox}`]: {
        width: 24,
        // prevent the checkbox column from growing
        padding: "0 12px 0 16px",
        "& > *": {
          padding: 0
        }
      }
    }
  }, {
    props: {
      padding: "checkbox"
    },
    style: {
      width: 48,
      // prevent the checkbox column from growing
      padding: "0 0 0 4px"
    }
  }, {
    props: {
      padding: "none"
    },
    style: {
      padding: 0
    }
  }, {
    props: {
      align: "left"
    },
    style: {
      textAlign: "left"
    }
  }, {
    props: {
      align: "center"
    },
    style: {
      textAlign: "center"
    }
  }, {
    props: {
      align: "right"
    },
    style: {
      textAlign: "right",
      flexDirection: "row-reverse"
    }
  }, {
    props: {
      align: "justify"
    },
    style: {
      textAlign: "justify"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.stickyHeader,
    style: {
      position: "sticky",
      top: 0,
      zIndex: 2,
      backgroundColor: (theme.vars || theme).palette.background.default
    }
  }]
})));
var TableCell = React189.forwardRef(function TableCell2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTableCell"
  });
  const {
    align = "inherit",
    className,
    component: componentProp,
    padding: paddingProp,
    scope: scopeProp,
    size: sizeProp,
    sortDirection,
    variant: variantProp,
    ...other
  } = props;
  const table = React189.useContext(TableContext_default);
  const tablelvl2 = React189.useContext(Tablelvl2Context_default);
  const isHeadCell = tablelvl2 && tablelvl2.variant === "head";
  let component;
  if (componentProp) {
    component = componentProp;
  } else {
    component = isHeadCell ? "th" : "td";
  }
  let scope = scopeProp;
  if (component === "td") {
    scope = void 0;
  } else if (!scope && isHeadCell) {
    scope = "col";
  }
  const variant = variantProp || tablelvl2 && tablelvl2.variant;
  const ownerState = {
    ...props,
    align,
    component,
    padding: paddingProp || (table && table.padding ? table.padding : "normal"),
    size: sizeProp || (table && table.size ? table.size : "medium"),
    sortDirection,
    stickyHeader: variant === "head" && table && table.stickyHeader,
    variant
  };
  const classes2 = useUtilityClasses84(ownerState);
  let ariaSort = null;
  if (sortDirection) {
    ariaSort = sortDirection === "asc" ? "ascending" : "descending";
  }
  return (0, import_jsx_runtime155.jsx)(TableCellRoot, {
    as: component,
    ref,
    className: clsx_default(classes2.root, className),
    "aria-sort": ariaSort,
    scope,
    ownerState,
    ...other
  });
});
true ? TableCell.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Set the text-align on the table cell content.
   *
   * Monetary or generally number fields **should be right aligned** as that allows
   * you to add them up quickly in your head without having to worry about decimals.
   * @default 'inherit'
   */
  align: import_prop_types128.default.oneOf(["center", "inherit", "justify", "left", "right"]),
  /**
   * The content of the component.
   */
  children: import_prop_types128.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types128.default.object,
  /**
   * @ignore
   */
  className: import_prop_types128.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types128.default.elementType,
  /**
   * Sets the padding applied to the cell.
   * The prop defaults to the value (`'default'`) inherited from the parent Table component.
   */
  padding: import_prop_types128.default.oneOf(["checkbox", "none", "normal"]),
  /**
   * Set scope attribute.
   */
  scope: import_prop_types128.default.string,
  /**
   * Specify the size of the cell.
   * The prop defaults to the value (`'medium'`) inherited from the parent Table component.
   */
  size: import_prop_types128.default.oneOfType([import_prop_types128.default.oneOf(["medium", "small"]), import_prop_types128.default.string]),
  /**
   * Set aria-sort direction.
   */
  sortDirection: import_prop_types128.default.oneOf(["asc", "desc", false]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types128.default.oneOfType([import_prop_types128.default.arrayOf(import_prop_types128.default.oneOfType([import_prop_types128.default.func, import_prop_types128.default.object, import_prop_types128.default.bool])), import_prop_types128.default.func, import_prop_types128.default.object]),
  /**
   * Specify the cell type.
   * The prop defaults to the value inherited from the parent TableHead, TableBody, or TableFooter components.
   */
  variant: import_prop_types128.default.oneOfType([import_prop_types128.default.oneOf(["body", "footer", "head"]), import_prop_types128.default.string])
} : void 0;
var TableCell_default = TableCell;

// node_modules/@mui/material/esm/Toolbar/Toolbar.js
var React190 = __toESM(require_react(), 1);
var import_prop_types129 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Toolbar/toolbarClasses.js
function getToolbarUtilityClass(slot) {
  return generateUtilityClass("MuiToolbar", slot);
}
var toolbarClasses = generateUtilityClasses("MuiToolbar", ["root", "gutters", "regular", "dense"]);

// node_modules/@mui/material/esm/Toolbar/Toolbar.js
var import_jsx_runtime156 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses85 = (ownerState) => {
  const {
    classes: classes2,
    disableGutters,
    variant
  } = ownerState;
  const slots = {
    root: ["root", !disableGutters && "gutters", variant]
  };
  return composeClasses(slots, getToolbarUtilityClass, classes2);
};
var ToolbarRoot2 = styled_default2("div", {
  name: "MuiToolbar",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, !ownerState.disableGutters && styles5.gutters, styles5[ownerState.variant]];
  }
})(memoTheme_default(({
  theme
}) => ({
  position: "relative",
  display: "flex",
  alignItems: "center",
  variants: [{
    props: ({
      ownerState
    }) => !ownerState.disableGutters,
    style: {
      paddingLeft: theme.spacing(2),
      paddingRight: theme.spacing(2),
      [theme.breakpoints.up("sm")]: {
        paddingLeft: theme.spacing(3),
        paddingRight: theme.spacing(3)
      }
    }
  }, {
    props: {
      variant: "dense"
    },
    style: {
      minHeight: 48
    }
  }, {
    props: {
      variant: "regular"
    },
    style: theme.mixins.toolbar
  }]
})));
var Toolbar3 = React190.forwardRef(function Toolbar4(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiToolbar"
  });
  const {
    className,
    component = "div",
    disableGutters = false,
    variant = "regular",
    ...other
  } = props;
  const ownerState = {
    ...props,
    component,
    disableGutters,
    variant
  };
  const classes2 = useUtilityClasses85(ownerState);
  return (0, import_jsx_runtime156.jsx)(ToolbarRoot2, {
    as: component,
    className: clsx_default(classes2.root, className),
    ref,
    ownerState,
    ...other
  });
});
true ? Toolbar3.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The Toolbar children, usually a mixture of `IconButton`, `Button` and `Typography`.
   * The Toolbar is a flex container, allowing flex item properties to be used to lay out the children.
   */
  children: import_prop_types129.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types129.default.object,
  /**
   * @ignore
   */
  className: import_prop_types129.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types129.default.elementType,
  /**
   * If `true`, disables gutter padding.
   * @default false
   */
  disableGutters: import_prop_types129.default.bool,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types129.default.oneOfType([import_prop_types129.default.arrayOf(import_prop_types129.default.oneOfType([import_prop_types129.default.func, import_prop_types129.default.object, import_prop_types129.default.bool])), import_prop_types129.default.func, import_prop_types129.default.object]),
  /**
   * The variant to use.
   * @default 'regular'
   */
  variant: import_prop_types129.default.oneOfType([import_prop_types129.default.oneOf(["dense", "regular"]), import_prop_types129.default.string])
} : void 0;
var Toolbar_default = Toolbar3;

// node_modules/@mui/material/esm/TablePaginationActions/TablePaginationActions.js
var React191 = __toESM(require_react(), 1);
var import_prop_types130 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/internal/svg-icons/KeyboardArrowLeft.js
var import_jsx_runtime157 = __toESM(require_jsx_runtime(), 1);
var KeyboardArrowLeft_default = createSvgIcon((0, import_jsx_runtime157.jsx)("path", {
  d: "M15.41 16.09l-4.58-4.59 4.58-4.59L14 5.5l-6 6 6 6z"
}), "KeyboardArrowLeft");

// node_modules/@mui/material/esm/internal/svg-icons/KeyboardArrowRight.js
var import_jsx_runtime158 = __toESM(require_jsx_runtime(), 1);
var KeyboardArrowRight_default = createSvgIcon((0, import_jsx_runtime158.jsx)("path", {
  d: "M8.59 16.34l4.58-4.59-4.58-4.59L10 5.75l6 6-6 6z"
}), "KeyboardArrowRight");

// node_modules/@mui/material/esm/internal/svg-icons/LastPage.js
var import_jsx_runtime159 = __toESM(require_jsx_runtime(), 1);
var LastPage_default = createSvgIcon((0, import_jsx_runtime159.jsx)("path", {
  d: "M5.59 7.41L10.18 12l-4.59 4.59L7 18l6-6-6-6zM16 6h2v12h-2z"
}), "LastPage");

// node_modules/@mui/material/esm/internal/svg-icons/FirstPage.js
var import_jsx_runtime160 = __toESM(require_jsx_runtime(), 1);
var FirstPage_default = createSvgIcon((0, import_jsx_runtime160.jsx)("path", {
  d: "M18.41 16.59L13.82 12l4.59-4.59L17 6l-6 6 6 6zM6 6h2v12H6z"
}), "FirstPage");

// node_modules/@mui/material/esm/TablePaginationActions/tablePaginationActionsClasses.js
function getTablePaginationActionsUtilityClass(slot) {
  return generateUtilityClass("MuiTablePaginationActions", slot);
}
var tablePaginationActionsClasses = generateUtilityClasses("MuiTablePaginationActions", ["root"]);

// node_modules/@mui/material/esm/TablePaginationActions/TablePaginationActions.js
var import_jsx_runtime161 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses86 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"]
  };
  return composeClasses(slots, getTablePaginationActionsUtilityClass, classes2);
};
var TablePaginationActionsRoot = styled_default2("div", {
  name: "MuiTablePaginationActions",
  slot: "Root"
})({});
var TablePaginationActions = React191.forwardRef(function TablePaginationActions2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTablePaginationActions"
  });
  const {
    backIconButtonProps,
    className,
    count,
    disabled = false,
    getItemAriaLabel,
    nextIconButtonProps,
    onPageChange,
    page,
    rowsPerPage,
    showFirstButton,
    showLastButton,
    slots = {},
    slotProps = {},
    ...other
  } = props;
  const isRtl = useRtl();
  const ownerState = props;
  const classes2 = useUtilityClasses86(ownerState);
  const handleFirstPageButtonClick = (event) => {
    onPageChange(event, 0);
  };
  const handleBackButtonClick = (event) => {
    onPageChange(event, page - 1);
  };
  const handleNextButtonClick = (event) => {
    onPageChange(event, page + 1);
  };
  const handleLastPageButtonClick = (event) => {
    onPageChange(event, Math.max(0, Math.ceil(count / rowsPerPage) - 1));
  };
  const FirstButton = slots.firstButton ?? IconButton_default;
  const LastButton = slots.lastButton ?? IconButton_default;
  const NextButton = slots.nextButton ?? IconButton_default;
  const PreviousButton = slots.previousButton ?? IconButton_default;
  const FirstButtonIcon = slots.firstButtonIcon ?? FirstPage_default;
  const LastButtonIcon = slots.lastButtonIcon ?? LastPage_default;
  const NextButtonIcon = slots.nextButtonIcon ?? KeyboardArrowRight_default;
  const PreviousButtonIcon = slots.previousButtonIcon ?? KeyboardArrowLeft_default;
  const FirstButtonSlot = isRtl ? LastButton : FirstButton;
  const PreviousButtonSlot = isRtl ? NextButton : PreviousButton;
  const NextButtonSlot = isRtl ? PreviousButton : NextButton;
  const LastButtonSlot = isRtl ? FirstButton : LastButton;
  const firstButtonSlotProps = isRtl ? slotProps.lastButton : slotProps.firstButton;
  const previousButtonSlotProps = isRtl ? slotProps.nextButton : slotProps.previousButton;
  const nextButtonSlotProps = isRtl ? slotProps.previousButton : slotProps.nextButton;
  const lastButtonSlotProps = isRtl ? slotProps.firstButton : slotProps.lastButton;
  return (0, import_jsx_runtime161.jsxs)(TablePaginationActionsRoot, {
    ref,
    className: clsx_default(classes2.root, className),
    ...other,
    children: [showFirstButton && (0, import_jsx_runtime161.jsx)(FirstButtonSlot, {
      onClick: handleFirstPageButtonClick,
      disabled: disabled || page === 0,
      "aria-label": getItemAriaLabel("first", page),
      title: getItemAriaLabel("first", page),
      ...firstButtonSlotProps,
      children: isRtl ? (0, import_jsx_runtime161.jsx)(LastButtonIcon, {
        ...slotProps.lastButtonIcon
      }) : (0, import_jsx_runtime161.jsx)(FirstButtonIcon, {
        ...slotProps.firstButtonIcon
      })
    }), (0, import_jsx_runtime161.jsx)(PreviousButtonSlot, {
      onClick: handleBackButtonClick,
      disabled: disabled || page === 0,
      color: "inherit",
      "aria-label": getItemAriaLabel("previous", page),
      title: getItemAriaLabel("previous", page),
      ...previousButtonSlotProps ?? backIconButtonProps,
      children: isRtl ? (0, import_jsx_runtime161.jsx)(NextButtonIcon, {
        ...slotProps.nextButtonIcon
      }) : (0, import_jsx_runtime161.jsx)(PreviousButtonIcon, {
        ...slotProps.previousButtonIcon
      })
    }), (0, import_jsx_runtime161.jsx)(NextButtonSlot, {
      onClick: handleNextButtonClick,
      disabled: disabled || (count !== -1 ? page >= Math.ceil(count / rowsPerPage) - 1 : false),
      color: "inherit",
      "aria-label": getItemAriaLabel("next", page),
      title: getItemAriaLabel("next", page),
      ...nextButtonSlotProps ?? nextIconButtonProps,
      children: isRtl ? (0, import_jsx_runtime161.jsx)(PreviousButtonIcon, {
        ...slotProps.previousButtonIcon
      }) : (0, import_jsx_runtime161.jsx)(NextButtonIcon, {
        ...slotProps.nextButtonIcon
      })
    }), showLastButton && (0, import_jsx_runtime161.jsx)(LastButtonSlot, {
      onClick: handleLastPageButtonClick,
      disabled: disabled || page >= Math.ceil(count / rowsPerPage) - 1,
      "aria-label": getItemAriaLabel("last", page),
      title: getItemAriaLabel("last", page),
      ...lastButtonSlotProps,
      children: isRtl ? (0, import_jsx_runtime161.jsx)(FirstButtonIcon, {
        ...slotProps.firstButtonIcon
      }) : (0, import_jsx_runtime161.jsx)(LastButtonIcon, {
        ...slotProps.lastButtonIcon
      })
    })]
  });
});
true ? TablePaginationActions.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop is an alias for `slotProps.previousButton` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.previousButton` instead.
   */
  backIconButtonProps: import_prop_types130.default.object,
  /**
   * @ignore
   */
  children: import_prop_types130.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types130.default.object,
  /**
   * @ignore
   */
  className: import_prop_types130.default.string,
  /**
   * @ignore
   */
  count: import_prop_types130.default.number.isRequired,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types130.default.bool,
  /**
   * Accepts a function which returns a string value that provides a user-friendly name for the current page.
   * This is important for screen reader users.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @param {string} type The link or button type to format ('first' | 'last' | 'next' | 'previous').
   * @returns {string}
   */
  getItemAriaLabel: import_prop_types130.default.func.isRequired,
  /**
   * This prop is an alias for `slotProps.nextButton` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.nextButton` instead.
   */
  nextIconButtonProps: import_prop_types130.default.object,
  /**
   * @ignore
   */
  onPageChange: import_prop_types130.default.func.isRequired,
  /**
   * @ignore
   */
  page: import_prop_types130.default.number.isRequired,
  /**
   * @ignore
   */
  rowsPerPage: import_prop_types130.default.number.isRequired,
  /**
   * @ignore
   */
  showFirstButton: import_prop_types130.default.bool.isRequired,
  /**
   * @ignore
   */
  showLastButton: import_prop_types130.default.bool.isRequired,
  /**
   * @ignore
   */
  slotProps: import_prop_types130.default.shape({
    firstButton: import_prop_types130.default.object,
    firstButtonIcon: import_prop_types130.default.object,
    lastButton: import_prop_types130.default.object,
    lastButtonIcon: import_prop_types130.default.object,
    nextButton: import_prop_types130.default.object,
    nextButtonIcon: import_prop_types130.default.object,
    previousButton: import_prop_types130.default.object,
    previousButtonIcon: import_prop_types130.default.object
  }),
  /**
   * @ignore
   */
  slots: import_prop_types130.default.shape({
    firstButton: import_prop_types130.default.elementType,
    firstButtonIcon: import_prop_types130.default.elementType,
    lastButton: import_prop_types130.default.elementType,
    lastButtonIcon: import_prop_types130.default.elementType,
    nextButton: import_prop_types130.default.elementType,
    nextButtonIcon: import_prop_types130.default.elementType,
    previousButton: import_prop_types130.default.elementType,
    previousButtonIcon: import_prop_types130.default.elementType
  })
} : void 0;
var TablePaginationActions_default = TablePaginationActions;

// node_modules/@mui/material/esm/TablePagination/tablePaginationClasses.js
function getTablePaginationUtilityClass(slot) {
  return generateUtilityClass("MuiTablePagination", slot);
}
var tablePaginationClasses = generateUtilityClasses("MuiTablePagination", ["root", "toolbar", "spacer", "selectLabel", "selectRoot", "select", "selectIcon", "input", "menuItem", "displayedRows", "actions"]);
var tablePaginationClasses_default = tablePaginationClasses;

// node_modules/@mui/material/esm/TablePagination/TablePagination.js
var import_jsx_runtime162 = __toESM(require_jsx_runtime(), 1);
var import_react11 = __toESM(require_react(), 1);
var _InputBase;
var TablePaginationRoot = styled_default2(TableCell_default, {
  name: "MuiTablePagination",
  slot: "Root"
})(memoTheme_default(({
  theme
}) => ({
  overflow: "auto",
  color: (theme.vars || theme).palette.text.primary,
  fontSize: theme.typography.pxToRem(14),
  // Increase the specificity to override TableCell.
  "&:last-child": {
    padding: 0
  }
})));
var TablePaginationToolbar = styled_default2(Toolbar_default, {
  name: "MuiTablePagination",
  slot: "Toolbar",
  overridesResolver: (props, styles5) => ({
    [`& .${tablePaginationClasses_default.actions}`]: styles5.actions,
    ...styles5.toolbar
  })
})(memoTheme_default(({
  theme
}) => ({
  minHeight: 52,
  paddingRight: 2,
  [`${theme.breakpoints.up("xs")} and (orientation: landscape)`]: {
    minHeight: 52
  },
  [theme.breakpoints.up("sm")]: {
    minHeight: 52,
    paddingRight: 2
  },
  [`& .${tablePaginationClasses_default.actions}`]: {
    flexShrink: 0,
    marginLeft: 20
  }
})));
var TablePaginationSpacer = styled_default2("div", {
  name: "MuiTablePagination",
  slot: "Spacer"
})({
  flex: "1 1 100%"
});
var TablePaginationSelectLabel = styled_default2("p", {
  name: "MuiTablePagination",
  slot: "SelectLabel"
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body2,
  flexShrink: 0
})));
var TablePaginationSelect = styled_default2(Select_default, {
  name: "MuiTablePagination",
  slot: "Select",
  overridesResolver: (props, styles5) => ({
    [`& .${tablePaginationClasses_default.selectIcon}`]: styles5.selectIcon,
    [`& .${tablePaginationClasses_default.select}`]: styles5.select,
    ...styles5.input,
    ...styles5.selectRoot
  })
})({
  color: "inherit",
  fontSize: "inherit",
  flexShrink: 0,
  marginRight: 32,
  marginLeft: 8,
  [`& .${tablePaginationClasses_default.select}`]: {
    paddingLeft: 8,
    paddingRight: 24,
    textAlign: "right",
    textAlignLast: "right"
    // Align <select> on Chrome.
  }
});
var TablePaginationMenuItem = styled_default2(MenuItem_default, {
  name: "MuiTablePagination",
  slot: "MenuItem"
})({});
var TablePaginationDisplayedRows = styled_default2("p", {
  name: "MuiTablePagination",
  slot: "DisplayedRows"
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.body2,
  flexShrink: 0
})));
function defaultLabelDisplayedRows({
  from,
  to,
  count
}) {
  return `${from}–${to} of ${count !== -1 ? count : `more than ${to}`}`;
}
function defaultGetAriaLabel(type) {
  return `Go to ${type} page`;
}
var useUtilityClasses87 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root"],
    toolbar: ["toolbar"],
    spacer: ["spacer"],
    selectLabel: ["selectLabel"],
    select: ["select"],
    input: ["input"],
    selectIcon: ["selectIcon"],
    menuItem: ["menuItem"],
    displayedRows: ["displayedRows"],
    actions: ["actions"]
  };
  return composeClasses(slots, getTablePaginationUtilityClass, classes2);
};
var TablePagination = React192.forwardRef(function TablePagination2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTablePagination"
  });
  const {
    ActionsComponent = TablePaginationActions_default,
    backIconButtonProps,
    colSpan: colSpanProp,
    component = TableCell_default,
    count,
    disabled = false,
    getItemAriaLabel = defaultGetAriaLabel,
    labelDisplayedRows = defaultLabelDisplayedRows,
    labelRowsPerPage = "Rows per page:",
    nextIconButtonProps,
    onPageChange,
    onRowsPerPageChange,
    page,
    rowsPerPage,
    rowsPerPageOptions = [10, 25, 50, 100],
    SelectProps = {},
    showFirstButton = false,
    showLastButton = false,
    slotProps = {},
    slots = {},
    ...other
  } = props;
  const ownerState = props;
  const classes2 = useUtilityClasses87(ownerState);
  const selectProps = (slotProps == null ? void 0 : slotProps.select) ?? SelectProps;
  const MenuItemComponent = selectProps.native ? "option" : TablePaginationMenuItem;
  let colSpan;
  if (component === TableCell_default || component === "td") {
    colSpan = colSpanProp || 1e3;
  }
  const selectId = useId_default(selectProps.id);
  const labelId = useId_default(selectProps.labelId);
  const getLabelDisplayedRowsTo = () => {
    if (count === -1) {
      return (page + 1) * rowsPerPage;
    }
    return rowsPerPage === -1 ? count : Math.min(count, (page + 1) * rowsPerPage);
  };
  const externalForwardedProps = {
    slots,
    slotProps
  };
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref,
    className: classes2.root,
    elementType: TablePaginationRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      component,
      ...other
    },
    ownerState,
    additionalProps: {
      colSpan
    }
  });
  const [ToolbarSlot, toolbarSlotProps] = useSlot("toolbar", {
    className: classes2.toolbar,
    elementType: TablePaginationToolbar,
    externalForwardedProps,
    ownerState
  });
  const [SpacerSlot, spacerSlotProps] = useSlot("spacer", {
    className: classes2.spacer,
    elementType: TablePaginationSpacer,
    externalForwardedProps,
    ownerState
  });
  const [SelectLabelSlot, selectLabelSlotProps] = useSlot("selectLabel", {
    className: classes2.selectLabel,
    elementType: TablePaginationSelectLabel,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      id: labelId
    }
  });
  const [SelectSlot, selectSlotProps] = useSlot("select", {
    className: classes2.select,
    elementType: TablePaginationSelect,
    externalForwardedProps,
    ownerState
  });
  const [MenuItemSlot, menuItemSlotProps] = useSlot("menuItem", {
    className: classes2.menuItem,
    elementType: MenuItemComponent,
    externalForwardedProps,
    ownerState
  });
  const [DisplayedRows, displayedRowsProps] = useSlot("displayedRows", {
    className: classes2.displayedRows,
    elementType: TablePaginationDisplayedRows,
    externalForwardedProps,
    ownerState
  });
  return (0, import_jsx_runtime162.jsx)(RootSlot, {
    ...rootSlotProps,
    children: (0, import_jsx_runtime162.jsxs)(ToolbarSlot, {
      ...toolbarSlotProps,
      children: [(0, import_jsx_runtime162.jsx)(SpacerSlot, {
        ...spacerSlotProps
      }), rowsPerPageOptions.length > 1 && (0, import_jsx_runtime162.jsx)(SelectLabelSlot, {
        ...selectLabelSlotProps,
        children: labelRowsPerPage
      }), rowsPerPageOptions.length > 1 && (0, import_jsx_runtime162.jsx)(SelectSlot, {
        variant: "standard",
        ...!selectProps.variant && {
          input: _InputBase || (_InputBase = (0, import_jsx_runtime162.jsx)(InputBase_default, {}))
        },
        value: rowsPerPage,
        onChange: onRowsPerPageChange,
        id: selectId,
        labelId,
        ...selectProps,
        classes: {
          ...selectProps.classes,
          // TODO v5 remove `classes.input`
          root: clsx_default(classes2.input, classes2.selectRoot, (selectProps.classes || {}).root),
          select: clsx_default(classes2.select, (selectProps.classes || {}).select),
          // TODO v5 remove `selectIcon`
          icon: clsx_default(classes2.selectIcon, (selectProps.classes || {}).icon)
        },
        disabled,
        ...selectSlotProps,
        children: rowsPerPageOptions.map((rowsPerPageOption) => (0, import_react11.createElement)(MenuItemSlot, {
          ...menuItemSlotProps,
          key: rowsPerPageOption.label ? rowsPerPageOption.label : rowsPerPageOption,
          value: rowsPerPageOption.value ? rowsPerPageOption.value : rowsPerPageOption
        }, rowsPerPageOption.label ? rowsPerPageOption.label : rowsPerPageOption))
      }), (0, import_jsx_runtime162.jsx)(DisplayedRows, {
        ...displayedRowsProps,
        children: labelDisplayedRows({
          from: count === 0 ? 0 : page * rowsPerPage + 1,
          to: getLabelDisplayedRowsTo(),
          count: count === -1 ? -1 : count,
          page
        })
      }), (0, import_jsx_runtime162.jsx)(ActionsComponent, {
        className: classes2.actions,
        backIconButtonProps,
        count,
        nextIconButtonProps,
        onPageChange,
        page,
        rowsPerPage,
        showFirstButton,
        showLastButton,
        slotProps: slotProps.actions,
        slots: slots.actions,
        getItemAriaLabel,
        disabled
      })]
    })
  });
});
true ? TablePagination.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The component used for displaying the actions.
   * Either a string to use a HTML element or a component.
   * @default TablePaginationActions
   */
  ActionsComponent: import_prop_types131.default.elementType,
  /**
   * Props applied to the back arrow [`IconButton`](https://mui.com/material-ui/api/icon-button/) component.
   *
   * This prop is an alias for `slotProps.actions.previousButton` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.actions.previousButton` instead.
   */
  backIconButtonProps: import_prop_types131.default.object,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types131.default.object,
  /**
   * @ignore
   */
  colSpan: import_prop_types131.default.number,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types131.default.elementType,
  /**
   * The total number of rows.
   *
   * To enable server side pagination for an unknown number of items, provide -1.
   */
  count: integerPropType_default.isRequired,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types131.default.bool,
  /**
   * Accepts a function which returns a string value that provides a user-friendly name for the current page.
   * This is important for screen reader users.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @param {string} type The link or button type to format ('first' | 'last' | 'next' | 'previous').
   * @returns {string}
   * @default function defaultGetAriaLabel(type) {
   *   return `Go to ${type} page`;
   * }
   */
  getItemAriaLabel: import_prop_types131.default.func,
  /**
   * Customize the displayed rows label. Invoked with a `{ from, to, count, page }`
   * object.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default function defaultLabelDisplayedRows({ from, to, count }) {
   *   return `${from}–${to} of ${count !== -1 ? count : `more than ${to}`}`;
   * }
   */
  labelDisplayedRows: import_prop_types131.default.func,
  /**
   * Customize the rows per page label.
   *
   * For localization purposes, you can use the provided [translations](https://mui.com/material-ui/guides/localization/).
   * @default 'Rows per page:'
   */
  labelRowsPerPage: import_prop_types131.default.node,
  /**
   * Props applied to the next arrow [`IconButton`](https://mui.com/material-ui/api/icon-button/) element.
   *
   * This prop is an alias for `slotProps.actions.nextButton` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.actions.nextButton` instead.
   */
  nextIconButtonProps: import_prop_types131.default.object,
  /**
   * Callback fired when the page is changed.
   *
   * @param {React.MouseEvent<HTMLButtonElement> | null} event The event source of the callback.
   * @param {number} page The page selected.
   */
  onPageChange: import_prop_types131.default.func.isRequired,
  /**
   * Callback fired when the number of rows per page is changed.
   *
   * @param {React.ChangeEvent<HTMLTextAreaElement | HTMLInputElement>} event The event source of the callback.
   */
  onRowsPerPageChange: import_prop_types131.default.func,
  /**
   * The zero-based index of the current page.
   */
  page: chainPropTypes(integerPropType_default.isRequired, (props) => {
    const {
      count,
      page,
      rowsPerPage
    } = props;
    if (count === -1) {
      return null;
    }
    const newLastPage = Math.max(0, Math.ceil(count / rowsPerPage) - 1);
    if (page < 0 || page > newLastPage) {
      return new Error(`MUI: The page prop of a TablePagination is out of range (0 to ${newLastPage}, but page is ${page}).`);
    }
    return null;
  }),
  /**
   * The number of rows per page.
   *
   * Set -1 to display all the rows.
   */
  rowsPerPage: integerPropType_default.isRequired,
  /**
   * Customizes the options of the rows per page select field. If less than two options are
   * available, no select field will be displayed.
   * Use -1 for the value with a custom label to show all the rows.
   * @default [10, 25, 50, 100]
   */
  rowsPerPageOptions: import_prop_types131.default.arrayOf(import_prop_types131.default.oneOfType([import_prop_types131.default.number, import_prop_types131.default.shape({
    label: import_prop_types131.default.string.isRequired,
    value: import_prop_types131.default.number.isRequired
  })]).isRequired),
  /**
   * Props applied to the rows per page [`Select`](https://mui.com/material-ui/api/select/) element.
   *
   * This prop is an alias for `slotProps.select` and will be overridden by it if both are used.
   * @deprecated Use `slotProps.select` instead.
   *
   * @default {}
   */
  SelectProps: import_prop_types131.default.object,
  /**
   * If `true`, show the first-page button.
   * @default false
   */
  showFirstButton: import_prop_types131.default.bool,
  /**
   * If `true`, show the last-page button.
   * @default false
   */
  showLastButton: import_prop_types131.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types131.default.shape({
    actions: import_prop_types131.default.shape({
      firstButton: import_prop_types131.default.object,
      firstButtonIcon: import_prop_types131.default.object,
      lastButton: import_prop_types131.default.object,
      lastButtonIcon: import_prop_types131.default.object,
      nextButton: import_prop_types131.default.object,
      nextButtonIcon: import_prop_types131.default.object,
      previousButton: import_prop_types131.default.object,
      previousButtonIcon: import_prop_types131.default.object
    }),
    displayedRows: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object]),
    menuItem: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object]),
    root: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object]),
    select: import_prop_types131.default.object,
    selectLabel: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object]),
    spacer: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object]),
    toolbar: import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types131.default.shape({
    actions: import_prop_types131.default.shape({
      firstButton: import_prop_types131.default.elementType,
      firstButtonIcon: import_prop_types131.default.elementType,
      lastButton: import_prop_types131.default.elementType,
      lastButtonIcon: import_prop_types131.default.elementType,
      nextButton: import_prop_types131.default.elementType,
      nextButtonIcon: import_prop_types131.default.elementType,
      previousButton: import_prop_types131.default.elementType,
      previousButtonIcon: import_prop_types131.default.elementType
    }),
    displayedRows: import_prop_types131.default.elementType,
    menuItem: import_prop_types131.default.elementType,
    root: import_prop_types131.default.elementType,
    select: import_prop_types131.default.elementType,
    selectLabel: import_prop_types131.default.elementType,
    spacer: import_prop_types131.default.elementType,
    toolbar: import_prop_types131.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types131.default.oneOfType([import_prop_types131.default.arrayOf(import_prop_types131.default.oneOfType([import_prop_types131.default.func, import_prop_types131.default.object, import_prop_types131.default.bool])), import_prop_types131.default.func, import_prop_types131.default.object])
} : void 0;
var TablePagination_default = TablePagination;

// node_modules/@mui/material/esm/ClickAwayListener/ClickAwayListener.js
var React193 = __toESM(require_react(), 1);
var import_prop_types132 = __toESM(require_prop_types(), 1);
function mapEventPropToEvent(eventProp) {
  return eventProp.substring(2).toLowerCase();
}
function clickedRootScrollbar(event, doc) {
  return doc.documentElement.clientWidth < event.clientX || doc.documentElement.clientHeight < event.clientY;
}
function ClickAwayListener(props) {
  const {
    children: children2,
    disableReactTree = false,
    mouseEvent = "onClick",
    onClickAway,
    touchEvent = "onTouchEnd"
  } = props;
  const movedRef = React193.useRef(false);
  const nodeRef = React193.useRef(null);
  const activatedRef = React193.useRef(false);
  const syntheticEventRef = React193.useRef(false);
  React193.useEffect(() => {
    setTimeout(() => {
      activatedRef.current = true;
    }, 0);
    return () => {
      activatedRef.current = false;
    };
  }, []);
  const handleRef = useForkRef(getReactElementRef(children2), nodeRef);
  const handleClickAway = useEventCallback_default((event) => {
    const insideReactTree = syntheticEventRef.current;
    syntheticEventRef.current = false;
    const doc = ownerDocument(nodeRef.current);
    if (!activatedRef.current || !nodeRef.current || "clientX" in event && clickedRootScrollbar(event, doc)) {
      return;
    }
    if (movedRef.current) {
      movedRef.current = false;
      return;
    }
    let insideDOM;
    if (event.composedPath) {
      insideDOM = event.composedPath().includes(nodeRef.current);
    } else {
      insideDOM = !doc.documentElement.contains(
        // @ts-expect-error returns `false` as intended when not dispatched from a Node
        event.target
      ) || nodeRef.current.contains(
        // @ts-expect-error returns `false` as intended when not dispatched from a Node
        event.target
      );
    }
    if (!insideDOM && (disableReactTree || !insideReactTree)) {
      onClickAway(event);
    }
  });
  const createHandleSynthetic = (handlerName) => (event) => {
    syntheticEventRef.current = true;
    const childrenPropsHandler = children2.props[handlerName];
    if (childrenPropsHandler) {
      childrenPropsHandler(event);
    }
  };
  const childrenProps = {
    ref: handleRef
  };
  if (touchEvent !== false) {
    childrenProps[touchEvent] = createHandleSynthetic(touchEvent);
  }
  React193.useEffect(() => {
    if (touchEvent !== false) {
      const mappedTouchEvent = mapEventPropToEvent(touchEvent);
      const doc = ownerDocument(nodeRef.current);
      const handleTouchMove = () => {
        movedRef.current = true;
      };
      doc.addEventListener(mappedTouchEvent, handleClickAway);
      doc.addEventListener("touchmove", handleTouchMove);
      return () => {
        doc.removeEventListener(mappedTouchEvent, handleClickAway);
        doc.removeEventListener("touchmove", handleTouchMove);
      };
    }
    return void 0;
  }, [handleClickAway, touchEvent]);
  if (mouseEvent !== false) {
    childrenProps[mouseEvent] = createHandleSynthetic(mouseEvent);
  }
  React193.useEffect(() => {
    if (mouseEvent !== false) {
      const mappedMouseEvent = mapEventPropToEvent(mouseEvent);
      const doc = ownerDocument(nodeRef.current);
      doc.addEventListener(mappedMouseEvent, handleClickAway);
      return () => {
        doc.removeEventListener(mappedMouseEvent, handleClickAway);
      };
    }
    return void 0;
  }, [handleClickAway, mouseEvent]);
  return React193.cloneElement(children2, childrenProps);
}
true ? ClickAwayListener.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │ To update them, edit the TypeScript types and run `pnpm proptypes`. │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The wrapped element.
   */
  children: elementAcceptingRef_default.isRequired,
  /**
   * If `true`, the React tree is ignored and only the DOM tree is considered.
   * This prop changes how portaled elements are handled.
   * @default false
   */
  disableReactTree: import_prop_types132.default.bool,
  /**
   * The mouse event to listen to. You can disable the listener by providing `false`.
   * @default 'onClick'
   */
  mouseEvent: import_prop_types132.default.oneOf(["onClick", "onMouseDown", "onMouseUp", "onPointerDown", "onPointerUp", false]),
  /**
   * Callback fired when a "click away" event is detected.
   */
  onClickAway: import_prop_types132.default.func.isRequired,
  /**
   * The touch event to listen to. You can disable the listener by providing `false`.
   * @default 'onTouchEnd'
   */
  touchEvent: import_prop_types132.default.oneOf(["onTouchEnd", "onTouchStart", false])
} : void 0;
if (true) {
  ClickAwayListener["propTypes"] = exactProp(ClickAwayListener.propTypes);
}

// node_modules/@mui/material/esm/Skeleton/Skeleton.js
var React194 = __toESM(require_react(), 1);
var import_prop_types133 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Skeleton/skeletonClasses.js
function getSkeletonUtilityClass(slot) {
  return generateUtilityClass("MuiSkeleton", slot);
}
var skeletonClasses = generateUtilityClasses("MuiSkeleton", ["root", "text", "rectangular", "rounded", "circular", "pulse", "wave", "withChildren", "fitContent", "heightAuto"]);

// node_modules/@mui/material/esm/Skeleton/Skeleton.js
var import_jsx_runtime163 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses88 = (ownerState) => {
  const {
    classes: classes2,
    variant,
    animation,
    hasChildren,
    width,
    height
  } = ownerState;
  const slots = {
    root: ["root", variant, animation, hasChildren && "withChildren", hasChildren && !width && "fitContent", hasChildren && !height && "heightAuto"]
  };
  return composeClasses(slots, getSkeletonUtilityClass, classes2);
};
var pulseKeyframe = keyframes`
  0% {
    opacity: 1;
  }

  50% {
    opacity: 0.4;
  }

  100% {
    opacity: 1;
  }
`;
var waveKeyframe = keyframes`
  0% {
    transform: translateX(-100%);
  }

  50% {
    /* +0.5s of delay between each loop */
    transform: translateX(100%);
  }

  100% {
    transform: translateX(100%);
  }
`;
var pulseAnimation = typeof pulseKeyframe !== "string" ? css`
        animation: ${pulseKeyframe} 2s ease-in-out 0.5s infinite;
      ` : null;
var waveAnimation = typeof waveKeyframe !== "string" ? css`
        &::after {
          animation: ${waveKeyframe} 2s linear 0.5s infinite;
        }
      ` : null;
var SkeletonRoot = styled_default2("span", {
  name: "MuiSkeleton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[ownerState.variant], ownerState.animation !== false && styles5[ownerState.animation], ownerState.hasChildren && styles5.withChildren, ownerState.hasChildren && !ownerState.width && styles5.fitContent, ownerState.hasChildren && !ownerState.height && styles5.heightAuto];
  }
})(memoTheme_default(({
  theme
}) => {
  const radiusUnit = getUnit(theme.shape.borderRadius) || "px";
  const radiusValue = toUnitless(theme.shape.borderRadius);
  return {
    display: "block",
    // Create a "on paper" color with sufficient contrast retaining the color
    backgroundColor: theme.vars ? theme.vars.palette.Skeleton.bg : theme.alpha(theme.palette.text.primary, theme.palette.mode === "light" ? 0.11 : 0.13),
    height: "1.2em",
    variants: [{
      props: {
        variant: "text"
      },
      style: {
        marginTop: 0,
        marginBottom: 0,
        height: "auto",
        transformOrigin: "0 55%",
        transform: "scale(1, 0.60)",
        borderRadius: `${radiusValue}${radiusUnit}/${Math.round(radiusValue / 0.6 * 10) / 10}${radiusUnit}`,
        "&:empty:before": {
          content: '"\\00a0"'
        }
      }
    }, {
      props: {
        variant: "circular"
      },
      style: {
        borderRadius: "50%"
      }
    }, {
      props: {
        variant: "rounded"
      },
      style: {
        borderRadius: (theme.vars || theme).shape.borderRadius
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.hasChildren,
      style: {
        "& > *": {
          visibility: "hidden"
        }
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.hasChildren && !ownerState.width,
      style: {
        maxWidth: "fit-content"
      }
    }, {
      props: ({
        ownerState
      }) => ownerState.hasChildren && !ownerState.height,
      style: {
        height: "auto"
      }
    }, {
      props: {
        animation: "pulse"
      },
      style: pulseAnimation || {
        animation: `${pulseKeyframe} 2s ease-in-out 0.5s infinite`
      }
    }, {
      props: {
        animation: "wave"
      },
      style: {
        position: "relative",
        overflow: "hidden",
        /* Fix bug in Safari https://bugs.webkit.org/show_bug.cgi?id=68196 */
        WebkitMaskImage: "-webkit-radial-gradient(white, black)",
        "&::after": {
          background: `linear-gradient(
                90deg,
                transparent,
                ${(theme.vars || theme).palette.action.hover},
                transparent
              )`,
          content: '""',
          position: "absolute",
          transform: "translateX(-100%)",
          bottom: 0,
          left: 0,
          right: 0,
          top: 0
        }
      }
    }, {
      props: {
        animation: "wave"
      },
      style: waveAnimation || {
        "&::after": {
          animation: `${waveKeyframe} 2s linear 0.5s infinite`
        }
      }
    }]
  };
}));
var Skeleton = React194.forwardRef(function Skeleton2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiSkeleton"
  });
  const {
    animation = "pulse",
    className,
    component = "span",
    height,
    style,
    variant = "text",
    width,
    ...other
  } = props;
  const ownerState = {
    ...props,
    animation,
    component,
    variant,
    hasChildren: Boolean(other.children)
  };
  const classes2 = useUtilityClasses88(ownerState);
  return (0, import_jsx_runtime163.jsx)(SkeletonRoot, {
    as: component,
    ref,
    className: clsx_default(classes2.root, className),
    ownerState,
    ...other,
    style: {
      width,
      height,
      ...style
    }
  });
});
true ? Skeleton.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The animation.
   * If `false` the animation effect is disabled.
   * @default 'pulse'
   */
  animation: import_prop_types133.default.oneOf(["pulse", "wave", false]),
  /**
   * Optional children to infer width and height from.
   */
  children: import_prop_types133.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types133.default.object,
  /**
   * @ignore
   */
  className: import_prop_types133.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types133.default.elementType,
  /**
   * Height of the skeleton.
   * Useful when you don't want to adapt the skeleton to a text element but for instance a card.
   */
  height: import_prop_types133.default.oneOfType([import_prop_types133.default.number, import_prop_types133.default.string]),
  /**
   * @ignore
   */
  style: import_prop_types133.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types133.default.oneOfType([import_prop_types133.default.arrayOf(import_prop_types133.default.oneOfType([import_prop_types133.default.func, import_prop_types133.default.object, import_prop_types133.default.bool])), import_prop_types133.default.func, import_prop_types133.default.object]),
  /**
   * The type of content that will be rendered.
   * @default 'text'
   */
  variant: import_prop_types133.default.oneOfType([import_prop_types133.default.oneOf(["circular", "rectangular", "rounded", "text"]), import_prop_types133.default.string]),
  /**
   * Width of the skeleton.
   * Useful when the skeleton is inside an inline element with no width of its own.
   */
  width: import_prop_types133.default.oneOfType([import_prop_types133.default.number, import_prop_types133.default.string])
} : void 0;
var Skeleton_default = Skeleton;

// node_modules/@mui/material/esm/Tabs/Tabs.js
var React197 = __toESM(require_react(), 1);
var import_react_is4 = __toESM(require_react_is(), 1);
var import_prop_types136 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/internal/animate.js
function easeInOutSin(time) {
  return (1 + Math.sin(Math.PI * time - Math.PI / 2)) / 2;
}
function animate(property, element, to, options3 = {}, cb = () => {
}) {
  const {
    ease = easeInOutSin,
    duration = 300
    // standard
  } = options3;
  let start2 = null;
  const from = element[property];
  let cancelled = false;
  const cancel = () => {
    cancelled = true;
  };
  const step = (timestamp) => {
    if (cancelled) {
      cb(new Error("Animation cancelled"));
      return;
    }
    if (start2 === null) {
      start2 = timestamp;
    }
    const time = Math.min(1, (timestamp - start2) / duration);
    element[property] = ease(time) * (to - from) + from;
    if (time >= 1) {
      requestAnimationFrame(() => {
        cb(null);
      });
      return;
    }
    requestAnimationFrame(step);
  };
  if (from === to) {
    cb(new Error("Element already at target position"));
    return cancel;
  }
  requestAnimationFrame(step);
  return cancel;
}

// node_modules/@mui/material/esm/Tabs/ScrollbarSize.js
var React195 = __toESM(require_react(), 1);
var import_prop_types134 = __toESM(require_prop_types(), 1);
var import_jsx_runtime164 = __toESM(require_jsx_runtime(), 1);
var styles4 = {
  width: 99,
  height: 99,
  position: "absolute",
  top: -9999,
  overflow: "scroll"
};
function ScrollbarSize(props) {
  const {
    onChange,
    ...other
  } = props;
  const scrollbarHeight = React195.useRef();
  const nodeRef = React195.useRef(null);
  const setMeasurements = () => {
    scrollbarHeight.current = nodeRef.current.offsetHeight - nodeRef.current.clientHeight;
  };
  useEnhancedEffect_default2(() => {
    const handleResize = debounce_default(() => {
      const prevHeight = scrollbarHeight.current;
      setMeasurements();
      if (prevHeight !== scrollbarHeight.current) {
        onChange(scrollbarHeight.current);
      }
    });
    const containerWindow = ownerWindow_default(nodeRef.current);
    containerWindow.addEventListener("resize", handleResize);
    return () => {
      handleResize.clear();
      containerWindow.removeEventListener("resize", handleResize);
    };
  }, [onChange]);
  React195.useEffect(() => {
    setMeasurements();
    onChange(scrollbarHeight.current);
  }, [onChange]);
  return (0, import_jsx_runtime164.jsx)("div", {
    style: styles4,
    ...other,
    ref: nodeRef
  });
}
true ? ScrollbarSize.propTypes = {
  onChange: import_prop_types134.default.func.isRequired
} : void 0;

// node_modules/@mui/material/esm/TabScrollButton/TabScrollButton.js
var React196 = __toESM(require_react(), 1);
var import_prop_types135 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/TabScrollButton/tabScrollButtonClasses.js
function getTabScrollButtonUtilityClass(slot) {
  return generateUtilityClass("MuiTabScrollButton", slot);
}
var tabScrollButtonClasses = generateUtilityClasses("MuiTabScrollButton", ["root", "vertical", "horizontal", "disabled"]);
var tabScrollButtonClasses_default = tabScrollButtonClasses;

// node_modules/@mui/material/esm/TabScrollButton/TabScrollButton.js
var import_jsx_runtime165 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses89 = (ownerState) => {
  const {
    classes: classes2,
    orientation,
    disabled
  } = ownerState;
  const slots = {
    root: ["root", orientation, disabled && "disabled"]
  };
  return composeClasses(slots, getTabScrollButtonUtilityClass, classes2);
};
var TabScrollButtonRoot = styled_default2(ButtonBase_default, {
  name: "MuiTabScrollButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.orientation && styles5[ownerState.orientation]];
  }
})({
  width: 40,
  flexShrink: 0,
  opacity: 0.8,
  [`&.${tabScrollButtonClasses_default.disabled}`]: {
    opacity: 0
  },
  variants: [{
    props: {
      orientation: "vertical"
    },
    style: {
      width: "100%",
      height: 40,
      "& svg": {
        transform: "var(--TabScrollButton-svgRotate)"
      }
    }
  }]
});
var TabScrollButton = React196.forwardRef(function TabScrollButton2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTabScrollButton"
  });
  const {
    className,
    slots = {},
    slotProps = {},
    direction,
    orientation,
    disabled,
    ...other
  } = props;
  const isRtl = useRtl();
  const ownerState = {
    isRtl,
    ...props
  };
  const classes2 = useUtilityClasses89(ownerState);
  const StartButtonIcon = slots.StartScrollButtonIcon ?? KeyboardArrowLeft_default;
  const EndButtonIcon = slots.EndScrollButtonIcon ?? KeyboardArrowRight_default;
  const startButtonIconProps = useSlotProps_default({
    elementType: StartButtonIcon,
    externalSlotProps: slotProps.startScrollButtonIcon,
    additionalProps: {
      fontSize: "small"
    },
    ownerState
  });
  const endButtonIconProps = useSlotProps_default({
    elementType: EndButtonIcon,
    externalSlotProps: slotProps.endScrollButtonIcon,
    additionalProps: {
      fontSize: "small"
    },
    ownerState
  });
  return (0, import_jsx_runtime165.jsx)(TabScrollButtonRoot, {
    component: "div",
    className: clsx_default(classes2.root, className),
    ref,
    role: null,
    ownerState,
    tabIndex: null,
    ...other,
    style: {
      ...other.style,
      ...orientation === "vertical" && {
        "--TabScrollButton-svgRotate": `rotate(${isRtl ? -90 : 90}deg)`
      }
    },
    children: direction === "left" ? (0, import_jsx_runtime165.jsx)(StartButtonIcon, {
      ...startButtonIconProps
    }) : (0, import_jsx_runtime165.jsx)(EndButtonIcon, {
      ...endButtonIconProps
    })
  });
});
true ? TabScrollButton.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types135.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types135.default.object,
  /**
   * @ignore
   */
  className: import_prop_types135.default.string,
  /**
   * The direction the button should indicate.
   */
  direction: import_prop_types135.default.oneOf(["left", "right"]).isRequired,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types135.default.bool,
  /**
   * The component orientation (layout flow direction).
   */
  orientation: import_prop_types135.default.oneOf(["horizontal", "vertical"]).isRequired,
  /**
   * The extra props for the slot components.
   * You can override the existing props or add new ones.
   * @default {}
   */
  slotProps: import_prop_types135.default.shape({
    endScrollButtonIcon: import_prop_types135.default.oneOfType([import_prop_types135.default.func, import_prop_types135.default.object]),
    startScrollButtonIcon: import_prop_types135.default.oneOfType([import_prop_types135.default.func, import_prop_types135.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types135.default.shape({
    EndScrollButtonIcon: import_prop_types135.default.elementType,
    StartScrollButtonIcon: import_prop_types135.default.elementType
  }),
  /**
   * @ignore
   */
  style: import_prop_types135.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types135.default.oneOfType([import_prop_types135.default.arrayOf(import_prop_types135.default.oneOfType([import_prop_types135.default.func, import_prop_types135.default.object, import_prop_types135.default.bool])), import_prop_types135.default.func, import_prop_types135.default.object])
} : void 0;
var TabScrollButton_default = TabScrollButton;

// node_modules/@mui/material/esm/Tabs/tabsClasses.js
function getTabsUtilityClass(slot) {
  return generateUtilityClass("MuiTabs", slot);
}
var tabsClasses = generateUtilityClasses("MuiTabs", ["root", "vertical", "list", "flexContainer", "flexContainerVertical", "centered", "scroller", "fixed", "scrollableX", "scrollableY", "hideScrollbar", "scrollButtons", "scrollButtonsHideMobile", "indicator"]);
var tabsClasses_default = tabsClasses;

// node_modules/@mui/material/esm/Tabs/Tabs.js
var import_jsx_runtime166 = __toESM(require_jsx_runtime(), 1);
var nextItem2 = (list, item) => {
  if (list === item) {
    return list.firstChild;
  }
  if (item && item.nextElementSibling) {
    return item.nextElementSibling;
  }
  return list.firstChild;
};
var previousItem2 = (list, item) => {
  if (list === item) {
    return list.lastChild;
  }
  if (item && item.previousElementSibling) {
    return item.previousElementSibling;
  }
  return list.lastChild;
};
var moveFocus2 = (list, currentFocus, traversalFunction) => {
  let wrappedOnce = false;
  let nextFocus = traversalFunction(list, currentFocus);
  while (nextFocus) {
    if (nextFocus === list.firstChild) {
      if (wrappedOnce) {
        return;
      }
      wrappedOnce = true;
    }
    const nextFocusDisabled = nextFocus.disabled || nextFocus.getAttribute("aria-disabled") === "true";
    if (!nextFocus.hasAttribute("tabindex") || nextFocusDisabled) {
      nextFocus = traversalFunction(list, nextFocus);
    } else {
      nextFocus.focus();
      return;
    }
  }
};
var useUtilityClasses90 = (ownerState) => {
  const {
    vertical,
    fixed,
    hideScrollbar,
    scrollableX,
    scrollableY,
    centered,
    scrollButtonsHideMobile,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["root", vertical && "vertical"],
    scroller: ["scroller", fixed && "fixed", hideScrollbar && "hideScrollbar", scrollableX && "scrollableX", scrollableY && "scrollableY"],
    list: ["list", "flexContainer", vertical && "flexContainerVertical", vertical && "vertical", centered && "centered"],
    indicator: ["indicator"],
    scrollButtons: ["scrollButtons", scrollButtonsHideMobile && "scrollButtonsHideMobile"],
    scrollableX: [scrollableX && "scrollableX"],
    hideScrollbar: [hideScrollbar && "hideScrollbar"]
  };
  return composeClasses(slots, getTabsUtilityClass, classes2);
};
var TabsRoot = styled_default2("div", {
  name: "MuiTabs",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [{
      [`& .${tabsClasses_default.scrollButtons}`]: styles5.scrollButtons
    }, {
      [`& .${tabsClasses_default.scrollButtons}`]: ownerState.scrollButtonsHideMobile && styles5.scrollButtonsHideMobile
    }, styles5.root, ownerState.vertical && styles5.vertical];
  }
})(memoTheme_default(({
  theme
}) => ({
  overflow: "hidden",
  minHeight: 48,
  // Add iOS momentum scrolling for iOS < 13.0
  WebkitOverflowScrolling: "touch",
  display: "flex",
  variants: [{
    props: ({
      ownerState
    }) => ownerState.vertical,
    style: {
      flexDirection: "column"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.scrollButtonsHideMobile,
    style: {
      [`& .${tabsClasses_default.scrollButtons}`]: {
        [theme.breakpoints.down("sm")]: {
          display: "none"
        }
      }
    }
  }]
})));
var TabsScroller = styled_default2("div", {
  name: "MuiTabs",
  slot: "Scroller",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.scroller, ownerState.fixed && styles5.fixed, ownerState.hideScrollbar && styles5.hideScrollbar, ownerState.scrollableX && styles5.scrollableX, ownerState.scrollableY && styles5.scrollableY];
  }
})({
  position: "relative",
  display: "inline-block",
  flex: "1 1 auto",
  whiteSpace: "nowrap",
  variants: [{
    props: ({
      ownerState
    }) => ownerState.fixed,
    style: {
      overflowX: "hidden",
      width: "100%"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.hideScrollbar,
    style: {
      // Hide dimensionless scrollbar on macOS
      scrollbarWidth: "none",
      // Firefox
      "&::-webkit-scrollbar": {
        display: "none"
        // Safari + Chrome
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.scrollableX,
    style: {
      overflowX: "auto",
      overflowY: "hidden"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.scrollableY,
    style: {
      overflowY: "auto",
      overflowX: "hidden"
    }
  }]
});
var List3 = styled_default2("div", {
  name: "MuiTabs",
  slot: "List",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.list, styles5.flexContainer, ownerState.vertical && styles5.flexContainerVertical, ownerState.centered && styles5.centered];
  }
})({
  display: "flex",
  variants: [{
    props: ({
      ownerState
    }) => ownerState.vertical,
    style: {
      flexDirection: "column"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.centered,
    style: {
      justifyContent: "center"
    }
  }]
});
var TabsIndicator = styled_default2("span", {
  name: "MuiTabs",
  slot: "Indicator"
})(memoTheme_default(({
  theme
}) => ({
  position: "absolute",
  height: 2,
  bottom: 0,
  width: "100%",
  transition: theme.transitions.create(),
  variants: [{
    props: {
      indicatorColor: "primary"
    },
    style: {
      backgroundColor: (theme.vars || theme).palette.primary.main
    }
  }, {
    props: {
      indicatorColor: "secondary"
    },
    style: {
      backgroundColor: (theme.vars || theme).palette.secondary.main
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.vertical,
    style: {
      height: "100%",
      width: 2,
      right: 0
    }
  }]
})));
var TabsScrollbarSize = styled_default2(ScrollbarSize)({
  overflowX: "auto",
  overflowY: "hidden",
  // Hide dimensionless scrollbar on macOS
  scrollbarWidth: "none",
  // Firefox
  "&::-webkit-scrollbar": {
    display: "none"
    // Safari + Chrome
  }
});
var defaultIndicatorStyle = {};
var warnedOnceTabPresent = false;
var Tabs = React197.forwardRef(function Tabs2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTabs"
  });
  const theme = useTheme();
  const isRtl = useRtl();
  const {
    "aria-label": ariaLabel,
    "aria-labelledby": ariaLabelledBy,
    action,
    centered = false,
    children: childrenProp,
    className,
    component = "div",
    allowScrollButtonsMobile = false,
    indicatorColor = "primary",
    onChange,
    orientation = "horizontal",
    ScrollButtonComponent,
    // TODO: remove in v7 (deprecated in v6)
    scrollButtons = "auto",
    selectionFollowsFocus,
    slots = {},
    slotProps = {},
    TabIndicatorProps = {},
    // TODO: remove in v7 (deprecated in v6)
    TabScrollButtonProps = {},
    // TODO: remove in v7 (deprecated in v6)
    textColor = "primary",
    value,
    variant = "standard",
    visibleScrollbar = false,
    ...other
  } = props;
  const scrollable = variant === "scrollable";
  const vertical = orientation === "vertical";
  const scrollStart = vertical ? "scrollTop" : "scrollLeft";
  const start2 = vertical ? "top" : "left";
  const end2 = vertical ? "bottom" : "right";
  const clientSize = vertical ? "clientHeight" : "clientWidth";
  const size = vertical ? "height" : "width";
  const ownerState = {
    ...props,
    component,
    allowScrollButtonsMobile,
    indicatorColor,
    orientation,
    vertical,
    scrollButtons,
    textColor,
    variant,
    visibleScrollbar,
    fixed: !scrollable,
    hideScrollbar: scrollable && !visibleScrollbar,
    scrollableX: scrollable && !vertical,
    scrollableY: scrollable && vertical,
    centered: centered && !scrollable,
    scrollButtonsHideMobile: !allowScrollButtonsMobile
  };
  const classes2 = useUtilityClasses90(ownerState);
  const startScrollButtonIconProps = useSlotProps_default({
    elementType: slots.StartScrollButtonIcon,
    externalSlotProps: slotProps.startScrollButtonIcon,
    ownerState
  });
  const endScrollButtonIconProps = useSlotProps_default({
    elementType: slots.EndScrollButtonIcon,
    externalSlotProps: slotProps.endScrollButtonIcon,
    ownerState
  });
  if (true) {
    if (centered && scrollable) {
      console.error('MUI: You can not use the `centered={true}` and `variant="scrollable"` properties at the same time on a `Tabs` component.');
    }
  }
  const [mounted, setMounted] = React197.useState(false);
  const [indicatorStyle, setIndicatorStyle] = React197.useState(defaultIndicatorStyle);
  const [displayStartScroll, setDisplayStartScroll] = React197.useState(false);
  const [displayEndScroll, setDisplayEndScroll] = React197.useState(false);
  const [updateScrollObserver, setUpdateScrollObserver] = React197.useState(false);
  const [scrollerStyle, setScrollerStyle] = React197.useState({
    overflow: "hidden",
    scrollbarWidth: 0
  });
  const valueToIndex = /* @__PURE__ */ new Map();
  const tabsRef = React197.useRef(null);
  const tabListRef = React197.useRef(null);
  const externalForwardedProps = {
    slots,
    slotProps: {
      indicator: TabIndicatorProps,
      scrollButtons: TabScrollButtonProps,
      ...slotProps
    }
  };
  const getTabsMeta = () => {
    const tabsNode = tabsRef.current;
    let tabsMeta;
    if (tabsNode) {
      const rect = tabsNode.getBoundingClientRect();
      tabsMeta = {
        clientWidth: tabsNode.clientWidth,
        scrollLeft: tabsNode.scrollLeft,
        scrollTop: tabsNode.scrollTop,
        scrollWidth: tabsNode.scrollWidth,
        top: rect.top,
        bottom: rect.bottom,
        left: rect.left,
        right: rect.right
      };
    }
    let tabMeta;
    if (tabsNode && value !== false) {
      const children3 = tabListRef.current.children;
      if (children3.length > 0) {
        const tab = children3[valueToIndex.get(value)];
        if (true) {
          if (!tab) {
            console.error([`MUI: The \`value\` provided to the Tabs component is invalid.`, `None of the Tabs' children match with "${value}".`, valueToIndex.keys ? `You can provide one of the following values: ${Array.from(valueToIndex.keys()).join(", ")}.` : null].join("\n"));
          }
        }
        tabMeta = tab ? tab.getBoundingClientRect() : null;
        if (true) {
          if (isLayoutSupported() && !warnedOnceTabPresent && tabMeta && tabMeta.width === 0 && tabMeta.height === 0 && // if the whole Tabs component is hidden, don't warn
          tabsMeta.clientWidth !== 0) {
            tabsMeta = null;
            console.error(["MUI: The `value` provided to the Tabs component is invalid.", `The Tab with this \`value\` ("${value}") is not part of the document layout.`, "Make sure the tab item is present in the document or that it's not `display: none`."].join("\n"));
            warnedOnceTabPresent = true;
          }
        }
      }
    }
    return {
      tabsMeta,
      tabMeta
    };
  };
  const updateIndicatorState = useEventCallback_default2(() => {
    const {
      tabsMeta,
      tabMeta
    } = getTabsMeta();
    let startValue = 0;
    let startIndicator;
    if (vertical) {
      startIndicator = "top";
      if (tabMeta && tabsMeta) {
        startValue = tabMeta.top - tabsMeta.top + tabsMeta.scrollTop;
      }
    } else {
      startIndicator = isRtl ? "right" : "left";
      if (tabMeta && tabsMeta) {
        startValue = (isRtl ? -1 : 1) * (tabMeta[startIndicator] - tabsMeta[startIndicator] + tabsMeta.scrollLeft);
      }
    }
    const newIndicatorStyle = {
      [startIndicator]: startValue,
      // May be wrong until the font is loaded.
      [size]: tabMeta ? tabMeta[size] : 0
    };
    if (typeof indicatorStyle[startIndicator] !== "number" || typeof indicatorStyle[size] !== "number") {
      setIndicatorStyle(newIndicatorStyle);
    } else {
      const dStart = Math.abs(indicatorStyle[startIndicator] - newIndicatorStyle[startIndicator]);
      const dSize = Math.abs(indicatorStyle[size] - newIndicatorStyle[size]);
      if (dStart >= 1 || dSize >= 1) {
        setIndicatorStyle(newIndicatorStyle);
      }
    }
  });
  const scroll = (scrollValue, {
    animation = true
  } = {}) => {
    if (animation) {
      animate(scrollStart, tabsRef.current, scrollValue, {
        duration: theme.transitions.duration.standard
      });
    } else {
      tabsRef.current[scrollStart] = scrollValue;
    }
  };
  const moveTabsScroll = (delta) => {
    let scrollValue = tabsRef.current[scrollStart];
    if (vertical) {
      scrollValue += delta;
    } else {
      scrollValue += delta * (isRtl ? -1 : 1);
    }
    scroll(scrollValue);
  };
  const getScrollSize = () => {
    const containerSize = tabsRef.current[clientSize];
    let totalSize = 0;
    const children3 = Array.from(tabListRef.current.children);
    for (let i2 = 0; i2 < children3.length; i2 += 1) {
      const tab = children3[i2];
      if (totalSize + tab[clientSize] > containerSize) {
        if (i2 === 0) {
          totalSize = containerSize;
        }
        break;
      }
      totalSize += tab[clientSize];
    }
    return totalSize;
  };
  const handleStartScrollClick = () => {
    moveTabsScroll(-1 * getScrollSize());
  };
  const handleEndScrollClick = () => {
    moveTabsScroll(getScrollSize());
  };
  const [ScrollbarSlot, {
    onChange: scrollbarOnChange,
    ...scrollbarSlotProps
  }] = useSlot("scrollbar", {
    className: clsx_default(classes2.scrollableX, classes2.hideScrollbar),
    elementType: TabsScrollbarSize,
    shouldForwardComponentProp: true,
    externalForwardedProps,
    ownerState
  });
  const handleScrollbarSizeChange = React197.useCallback((scrollbarWidth) => {
    scrollbarOnChange == null ? void 0 : scrollbarOnChange(scrollbarWidth);
    setScrollerStyle({
      overflow: null,
      scrollbarWidth
    });
  }, [scrollbarOnChange]);
  const [ScrollButtonsSlot, scrollButtonSlotProps] = useSlot("scrollButtons", {
    className: clsx_default(classes2.scrollButtons, TabScrollButtonProps.className),
    elementType: TabScrollButton_default,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      orientation,
      slots: {
        StartScrollButtonIcon: slots.startScrollButtonIcon || slots.StartScrollButtonIcon,
        EndScrollButtonIcon: slots.endScrollButtonIcon || slots.EndScrollButtonIcon
      },
      slotProps: {
        startScrollButtonIcon: startScrollButtonIconProps,
        endScrollButtonIcon: endScrollButtonIconProps
      }
    }
  });
  const getConditionalElements = () => {
    const conditionalElements2 = {};
    conditionalElements2.scrollbarSizeListener = scrollable ? (0, import_jsx_runtime166.jsx)(ScrollbarSlot, {
      ...scrollbarSlotProps,
      onChange: handleScrollbarSizeChange
    }) : null;
    const scrollButtonsActive = displayStartScroll || displayEndScroll;
    const showScrollButtons = scrollable && (scrollButtons === "auto" && scrollButtonsActive || scrollButtons === true);
    conditionalElements2.scrollButtonStart = showScrollButtons ? (0, import_jsx_runtime166.jsx)(ScrollButtonsSlot, {
      direction: isRtl ? "right" : "left",
      onClick: handleStartScrollClick,
      disabled: !displayStartScroll,
      ...scrollButtonSlotProps
    }) : null;
    conditionalElements2.scrollButtonEnd = showScrollButtons ? (0, import_jsx_runtime166.jsx)(ScrollButtonsSlot, {
      direction: isRtl ? "left" : "right",
      onClick: handleEndScrollClick,
      disabled: !displayEndScroll,
      ...scrollButtonSlotProps
    }) : null;
    return conditionalElements2;
  };
  const scrollSelectedIntoView = useEventCallback_default2((animation) => {
    const {
      tabsMeta,
      tabMeta
    } = getTabsMeta();
    if (!tabMeta || !tabsMeta) {
      return;
    }
    if (tabMeta[start2] < tabsMeta[start2]) {
      const nextScrollStart = tabsMeta[scrollStart] + (tabMeta[start2] - tabsMeta[start2]);
      scroll(nextScrollStart, {
        animation
      });
    } else if (tabMeta[end2] > tabsMeta[end2]) {
      const nextScrollStart = tabsMeta[scrollStart] + (tabMeta[end2] - tabsMeta[end2]);
      scroll(nextScrollStart, {
        animation
      });
    }
  });
  const updateScrollButtonState = useEventCallback_default2(() => {
    if (scrollable && scrollButtons !== false) {
      setUpdateScrollObserver(!updateScrollObserver);
    }
  });
  React197.useEffect(() => {
    const handleResize = debounce_default(() => {
      if (tabsRef.current) {
        updateIndicatorState();
      }
    });
    let resizeObserver;
    const handleMutation = (records) => {
      records.forEach((record) => {
        record.removedNodes.forEach((item) => {
          resizeObserver == null ? void 0 : resizeObserver.unobserve(item);
        });
        record.addedNodes.forEach((item) => {
          resizeObserver == null ? void 0 : resizeObserver.observe(item);
        });
      });
      handleResize();
      updateScrollButtonState();
    };
    const win = ownerWindow_default(tabsRef.current);
    win.addEventListener("resize", handleResize);
    let mutationObserver;
    if (typeof ResizeObserver !== "undefined") {
      resizeObserver = new ResizeObserver(handleResize);
      Array.from(tabListRef.current.children).forEach((child) => {
        resizeObserver.observe(child);
      });
    }
    if (typeof MutationObserver !== "undefined") {
      mutationObserver = new MutationObserver(handleMutation);
      mutationObserver.observe(tabListRef.current, {
        childList: true
      });
    }
    return () => {
      handleResize.clear();
      win.removeEventListener("resize", handleResize);
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
      resizeObserver == null ? void 0 : resizeObserver.disconnect();
    };
  }, [updateIndicatorState, updateScrollButtonState]);
  React197.useEffect(() => {
    const tabListChildren = Array.from(tabListRef.current.children);
    const length = tabListChildren.length;
    if (typeof IntersectionObserver !== "undefined" && length > 0 && scrollable && scrollButtons !== false) {
      const firstTab = tabListChildren[0];
      const lastTab = tabListChildren[length - 1];
      const observerOptions = {
        root: tabsRef.current,
        threshold: 0.99
      };
      const handleScrollButtonStart = (entries) => {
        setDisplayStartScroll(!entries[0].isIntersecting);
      };
      const firstObserver = new IntersectionObserver(handleScrollButtonStart, observerOptions);
      firstObserver.observe(firstTab);
      const handleScrollButtonEnd = (entries) => {
        setDisplayEndScroll(!entries[0].isIntersecting);
      };
      const lastObserver = new IntersectionObserver(handleScrollButtonEnd, observerOptions);
      lastObserver.observe(lastTab);
      return () => {
        firstObserver.disconnect();
        lastObserver.disconnect();
      };
    }
    return void 0;
  }, [scrollable, scrollButtons, updateScrollObserver, childrenProp == null ? void 0 : childrenProp.length]);
  React197.useEffect(() => {
    setMounted(true);
  }, []);
  React197.useEffect(() => {
    updateIndicatorState();
  });
  React197.useEffect(() => {
    scrollSelectedIntoView(defaultIndicatorStyle !== indicatorStyle);
  }, [scrollSelectedIntoView, indicatorStyle]);
  React197.useImperativeHandle(action, () => ({
    updateIndicator: updateIndicatorState,
    updateScrollButtons: updateScrollButtonState
  }), [updateIndicatorState, updateScrollButtonState]);
  const [IndicatorSlot, indicatorSlotProps] = useSlot("indicator", {
    className: clsx_default(classes2.indicator, TabIndicatorProps.className),
    elementType: TabsIndicator,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      style: indicatorStyle
    }
  });
  const indicator = (0, import_jsx_runtime166.jsx)(IndicatorSlot, {
    ...indicatorSlotProps
  });
  let childIndex = 0;
  const children2 = React197.Children.map(childrenProp, (child) => {
    if (!React197.isValidElement(child)) {
      return null;
    }
    if (true) {
      if ((0, import_react_is4.isFragment)(child)) {
        console.error(["MUI: The Tabs component doesn't accept a Fragment as a child.", "Consider providing an array instead."].join("\n"));
      }
    }
    const childValue = child.props.value === void 0 ? childIndex : child.props.value;
    valueToIndex.set(childValue, childIndex);
    const selected = childValue === value;
    childIndex += 1;
    return React197.cloneElement(child, {
      fullWidth: variant === "fullWidth",
      indicator: selected && !mounted && indicator,
      selected,
      selectionFollowsFocus,
      onChange,
      textColor,
      value: childValue,
      ...childIndex === 1 && value === false && !child.props.tabIndex ? {
        tabIndex: 0
      } : {}
    });
  });
  const handleKeyDown = (event) => {
    if (event.altKey || event.shiftKey || event.ctrlKey || event.metaKey) {
      return;
    }
    const list = tabListRef.current;
    const currentFocus = getActiveElement_default(ownerDocument_default(list));
    const role = currentFocus == null ? void 0 : currentFocus.getAttribute("role");
    if (role !== "tab") {
      return;
    }
    let previousItemKey = orientation === "horizontal" ? "ArrowLeft" : "ArrowUp";
    let nextItemKey = orientation === "horizontal" ? "ArrowRight" : "ArrowDown";
    if (orientation === "horizontal" && isRtl) {
      previousItemKey = "ArrowRight";
      nextItemKey = "ArrowLeft";
    }
    switch (event.key) {
      case previousItemKey:
        event.preventDefault();
        moveFocus2(list, currentFocus, previousItem2);
        break;
      case nextItemKey:
        event.preventDefault();
        moveFocus2(list, currentFocus, nextItem2);
        break;
      case "Home":
        event.preventDefault();
        moveFocus2(list, null, nextItem2);
        break;
      case "End":
        event.preventDefault();
        moveFocus2(list, null, previousItem2);
        break;
      default:
        break;
    }
  };
  const conditionalElements = getConditionalElements();
  const [RootSlot, rootSlotProps] = useSlot("root", {
    ref,
    className: clsx_default(classes2.root, className),
    elementType: TabsRoot,
    externalForwardedProps: {
      ...externalForwardedProps,
      ...other,
      component
    },
    ownerState
  });
  const [ScrollerSlot, scrollerSlotProps] = useSlot("scroller", {
    ref: tabsRef,
    className: classes2.scroller,
    elementType: TabsScroller,
    externalForwardedProps,
    ownerState,
    additionalProps: {
      style: {
        overflow: scrollerStyle.overflow,
        [vertical ? `margin${isRtl ? "Left" : "Right"}` : "marginBottom"]: visibleScrollbar ? void 0 : -scrollerStyle.scrollbarWidth
      }
    }
  });
  const [ListSlot, listSlotProps] = useSlot("list", {
    ref: tabListRef,
    className: clsx_default(classes2.list, classes2.flexContainer),
    elementType: List3,
    externalForwardedProps,
    ownerState,
    getSlotProps: (handlers) => ({
      ...handlers,
      onKeyDown: (event) => {
        var _a;
        handleKeyDown(event);
        (_a = handlers.onKeyDown) == null ? void 0 : _a.call(handlers, event);
      }
    })
  });
  return (0, import_jsx_runtime166.jsxs)(RootSlot, {
    ...rootSlotProps,
    children: [conditionalElements.scrollButtonStart, conditionalElements.scrollbarSizeListener, (0, import_jsx_runtime166.jsxs)(ScrollerSlot, {
      ...scrollerSlotProps,
      children: [(0, import_jsx_runtime166.jsx)(ListSlot, {
        "aria-label": ariaLabel,
        "aria-labelledby": ariaLabelledBy,
        "aria-orientation": orientation === "vertical" ? "vertical" : null,
        role: "tablist",
        ...listSlotProps,
        children: children2
      }), mounted && indicator]
    }), conditionalElements.scrollButtonEnd]
  });
});
true ? Tabs.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * Callback fired when the component mounts.
   * This is useful when you want to trigger an action programmatically.
   * It supports two actions: `updateIndicator()` and `updateScrollButtons()`
   *
   * @param {object} actions This object contains all possible actions
   * that can be triggered programmatically.
   */
  action: refType_default,
  /**
   * If `true`, the scroll buttons aren't forced hidden on mobile.
   * By default the scroll buttons are hidden on mobile and takes precedence over `scrollButtons`.
   * @default false
   */
  allowScrollButtonsMobile: import_prop_types136.default.bool,
  /**
   * The label for the Tabs as a string.
   */
  "aria-label": import_prop_types136.default.string,
  /**
   * An id or list of ids separated by a space that label the Tabs.
   */
  "aria-labelledby": import_prop_types136.default.string,
  /**
   * If `true`, the tabs are centered.
   * This prop is intended for large views.
   * @default false
   */
  centered: import_prop_types136.default.bool,
  /**
   * The content of the component.
   */
  children: import_prop_types136.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types136.default.object,
  /**
   * @ignore
   */
  className: import_prop_types136.default.string,
  /**
   * The component used for the root node.
   * Either a string to use a HTML element or a component.
   */
  component: import_prop_types136.default.elementType,
  /**
   * Determines the color of the indicator.
   * @default 'primary'
   */
  indicatorColor: import_prop_types136.default.oneOfType([import_prop_types136.default.oneOf(["primary", "secondary"]), import_prop_types136.default.string]),
  /**
   * Callback fired when the value changes.
   *
   * @param {React.SyntheticEvent} event The event source of the callback. **Warning**: This is a generic event not a change event.
   * @param {any} value We default to the index of the child (number)
   */
  onChange: import_prop_types136.default.func,
  /**
   * The component orientation (layout flow direction).
   * @default 'horizontal'
   */
  orientation: import_prop_types136.default.oneOf(["horizontal", "vertical"]),
  /**
   * The component used to render the scroll buttons.
   * @deprecated use the `slots.scrollButtons` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default TabScrollButton
   */
  ScrollButtonComponent: import_prop_types136.default.elementType,
  /**
   * Determine behavior of scroll buttons when tabs are set to scroll:
   *
   * - `auto` will only present them when not all the items are visible.
   * - `true` will always present them.
   * - `false` will never present them.
   *
   * By default the scroll buttons are hidden on mobile.
   * This behavior can be disabled with `allowScrollButtonsMobile`.
   * @default 'auto'
   */
  scrollButtons: import_prop_types136.default.oneOf(["auto", false, true]),
  /**
   * If `true` the selected tab changes on focus. Otherwise it only
   * changes on activation.
   */
  selectionFollowsFocus: import_prop_types136.default.bool,
  /**
   * The props used for each slot inside.
   * @default {}
   */
  slotProps: import_prop_types136.default.shape({
    endScrollButtonIcon: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    indicator: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    list: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    root: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    scrollbar: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    scrollButtons: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    scroller: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object]),
    startScrollButtonIcon: import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object])
  }),
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots: import_prop_types136.default.shape({
    endScrollButtonIcon: import_prop_types136.default.elementType,
    EndScrollButtonIcon: import_prop_types136.default.elementType,
    indicator: import_prop_types136.default.elementType,
    list: import_prop_types136.default.elementType,
    root: import_prop_types136.default.elementType,
    scrollbar: import_prop_types136.default.elementType,
    scrollButtons: import_prop_types136.default.elementType,
    scroller: import_prop_types136.default.elementType,
    startScrollButtonIcon: import_prop_types136.default.elementType,
    StartScrollButtonIcon: import_prop_types136.default.elementType
  }),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types136.default.oneOfType([import_prop_types136.default.arrayOf(import_prop_types136.default.oneOfType([import_prop_types136.default.func, import_prop_types136.default.object, import_prop_types136.default.bool])), import_prop_types136.default.func, import_prop_types136.default.object]),
  /**
   * Props applied to the tab indicator element.
   * @deprecated use the `slotProps.indicator` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default  {}
   */
  TabIndicatorProps: import_prop_types136.default.object,
  /**
   * Props applied to the [`TabScrollButton`](https://mui.com/material-ui/api/tab-scroll-button/) element.
   * @deprecated use the `slotProps.scrollButtons` prop instead. This prop will be removed in a future major release. See [Migrating from deprecated APIs](https://mui.com/material-ui/migration/migrating-from-deprecated-apis/) for more details.
   * @default {}
   */
  TabScrollButtonProps: import_prop_types136.default.object,
  /**
   * Determines the color of the `Tab`.
   * @default 'primary'
   */
  textColor: import_prop_types136.default.oneOf(["inherit", "primary", "secondary"]),
  /**
   * The value of the currently selected `Tab`.
   * If you don't want any selected `Tab`, you can set this prop to `false`.
   */
  value: import_prop_types136.default.any,
  /**
   * Determines additional display behavior of the tabs:
   *
   *  - `scrollable` will invoke scrolling properties and allow for horizontally
   *  scrolling (or swiping) of the tab bar.
   *  - `fullWidth` will make the tabs grow to use all the available space,
   *  which should be used for small views, like on mobile.
   *  - `standard` will render the default state.
   * @default 'standard'
   */
  variant: import_prop_types136.default.oneOf(["fullWidth", "scrollable", "standard"]),
  /**
   * If `true`, the scrollbar is visible. It can be useful when displaying
   * a long vertical list of tabs.
   * @default false
   */
  visibleScrollbar: import_prop_types136.default.bool
} : void 0;
var Tabs_default = Tabs;

// node_modules/@mui/material/esm/Tab/Tab.js
var React198 = __toESM(require_react(), 1);
var import_prop_types137 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/Tab/tabClasses.js
function getTabUtilityClass(slot) {
  return generateUtilityClass("MuiTab", slot);
}
var tabClasses = generateUtilityClasses("MuiTab", ["root", "labelIcon", "textColorInherit", "textColorPrimary", "textColorSecondary", "selected", "disabled", "fullWidth", "wrapped", "iconWrapper", "icon"]);
var tabClasses_default = tabClasses;

// node_modules/@mui/material/esm/Tab/Tab.js
var import_jsx_runtime167 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses91 = (ownerState) => {
  const {
    classes: classes2,
    textColor,
    fullWidth,
    wrapped,
    icon,
    label,
    selected,
    disabled
  } = ownerState;
  const slots = {
    root: ["root", icon && label && "labelIcon", `textColor${capitalize_default(textColor)}`, fullWidth && "fullWidth", wrapped && "wrapped", selected && "selected", disabled && "disabled"],
    icon: ["iconWrapper", "icon"]
  };
  return composeClasses(slots, getTabUtilityClass, classes2);
};
var TabRoot = styled_default2(ButtonBase_default, {
  name: "MuiTab",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, ownerState.label && ownerState.icon && styles5.labelIcon, styles5[`textColor${capitalize_default(ownerState.textColor)}`], ownerState.fullWidth && styles5.fullWidth, ownerState.wrapped && styles5.wrapped, {
      [`& .${tabClasses_default.iconWrapper}`]: styles5.iconWrapper
    }, {
      [`& .${tabClasses_default.icon}`]: styles5.icon
    }];
  }
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.button,
  maxWidth: 360,
  minWidth: 90,
  position: "relative",
  minHeight: 48,
  flexShrink: 0,
  padding: "12px 16px",
  overflow: "hidden",
  whiteSpace: "normal",
  textAlign: "center",
  lineHeight: 1.25,
  variants: [{
    props: ({
      ownerState
    }) => ownerState.label && (ownerState.iconPosition === "top" || ownerState.iconPosition === "bottom"),
    style: {
      flexDirection: "column"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.label && ownerState.iconPosition !== "top" && ownerState.iconPosition !== "bottom",
    style: {
      flexDirection: "row"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.icon && ownerState.label,
    style: {
      minHeight: 72,
      paddingTop: 9,
      paddingBottom: 9
    }
  }, {
    props: ({
      ownerState,
      iconPosition
    }) => ownerState.icon && ownerState.label && iconPosition === "top",
    style: {
      [`& > .${tabClasses_default.icon}`]: {
        marginBottom: 6
      }
    }
  }, {
    props: ({
      ownerState,
      iconPosition
    }) => ownerState.icon && ownerState.label && iconPosition === "bottom",
    style: {
      [`& > .${tabClasses_default.icon}`]: {
        marginTop: 6
      }
    }
  }, {
    props: ({
      ownerState,
      iconPosition
    }) => ownerState.icon && ownerState.label && iconPosition === "start",
    style: {
      [`& > .${tabClasses_default.icon}`]: {
        marginRight: theme.spacing(1)
      }
    }
  }, {
    props: ({
      ownerState,
      iconPosition
    }) => ownerState.icon && ownerState.label && iconPosition === "end",
    style: {
      [`& > .${tabClasses_default.icon}`]: {
        marginLeft: theme.spacing(1)
      }
    }
  }, {
    props: {
      textColor: "inherit"
    },
    style: {
      color: "inherit",
      opacity: 0.6,
      // same opacity as theme.palette.text.secondary
      [`&.${tabClasses_default.selected}`]: {
        opacity: 1
      },
      [`&.${tabClasses_default.disabled}`]: {
        opacity: (theme.vars || theme).palette.action.disabledOpacity
      }
    }
  }, {
    props: {
      textColor: "primary"
    },
    style: {
      color: (theme.vars || theme).palette.text.secondary,
      [`&.${tabClasses_default.selected}`]: {
        color: (theme.vars || theme).palette.primary.main
      },
      [`&.${tabClasses_default.disabled}`]: {
        color: (theme.vars || theme).palette.text.disabled
      }
    }
  }, {
    props: {
      textColor: "secondary"
    },
    style: {
      color: (theme.vars || theme).palette.text.secondary,
      [`&.${tabClasses_default.selected}`]: {
        color: (theme.vars || theme).palette.secondary.main
      },
      [`&.${tabClasses_default.disabled}`]: {
        color: (theme.vars || theme).palette.text.disabled
      }
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.fullWidth,
    style: {
      flexShrink: 1,
      flexGrow: 1,
      flexBasis: 0,
      maxWidth: "none"
    }
  }, {
    props: ({
      ownerState
    }) => ownerState.wrapped,
    style: {
      fontSize: theme.typography.pxToRem(12)
    }
  }]
})));
var Tab = React198.forwardRef(function Tab2(inProps, ref) {
  const props = useDefaultProps({
    props: inProps,
    name: "MuiTab"
  });
  const {
    className,
    disabled = false,
    disableFocusRipple = false,
    // eslint-disable-next-line react/prop-types
    fullWidth,
    icon: iconProp,
    iconPosition = "top",
    // eslint-disable-next-line react/prop-types
    indicator,
    label,
    onChange,
    onClick,
    onFocus,
    // eslint-disable-next-line react/prop-types
    selected,
    // eslint-disable-next-line react/prop-types
    selectionFollowsFocus,
    // eslint-disable-next-line react/prop-types
    textColor = "inherit",
    value,
    wrapped = false,
    ...other
  } = props;
  const ownerState = {
    ...props,
    disabled,
    disableFocusRipple,
    selected,
    icon: !!iconProp,
    iconPosition,
    label: !!label,
    fullWidth,
    textColor,
    wrapped
  };
  const classes2 = useUtilityClasses91(ownerState);
  const icon = iconProp && label && React198.isValidElement(iconProp) ? React198.cloneElement(iconProp, {
    className: clsx_default(classes2.icon, iconProp.props.className)
  }) : iconProp;
  const handleClick = (event) => {
    if (!selected && onChange) {
      onChange(event, value);
    }
    if (onClick) {
      onClick(event);
    }
  };
  const handleFocus = (event) => {
    if (selectionFollowsFocus && !selected && onChange) {
      onChange(event, value);
    }
    if (onFocus) {
      onFocus(event);
    }
  };
  return (0, import_jsx_runtime167.jsxs)(TabRoot, {
    focusRipple: !disableFocusRipple,
    className: clsx_default(classes2.root, className),
    ref,
    role: "tab",
    "aria-selected": selected,
    disabled,
    onClick: handleClick,
    onFocus: handleFocus,
    ownerState,
    tabIndex: selected ? 0 : -1,
    ...other,
    children: [iconPosition === "top" || iconPosition === "start" ? (0, import_jsx_runtime167.jsxs)(React198.Fragment, {
      children: [icon, label]
    }) : (0, import_jsx_runtime167.jsxs)(React198.Fragment, {
      children: [label, icon]
    }), indicator]
  });
});
true ? Tab.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * This prop isn't supported.
   * Use the `component` prop if you need to change the children structure.
   */
  children: unsupportedProp_default,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types137.default.object,
  /**
   * @ignore
   */
  className: import_prop_types137.default.string,
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types137.default.bool,
  /**
   * If `true`, the  keyboard focus ripple is disabled.
   * @default false
   */
  disableFocusRipple: import_prop_types137.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   *
   * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
   * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
   * @default false
   */
  disableRipple: import_prop_types137.default.bool,
  /**
   * The icon to display.
   */
  icon: import_prop_types137.default.oneOfType([import_prop_types137.default.element, import_prop_types137.default.string]),
  /**
   * The position of the icon relative to the label.
   * @default 'top'
   */
  iconPosition: import_prop_types137.default.oneOf(["bottom", "end", "start", "top"]),
  /**
   * The label element.
   */
  label: import_prop_types137.default.node,
  /**
   * @ignore
   */
  onChange: import_prop_types137.default.func,
  /**
   * @ignore
   */
  onClick: import_prop_types137.default.func,
  /**
   * @ignore
   */
  onFocus: import_prop_types137.default.func,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types137.default.oneOfType([import_prop_types137.default.arrayOf(import_prop_types137.default.oneOfType([import_prop_types137.default.func, import_prop_types137.default.object, import_prop_types137.default.bool])), import_prop_types137.default.func, import_prop_types137.default.object]),
  /**
   * You can provide your own value. Otherwise, we fallback to the child position index.
   */
  value: import_prop_types137.default.any,
  /**
   * Tab labels appear in a single row.
   * They can use a second line if needed.
   * @default false
   */
  wrapped: import_prop_types137.default.bool
} : void 0;
var Tab_default = Tab;

// node_modules/@mui/material/esm/ToggleButton/ToggleButton.js
var React201 = __toESM(require_react(), 1);
var import_prop_types138 = __toESM(require_prop_types(), 1);

// node_modules/@mui/material/esm/ToggleButton/toggleButtonClasses.js
function getToggleButtonUtilityClass(slot) {
  return generateUtilityClass("MuiToggleButton", slot);
}
var toggleButtonClasses = generateUtilityClasses("MuiToggleButton", ["root", "disabled", "selected", "standard", "primary", "secondary", "sizeSmall", "sizeMedium", "sizeLarge", "fullWidth"]);
var toggleButtonClasses_default = toggleButtonClasses;

// node_modules/@mui/material/esm/ToggleButtonGroup/ToggleButtonGroupContext.js
var React199 = __toESM(require_react(), 1);
var ToggleButtonGroupContext = React199.createContext({});
if (true) {
  ToggleButtonGroupContext.displayName = "ToggleButtonGroupContext";
}
var ToggleButtonGroupContext_default = ToggleButtonGroupContext;

// node_modules/@mui/material/esm/ToggleButtonGroup/ToggleButtonGroupButtonContext.js
var React200 = __toESM(require_react(), 1);
var ToggleButtonGroupButtonContext = React200.createContext(void 0);
if (true) {
  ToggleButtonGroupButtonContext.displayName = "ToggleButtonGroupButtonContext";
}
var ToggleButtonGroupButtonContext_default = ToggleButtonGroupButtonContext;

// node_modules/@mui/material/esm/ToggleButtonGroup/isValueSelected.js
function isValueSelected(value, candidate) {
  if (candidate === void 0 || value === void 0) {
    return false;
  }
  if (Array.isArray(candidate)) {
    return candidate.includes(value);
  }
  return value === candidate;
}

// node_modules/@mui/material/esm/ToggleButton/ToggleButton.js
var import_jsx_runtime168 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses92 = (ownerState) => {
  const {
    classes: classes2,
    fullWidth,
    selected,
    disabled,
    size,
    color
  } = ownerState;
  const slots = {
    root: ["root", selected && "selected", disabled && "disabled", fullWidth && "fullWidth", `size${capitalize_default(size)}`, color]
  };
  return composeClasses(slots, getToggleButtonUtilityClass, classes2);
};
var ToggleButtonRoot = styled_default2(ButtonBase_default, {
  name: "MuiToggleButton",
  slot: "Root",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.root, styles5[`size${capitalize_default(ownerState.size)}`]];
  }
})(memoTheme_default(({
  theme
}) => ({
  ...theme.typography.button,
  borderRadius: (theme.vars || theme).shape.borderRadius,
  padding: 11,
  border: `1px solid ${(theme.vars || theme).palette.divider}`,
  color: (theme.vars || theme).palette.action.active,
  [`&.${toggleButtonClasses_default.disabled}`]: {
    color: (theme.vars || theme).palette.action.disabled,
    border: `1px solid ${(theme.vars || theme).palette.action.disabledBackground}`
  },
  "&:hover": {
    textDecoration: "none",
    // Reset on mouse devices
    backgroundColor: theme.alpha((theme.vars || theme).palette.text.primary, (theme.vars || theme).palette.action.hoverOpacity),
    "@media (hover: none)": {
      backgroundColor: "transparent"
    }
  },
  variants: [{
    props: {
      color: "standard"
    },
    style: {
      [`&.${toggleButtonClasses_default.selected}`]: {
        color: (theme.vars || theme).palette.text.primary,
        backgroundColor: theme.alpha((theme.vars || theme).palette.text.primary, (theme.vars || theme).palette.action.selectedOpacity),
        "&:hover": {
          backgroundColor: theme.alpha((theme.vars || theme).palette.text.primary, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`),
          // Reset on touch devices, it doesn't add specificity
          "@media (hover: none)": {
            backgroundColor: theme.alpha((theme.vars || theme).palette.text.primary, (theme.vars || theme).palette.action.selectedOpacity)
          }
        }
      }
    }
  }, ...Object.entries(theme.palette).filter(createSimplePaletteValueFilter()).map(([color]) => ({
    props: {
      color
    },
    style: {
      [`&.${toggleButtonClasses_default.selected}`]: {
        color: (theme.vars || theme).palette[color].main,
        backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.selectedOpacity),
        "&:hover": {
          backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, `${(theme.vars || theme).palette.action.selectedOpacity} + ${(theme.vars || theme).palette.action.hoverOpacity}`),
          // Reset on touch devices, it doesn't add specificity
          "@media (hover: none)": {
            backgroundColor: theme.alpha((theme.vars || theme).palette[color].main, (theme.vars || theme).palette.action.selectedOpacity)
          }
        }
      }
    }
  })), {
    props: {
      fullWidth: true
    },
    style: {
      width: "100%"
    }
  }, {
    props: {
      size: "small"
    },
    style: {
      padding: 7,
      fontSize: theme.typography.pxToRem(13)
    }
  }, {
    props: {
      size: "large"
    },
    style: {
      padding: 15,
      fontSize: theme.typography.pxToRem(15)
    }
  }]
})));
var ToggleButton = React201.forwardRef(function ToggleButton2(inProps, ref) {
  const {
    value: contextValue,
    ...contextProps
  } = React201.useContext(ToggleButtonGroupContext_default);
  const toggleButtonGroupButtonContextPositionClassName = React201.useContext(ToggleButtonGroupButtonContext_default);
  const resolvedProps = resolveProps({
    ...contextProps,
    selected: isValueSelected(inProps.value, contextValue)
  }, inProps);
  const props = useDefaultProps({
    props: resolvedProps,
    name: "MuiToggleButton"
  });
  const {
    children: children2,
    className,
    color = "standard",
    disabled = false,
    disableFocusRipple = false,
    fullWidth = false,
    onChange,
    onClick,
    selected,
    size = "medium",
    value,
    ...other
  } = props;
  const ownerState = {
    ...props,
    color,
    disabled,
    disableFocusRipple,
    fullWidth,
    size
  };
  const classes2 = useUtilityClasses92(ownerState);
  const handleChange = (event) => {
    if (onClick) {
      onClick(event, value);
      if (event.defaultPrevented) {
        return;
      }
    }
    if (onChange) {
      onChange(event, value);
    }
  };
  const positionClassName = toggleButtonGroupButtonContextPositionClassName || "";
  return (0, import_jsx_runtime168.jsx)(ToggleButtonRoot, {
    className: clsx_default(contextProps.className, classes2.root, className, positionClassName),
    disabled,
    focusRipple: !disableFocusRipple,
    ref,
    onClick: handleChange,
    onChange,
    value,
    ownerState,
    "aria-pressed": selected,
    ...other,
    children: children2
  });
});
true ? ToggleButton.propTypes = {
  // ┌────────────────────────────── Warning ──────────────────────────────┐
  // │ These PropTypes are generated from the TypeScript type definitions. │
  // │    To update them, edit the d.ts file and run `pnpm proptypes`.     │
  // └─────────────────────────────────────────────────────────────────────┘
  /**
   * The content of the component.
   */
  children: import_prop_types138.default.node,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types138.default.object,
  /**
   * @ignore
   */
  className: import_prop_types138.default.string,
  /**
   * The color of the button when it is in an active state.
   * It supports both default and custom theme colors, which can be added as shown in the
   * [palette customization guide](https://mui.com/material-ui/customization/palette/#custom-colors).
   * @default 'standard'
   */
  color: import_prop_types138.default.oneOfType([import_prop_types138.default.oneOf(["standard", "primary", "secondary", "error", "info", "success", "warning"]), import_prop_types138.default.string]),
  /**
   * If `true`, the component is disabled.
   * @default false
   */
  disabled: import_prop_types138.default.bool,
  /**
   * If `true`, the  keyboard focus ripple is disabled.
   * @default false
   */
  disableFocusRipple: import_prop_types138.default.bool,
  /**
   * If `true`, the ripple effect is disabled.
   *
   * ⚠️ Without a ripple there is no styling for :focus-visible by default. Be sure
   * to highlight the element by applying separate styles with the `.Mui-focusVisible` class.
   * @default false
   */
  disableRipple: import_prop_types138.default.bool,
  /**
   * If `true`, the button will take up the full width of its container.
   * @default false
   */
  fullWidth: import_prop_types138.default.bool,
  /**
   * Callback fired when the state changes.
   *
   * @param {React.MouseEvent<HTMLElement>} event The event source of the callback.
   * @param {any} value of the selected button.
   */
  onChange: import_prop_types138.default.func,
  /**
   * Callback fired when the button is clicked.
   *
   * @param {React.MouseEvent<HTMLElement>} event The event source of the callback.
   * @param {any} value of the selected button.
   */
  onClick: import_prop_types138.default.func,
  /**
   * If `true`, the button is rendered in an active state.
   */
  selected: import_prop_types138.default.bool,
  /**
   * The size of the component.
   * The prop defaults to the value inherited from the parent ToggleButtonGroup component.
   * @default 'medium'
   */
  size: import_prop_types138.default.oneOfType([import_prop_types138.default.oneOf(["small", "medium", "large"]), import_prop_types138.default.string]),
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types138.default.oneOfType([import_prop_types138.default.arrayOf(import_prop_types138.default.oneOfType([import_prop_types138.default.func, import_prop_types138.default.object, import_prop_types138.default.bool])), import_prop_types138.default.func, import_prop_types138.default.object]),
  /**
   * The value to associate with the button when selected in a
   * ToggleButtonGroup.
   */
  value: import_prop_types138.default.any.isRequired
} : void 0;
var ToggleButton_default = ToggleButton;

// node_modules/@mui/x-data-grid/esm/components/GridColumnUnsortedIcon.js
var React202 = __toESM(require_react(), 1);
var import_jsx_runtime169 = __toESM(require_jsx_runtime(), 1);
var _excluded61 = ["sortingOrder"];
var GridColumnUnsortedIcon = React202.memo(function GridColumnHeaderSortIcon2(props) {
  const {
    sortingOrder
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded61);
  const rootProps = useGridRootProps();
  const [nextSortDirection] = sortingOrder;
  const Icon = nextSortDirection === "asc" ? rootProps.slots.columnSortedAscendingIcon : rootProps.slots.columnSortedDescendingIcon;
  return Icon ? (0, import_jsx_runtime169.jsx)(Icon, _extends({}, other)) : null;
});
if (true) GridColumnUnsortedIcon.displayName = "GridColumnUnsortedIcon";

// node_modules/@mui/x-data-grid/esm/material/index.js
var import_jsx_runtime170 = __toESM(require_jsx_runtime(), 1);

// node_modules/@mui/x-data-grid/esm/material/variables.js
var React203 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/hash/hash.js
var encoder = new TextEncoder();
var bufferLength = 2 * 1024;
var buffer = new ArrayBuffer(bufferLength);
var uint8View = new Uint8Array(buffer);
var int32View = new Int32Array(buffer);
var hash3 = xxh;
function xxh(input) {
  const requiredLength = input.length * 2;
  if (requiredLength > bufferLength) {
    bufferLength = requiredLength + (4 - requiredLength % 4);
    buffer = new ArrayBuffer(bufferLength);
    uint8View = new Uint8Array(buffer);
    int32View = new Int32Array(buffer);
  }
  const length8 = encoder.encodeInto(input, uint8View).written;
  const seed = 0;
  const len = length8 | 0;
  let i2 = 0;
  let h = (seed + len | 0) + 374761393 | 0;
  if (len < 16) {
    for (; (i2 + 3 | 0) < len; i2 = i2 + 4 | 0) {
      h = Math.imul(rotl32(h + Math.imul(int32View[i2] | 0, 3266489917) | 0, 17) | 0, 668265263);
    }
  } else {
    let v0 = seed + 606290984 | 0;
    let v1 = seed + 2246822519 | 0;
    let v2 = seed;
    let v3 = seed - 2654435761 | 0;
    for (; (i2 + 15 | 0) < len; i2 = i2 + 16 | 0) {
      v0 = Math.imul(rotl32(v0 + Math.imul(int32View[i2 + 0 | 0] | 0, 2246822519) | 0, 13) | 0, 2654435761);
      v1 = Math.imul(rotl32(v1 + Math.imul(int32View[i2 + 4 | 0] | 0, 2246822519) | 0, 13) | 0, 2654435761);
      v2 = Math.imul(rotl32(v2 + Math.imul(int32View[i2 + 8 | 0] | 0, 2246822519) | 0, 13) | 0, 2654435761);
      v3 = Math.imul(rotl32(v3 + Math.imul(int32View[i2 + 12 | 0] | 0, 2246822519) | 0, 13) | 0, 2654435761);
    }
    h = (((rotl32(v0, 1) | 0 + rotl32(v1, 7) | 0) + rotl32(v2, 12) | 0) + rotl32(v3, 18) | 0) + len | 0;
    for (; (i2 + 3 | 0) < len; i2 = i2 + 4 | 0) {
      h = Math.imul(rotl32(h + Math.imul(int32View[i2] | 0, 3266489917) | 0, 17) | 0, 668265263);
    }
  }
  for (; i2 < len; i2 = i2 + 1 | 0) {
    h = Math.imul(rotl32(h + Math.imul(uint8View[i2] | 0, 374761393) | 0, 11) | 0, 2654435761);
  }
  h = Math.imul(h ^ h >>> 15, 2246822519);
  h = Math.imul(h ^ h >>> 13, 3266489917);
  return ((h ^ h >>> 16) >>> 0).toString();
}
function rotl32(x, r) {
  return x << r | x >>> 32 - r;
}

// node_modules/@mui/x-data-grid/esm/material/variables.js
function useMaterialCSSVariables() {
  const theme = useTheme();
  return React203.useMemo(() => {
    const id = hash3(stringifyTheme(theme));
    const variables = transformTheme(theme);
    return {
      id,
      variables
    };
  }, [theme]);
}
function transformTheme(t) {
  var _a, _b, _c, _d, _e, _f;
  const borderColor = getBorderColor(t);
  const dataGridPalette = (t.vars || t).palette.DataGrid;
  const paperColor = (t.vars || t).palette.background.paper;
  const backgroundBase = (dataGridPalette == null ? void 0 : dataGridPalette.bg) ?? (t.palette.mode === "dark" ? colorMixIfSupported(`color-mix(in srgb, ${paperColor} 95%, #fff)`, paperColor) : paperColor);
  const backgroundHeader = (dataGridPalette == null ? void 0 : dataGridPalette.headerBg) ?? backgroundBase;
  const backgroundPinned = (dataGridPalette == null ? void 0 : dataGridPalette.pinnedBg) ?? backgroundBase;
  const backgroundBackdrop = t.vars ? `rgba(${t.vars.palette.background.defaultChannel} / ${t.vars.palette.action.disabledOpacity})` : alpha(t.palette.background.default, t.palette.action.disabledOpacity);
  const backgroundOverlay = t.palette.mode === "dark" ? colorMixIfSupported(`color-mix(in srgb, ${paperColor} 90%, #fff)`, paperColor) : paperColor;
  const selectedColor = t.vars ? `rgb(${t.vars.palette.primary.mainChannel})` : t.palette.primary.main;
  const radius = getRadius(t);
  const fontBody = ((_b = (_a = t.vars) == null ? void 0 : _a.font) == null ? void 0 : _b.body2) ?? formatFont(t.typography.body2);
  const fontSmall = ((_d = (_c = t.vars) == null ? void 0 : _c.font) == null ? void 0 : _d.caption) ?? formatFont(t.typography.caption);
  const fontLarge = ((_f = (_e = t.vars) == null ? void 0 : _e.font) == null ? void 0 : _f.body1) ?? formatFont(t.typography.body1);
  const k2 = vars.keys;
  return {
    [k2.spacingUnit]: t.vars ? t.vars.spacing ?? t.spacing(1) : t.spacing(1),
    [k2.colors.border.base]: borderColor,
    [k2.colors.background.base]: backgroundBase,
    [k2.colors.background.overlay]: backgroundOverlay,
    [k2.colors.background.backdrop]: backgroundBackdrop,
    [k2.colors.foreground.base]: (t.vars || t).palette.text.primary,
    [k2.colors.foreground.muted]: (t.vars || t).palette.text.secondary,
    [k2.colors.foreground.accent]: (t.vars || t).palette.primary.dark,
    [k2.colors.foreground.disabled]: (t.vars || t).palette.text.disabled,
    [k2.colors.foreground.error]: (t.vars || t).palette.error.dark,
    [k2.colors.interactive.hover]: supportsColorMix ? (t.vars || t).palette.action.hover : (t.vars || t).palette.grey[t.palette.mode === "dark" ? 800 : 100],
    [k2.colors.interactive.hoverOpacity]: (t.vars || t).palette.action.hoverOpacity,
    [k2.colors.interactive.focus]: removeOpacity2((t.vars || t).palette.primary.main),
    [k2.colors.interactive.focusOpacity]: (t.vars || t).palette.action.focusOpacity,
    [k2.colors.interactive.disabled]: removeOpacity2((t.vars || t).palette.action.disabled),
    [k2.colors.interactive.disabledOpacity]: (t.vars || t).palette.action.disabledOpacity,
    [k2.colors.interactive.selected]: supportsColorMix ? selectedColor : (t.vars || t).palette.grey[t.palette.mode === "dark" ? 700 : 200],
    [k2.colors.interactive.selectedOpacity]: (t.vars || t).palette.action.selectedOpacity,
    [k2.header.background.base]: backgroundHeader,
    [k2.cell.background.pinned]: backgroundPinned,
    [k2.radius.base]: radius,
    [k2.typography.fontFamily.base]: t.typography.fontFamily,
    [k2.typography.fontWeight.light]: t.typography.fontWeightLight,
    [k2.typography.fontWeight.regular]: t.typography.fontWeightRegular,
    [k2.typography.fontWeight.medium]: t.typography.fontWeightMedium,
    [k2.typography.fontWeight.bold]: t.typography.fontWeightBold,
    [k2.typography.font.body]: fontBody,
    [k2.typography.font.small]: fontSmall,
    [k2.typography.font.large]: fontLarge,
    [k2.transitions.easing.easeIn]: t.transitions.easing.easeIn,
    [k2.transitions.easing.easeOut]: t.transitions.easing.easeOut,
    [k2.transitions.easing.easeInOut]: t.transitions.easing.easeInOut,
    [k2.transitions.duration.short]: `${t.transitions.duration.shorter}ms`,
    [k2.transitions.duration.base]: `${t.transitions.duration.short}ms`,
    [k2.transitions.duration.long]: `${t.transitions.duration.standard}ms`,
    [k2.shadows.base]: (t.vars || t).shadows[2],
    [k2.shadows.overlay]: (t.vars || t).shadows[8],
    [k2.zIndex.panel]: (t.vars || t).zIndex.modal,
    [k2.zIndex.menu]: (t.vars || t).zIndex.modal
  };
}
function getRadius(theme) {
  if (theme.vars) {
    return theme.vars.shape.borderRadius;
  }
  return typeof theme.shape.borderRadius === "number" ? `${theme.shape.borderRadius}px` : theme.shape.borderRadius;
}
function getBorderColor(theme) {
  if (theme.vars) {
    return theme.vars.palette.TableCell.border;
  }
  if (theme.palette.mode === "light") {
    return lighten(alpha(theme.palette.divider, 1), 0.88);
  }
  return darken(alpha(theme.palette.divider, 1), 0.68);
}
function setOpacity2(color, opacity) {
  return `rgba(from ${color} r g b / ${opacity})`;
}
function removeOpacity2(color) {
  return setOpacity2(color, 1);
}
function formatFont(font) {
  if (!font) {
    return void 0;
  }
  const fontSize = typeof font.fontSize === "number" ? `${font.fontSize}px` : font.fontSize;
  return `${font.fontWeight} ${fontSize} / ${font.lineHeight} ${font.fontFamily}`;
}
function stringifyTheme(input) {
  const seen = /* @__PURE__ */ new WeakSet();
  return JSON.stringify(input, (_, v) => {
    if (typeof window !== "undefined" && v === window || typeof document !== "undefined" && v === document) {
      return v.toString();
    }
    if (v !== null && typeof v === "object") {
      if (React203.isValidElement(v)) {
        return null;
      }
      if (seen.has(v)) {
        return null;
      }
      seen.add(v);
    }
    return v;
  });
}

// node_modules/@mui/x-data-grid/esm/material/index.js
var _excluded62 = ["id", "label", "labelId", "material", "disabled", "slotProps", "onChange", "onKeyDown", "onOpen", "onClose", "size", "style", "fullWidth"];
var _excluded214 = ["onRowsPerPageChange", "material", "disabled"];
var _excluded312 = ["material"];
var _excluded410 = ["autoFocus", "label", "fullWidth", "slotProps", "className", "material"];
var _excluded510 = ["material"];
var _excluded63 = ["material"];
var _excluded72 = ["material"];
var _excluded82 = ["material"];
var _excluded92 = ["material"];
var _excluded0 = ["material"];
var _excluded1 = ["material"];
var _excluded102 = ["material"];
var _excluded112 = ["material"];
var _excluded122 = ["material", "label", "className"];
var _excluded132 = ["material"];
var _excluded142 = ["inert", "iconStart", "iconEnd", "children", "material"];
var _excluded152 = ["slotProps", "material"];
var _excluded162 = ["id", "multiple", "freeSolo", "options", "getOptionLabel", "isOptionEqualToValue", "value", "onChange", "label", "placeholder", "slotProps", "material"];
var _excluded172 = ["key"];
var _excluded182 = ["inputProps", "InputProps", "InputLabelProps"];
var _excluded192 = ["slotProps", "material"];
var _excluded202 = ["material"];
var _excluded215 = ["ref", "open", "children", "className", "clickAwayTouchEvent", "clickAwayMouseEvent", "flip", "focusTrap", "onExited", "onClickAway", "onDidShow", "onDidHide", "id", "target", "transition", "placement", "material"];
var _excluded222 = ["native"];
var _excluded232 = ["children", "value", "active"];
var _excluded242 = ["items", "value", "material"];
var InputAdornment3 = styled_default2(InputAdornment_default, {
  slot: "internal"
})(({
  theme
}) => ({
  [`&.${inputAdornmentClasses_default.positionEnd} .${iconButtonClasses_default.sizeSmall}`]: {
    marginRight: theme.spacing(-0.75)
  }
}));
var FormControlLabel3 = styled_default2(FormControlLabel_default, {
  slot: "internal",
  shouldForwardProp: (prop) => prop !== "fullWidth"
})(({
  theme
}) => ({
  gap: theme.spacing(0.5),
  margin: 0,
  overflow: "hidden",
  [`& .${formControlLabelClasses_default.label}`]: {
    fontSize: theme.typography.pxToRem(14),
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap"
  },
  variants: [{
    props: {
      fullWidth: true
    },
    style: {
      width: "100%"
    }
  }]
}));
var Checkbox3 = styled_default2(Checkbox_default, {
  slot: "internal",
  shouldForwardProp: (prop) => prop !== "density"
})(({
  theme
}) => ({
  variants: [{
    props: {
      density: "compact"
    },
    style: {
      padding: theme.spacing(0.5)
    }
  }]
}));
var ListItemText3 = styled_default2(ListItemText_default, {
  slot: "internal"
})({
  [`& .${listItemTextClasses_default.primary}`]: {
    overflowX: "clip",
    textOverflow: "ellipsis",
    maxWidth: "300px"
  }
});
var BaseSelect = forwardRef2(function BaseSelect2(props, ref) {
  var _a, _b;
  const {
    id,
    label,
    labelId,
    material,
    disabled,
    slotProps,
    onChange,
    onKeyDown,
    onOpen,
    onClose,
    size,
    style,
    fullWidth
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded62);
  const theme = useTheme();
  const textFieldDefaults = ((_b = (_a = theme.components) == null ? void 0 : _a.MuiTextField) == null ? void 0 : _b.defaultProps) ?? {};
  const computedSize = size ?? textFieldDefaults.size;
  const computedVariant = textFieldDefaults.variant ?? "outlined";
  const menuProps = {
    PaperProps: {
      onKeyDown
    }
  };
  if (onClose) {
    menuProps.onClose = onClose;
  }
  return (0, import_jsx_runtime170.jsxs)(FormControl_default, {
    size: computedSize,
    fullWidth,
    style,
    disabled,
    ref,
    children: [(0, import_jsx_runtime170.jsx)(InputLabel_default, {
      id: labelId,
      htmlFor: id,
      shrink: true,
      variant: computedVariant,
      children: label
    }), (0, import_jsx_runtime170.jsx)(Select_default, _extends({
      id,
      labelId,
      label,
      displayEmpty: true,
      onChange,
      variant: computedVariant
    }, other, {
      inputProps: slotProps == null ? void 0 : slotProps.htmlInput,
      onOpen,
      MenuProps: menuProps,
      size: computedSize
    }, material))]
  });
});
if (true) BaseSelect.displayName = "BaseSelect";
var StyledPagination = styled_default2(TablePagination_default, {
  slot: "internal"
})(({
  theme
}) => ({
  [`& .${tablePaginationClasses_default.selectLabel}`]: {
    display: "none",
    [theme.breakpoints.up("sm")]: {
      display: "block"
    }
  },
  [`& .${tablePaginationClasses_default.input}`]: {
    display: "none",
    [theme.breakpoints.up("sm")]: {
      display: "inline-flex"
    }
  }
}));
var BasePagination = forwardRef2(function BasePagination2(props, ref) {
  const {
    onRowsPerPageChange,
    material,
    disabled
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded214);
  const computedProps = React204.useMemo(() => {
    if (!disabled) {
      return void 0;
    }
    return {
      backIconButtonProps: {
        disabled: true
      },
      nextIconButtonProps: {
        disabled: true
      }
    };
  }, [disabled]);
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const {
    estimatedRowCount
  } = rootProps;
  return (0, import_jsx_runtime170.jsx)(StyledPagination, _extends({
    component: "div",
    onRowsPerPageChange: useEventCallback_default((event) => {
      onRowsPerPageChange == null ? void 0 : onRowsPerPageChange(Number(event.target.value));
    }),
    labelRowsPerPage: apiRef.current.getLocaleText("paginationRowsPerPage"),
    labelDisplayedRows: (params) => apiRef.current.getLocaleText("paginationDisplayedRows")(_extends({}, params, {
      estimated: estimatedRowCount
    })),
    getItemAriaLabel: apiRef.current.getLocaleText("paginationItemAriaLabel")
  }, computedProps, other, material, {
    ref
  }));
});
if (true) BasePagination.displayName = "BasePagination";
var BaseBadge = forwardRef2(function BaseBadge2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded312);
  return (0, import_jsx_runtime170.jsx)(Badge_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseBadge.displayName = "BaseBadge";
var BaseCheckbox = forwardRef2(function BaseCheckbox2(props, ref) {
  const {
    autoFocus,
    label,
    fullWidth,
    slotProps,
    className,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded410);
  const elementRef = React204.useRef(null);
  const handleRef = useForkRef(elementRef, ref);
  const rippleRef = React204.useRef(null);
  React204.useEffect(() => {
    var _a;
    if (autoFocus) {
      const input = (_a = elementRef.current) == null ? void 0 : _a.querySelector("input");
      input == null ? void 0 : input.focus({
        preventScroll: true
      });
    } else if (autoFocus === false && rippleRef.current) {
      rippleRef.current.stop({});
    }
  }, [autoFocus]);
  if (!label) {
    return (0, import_jsx_runtime170.jsx)(Checkbox3, _extends({}, other, material, {
      className: clsx_default(className, material == null ? void 0 : material.className),
      inputProps: slotProps == null ? void 0 : slotProps.htmlInput,
      ref: handleRef,
      touchRippleRef: rippleRef
    }));
  }
  return (0, import_jsx_runtime170.jsx)(FormControlLabel3, {
    className,
    control: (0, import_jsx_runtime170.jsx)(Checkbox3, _extends({}, other, material, {
      inputProps: slotProps == null ? void 0 : slotProps.htmlInput,
      ref: handleRef,
      touchRippleRef: rippleRef
    })),
    label,
    fullWidth
  });
});
if (true) BaseCheckbox.displayName = "BaseCheckbox";
var BaseCircularProgress = forwardRef2(function BaseCircularProgress2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded510);
  return (0, import_jsx_runtime170.jsx)(CircularProgress_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseCircularProgress.displayName = "BaseCircularProgress";
var BaseDivider = forwardRef2(function BaseDivider2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded63);
  return (0, import_jsx_runtime170.jsx)(Divider_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseDivider.displayName = "BaseDivider";
var BaseLinearProgress = forwardRef2(function BaseLinearProgress2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded72);
  return (0, import_jsx_runtime170.jsx)(LinearProgress_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseLinearProgress.displayName = "BaseLinearProgress";
var BaseButton = forwardRef2(function BaseButton2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded82);
  return (0, import_jsx_runtime170.jsx)(Button_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseButton.displayName = "BaseButton";
var StyledToggleButton = styled_default2(ToggleButton_default, {
  slot: "internal"
})(({
  theme
}) => ({
  gap: theme.spacing(1),
  border: 0
}));
var BaseToggleButton = forwardRef2(function BaseToggleButton2(props, ref) {
  const {
    material
  } = props, rest = _objectWithoutPropertiesLoose(props, _excluded92);
  return (0, import_jsx_runtime170.jsx)(StyledToggleButton, _extends({
    size: "small",
    color: "primary"
  }, rest, material, {
    ref
  }));
});
if (true) BaseToggleButton.displayName = "BaseToggleButton";
var BaseChip = forwardRef2(function BaseChip2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded0);
  return (0, import_jsx_runtime170.jsx)(Chip_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseChip.displayName = "BaseChip";
var BaseIconButton = forwardRef2(function BaseIconButton2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded1);
  return (0, import_jsx_runtime170.jsx)(IconButton_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseIconButton.displayName = "BaseIconButton";
var BaseTooltip = forwardRef2(function BaseTooltip2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded102);
  return (0, import_jsx_runtime170.jsx)(Tooltip_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseTooltip.displayName = "BaseTooltip";
var BaseSkeleton = forwardRef2(function BaseSkeleton2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded112);
  return (0, import_jsx_runtime170.jsx)(Skeleton_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseSkeleton.displayName = "BaseSkeleton";
var BaseSwitch = forwardRef2(function BaseSwitch2(props, ref) {
  const {
    material,
    label,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded122);
  if (!label) {
    return (0, import_jsx_runtime170.jsx)(Switch_default, _extends({}, other, material, {
      className,
      ref
    }));
  }
  return (0, import_jsx_runtime170.jsx)(FormControlLabel3, {
    className,
    control: (0, import_jsx_runtime170.jsx)(Switch_default, _extends({}, other, material, {
      ref
    })),
    label
  });
});
if (true) BaseSwitch.displayName = "BaseSwitch";
var BaseMenuList = forwardRef2(function BaseMenuList2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded132);
  return (0, import_jsx_runtime170.jsx)(MenuList_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseMenuList.displayName = "BaseMenuList";
function BaseMenuItem(props) {
  const {
    inert,
    iconStart,
    iconEnd,
    children: children2,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded142);
  if (inert) {
    other.disableRipple = true;
  }
  return React204.createElement(MenuItem_default, _extends({}, other, material), [iconStart && (0, import_jsx_runtime170.jsx)(ListItemIcon_default, {
    children: iconStart
  }, "1"), (0, import_jsx_runtime170.jsx)(ListItemText3, {
    children: children2
  }, "2"), iconEnd && (0, import_jsx_runtime170.jsx)(ListItemIcon_default, {
    children: iconEnd
  }, "3")]);
}
function BaseTextField(props) {
  var _a, _b;
  const {
    slotProps,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded152);
  const theme = useTheme();
  const textFieldDefaults = ((_b = (_a = theme.components) == null ? void 0 : _a.MuiTextField) == null ? void 0 : _b.defaultProps) ?? {};
  const computedVariant = other.variant ?? textFieldDefaults.variant ?? "outlined";
  const computedSize = other.size ?? textFieldDefaults.size;
  return (0, import_jsx_runtime170.jsx)(TextField_default, _extends({
    variant: computedVariant,
    size: computedSize
  }, other, material, {
    inputProps: slotProps == null ? void 0 : slotProps.htmlInput,
    InputProps: transformInputProps(slotProps == null ? void 0 : slotProps.input),
    InputLabelProps: _extends({
      shrink: true
    }, slotProps == null ? void 0 : slotProps.inputLabel)
  }));
}
function BaseAutocomplete(props) {
  const rootProps = useGridRootProps();
  const {
    id,
    multiple,
    freeSolo,
    options: options3,
    getOptionLabel,
    isOptionEqualToValue,
    value,
    onChange,
    label,
    placeholder,
    slotProps,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded162);
  return (0, import_jsx_runtime170.jsx)(Autocomplete_default, _extends({
    id,
    multiple,
    freeSolo,
    options: options3,
    getOptionLabel,
    isOptionEqualToValue,
    value,
    onChange,
    renderTags: (currentValue, getTagProps) => currentValue.map((option, index) => {
      const _getTagProps = getTagProps({
        index
      }), {
        key
      } = _getTagProps, tagProps = _objectWithoutPropertiesLoose(_getTagProps, _excluded172);
      return (0, import_jsx_runtime170.jsx)(Chip_default, _extends({
        variant: "outlined",
        size: "small",
        label: typeof option === "string" ? option : getOptionLabel == null ? void 0 : getOptionLabel(option)
      }, tagProps), key);
    }),
    renderInput: (params) => {
      var _a;
      const {
        inputProps,
        InputProps,
        InputLabelProps
      } = params, inputRest = _objectWithoutPropertiesLoose(params, _excluded182);
      return (0, import_jsx_runtime170.jsx)(TextField_default, _extends({}, inputRest, {
        label,
        placeholder,
        inputProps,
        InputProps: transformInputProps(InputProps, false),
        InputLabelProps: _extends({
          shrink: true
        }, InputLabelProps)
      }, slotProps == null ? void 0 : slotProps.textField, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField));
    }
  }, other, material));
}
function BaseInput(props) {
  return (0, import_jsx_runtime170.jsx)(InputBase_default, _extends({}, transformInputProps(props)));
}
function transformInputProps(props, wrapAdornments = true) {
  if (!props) {
    return void 0;
  }
  const {
    slotProps,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded192);
  const result = other;
  if (wrapAdornments) {
    if (result.startAdornment) {
      result.startAdornment = (0, import_jsx_runtime170.jsx)(InputAdornment3, {
        position: "start",
        children: result.startAdornment
      });
    }
    if (result.endAdornment) {
      result.endAdornment = (0, import_jsx_runtime170.jsx)(InputAdornment3, {
        position: "end",
        children: result.endAdornment
      });
    }
  }
  for (const k2 in material) {
    if (Object.hasOwn(material, k2)) {
      result[k2] = material[k2];
    }
  }
  if (slotProps == null ? void 0 : slotProps.htmlInput) {
    if (result.inputProps) {
      result.inputProps = _extends({}, result.inputProps, slotProps == null ? void 0 : slotProps.htmlInput);
    } else {
      result.inputProps = slotProps == null ? void 0 : slotProps.htmlInput;
    }
  }
  return result;
}
var BaseTextarea = forwardRef2(function BaseTextarea2(props, ref) {
  const {
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded202);
  return (0, import_jsx_runtime170.jsx)(TextareaAutosize_default, _extends({}, other, material, {
    ref
  }));
});
if (true) BaseTextarea.displayName = "BaseTextarea";
var transformOrigin = {
  "bottom-start": "top left",
  "bottom-end": "top right"
};
function BasePopper(props) {
  const {
    open,
    children: children2,
    className,
    flip: flip2,
    onExited,
    onDidShow,
    onDidHide,
    id,
    target,
    transition: transition2,
    placement,
    material
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded215);
  const modifiers = React204.useMemo(() => {
    const result = [{
      name: "preventOverflow",
      options: {
        padding: 8
      }
    }];
    if (flip2) {
      result.push({
        name: "flip",
        enabled: true
      });
    }
    if (onDidShow || onDidHide) {
      result.push({
        name: "isPlaced",
        enabled: true,
        phase: "main",
        fn: () => {
          onDidShow == null ? void 0 : onDidShow();
        },
        effect: () => () => {
          onDidHide == null ? void 0 : onDidHide();
        }
      });
    }
    return result;
  }, [flip2, onDidShow, onDidHide]);
  let content;
  if (!transition2) {
    content = wrappers(props, children2);
  } else {
    const handleExited = (popperOnExited) => (node) => {
      if (popperOnExited) {
        popperOnExited();
      }
      if (onExited) {
        onExited(node);
      }
    };
    content = (p) => {
      var _a;
      return wrappers(props, (0, import_jsx_runtime170.jsx)(Grow_default, _extends({}, p.TransitionProps, {
        style: {
          transformOrigin: transformOrigin[p.placement]
        },
        onExited: handleExited((_a = p.TransitionProps) == null ? void 0 : _a.onExited),
        children: (0, import_jsx_runtime170.jsx)(Paper_default, {
          children: children2
        })
      })));
    };
  }
  return (0, import_jsx_runtime170.jsx)(Popper_default, _extends({
    id,
    className,
    open,
    anchorEl: target,
    transition: transition2,
    placement,
    modifiers
  }, other, material, {
    children: content
  }));
}
function wrappers(props, content) {
  return focusTrapWrapper(props, clickAwayWrapper(props, content));
}
function clickAwayWrapper(props, content) {
  if (props.onClickAway === void 0) {
    return content;
  }
  return (0, import_jsx_runtime170.jsx)(ClickAwayListener, {
    onClickAway: props.onClickAway,
    touchEvent: props.clickAwayTouchEvent,
    mouseEvent: props.clickAwayMouseEvent,
    children: content
  });
}
function focusTrapWrapper(props, content) {
  if (props.focusTrap === void 0) {
    return content;
  }
  return (0, import_jsx_runtime170.jsx)(FocusTrap_default, {
    open: true,
    disableEnforceFocus: true,
    disableAutoFocus: true,
    children: (0, import_jsx_runtime170.jsx)("div", {
      tabIndex: -1,
      children: content
    })
  });
}
function BaseSelectOption(_ref) {
  let {
    native
  } = _ref, props = _objectWithoutPropertiesLoose(_ref, _excluded222);
  if (native) {
    return (0, import_jsx_runtime170.jsx)("option", _extends({}, props));
  }
  return (0, import_jsx_runtime170.jsx)(MenuItem_default, _extends({}, props));
}
var StyledTabs = styled_default2(Tabs_default, {
  name: "MuiDataGrid",
  slot: "Tabs"
})(({
  theme
}) => ({
  borderBottom: `1px solid ${theme.palette.divider}`
}));
var StyledTab = styled_default2(Tab_default, {
  name: "MuiDataGrid",
  slot: "Tab"
})({
  flex: 1,
  minWidth: "fit-content"
});
var StyledTabPanel = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "TabPanel"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  overflow: "hidden"
});
function TabPanel(props) {
  const {
    children: children2,
    active
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded232);
  return (0, import_jsx_runtime170.jsx)(StyledTabPanel, _extends({
    role: "tabpanel",
    style: {
      display: active ? "flex" : "none"
    }
  }, other, {
    children: children2
  }));
}
function BaseTabs(_ref2) {
  let {
    items,
    value,
    material
  } = _ref2, props = _objectWithoutPropertiesLoose(_ref2, _excluded242);
  const id = useId();
  const labelId = `${id}-tab-${value}`;
  const panelId = `${id}-tabpanel-${value}`;
  return (0, import_jsx_runtime170.jsxs)(React204.Fragment, {
    children: [(0, import_jsx_runtime170.jsx)(StyledTabs, _extends({}, props, {
      value,
      variant: "scrollable",
      scrollButtons: "auto"
    }, material, {
      children: items.map((item) => (0, import_jsx_runtime170.jsx)(StyledTab, {
        value: item.value,
        label: item.label,
        id: labelId,
        "aria-controls": panelId
      }, item.value))
    })), items.map((item) => (0, import_jsx_runtime170.jsx)(TabPanel, {
      value: item.value,
      active: value === item.value,
      id: panelId,
      "aria-labelledby": labelId,
      children: item.children
    }, item.value))]
  });
}
var iconSlots = {
  booleanCellTrueIcon: GridCheckIcon,
  booleanCellFalseIcon: GridCloseIcon,
  columnMenuIcon: GridTripleDotsVerticalIcon,
  openFilterButtonIcon: GridFilterListIcon,
  filterPanelDeleteIcon: GridCloseIcon,
  undoIcon: GridUndoIcon,
  redoIcon: GridRedoIcon,
  columnFilteredIcon: GridFilterAltIcon,
  columnSelectorIcon: GridColumnIcon,
  columnUnsortedIcon: GridColumnUnsortedIcon,
  columnSortedAscendingIcon: GridArrowUpwardIcon,
  columnSortedDescendingIcon: GridArrowDownwardIcon,
  columnResizeIcon: GridSeparatorIcon,
  densityCompactIcon: GridViewHeadlineIcon,
  densityStandardIcon: GridTableRowsIcon,
  densityComfortableIcon: GridViewStreamIcon,
  exportIcon: GridDownloadIcon,
  moreActionsIcon: GridMoreVertIcon,
  treeDataCollapseIcon: GridExpandMoreIcon,
  treeDataExpandIcon: GridKeyboardArrowRight,
  groupingCriteriaCollapseIcon: GridExpandMoreIcon,
  groupingCriteriaExpandIcon: GridKeyboardArrowRight,
  detailPanelExpandIcon: GridAddIcon,
  detailPanelCollapseIcon: GridRemoveIcon,
  rowReorderIcon: GridDragIcon,
  quickFilterIcon: GridSearchIcon,
  quickFilterClearIcon: GridClearIcon,
  columnMenuHideIcon: GridVisibilityOffIcon,
  columnMenuSortAscendingIcon: GridArrowUpwardIcon,
  columnMenuSortDescendingIcon: GridArrowDownwardIcon,
  columnMenuUnsortIcon: null,
  columnMenuFilterIcon: GridFilterAltIcon,
  columnMenuManageColumnsIcon: GridViewColumnIcon,
  columnMenuClearIcon: GridClearIcon,
  loadIcon: GridLoadIcon,
  filterPanelAddIcon: GridAddIcon,
  filterPanelRemoveAllIcon: GridDeleteForeverIcon,
  columnReorderIcon: GridDragIcon,
  menuItemCheckIcon: GridCheckIcon,
  longTextCellExpandIcon: GridLongTextCellExpandIcon,
  longTextCellCollapseIcon: GridLongTextCellCollapseIcon
};
var baseSlots = {
  baseAutocomplete: BaseAutocomplete,
  baseBadge: BaseBadge,
  baseCheckbox: BaseCheckbox,
  baseChip: BaseChip,
  baseCircularProgress: BaseCircularProgress,
  baseDivider: BaseDivider,
  baseInput: BaseInput,
  baseTextarea: BaseTextarea,
  baseLinearProgress: BaseLinearProgress,
  baseMenuList: BaseMenuList,
  baseMenuItem: BaseMenuItem,
  baseTextField: BaseTextField,
  baseButton: BaseButton,
  baseIconButton: BaseIconButton,
  baseToggleButton: BaseToggleButton,
  baseTooltip: BaseTooltip,
  baseTabs: BaseTabs,
  basePagination: BasePagination,
  basePopper: BasePopper,
  baseSelect: BaseSelect,
  baseSelectOption: BaseSelectOption,
  baseSkeleton: BaseSkeleton,
  baseSwitch: BaseSwitch
};
var materialSlots = _extends({}, baseSlots, iconSlots);
var material_default = materialSlots;

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridApiRef.js
var React205 = __toESM(require_react(), 1);
var useGridApiRef = () => React205.useRef(null);

// node_modules/@mui/x-data-grid/esm/DataGrid/DataGrid.js
var React258 = __toESM(require_react(), 1);
var import_prop_types140 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridAriaAttributes.js
var useGridAriaAttributes = () => {
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const visibleColumns = useGridSelector(apiRef, gridVisibleColumnDefinitionsSelector);
  const accessibleRowCount = useGridSelector(apiRef, gridExpandedRowCountSelector);
  const headerGroupingMaxDepth = useGridSelector(apiRef, gridColumnGroupsHeaderMaxDepthSelector);
  const pinnedRowsCount = useGridSelector(apiRef, gridPinnedRowsCountSelector);
  const ariaLabel = rootProps["aria-label"];
  const ariaLabelledby = rootProps["aria-labelledby"];
  const shouldUseLabelAsAriaLabel = !ariaLabel && !ariaLabelledby && rootProps.label;
  return {
    role: "grid",
    "aria-label": shouldUseLabelAsAriaLabel ? rootProps.label : ariaLabel,
    "aria-labelledby": ariaLabelledby,
    "aria-colcount": visibleColumns.length,
    "aria-rowcount": headerGroupingMaxDepth + 1 + pinnedRowsCount + accessibleRowCount,
    "aria-multiselectable": isMultipleRowSelectionEnabled(rootProps)
  };
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowAriaAttributes.js
var React206 = __toESM(require_react(), 1);
var useGridRowAriaAttributes = () => {
  const apiRef = useGridPrivateApiContext();
  const headerGroupingMaxDepth = useGridSelector(apiRef, gridColumnGroupsHeaderMaxDepthSelector);
  return React206.useCallback((rowNode, index) => {
    const ariaAttributes = {};
    const ariaRowIndex = index + headerGroupingMaxDepth + 2;
    ariaAttributes["aria-rowindex"] = ariaRowIndex;
    if (rowNode && apiRef.current.isRowSelectable(rowNode.id)) {
      ariaAttributes["aria-selected"] = apiRef.current.isRowSelected(rowNode.id);
    }
    return ariaAttributes;
  }, [apiRef, headerGroupingMaxDepth]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowsOverridableMethods.js
var React207 = __toESM(require_react(), 1);
var useGridRowsOverridableMethods = (apiRef) => {
  const setRowPosition = React207.useCallback((sourceRowId, targetRowId, position) => {
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    const targetNode = gridRowNodeSelector(apiRef, targetRowId);
    if (!sourceNode) {
      throw new Error(`MUI X: No row with id #${sourceRowId} found.`);
    }
    if (!targetNode) {
      throw new Error(`MUI X: No row with id #${targetRowId} found.`);
    }
    if (sourceNode.type !== "leaf") {
      throw new Error(`MUI X: The row reordering does not support reordering of footer or grouping rows.`);
    }
    if (position === "inside") {
      throw new Error(`MUI X: The 'inside' position is only supported for tree data. Use 'above' or 'below' for flat data.`);
    }
    const sortedFilteredRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const targetRowIndexUnadjusted = sortedFilteredRowIndexLookup[targetRowId];
    if (targetRowIndexUnadjusted === void 0) {
      throw new Error(`MUI X: Target row with id #${targetRowId} not found in current view.`);
    }
    const sourceRowIndex = sortedFilteredRowIndexLookup[sourceRowId];
    if (sourceRowIndex === void 0) {
      throw new Error(`MUI X: Source row with id #${sourceRowId} not found in current view.`);
    }
    const dragDirection = targetRowIndexUnadjusted < sourceRowIndex ? "up" : "down";
    let targetRowIndex;
    if (dragDirection === "up") {
      targetRowIndex = position === "above" ? targetRowIndexUnadjusted : targetRowIndexUnadjusted + 1;
    } else {
      targetRowIndex = position === "above" ? targetRowIndexUnadjusted - 1 : targetRowIndexUnadjusted;
    }
    if (targetRowIndex === sourceRowIndex) {
      return;
    }
    apiRef.current.setState((state) => {
      const group = gridRowTreeSelector(apiRef)[GRID_ROOT_GROUP_ID];
      const allRows = group.children;
      const updatedRows = [...allRows];
      updatedRows.splice(targetRowIndex, 0, updatedRows.splice(sourceRowIndex, 1)[0]);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: _extends({}, state.rows.tree, {
            [GRID_ROOT_GROUP_ID]: _extends({}, group, {
              children: updatedRows
            })
          })
        })
      });
    });
    apiRef.current.publishEvent("rowsSet");
  }, [apiRef]);
  const setRowIndex = React207.useCallback((rowId, targetIndex) => {
    const node = gridRowNodeSelector(apiRef, rowId);
    if (!node) {
      throw new Error(`MUI X: No row with id #${rowId} found.`);
    }
    if (node.type !== "leaf") {
      throw new Error(`MUI X: The row reordering does not support reordering of footer or grouping rows.`);
    }
    apiRef.current.setState((state) => {
      const group = gridRowTreeSelector(apiRef)[GRID_ROOT_GROUP_ID];
      const allRows = group.children;
      const oldIndex = allRows.findIndex((row) => row === rowId);
      if (oldIndex === -1 || oldIndex === targetIndex) {
        return state;
      }
      const updatedRows = [...allRows];
      updatedRows.splice(targetIndex, 0, updatedRows.splice(oldIndex, 1)[0]);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: _extends({}, state.rows.tree, {
            [GRID_ROOT_GROUP_ID]: _extends({}, group, {
              children: updatedRows
            })
          })
        })
      });
    });
    apiRef.current.publishEvent("rowsSet");
  }, [apiRef]);
  return {
    setRowIndex,
    setRowPosition
  };
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridParamsOverridableMethods.js
var React208 = __toESM(require_react(), 1);
var useGridParamsOverridableMethods = (apiRef) => {
  const getCellValue2 = React208.useCallback((id, field) => {
    const colDef = apiRef.current.getColumn(field);
    const row = apiRef.current.getRow(id);
    if (!row) {
      throw new Error(`No row with id #${id} found`);
    }
    if (!colDef || !colDef.valueGetter) {
      return row[field];
    }
    return colDef.valueGetter(row[colDef.field], row, colDef, apiRef);
  }, [apiRef]);
  const getRowValue2 = React208.useCallback((row, colDef) => getRowValue(row, colDef, apiRef), [apiRef]);
  const getRowFormattedValue = React208.useCallback((row, colDef) => {
    const value = getRowValue2(row, colDef);
    if (!colDef || !colDef.valueFormatter) {
      return value;
    }
    return colDef.valueFormatter(value, row, colDef, apiRef);
  }, [apiRef, getRowValue2]);
  return {
    getCellValue: getCellValue2,
    getRowValue: getRowValue2,
    getRowFormattedValue
  };
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridCellEditable.js
var React209 = __toESM(require_react(), 1);
var useIsCellEditable = () => React209.useCallback((params) => !isAutogeneratedRowNode(params.rowNode), []);

// node_modules/@mui/x-data-grid/esm/context/GridContextProvider.js
var React210 = __toESM(require_react(), 1);
var import_jsx_runtime171 = __toESM(require_jsx_runtime(), 1);
function GridContextProvider({
  privateApiRef,
  configuration: configuration4,
  props,
  children: children2
}) {
  const apiRef = React210.useRef(privateApiRef.current.getPublicApi());
  return (0, import_jsx_runtime171.jsx)(GridConfigurationContext.Provider, {
    value: configuration4,
    children: (0, import_jsx_runtime171.jsx)(GridRootPropsContext.Provider, {
      value: props,
      children: (0, import_jsx_runtime171.jsx)(GridPrivateApiContext.Provider, {
        value: privateApiRef,
        children: (0, import_jsx_runtime171.jsx)(GridApiContext.Provider, {
          value: apiRef,
          children: (0, import_jsx_runtime171.jsx)(GridPanelContextProvider, {
            children: (0, import_jsx_runtime171.jsx)(GridCSSVariablesContext, {
              children: children2
            })
          })
        })
      })
    })
  });
}

// node_modules/@mui/x-data-grid/esm/DataGrid/useDataGridComponent.js
var React250 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridRefs.js
var React211 = __toESM(require_react(), 1);
var useGridRefs = (apiRef) => {
  const rootElementRef = React211.useRef(null);
  const mainElementRef = React211.useRef(null);
  const virtualScrollerRef = React211.useRef(null);
  const virtualScrollbarVerticalRef = React211.useRef(null);
  const virtualScrollbarHorizontalRef = React211.useRef(null);
  const columnHeadersContainerRef = React211.useRef(null);
  apiRef.current.register("public", {
    rootElementRef
  });
  apiRef.current.register("private", {
    mainElementRef,
    virtualScrollerRef,
    virtualScrollbarVerticalRef,
    virtualScrollbarHorizontalRef,
    columnHeadersContainerRef
  });
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridIsRtl.js
var React212 = __toESM(require_react(), 1);
var useGridIsRtl = (apiRef) => {
  const isRtl = useRtl();
  if (apiRef.current.state.isRtl === void 0) {
    apiRef.current.state.isRtl = isRtl;
  }
  const isFirstEffect = React212.useRef(true);
  React212.useEffect(() => {
    if (isFirstEffect.current) {
      isFirstEffect.current = false;
    } else {
      apiRef.current.setState((state) => _extends({}, state, {
        isRtl
      }));
    }
  }, [apiRef, isRtl]);
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridLoggerFactory.js
var React213 = __toESM(require_react(), 1);
var forceDebug = localStorageAvailable() && window.localStorage.getItem("DEBUG") != null;
var noop5 = () => {
};
var noopLogger = {
  debug: noop5,
  info: noop5,
  warn: noop5,
  error: noop5
};
var LOG_LEVELS = ["debug", "info", "warn", "error"];
function getAppender(name, logLevel, appender = console) {
  const minLogLevelIdx = LOG_LEVELS.indexOf(logLevel);
  if (minLogLevelIdx === -1) {
    throw new Error(`MUI X: Log level ${logLevel} not recognized.`);
  }
  const logger = LOG_LEVELS.reduce((loggerObj, method, idx) => {
    if (idx >= minLogLevelIdx) {
      loggerObj[method] = (...args) => {
        const [message, ...other] = args;
        appender[method](`MUI X: ${name} - ${message}`, ...other);
      };
    } else {
      loggerObj[method] = noop5;
    }
    return loggerObj;
  }, {});
  return logger;
}
var useGridLoggerFactory = (apiRef, props) => {
  const getLogger = React213.useCallback((name) => {
    if (forceDebug) {
      return getAppender(name, "debug", props.logger);
    }
    if (!props.logLevel) {
      return noopLogger;
    }
    return getAppender(name, props.logLevel.toString(), props.logger);
  }, [props.logLevel, props.logger]);
  useGridApiMethod(apiRef, {
    getLogger
  }, "private");
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridLocaleText.js
var React214 = __toESM(require_react(), 1);
var useGridLocaleText = (apiRef, props) => {
  const getLocaleText = React214.useCallback((key) => {
    if (props.localeText[key] == null) {
      throw new Error(`Missing translation for key ${key}.`);
    }
    return props.localeText[key];
  }, [props.localeText]);
  apiRef.current.register("public", {
    getLocaleText
  });
};

// node_modules/@mui/x-data-grid/esm/hooks/core/strategyProcessing/gridStrategyProcessingApi.js
var GridStrategyGroup = (function(GridStrategyGroup2) {
  GridStrategyGroup2["DataSource"] = "dataSource";
  GridStrategyGroup2["RowTree"] = "rowTree";
  return GridStrategyGroup2;
})({});

// node_modules/@mui/x-data-grid/esm/hooks/core/strategyProcessing/useGridRegisterStrategyProcessor.js
var React215 = __toESM(require_react(), 1);
var useGridRegisterStrategyProcessor = (apiRef, strategyName, group, processor) => {
  const registerPreProcessor = React215.useCallback(() => {
    apiRef.current.registerStrategyProcessor(strategyName, group, processor);
  }, [apiRef, processor, group, strategyName]);
  useFirstRender(() => {
    registerPreProcessor();
  });
  const isFirstRender = React215.useRef(true);
  React215.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
    } else {
      registerPreProcessor();
    }
  }, [registerPreProcessor]);
};

// node_modules/@mui/x-data-grid/esm/hooks/core/strategyProcessing/useGridStrategyProcessing.js
var React216 = __toESM(require_react(), 1);
var GRID_DEFAULT_STRATEGY = "none";
var GRID_STRATEGIES_PROCESSORS = {
  dataSourceRowsUpdate: GridStrategyGroup.DataSource,
  rowTreeCreation: GridStrategyGroup.RowTree,
  filtering: GridStrategyGroup.RowTree,
  sorting: GridStrategyGroup.RowTree,
  visibleRowsLookupCreation: GridStrategyGroup.RowTree
};
var useGridStrategyProcessing = (apiRef) => {
  const availableStrategies = React216.useRef(/* @__PURE__ */ new Map());
  const strategiesCache = React216.useRef({});
  const registerStrategyProcessor = React216.useCallback((strategyName, processorName, processor) => {
    const cleanup = () => {
      const _ref = strategiesCache.current[processorName], otherProcessors = _objectWithoutPropertiesLoose(_ref, [strategyName].map(toPropertyKey));
      strategiesCache.current[processorName] = otherProcessors;
    };
    if (!strategiesCache.current[processorName]) {
      strategiesCache.current[processorName] = {};
    }
    const groupPreProcessors = strategiesCache.current[processorName];
    const previousProcessor = groupPreProcessors[strategyName];
    groupPreProcessors[strategyName] = processor;
    if (!previousProcessor || previousProcessor === processor) {
      return cleanup;
    }
    if (strategyName === apiRef.current.getActiveStrategy(GRID_STRATEGIES_PROCESSORS[processorName])) {
      apiRef.current.publishEvent("activeStrategyProcessorChange", processorName);
    }
    return cleanup;
  }, [apiRef]);
  const applyStrategyProcessor = React216.useCallback((processorName, params) => {
    const activeStrategy = apiRef.current.getActiveStrategy(GRID_STRATEGIES_PROCESSORS[processorName]);
    if (activeStrategy == null) {
      throw new Error("Can't apply a strategy processor before defining an active strategy");
    }
    const groupCache = strategiesCache.current[processorName];
    if (!groupCache || !groupCache[activeStrategy]) {
      throw new Error(`No processor found for processor "${processorName}" on strategy "${activeStrategy}"`);
    }
    const processor = groupCache[activeStrategy];
    return processor(params);
  }, [apiRef]);
  const getActiveStrategy = React216.useCallback((strategyGroup) => {
    const strategyEntries = Array.from(availableStrategies.current.entries());
    const availableStrategyEntry = strategyEntries.find(([, strategy]) => {
      if (strategy.group !== strategyGroup) {
        return false;
      }
      return strategy.isAvailable();
    });
    return (availableStrategyEntry == null ? void 0 : availableStrategyEntry[0]) ?? GRID_DEFAULT_STRATEGY;
  }, []);
  const setStrategyAvailability2 = React216.useCallback((strategyGroup, strategyName, isAvailable) => {
    availableStrategies.current.set(strategyName, {
      group: strategyGroup,
      isAvailable
    });
    apiRef.current.publishEvent("strategyAvailabilityChange");
  }, [apiRef]);
  const strategyProcessingApi = {
    registerStrategyProcessor,
    applyStrategyProcessor,
    getActiveStrategy,
    setStrategyAvailability: setStrategyAvailability2
  };
  useGridApiMethod(apiRef, strategyProcessingApi, "private");
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridStateInitialization.js
var React217 = __toESM(require_react(), 1);
var useGridStateInitialization = (apiRef) => {
  const controlStateMapRef = React217.useRef({});
  const registerControlState = React217.useCallback((controlStateItem) => {
    controlStateMapRef.current[controlStateItem.stateId] = controlStateItem;
  }, []);
  const setState2 = React217.useCallback((state, reason) => {
    let newState;
    if (isFunction(state)) {
      newState = state(apiRef.current.state);
    } else {
      newState = state;
    }
    if (apiRef.current.state === newState) {
      return false;
    }
    const apiRefWithNewState = {
      current: {
        state: newState
      }
    };
    let ignoreSetState = false;
    const updatedControlStateIds = [];
    Object.keys(controlStateMapRef.current).forEach((stateId) => {
      const controlState = controlStateMapRef.current[stateId];
      const oldSubState = controlState.stateSelector(apiRef);
      const newSubState = controlState.stateSelector(apiRefWithNewState);
      if (newSubState === oldSubState) {
        return;
      }
      updatedControlStateIds.push({
        stateId: controlState.stateId,
        hasPropChanged: newSubState !== controlState.propModel
      });
      if (controlState.propModel !== void 0 && newSubState !== controlState.propModel) {
        ignoreSetState = true;
      }
    });
    if (updatedControlStateIds.length > 1) {
      throw new Error(`You're not allowed to update several sub-state in one transaction. You already updated ${updatedControlStateIds[0].stateId}, therefore, you're not allowed to update ${updatedControlStateIds.map((el) => el.stateId).join(", ")} in the same transaction.`);
    }
    if (!ignoreSetState) {
      apiRef.current.state = newState;
      apiRef.current.publishEvent("stateChange", newState);
      apiRef.current.store.update(newState);
    }
    if (updatedControlStateIds.length === 1) {
      const {
        stateId,
        hasPropChanged
      } = updatedControlStateIds[0];
      const controlState = controlStateMapRef.current[stateId];
      const model = controlState.stateSelector(apiRefWithNewState);
      if (controlState.propOnChange && hasPropChanged) {
        controlState.propOnChange(model, {
          reason,
          api: apiRef.current
        });
      }
      if (!ignoreSetState) {
        apiRef.current.publishEvent(controlState.changeEvent, model, {
          reason
        });
      }
    }
    return !ignoreSetState;
  }, [apiRef]);
  const updateControlState = React217.useCallback((key, state, reason) => {
    return apiRef.current.setState((previousState) => {
      return _extends({}, previousState, {
        [key]: state(previousState[key])
      });
    }, reason);
  }, [apiRef]);
  const publicStateApi = {
    setState: setState2
  };
  const privateStateApi = {
    updateControlState,
    registerControlState
  };
  useGridApiMethod(apiRef, publicStateApi, "public");
  useGridApiMethod(apiRef, privateStateApi, "private");
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridProps.js
var React218 = __toESM(require_react(), 1);
var propsStateInitializer = (state, props) => {
  return _extends({}, state, {
    props: {
      listView: props.listView,
      getRowId: props.getRowId,
      isCellEditable: props.isCellEditable,
      isRowSelectable: props.isRowSelectable,
      dataSource: props.dataSource
    }
  });
};
var useGridProps = (apiRef, props) => {
  const isFirstRender = React218.useRef(true);
  React218.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    apiRef.current.setState((state) => _extends({}, state, {
      props: {
        listView: props.listView,
        getRowId: props.getRowId,
        isCellEditable: props.isCellEditable,
        isRowSelectable: props.isRowSelectable,
        dataSource: props.dataSource
      }
    }));
  }, [apiRef, props.listView, props.getRowId, props.isCellEditable, props.isRowSelectable, props.dataSource]);
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridInitialization.js
var useGridInitialization = (privateApiRef, props) => {
  useGridRefs(privateApiRef);
  useGridProps(privateApiRef, props);
  useGridIsRtl(privateApiRef);
  useGridLoggerFactory(privateApiRef, props);
  useGridStateInitialization(privateApiRef);
  useGridPipeProcessing(privateApiRef);
  useGridStrategyProcessing(privateApiRef);
  useGridLocaleText(privateApiRef, props);
  privateApiRef.current.register("private", {
    rootProps: props
  });
};

// node_modules/@mui/x-data-grid/esm/hooks/utils/useGridInitializeState.js
var React219 = __toESM(require_react(), 1);
var useGridInitializeState = (initializer, privateApiRef, props, key) => {
  const previousKey = React219.useRef(key);
  const isInitialized = React219.useRef(false);
  if (key !== previousKey.current) {
    isInitialized.current = false;
    previousKey.current = key;
  }
  if (!isInitialized.current) {
    privateApiRef.current.state = initializer(privateApiRef.current.state, props, privateApiRef);
    isInitialized.current = true;
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/features/clipboard/useGridClipboard.js
var React220 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/export/serializers/csvSerializer.js
function sanitizeCellValue(value, csvOptions) {
  if (value === null || value === void 0) {
    return "";
  }
  const valueStr = typeof value === "string" ? value : `${value}`;
  if (csvOptions.shouldAppendQuotes || csvOptions.escapeFormulas) {
    const escapedValue = valueStr.replace(/"/g, '""');
    if (csvOptions.escapeFormulas) {
      if (["=", "+", "-", "@", "	", "\r"].includes(escapedValue[0])) {
        return `"'${escapedValue}"`;
      }
    }
    if ([csvOptions.delimiter, "\n", "\r", '"'].some((delimiter) => valueStr.includes(delimiter))) {
      return `"${escapedValue}"`;
    }
    return escapedValue;
  }
  return valueStr;
}
var serializeCellValue = (cellParams, options3) => {
  var _a, _b;
  const {
    csvOptions,
    ignoreValueFormatter
  } = options3;
  let value;
  if (ignoreValueFormatter) {
    const columnType = cellParams.colDef.type;
    if (columnType === "number") {
      value = String(cellParams.value);
    } else if (columnType === "date" || columnType === "dateTime") {
      value = (_a = cellParams.value) == null ? void 0 : _a.toISOString();
    } else if (typeof ((_b = cellParams.value) == null ? void 0 : _b.toString) === "function") {
      value = cellParams.value.toString();
    } else {
      value = cellParams.value;
    }
  } else {
    value = cellParams.formattedValue;
  }
  return sanitizeCellValue(value, csvOptions);
};
var CSVRow = class {
  constructor(options3) {
    __publicField(this, "rowString", "");
    __publicField(this, "isEmpty", true);
    this.options = options3;
  }
  addValue(value) {
    if (!this.isEmpty) {
      this.rowString += this.options.csvOptions.delimiter;
    }
    if (typeof this.options.sanitizeCellValue === "function") {
      this.rowString += this.options.sanitizeCellValue(value, this.options.csvOptions);
    } else {
      this.rowString += value;
    }
    this.isEmpty = false;
  }
  getRowString() {
    return this.rowString;
  }
};
var serializeRow = ({
  id,
  columns,
  getCellParams,
  csvOptions,
  ignoreValueFormatter
}) => {
  const row = new CSVRow({
    csvOptions
  });
  columns.forEach((column) => {
    const cellParams = getCellParams(id, column.field);
    if (String(cellParams.formattedValue) === "[object Object]") {
      warnOnce(["MUI X: When the value of a field is an object or a `renderCell` is provided, the CSV export might not display the value correctly.", "You can provide a `valueFormatter` with a string representation to be used."]);
    }
    row.addValue(serializeCellValue(cellParams, {
      ignoreValueFormatter,
      csvOptions
    }));
  });
  return row.getRowString();
};
function buildCSV(options3) {
  const {
    columns,
    rowIds,
    csvOptions,
    ignoreValueFormatter,
    apiRef
  } = options3;
  const CSVBody = rowIds.reduce((acc, id) => `${acc}${serializeRow({
    id,
    columns,
    getCellParams: apiRef.current.getCellParams,
    ignoreValueFormatter,
    csvOptions
  })}\r
`, "").trim();
  if (!csvOptions.includeHeaders) {
    return CSVBody;
  }
  const filteredColumns = columns.filter((column) => column.field !== GRID_CHECKBOX_SELECTION_COL_DEF.field);
  const headerRows = [];
  if (csvOptions.includeColumnGroupsHeaders) {
    const columnGroupLookup = apiRef.current.getAllGroupDetails();
    let maxColumnGroupsDepth = 0;
    const columnGroupPathsLookup = filteredColumns.reduce((acc, column) => {
      const columnGroupPath = apiRef.current.getColumnGroupPath(column.field);
      acc[column.field] = columnGroupPath;
      maxColumnGroupsDepth = Math.max(maxColumnGroupsDepth, columnGroupPath.length);
      return acc;
    }, {});
    for (let i2 = 0; i2 < maxColumnGroupsDepth; i2 += 1) {
      const headerGroupRow = new CSVRow({
        csvOptions,
        sanitizeCellValue
      });
      headerRows.push(headerGroupRow);
      filteredColumns.forEach((column) => {
        const columnGroupId = (columnGroupPathsLookup[column.field] || [])[i2];
        const columnGroup = columnGroupLookup[columnGroupId];
        headerGroupRow.addValue(columnGroup ? columnGroup.headerName || columnGroup.groupId : "");
      });
    }
  }
  const mainHeaderRow = new CSVRow({
    csvOptions,
    sanitizeCellValue
  });
  filteredColumns.forEach((column) => {
    mainHeaderRow.addValue(column.headerName || column.field);
  });
  headerRows.push(mainHeaderRow);
  const CSVHead = `${headerRows.map((row) => row.getRowString()).join("\r\n")}\r
`;
  return `${CSVHead}${CSVBody}`.trim();
}

// node_modules/@mui/x-data-grid/esm/hooks/features/clipboard/useGridClipboard.js
function writeToClipboardPolyfill(data) {
  const span = document.createElement("span");
  span.style.whiteSpace = "pre";
  span.style.userSelect = "all";
  span.style.opacity = "0px";
  span.textContent = data;
  document.body.appendChild(span);
  const range = document.createRange();
  range.selectNode(span);
  const selection = window.getSelection();
  selection.removeAllRanges();
  selection.addRange(range);
  try {
    document.execCommand("copy");
  } finally {
    document.body.removeChild(span);
  }
}
function copyToClipboard(data) {
  if (navigator.clipboard) {
    navigator.clipboard.writeText(data).catch(() => {
      writeToClipboardPolyfill(data);
    });
  } else {
    writeToClipboardPolyfill(data);
  }
}
function hasNativeSelection(element) {
  var _a;
  if ((_a = window.getSelection()) == null ? void 0 : _a.toString()) {
    return true;
  }
  if (element && (element.selectionEnd || 0) - (element.selectionStart || 0) > 0) {
    return true;
  }
  return false;
}
var useGridClipboard = (apiRef, props) => {
  const ignoreValueFormatterProp = props.ignoreValueFormatterDuringExport;
  const ignoreValueFormatter = (typeof ignoreValueFormatterProp === "object" ? ignoreValueFormatterProp == null ? void 0 : ignoreValueFormatterProp.clipboardExport : ignoreValueFormatterProp) || false;
  const clipboardCopyCellDelimiter = props.clipboardCopyCellDelimiter;
  const handleCopy = React220.useCallback((event) => {
    if (!isCopyShortcut(event)) {
      return;
    }
    if (hasNativeSelection(event.target)) {
      return;
    }
    let textToCopy = "";
    const selectedRowsCount = gridRowSelectionCountSelector(apiRef);
    if (selectedRowsCount > 0) {
      textToCopy = apiRef.current.getDataAsCsv({
        includeHeaders: false,
        delimiter: clipboardCopyCellDelimiter,
        shouldAppendQuotes: false,
        escapeFormulas: false
      });
    } else {
      const focusedCell = gridFocusCellSelector(apiRef);
      if (focusedCell) {
        const cellParams = apiRef.current.getCellParams(focusedCell.id, focusedCell.field);
        textToCopy = serializeCellValue(cellParams, {
          csvOptions: {
            delimiter: clipboardCopyCellDelimiter,
            shouldAppendQuotes: false,
            escapeFormulas: false
          },
          ignoreValueFormatter
        });
      }
    }
    textToCopy = apiRef.current.unstable_applyPipeProcessors("clipboardCopy", textToCopy);
    if (textToCopy) {
      copyToClipboard(textToCopy);
      apiRef.current.publishEvent("clipboardCopy", textToCopy);
    }
  }, [apiRef, ignoreValueFormatter, clipboardCopyCellDelimiter]);
  useGridNativeEventListener(apiRef, () => apiRef.current.rootElementRef.current, "keydown", handleCopy);
  useGridEventPriority(apiRef, "clipboardCopy", props.onClipboardCopy);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columnMenu/useGridColumnMenu.js
var React221 = __toESM(require_react(), 1);
var columnMenuStateInitializer = (state) => _extends({}, state, {
  columnMenu: {
    open: false
  }
});
var useGridColumnMenu = (apiRef) => {
  const logger = useGridLogger(apiRef, "useGridColumnMenu");
  const subscriptionRefs = React221.useRef({});
  const unsubscribeFromScrollChange = React221.useCallback(() => {
    var _a, _b, _c, _d;
    (_b = (_a = subscriptionRefs.current).wheel) == null ? void 0 : _b.call(_a);
    (_d = (_c = subscriptionRefs.current).touchMove) == null ? void 0 : _d.call(_c);
  }, []);
  const subscribeToScrollChange = React221.useCallback(() => {
    unsubscribeFromScrollChange();
    subscriptionRefs.current.wheel = apiRef.current.subscribeEvent("virtualScrollerWheel", apiRef.current.hideColumnMenu);
    subscriptionRefs.current.touchMove = apiRef.current.subscribeEvent("virtualScrollerTouchMove", apiRef.current.hideColumnMenu);
  }, [apiRef, unsubscribeFromScrollChange]);
  const showColumnMenu = React221.useCallback((field) => {
    const columnMenuState = gridColumnMenuSelector(apiRef);
    const newState = {
      open: true,
      field
    };
    const shouldUpdate = newState.open !== columnMenuState.open || newState.field !== columnMenuState.field;
    if (shouldUpdate) {
      apiRef.current.setState((state) => {
        if (state.columnMenu.open && state.columnMenu.field === field) {
          return state;
        }
        logger.debug("Opening Column Menu");
        subscribeToScrollChange();
        return _extends({}, state, {
          columnMenu: {
            open: true,
            field
          }
        });
      });
      apiRef.current.hidePreferences();
    }
  }, [apiRef, logger, subscribeToScrollChange]);
  const hideColumnMenu = React221.useCallback(() => {
    const columnMenuState = gridColumnMenuSelector(apiRef);
    if (columnMenuState.field) {
      const columnLookup = gridColumnLookupSelector(apiRef);
      const columnVisibilityModel = gridColumnVisibilityModelSelector(apiRef);
      const orderedFields = gridColumnFieldsSelector(apiRef);
      let fieldToFocus = columnMenuState.field;
      if (!columnLookup[fieldToFocus]) {
        fieldToFocus = orderedFields[0];
      }
      if (columnVisibilityModel[fieldToFocus] === false) {
        const visibleOrderedFields = orderedFields.filter((field) => {
          if (field === fieldToFocus) {
            return true;
          }
          return columnVisibilityModel[field] !== false;
        });
        const fieldIndex = visibleOrderedFields.indexOf(fieldToFocus);
        fieldToFocus = visibleOrderedFields[fieldIndex + 1] || visibleOrderedFields[fieldIndex - 1];
      }
      apiRef.current.setColumnHeaderFocus(fieldToFocus);
    }
    const newState = {
      open: false,
      field: void 0
    };
    const shouldUpdate = newState.open !== columnMenuState.open || newState.field !== columnMenuState.field;
    if (shouldUpdate) {
      apiRef.current.setState((state) => {
        logger.debug("Hiding Column Menu");
        unsubscribeFromScrollChange();
        return _extends({}, state, {
          columnMenu: newState
        });
      });
    }
  }, [apiRef, logger, unsubscribeFromScrollChange]);
  const toggleColumnMenu = React221.useCallback((field) => {
    logger.debug("Toggle Column Menu");
    const columnMenu = gridColumnMenuSelector(apiRef);
    if (!columnMenu.open || columnMenu.field !== field) {
      showColumnMenu(field);
    } else {
      hideColumnMenu();
    }
  }, [apiRef, logger, showColumnMenu, hideColumnMenu]);
  const columnMenuApi = {
    showColumnMenu,
    hideColumnMenu,
    toggleColumnMenu
  };
  useGridApiMethod(apiRef, columnMenuApi, "public");
  useGridEvent(apiRef, "columnResizeStart", hideColumnMenu);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columns/useGridColumns.js
var React222 = __toESM(require_react(), 1);
var import_jsx_runtime172 = __toESM(require_jsx_runtime(), 1);
var columnsStateInitializer = (state, props, apiRef) => {
  var _a, _b, _c;
  apiRef.current.caches.columns = {
    lastColumnsProp: props.columns
  };
  const columnsState = createColumnsState({
    apiRef,
    columnsToUpsert: props.columns,
    initialState: (_a = props.initialState) == null ? void 0 : _a.columns,
    columnVisibilityModel: props.columnVisibilityModel ?? ((_c = (_b = props.initialState) == null ? void 0 : _b.columns) == null ? void 0 : _c.columnVisibilityModel) ?? {},
    keepOnlyColumnsToUpsert: true
  });
  return _extends({}, state, {
    columns: columnsState,
    // In pro/premium, this part of the state is defined. We give it an empty but defined value
    // for the community version.
    pinnedColumns: state.pinnedColumns ?? EMPTY_PINNED_COLUMN_FIELDS
  });
};
function useGridColumns(apiRef, props) {
  var _a, _b;
  const logger = useGridLogger(apiRef, "useGridColumns");
  apiRef.current.registerControlState({
    stateId: "visibleColumns",
    propModel: props.columnVisibilityModel,
    propOnChange: props.onColumnVisibilityModelChange,
    stateSelector: gridColumnVisibilityModelSelector,
    changeEvent: "columnVisibilityModelChange"
  });
  const setGridColumnsState = React222.useCallback((columnsState) => {
    logger.debug("Updating columns state.");
    apiRef.current.setState(mergeColumnsState(columnsState));
    apiRef.current.publishEvent("columnsChange", columnsState.orderedFields);
  }, [logger, apiRef]);
  const getColumn = React222.useCallback((field) => gridColumnLookupSelector(apiRef)[field], [apiRef]);
  const getAllColumns = React222.useCallback(() => gridColumnDefinitionsSelector(apiRef), [apiRef]);
  const getVisibleColumns = React222.useCallback(() => gridVisibleColumnDefinitionsSelector(apiRef), [apiRef]);
  const getColumnIndex = React222.useCallback((field, useVisibleColumns = true) => {
    const columns = useVisibleColumns ? gridVisibleColumnDefinitionsSelector(apiRef) : gridColumnDefinitionsSelector(apiRef);
    return columns.findIndex((col) => col.field === field);
  }, [apiRef]);
  const getColumnPosition = React222.useCallback((field) => {
    const index = getColumnIndex(field);
    return gridColumnPositionsSelector(apiRef)[index];
  }, [apiRef, getColumnIndex]);
  const setColumnVisibilityModel = React222.useCallback((model) => {
    var _a2, _b2;
    const currentModel = gridColumnVisibilityModelSelector(apiRef);
    if (currentModel !== model) {
      apiRef.current.setState((state) => _extends({}, state, {
        columns: createColumnsState({
          apiRef,
          columnsToUpsert: [],
          initialState: void 0,
          columnVisibilityModel: model,
          keepOnlyColumnsToUpsert: false
        })
      }));
      (_b2 = (_a2 = apiRef.current).updateRenderContext) == null ? void 0 : _b2.call(_a2);
    }
  }, [apiRef]);
  const updateColumns = React222.useCallback((columns) => {
    let columnsToUpdate = columns;
    if (gridPivotActiveSelector(apiRef)) {
      const nonPivotColumns = [];
      const pivotColumns = [];
      const pivotInitialColumns = gridPivotInitialColumnsSelector(apiRef);
      columns.forEach((column) => {
        const isNonPivotColumn = pivotInitialColumns.has(column.field);
        if (isNonPivotColumn) {
          nonPivotColumns.push(column);
        } else {
          pivotColumns.push(column);
        }
      });
      if (nonPivotColumns.length > 0) {
        apiRef.current.updateNonPivotColumns(nonPivotColumns);
      }
      if (pivotColumns.length === 0) {
        return;
      }
      columnsToUpdate = pivotColumns;
    }
    const columnsState = createColumnsState({
      apiRef,
      columnsToUpsert: columnsToUpdate,
      initialState: void 0,
      keepOnlyColumnsToUpsert: false,
      updateInitialVisibilityModel: true
    });
    setGridColumnsState(columnsState);
  }, [apiRef, setGridColumnsState]);
  const setColumnVisibility = React222.useCallback((field, isVisible) => {
    const columnVisibilityModel = gridColumnVisibilityModelSelector(apiRef);
    const isCurrentlyVisible = columnVisibilityModel[field] ?? true;
    if (isVisible !== isCurrentlyVisible) {
      const newModel = _extends({}, columnVisibilityModel, {
        [field]: isVisible
      });
      apiRef.current.setColumnVisibilityModel(newModel);
    }
  }, [apiRef]);
  const getColumnIndexRelativeToVisibleColumns = React222.useCallback((field) => {
    const allColumns = gridColumnFieldsSelector(apiRef);
    return allColumns.findIndex((col) => col === field);
  }, [apiRef]);
  const setColumnIndex = React222.useCallback((field, targetIndexPosition) => {
    const allColumns = gridColumnFieldsSelector(apiRef);
    const oldIndexPosition = getColumnIndexRelativeToVisibleColumns(field);
    if (oldIndexPosition === targetIndexPosition) {
      return;
    }
    logger.debug(`Moving column ${field} to index ${targetIndexPosition}`);
    const updatedColumns = [...allColumns];
    const fieldRemoved = updatedColumns.splice(oldIndexPosition, 1)[0];
    updatedColumns.splice(targetIndexPosition, 0, fieldRemoved);
    setGridColumnsState(_extends({}, gridColumnsStateSelector(apiRef), {
      orderedFields: updatedColumns
    }));
    const params = {
      column: apiRef.current.getColumn(field),
      targetIndex: apiRef.current.getColumnIndexRelativeToVisibleColumns(field),
      oldIndex: oldIndexPosition
    };
    apiRef.current.publishEvent("columnIndexChange", params);
  }, [apiRef, logger, setGridColumnsState, getColumnIndexRelativeToVisibleColumns]);
  const setColumnWidth = React222.useCallback((field, width) => {
    logger.debug(`Updating column ${field} width to ${width}`);
    const columnsState = gridColumnsStateSelector(apiRef);
    const column = columnsState.lookup[field];
    const newColumn = _extends({}, column, {
      width,
      hasBeenResized: true
    });
    setGridColumnsState(hydrateColumnsWidth(_extends({}, columnsState, {
      lookup: _extends({}, columnsState.lookup, {
        [field]: newColumn
      })
    }), apiRef.current.getRootDimensions()));
    apiRef.current.publishEvent("columnWidthChange", {
      element: apiRef.current.getColumnHeaderElement(field),
      colDef: newColumn,
      width
    });
  }, [apiRef, logger, setGridColumnsState]);
  const columnApi = {
    getColumn,
    getAllColumns,
    getColumnIndex,
    getColumnPosition,
    getVisibleColumns,
    getColumnIndexRelativeToVisibleColumns,
    updateColumns,
    setColumnVisibilityModel,
    setColumnVisibility,
    setColumnWidth
  };
  const columnReorderApi = {
    setColumnIndex
  };
  useGridApiMethod(apiRef, columnApi, "public");
  useGridApiMethod(apiRef, columnReorderApi, props.signature === GridSignature.DataGrid ? "private" : "public");
  const stateExportPreProcessing = React222.useCallback((prevState, context) => {
    var _a2, _b2;
    const columnsStateToExport = {};
    const columnVisibilityModelToExport = gridColumnVisibilityModelSelector(apiRef);
    const shouldExportColumnVisibilityModel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the model is controlled
      props.columnVisibilityModel != null || // Always export if the model has been initialized
      // TODO v6 Do a nullish check instead to export even if the initial model equals "{}"
      Object.keys(((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.columns) == null ? void 0 : _b2.columnVisibilityModel) ?? {}).length > 0 || // Always export if the model is not empty
      Object.keys(columnVisibilityModelToExport).length > 0
    );
    if (shouldExportColumnVisibilityModel) {
      columnsStateToExport.columnVisibilityModel = columnVisibilityModelToExport;
    }
    columnsStateToExport.orderedFields = gridColumnFieldsSelector(apiRef);
    const columns = gridColumnDefinitionsSelector(apiRef);
    const dimensions = {};
    columns.forEach((colDef) => {
      if (colDef.hasBeenResized) {
        const colDefDimensions = {};
        COLUMNS_DIMENSION_PROPERTIES.forEach((propertyName) => {
          let propertyValue = colDef[propertyName];
          if (propertyValue === Infinity) {
            propertyValue = -1;
          }
          colDefDimensions[propertyName] = propertyValue;
        });
        dimensions[colDef.field] = colDefDimensions;
      }
    });
    if (Object.keys(dimensions).length > 0) {
      columnsStateToExport.dimensions = dimensions;
    }
    return _extends({}, prevState, {
      columns: columnsStateToExport
    });
  }, [apiRef, props.columnVisibilityModel, (_a = props.initialState) == null ? void 0 : _a.columns]);
  const stateRestorePreProcessing = React222.useCallback((params, context) => {
    const initialState = context.stateToRestore.columns;
    const columnVisibilityModelToImport = initialState == null ? void 0 : initialState.columnVisibilityModel;
    if (initialState == null) {
      return params;
    }
    const columnsState = createColumnsState({
      apiRef,
      columnsToUpsert: [],
      initialState,
      columnVisibilityModel: columnVisibilityModelToImport,
      keepOnlyColumnsToUpsert: false
    });
    if (initialState != null) {
      apiRef.current.setState((prevState) => _extends({}, prevState, {
        columns: _extends({}, prevState.columns, {
          lookup: columnsState.lookup,
          orderedFields: columnsState.orderedFields,
          initialColumnVisibilityModel: columnsState.initialColumnVisibilityModel
        })
      }));
    }
    if (columnVisibilityModelToImport != null) {
      apiRef.current.setState((prevState) => _extends({}, prevState, {
        columns: _extends({}, prevState.columns, {
          columnVisibilityModel: columnVisibilityModelToImport
        })
      }));
    }
    if (initialState != null) {
      apiRef.current.publishEvent("columnsChange", columnsState.orderedFields);
    }
    return params;
  }, [apiRef]);
  const preferencePanelPreProcessing = React222.useCallback((initialValue, value) => {
    var _a2;
    if (value === GridPreferencePanelsValue.columns) {
      const ColumnsPanel = props.slots.columnsPanel;
      return (0, import_jsx_runtime172.jsx)(ColumnsPanel, _extends({}, (_a2 = props.slotProps) == null ? void 0 : _a2.columnsPanel));
    }
    return initialValue;
  }, [props.slots.columnsPanel, (_b = props.slotProps) == null ? void 0 : _b.columnsPanel]);
  const addColumnMenuItems = React222.useCallback((columnMenuItems) => {
    const isPivotActive = gridPivotActiveSelector(apiRef);
    if (props.disableColumnSelector || isPivotActive) {
      return columnMenuItems;
    }
    return [...columnMenuItems, "columnMenuColumnsItem"];
  }, [props.disableColumnSelector, apiRef]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuItems);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  useGridRegisterPipeProcessor(apiRef, "preferencePanel", preferencePanelPreProcessing);
  const prevInnerWidth = React222.useRef(null);
  const handleGridSizeChange = (size) => {
    if (prevInnerWidth.current !== size.width) {
      prevInnerWidth.current = size.width;
      const hasFlexColumns = gridVisibleColumnDefinitionsSelector(apiRef).some((col) => col.flex && col.flex > 0);
      if (!hasFlexColumns) {
        return;
      }
      setGridColumnsState(hydrateColumnsWidth(gridColumnsStateSelector(apiRef), apiRef.current.getRootDimensions()));
    }
  };
  useGridEvent(apiRef, "viewportInnerSizeChange", handleGridSizeChange);
  const hydrateColumns = React222.useCallback(() => {
    logger.info(`Columns pipe processing have changed, regenerating the columns`);
    const columnsState = createColumnsState({
      apiRef,
      columnsToUpsert: [],
      initialState: void 0,
      keepOnlyColumnsToUpsert: false
    });
    setGridColumnsState(columnsState);
  }, [apiRef, logger, setGridColumnsState]);
  useGridRegisterPipeApplier(apiRef, "hydrateColumns", hydrateColumns);
  React222.useEffect(() => {
    if (apiRef.current.caches.columns.lastColumnsProp === props.columns) {
      return;
    }
    apiRef.current.caches.columns.lastColumnsProp = props.columns;
    logger.info(`GridColumns have changed, new length ${props.columns.length}`);
    const columnsState = createColumnsState({
      apiRef,
      initialState: void 0,
      // If the user provides a model, we don't want to set it in the state here because it has it's dedicated `useEffect` which calls `setColumnVisibilityModel`
      columnsToUpsert: props.columns,
      keepOnlyColumnsToUpsert: true,
      updateInitialVisibilityModel: true,
      columnVisibilityModel: props.columnVisibilityModel
    });
    setGridColumnsState(columnsState);
  }, [logger, apiRef, setGridColumnsState, props.columns, props.columnVisibilityModel]);
  React222.useEffect(() => {
    if (props.columnVisibilityModel !== void 0) {
      apiRef.current.setColumnVisibilityModel(props.columnVisibilityModel);
    }
  }, [apiRef, logger, props.columnVisibilityModel]);
}
function mergeColumnsState(columnsState) {
  return (state) => _extends({}, state, {
    columns: columnsState
  });
}

// node_modules/@mui/x-data-grid/esm/hooks/features/density/useGridDensity.js
var React223 = __toESM(require_react(), 1);
var densityStateInitializer = (state, props) => {
  var _a;
  return _extends({}, state, {
    density: ((_a = props.initialState) == null ? void 0 : _a.density) ?? props.density ?? "standard"
  });
};
var useGridDensity = (apiRef, props) => {
  var _a;
  const logger = useGridLogger(apiRef, "useDensity");
  apiRef.current.registerControlState({
    stateId: "density",
    propModel: props.density,
    propOnChange: props.onDensityChange,
    stateSelector: gridDensitySelector,
    changeEvent: "densityChange"
  });
  const setDensity = useEventCallback_default((newDensity) => {
    const currentDensity = gridDensitySelector(apiRef);
    if (currentDensity === newDensity) {
      return;
    }
    logger.debug(`Set grid density to ${newDensity}`);
    apiRef.current.setState((state) => _extends({}, state, {
      density: newDensity
    }));
  });
  const densityApi = {
    setDensity
  };
  useGridApiMethod(apiRef, densityApi, "public");
  const stateExportPreProcessing = React223.useCallback((prevState, context) => {
    var _a2;
    const exportedDensity = gridDensitySelector(apiRef);
    const shouldExportRowCount = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the `density` is controlled
      props.density != null || // Always export if the `density` has been initialized
      ((_a2 = props.initialState) == null ? void 0 : _a2.density) != null
    );
    if (!shouldExportRowCount) {
      return prevState;
    }
    return _extends({}, prevState, {
      density: exportedDensity
    });
  }, [apiRef, props.density, (_a = props.initialState) == null ? void 0 : _a.density]);
  const stateRestorePreProcessing = React223.useCallback((params, context) => {
    var _a2;
    const restoredDensity = ((_a2 = context.stateToRestore) == null ? void 0 : _a2.density) ? context.stateToRestore.density : gridDensitySelector(apiRef);
    apiRef.current.setState((state) => _extends({}, state, {
      density: restoredDensity
    }));
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  React223.useEffect(() => {
    if (props.density) {
      apiRef.current.setDensity(props.density);
    }
  }, [apiRef, props.density]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/export/useGridCsvExport.js
var React224 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/utils/exportAs.js
function exportAs(blob, extension = "csv", filename = document.title || "untitled") {
  const fullName = `${filename}.${extension}`;
  if ("download" in HTMLAnchorElement.prototype) {
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = fullName;
    a.click();
    setTimeout(() => {
      URL.revokeObjectURL(url);
    });
    return;
  }
  throw new Error("MUI X: exportAs not supported.");
}

// node_modules/@mui/x-data-grid/esm/hooks/features/export/utils.js
var getColumnsToExport = ({
  apiRef,
  options: options3
}) => {
  const columns = gridColumnDefinitionsSelector(apiRef);
  if (options3.fields) {
    return options3.fields.reduce((currentColumns, field) => {
      const column = columns.find((col) => col.field === field);
      if (column) {
        currentColumns.push(column);
      }
      return currentColumns;
    }, []);
  }
  const validColumns = options3.allColumns ? columns : gridVisibleColumnDefinitionsSelector(apiRef);
  return validColumns.filter((column) => column.disableExport !== true);
};
var defaultGetRowsToExport = ({
  apiRef
}) => {
  var _a, _b;
  const filteredSortedRowIds = gridFilteredSortedRowIdsSelector(apiRef);
  const rowTree = gridRowTreeSelector(apiRef);
  const selectedRowsCount = gridRowSelectionCountSelector(apiRef);
  const bodyRows = filteredSortedRowIds.filter((id) => rowTree[id].type !== "footer");
  const pinnedRows = gridPinnedRowsSelector(apiRef);
  const topPinnedRowsIds = ((_a = pinnedRows == null ? void 0 : pinnedRows.top) == null ? void 0 : _a.map((row) => row.id)) || [];
  const bottomPinnedRowsIds = ((_b = pinnedRows == null ? void 0 : pinnedRows.bottom) == null ? void 0 : _b.map((row) => row.id)) || [];
  bodyRows.unshift(...topPinnedRowsIds);
  bodyRows.push(...bottomPinnedRowsIds);
  if (selectedRowsCount > 0) {
    const selectedRows = gridRowSelectionIdsSelector(apiRef);
    return bodyRows.filter((id) => selectedRows.has(id));
  }
  return bodyRows;
};

// node_modules/@mui/x-data-grid/esm/hooks/features/export/useGridCsvExport.js
var import_jsx_runtime173 = __toESM(require_jsx_runtime(), 1);
var useGridCsvExport = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridCsvExport");
  const ignoreValueFormatterProp = props.ignoreValueFormatterDuringExport;
  const ignoreValueFormatter = (typeof ignoreValueFormatterProp === "object" ? ignoreValueFormatterProp == null ? void 0 : ignoreValueFormatterProp.csvExport : ignoreValueFormatterProp) || false;
  const getDataAsCsv = React224.useCallback((options3 = {}) => {
    logger.debug(`Get data as CSV`);
    const exportedColumns = getColumnsToExport({
      apiRef,
      options: options3
    });
    const getRowsToExport = options3.getRowsToExport ?? defaultGetRowsToExport;
    const exportedRowIds = getRowsToExport({
      apiRef
    });
    return buildCSV({
      columns: exportedColumns,
      rowIds: exportedRowIds,
      csvOptions: {
        delimiter: options3.delimiter || ",",
        shouldAppendQuotes: options3.shouldAppendQuotes ?? true,
        includeHeaders: options3.includeHeaders ?? true,
        includeColumnGroupsHeaders: options3.includeColumnGroupsHeaders ?? true,
        escapeFormulas: options3.escapeFormulas ?? true
      },
      ignoreValueFormatter,
      apiRef
    });
  }, [logger, apiRef, ignoreValueFormatter]);
  const exportDataAsCsv = React224.useCallback((options3) => {
    logger.debug(`Export data as CSV`);
    const csv = getDataAsCsv(options3);
    const blob = new Blob([(options3 == null ? void 0 : options3.utf8WithBom) ? new Uint8Array([239, 187, 191]) : "", csv], {
      type: "text/csv"
    });
    exportAs(blob, "csv", options3 == null ? void 0 : options3.fileName);
  }, [logger, getDataAsCsv]);
  const csvExportApi = {
    getDataAsCsv,
    exportDataAsCsv
  };
  useGridApiMethod(apiRef, csvExportApi, "public");
  const addExportMenuButtons = React224.useCallback((initialValue, options3) => {
    var _a;
    if ((_a = options3.csvOptions) == null ? void 0 : _a.disableToolbarButton) {
      return initialValue;
    }
    return [...initialValue, {
      component: (0, import_jsx_runtime173.jsx)(GridCsvExportMenuItem, {
        options: options3.csvOptions
      }),
      componentName: "csvExport"
    }];
  }, []);
  useGridRegisterPipeProcessor(apiRef, "exportMenu", addExportMenuButtons);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/export/useGridPrintExport.js
var React226 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/export/loadStyleSheets.js
function loadStyleSheets(document2, root, nonce) {
  const stylesheetLoadPromises = [];
  const headStyleElements = root.querySelectorAll("style, link[rel='stylesheet']");
  for (let i2 = 0; i2 < headStyleElements.length; i2 += 1) {
    const node = headStyleElements[i2];
    const newHeadStyleElement = document2.createElement(node.tagName);
    if (node.tagName === "STYLE") {
      const sheet = node.sheet;
      if (sheet) {
        let styleCSS = "";
        for (let j = 0; j < sheet.cssRules.length; j += 1) {
          if (typeof sheet.cssRules[j].cssText === "string") {
            styleCSS += `${sheet.cssRules[j].cssText}\r
`;
          }
        }
        newHeadStyleElement.appendChild(document2.createTextNode(styleCSS));
      }
    } else if (node.getAttribute("href")) {
      for (let j = 0; j < node.attributes.length; j += 1) {
        const attr = node.attributes[j];
        if (attr) {
          newHeadStyleElement.setAttribute(attr.nodeName, attr.nodeValue || "");
        }
      }
      stylesheetLoadPromises.push(new Promise((resolve) => {
        newHeadStyleElement.addEventListener("load", () => resolve());
      }));
    }
    if (nonce) {
      newHeadStyleElement.setAttribute("nonce", nonce);
    }
    document2.head.appendChild(newHeadStyleElement);
    if (nonce) {
      newHeadStyleElement.setAttribute("nonce", nonce);
    }
  }
  return stylesheetLoadPromises;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/useGridPaginationModel.js
var React225 = __toESM(require_react(), 1);
var getDerivedPaginationModel = (paginationState, signature, paginationModelProp) => {
  let paginationModel = paginationState.paginationModel;
  const rowCount = paginationState.rowCount;
  const pageSize2 = (paginationModelProp == null ? void 0 : paginationModelProp.pageSize) ?? paginationModel.pageSize;
  const page = (paginationModelProp == null ? void 0 : paginationModelProp.page) ?? paginationModel.page;
  const pageCount = getPageCount(rowCount, pageSize2, page);
  if (paginationModelProp && ((paginationModelProp == null ? void 0 : paginationModelProp.page) !== paginationModel.page || (paginationModelProp == null ? void 0 : paginationModelProp.pageSize) !== paginationModel.pageSize)) {
    paginationModel = paginationModelProp;
  }
  const validPage = pageSize2 === -1 ? 0 : getValidPage(paginationModel.page, pageCount);
  if (validPage !== paginationModel.page) {
    paginationModel = _extends({}, paginationModel, {
      page: validPage
    });
  }
  throwIfPageSizeExceedsTheLimit(paginationModel.pageSize, signature);
  return paginationModel;
};
var useGridPaginationModel = (apiRef, props) => {
  var _a, _b;
  const logger = useGridLogger(apiRef, "useGridPaginationModel");
  const densityFactor = useGridSelector(apiRef, gridDensityFactorSelector);
  const previousFilterModel = React225.useRef(gridFilterModelSelector(apiRef));
  const rowHeight = Math.floor(props.rowHeight * densityFactor);
  apiRef.current.registerControlState({
    stateId: "paginationModel",
    propModel: props.paginationModel,
    propOnChange: props.onPaginationModelChange,
    stateSelector: gridPaginationModelSelector,
    changeEvent: "paginationModelChange"
  });
  const setPage = React225.useCallback((page) => {
    const currentModel = gridPaginationModelSelector(apiRef);
    if (page === currentModel.page) {
      return;
    }
    logger.debug(`Setting page to ${page}`);
    apiRef.current.setPaginationModel({
      page,
      pageSize: currentModel.pageSize
    });
  }, [apiRef, logger]);
  const debouncedSetPage = React225.useMemo(() => debounce(setPage, 0), [setPage]);
  const setPageSize = React225.useCallback((pageSize2) => {
    const currentModel = gridPaginationModelSelector(apiRef);
    if (pageSize2 === currentModel.pageSize) {
      return;
    }
    logger.debug(`Setting page size to ${pageSize2}`);
    apiRef.current.setPaginationModel({
      pageSize: pageSize2,
      page: currentModel.page
    });
  }, [apiRef, logger]);
  const setPaginationModel = React225.useCallback((paginationModel) => {
    const currentModel = gridPaginationModelSelector(apiRef);
    if (paginationModel === currentModel) {
      return;
    }
    logger.debug("Setting 'paginationModel' to", paginationModel);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        paginationModel: getDerivedPaginationModel(state.pagination, props.signature, paginationModel)
      })
    }), "setPaginationModel");
  }, [apiRef, logger, props.signature]);
  const paginationModelApi = {
    setPage,
    setPageSize,
    setPaginationModel
  };
  useGridApiMethod(apiRef, paginationModelApi, "public");
  const stateExportPreProcessing = React225.useCallback((prevState, context) => {
    var _a2, _b2;
    const paginationModel = gridPaginationModelSelector(apiRef);
    const shouldExportPaginationModel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the `paginationModel` is controlled
      props.paginationModel != null || // Always export if the `paginationModel` has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.pagination) == null ? void 0 : _b2.paginationModel) != null || // Export if `page` or `pageSize` is not equal to the default value
      paginationModel.page !== 0 && paginationModel.pageSize !== defaultPageSize(props.autoPageSize)
    );
    if (!shouldExportPaginationModel) {
      return prevState;
    }
    return _extends({}, prevState, {
      pagination: _extends({}, prevState.pagination, {
        paginationModel
      })
    });
  }, [apiRef, props.paginationModel, (_b = (_a = props.initialState) == null ? void 0 : _a.pagination) == null ? void 0 : _b.paginationModel, props.autoPageSize]);
  const stateRestorePreProcessing = React225.useCallback((params, context) => {
    var _a2, _b2;
    const paginationModel = ((_a2 = context.stateToRestore.pagination) == null ? void 0 : _a2.paginationModel) ? _extends({}, getDefaultGridPaginationModel(props.autoPageSize), (_b2 = context.stateToRestore.pagination) == null ? void 0 : _b2.paginationModel) : gridPaginationModelSelector(apiRef);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        paginationModel: getDerivedPaginationModel(state.pagination, props.signature, paginationModel)
      })
    }), "stateRestorePreProcessing");
    return params;
  }, [apiRef, props.autoPageSize, props.signature]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  const handlePaginationModelChange = () => {
    var _a2;
    const paginationModel = gridPaginationModelSelector(apiRef);
    if ((_a2 = apiRef.current.virtualScrollerRef) == null ? void 0 : _a2.current) {
      apiRef.current.scrollToIndexes({
        rowIndex: paginationModel.page * paginationModel.pageSize
      });
    }
  };
  const handleUpdateAutoPageSize = React225.useCallback(() => {
    if (!props.autoPageSize) {
      return;
    }
    const dimensions = apiRef.current.getRootDimensions();
    const maximumPageSizeWithoutScrollBar = Math.max(1, Math.floor(dimensions.viewportInnerSize.height / rowHeight));
    apiRef.current.setPageSize(maximumPageSizeWithoutScrollBar);
  }, [apiRef, props.autoPageSize, rowHeight]);
  const handleRowCountChange = React225.useCallback((newRowCount) => {
    if (newRowCount == null) {
      return;
    }
    const paginationModel = gridPaginationModelSelector(apiRef);
    if (paginationModel.page === 0) {
      return;
    }
    const pageCount = gridPageCountSelector(apiRef);
    if (paginationModel.page > pageCount - 1) {
      queueMicrotask(() => {
        debouncedSetPage(Math.max(0, pageCount - 1));
      });
    }
  }, [apiRef, debouncedSetPage]);
  const navigateToStart = React225.useCallback(() => {
    const paginationModel = gridPaginationModelSelector(apiRef);
    if (paginationModel.page !== 0) {
      debouncedSetPage(0);
    }
    const scrollPosition = apiRef.current.getScrollPosition();
    if (scrollPosition.top !== 0) {
      apiRef.current.scroll({
        top: 0
      });
    }
  }, [apiRef, debouncedSetPage]);
  const debouncedNavigateToStart = React225.useMemo(() => debounce(navigateToStart, 0), [navigateToStart]);
  const handleFilterModelChange = React225.useCallback((filterModel) => {
    const currentActiveFilters = _extends({}, filterModel, {
      // replace items with the active items
      items: gridFilterActiveItemsSelector(apiRef)
    });
    if (isDeepEqual(currentActiveFilters, previousFilterModel.current)) {
      return;
    }
    previousFilterModel.current = currentActiveFilters;
    debouncedNavigateToStart();
  }, [apiRef, debouncedNavigateToStart]);
  useGridEvent(apiRef, "viewportInnerSizeChange", handleUpdateAutoPageSize);
  useGridEvent(apiRef, "paginationModelChange", handlePaginationModelChange);
  useGridEvent(apiRef, "rowCountChange", handleRowCountChange);
  useGridEvent(apiRef, "sortModelChange", debouncedNavigateToStart);
  useGridEvent(apiRef, "filterModelChange", handleFilterModelChange);
  const isFirstRender = React225.useRef(true);
  React225.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    if (!props.pagination) {
      return;
    }
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        paginationModel: getDerivedPaginationModel(state.pagination, props.signature, props.paginationModel)
      })
    }));
  }, [apiRef, props.paginationModel, props.signature, props.pagination]);
  React225.useEffect(() => {
    apiRef.current.setState((state) => {
      const isEnabled = props.pagination === true;
      if (state.pagination.paginationMode === props.paginationMode && state.pagination.enabled === isEnabled) {
        return state;
      }
      return _extends({}, state, {
        pagination: _extends({}, state.pagination, {
          paginationMode: props.paginationMode,
          enabled: isEnabled
        })
      });
    });
  }, [apiRef, props.paginationMode, props.pagination]);
  React225.useEffect(handleUpdateAutoPageSize, [handleUpdateAutoPageSize]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/export/useGridPrintExport.js
var import_jsx_runtime174 = __toESM(require_jsx_runtime(), 1);
var DEBUG_MODE = false;
function raf() {
  return new Promise((resolve) => {
    requestAnimationFrame(() => {
      resolve();
    });
  });
}
function buildPrintWindow(title) {
  const iframeEl = document.createElement("iframe");
  iframeEl.style.position = "absolute";
  iframeEl.style.width = "0px";
  iframeEl.style.height = "0px";
  if (DEBUG_MODE) {
    iframeEl.style.width = "100%";
    iframeEl.style.height = "10000px";
  }
  iframeEl.title = title || document.title;
  return iframeEl;
}
var useGridPrintExport = (apiRef, props) => {
  const hasRootReference = apiRef.current.rootElementRef.current !== null;
  const logger = useGridLogger(apiRef, "useGridPrintExport");
  const doc = React226.useRef(null);
  const previousGridState = React226.useRef(null);
  const previousColumnVisibility = React226.useRef({});
  const previousRows = React226.useRef([]);
  const previousVirtualizationState = React226.useRef(null);
  React226.useEffect(() => {
    doc.current = ownerDocument(apiRef.current.rootElementRef.current);
  }, [apiRef, hasRootReference]);
  const updateGridColumnsForPrint = React226.useCallback((fields, allColumns, includeCheckboxes) => new Promise((resolve) => {
    const exportedColumnFields = getColumnsToExport({
      apiRef,
      options: {
        fields,
        allColumns
      }
    }).map((column) => column.field);
    const columns = gridColumnDefinitionsSelector(apiRef);
    const newColumnVisibilityModel = {};
    columns.forEach((column) => {
      newColumnVisibilityModel[column.field] = exportedColumnFields.includes(column.field);
    });
    if (includeCheckboxes) {
      newColumnVisibilityModel[GRID_CHECKBOX_SELECTION_COL_DEF.field] = true;
    }
    apiRef.current.setColumnVisibilityModel(newColumnVisibilityModel);
    resolve();
  }), [apiRef]);
  const updateGridRowsForPrint = React226.useCallback((getRowsToExport) => {
    const rowsToExportIds = getRowsToExport({
      apiRef
    });
    const newRows = rowsToExportIds.reduce((acc, id) => {
      const row = apiRef.current.getRow(id);
      if (!row[GRID_ID_AUTOGENERATED]) {
        acc.push(row);
      }
      return acc;
    }, []);
    apiRef.current.setRows(newRows);
  }, [apiRef]);
  const handlePrintWindowLoad = React226.useCallback((printWindow, options3) => {
    var _a, _b;
    const normalizeOptions = _extends({
      copyStyles: true,
      hideToolbar: false,
      hideFooter: false,
      includeCheckboxes: false
    }, options3);
    const printDoc = printWindow.contentDocument;
    if (!printDoc) {
      return;
    }
    const gridRootElement = apiRef.current.rootElementRef.current;
    const gridClone = gridRootElement.cloneNode(true);
    const virtualScrollerContent = gridClone.querySelector(`.${gridClasses.virtualScrollerContent}`);
    const gridMain = gridClone.querySelector(`.${gridClasses.main}`);
    gridMain.style.overflow = "visible";
    gridClone.querySelector(`.${gridClasses.virtualScrollerRenderZone}`).style.position = "static";
    virtualScrollerContent.style.flexBasis = "auto";
    (_a = gridClone.querySelector(`.${gridClasses["scrollbar--vertical"]}`)) == null ? void 0 : _a.remove();
    if (!(virtualScrollerContent.nextSibling instanceof HTMLElement && virtualScrollerContent.nextSibling.classList.contains(gridClasses.filler))) {
      const filler = document.createElement("div");
      filler.style.height = scrollbarSizeCssExpression;
      virtualScrollerContent.insertAdjacentElement("afterend", filler);
    }
    const gridFooterElement = gridClone.querySelector(`.${gridClasses.footerContainer}`);
    if (normalizeOptions.hideToolbar) {
      (_b = gridClone.querySelector(`.${gridClasses.toolbar}`)) == null ? void 0 : _b.remove();
    }
    if (normalizeOptions.hideFooter && gridFooterElement) {
      gridFooterElement.remove();
    }
    gridClone.style.height = "auto";
    gridClone.style.boxSizing = "content-box";
    if (!normalizeOptions.hideFooter && gridFooterElement) {
      gridFooterElement.style.width = "100%";
    }
    const container = document.createElement("div");
    container.appendChild(gridClone);
    printDoc.body.style.marginTop = "0px";
    printDoc.body.innerHTML = container.innerHTML;
    const defaultPageStyle = typeof normalizeOptions.pageStyle === "function" ? normalizeOptions.pageStyle() : normalizeOptions.pageStyle;
    if (typeof defaultPageStyle === "string") {
      const styleElement = printDoc.createElement("style");
      styleElement.appendChild(printDoc.createTextNode(defaultPageStyle));
      printDoc.head.appendChild(styleElement);
    }
    if (normalizeOptions.bodyClassName) {
      printDoc.body.classList.add(...normalizeOptions.bodyClassName.split(" "));
    }
    let stylesheetLoadPromises = [];
    if (normalizeOptions.copyStyles) {
      const rootCandidate = gridRootElement.getRootNode();
      const root = rootCandidate.constructor.name === "ShadowRoot" ? rootCandidate : doc.current;
      stylesheetLoadPromises = loadStyleSheets(printDoc, root);
    }
    if (!DEBUG_MODE) {
      Promise.all(stylesheetLoadPromises).then(() => {
        printWindow.contentWindow.print();
      });
    }
  }, [apiRef, doc]);
  const handlePrintWindowAfterPrint = React226.useCallback((printWindow) => {
    var _a, _b;
    doc.current.body.removeChild(printWindow);
    apiRef.current.restoreState(previousGridState.current || {});
    if (!((_b = (_a = previousGridState.current) == null ? void 0 : _a.columns) == null ? void 0 : _b.columnVisibilityModel)) {
      apiRef.current.setColumnVisibilityModel(previousColumnVisibility.current);
    }
    apiRef.current.setState((state) => _extends({}, state, {
      virtualization: previousVirtualizationState.current
    }));
    apiRef.current.setRows(previousRows.current);
    previousGridState.current = null;
    previousColumnVisibility.current = {};
    previousRows.current = [];
  }, [apiRef]);
  const exportDataAsPrint = React226.useCallback(async (options3) => {
    logger.debug(`Export data as Print`);
    if (!apiRef.current.rootElementRef.current) {
      throw new Error("MUI X: No grid root element available.");
    }
    previousGridState.current = apiRef.current.exportState();
    previousColumnVisibility.current = gridColumnVisibilityModelSelector(apiRef);
    previousRows.current = apiRef.current.getSortedRows().filter((row) => !row[GRID_ID_AUTOGENERATED]);
    if (props.pagination) {
      const visibleRowCount = gridExpandedRowCountSelector(apiRef);
      const paginationModel = {
        page: 0,
        pageSize: visibleRowCount
      };
      apiRef.current.setState((state) => _extends({}, state, {
        pagination: _extends({}, state.pagination, {
          paginationModel: getDerivedPaginationModel(
            state.pagination,
            // Using signature `DataGridPro` to allow more than 100 rows in the print export
            "DataGridPro",
            paginationModel
          )
        })
      }));
    }
    previousVirtualizationState.current = apiRef.current.state.virtualization;
    apiRef.current.unstable_setVirtualization(false);
    await updateGridColumnsForPrint(options3 == null ? void 0 : options3.fields, options3 == null ? void 0 : options3.allColumns, options3 == null ? void 0 : options3.includeCheckboxes);
    updateGridRowsForPrint((options3 == null ? void 0 : options3.getRowsToExport) ?? defaultGetRowsToExport);
    await raf();
    const printWindow = buildPrintWindow(options3 == null ? void 0 : options3.fileName);
    if (false) {
      doc.current.body.appendChild(printWindow);
      handlePrintWindowLoad(printWindow, options3);
      handlePrintWindowAfterPrint(printWindow);
    } else {
      printWindow.onload = () => {
        handlePrintWindowLoad(printWindow, options3);
        const mediaQueryList = printWindow.contentWindow.matchMedia("print");
        mediaQueryList.addEventListener("change", (mql) => {
          const isAfterPrint = mql.matches === false;
          if (isAfterPrint) {
            handlePrintWindowAfterPrint(printWindow);
          }
        });
      };
      doc.current.body.appendChild(printWindow);
    }
  }, [props, logger, apiRef, handlePrintWindowLoad, handlePrintWindowAfterPrint, updateGridColumnsForPrint, updateGridRowsForPrint]);
  const printExportApi = {
    exportDataAsPrint
  };
  useGridApiMethod(apiRef, printExportApi, "public");
  const addExportMenuButtons = React226.useCallback((initialValue, options3) => {
    var _a;
    if ((_a = options3.printOptions) == null ? void 0 : _a.disableToolbarButton) {
      return initialValue;
    }
    return [...initialValue, {
      component: (0, import_jsx_runtime174.jsx)(GridPrintExportMenuItem, {
        options: options3.printOptions
      }),
      componentName: "printExport"
    }];
  }, []);
  useGridRegisterPipeProcessor(apiRef, "exportMenu", addExportMenuButtons);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/filter/useGridFilter.js
var React227 = __toESM(require_react(), 1);
var import_jsx_runtime175 = __toESM(require_jsx_runtime(), 1);
var filterStateInitializer = (state, props, apiRef) => {
  var _a, _b;
  const filterModel = props.filterModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.filter) == null ? void 0 : _b.filterModel) ?? getDefaultGridFilterModel();
  return _extends({}, state, {
    filter: _extends({
      filterModel: sanitizeFilterModel(filterModel, props.disableMultipleColumnsFiltering, apiRef)
    }, defaultGridFilterLookup),
    visibleRowsLookup: {}
  });
};
var getVisibleRowsLookup = (params) => {
  return params.filteredRowsLookup;
};
function getVisibleRowsLookupState(apiRef, state) {
  return apiRef.current.applyStrategyProcessor("visibleRowsLookupCreation", {
    tree: state.rows.tree,
    filteredRowsLookup: state.filter.filteredRowsLookup
  });
}
function createMemoizedValues() {
  return lruMemoize(Object.values);
}
var useGridFilter = (apiRef, props, configuration4) => {
  var _a, _b, _c;
  const logger = useGridLogger(apiRef, "useGridFilter");
  apiRef.current.registerControlState({
    stateId: "filter",
    propModel: props.filterModel,
    propOnChange: props.onFilterModelChange,
    stateSelector: gridFilterModelSelector,
    changeEvent: "filterModelChange"
  });
  const updateFilteredRows = React227.useCallback(() => {
    apiRef.current.setState((state) => {
      const filterModel = gridFilterModelSelector(apiRef);
      const filterState = apiRef.current.getFilterState(filterModel);
      const newState = _extends({}, state, {
        filter: _extends({}, state.filter, filterState)
      });
      const visibleRowsLookupState = getVisibleRowsLookupState(apiRef, newState);
      return _extends({}, newState, {
        visibleRowsLookup: visibleRowsLookupState
      });
    });
    apiRef.current.publishEvent("filteredRowsSet");
  }, [apiRef]);
  const addColumnMenuItem = React227.useCallback((columnMenuItems, colDef) => {
    if (colDef == null || colDef.filterable === false || props.disableColumnFilter) {
      return columnMenuItems;
    }
    return [...columnMenuItems, "columnMenuFilterItem"];
  }, [props.disableColumnFilter]);
  const upsertFilterItem = React227.useCallback((item) => {
    const filterModel = gridFilterModelSelector(apiRef);
    const items = [...filterModel.items];
    const itemIndex = items.findIndex((filterItem) => filterItem.id === item.id);
    if (itemIndex === -1) {
      items.push(item);
    } else {
      items[itemIndex] = item;
    }
    apiRef.current.setFilterModel(_extends({}, filterModel, {
      items
    }), "upsertFilterItem");
  }, [apiRef]);
  const upsertFilterItems = React227.useCallback((items) => {
    const filterModel = gridFilterModelSelector(apiRef);
    const existingItems = [...filterModel.items];
    items.forEach((item) => {
      const itemIndex = existingItems.findIndex((filterItem) => filterItem.id === item.id);
      if (itemIndex === -1) {
        existingItems.push(item);
      } else {
        existingItems[itemIndex] = item;
      }
    });
    apiRef.current.setFilterModel(_extends({}, filterModel, {
      items: existingItems
    }), "upsertFilterItems");
  }, [apiRef]);
  const deleteFilterItem = React227.useCallback((itemToDelete) => {
    const filterModel = gridFilterModelSelector(apiRef);
    const items = filterModel.items.filter((item) => item.id !== itemToDelete.id);
    if (items.length === filterModel.items.length) {
      return;
    }
    apiRef.current.setFilterModel(_extends({}, filterModel, {
      items
    }), "deleteFilterItem");
  }, [apiRef]);
  const showFilterPanel = React227.useCallback((targetColumnField, panelId, labelId) => {
    logger.debug("Displaying filter panel");
    if (targetColumnField) {
      const filterModel = gridFilterModelSelector(apiRef);
      const filterItemsWithValue = filterModel.items.filter((item) => {
        var _a2;
        if (item.value !== void 0) {
          if (Array.isArray(item.value) && item.value.length === 0) {
            return false;
          }
          return true;
        }
        const column = apiRef.current.getColumn(item.field);
        const filterOperator = (_a2 = column.filterOperators) == null ? void 0 : _a2.find((operator) => operator.value === item.operator);
        const requiresFilterValue = typeof (filterOperator == null ? void 0 : filterOperator.requiresFilterValue) === "undefined" ? true : filterOperator == null ? void 0 : filterOperator.requiresFilterValue;
        if (requiresFilterValue) {
          return false;
        }
        return true;
      });
      let newFilterItems;
      const filterItemOnTarget = filterItemsWithValue.find((item) => item.field === targetColumnField);
      const targetColumn = apiRef.current.getColumn(targetColumnField);
      if (filterItemOnTarget) {
        newFilterItems = filterItemsWithValue;
      } else if (props.disableMultipleColumnsFiltering) {
        newFilterItems = [cleanFilterItem({
          field: targetColumnField,
          operator: targetColumn.filterOperators[0].value
        }, apiRef)];
      } else {
        newFilterItems = [...filterItemsWithValue, cleanFilterItem({
          field: targetColumnField,
          operator: targetColumn.filterOperators[0].value
        }, apiRef)];
      }
      apiRef.current.setFilterModel(_extends({}, filterModel, {
        items: newFilterItems
      }));
    }
    apiRef.current.showPreferences(GridPreferencePanelsValue.filters, panelId, labelId);
  }, [apiRef, logger, props.disableMultipleColumnsFiltering]);
  const hideFilterPanel = React227.useCallback(() => {
    logger.debug("Hiding filter panel");
    apiRef.current.hidePreferences();
  }, [apiRef, logger]);
  const setFilterLogicOperator = React227.useCallback((logicOperator) => {
    const filterModel = gridFilterModelSelector(apiRef);
    if (filterModel.logicOperator === logicOperator) {
      return;
    }
    apiRef.current.setFilterModel(_extends({}, filterModel, {
      logicOperator
    }), "changeLogicOperator");
  }, [apiRef]);
  const setQuickFilterValues = React227.useCallback((values3) => {
    const filterModel = gridFilterModelSelector(apiRef);
    if (isDeepEqual(filterModel.quickFilterValues, values3)) {
      return;
    }
    apiRef.current.setFilterModel(_extends({}, filterModel, {
      quickFilterValues: [...values3]
    }));
  }, [apiRef]);
  const setFilterModel = React227.useCallback((model, reason) => {
    const currentModel = gridFilterModelSelector(apiRef);
    if (currentModel !== model) {
      logger.debug("Setting filter model");
      apiRef.current.updateControlState("filter", mergeStateWithFilterModel(model, props.disableMultipleColumnsFiltering, apiRef), reason);
      apiRef.current.unstable_applyFilters();
    }
  }, [apiRef, logger, props.disableMultipleColumnsFiltering]);
  const getFilterState = React227.useCallback((inputFilterModel) => {
    const filterModel = sanitizeFilterModel(inputFilterModel, props.disableMultipleColumnsFiltering, apiRef);
    const filterValueGetter = configuration4.hooks.useFilterValueGetter(apiRef, props);
    const isRowMatchingFilters = props.filterMode === "client" ? buildAggregatedFilterApplier(filterModel, filterValueGetter, apiRef, props.disableEval) : null;
    const filterResult = apiRef.current.applyStrategyProcessor("filtering", {
      isRowMatchingFilters,
      filterModel: filterModel ?? getDefaultGridFilterModel(),
      filterValueGetter
    });
    return _extends({}, filterResult, {
      filterModel
    });
  }, [apiRef, configuration4.hooks, props]);
  const filterApi = {
    setFilterLogicOperator,
    unstable_applyFilters: updateFilteredRows,
    deleteFilterItem,
    upsertFilterItem,
    upsertFilterItems,
    setFilterModel,
    showFilterPanel,
    hideFilterPanel,
    setQuickFilterValues,
    ignoreDiacritics: props.ignoreDiacritics,
    getFilterState
  };
  useGridApiMethod(apiRef, filterApi, "public");
  const stateExportPreProcessing = React227.useCallback((prevState, context) => {
    var _a2, _b2;
    const filterModelToExport = gridFilterModelSelector(apiRef);
    filterModelToExport.items.forEach((item) => {
      delete item.fromInput;
    });
    const shouldExportFilterModel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the model is controlled
      props.filterModel != null || // Always export if the model has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.filter) == null ? void 0 : _b2.filterModel) != null || // Export if the model is not equal to the default value
      !isDeepEqual(filterModelToExport, getDefaultGridFilterModel())
    );
    if (!shouldExportFilterModel) {
      return prevState;
    }
    return _extends({}, prevState, {
      filter: {
        filterModel: filterModelToExport
      }
    });
  }, [apiRef, props.filterModel, (_b = (_a = props.initialState) == null ? void 0 : _a.filter) == null ? void 0 : _b.filterModel]);
  const stateRestorePreProcessing = React227.useCallback((params, context) => {
    var _a2;
    const filterModel = (_a2 = context.stateToRestore.filter) == null ? void 0 : _a2.filterModel;
    if (filterModel == null) {
      return params;
    }
    apiRef.current.updateControlState("filter", mergeStateWithFilterModel(filterModel, props.disableMultipleColumnsFiltering, apiRef), "restoreState");
    return _extends({}, params, {
      callbacks: [...params.callbacks, apiRef.current.unstable_applyFilters]
    });
  }, [apiRef, props.disableMultipleColumnsFiltering]);
  const preferencePanelPreProcessing = React227.useCallback((initialValue, value) => {
    var _a2;
    if (value === GridPreferencePanelsValue.filters) {
      const FilterPanel = props.slots.filterPanel;
      return (0, import_jsx_runtime175.jsx)(FilterPanel, _extends({}, (_a2 = props.slotProps) == null ? void 0 : _a2.filterPanel));
    }
    return initialValue;
  }, [props.slots.filterPanel, (_c = props.slotProps) == null ? void 0 : _c.filterPanel]);
  const {
    getRowId
  } = props;
  const getRowsRef = useLazyRef(createMemoizedValues);
  const flatFilteringMethod = React227.useCallback((params) => {
    var _a2;
    if (props.filterMode !== "client" || !params.isRowMatchingFilters || !params.filterModel.items.length && !((_a2 = params.filterModel.quickFilterValues) == null ? void 0 : _a2.length)) {
      return defaultGridFilterLookup;
    }
    const dataRowIdToModelLookup = gridRowsLookupSelector(apiRef);
    const filteredRowsLookup = {};
    const {
      isRowMatchingFilters
    } = params;
    const filterCache = {};
    const result = {
      passingFilterItems: null,
      passingQuickFilterValues: null
    };
    const rows = getRowsRef.current(apiRef.current.state.rows.dataRowIdToModelLookup);
    for (let i2 = 0; i2 < rows.length; i2 += 1) {
      const row = rows[i2];
      const id = getRowId ? getRowId(row) : row.id;
      isRowMatchingFilters(row, void 0, result);
      const isRowPassing = passFilterLogic([result.passingFilterItems], [result.passingQuickFilterValues], params.filterModel, params.filterValueGetter, apiRef, filterCache);
      if (!isRowPassing) {
        filteredRowsLookup[id] = isRowPassing;
      }
    }
    const footerId = "auto-generated-group-footer-root";
    const footer = dataRowIdToModelLookup[footerId];
    if (footer) {
      filteredRowsLookup[footerId] = true;
    }
    return {
      filteredRowsLookup,
      filteredChildrenCountLookup: {},
      filteredDescendantCountLookup: {}
    };
  }, [apiRef, props.filterMode, getRowId, getRowsRef]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuItem);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  useGridRegisterPipeProcessor(apiRef, "preferencePanel", preferencePanelPreProcessing);
  useGridRegisterStrategyProcessor(apiRef, GRID_DEFAULT_STRATEGY, "filtering", flatFilteringMethod);
  useGridRegisterStrategyProcessor(apiRef, GRID_DEFAULT_STRATEGY, "visibleRowsLookupCreation", getVisibleRowsLookup);
  const handleColumnsChange = React227.useCallback(() => {
    logger.debug("onColUpdated - GridColumns changed, applying filters");
    const filterModel = gridFilterModelSelector(apiRef);
    const columnsLookup = gridColumnLookupSelector(apiRef);
    const newFilterItems = filterModel.items.filter((item) => item.field && columnsLookup[item.field]);
    if (newFilterItems.length < filterModel.items.length) {
      apiRef.current.setFilterModel(_extends({}, filterModel, {
        items: newFilterItems
      }));
    }
  }, [apiRef, logger]);
  const handleStrategyProcessorChange = React227.useCallback((methodName) => {
    if (methodName === "filtering") {
      apiRef.current.unstable_applyFilters();
    }
  }, [apiRef]);
  const updateVisibleRowsLookupState = React227.useCallback(() => {
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        visibleRowsLookup: getVisibleRowsLookupState(apiRef, state)
      });
    });
  }, [apiRef]);
  useGridEvent(apiRef, "rowsSet", updateFilteredRows);
  useGridEvent(apiRef, "columnsChange", handleColumnsChange);
  useGridEvent(apiRef, "activeStrategyProcessorChange", handleStrategyProcessorChange);
  useGridEvent(apiRef, "rowExpansionChange", updateVisibleRowsLookupState);
  useGridEvent(apiRef, "columnVisibilityModelChange", () => {
    var _a2;
    const filterModel = gridFilterModelSelector(apiRef);
    if (((_a2 = filterModel.quickFilterValues) == null ? void 0 : _a2.length) && shouldQuickFilterExcludeHiddenColumns(filterModel)) {
      updateFilteredRows();
    }
  });
  useFirstRender(() => {
    if (props.signature === "DataGrid") {
      updateFilteredRows();
    }
  });
  useEnhancedEffect_default(() => {
    if (props.filterModel !== void 0) {
      apiRef.current.setFilterModel(props.filterModel);
    }
  }, [apiRef, logger, props.filterModel]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/focus/useGridFocus.js
var React228 = __toESM(require_react(), 1);
var focusStateInitializer = (state) => _extends({}, state, {
  focus: {
    cell: null,
    columnHeader: null,
    columnHeaderFilter: null,
    columnGroupHeader: null
  },
  tabIndex: {
    cell: null,
    columnHeader: null,
    columnHeaderFilter: null,
    columnGroupHeader: null
  }
});
var useGridFocus = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridFocus");
  const lastClickedCell = React228.useRef(null);
  const hasRootReference = apiRef.current.rootElementRef.current !== null;
  const publishCellFocusOut = React228.useCallback((cell, event) => {
    if (cell) {
      if (apiRef.current.getRow(cell.id)) {
        apiRef.current.publishEvent("cellFocusOut", apiRef.current.getCellParams(cell.id, cell.field), event);
      }
    }
  }, [apiRef]);
  const setCellFocus = React228.useCallback((id, field) => {
    const focusedCell = gridFocusCellSelector(apiRef);
    if ((focusedCell == null ? void 0 : focusedCell.id) === id && (focusedCell == null ? void 0 : focusedCell.field) === field) {
      if (apiRef.current.getCellMode(id, field) !== "view") {
        return;
      }
      const cellElement = apiRef.current.getCellElement(id, field);
      if (!cellElement) {
        return;
      }
      const gridRoot = apiRef.current.rootElementRef.current;
      const doc = ownerDocument(gridRoot);
      const activeElement2 = doc.activeElement;
      const allowTakingFocus = !activeElement2 || activeElement2 === doc.body || activeElement2 === doc.documentElement || (gridRoot == null ? void 0 : gridRoot.contains(activeElement2));
      if (!allowTakingFocus) {
        return;
      }
      if (cellElement.contains(doc.activeElement)) {
        return;
      }
      if (doesSupportPreventScroll()) {
        cellElement.focus({
          preventScroll: true
        });
      } else {
        const scrollPosition = apiRef.current.getScrollPosition();
        cellElement.focus();
        apiRef.current.scroll(scrollPosition);
      }
      return;
    }
    apiRef.current.setState((state) => {
      logger.debug(`Focusing on cell with id=${id} and field=${field}`);
      return _extends({}, state, {
        tabIndex: {
          cell: {
            id,
            field
          },
          columnHeader: null,
          columnHeaderFilter: null,
          columnGroupHeader: null
        },
        focus: {
          cell: {
            id,
            field
          },
          columnHeader: null,
          columnHeaderFilter: null,
          columnGroupHeader: null
        }
      });
    });
    if (!apiRef.current.getRow(id)) {
      return;
    }
    if (focusedCell) {
      publishCellFocusOut(focusedCell, {});
    }
    apiRef.current.publishEvent("cellFocusIn", apiRef.current.getCellParams(id, field));
  }, [apiRef, logger, publishCellFocusOut]);
  const setColumnHeaderFocus = React228.useCallback((field, event = {}) => {
    const cell = gridFocusCellSelector(apiRef);
    publishCellFocusOut(cell, event);
    apiRef.current.setState((state) => {
      logger.debug(`Focusing on column header with colIndex=${field}`);
      return _extends({}, state, {
        tabIndex: {
          columnHeader: {
            field
          },
          columnHeaderFilter: null,
          cell: null,
          columnGroupHeader: null
        },
        focus: {
          columnHeader: {
            field
          },
          columnHeaderFilter: null,
          cell: null,
          columnGroupHeader: null
        }
      });
    });
  }, [apiRef, logger, publishCellFocusOut]);
  const setColumnHeaderFilterFocus = React228.useCallback((field, event = {}) => {
    const cell = gridFocusCellSelector(apiRef);
    publishCellFocusOut(cell, event);
    apiRef.current.setState((state) => {
      logger.debug(`Focusing on column header filter with colIndex=${field}`);
      return _extends({}, state, {
        tabIndex: {
          columnHeader: null,
          columnHeaderFilter: {
            field
          },
          cell: null,
          columnGroupHeader: null
        },
        focus: {
          columnHeader: null,
          columnHeaderFilter: {
            field
          },
          cell: null,
          columnGroupHeader: null
        }
      });
    });
  }, [apiRef, logger, publishCellFocusOut]);
  const setColumnGroupHeaderFocus = React228.useCallback((field, depth, event = {}) => {
    const cell = gridFocusCellSelector(apiRef);
    if (cell) {
      apiRef.current.publishEvent("cellFocusOut", apiRef.current.getCellParams(cell.id, cell.field), event);
    }
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        tabIndex: {
          columnGroupHeader: {
            field,
            depth
          },
          columnHeader: null,
          columnHeaderFilter: null,
          cell: null
        },
        focus: {
          columnGroupHeader: {
            field,
            depth
          },
          columnHeader: null,
          columnHeaderFilter: null,
          cell: null
        }
      });
    });
  }, [apiRef]);
  const getColumnGroupHeaderFocus = React228.useCallback(() => gridFocusColumnGroupHeaderSelector(apiRef), [apiRef]);
  const moveFocusToRelativeCell = React228.useCallback((id, field, direction) => {
    let columnIndexToFocus = apiRef.current.getColumnIndex(field);
    const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
    const currentPage = getVisibleRows(apiRef, {
      pagination: props.pagination,
      paginationMode: props.paginationMode
    });
    const pinnedRows = gridPinnedRowsSelector(apiRef);
    const currentPageRows = [].concat(pinnedRows.top || [], currentPage.rows, pinnedRows.bottom || []);
    let rowIndexToFocus = currentPageRows.findIndex((row) => row.id === id);
    if (direction === "right") {
      columnIndexToFocus += 1;
    } else if (direction === "left") {
      columnIndexToFocus -= 1;
    } else {
      rowIndexToFocus += 1;
    }
    if (columnIndexToFocus >= visibleColumns.length) {
      rowIndexToFocus += 1;
      if (rowIndexToFocus < currentPageRows.length) {
        columnIndexToFocus = 0;
      }
    } else if (columnIndexToFocus < 0) {
      rowIndexToFocus -= 1;
      if (rowIndexToFocus >= 0) {
        columnIndexToFocus = visibleColumns.length - 1;
      }
    }
    rowIndexToFocus = clamp(rowIndexToFocus, 0, currentPageRows.length - 1);
    const rowToFocus = currentPageRows[rowIndexToFocus];
    if (!rowToFocus) {
      return;
    }
    const colSpanInfo = apiRef.current.unstable_getCellColSpanInfo(rowToFocus.id, columnIndexToFocus);
    if (colSpanInfo && colSpanInfo.spannedByColSpan) {
      if (direction === "left" || direction === "below") {
        columnIndexToFocus = colSpanInfo.leftVisibleCellIndex;
      } else if (direction === "right") {
        columnIndexToFocus = colSpanInfo.rightVisibleCellIndex;
      }
    }
    columnIndexToFocus = clamp(columnIndexToFocus, 0, visibleColumns.length - 1);
    const columnToFocus = visibleColumns[columnIndexToFocus];
    apiRef.current.setCellFocus(rowToFocus.id, columnToFocus.field);
  }, [apiRef, props.pagination, props.paginationMode]);
  const handleCellDoubleClick = React228.useCallback(({
    id,
    field
  }) => {
    apiRef.current.setCellFocus(id, field);
  }, [apiRef]);
  const handleCellKeyDown = React228.useCallback((params, event) => {
    if (isPasteShortcut(event) || event.key === "Enter" || event.key === "Tab" || event.key === "Shift" || isNavigationKey(event.key)) {
      return;
    }
    apiRef.current.setCellFocus(params.id, params.field);
  }, [apiRef]);
  const handleColumnHeaderFocus = React228.useCallback(({
    field
  }, event) => {
    if (event.target !== event.currentTarget) {
      return;
    }
    apiRef.current.setColumnHeaderFocus(field, event);
  }, [apiRef]);
  const handleColumnGroupHeaderFocus = React228.useCallback(({
    fields,
    depth
  }, event) => {
    if (event.target !== event.currentTarget) {
      return;
    }
    const focusedColumnGroup = gridFocusColumnGroupHeaderSelector(apiRef);
    if (focusedColumnGroup !== null && focusedColumnGroup.depth === depth && fields.includes(focusedColumnGroup.field)) {
      return;
    }
    apiRef.current.setColumnGroupHeaderFocus(fields[0], depth, event);
  }, [apiRef]);
  const handleBlur = React228.useCallback((_, event) => {
    var _a, _b;
    if ((_b = (_a = event.relatedTarget) == null ? void 0 : _a.getAttribute("class")) == null ? void 0 : _b.includes(gridClasses.columnHeader)) {
      return;
    }
    logger.debug(`Clearing focus`);
    apiRef.current.setState((state) => _extends({}, state, {
      focus: {
        cell: null,
        columnHeader: null,
        columnHeaderFilter: null,
        columnGroupHeader: null
      }
    }));
  }, [logger, apiRef]);
  const handleCellMouseDown = React228.useCallback((params) => {
    lastClickedCell.current = params;
  }, []);
  const handleDocumentClick = React228.useCallback((event) => {
    const cellParams = lastClickedCell.current;
    lastClickedCell.current = null;
    const focusedCell = gridFocusCellSelector(apiRef);
    const canUpdateFocus = apiRef.current.unstable_applyPipeProcessors("canUpdateFocus", true, {
      event,
      cell: cellParams
    });
    if (!canUpdateFocus) {
      return;
    }
    if (!focusedCell) {
      if (cellParams) {
        apiRef.current.setCellFocus(cellParams.id, cellParams.field);
      }
      return;
    }
    if ((cellParams == null ? void 0 : cellParams.id) === focusedCell.id && (cellParams == null ? void 0 : cellParams.field) === focusedCell.field) {
      return;
    }
    const cellElement = apiRef.current.getCellElement(focusedCell.id, focusedCell.field);
    if (cellElement == null ? void 0 : cellElement.contains(event.target)) {
      return;
    }
    if (cellParams) {
      apiRef.current.setCellFocus(cellParams.id, cellParams.field);
    } else {
      apiRef.current.setState((state) => _extends({}, state, {
        focus: {
          cell: null,
          columnHeader: null,
          columnHeaderFilter: null,
          columnGroupHeader: null
        }
      }));
      publishCellFocusOut(focusedCell, event);
    }
  }, [apiRef, publishCellFocusOut]);
  const handleCellModeChange = React228.useCallback((params) => {
    if (params.cellMode === "view") {
      return;
    }
    const cell = gridFocusCellSelector(apiRef);
    if ((cell == null ? void 0 : cell.id) !== params.id || (cell == null ? void 0 : cell.field) !== params.field) {
      apiRef.current.setCellFocus(params.id, params.field);
    }
  }, [apiRef]);
  const handleRowsSet = React228.useCallback(() => {
    const cell = gridFocusCellSelector(apiRef);
    if (cell && !apiRef.current.getRow(cell.id)) {
      const lastFocusedRowId = cell.id;
      let nextRowId = null;
      if (typeof lastFocusedRowId !== "undefined") {
        const rowEl = apiRef.current.getRowElement(lastFocusedRowId);
        const lastFocusedRowIndex = (rowEl == null ? void 0 : rowEl.dataset.rowindex) ? Number(rowEl == null ? void 0 : rowEl.dataset.rowindex) : 0;
        const currentPage = getVisibleRows(apiRef, {
          pagination: props.pagination,
          paginationMode: props.paginationMode
        });
        const nextRow = currentPage.rows[clamp(lastFocusedRowIndex, 0, currentPage.rows.length - 1)];
        nextRowId = (nextRow == null ? void 0 : nextRow.id) ?? null;
      }
      apiRef.current.setState((state) => _extends({}, state, {
        focus: {
          cell: nextRowId === null ? null : {
            id: nextRowId,
            field: cell.field
          },
          columnHeader: null,
          columnHeaderFilter: null,
          columnGroupHeader: null
        }
      }));
    }
  }, [apiRef, props.pagination, props.paginationMode]);
  const debouncedHandleRowsSet = React228.useMemo(() => debounce(handleRowsSet, 0), [handleRowsSet]);
  const handlePaginationModelChange = useEventCallback_default(() => {
    const currentFocusedCell = gridFocusCellSelector(apiRef);
    if (!currentFocusedCell) {
      return;
    }
    const currentPage = getVisibleRows(apiRef, {
      pagination: props.pagination,
      paginationMode: props.paginationMode
    });
    const rowIsInCurrentPage = currentPage.rows.find((row) => row.id === currentFocusedCell.id);
    if (rowIsInCurrentPage || currentPage.rows.length === 0) {
      return;
    }
    const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        tabIndex: {
          cell: {
            id: currentPage.rows[0].id,
            field: visibleColumns[0].field
          },
          columnGroupHeader: null,
          columnHeader: null,
          columnHeaderFilter: null
        }
      });
    });
  });
  const focusApi = {
    setCellFocus,
    setColumnHeaderFocus,
    setColumnHeaderFilterFocus
  };
  const focusPrivateApi = {
    moveFocusToRelativeCell,
    setColumnGroupHeaderFocus,
    getColumnGroupHeaderFocus
  };
  useGridApiMethod(apiRef, focusApi, "public");
  useGridApiMethod(apiRef, focusPrivateApi, "private");
  React228.useEffect(() => {
    const doc = ownerDocument(apiRef.current.rootElementRef.current);
    doc.addEventListener("mouseup", handleDocumentClick);
    return () => {
      doc.removeEventListener("mouseup", handleDocumentClick);
    };
  }, [apiRef, hasRootReference, handleDocumentClick]);
  useGridEvent(apiRef, "columnHeaderBlur", handleBlur);
  useGridEvent(apiRef, "cellDoubleClick", handleCellDoubleClick);
  useGridEvent(apiRef, "cellMouseDown", handleCellMouseDown);
  useGridEvent(apiRef, "cellKeyDown", handleCellKeyDown);
  useGridEvent(apiRef, "cellModeChange", handleCellModeChange);
  useGridEvent(apiRef, "columnHeaderFocus", handleColumnHeaderFocus);
  useGridEvent(apiRef, "columnGroupHeaderFocus", handleColumnGroupHeaderFocus);
  useGridEvent(apiRef, "rowsSet", debouncedHandleRowsSet);
  useGridEvent(apiRef, "paginationModelChange", handlePaginationModelChange);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/keyboardNavigation/useGridKeyboardNavigation.js
var React229 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/keyboardNavigation/utils.js
var getLeftColumnIndex = ({
  currentColIndex,
  firstColIndex,
  lastColIndex,
  isRtl
}) => {
  if (isRtl) {
    if (currentColIndex < lastColIndex) {
      return currentColIndex + 1;
    }
  } else if (!isRtl) {
    if (currentColIndex > firstColIndex) {
      return currentColIndex - 1;
    }
  }
  return null;
};
var getRightColumnIndex = ({
  currentColIndex,
  firstColIndex,
  lastColIndex,
  isRtl
}) => {
  if (isRtl) {
    if (currentColIndex > firstColIndex) {
      return currentColIndex - 1;
    }
  } else if (!isRtl) {
    if (currentColIndex < lastColIndex) {
      return currentColIndex + 1;
    }
  }
  return null;
};
function findNonRowSpannedCell(apiRef, rowId, colIndex, rowSpanScanDirection) {
  var _a, _b;
  const rowSpanHiddenCells = Rowspan.selectors.hiddenCells(apiRef.current.virtualizer.store.state);
  if (!((_a = rowSpanHiddenCells[rowId]) == null ? void 0 : _a[colIndex])) {
    return rowId;
  }
  const filteredSortedRowIds = gridFilteredSortedRowIdsSelector(apiRef);
  let nextRowIndex = filteredSortedRowIds.indexOf(rowId) + (rowSpanScanDirection === "down" ? 1 : -1);
  while (nextRowIndex >= 0 && nextRowIndex < filteredSortedRowIds.length) {
    const nextRowId = filteredSortedRowIds[nextRowIndex];
    if (!((_b = rowSpanHiddenCells[nextRowId]) == null ? void 0 : _b[colIndex])) {
      return nextRowId;
    }
    nextRowIndex += rowSpanScanDirection === "down" ? 1 : -1;
  }
  return rowId;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/keyboardNavigation/useGridKeyboardNavigation.js
var gridVisibleRowsWithPinnedRowsSelector = createSelectorMemoized2(gridVisibleRowsSelector, gridPinnedRowsSelector, (visibleRows, pinnedRows) => {
  return (pinnedRows.top || []).concat(visibleRows.rows, pinnedRows.bottom || []);
});
var useGridKeyboardNavigation = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridKeyboardNavigation");
  const isRtl = useRtl();
  const getCurrentPageRows = React229.useCallback(() => {
    return gridVisibleRowsWithPinnedRowsSelector(apiRef);
  }, [apiRef]);
  const headerFilteringEnabled = props.signature !== "DataGrid" && props.headerFilters;
  const goToCell = React229.useCallback((colIndex, rowId, closestColumnToUse = "left", rowSpanScanDirection = "up") => {
    const visibleSortedRows = gridExpandedSortedRowEntriesSelector(apiRef);
    const nextCellColSpanInfo = apiRef.current.unstable_getCellColSpanInfo(rowId, colIndex);
    if (nextCellColSpanInfo && nextCellColSpanInfo.spannedByColSpan) {
      if (closestColumnToUse === "left") {
        colIndex = nextCellColSpanInfo.leftVisibleCellIndex;
      } else if (closestColumnToUse === "right") {
        colIndex = nextCellColSpanInfo.rightVisibleCellIndex;
      }
    }
    const field = gridVisibleColumnFieldsSelector(apiRef)[colIndex];
    const nonRowSpannedRowId = findNonRowSpannedCell(apiRef, rowId, colIndex, rowSpanScanDirection);
    const rowIndexRelativeToAllRows = visibleSortedRows.findIndex((row) => row.id === nonRowSpannedRowId);
    logger.debug(`Navigating to cell row ${rowIndexRelativeToAllRows}, col ${colIndex}`);
    apiRef.current.scrollToIndexes({
      colIndex,
      rowIndex: rowIndexRelativeToAllRows
    });
    apiRef.current.setCellFocus(nonRowSpannedRowId, field);
  }, [apiRef, logger]);
  const goToHeader = React229.useCallback((colIndex, event) => {
    logger.debug(`Navigating to header col ${colIndex}`);
    apiRef.current.scrollToIndexes({
      colIndex
    });
    const field = apiRef.current.getVisibleColumns()[colIndex].field;
    apiRef.current.setColumnHeaderFocus(field, event);
  }, [apiRef, logger]);
  const goToHeaderFilter = React229.useCallback((colIndex, event) => {
    logger.debug(`Navigating to header filter col ${colIndex}`);
    apiRef.current.scrollToIndexes({
      colIndex
    });
    const field = apiRef.current.getVisibleColumns()[colIndex].field;
    apiRef.current.setColumnHeaderFilterFocus(field, event);
  }, [apiRef, logger]);
  const goToGroupHeader = React229.useCallback((colIndex, depth, event) => {
    logger.debug(`Navigating to header col ${colIndex}`);
    apiRef.current.scrollToIndexes({
      colIndex
    });
    const {
      field
    } = apiRef.current.getVisibleColumns()[colIndex];
    apiRef.current.setColumnGroupHeaderFocus(field, depth, event);
  }, [apiRef, logger]);
  const getRowIdFromIndex = React229.useCallback((rowIndex) => {
    var _a;
    return (_a = getCurrentPageRows()[rowIndex]) == null ? void 0 : _a.id;
  }, [getCurrentPageRows]);
  const handleColumnHeaderKeyDown = React229.useCallback((params, event) => {
    const headerTitleNode = event.currentTarget.querySelector(`.${gridClasses.columnHeaderTitleContainerContent}`);
    const isFromInsideContent = !!headerTitleNode && headerTitleNode.contains(event.target);
    if (isFromInsideContent && params.field !== GRID_CHECKBOX_SELECTION_COL_DEF.field) {
      return;
    }
    if (!isNavigationKey(event.key) && event.key !== "Tab" && event.key !== "Enter") {
      return;
    }
    if (event.key === "Tab" && (props.tabNavigation === "none" || props.tabNavigation === "content" && event.shiftKey)) {
      return;
    }
    const currentPageRows = getCurrentPageRows();
    const viewportPageSize = apiRef.current.getViewportPageSize();
    const colIndexBefore = params.field ? apiRef.current.getColumnIndex(params.field) : 0;
    const firstRowIndexInPage = currentPageRows.length > 0 ? 0 : null;
    const lastRowIndexInPage = currentPageRows.length > 0 ? currentPageRows.length - 1 : null;
    const firstColIndex = 0;
    const lastColIndex = Math.max(0, gridVisibleColumnDefinitionsSelector(apiRef).length - 1);
    const columnGroupMaxDepth = gridColumnGroupsHeaderMaxDepthSelector(apiRef);
    let shouldPreventDefault = true;
    if (event.key === "Tab" && props.tabNavigation === "content" && !event.shiftKey) {
      if (firstRowIndexInPage !== null) {
        goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
        event.preventDefault();
      }
      return;
    }
    switch (event.key) {
      case "ArrowDown": {
        if (headerFilteringEnabled) {
          goToHeaderFilter(colIndexBefore, event);
        } else if (firstRowIndexInPage !== null) {
          goToCell(colIndexBefore, getRowIdFromIndex(firstRowIndexInPage));
        }
        break;
      }
      case "ArrowRight": {
        const rightColIndex = getRightColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (rightColIndex !== null) {
          goToHeader(rightColIndex, event);
        }
        break;
      }
      case "ArrowLeft": {
        const leftColIndex = getLeftColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (leftColIndex !== null) {
          goToHeader(leftColIndex, event);
        }
        break;
      }
      case "ArrowUp": {
        if (columnGroupMaxDepth > 0) {
          goToGroupHeader(colIndexBefore, columnGroupMaxDepth - 1, event);
        }
        break;
      }
      case "PageDown": {
        if (firstRowIndexInPage !== null && lastRowIndexInPage !== null) {
          goToCell(colIndexBefore, getRowIdFromIndex(Math.min(firstRowIndexInPage + viewportPageSize, lastRowIndexInPage)));
        }
        break;
      }
      case "Home": {
        goToHeader(firstColIndex, event);
        break;
      }
      case "End": {
        goToHeader(lastColIndex, event);
        break;
      }
      case "Enter": {
        if (event.ctrlKey || event.metaKey) {
          apiRef.current.toggleColumnMenu(params.field);
        }
        break;
      }
      case "Tab": {
        if (event.shiftKey) {
          if (colIndexBefore === firstColIndex) {
            if (columnGroupMaxDepth > 0) {
              goToGroupHeader(lastColIndex, columnGroupMaxDepth - 1, event);
            } else {
              shouldPreventDefault = false;
            }
          } else {
            goToHeader(colIndexBefore - 1, event);
          }
        } else if (colIndexBefore === lastColIndex) {
          if (headerFilteringEnabled) {
            goToHeaderFilter(firstColIndex, event);
          } else if (props.tabNavigation === "all" && firstRowIndexInPage !== null) {
            goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
          } else {
            shouldPreventDefault = false;
          }
        } else {
          goToHeader(colIndexBefore + 1, event);
        }
        break;
      }
      case " ": {
        break;
      }
      default: {
        shouldPreventDefault = false;
      }
    }
    if (shouldPreventDefault) {
      event.preventDefault();
    }
  }, [apiRef, props.tabNavigation, getCurrentPageRows, headerFilteringEnabled, goToHeaderFilter, goToCell, getRowIdFromIndex, isRtl, goToHeader, goToGroupHeader]);
  const handleHeaderFilterKeyDown = React229.useCallback((params, event) => {
    const isEditing = gridHeaderFilteringEditFieldSelector(apiRef) === params.field;
    const isHeaderMenuOpen = gridHeaderFilteringMenuSelector(apiRef) === params.field;
    if (isHeaderMenuOpen || isEditing && event.key !== "Tab") {
      return;
    }
    if (!isNavigationKey(event.key) && event.key !== "Tab") {
      return;
    }
    if (event.key === "Tab" && (props.tabNavigation === "none" || props.tabNavigation === "content" && event.shiftKey)) {
      return;
    }
    const currentPageRows = getCurrentPageRows();
    const viewportPageSize = apiRef.current.getViewportPageSize();
    const colIndexBefore = params.field ? apiRef.current.getColumnIndex(params.field) : 0;
    const firstRowIndexInPage = currentPageRows.length > 0 ? 0 : null;
    const lastRowIndexInPage = currentPageRows.length > 0 ? currentPageRows.length - 1 : null;
    const firstColIndex = 0;
    const lastColIndex = Math.max(0, gridVisibleColumnDefinitionsSelector(apiRef).length - 1);
    let shouldPreventDefault = true;
    if (event.key === "Tab" && props.tabNavigation === "content" && !event.shiftKey) {
      if (firstRowIndexInPage !== null) {
        goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
        event.preventDefault();
      }
      return;
    }
    switch (event.key) {
      case "ArrowDown": {
        if (firstRowIndexInPage !== null) {
          goToCell(colIndexBefore, getRowIdFromIndex(firstRowIndexInPage));
        }
        break;
      }
      case "ArrowRight": {
        const rightColIndex = getRightColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (rightColIndex !== null) {
          goToHeaderFilter(rightColIndex, event);
        }
        break;
      }
      case "ArrowLeft": {
        const leftColIndex = getLeftColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (leftColIndex !== null) {
          goToHeaderFilter(leftColIndex, event);
        } else {
          apiRef.current.setColumnHeaderFilterFocus(params.field, event);
        }
        break;
      }
      case "ArrowUp": {
        goToHeader(colIndexBefore, event);
        break;
      }
      case "PageDown": {
        if (firstRowIndexInPage !== null && lastRowIndexInPage !== null) {
          goToCell(colIndexBefore, getRowIdFromIndex(Math.min(firstRowIndexInPage + viewportPageSize, lastRowIndexInPage)));
        }
        break;
      }
      case "Home": {
        goToHeaderFilter(firstColIndex, event);
        break;
      }
      case "End": {
        goToHeaderFilter(lastColIndex, event);
        break;
      }
      case "Tab": {
        if (event.shiftKey) {
          if (colIndexBefore === firstColIndex) {
            goToHeader(lastColIndex, event);
          } else {
            goToHeaderFilter(colIndexBefore - 1, event);
          }
        } else if (colIndexBefore === lastColIndex) {
          if (props.tabNavigation === "all" && firstRowIndexInPage !== null) {
            goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
          } else {
            shouldPreventDefault = false;
          }
        } else {
          goToHeaderFilter(colIndexBefore + 1, event);
        }
        break;
      }
      case " ": {
        break;
      }
      default: {
        shouldPreventDefault = false;
      }
    }
    if (shouldPreventDefault) {
      event.preventDefault();
    }
  }, [apiRef, props.tabNavigation, getCurrentPageRows, goToHeaderFilter, isRtl, goToHeader, goToCell, getRowIdFromIndex]);
  const handleColumnGroupHeaderKeyDown = React229.useCallback((params, event) => {
    const focusedColumnGroup = gridFocusColumnGroupHeaderSelector(apiRef);
    if (focusedColumnGroup === null) {
      return;
    }
    const {
      field: currentField,
      depth: currentDepth
    } = focusedColumnGroup;
    const {
      fields,
      depth,
      maxDepth
    } = params;
    if (!isNavigationKey(event.key) && event.key !== "Tab") {
      return;
    }
    if (event.key === "Tab" && (props.tabNavigation === "none" || props.tabNavigation === "content" && event.shiftKey)) {
      return;
    }
    const currentPageRows = getCurrentPageRows();
    const viewportPageSize = apiRef.current.getViewportPageSize();
    const currentColIndex = apiRef.current.getColumnIndex(currentField);
    const colIndexBefore = currentField ? apiRef.current.getColumnIndex(currentField) : 0;
    const firstRowIndexInPage = currentPageRows.length > 0 ? 0 : null;
    const lastRowIndexInPage = currentPageRows.length > 0 ? currentPageRows.length - 1 : null;
    const firstColIndex = 0;
    const lastColIndex = Math.max(0, gridVisibleColumnDefinitionsSelector(apiRef).length - 1);
    let shouldPreventDefault = true;
    if (event.key === "Tab" && props.tabNavigation === "content" && !event.shiftKey) {
      if (firstRowIndexInPage !== null) {
        goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
        event.preventDefault();
      }
      return;
    }
    switch (event.key) {
      case "ArrowDown": {
        if (depth === maxDepth - 1) {
          goToHeader(currentColIndex, event);
        } else {
          goToGroupHeader(currentColIndex, currentDepth + 1, event);
        }
        break;
      }
      case "ArrowUp": {
        if (depth > 0) {
          goToGroupHeader(currentColIndex, currentDepth - 1, event);
        }
        break;
      }
      case "ArrowRight": {
        const remainingRightColumns = fields.length - fields.indexOf(currentField) - 1;
        if (currentColIndex + remainingRightColumns + 1 <= lastColIndex) {
          goToGroupHeader(currentColIndex + remainingRightColumns + 1, currentDepth, event);
        }
        break;
      }
      case "ArrowLeft": {
        const remainingLeftColumns = fields.indexOf(currentField);
        if (currentColIndex - remainingLeftColumns - 1 >= firstColIndex) {
          goToGroupHeader(currentColIndex - remainingLeftColumns - 1, currentDepth, event);
        }
        break;
      }
      case "PageDown": {
        if (firstRowIndexInPage !== null && lastRowIndexInPage !== null) {
          goToCell(colIndexBefore, getRowIdFromIndex(Math.min(firstRowIndexInPage + viewportPageSize, lastRowIndexInPage)));
        }
        break;
      }
      case "Home": {
        goToGroupHeader(firstColIndex, currentDepth, event);
        break;
      }
      case "End": {
        goToGroupHeader(lastColIndex, currentDepth, event);
        break;
      }
      case "Tab": {
        if (event.shiftKey) {
          const remainingLeftColumns = fields.indexOf(currentField);
          const targetColIndex = currentColIndex - remainingLeftColumns - 1;
          if (targetColIndex < firstColIndex) {
            if (depth === 0) {
              shouldPreventDefault = false;
            } else {
              goToGroupHeader(lastColIndex, currentDepth - 1, event);
            }
          } else {
            goToGroupHeader(targetColIndex, currentDepth, event);
          }
        } else {
          const remainingRightColumns = fields.length - fields.indexOf(currentField) - 1;
          const targetColIndex = currentColIndex + remainingRightColumns + 1;
          if (targetColIndex > lastColIndex && depth === maxDepth - 1) {
            goToHeader(firstColIndex, event);
          } else if (targetColIndex > lastColIndex) {
            goToGroupHeader(firstColIndex, currentDepth + 1, event);
          } else {
            goToGroupHeader(targetColIndex, currentDepth, event);
          }
        }
        break;
      }
      case " ": {
        break;
      }
      default: {
        shouldPreventDefault = false;
      }
    }
    if (shouldPreventDefault) {
      event.preventDefault();
    }
  }, [apiRef, props.tabNavigation, getCurrentPageRows, goToHeader, goToGroupHeader, goToCell, getRowIdFromIndex]);
  const handleCellKeyDown = React229.useCallback((params, event) => {
    if (isEventTargetInPortal(event)) {
      return;
    }
    const cellParams = apiRef.current.getCellParams(params.id, params.field);
    if (cellParams.cellMode === GridCellModes.Edit || !isNavigationKey(event.key) && event.key !== "Tab") {
      return;
    }
    if (event.key === "Tab" && (props.tabNavigation === "none" || props.tabNavigation === "header" && !event.shiftKey)) {
      return;
    }
    const canUpdateFocus = apiRef.current.unstable_applyPipeProcessors("canUpdateFocus", true, {
      event,
      cell: cellParams
    });
    if (!canUpdateFocus) {
      return;
    }
    const currentPageRows = getCurrentPageRows();
    if (currentPageRows.length === 0) {
      return;
    }
    const previousAreaNavigationFn = headerFilteringEnabled ? goToHeaderFilter : goToHeader;
    const viewportPageSize = apiRef.current.getViewportPageSize();
    const colIndexBefore = params.field ? apiRef.current.getColumnIndex(params.field) : 0;
    const rowIndexBefore = currentPageRows.findIndex((row) => row.id === params.id);
    const firstRowIndexInPage = 0;
    const lastRowIndexInPage = currentPageRows.length - 1;
    const firstColIndex = 0;
    const lastColIndex = Math.max(0, gridVisibleColumnDefinitionsSelector(apiRef).length - 1);
    let shouldPreventDefault = true;
    if (event.key === "Tab" && props.tabNavigation === "header" && event.shiftKey) {
      previousAreaNavigationFn(lastColIndex, event);
      event.preventDefault();
      return;
    }
    switch (event.key) {
      case "ArrowDown": {
        if (rowIndexBefore < lastRowIndexInPage) {
          goToCell(colIndexBefore, getRowIdFromIndex(rowIndexBefore + 1), isRtl ? "right" : "left", "down");
        }
        break;
      }
      case "ArrowUp": {
        if (rowIndexBefore > firstRowIndexInPage) {
          goToCell(colIndexBefore, getRowIdFromIndex(rowIndexBefore - 1));
        } else {
          previousAreaNavigationFn(colIndexBefore, event);
        }
        break;
      }
      case "ArrowRight": {
        const rightColIndex = getRightColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (rightColIndex !== null) {
          goToCell(rightColIndex, getRowIdFromIndex(rowIndexBefore), isRtl ? "left" : "right");
        }
        break;
      }
      case "ArrowLeft": {
        const leftColIndex = getLeftColumnIndex({
          currentColIndex: colIndexBefore,
          firstColIndex,
          lastColIndex,
          isRtl
        });
        if (leftColIndex !== null) {
          goToCell(leftColIndex, getRowIdFromIndex(rowIndexBefore), isRtl ? "right" : "left");
        }
        break;
      }
      case "Tab": {
        if (event.shiftKey) {
          if (colIndexBefore === firstColIndex && rowIndexBefore === firstRowIndexInPage) {
            if (props.tabNavigation === "all") {
              previousAreaNavigationFn(lastColIndex, event);
            } else {
              shouldPreventDefault = false;
            }
          } else if (colIndexBefore === firstColIndex) {
            goToCell(lastColIndex, getRowIdFromIndex(rowIndexBefore - 1));
          } else {
            goToCell(colIndexBefore - 1, getRowIdFromIndex(rowIndexBefore), "left");
          }
        } else if (colIndexBefore === lastColIndex) {
          if (rowIndexBefore !== lastRowIndexInPage) {
            goToCell(firstColIndex, getRowIdFromIndex(rowIndexBefore + 1));
          } else {
            shouldPreventDefault = false;
          }
        } else {
          goToCell(colIndexBefore + 1, getRowIdFromIndex(rowIndexBefore), "right");
        }
        break;
      }
      case " ": {
        const field = params.field;
        if (field === GRID_DETAIL_PANEL_TOGGLE_FIELD) {
          break;
        }
        const colDef = params.colDef;
        if (colDef && (colDef.field === GRID_TREE_DATA_GROUPING_FIELD || isGroupingColumn(colDef.field))) {
          break;
        }
        if (colDef && colDef.type === "longText") {
          break;
        }
        if (!event.shiftKey && rowIndexBefore < lastRowIndexInPage) {
          goToCell(colIndexBefore, getRowIdFromIndex(Math.min(rowIndexBefore + viewportPageSize, lastRowIndexInPage)));
        }
        break;
      }
      case "PageDown": {
        if (rowIndexBefore < lastRowIndexInPage) {
          goToCell(colIndexBefore, getRowIdFromIndex(Math.min(rowIndexBefore + viewportPageSize, lastRowIndexInPage)));
        }
        break;
      }
      case "PageUp": {
        const nextRowIndex = Math.max(rowIndexBefore - viewportPageSize, firstRowIndexInPage);
        if (nextRowIndex !== rowIndexBefore && nextRowIndex >= firstRowIndexInPage) {
          goToCell(colIndexBefore, getRowIdFromIndex(nextRowIndex));
        } else {
          goToHeader(colIndexBefore, event);
        }
        break;
      }
      case "Home": {
        if (event.ctrlKey || event.metaKey || event.shiftKey) {
          goToCell(firstColIndex, getRowIdFromIndex(firstRowIndexInPage));
        } else {
          goToCell(firstColIndex, getRowIdFromIndex(rowIndexBefore));
        }
        break;
      }
      case "End": {
        if (event.ctrlKey || event.metaKey || event.shiftKey) {
          goToCell(lastColIndex, getRowIdFromIndex(lastRowIndexInPage));
        } else {
          goToCell(lastColIndex, getRowIdFromIndex(rowIndexBefore));
        }
        break;
      }
      default: {
        shouldPreventDefault = false;
      }
    }
    if (shouldPreventDefault) {
      event.preventDefault();
    }
  }, [apiRef, props.tabNavigation, getCurrentPageRows, isRtl, goToCell, getRowIdFromIndex, headerFilteringEnabled, goToHeaderFilter, goToHeader]);
  const checkIfCanStartEditing = React229.useCallback((initialValue, {
    event
  }) => {
    if (event.key === " ") {
      return false;
    }
    return initialValue;
  }, []);
  useGridRegisterPipeProcessor(apiRef, "canStartEditing", checkIfCanStartEditing);
  useGridEvent(apiRef, "columnHeaderKeyDown", handleColumnHeaderKeyDown);
  useGridEvent(apiRef, "headerFilterKeyDown", handleHeaderFilterKeyDown);
  useGridEvent(apiRef, "columnGroupHeaderKeyDown", handleColumnGroupHeaderKeyDown);
  useGridEvent(apiRef, "cellKeyDown", handleCellKeyDown);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/useGridRowCount.js
var React230 = __toESM(require_react(), 1);
var useGridRowCount = (apiRef, props) => {
  var _a, _b;
  const logger = useGridLogger(apiRef, "useGridRowCount");
  const previousPageSize = useLazyRef(() => gridPaginationModelSelector(apiRef).pageSize);
  apiRef.current.registerControlState({
    stateId: "paginationRowCount",
    propModel: props.rowCount,
    propOnChange: props.onRowCountChange,
    stateSelector: gridPaginationRowCountSelector,
    changeEvent: "rowCountChange"
  });
  const setRowCount = React230.useCallback((newRowCount) => {
    const rowCountState = gridPaginationRowCountSelector(apiRef);
    if (rowCountState === newRowCount) {
      return;
    }
    logger.debug("Setting 'rowCount' to", newRowCount);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        rowCount: newRowCount
      })
    }));
  }, [apiRef, logger]);
  const paginationRowCountApi = {
    setRowCount
  };
  useGridApiMethod(apiRef, paginationRowCountApi, "public");
  const stateExportPreProcessing = React230.useCallback((prevState, context) => {
    var _a2, _b2;
    const exportedRowCount = gridPaginationRowCountSelector(apiRef);
    const shouldExportRowCount = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the `rowCount` is controlled
      props.rowCount != null || // Always export if the `rowCount` has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.pagination) == null ? void 0 : _b2.rowCount) != null
    );
    if (!shouldExportRowCount) {
      return prevState;
    }
    return _extends({}, prevState, {
      pagination: _extends({}, prevState.pagination, {
        rowCount: exportedRowCount
      })
    });
  }, [apiRef, props.rowCount, (_b = (_a = props.initialState) == null ? void 0 : _a.pagination) == null ? void 0 : _b.rowCount]);
  const stateRestorePreProcessing = React230.useCallback((params, context) => {
    var _a2;
    const restoredRowCount = ((_a2 = context.stateToRestore.pagination) == null ? void 0 : _a2.rowCount) ? context.stateToRestore.pagination.rowCount : gridPaginationRowCountSelector(apiRef);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        rowCount: restoredRowCount
      })
    }));
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  const handlePaginationModelChange = React230.useCallback((model) => {
    if (props.paginationMode === "client" || !previousPageSize.current) {
      return;
    }
    if (model.pageSize !== previousPageSize.current) {
      previousPageSize.current = model.pageSize;
      const rowCountState = gridPaginationRowCountSelector(apiRef);
      if (rowCountState === -1) {
        apiRef.current.setPage(0);
      }
    }
  }, [props.paginationMode, previousPageSize, apiRef]);
  useGridEvent(apiRef, "paginationModelChange", handlePaginationModelChange);
  React230.useEffect(() => {
    if (props.paginationMode === "server" && props.rowCount != null) {
      apiRef.current.setRowCount(props.rowCount);
    }
  }, [apiRef, props.paginationMode, props.rowCount]);
  useStoreEffect(
    // typings not supported currently, but methods work
    apiRef.current.store,
    () => {
      const isLastPage = gridPaginationMetaSelector(apiRef).hasNextPage === false;
      if (isLastPage) {
        return true;
      }
      if (props.paginationMode === "client") {
        return gridFilteredTopLevelRowCountSelector(apiRef);
      }
      return void 0;
    },
    (_, isLastPageOrRowCount) => {
      if (isLastPageOrRowCount === true && gridPaginationRowCountSelector(apiRef) !== -1) {
        const visibleTopLevelRowCount = gridFilteredTopLevelRowCountSelector(apiRef);
        const paginationModel = gridPaginationModelSelector(apiRef);
        apiRef.current.setRowCount(paginationModel.pageSize * paginationModel.page + visibleTopLevelRowCount);
      } else if (typeof isLastPageOrRowCount === "number") {
        apiRef.current.setRowCount(isLastPageOrRowCount);
      }
    }
  );
  React230.useEffect(() => {
    if (props.paginationMode === "client") {
      apiRef.current.setRowCount(gridFilteredTopLevelRowCountSelector(apiRef));
    }
  }, [apiRef, props.paginationMode]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/useGridPaginationMeta.js
var React231 = __toESM(require_react(), 1);
var useGridPaginationMeta = (apiRef, props) => {
  var _a, _b;
  const logger = useGridLogger(apiRef, "useGridPaginationMeta");
  apiRef.current.registerControlState({
    stateId: "paginationMeta",
    propModel: props.paginationMeta,
    propOnChange: props.onPaginationMetaChange,
    stateSelector: gridPaginationMetaSelector,
    changeEvent: "paginationMetaChange"
  });
  const setPaginationMeta = React231.useCallback((newPaginationMeta) => {
    const paginationMeta = gridPaginationMetaSelector(apiRef);
    if (paginationMeta === newPaginationMeta) {
      return;
    }
    logger.debug("Setting 'paginationMeta' to", newPaginationMeta);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        meta: newPaginationMeta
      })
    }));
  }, [apiRef, logger]);
  const paginationMetaApi = {
    setPaginationMeta
  };
  useGridApiMethod(apiRef, paginationMetaApi, "public");
  const stateExportPreProcessing = React231.useCallback((prevState, context) => {
    var _a2, _b2;
    const exportedPaginationMeta = gridPaginationMetaSelector(apiRef);
    const shouldExportRowCount = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the `paginationMeta` is controlled
      props.paginationMeta != null || // Always export if the `paginationMeta` has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.pagination) == null ? void 0 : _b2.meta) != null
    );
    if (!shouldExportRowCount) {
      return prevState;
    }
    return _extends({}, prevState, {
      pagination: _extends({}, prevState.pagination, {
        meta: exportedPaginationMeta
      })
    });
  }, [apiRef, props.paginationMeta, (_b = (_a = props.initialState) == null ? void 0 : _a.pagination) == null ? void 0 : _b.meta]);
  const stateRestorePreProcessing = React231.useCallback((params, context) => {
    var _a2;
    const restoredPaginationMeta = ((_a2 = context.stateToRestore.pagination) == null ? void 0 : _a2.meta) ? context.stateToRestore.pagination.meta : gridPaginationMetaSelector(apiRef);
    apiRef.current.setState((state) => _extends({}, state, {
      pagination: _extends({}, state.pagination, {
        meta: restoredPaginationMeta
      })
    }));
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  React231.useEffect(() => {
    if (props.paginationMeta) {
      apiRef.current.setPaginationMeta(props.paginationMeta);
    }
  }, [apiRef, props.paginationMeta]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/pagination/useGridPagination.js
var paginationStateInitializer = (state, props) => {
  var _a, _b, _c, _d, _e, _f, _g;
  const paginationModel = _extends({}, getDefaultGridPaginationModel(props.autoPageSize), props.paginationModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.pagination) == null ? void 0 : _b.paginationModel));
  throwIfPageSizeExceedsTheLimit(paginationModel.pageSize, props.signature);
  const rowCount = props.rowCount ?? ((_d = (_c = props.initialState) == null ? void 0 : _c.pagination) == null ? void 0 : _d.rowCount) ?? (props.paginationMode === "client" ? (_e = state.rows) == null ? void 0 : _e.totalRowCount : void 0);
  const meta = props.paginationMeta ?? ((_g = (_f = props.initialState) == null ? void 0 : _f.pagination) == null ? void 0 : _g.meta) ?? {};
  return _extends({}, state, {
    pagination: _extends({}, state.pagination, {
      paginationModel,
      rowCount,
      meta,
      enabled: props.pagination === true,
      paginationMode: props.paginationMode
    })
  });
};
var useGridPagination = (apiRef, props) => {
  useGridPaginationMeta(apiRef, props);
  useGridPaginationModel(apiRef, props);
  useGridRowCount(apiRef, props);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/preferencesPanel/useGridPreferencesPanel.js
var React232 = __toESM(require_react(), 1);
var preferencePanelStateInitializer = (state, props) => {
  var _a;
  return _extends({}, state, {
    preferencePanel: ((_a = props.initialState) == null ? void 0 : _a.preferencePanel) ?? {
      open: false
    }
  });
};
var useGridPreferencesPanel = (apiRef, props) => {
  var _a;
  const logger = useGridLogger(apiRef, "useGridPreferencesPanel");
  const hidePreferences = React232.useCallback(() => {
    apiRef.current.setState((state) => {
      if (!state.preferencePanel.open) {
        return state;
      }
      logger.debug("Hiding Preferences Panel");
      const preferencePanelState = gridPreferencePanelStateSelector(apiRef);
      apiRef.current.publishEvent("preferencePanelClose", {
        openedPanelValue: preferencePanelState.openedPanelValue
      });
      return _extends({}, state, {
        preferencePanel: {
          open: false
        }
      });
    });
  }, [apiRef, logger]);
  const showPreferences = React232.useCallback((newValue, panelId, labelId) => {
    logger.debug("Opening Preferences Panel");
    apiRef.current.setState((state) => _extends({}, state, {
      preferencePanel: _extends({}, state.preferencePanel, {
        open: true,
        openedPanelValue: newValue,
        panelId,
        labelId
      })
    }));
    apiRef.current.publishEvent("preferencePanelOpen", {
      openedPanelValue: newValue
    });
  }, [logger, apiRef]);
  useGridApiMethod(apiRef, {
    showPreferences,
    hidePreferences
  }, "public");
  const stateExportPreProcessing = React232.useCallback((prevState, context) => {
    var _a2;
    const preferencePanelToExport = gridPreferencePanelStateSelector(apiRef);
    const shouldExportPreferencePanel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the panel was initialized
      ((_a2 = props.initialState) == null ? void 0 : _a2.preferencePanel) != null || // Always export if the panel is opened
      preferencePanelToExport.open
    );
    if (!shouldExportPreferencePanel) {
      return prevState;
    }
    return _extends({}, prevState, {
      preferencePanel: preferencePanelToExport
    });
  }, [apiRef, (_a = props.initialState) == null ? void 0 : _a.preferencePanel]);
  const stateRestorePreProcessing = React232.useCallback((params, context) => {
    const preferencePanel = context.stateToRestore.preferencePanel;
    if (preferencePanel != null) {
      apiRef.current.setState((state) => _extends({}, state, {
        preferencePanel
      }));
    }
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridEditing.js
var React235 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridCellEditing.js
var React233 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/utils.js
var getDefaultCellValue = (colDef) => {
  switch (colDef.type) {
    case "boolean":
      return false;
    case "date":
    case "dateTime":
    case "number":
      return void 0;
    case "singleSelect":
      return null;
    case "string":
    default:
      return "";
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridCellEditing.js
var _excluded64 = ["id", "field"];
var _excluded216 = ["id", "field"];
var useGridCellEditing = (apiRef, props) => {
  const [cellModesModel, setCellModesModel] = React233.useState({});
  const cellModesModelRef = React233.useRef(cellModesModel);
  const prevCellModesModel = React233.useRef({});
  const {
    processRowUpdate,
    onProcessRowUpdateError,
    cellModesModel: cellModesModelProp,
    onCellModesModelChange
  } = props;
  const runIfEditModeIsCell = (callback) => (...args) => {
    if (props.editMode === GridEditModes.Cell) {
      callback(...args);
    }
  };
  const throwIfNotEditable = React233.useCallback((id, field) => {
    const params = apiRef.current.getCellParams(id, field);
    if (!apiRef.current.isCellEditable(params)) {
      throw new Error(`MUI X: The cell with id=${id} and field=${field} is not editable.`);
    }
  }, [apiRef]);
  const throwIfNotInMode = React233.useCallback((id, field, mode) => {
    if (apiRef.current.getCellMode(id, field) !== mode) {
      throw new Error(`MUI X: The cell with id=${id} and field=${field} is not in ${mode} mode.`);
    }
  }, [apiRef]);
  const handleCellDoubleClick = React233.useCallback((params, event) => {
    if (!params.isEditable) {
      return;
    }
    if (params.cellMode === GridCellModes.Edit) {
      return;
    }
    const newParams = _extends({}, params, {
      reason: GridCellEditStartReasons.cellDoubleClick
    });
    apiRef.current.publishEvent("cellEditStart", newParams, event);
  }, [apiRef]);
  const handleCellFocusOut = React233.useCallback((params, event) => {
    if (params.cellMode === GridCellModes.View) {
      return;
    }
    if (apiRef.current.getCellMode(params.id, params.field) === GridCellModes.View) {
      return;
    }
    const newParams = _extends({}, params, {
      reason: GridCellEditStopReasons.cellFocusOut
    });
    apiRef.current.publishEvent("cellEditStop", newParams, event);
  }, [apiRef]);
  const handleCellKeyDown = React233.useCallback((params, event) => {
    if (params.cellMode === GridCellModes.Edit) {
      if (event.which === 229) {
        return;
      }
      let reason;
      if (event.key === "Escape") {
        reason = GridCellEditStopReasons.escapeKeyDown;
      } else if (event.key === "Enter") {
        reason = GridCellEditStopReasons.enterKeyDown;
      } else if (event.key === "Tab") {
        reason = event.shiftKey ? GridCellEditStopReasons.shiftTabKeyDown : GridCellEditStopReasons.tabKeyDown;
        event.preventDefault();
      }
      if (reason) {
        const newParams = _extends({}, params, {
          reason
        });
        apiRef.current.publishEvent("cellEditStop", newParams, event);
      }
    } else if (params.isEditable) {
      let reason;
      const canStartEditing = apiRef.current.unstable_applyPipeProcessors("canStartEditing", true, {
        event,
        cellParams: params,
        editMode: "cell"
      });
      if (!canStartEditing) {
        return;
      }
      if (isPrintableKey(event)) {
        reason = GridCellEditStartReasons.printableKeyDown;
      } else if (isPasteShortcut(event)) {
        reason = GridCellEditStartReasons.pasteKeyDown;
      } else if (event.key === "Enter") {
        reason = GridCellEditStartReasons.enterKeyDown;
        event.preventDefault();
      } else if (event.key === "Backspace" || event.key === "Delete") {
        reason = GridCellEditStartReasons.deleteKeyDown;
      }
      if (reason) {
        const newParams = _extends({}, params, {
          reason,
          key: event.key
        });
        apiRef.current.publishEvent("cellEditStart", newParams, event);
      }
    }
  }, [apiRef]);
  const handleCellEditStart = React233.useCallback((params) => {
    const {
      id,
      field,
      reason
    } = params;
    const startCellEditModeParams = {
      id,
      field
    };
    if (reason === GridCellEditStartReasons.printableKeyDown || reason === GridCellEditStartReasons.deleteKeyDown || reason === GridCellEditStartReasons.pasteKeyDown) {
      startCellEditModeParams.deleteValue = true;
    }
    apiRef.current.startCellEditMode(startCellEditModeParams);
  }, [apiRef]);
  const handleCellEditStop = React233.useCallback((params) => {
    const {
      id,
      field,
      reason
    } = params;
    apiRef.current.runPendingEditCellValueMutation(id, field);
    let cellToFocusAfter;
    if (reason === GridCellEditStopReasons.enterKeyDown) {
      cellToFocusAfter = "below";
    } else if (reason === GridCellEditStopReasons.tabKeyDown) {
      cellToFocusAfter = "right";
    } else if (reason === GridCellEditStopReasons.shiftTabKeyDown) {
      cellToFocusAfter = "left";
    }
    const ignoreModifications = reason === "escapeKeyDown";
    apiRef.current.stopCellEditMode({
      id,
      field,
      ignoreModifications,
      cellToFocusAfter
    });
  }, [apiRef]);
  const runIfNoFieldErrors = (callback) => async (...args) => {
    var _a;
    if (callback) {
      const {
        id,
        field
      } = args[0];
      const editRowsState = apiRef.current.state.editRows;
      const hasFieldErrors = (_a = editRowsState[id][field]) == null ? void 0 : _a.error;
      if (!hasFieldErrors) {
        callback(...args);
      }
    }
  };
  useGridEvent(apiRef, "cellDoubleClick", runIfEditModeIsCell(handleCellDoubleClick));
  useGridEvent(apiRef, "cellFocusOut", runIfEditModeIsCell(handleCellFocusOut));
  useGridEvent(apiRef, "cellKeyDown", runIfEditModeIsCell(handleCellKeyDown));
  useGridEvent(apiRef, "cellEditStart", runIfEditModeIsCell(handleCellEditStart));
  useGridEvent(apiRef, "cellEditStop", runIfEditModeIsCell(handleCellEditStop));
  useGridEventPriority(apiRef, "cellEditStart", props.onCellEditStart);
  useGridEventPriority(apiRef, "cellEditStop", runIfNoFieldErrors(props.onCellEditStop));
  const getCellMode = React233.useCallback((id, field) => {
    const editingState = gridEditRowsStateSelector(apiRef);
    const isEditing = editingState[id] && editingState[id][field];
    return isEditing ? GridCellModes.Edit : GridCellModes.View;
  }, [apiRef]);
  const updateCellModesModel = useEventCallback_default((newModel) => {
    const isNewModelDifferentFromProp = newModel !== props.cellModesModel;
    if (onCellModesModelChange && isNewModelDifferentFromProp) {
      onCellModesModelChange(newModel, {
        api: apiRef.current
      });
    }
    if (props.cellModesModel && isNewModelDifferentFromProp) {
      return;
    }
    setCellModesModel(newModel);
    cellModesModelRef.current = newModel;
    apiRef.current.publishEvent("cellModesModelChange", newModel);
  });
  const updateFieldInCellModesModel = React233.useCallback((id, field, newProps) => {
    const newModel = _extends({}, cellModesModelRef.current);
    if (newProps !== null) {
      newModel[id] = _extends({}, newModel[id], {
        [field]: _extends({}, newProps)
      });
    } else {
      const _newModel$id = newModel[id], otherFields = _objectWithoutPropertiesLoose(_newModel$id, [field].map(toPropertyKey));
      newModel[id] = otherFields;
      if (Object.keys(newModel[id]).length === 0) {
        delete newModel[id];
      }
    }
    updateCellModesModel(newModel);
  }, [updateCellModesModel]);
  const updateOrDeleteFieldState = React233.useCallback((id, field, newProps) => {
    apiRef.current.setState((state) => {
      const newEditingState = _extends({}, state.editRows);
      if (newProps !== null) {
        newEditingState[id] = _extends({}, newEditingState[id], {
          [field]: _extends({}, newProps)
        });
      } else {
        delete newEditingState[id][field];
        if (Object.keys(newEditingState[id]).length === 0) {
          delete newEditingState[id];
        }
      }
      return _extends({}, state, {
        editRows: newEditingState
      });
    });
  }, [apiRef]);
  const startCellEditMode = React233.useCallback((params) => {
    const {
      id,
      field
    } = params, other = _objectWithoutPropertiesLoose(params, _excluded64);
    throwIfNotEditable(id, field);
    throwIfNotInMode(id, field, GridCellModes.View);
    updateFieldInCellModesModel(id, field, _extends({
      mode: GridCellModes.Edit
    }, other));
  }, [throwIfNotEditable, throwIfNotInMode, updateFieldInCellModesModel]);
  const updateStateToStartCellEditMode = useEventCallback_default(async (params) => {
    const {
      id,
      field,
      deleteValue,
      initialValue
    } = params;
    const value = apiRef.current.getCellValue(id, field);
    let newValue = value;
    if (deleteValue) {
      newValue = getDefaultCellValue(apiRef.current.getColumn(field));
    } else if (initialValue) {
      newValue = initialValue;
    }
    const column = apiRef.current.getColumn(field);
    const shouldProcessEditCellProps = !!column.preProcessEditCellProps && deleteValue;
    let newProps = {
      value: newValue,
      error: false,
      isProcessingProps: shouldProcessEditCellProps
    };
    updateOrDeleteFieldState(id, field, newProps);
    apiRef.current.setCellFocus(id, field);
    if (shouldProcessEditCellProps) {
      newProps = await Promise.resolve(column.preProcessEditCellProps({
        id,
        row: apiRef.current.getRow(id),
        props: newProps,
        hasChanged: newValue !== value
      }));
      if (apiRef.current.getCellMode(id, field) === GridCellModes.Edit) {
        const editingState = gridEditRowsStateSelector(apiRef);
        updateOrDeleteFieldState(id, field, _extends({}, newProps, {
          value: editingState[id][field].value,
          isProcessingProps: false
        }));
      }
    }
  });
  const stopCellEditMode = React233.useCallback((params) => {
    const {
      id,
      field
    } = params, other = _objectWithoutPropertiesLoose(params, _excluded216);
    throwIfNotInMode(id, field, GridCellModes.Edit);
    updateFieldInCellModesModel(id, field, _extends({
      mode: GridCellModes.View
    }, other));
  }, [throwIfNotInMode, updateFieldInCellModesModel]);
  const updateStateToStopCellEditMode = useEventCallback_default(async (params) => {
    var _a;
    const {
      id,
      field,
      ignoreModifications,
      cellToFocusAfter = "none"
    } = params;
    throwIfNotInMode(id, field, GridCellModes.Edit);
    apiRef.current.runPendingEditCellValueMutation(id, field);
    const finishCellEditMode = () => {
      updateOrDeleteFieldState(id, field, null);
      updateFieldInCellModesModel(id, field, null);
      if (cellToFocusAfter !== "none") {
        apiRef.current.moveFocusToRelativeCell(id, field, cellToFocusAfter);
      }
    };
    if (ignoreModifications) {
      finishCellEditMode();
      return;
    }
    const editingState = gridEditRowsStateSelector(apiRef);
    const {
      error,
      isProcessingProps
    } = editingState[id][field];
    const row = apiRef.current.getRow(id);
    if (error || isProcessingProps) {
      prevCellModesModel.current[id][field].mode = GridCellModes.Edit;
      updateFieldInCellModesModel(id, field, {
        mode: GridCellModes.Edit
      });
      return;
    }
    const rowUpdate = apiRef.current.getRowWithUpdatedValuesFromCellEditing(id, field);
    if ((_a = props.dataSource) == null ? void 0 : _a.updateRow) {
      if (isDeepEqual(row, rowUpdate)) {
        finishCellEditMode();
        return;
      }
      const handleError = () => {
        prevCellModesModel.current[id][field].mode = GridCellModes.Edit;
        updateFieldInCellModesModel(id, field, {
          mode: GridCellModes.Edit
        });
      };
      const updateRowParams = {
        rowId: id,
        updatedRow: rowUpdate,
        previousRow: row
      };
      try {
        await apiRef.current.dataSource.editRow(updateRowParams);
        finishCellEditMode();
      } catch {
        handleError();
      }
    } else if (processRowUpdate) {
      const handleError = (errorThrown) => {
        prevCellModesModel.current[id][field].mode = GridCellModes.Edit;
        updateFieldInCellModesModel(id, field, {
          mode: GridCellModes.Edit
        });
        if (onProcessRowUpdateError) {
          onProcessRowUpdateError(errorThrown);
        } else {
          warnOnce(["MUI X: A call to `processRowUpdate()` threw an error which was not handled because `onProcessRowUpdateError()` is missing.", "To handle the error pass a callback to the `onProcessRowUpdateError()` prop, for example `<DataGrid onProcessRowUpdateError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/editing/persistence/."], "error");
        }
      };
      try {
        Promise.resolve(processRowUpdate(rowUpdate, row, {
          rowId: id
        })).then((finalRowUpdate) => {
          apiRef.current.updateRows([finalRowUpdate]);
          finishCellEditMode();
        }).catch(handleError);
      } catch (errorThrown) {
        handleError(errorThrown);
      }
    } else {
      apiRef.current.updateRows([rowUpdate]);
      finishCellEditMode();
    }
  });
  const setCellEditingEditCellValue = React233.useCallback(async (params) => {
    var _a, _b;
    const {
      id,
      field,
      value,
      debounceMs,
      unstable_skipValueParser: skipValueParser
    } = params;
    throwIfNotEditable(id, field);
    throwIfNotInMode(id, field, GridCellModes.Edit);
    const column = apiRef.current.getColumn(field);
    const row = apiRef.current.getRow(id);
    let parsedValue = value;
    if (column.valueParser && !skipValueParser) {
      parsedValue = column.valueParser(value, row, column, apiRef);
    }
    let editingState = gridEditRowsStateSelector(apiRef);
    let newProps = _extends({}, editingState[id][field], {
      value: parsedValue,
      changeReason: debounceMs ? "debouncedSetEditCellValue" : "setEditCellValue"
    });
    if (column.preProcessEditCellProps) {
      const hasChanged = value !== editingState[id][field].value;
      newProps = _extends({}, newProps, {
        isProcessingProps: true
      });
      updateOrDeleteFieldState(id, field, newProps);
      newProps = await Promise.resolve(column.preProcessEditCellProps({
        id,
        row,
        props: newProps,
        hasChanged
      }));
    }
    if (apiRef.current.getCellMode(id, field) === GridCellModes.View) {
      return false;
    }
    editingState = gridEditRowsStateSelector(apiRef);
    newProps = _extends({}, newProps, {
      isProcessingProps: false
    });
    newProps.value = column.preProcessEditCellProps ? editingState[id][field].value : parsedValue;
    updateOrDeleteFieldState(id, field, newProps);
    editingState = gridEditRowsStateSelector(apiRef);
    return !((_b = (_a = editingState[id]) == null ? void 0 : _a[field]) == null ? void 0 : _b.error);
  }, [apiRef, throwIfNotEditable, throwIfNotInMode, updateOrDeleteFieldState]);
  const getRowWithUpdatedValuesFromCellEditing = React233.useCallback((id, field) => {
    const column = apiRef.current.getColumn(field);
    const editingState = gridEditRowsStateSelector(apiRef);
    const row = apiRef.current.getRow(id);
    if (!editingState[id] || !editingState[id][field]) {
      return apiRef.current.getRow(id);
    }
    const {
      value
    } = editingState[id][field];
    return column.valueSetter ? column.valueSetter(value, row, column, apiRef) : _extends({}, row, {
      [field]: value
    });
  }, [apiRef]);
  const editingApi = {
    getCellMode,
    startCellEditMode,
    stopCellEditMode
  };
  const editingPrivateApi = {
    setCellEditingEditCellValue,
    getRowWithUpdatedValuesFromCellEditing
  };
  useGridApiMethod(apiRef, editingApi, "public");
  useGridApiMethod(apiRef, editingPrivateApi, "private");
  React233.useEffect(() => {
    if (cellModesModelProp) {
      updateCellModesModel(cellModesModelProp);
    }
  }, [cellModesModelProp, updateCellModesModel]);
  useEnhancedEffect_default(() => {
    const rowsLookup = gridRowsLookupSelector(apiRef);
    const copyOfPrevCellModes = prevCellModesModel.current;
    prevCellModesModel.current = deepClone(cellModesModel);
    Object.entries(cellModesModel).forEach(([id, fields]) => {
      Object.entries(fields).forEach(([field, params]) => {
        var _a, _b;
        const prevMode = ((_b = (_a = copyOfPrevCellModes[id]) == null ? void 0 : _a[field]) == null ? void 0 : _b.mode) || GridCellModes.View;
        const originalId = rowsLookup[id] ? apiRef.current.getRowId(rowsLookup[id]) : id;
        if (params.mode === GridCellModes.Edit && prevMode === GridCellModes.View) {
          updateStateToStartCellEditMode(_extends({
            id: originalId,
            field
          }, params));
        } else if (params.mode === GridCellModes.View && prevMode === GridCellModes.Edit) {
          updateStateToStopCellEditMode(_extends({
            id: originalId,
            field
          }, params));
        }
      });
    });
  }, [apiRef, cellModesModel, updateStateToStartCellEditMode, updateStateToStopCellEditMode]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridRowEditing.js
var React234 = __toESM(require_react(), 1);
var _excluded65 = ["id"];
var _excluded217 = ["id"];
var useGridRowEditing = (apiRef, props) => {
  const [rowModesModel, setRowModesModel] = React234.useState({});
  const rowModesModelRef = React234.useRef(rowModesModel);
  const prevRowModesModel = React234.useRef({});
  const prevRowValuesLookup = React234.useRef({});
  const focusTimeout = React234.useRef(void 0);
  const nextFocusedCell = React234.useRef(null);
  const {
    processRowUpdate,
    onProcessRowUpdateError,
    rowModesModel: rowModesModelProp,
    onRowModesModelChange
  } = props;
  const runIfEditModeIsRow = (callback) => (...args) => {
    if (props.editMode === GridEditModes.Row) {
      callback(...args);
    }
  };
  const throwIfNotEditable = React234.useCallback((id, field) => {
    const params = apiRef.current.getCellParams(id, field);
    if (!apiRef.current.isCellEditable(params)) {
      throw new Error(`MUI X: The cell with id=${id} and field=${field} is not editable.`);
    }
  }, [apiRef]);
  const throwIfNotInMode = React234.useCallback((id, mode) => {
    if (apiRef.current.getRowMode(id) !== mode) {
      throw new Error(`MUI X: The row with id=${id} is not in ${mode} mode.`);
    }
  }, [apiRef]);
  const hasFieldsWithErrors = React234.useCallback((rowId) => {
    const editingState = gridEditRowsStateSelector(apiRef);
    return Object.values(editingState[rowId]).some((fieldProps) => fieldProps.error);
  }, [apiRef]);
  const handleCellDoubleClick = React234.useCallback((params, event) => {
    if (!params.isEditable) {
      return;
    }
    if (apiRef.current.getRowMode(params.id) === GridRowModes.Edit) {
      return;
    }
    const rowParams = apiRef.current.getRowParams(params.id);
    const newParams = _extends({}, rowParams, {
      field: params.field,
      reason: GridRowEditStartReasons.cellDoubleClick
    });
    apiRef.current.publishEvent("rowEditStart", newParams, event);
  }, [apiRef]);
  const handleCellFocusIn = React234.useCallback((params) => {
    nextFocusedCell.current = params;
  }, []);
  const handleCellFocusOut = React234.useCallback((params, event) => {
    if (!params.isEditable) {
      return;
    }
    if (apiRef.current.getRowMode(params.id) === GridRowModes.View) {
      return;
    }
    nextFocusedCell.current = null;
    focusTimeout.current = setTimeout(() => {
      var _a;
      if (((_a = nextFocusedCell.current) == null ? void 0 : _a.id) !== params.id) {
        if (!apiRef.current.getRow(params.id)) {
          return;
        }
        if (apiRef.current.getRowMode(params.id) === GridRowModes.View) {
          return;
        }
        if (hasFieldsWithErrors(params.id)) {
          return;
        }
        const rowParams = apiRef.current.getRowParams(params.id);
        const newParams = _extends({}, rowParams, {
          field: params.field,
          reason: GridRowEditStopReasons.rowFocusOut
        });
        apiRef.current.publishEvent("rowEditStop", newParams, event);
      }
    });
  }, [apiRef, hasFieldsWithErrors]);
  React234.useEffect(() => {
    return () => {
      clearTimeout(focusTimeout.current);
    };
  }, []);
  const handleCellKeyDown = React234.useCallback((params, event) => {
    if (params.cellMode === GridRowModes.Edit) {
      if (event.which === 229) {
        return;
      }
      let reason;
      if (event.key === "Escape") {
        reason = GridRowEditStopReasons.escapeKeyDown;
      } else if (event.key === "Enter") {
        reason = GridRowEditStopReasons.enterKeyDown;
      } else if (event.key === "Tab") {
        const columnFields = gridVisibleColumnFieldsSelector(apiRef).filter((field) => {
          const column = apiRef.current.getColumn(field);
          if (column.type === GRID_ACTIONS_COLUMN_TYPE) {
            return true;
          }
          return apiRef.current.isCellEditable(apiRef.current.getCellParams(params.id, field));
        });
        if (event.shiftKey) {
          if (params.field === columnFields[0]) {
            reason = GridRowEditStopReasons.shiftTabKeyDown;
          }
        } else if (params.field === columnFields[columnFields.length - 1]) {
          reason = GridRowEditStopReasons.tabKeyDown;
        }
        event.preventDefault();
        if (!reason) {
          const index = columnFields.findIndex((field) => field === params.field);
          const nextFieldToFocus = columnFields[event.shiftKey ? index - 1 : index + 1];
          apiRef.current.setCellFocus(params.id, nextFieldToFocus);
        }
      }
      if (reason) {
        if (reason !== GridRowEditStopReasons.escapeKeyDown && hasFieldsWithErrors(params.id)) {
          return;
        }
        const newParams = _extends({}, apiRef.current.getRowParams(params.id), {
          reason,
          field: params.field
        });
        apiRef.current.publishEvent("rowEditStop", newParams, event);
      }
    } else if (params.isEditable) {
      let reason;
      const canStartEditing = apiRef.current.unstable_applyPipeProcessors("canStartEditing", true, {
        event,
        cellParams: params,
        editMode: "row"
      });
      if (!canStartEditing) {
        return;
      }
      if (isPrintableKey(event)) {
        reason = GridRowEditStartReasons.printableKeyDown;
      } else if (isPasteShortcut(event)) {
        reason = GridRowEditStartReasons.printableKeyDown;
      } else if (event.key === "Enter") {
        reason = GridRowEditStartReasons.enterKeyDown;
      } else if (event.key === "Backspace" || event.key === "Delete") {
        reason = GridRowEditStartReasons.deleteKeyDown;
      }
      if (reason) {
        const rowParams = apiRef.current.getRowParams(params.id);
        const newParams = _extends({}, rowParams, {
          field: params.field,
          reason,
          // Only pass the pressed key when the row editing is controlled via `rowModesModel`.
          // In uncontrolled mode, the default editor already inserts the character and passing it here would duplicate it.
          key: rowModesModelProp && isPrintableKey(event) ? event.key : void 0
        });
        apiRef.current.publishEvent("rowEditStart", newParams, event);
      }
    }
  }, [apiRef, hasFieldsWithErrors, rowModesModelProp]);
  const handleRowEditStart = React234.useCallback((params) => {
    const {
      id,
      field,
      reason
    } = params;
    const startRowEditModeParams = {
      id,
      fieldToFocus: field
    };
    if (reason === GridRowEditStartReasons.printableKeyDown || reason === GridRowEditStartReasons.deleteKeyDown) {
      if (rowModesModelProp && reason === GridRowEditStartReasons.printableKeyDown && params.key && field) {
        startRowEditModeParams.initialValue = params.key;
      } else {
        startRowEditModeParams.deleteValue = !!field;
      }
    }
    apiRef.current.startRowEditMode(startRowEditModeParams);
  }, [apiRef, rowModesModelProp]);
  const handleRowEditStop = React234.useCallback((params) => {
    const {
      id,
      reason,
      field
    } = params;
    apiRef.current.runPendingEditCellValueMutation(id);
    let cellToFocusAfter;
    if (reason === GridRowEditStopReasons.enterKeyDown) {
      cellToFocusAfter = "below";
    } else if (reason === GridRowEditStopReasons.tabKeyDown) {
      cellToFocusAfter = "right";
    } else if (reason === GridRowEditStopReasons.shiftTabKeyDown) {
      cellToFocusAfter = "left";
    }
    const ignoreModifications = reason === "escapeKeyDown";
    apiRef.current.stopRowEditMode({
      id,
      ignoreModifications,
      field,
      cellToFocusAfter
    });
  }, [apiRef]);
  useGridEvent(apiRef, "cellDoubleClick", runIfEditModeIsRow(handleCellDoubleClick));
  useGridEvent(apiRef, "cellFocusIn", runIfEditModeIsRow(handleCellFocusIn));
  useGridEvent(apiRef, "cellFocusOut", runIfEditModeIsRow(handleCellFocusOut));
  useGridEvent(apiRef, "cellKeyDown", runIfEditModeIsRow(handleCellKeyDown));
  useGridEvent(apiRef, "rowEditStart", runIfEditModeIsRow(handleRowEditStart));
  useGridEvent(apiRef, "rowEditStop", runIfEditModeIsRow(handleRowEditStop));
  useGridEventPriority(apiRef, "rowEditStart", props.onRowEditStart);
  useGridEventPriority(apiRef, "rowEditStop", props.onRowEditStop);
  const getRowMode = React234.useCallback((id) => {
    const isEditing = gridRowIsEditingSelector(apiRef, {
      rowId: id,
      editMode: props.editMode
    });
    return isEditing ? GridRowModes.Edit : GridRowModes.View;
  }, [apiRef, props.editMode]);
  const updateRowModesModel = useEventCallback_default((newModel) => {
    const isNewModelDifferentFromProp = newModel !== props.rowModesModel;
    if (onRowModesModelChange && isNewModelDifferentFromProp) {
      onRowModesModelChange(newModel, {
        api: apiRef.current
      });
    }
    if (props.rowModesModel && isNewModelDifferentFromProp) {
      return;
    }
    setRowModesModel(newModel);
    rowModesModelRef.current = newModel;
    apiRef.current.publishEvent("rowModesModelChange", newModel);
  });
  const updateRowInRowModesModel = React234.useCallback((id, newProps) => {
    const newModel = _extends({}, rowModesModelRef.current);
    if (newProps !== null) {
      newModel[id] = _extends({}, newProps);
    } else {
      delete newModel[id];
    }
    updateRowModesModel(newModel);
  }, [updateRowModesModel]);
  const updateOrDeleteRowState = React234.useCallback((id, newProps) => {
    apiRef.current.setState((state) => {
      const newEditingState = _extends({}, state.editRows);
      if (newProps !== null) {
        newEditingState[id] = newProps;
      } else {
        delete newEditingState[id];
      }
      return _extends({}, state, {
        editRows: newEditingState
      });
    });
  }, [apiRef]);
  const updateOrDeleteFieldState = React234.useCallback((id, field, newProps) => {
    apiRef.current.setState((state) => {
      const newEditingState = _extends({}, state.editRows);
      if (newProps !== null) {
        newEditingState[id] = _extends({}, newEditingState[id], {
          [field]: _extends({}, newProps)
        });
      } else {
        delete newEditingState[id][field];
        if (Object.keys(newEditingState[id]).length === 0) {
          delete newEditingState[id];
        }
      }
      return _extends({}, state, {
        editRows: newEditingState
      });
    });
  }, [apiRef]);
  const startRowEditMode = React234.useCallback((params) => {
    const {
      id
    } = params, other = _objectWithoutPropertiesLoose(params, _excluded65);
    throwIfNotInMode(id, GridRowModes.View);
    updateRowInRowModesModel(id, _extends({
      mode: GridRowModes.Edit
    }, other));
  }, [throwIfNotInMode, updateRowInRowModesModel]);
  const updateStateToStartRowEditMode = useEventCallback_default((params) => {
    const {
      id,
      fieldToFocus,
      deleteValue,
      initialValue
    } = params;
    const row = apiRef.current.getRow(id);
    const columns = gridColumnDefinitionsSelector(apiRef);
    const newProps = columns.reduce((acc, col) => {
      const field = col.field;
      const cellParams = apiRef.current.getCellParams(id, field);
      if (!cellParams.isEditable) {
        return acc;
      }
      const column = apiRef.current.getColumn(field);
      let newValue = apiRef.current.getCellValue(id, field);
      if (fieldToFocus === field && (deleteValue || initialValue)) {
        if (deleteValue) {
          newValue = getDefaultCellValue(column);
        } else if (initialValue) {
          newValue = initialValue;
        }
      }
      acc[field] = {
        value: newValue,
        error: false,
        isProcessingProps: column.editable && !!column.preProcessEditCellProps && deleteValue
      };
      return acc;
    }, {});
    prevRowValuesLookup.current[id] = row;
    updateOrDeleteRowState(id, newProps);
    if (fieldToFocus) {
      apiRef.current.setCellFocus(id, fieldToFocus);
    }
    columns.filter((column) => {
      const isCellEditable = apiRef.current.getCellParams(id, column.field).isEditable;
      return isCellEditable && column.editable && !!column.preProcessEditCellProps && deleteValue;
    }).forEach((column) => {
      const field = column.field;
      const value = apiRef.current.getCellValue(id, field);
      const newValue = deleteValue ? getDefaultCellValue(column) : initialValue ?? value;
      Promise.resolve(column.preProcessEditCellProps({
        id,
        row,
        props: newProps[field],
        hasChanged: newValue !== value
      })).then((processedProps) => {
        if (apiRef.current.getRowMode(id) === GridRowModes.Edit) {
          const editingState = gridEditRowsStateSelector(apiRef);
          updateOrDeleteFieldState(id, field, _extends({}, processedProps, {
            value: editingState[id][field].value,
            isProcessingProps: false
          }));
        }
      });
    });
  });
  const stopRowEditMode = React234.useCallback((params) => {
    const {
      id
    } = params, other = _objectWithoutPropertiesLoose(params, _excluded217);
    throwIfNotInMode(id, GridRowModes.Edit);
    updateRowInRowModesModel(id, _extends({
      mode: GridRowModes.View
    }, other));
  }, [throwIfNotInMode, updateRowInRowModesModel]);
  const updateStateToStopRowEditMode = useEventCallback_default(async (params) => {
    var _a;
    const {
      id,
      ignoreModifications,
      field: focusedField,
      cellToFocusAfter = "none"
    } = params;
    apiRef.current.runPendingEditCellValueMutation(id);
    const finishRowEditMode = () => {
      if (cellToFocusAfter !== "none" && focusedField) {
        apiRef.current.moveFocusToRelativeCell(id, focusedField, cellToFocusAfter);
      }
      updateOrDeleteRowState(id, null);
      updateRowInRowModesModel(id, null);
      delete prevRowValuesLookup.current[id];
    };
    if (ignoreModifications && apiRef.current.getRow(id)) {
      finishRowEditMode();
      return;
    }
    const editingState = gridEditRowsStateSelector(apiRef);
    if (!editingState[id]) {
      finishRowEditMode();
      return;
    }
    const row = prevRowValuesLookup.current[id];
    const isSomeFieldProcessingProps = Object.values(editingState[id]).some((fieldProps) => fieldProps.isProcessingProps);
    if (isSomeFieldProcessingProps) {
      prevRowModesModel.current[id].mode = GridRowModes.Edit;
      return;
    }
    if (hasFieldsWithErrors(id)) {
      prevRowModesModel.current[id].mode = GridRowModes.Edit;
      updateRowInRowModesModel(id, {
        mode: GridRowModes.Edit
      });
      return;
    }
    const rowUpdate = apiRef.current.getRowWithUpdatedValuesFromRowEditing(id);
    if ((_a = props.dataSource) == null ? void 0 : _a.updateRow) {
      if (isDeepEqual(row, rowUpdate)) {
        finishRowEditMode();
        return;
      }
      const handleError = () => {
        prevRowModesModel.current[id].mode = GridRowModes.Edit;
        updateRowInRowModesModel(id, {
          mode: GridRowModes.Edit
        });
      };
      const updateRowParams = {
        rowId: id,
        updatedRow: rowUpdate,
        previousRow: row
      };
      try {
        await apiRef.current.dataSource.editRow(updateRowParams);
        finishRowEditMode();
      } catch {
        handleError();
      }
    } else if (processRowUpdate) {
      const handleError = (errorThrown) => {
        if (prevRowModesModel.current[id]) {
          prevRowModesModel.current[id].mode = GridRowModes.Edit;
          updateRowInRowModesModel(id, {
            mode: GridRowModes.Edit
          });
        }
        if (onProcessRowUpdateError) {
          onProcessRowUpdateError(errorThrown);
        } else {
          warnOnce(["MUI X: A call to `processRowUpdate()` threw an error which was not handled because `onProcessRowUpdateError()` is missing.", "To handle the error pass a callback to the `onProcessRowUpdateError()` prop, for example `<DataGrid onProcessRowUpdateError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/editing/persistence/."], "error");
        }
      };
      try {
        Promise.resolve(processRowUpdate(rowUpdate, row, {
          rowId: id
        })).then((finalRowUpdate) => {
          if (apiRef.current.getRow(id)) {
            apiRef.current.updateRows([finalRowUpdate]);
          }
          finishRowEditMode();
        }).catch(handleError);
      } catch (errorThrown) {
        handleError(errorThrown);
      }
    } else {
      if (apiRef.current.getRow(id)) {
        apiRef.current.updateRows([rowUpdate]);
      }
      finishRowEditMode();
    }
  });
  const setRowEditingEditCellValue = React234.useCallback((params) => {
    const {
      id,
      field,
      value,
      debounceMs,
      unstable_skipValueParser: skipValueParser
    } = params;
    throwIfNotEditable(id, field);
    const column = apiRef.current.getColumn(field);
    const row = apiRef.current.getRow(id);
    let parsedValue = value;
    if (column.valueParser && !skipValueParser) {
      parsedValue = column.valueParser(value, row, column, apiRef);
    }
    let editingState = gridEditRowsStateSelector(apiRef);
    let newProps = _extends({}, editingState[id][field], {
      value: parsedValue,
      changeReason: debounceMs ? "debouncedSetEditCellValue" : "setEditCellValue"
    });
    if (!column.preProcessEditCellProps) {
      updateOrDeleteFieldState(id, field, newProps);
    }
    return new Promise((resolve) => {
      const promises = [];
      if (column.preProcessEditCellProps) {
        const hasChanged = newProps.value !== editingState[id][field].value;
        newProps = _extends({}, newProps, {
          isProcessingProps: true
        });
        updateOrDeleteFieldState(id, field, newProps);
        const _editingState$id = editingState[id], otherFieldsProps = _objectWithoutPropertiesLoose(_editingState$id, [field].map(toPropertyKey));
        const promise = Promise.resolve(column.preProcessEditCellProps({
          id,
          row,
          props: newProps,
          hasChanged,
          otherFieldsProps
        })).then((processedProps) => {
          if (apiRef.current.getRowMode(id) === GridRowModes.View) {
            resolve(false);
            return;
          }
          editingState = gridEditRowsStateSelector(apiRef);
          processedProps = _extends({}, processedProps, {
            isProcessingProps: false
          });
          processedProps.value = column.preProcessEditCellProps ? editingState[id][field].value : parsedValue;
          updateOrDeleteFieldState(id, field, processedProps);
        });
        promises.push(promise);
      }
      Object.entries(editingState[id]).forEach(([thisField, fieldProps]) => {
        if (thisField === field) {
          return;
        }
        const fieldColumn = apiRef.current.getColumn(thisField);
        if (!fieldColumn.preProcessEditCellProps) {
          return;
        }
        fieldProps = _extends({}, fieldProps, {
          isProcessingProps: true
        });
        updateOrDeleteFieldState(id, thisField, fieldProps);
        editingState = gridEditRowsStateSelector(apiRef);
        const _editingState$id2 = editingState[id], otherFieldsProps = _objectWithoutPropertiesLoose(_editingState$id2, [thisField].map(toPropertyKey));
        const promise = Promise.resolve(fieldColumn.preProcessEditCellProps({
          id,
          row,
          props: fieldProps,
          hasChanged: false,
          otherFieldsProps
        })).then((processedProps) => {
          if (apiRef.current.getRowMode(id) === GridRowModes.View) {
            resolve(false);
            return;
          }
          processedProps = _extends({}, processedProps, {
            isProcessingProps: false
          });
          updateOrDeleteFieldState(id, thisField, processedProps);
        });
        promises.push(promise);
      });
      Promise.all(promises).then(() => {
        if (apiRef.current.getRowMode(id) === GridRowModes.Edit) {
          editingState = gridEditRowsStateSelector(apiRef);
          resolve(!editingState[id][field].error);
        } else {
          resolve(false);
        }
      });
    });
  }, [apiRef, throwIfNotEditable, updateOrDeleteFieldState]);
  const getRowWithUpdatedValuesFromRowEditing = React234.useCallback((id) => {
    const editingState = gridEditRowsStateSelector(apiRef);
    const row = apiRef.current.getRow(id);
    if (!editingState[id]) {
      return apiRef.current.getRow(id);
    }
    let rowUpdate = _extends({}, prevRowValuesLookup.current[id], row);
    Object.entries(editingState[id]).forEach(([field, fieldProps]) => {
      const column = apiRef.current.getColumn(field);
      if (column == null ? void 0 : column.valueSetter) {
        rowUpdate = column.valueSetter(fieldProps.value, rowUpdate, column, apiRef);
      } else {
        rowUpdate[field] = fieldProps.value;
      }
    });
    return rowUpdate;
  }, [apiRef]);
  const editingApi = {
    getRowMode,
    startRowEditMode,
    stopRowEditMode
  };
  const editingPrivateApi = {
    setRowEditingEditCellValue,
    getRowWithUpdatedValuesFromRowEditing
  };
  useGridApiMethod(apiRef, editingApi, "public");
  useGridApiMethod(apiRef, editingPrivateApi, "private");
  React234.useEffect(() => {
    if (rowModesModelProp) {
      updateRowModesModel(rowModesModelProp);
    }
  }, [rowModesModelProp, updateRowModesModel]);
  useEnhancedEffect_default(() => {
    const rowsLookup = gridRowsLookupSelector(apiRef);
    const copyOfPrevRowModesModel = prevRowModesModel.current;
    prevRowModesModel.current = deepClone(rowModesModel);
    const ids = /* @__PURE__ */ new Set([...Object.keys(rowModesModel), ...Object.keys(copyOfPrevRowModesModel)]);
    Array.from(ids).forEach((id) => {
      var _a;
      const params = rowModesModel[id] ?? {
        mode: GridRowModes.View
      };
      const prevMode = ((_a = copyOfPrevRowModesModel[id]) == null ? void 0 : _a.mode) || GridRowModes.View;
      const originalId = rowsLookup[id] ? apiRef.current.getRowId(rowsLookup[id]) : id;
      if (params.mode === GridRowModes.Edit && prevMode === GridRowModes.View) {
        updateStateToStartRowEditMode(_extends({
          id: originalId
        }, params));
      } else if (params.mode === GridRowModes.View && prevMode === GridRowModes.Edit) {
        updateStateToStopRowEditMode(_extends({
          id: originalId
        }, params));
      }
    });
  }, [apiRef, rowModesModel, updateOrDeleteRowState, updateStateToStartRowEditMode, updateStateToStopRowEditMode, updateRowInRowModesModel]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/editing/useGridEditing.js
var editingStateInitializer = (state) => _extends({}, state, {
  editRows: {}
});
var useGridEditing = (apiRef, props, configuration4) => {
  useGridCellEditing(apiRef, props);
  useGridRowEditing(apiRef, props);
  const debounceMap = React235.useRef({});
  const {
    isCellEditable: isCellEditableProp
  } = props;
  const isCellEditableFn = configuration4.hooks.useIsCellEditable(apiRef, props);
  const isCellEditable = React235.useCallback((params) => {
    if (!isCellEditableFn(params)) {
      return false;
    }
    if (!params.colDef.editable) {
      return false;
    }
    if (!params.colDef.renderEditCell) {
      return false;
    }
    if (isCellEditableProp) {
      return isCellEditableProp(params);
    }
    return true;
  }, [isCellEditableProp, isCellEditableFn]);
  const maybeDebounce = (id, field, debounceMs, callback) => {
    if (!debounceMs) {
      callback();
      return;
    }
    if (!debounceMap.current[id]) {
      debounceMap.current[id] = {};
    }
    if (debounceMap.current[id][field]) {
      const [timeout3] = debounceMap.current[id][field];
      clearTimeout(timeout3);
    }
    const runImmediately = () => {
      const [timeout3] = debounceMap.current[id][field];
      clearTimeout(timeout3);
      callback();
      delete debounceMap.current[id][field];
    };
    const timeout2 = setTimeout(() => {
      callback();
      delete debounceMap.current[id][field];
    }, debounceMs);
    debounceMap.current[id][field] = [timeout2, runImmediately];
  };
  React235.useEffect(() => {
    const debounces = debounceMap.current;
    return () => {
      Object.entries(debounces).forEach(([id, fields]) => {
        Object.keys(fields).forEach((field) => {
          const [timeout2] = debounces[id][field];
          clearTimeout(timeout2);
          delete debounces[id][field];
        });
      });
    };
  }, []);
  const runPendingEditCellValueMutation = React235.useCallback((id, field) => {
    if (!debounceMap.current[id]) {
      return;
    }
    if (!field) {
      Object.keys(debounceMap.current[id]).forEach((debouncedField) => {
        const [, runCallback] = debounceMap.current[id][debouncedField];
        runCallback();
      });
    } else if (debounceMap.current[id][field]) {
      const [, runCallback] = debounceMap.current[id][field];
      runCallback();
    }
  }, []);
  const setEditCellValue = React235.useCallback((params) => {
    const {
      id,
      field,
      debounceMs
    } = params;
    return new Promise((resolve) => {
      maybeDebounce(id, field, debounceMs, async () => {
        const setEditCellValueToCall = props.editMode === GridEditModes.Row ? apiRef.current.setRowEditingEditCellValue : apiRef.current.setCellEditingEditCellValue;
        if (apiRef.current.getCellMode(id, field) === GridCellModes.Edit) {
          const result = await setEditCellValueToCall(params);
          resolve(result);
        }
      });
    });
  }, [apiRef, props.editMode]);
  const getRowWithUpdatedValues = React235.useCallback((id, field) => {
    return props.editMode === GridEditModes.Cell ? apiRef.current.getRowWithUpdatedValuesFromCellEditing(id, field) : apiRef.current.getRowWithUpdatedValuesFromRowEditing(id);
  }, [apiRef, props.editMode]);
  const getEditCellMeta = React235.useCallback((id, field) => {
    var _a;
    const editingState = gridEditRowsStateSelector(apiRef);
    return ((_a = editingState[id]) == null ? void 0 : _a[field]) ?? null;
  }, [apiRef]);
  const editingSharedApi = {
    isCellEditable,
    setEditCellValue,
    getRowWithUpdatedValues,
    unstable_getEditCellMeta: getEditCellMeta
  };
  const editingSharedPrivateApi = {
    runPendingEditCellValueMutation
  };
  useGridApiMethod(apiRef, editingSharedApi, "public");
  useGridApiMethod(apiRef, editingSharedPrivateApi, "private");
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRows.js
var React236 = __toESM(require_react(), 1);
var rowsStateInitializer = (state, props, apiRef) => {
  const isDataSourceAvailable = !!props.dataSource;
  apiRef.current.caches.rows = createRowsInternalCache({
    rows: isDataSourceAvailable ? [] : props.rows,
    getRowId: props.getRowId,
    loading: props.loading,
    rowCount: props.rowCount
  });
  return _extends({}, state, {
    rows: getRowsStateFromCache({
      apiRef,
      rowCountProp: props.rowCount,
      loadingProp: isDataSourceAvailable ? true : props.loading,
      previousTree: null,
      previousTreeDepths: null
    })
  });
};
var useGridRows = (apiRef, props, configuration4) => {
  if (true) {
    try {
      Object.freeze(props.rows);
    } catch (error) {
    }
  }
  const logger = useGridLogger(apiRef, "useGridRows");
  const lastUpdateMs = React236.useRef(Date.now());
  const lastRowCount = React236.useRef(props.rowCount);
  const timeout2 = useTimeout();
  const {
    setRowIndex,
    setRowPosition
  } = configuration4.hooks.useGridRowsOverridableMethods(apiRef, props);
  const getRow = React236.useCallback((id) => {
    const model = gridRowsLookupSelector(apiRef)[id];
    if (model) {
      return model;
    }
    const node = gridRowNodeSelector(apiRef, id);
    if (node && isAutogeneratedRowNode(node)) {
      return {
        [GRID_ID_AUTOGENERATED]: id
      };
    }
    return null;
  }, [apiRef]);
  const getRowId = React236.useCallback((row) => gridRowIdSelector(apiRef, row), [apiRef]);
  const throttledRowsChange = React236.useCallback(({
    cache,
    throttle: throttle2
  }) => {
    const run = () => {
      lastUpdateMs.current = Date.now();
      apiRef.current.setState((state) => _extends({}, state, {
        rows: getRowsStateFromCache({
          apiRef,
          rowCountProp: props.rowCount,
          loadingProp: props.loading,
          previousTree: gridRowTreeSelector(apiRef),
          previousTreeDepths: gridRowTreeDepthsSelector(apiRef),
          previousGroupsToFetch: gridRowGroupsToFetchSelector(apiRef)
        })
      }));
      apiRef.current.publishEvent("rowsSet");
    };
    timeout2.clear();
    apiRef.current.caches.rows = cache;
    if (!throttle2) {
      run();
      return;
    }
    const throttleRemainingTimeMs = props.throttleRowsMs - (Date.now() - lastUpdateMs.current);
    if (throttleRemainingTimeMs > 0) {
      timeout2.start(throttleRemainingTimeMs, run);
      return;
    }
    run();
  }, [props.throttleRowsMs, props.rowCount, props.loading, apiRef, timeout2]);
  const setRows = React236.useCallback((rows) => {
    logger.debug(`Updating all rows, new length ${rows.length}`);
    if (!props.dataSource && gridPivotActiveSelector(apiRef)) {
      apiRef.current.updateNonPivotRows(rows, false);
      return;
    }
    const cache = createRowsInternalCache({
      rows,
      getRowId: props.getRowId,
      loading: props.loading,
      rowCount: props.rowCount
    });
    const prevCache = apiRef.current.caches.rows;
    cache.rowsBeforePartialUpdates = prevCache.rowsBeforePartialUpdates;
    throttledRowsChange({
      cache,
      throttle: true
    });
  }, [logger, props.getRowId, props.dataSource, props.loading, props.rowCount, throttledRowsChange, apiRef]);
  const updateRows = React236.useCallback((updates) => {
    if (props.signature === GridSignature.DataGrid && updates.length > 1) {
      throw new Error(["MUI X: You cannot update several rows at once in `apiRef.current.updateRows` on the DataGrid.", "You need to upgrade to DataGridPro or DataGridPremium component to unlock this feature."].join("\n"));
    }
    if (!props.dataSource && gridPivotActiveSelector(apiRef)) {
      apiRef.current.updateNonPivotRows(updates);
      return;
    }
    const nonPinnedRowsUpdates = computeRowsUpdates(apiRef, updates, props.getRowId);
    const cache = updateCacheWithNewRows({
      updates: nonPinnedRowsUpdates,
      getRowId: props.getRowId,
      previousCache: apiRef.current.caches.rows
    });
    throttledRowsChange({
      cache,
      throttle: true
    });
  }, [props.signature, props.dataSource, props.getRowId, throttledRowsChange, apiRef]);
  const updateNestedRows = React236.useCallback((updates, groupKeys) => {
    const nonPinnedRowsUpdates = computeRowsUpdates(apiRef, updates, props.getRowId);
    const cache = updateCacheWithNewRows({
      updates: nonPinnedRowsUpdates,
      getRowId: props.getRowId,
      previousCache: apiRef.current.caches.rows,
      groupKeys: groupKeys ?? []
    });
    throttledRowsChange({
      cache,
      throttle: false
    });
  }, [props.getRowId, throttledRowsChange, apiRef]);
  const setLoading = React236.useCallback((loading) => {
    logger.debug(`Setting loading to ${loading}`);
    apiRef.current.setState((state) => _extends({}, state, {
      rows: _extends({}, state.rows, {
        loading
      })
    }));
    apiRef.current.caches.rows.loadingPropBeforePartialUpdates = loading;
  }, [apiRef, logger]);
  const getRowModels = React236.useCallback(() => {
    const dataRows = gridDataRowIdsSelector(apiRef);
    const idRowsLookup = gridRowsLookupSelector(apiRef);
    return new Map(dataRows.map((id) => [id, idRowsLookup[id] ?? {}]));
  }, [apiRef]);
  const getRowsCount = React236.useCallback(() => gridRowCountSelector(apiRef), [apiRef]);
  const getAllRowIds = React236.useCallback(() => gridDataRowIdsSelector(apiRef), [apiRef]);
  const getRowIndexRelativeToVisibleRows = React236.useCallback((id) => {
    const {
      rowIdToIndexMap
    } = getVisibleRows(apiRef);
    return rowIdToIndexMap.get(id);
  }, [apiRef]);
  const setRowChildrenExpansion = React236.useCallback((id, isExpanded) => {
    const currentNode = gridRowNodeSelector(apiRef, id);
    if (!currentNode) {
      throw new Error(`MUI X: No row with id #${id} found.`);
    }
    if (currentNode.type !== "group") {
      throw new Error("MUI X: Only group nodes can be expanded or collapsed.");
    }
    const newNode = _extends({}, currentNode, {
      childrenExpanded: isExpanded
    });
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: _extends({}, state.rows.tree, {
            [id]: newNode
          })
        })
      });
    });
    apiRef.current.publishEvent("rowExpansionChange", newNode);
  }, [apiRef]);
  const expandAllRows = React236.useCallback(() => {
    const tree = _extends({}, gridRowTreeSelector(apiRef));
    const traverse = (nodeId) => {
      const node = tree[nodeId];
      if ((node == null ? void 0 : node.type) === "group") {
        tree[nodeId] = _extends({}, node, {
          childrenExpanded: true
        });
        node.children.forEach(traverse);
      }
    };
    traverse(GRID_ROOT_GROUP_ID);
    apiRef.current.setState((state) => _extends({}, state, {
      rows: _extends({}, state.rows, {
        tree
      })
    }));
    apiRef.current.publishEvent("rowExpansionChange", tree[GRID_ROOT_GROUP_ID]);
  }, [apiRef]);
  const collapseAllRows = React236.useCallback(() => {
    const tree = _extends({}, gridRowTreeSelector(apiRef));
    const traverse = (nodeId) => {
      const node = tree[nodeId];
      if ((node == null ? void 0 : node.type) === "group") {
        tree[nodeId] = _extends({}, node, {
          childrenExpanded: false
        });
        node.children.forEach(traverse);
      }
    };
    traverse(GRID_ROOT_GROUP_ID);
    apiRef.current.setState((state) => _extends({}, state, {
      rows: _extends({}, state.rows, {
        tree
      })
    }));
    apiRef.current.publishEvent("rowExpansionChange", tree[GRID_ROOT_GROUP_ID]);
  }, [apiRef]);
  const getRowNode = React236.useCallback((id) => gridRowNodeSelector(apiRef, id) ?? null, [apiRef]);
  const getRowGroupChildren = React236.useCallback(({
    skipAutoGeneratedRows = true,
    groupId,
    applySorting,
    applyFiltering,
    directChildrenOnly = false
  }) => {
    const tree = gridRowTreeSelector(apiRef);
    let children2;
    if (applySorting) {
      const groupNode = tree[groupId];
      if (!groupNode) {
        return [];
      }
      const sortedRowIds = gridSortedRowIdsSelector(apiRef);
      children2 = [];
      const startIndex = sortedRowIds.findIndex((id) => id === groupId) + 1;
      for (let index = startIndex; index < sortedRowIds.length && (directChildrenOnly ? tree[sortedRowIds[index]].depth === groupNode.depth + 1 : tree[sortedRowIds[index]].depth > groupNode.depth); index += 1) {
        const id = sortedRowIds[index];
        if (!skipAutoGeneratedRows || !isAutogeneratedRowNode(tree[id])) {
          children2.push(id);
        }
      }
    } else {
      children2 = getTreeNodeDescendants(tree, groupId, skipAutoGeneratedRows, directChildrenOnly);
    }
    if (applyFiltering) {
      const filteredRowsLookup = gridFilteredRowsLookupSelector(apiRef);
      children2 = isObjectEmpty(filteredRowsLookup) ? children2 : children2.filter((childId) => filteredRowsLookup[childId] !== false);
    }
    return children2;
  }, [apiRef]);
  const replaceRows = React236.useCallback((firstRowToRender, newRows) => {
    if (props.signature === GridSignature.DataGrid && newRows.length > 1) {
      throw new Error(["MUI X: You cannot replace rows using `apiRef.current.unstable_replaceRows` on the DataGrid.", "You need to upgrade to DataGridPro or DataGridPremium component to unlock this feature."].join("\n"));
    }
    if (newRows.length === 0) {
      return;
    }
    const treeDepth = gridRowMaximumTreeDepthSelector(apiRef);
    if (treeDepth > 1) {
      throw new Error("`apiRef.current.unstable_replaceRows` is not compatible with tree data and row grouping");
    }
    const tree = _extends({}, gridRowTreeSelector(apiRef));
    const dataRowIdToModelLookup = _extends({}, gridRowsLookupSelector(apiRef));
    const rootGroup = tree[GRID_ROOT_GROUP_ID];
    const rootGroupChildren = [...rootGroup.children];
    const seenIds = /* @__PURE__ */ new Set();
    for (let i2 = 0; i2 < newRows.length; i2 += 1) {
      const rowModel = newRows[i2];
      const rowId = getRowIdFromRowModel(rowModel, props.getRowId, "A row was provided without id when calling replaceRows().");
      const [removedRowId] = rootGroupChildren.splice(firstRowToRender + i2, 1, rowId);
      if (!seenIds.has(removedRowId)) {
        delete dataRowIdToModelLookup[removedRowId];
        delete tree[removedRowId];
      }
      const rowTreeNodeConfig = {
        id: rowId,
        depth: 0,
        parent: GRID_ROOT_GROUP_ID,
        type: "leaf",
        groupingKey: null
      };
      dataRowIdToModelLookup[rowId] = rowModel;
      tree[rowId] = rowTreeNodeConfig;
      seenIds.add(rowId);
    }
    tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
      children: rootGroupChildren
    });
    const dataRowIds = rootGroupChildren.filter((childId) => {
      var _a;
      return ((_a = tree[childId]) == null ? void 0 : _a.type) === "leaf";
    });
    apiRef.current.caches.rows.dataRowIdToModelLookup = dataRowIdToModelLookup;
    apiRef.current.setState((state) => _extends({}, state, {
      rows: _extends({}, state.rows, {
        loading: props.loading,
        totalRowCount: Math.max(props.rowCount || 0, rootGroupChildren.length),
        dataRowIdToModelLookup,
        dataRowIds,
        tree
      })
    }));
    apiRef.current.publishEvent("rowsSet");
  }, [apiRef, props.signature, props.getRowId, props.loading, props.rowCount]);
  const rowApi = {
    getRow,
    setLoading,
    getRowId,
    getRowModels,
    getRowsCount,
    getAllRowIds,
    setRows,
    updateRows,
    getRowNode,
    getRowIndexRelativeToVisibleRows,
    unstable_replaceRows: replaceRows
  };
  const rowProApi = {
    setRowIndex,
    setRowPosition,
    setRowChildrenExpansion,
    getRowGroupChildren,
    expandAllRows,
    collapseAllRows
  };
  const rowProPrivateApi = {
    updateNestedRows
  };
  const groupRows = React236.useCallback(() => {
    logger.info(`Row grouping pre-processing have changed, regenerating the row tree`);
    let cache;
    if (apiRef.current.caches.rows.rowsBeforePartialUpdates === props.rows) {
      cache = _extends({}, apiRef.current.caches.rows, {
        updates: {
          type: "full",
          rows: gridDataRowIdsSelector(apiRef)
        }
      });
    } else {
      cache = createRowsInternalCache({
        rows: props.rows,
        getRowId: props.getRowId,
        loading: props.loading,
        rowCount: props.rowCount
      });
    }
    throttledRowsChange({
      cache,
      throttle: false
    });
  }, [logger, apiRef, props.rows, props.getRowId, props.loading, props.rowCount, throttledRowsChange]);
  const previousDataSource = useLazyRef(() => props.dataSource);
  const handleStrategyProcessorChange = React236.useCallback((methodName) => {
    if (props.dataSource && props.dataSource !== previousDataSource.current) {
      previousDataSource.current = props.dataSource;
      return;
    }
    if (methodName === "rowTreeCreation") {
      groupRows();
    }
  }, [groupRows, previousDataSource, props.dataSource]);
  const handleStrategyActivityChange = React236.useCallback(() => {
    if (apiRef.current.getActiveStrategy(GridStrategyGroup.RowTree) !== gridRowGroupingNameSelector(apiRef)) {
      groupRows();
    }
  }, [apiRef, groupRows]);
  useGridEvent(apiRef, "activeStrategyProcessorChange", handleStrategyProcessorChange);
  useGridEvent(apiRef, "strategyAvailabilityChange", handleStrategyActivityChange);
  const applyHydrateRowsProcessor = React236.useCallback(() => {
    apiRef.current.setState((state) => {
      const response = apiRef.current.unstable_applyPipeProcessors("hydrateRows", {
        tree: gridRowTreeSelector(apiRef),
        treeDepths: gridRowTreeDepthsSelector(apiRef),
        dataRowIds: gridDataRowIdsSelector(apiRef),
        dataRowIdToModelLookup: gridRowsLookupSelector(apiRef)
      });
      return _extends({}, state, {
        rows: _extends({}, state.rows, response, {
          totalTopLevelRowCount: getTopLevelRowCount({
            tree: response.tree,
            rowCountProp: props.rowCount
          })
        })
      });
    });
    apiRef.current.publishEvent("rowsSet");
  }, [apiRef, props.rowCount]);
  useGridRegisterPipeApplier(apiRef, "hydrateRows", applyHydrateRowsProcessor);
  useGridApiMethod(apiRef, rowApi, "public");
  useGridApiMethod(apiRef, rowProApi, props.signature === GridSignature.DataGrid ? "private" : "public");
  useGridApiMethod(apiRef, rowProPrivateApi, "private");
  const isFirstRender = React236.useRef(true);
  React236.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    let isRowCountPropUpdated = false;
    if (props.rowCount !== lastRowCount.current) {
      isRowCountPropUpdated = true;
      lastRowCount.current = props.rowCount;
    }
    const currentRows = props.dataSource ? gridDataRowsSelector(apiRef) : props.rows;
    const areNewRowsAlreadyInState = apiRef.current.caches.rows.rowsBeforePartialUpdates === currentRows;
    const isNewLoadingAlreadyInState = apiRef.current.caches.rows.loadingPropBeforePartialUpdates === props.loading;
    const isNewRowCountAlreadyInState = apiRef.current.caches.rows.rowCountPropBeforePartialUpdates === props.rowCount;
    if (areNewRowsAlreadyInState) {
      if (!isNewLoadingAlreadyInState) {
        apiRef.current.setState((state) => _extends({}, state, {
          rows: _extends({}, state.rows, {
            loading: props.loading
          })
        }));
        apiRef.current.caches.rows.loadingPropBeforePartialUpdates = props.loading;
      }
      if (!isNewRowCountAlreadyInState) {
        apiRef.current.setState((state) => _extends({}, state, {
          rows: _extends({}, state.rows, {
            totalRowCount: Math.max(props.rowCount || 0, state.rows.totalRowCount),
            totalTopLevelRowCount: Math.max(props.rowCount || 0, state.rows.totalTopLevelRowCount)
          })
        }));
        apiRef.current.caches.rows.rowCountPropBeforePartialUpdates = props.rowCount;
      }
      if (!isRowCountPropUpdated) {
        return;
      }
    }
    logger.debug(`Updating all rows, new length ${currentRows == null ? void 0 : currentRows.length}`);
    throttledRowsChange({
      cache: createRowsInternalCache({
        rows: currentRows,
        getRowId: props.getRowId,
        loading: props.loading,
        rowCount: props.rowCount
      }),
      throttle: false
    });
  }, [props.rows, props.rowCount, props.getRowId, props.loading, props.dataSource, logger, throttledRowsChange, apiRef]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowsPreProcessors.js
var createFlatRowTree = (rows) => {
  const tree = {
    [GRID_ROOT_GROUP_ID]: _extends({}, buildRootGroup(), {
      children: rows
    })
  };
  for (let i2 = 0; i2 < rows.length; i2 += 1) {
    const rowId = rows[i2];
    tree[rowId] = {
      id: rowId,
      depth: 0,
      parent: GRID_ROOT_GROUP_ID,
      type: "leaf",
      groupingKey: null
    };
  }
  return {
    groupingName: GRID_DEFAULT_STRATEGY,
    tree,
    treeDepths: {
      0: rows.length
    },
    dataRowIds: rows
  };
};
var updateFlatRowTree = ({
  previousTree,
  actions
}) => {
  const tree = _extends({}, previousTree);
  const idsToRemoveFromRootGroup = {};
  for (let i2 = 0; i2 < actions.remove.length; i2 += 1) {
    const idToDelete = actions.remove[i2];
    idsToRemoveFromRootGroup[idToDelete] = true;
    delete tree[idToDelete];
  }
  for (let i2 = 0; i2 < actions.insert.length; i2 += 1) {
    const idToInsert = actions.insert[i2];
    tree[idToInsert] = {
      id: idToInsert,
      depth: 0,
      parent: GRID_ROOT_GROUP_ID,
      type: "leaf",
      groupingKey: null
    };
  }
  const rootGroup = tree[GRID_ROOT_GROUP_ID];
  let rootGroupChildren = [...rootGroup.children, ...actions.insert];
  if (Object.values(idsToRemoveFromRootGroup).length) {
    rootGroupChildren = rootGroupChildren.filter((id) => !idsToRemoveFromRootGroup[id]);
  }
  tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
    children: rootGroupChildren
  });
  return {
    groupingName: GRID_DEFAULT_STRATEGY,
    tree,
    treeDepths: {
      0: rootGroupChildren.length
    },
    dataRowIds: rootGroupChildren
  };
};
var flatRowTreeCreationMethod = (params) => {
  if (params.updates.type === "full") {
    return createFlatRowTree(params.updates.rows);
  }
  return updateFlatRowTree({
    previousTree: params.previousTree,
    actions: params.updates.actions
  });
};
var useGridRowsPreProcessors = (apiRef) => {
  useGridRegisterStrategyProcessor(apiRef, GRID_DEFAULT_STRATEGY, "rowTreeCreation", flatRowTreeCreationMethod);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridParamsApi.js
var React237 = __toESM(require_react(), 1);
var MissingRowIdError = class extends Error {
};
function useGridParamsApi(apiRef, props, configuration4) {
  var _a;
  const getColumnHeaderParams = React237.useCallback((field) => ({
    field,
    colDef: apiRef.current.getColumn(field)
  }), [apiRef]);
  const getRowParams = React237.useCallback((id) => {
    const row = apiRef.current.getRow(id);
    if (!row) {
      throw new MissingRowIdError(`No row with id #${id} found`);
    }
    const params = {
      id,
      columns: apiRef.current.getAllColumns(),
      row
    };
    return params;
  }, [apiRef]);
  const getCellParamsForRow = React237.useCallback((id, field, row, {
    cellMode,
    colDef,
    hasFocus,
    rowNode,
    tabIndex,
    value: forcedValue,
    formattedValue: forcedFormattedValue
  }) => {
    const value = forcedValue !== void 0 ? forcedValue : apiRef.current.getRowValue(row, colDef);
    const formattedValue = forcedFormattedValue !== void 0 ? forcedFormattedValue : apiRef.current.getRowFormattedValue(row, colDef);
    const params = {
      id,
      field,
      row,
      rowNode,
      colDef,
      cellMode,
      hasFocus,
      tabIndex,
      value,
      formattedValue,
      isEditable: false,
      api: apiRef.current
    };
    params.isEditable = colDef && apiRef.current.isCellEditable(params);
    return params;
  }, [apiRef]);
  const getCellParams = React237.useCallback((id, field) => {
    var _a2;
    const row = apiRef.current.getRow(id);
    const rowNode = gridRowNodeSelector(apiRef, id);
    if (!row || !rowNode) {
      throw new MissingRowIdError(`No row with id #${id} found`);
    }
    const cellFocus = gridFocusCellSelector(apiRef);
    const cellTabIndex = gridTabIndexCellSelector(apiRef);
    const cellMode = apiRef.current.getCellMode(id, field);
    return apiRef.current.getCellParamsForRow(id, field, row, {
      colDef: props.listView && ((_a2 = props.listViewColumn) == null ? void 0 : _a2.field) === field ? gridListColumnSelector(apiRef) : apiRef.current.getColumn(field),
      rowNode,
      hasFocus: cellFocus !== null && cellFocus.field === field && cellFocus.id === id,
      tabIndex: cellTabIndex && cellTabIndex.field === field && cellTabIndex.id === id ? 0 : -1,
      cellMode
    });
  }, [apiRef, props.listView, (_a = props.listViewColumn) == null ? void 0 : _a.field]);
  const getColumnHeaderElement = React237.useCallback((field) => {
    if (!apiRef.current.rootElementRef.current) {
      return null;
    }
    return getGridColumnHeaderElement(apiRef.current.rootElementRef.current, field);
  }, [apiRef]);
  const getRowElement = React237.useCallback((id) => {
    if (!apiRef.current.rootElementRef.current) {
      return null;
    }
    return getGridRowElement(apiRef.current.rootElementRef.current, id);
  }, [apiRef]);
  const getCellElement = React237.useCallback((id, field) => {
    if (!apiRef.current.rootElementRef.current) {
      return null;
    }
    return getGridCellElement(apiRef.current.rootElementRef.current, {
      id,
      field
    });
  }, [apiRef]);
  const overridableParamsMethods = configuration4.hooks.useGridParamsOverridableMethods(apiRef);
  const paramsApi = {
    getCellValue: overridableParamsMethods.getCellValue,
    getCellParams,
    getCellElement,
    getRowValue: overridableParamsMethods.getRowValue,
    getRowFormattedValue: overridableParamsMethods.getRowFormattedValue,
    getRowParams,
    getRowElement,
    getColumnHeaderParams,
    getColumnHeaderElement
  };
  const paramsPrivateApi = {
    getCellParamsForRow
  };
  useGridApiMethod(apiRef, paramsApi, "public");
  useGridApiMethod(apiRef, paramsPrivateApi, "private");
}

// node_modules/@mui/x-data-grid/esm/hooks/features/rowSelection/useGridRowSelection.js
var React238 = __toESM(require_react(), 1);
var emptyModel = {
  type: "include",
  ids: /* @__PURE__ */ new Set()
};
var rowSelectionStateInitializer = (state, props) => _extends({}, state, {
  rowSelection: props.rowSelection ? props.rowSelectionModel ?? emptyModel : emptyModel
});
var useGridRowSelection = (apiRef, props) => {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  const logger = useGridLogger(apiRef, "useGridSelection");
  const isNestedData = useGridSelector(apiRef, gridRowMaximumTreeDepthSelector) > 1;
  const applyAutoSelection = props.signature !== GridSignature.DataGrid && (((_a = props.rowSelectionPropagation) == null ? void 0 : _a.parents) || ((_b = props.rowSelectionPropagation) == null ? void 0 : _b.descendants)) && isNestedData;
  const propRowSelectionModel = React238.useMemo(() => {
    return props.rowSelectionModel;
  }, [props.rowSelectionModel]);
  const lastRowToggled = React238.useRef(null);
  apiRef.current.registerControlState({
    stateId: "rowSelection",
    propModel: propRowSelectionModel,
    propOnChange: props.onRowSelectionModelChange,
    stateSelector: gridRowSelectionStateSelector,
    changeEvent: "rowSelectionChange"
  });
  const {
    checkboxSelection,
    disableRowSelectionOnClick,
    isRowSelectable: propIsRowSelectable
  } = props;
  const canHaveMultipleSelection = isMultipleRowSelectionEnabled(props);
  const expandMouseRowRangeSelection = React238.useCallback((id) => {
    let endId = id;
    const startId = lastRowToggled.current ?? id;
    const isSelected = apiRef.current.isRowSelected(id);
    if (isSelected) {
      const visibleRowIds = gridExpandedSortedRowIdsSelector(apiRef);
      const startIndex = visibleRowIds.findIndex((rowId) => rowId === startId);
      const endIndex = visibleRowIds.findIndex((rowId) => rowId === endId);
      if (startIndex === endIndex) {
        return;
      }
      if (startIndex > endIndex) {
        endId = visibleRowIds[endIndex + 1];
      } else {
        endId = visibleRowIds[endIndex - 1];
      }
    }
    lastRowToggled.current = id;
    apiRef.current.selectRowRange({
      startId,
      endId
    }, !isSelected);
  }, [apiRef]);
  const getRowsToBeSelected = useEventCallback_default(() => {
    const rowsToBeSelected = props.pagination && props.checkboxSelectionVisibleOnly && props.paginationMode === "client" ? gridPaginatedVisibleSortedGridRowIdsSelector(apiRef) : gridExpandedSortedRowIdsSelector(apiRef);
    return rowsToBeSelected;
  });
  const setRowSelectionModel = React238.useCallback((model, reason) => {
    if (props.signature === GridSignature.DataGrid && !canHaveMultipleSelection && (model.type !== "include" || model.ids.size > 1)) {
      throw new Error(["MUI X: `rowSelectionModel` can only contain 1 item in DataGrid.", "You need to upgrade to DataGridPro or DataGridPremium component to unlock multiple selection."].join("\n"));
    }
    const currentModel = gridRowSelectionStateSelector(apiRef);
    if (currentModel !== model) {
      logger.debug(`Setting selection model`);
      if (lastRowToggled.current !== null) {
        const isInModel = model.ids.has(lastRowToggled.current);
        if (model.type === "include" && !isInModel || model.type === "exclude" && isInModel) {
          lastRowToggled.current = null;
        }
      }
      apiRef.current.setState((state) => _extends({}, state, {
        rowSelection: props.rowSelection ? model : emptyModel
      }), reason);
    }
  }, [apiRef, logger, props.rowSelection, props.signature, canHaveMultipleSelection]);
  const isRowSelected = React238.useCallback((id) => {
    const selectionManager = gridRowSelectionManagerSelector(apiRef);
    return selectionManager.has(id);
  }, [apiRef]);
  const isRowSelectable = React238.useCallback((id) => {
    if (props.rowSelection === false) {
      return false;
    }
    if (props.keepNonExistentRowsSelected && !apiRef.current.getRow(id)) {
      return true;
    }
    if (propIsRowSelectable && !propIsRowSelectable(apiRef.current.getRowParams(id))) {
      return false;
    }
    const rowNode = gridRowNodeSelector(apiRef, id);
    if ((rowNode == null ? void 0 : rowNode.type) === "footer" || (rowNode == null ? void 0 : rowNode.type) === "pinnedRow") {
      return false;
    }
    return true;
  }, [apiRef, props.rowSelection, props.keepNonExistentRowsSelected, propIsRowSelectable]);
  const getSelectedRows = React238.useCallback(() => gridRowSelectionIdsSelector(apiRef), [apiRef]);
  const selectRow = React238.useCallback((id, isSelected = true, resetSelection = false) => {
    var _a2, _b2, _c2, _d2, _e2, _f2;
    if (!apiRef.current.isRowSelectable(id)) {
      return;
    }
    const tree = gridRowTreeSelector(apiRef);
    lastRowToggled.current = isSelected ? id : null;
    if (resetSelection) {
      logger.debug(`Setting selection for row ${id}`);
      const newSelectionModel = {
        type: "include",
        ids: /* @__PURE__ */ new Set()
      };
      const addRow = (rowId) => {
        newSelectionModel.ids.add(rowId);
      };
      if (isSelected) {
        addRow(id);
        if (applyAutoSelection) {
          findRowsToSelect(apiRef, tree, id, ((_a2 = props.rowSelectionPropagation) == null ? void 0 : _a2.descendants) ?? false, ((_b2 = props.rowSelectionPropagation) == null ? void 0 : _b2.parents) ?? false, addRow);
        }
      }
      apiRef.current.setRowSelectionModel(newSelectionModel, "singleRowSelection");
    } else {
      logger.debug(`Toggling selection for row ${id}`);
      const selectionModel = gridRowSelectionStateSelector(apiRef);
      const newSelectionModel = {
        type: selectionModel.type,
        ids: new Set(selectionModel.ids)
      };
      const selectionManager = createRowSelectionManager(newSelectionModel);
      selectionManager.unselect(id);
      const addRow = (rowId) => {
        selectionManager.select(rowId);
      };
      const removeRow = (rowId) => {
        selectionManager.unselect(rowId);
      };
      if (isSelected) {
        addRow(id);
        if (applyAutoSelection) {
          findRowsToSelect(apiRef, tree, id, ((_c2 = props.rowSelectionPropagation) == null ? void 0 : _c2.descendants) ?? false, ((_d2 = props.rowSelectionPropagation) == null ? void 0 : _d2.parents) ?? false, addRow);
        }
      } else if (applyAutoSelection) {
        findRowsToDeselect(apiRef, tree, id, ((_e2 = props.rowSelectionPropagation) == null ? void 0 : _e2.descendants) ?? false, ((_f2 = props.rowSelectionPropagation) == null ? void 0 : _f2.parents) ?? false, removeRow);
      }
      const isSelectionValid = newSelectionModel.type === "include" && newSelectionModel.ids.size < 2 || canHaveMultipleSelection;
      if (isSelectionValid) {
        apiRef.current.setRowSelectionModel(newSelectionModel, "singleRowSelection");
      }
    }
  }, [apiRef, logger, applyAutoSelection, (_c = props.rowSelectionPropagation) == null ? void 0 : _c.descendants, (_d = props.rowSelectionPropagation) == null ? void 0 : _d.parents, canHaveMultipleSelection]);
  const selectRows = React238.useCallback((ids, isSelected = true, resetSelection = false) => {
    var _a2, _b2, _c2, _d2, _e2, _f2;
    logger.debug(`Setting selection for several rows`);
    if (props.rowSelection === false) {
      return;
    }
    const tree = gridRowTreeSelector(apiRef);
    const selectableIds = /* @__PURE__ */ new Set();
    for (let i2 = 0; i2 < ids.length; i2 += 1) {
      const id = ids[i2];
      if (apiRef.current.isRowSelectable(id)) {
        selectableIds.add(id);
      }
    }
    const currentSelectionModel = gridRowSelectionStateSelector(apiRef);
    let newSelectionModel;
    if (resetSelection) {
      newSelectionModel = {
        type: "include",
        ids: selectableIds
      };
      if (isSelected) {
        const selectionManager = createRowSelectionManager(newSelectionModel);
        if (applyAutoSelection) {
          const addRow = (rowId) => {
            selectionManager.select(rowId);
          };
          for (const id of selectableIds) {
            findRowsToSelect(apiRef, tree, id, ((_a2 = props.rowSelectionPropagation) == null ? void 0 : _a2.descendants) ?? false, ((_b2 = props.rowSelectionPropagation) == null ? void 0 : _b2.parents) ?? false, addRow);
          }
        }
      } else {
        newSelectionModel.ids = /* @__PURE__ */ new Set();
      }
      if (currentSelectionModel.type === newSelectionModel.type && newSelectionModel.ids.size === currentSelectionModel.ids.size && Array.from(newSelectionModel.ids).every((id) => currentSelectionModel.ids.has(id))) {
        return;
      }
    } else {
      newSelectionModel = {
        type: currentSelectionModel.type,
        ids: new Set(currentSelectionModel.ids)
      };
      const selectionManager = createRowSelectionManager(newSelectionModel);
      const addRow = (rowId) => {
        selectionManager.select(rowId);
      };
      const removeRow = (rowId) => {
        selectionManager.unselect(rowId);
      };
      for (const id of selectableIds) {
        if (isSelected) {
          selectionManager.select(id);
          if (applyAutoSelection) {
            findRowsToSelect(apiRef, tree, id, ((_c2 = props.rowSelectionPropagation) == null ? void 0 : _c2.descendants) ?? false, ((_d2 = props.rowSelectionPropagation) == null ? void 0 : _d2.parents) ?? false, addRow);
          }
        } else {
          removeRow(id);
          if (applyAutoSelection) {
            findRowsToDeselect(apiRef, tree, id, ((_e2 = props.rowSelectionPropagation) == null ? void 0 : _e2.descendants) ?? false, ((_f2 = props.rowSelectionPropagation) == null ? void 0 : _f2.parents) ?? false, removeRow);
          }
        }
      }
    }
    const isSelectionValid = newSelectionModel.type === "include" && newSelectionModel.ids.size < 2 || canHaveMultipleSelection;
    if (isSelectionValid) {
      apiRef.current.setRowSelectionModel(newSelectionModel, "multipleRowsSelection");
    }
  }, [logger, applyAutoSelection, canHaveMultipleSelection, apiRef, (_e = props.rowSelectionPropagation) == null ? void 0 : _e.descendants, (_f = props.rowSelectionPropagation) == null ? void 0 : _f.parents, props.rowSelection]);
  const getPropagatedRowSelectionModel = React238.useCallback((inputSelectionModel) => {
    var _a2, _b2;
    if (!isNestedData || !applyAutoSelection || inputSelectionModel.type === "exclude" || inputSelectionModel.ids.size === 0 && inputSelectionModel.type === "include") {
      return inputSelectionModel;
    }
    const propagatedSelectionModel = {
      type: inputSelectionModel.type,
      ids: new Set(inputSelectionModel.ids)
    };
    const tree = gridRowTreeSelector(apiRef);
    const selectionManager = createRowSelectionManager(propagatedSelectionModel);
    const addRow = (rowId) => {
      selectionManager.select(rowId);
    };
    for (const id of inputSelectionModel.ids) {
      findRowsToSelect(apiRef, tree, id, ((_a2 = props.rowSelectionPropagation) == null ? void 0 : _a2.descendants) ?? false, ((_b2 = props.rowSelectionPropagation) == null ? void 0 : _b2.parents) ?? false, addRow, selectionManager);
    }
    return propagatedSelectionModel;
  }, [apiRef, (_g = props.rowSelectionPropagation) == null ? void 0 : _g.descendants, (_h = props.rowSelectionPropagation) == null ? void 0 : _h.parents, isNestedData, applyAutoSelection]);
  const selectRowRange = React238.useCallback(({
    startId,
    endId
  }, isSelected = true, resetSelection = false) => {
    if (!apiRef.current.getRow(startId) || !apiRef.current.getRow(endId)) {
      return;
    }
    logger.debug(`Expanding selection from row ${startId} to row ${endId}`);
    const allPagesRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const startIndex = allPagesRowIds.indexOf(startId);
    const endIndex = allPagesRowIds.indexOf(endId);
    const [start2, end2] = startIndex > endIndex ? [endIndex, startIndex] : [startIndex, endIndex];
    const rowsBetweenStartAndEnd = allPagesRowIds.slice(start2, end2 + 1);
    apiRef.current.selectRows(rowsBetweenStartAndEnd, isSelected, resetSelection);
  }, [apiRef, logger]);
  const selectionPublicApi = {
    selectRow,
    setRowSelectionModel,
    getSelectedRows,
    isRowSelected,
    isRowSelectable
  };
  const selectionPrivateApi = {
    selectRows,
    selectRowRange,
    getPropagatedRowSelectionModel
  };
  useGridApiMethod(apiRef, selectionPublicApi, "public");
  useGridApiMethod(apiRef, selectionPrivateApi, props.signature === GridSignature.DataGrid ? "private" : "public");
  const isFirstRender = React238.useRef(true);
  const removeOutdatedSelection = React238.useCallback(() => {
    var _a2, _b2;
    if (isFirstRender.current) {
      return;
    }
    const currentSelection = gridRowSelectionStateSelector(apiRef);
    const rowsLookup = gridRowsLookupSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const filteredRowsLookup = gridFilteredRowsLookupSelector(apiRef);
    const isNonExistent = (id) => {
      if (props.filterMode === "server") {
        return !rowsLookup[id];
      }
      return !rowTree[id] || filteredRowsLookup[id] === false;
    };
    const newSelectionModel = {
      type: currentSelection.type,
      ids: new Set(currentSelection.ids)
    };
    const selectionManager = createRowSelectionManager(newSelectionModel);
    let hasChanged = false;
    for (const id of currentSelection.ids) {
      if (isNonExistent(id)) {
        if (props.keepNonExistentRowsSelected) {
          continue;
        }
        selectionManager.unselect(id);
        hasChanged = true;
        continue;
      }
      if (!((_a2 = props.rowSelectionPropagation) == null ? void 0 : _a2.parents)) {
        continue;
      }
      const node = rowTree[id];
      if ((node == null ? void 0 : node.type) === "group") {
        const isAutoGenerated = node.isAutoGenerated;
        if (isAutoGenerated) {
          selectionManager.unselect(id);
          hasChanged = true;
          continue;
        }
        if (!node.children.every((childId) => filteredRowsLookup[childId] === false)) {
          selectionManager.unselect(id);
          hasChanged = true;
        }
      }
    }
    const shouldReapplyPropagation = isNestedData && ((_b2 = props.rowSelectionPropagation) == null ? void 0 : _b2.parents) && (newSelectionModel.ids.size > 0 || // In case of exclude selection, newSelectionModel.ids.size === 0 means all rows are selected
    newSelectionModel.type === "exclude");
    if (hasChanged || shouldReapplyPropagation) {
      if (shouldReapplyPropagation) {
        if (newSelectionModel.type === "exclude") {
          const unfilteredSelectedRowIds = getRowsToBeSelected();
          const selectedRowIds = [];
          for (let i2 = 0; i2 < unfilteredSelectedRowIds.length; i2 += 1) {
            const rowId = unfilteredSelectedRowIds[i2];
            if ((props.keepNonExistentRowsSelected || !isNonExistent(rowId)) && selectionManager.has(rowId)) {
              selectedRowIds.push(rowId);
            }
          }
          apiRef.current.selectRows(selectedRowIds, true, true);
        } else {
          apiRef.current.selectRows(Array.from(newSelectionModel.ids), true, true);
        }
      } else {
        apiRef.current.setRowSelectionModel(newSelectionModel, "multipleRowsSelection");
      }
    }
  }, [apiRef, isNestedData, (_i = props.rowSelectionPropagation) == null ? void 0 : _i.parents, props.keepNonExistentRowsSelected, props.filterMode, getRowsToBeSelected]);
  const handleSingleRowSelection = React238.useCallback((id, event) => {
    const hasCtrlKey = event.metaKey || event.ctrlKey;
    const isMultipleSelectionDisabled = !checkboxSelection && !hasCtrlKey && !isKeyboardEvent(event);
    const resetSelection = !canHaveMultipleSelection || isMultipleSelectionDisabled;
    const isSelected = apiRef.current.isRowSelected(id);
    const selectedRowsCount = gridRowSelectionCountSelector(apiRef);
    const shouldStaySelected = selectedRowsCount > 1 && resetSelection;
    const newSelectionState = shouldStaySelected || !isSelected;
    apiRef.current.selectRow(id, newSelectionState, resetSelection);
  }, [apiRef, canHaveMultipleSelection, checkboxSelection]);
  const handleRowClick = React238.useCallback((params, event) => {
    var _a2;
    if (disableRowSelectionOnClick) {
      return;
    }
    const field = (_a2 = event.target.closest(`.${gridClasses.cell}`)) == null ? void 0 : _a2.getAttribute("data-field");
    if (field === GRID_CHECKBOX_SELECTION_COL_DEF.field) {
      return;
    }
    if (field === GRID_DETAIL_PANEL_TOGGLE_FIELD) {
      return;
    }
    if (field) {
      const column = apiRef.current.getColumn(field);
      if ((column == null ? void 0 : column.type) === GRID_ACTIONS_COLUMN_TYPE) {
        return;
      }
    }
    const rowNode = gridRowNodeSelector(apiRef, params.id);
    if (rowNode.type === "pinnedRow") {
      return;
    }
    if (event.shiftKey && canHaveMultipleSelection) {
      expandMouseRowRangeSelection(params.id);
    } else {
      handleSingleRowSelection(params.id, event);
    }
  }, [disableRowSelectionOnClick, canHaveMultipleSelection, apiRef, expandMouseRowRangeSelection, handleSingleRowSelection]);
  const preventSelectionOnShift = React238.useCallback((params, event) => {
    var _a2;
    if (canHaveMultipleSelection && event.shiftKey) {
      (_a2 = window.getSelection()) == null ? void 0 : _a2.removeAllRanges();
    }
  }, [canHaveMultipleSelection]);
  const handleRowSelectionCheckboxChange = React238.useCallback((params, event) => {
    if (canHaveMultipleSelection && event.nativeEvent.shiftKey) {
      expandMouseRowRangeSelection(params.id);
    } else {
      apiRef.current.selectRow(params.id, params.value, !canHaveMultipleSelection);
    }
  }, [apiRef, expandMouseRowRangeSelection, canHaveMultipleSelection]);
  const toggleAllRows = React238.useCallback((value) => {
    var _a2;
    const filterModel = gridFilterModelSelector(apiRef);
    const quickFilterModel = gridQuickFilterValuesSelector(apiRef);
    const hasFilters = filterModel.items.length > 0 || (quickFilterModel == null ? void 0 : quickFilterModel.some((val) => val.length));
    if (!props.isRowSelectable && !props.checkboxSelectionVisibleOnly && (!isNestedData || ((_a2 = props.rowSelectionPropagation) == null ? void 0 : _a2.descendants)) && !hasFilters && !props.disableRowSelectionExcludeModel) {
      apiRef.current.setRowSelectionModel({
        type: value ? "exclude" : "include",
        ids: /* @__PURE__ */ new Set()
      }, "multipleRowsSelection");
    } else {
      apiRef.current.selectRows(getRowsToBeSelected(), value);
    }
  }, [apiRef, getRowsToBeSelected, props.checkboxSelectionVisibleOnly, props.isRowSelectable, (_j = props.rowSelectionPropagation) == null ? void 0 : _j.descendants, props.disableRowSelectionExcludeModel, isNestedData]);
  const handleHeaderSelectionCheckboxChange = React238.useCallback((params) => {
    toggleAllRows(params.value);
  }, [toggleAllRows]);
  const handleCellKeyDown = React238.useCallback((params, event) => {
    if (apiRef.current.getCellMode(params.id, params.field) === GridCellModes.Edit) {
      return;
    }
    if (isEventTargetInPortal(event)) {
      return;
    }
    if (isNavigationKey(event.key) && event.shiftKey) {
      const focusCell = gridFocusCellSelector(apiRef);
      if (focusCell && focusCell.id !== params.id) {
        event.preventDefault();
        const isNextRowSelected = apiRef.current.isRowSelected(focusCell.id);
        if (!canHaveMultipleSelection) {
          apiRef.current.selectRow(focusCell.id, !isNextRowSelected, true);
          return;
        }
        const newRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(focusCell.id);
        const previousRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(params.id);
        let start2;
        let end2;
        if (newRowIndex > previousRowIndex) {
          if (isNextRowSelected) {
            start2 = previousRowIndex;
            end2 = newRowIndex - 1;
          } else {
            start2 = previousRowIndex;
            end2 = newRowIndex;
          }
        } else {
          if (isNextRowSelected) {
            start2 = newRowIndex + 1;
            end2 = previousRowIndex;
          } else {
            start2 = newRowIndex;
            end2 = previousRowIndex;
          }
        }
        const visibleRows = getVisibleRows(apiRef);
        const rowsBetweenStartAndEnd = [];
        for (let i2 = start2; i2 <= end2; i2 += 1) {
          rowsBetweenStartAndEnd.push(visibleRows.rows[i2].id);
        }
        apiRef.current.selectRows(rowsBetweenStartAndEnd, !isNextRowSelected);
        return;
      }
    }
    if (event.key === " " && event.shiftKey) {
      event.preventDefault();
      handleSingleRowSelection(params.id, event);
      return;
    }
    if (String.fromCharCode(event.keyCode) === "A" && (event.ctrlKey || event.metaKey)) {
      event.preventDefault();
      if (canHaveMultipleSelection) {
        toggleAllRows(true);
      }
    }
  }, [apiRef, canHaveMultipleSelection, handleSingleRowSelection, toggleAllRows]);
  const syncControlledState = useEventCallback_default(() => {
    if (!props.rowSelection) {
      apiRef.current.setRowSelectionModel(emptyModel);
      return;
    }
    if (propRowSelectionModel === void 0) {
      return;
    }
    if (!applyAutoSelection || !isNestedData || propRowSelectionModel.type === "include" && propRowSelectionModel.ids.size === 0) {
      apiRef.current.setRowSelectionModel(propRowSelectionModel);
      return;
    }
    const newSelectionModel = apiRef.current.getPropagatedRowSelectionModel(propRowSelectionModel);
    if (newSelectionModel.type !== propRowSelectionModel.type || newSelectionModel.ids.size !== propRowSelectionModel.ids.size || !Array.from(propRowSelectionModel.ids).every((id) => newSelectionModel.ids.has(id))) {
      apiRef.current.setRowSelectionModel(newSelectionModel);
      return;
    }
    apiRef.current.setRowSelectionModel(propRowSelectionModel);
  });
  useGridEvent(apiRef, "filteredRowsSet", runIf(props.rowSelection, removeOutdatedSelection));
  useGridEvent(apiRef, "rowClick", runIf(props.rowSelection, handleRowClick));
  useGridEvent(apiRef, "rowSelectionCheckboxChange", runIf(props.rowSelection, handleRowSelectionCheckboxChange));
  useGridEvent(apiRef, "headerSelectionCheckboxChange", handleHeaderSelectionCheckboxChange);
  useGridEvent(apiRef, "cellMouseDown", runIf(props.rowSelection, preventSelectionOnShift));
  useGridEvent(apiRef, "cellKeyDown", runIf(props.rowSelection, handleCellKeyDown));
  React238.useEffect(() => {
    syncControlledState();
  }, [apiRef, propRowSelectionModel, props.rowSelection, syncControlledState]);
  const isStateControlled = propRowSelectionModel != null;
  React238.useEffect(() => {
    if (isStateControlled || !props.rowSelection || typeof isRowSelectable !== "function") {
      return;
    }
    const currentSelection = gridRowSelectionStateSelector(apiRef);
    if (currentSelection.type !== "include") {
      return;
    }
    const selectableIds = /* @__PURE__ */ new Set();
    for (const id of currentSelection.ids) {
      if (isRowSelectable(id)) {
        selectableIds.add(id);
      }
    }
    if (selectableIds.size < currentSelection.ids.size) {
      apiRef.current.setRowSelectionModel({
        type: currentSelection.type,
        ids: selectableIds
      });
    }
  }, [apiRef, isRowSelectable, isStateControlled, props.rowSelection]);
  React238.useEffect(() => {
    if (!props.rowSelection || isStateControlled) {
      return;
    }
    const currentSelection = gridRowSelectionStateSelector(apiRef);
    if (!canHaveMultipleSelection && (currentSelection.type === "include" && currentSelection.ids.size > 1 || currentSelection.type === "exclude")) {
      apiRef.current.setRowSelectionModel(emptyModel);
    }
  }, [apiRef, canHaveMultipleSelection, checkboxSelection, isStateControlled, props.rowSelection]);
  React238.useEffect(() => {
    runIf(props.rowSelection, removeOutdatedSelection);
  }, [props.rowSelection, removeOutdatedSelection]);
  React238.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
    }
  }, []);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rowSelection/useGridRowSelectionPreProcessors.js
var React239 = __toESM(require_react(), 1);
var useUtilityClasses93 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  return React239.useMemo(() => {
    const slots = {
      cellCheckbox: ["cellCheckbox"],
      columnHeaderCheckbox: ["columnHeaderCheckbox"]
    };
    return composeClasses(slots, getDataGridUtilityClass, classes2);
  }, [classes2]);
};
var useGridRowSelectionPreProcessors = (apiRef, props) => {
  const ownerState = {
    classes: props.classes
  };
  const classes2 = useUtilityClasses93(ownerState);
  const updateSelectionColumn = React239.useCallback((columnsState) => {
    const selectionColumn = _extends({}, GRID_CHECKBOX_SELECTION_COL_DEF, {
      cellClassName: classes2.cellCheckbox,
      headerClassName: classes2.columnHeaderCheckbox,
      headerName: apiRef.current.getLocaleText("checkboxSelectionHeaderName")
    });
    const shouldHaveSelectionColumn = props.checkboxSelection;
    const hasSelectionColumn = columnsState.lookup[GRID_CHECKBOX_SELECTION_FIELD] != null;
    if (shouldHaveSelectionColumn && !hasSelectionColumn) {
      columnsState.lookup[GRID_CHECKBOX_SELECTION_FIELD] = selectionColumn;
      columnsState.orderedFields = [GRID_CHECKBOX_SELECTION_FIELD, ...columnsState.orderedFields];
    } else if (!shouldHaveSelectionColumn && hasSelectionColumn) {
      delete columnsState.lookup[GRID_CHECKBOX_SELECTION_FIELD];
      columnsState.orderedFields = columnsState.orderedFields.filter((field) => field !== GRID_CHECKBOX_SELECTION_FIELD);
    } else if (shouldHaveSelectionColumn && hasSelectionColumn) {
      columnsState.lookup[GRID_CHECKBOX_SELECTION_FIELD] = _extends({}, selectionColumn, columnsState.lookup[GRID_CHECKBOX_SELECTION_FIELD]);
      if (!props.columns.some((col) => col.field === GRID_CHECKBOX_SELECTION_FIELD)) {
        columnsState.orderedFields = [GRID_CHECKBOX_SELECTION_FIELD, ...columnsState.orderedFields.filter((field) => field !== GRID_CHECKBOX_SELECTION_FIELD)];
      }
    }
    return columnsState;
  }, [apiRef, classes2, props.columns, props.checkboxSelection]);
  useGridRegisterPipeProcessor(apiRef, "hydrateColumns", updateSelectionColumn);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/sorting/useGridSorting.js
var React240 = __toESM(require_react(), 1);
var sortingStateInitializer = (state, props) => {
  var _a, _b;
  const sortModel = props.sortModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.sorting) == null ? void 0 : _b.sortModel) ?? [];
  return _extends({}, state, {
    sorting: {
      sortModel: sanitizeSortModel(sortModel, props.disableMultipleColumnsSorting),
      sortedRows: []
    }
  });
};
var useGridSorting = (apiRef, props) => {
  var _a, _b;
  const logger = useGridLogger(apiRef, "useGridSorting");
  apiRef.current.registerControlState({
    stateId: "sortModel",
    propModel: props.sortModel,
    propOnChange: props.onSortModelChange,
    stateSelector: gridSortModelSelector,
    changeEvent: "sortModelChange"
  });
  const upsertSortModel = React240.useCallback((field, sortItem) => {
    const sortModel = gridSortModelSelector(apiRef);
    const existingIdx = sortModel.findIndex((c) => c.field === field);
    let newSortModel = [...sortModel];
    if (existingIdx > -1) {
      if ((sortItem == null ? void 0 : sortItem.sort) == null) {
        newSortModel.splice(existingIdx, 1);
      } else {
        newSortModel.splice(existingIdx, 1, sortItem);
      }
    } else {
      newSortModel = [...sortModel, sortItem];
    }
    return newSortModel;
  }, [apiRef]);
  const createSortItem = React240.useCallback((col, directionOverride) => {
    const sortModel = gridSortModelSelector(apiRef);
    const existing = sortModel.find((c) => c.field === col.field);
    if (existing) {
      const nextSort = directionOverride === void 0 ? getNextGridSortDirection(col.sortingOrder ?? props.sortingOrder, existing.sort) : directionOverride;
      return nextSort === void 0 ? void 0 : _extends({}, existing, {
        sort: nextSort
      });
    }
    return {
      field: col.field,
      sort: directionOverride === void 0 ? getNextGridSortDirection(col.sortingOrder ?? props.sortingOrder) : directionOverride
    };
  }, [apiRef, props.sortingOrder]);
  const addColumnMenuItem = React240.useCallback((columnMenuItems, colDef) => {
    if (colDef == null || colDef.sortable === false || props.disableColumnSorting) {
      return columnMenuItems;
    }
    const sortingOrder = colDef.sortingOrder || props.sortingOrder;
    if (sortingOrder.some((item) => !!item)) {
      return [...columnMenuItems, "columnMenuSortItem"];
    }
    return columnMenuItems;
  }, [props.sortingOrder, props.disableColumnSorting]);
  const applySorting = React240.useCallback(() => {
    apiRef.current.setState((state) => {
      if (props.sortingMode === "server") {
        logger.debug("Skipping sorting rows as sortingMode = server");
        return _extends({}, state, {
          sorting: _extends({}, state.sorting, {
            sortedRows: getTreeNodeDescendants(gridRowTreeSelector(apiRef), GRID_ROOT_GROUP_ID, false)
          })
        });
      }
      const sortModel = gridSortModelSelector(apiRef);
      const sortRowList = buildAggregatedSortingApplier(sortModel, apiRef);
      const sortedRows = apiRef.current.applyStrategyProcessor("sorting", {
        sortRowList
      });
      return _extends({}, state, {
        sorting: _extends({}, state.sorting, {
          sortedRows
        })
      });
    });
    apiRef.current.publishEvent("sortedRowsSet");
  }, [apiRef, logger, props.sortingMode]);
  const setSortModel = React240.useCallback((model) => {
    const currentModel = gridSortModelSelector(apiRef);
    if (currentModel !== model) {
      logger.debug(`Setting sort model`);
      apiRef.current.setState(mergeStateWithSortModel(model, props.disableMultipleColumnsSorting));
      apiRef.current.applySorting();
    }
  }, [apiRef, logger, props.disableMultipleColumnsSorting]);
  const sortColumn = React240.useCallback((field, direction, allowMultipleSorting) => {
    const column = apiRef.current.getColumn(field);
    const sortItem = createSortItem(column, direction);
    let sortModel;
    if (!allowMultipleSorting || props.disableMultipleColumnsSorting) {
      sortModel = (sortItem == null ? void 0 : sortItem.sort) == null ? [] : [sortItem];
    } else {
      sortModel = upsertSortModel(column.field, sortItem);
    }
    apiRef.current.setSortModel(sortModel);
  }, [apiRef, upsertSortModel, createSortItem, props.disableMultipleColumnsSorting]);
  const getSortModel = React240.useCallback(() => gridSortModelSelector(apiRef), [apiRef]);
  const getSortedRows = React240.useCallback(() => {
    const sortedRows = gridSortedRowEntriesSelector(apiRef);
    return sortedRows.map((row) => row.model);
  }, [apiRef]);
  const getSortedRowIds = React240.useCallback(() => gridSortedRowIdsSelector(apiRef), [apiRef]);
  const getRowIdFromRowIndex = React240.useCallback((index) => apiRef.current.getSortedRowIds()[index], [apiRef]);
  const sortApi = {
    getSortModel,
    getSortedRows,
    getSortedRowIds,
    getRowIdFromRowIndex,
    setSortModel,
    sortColumn,
    applySorting
  };
  useGridApiMethod(apiRef, sortApi, "public");
  const stateExportPreProcessing = React240.useCallback((prevState, context) => {
    var _a2, _b2;
    const sortModelToExport = gridSortModelSelector(apiRef);
    const shouldExportSortModel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the model is controlled
      props.sortModel != null || // Always export if the model has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.sorting) == null ? void 0 : _b2.sortModel) != null || // Export if the model is not empty
      sortModelToExport.length > 0
    );
    if (!shouldExportSortModel) {
      return prevState;
    }
    return _extends({}, prevState, {
      sorting: {
        sortModel: sortModelToExport
      }
    });
  }, [apiRef, props.sortModel, (_b = (_a = props.initialState) == null ? void 0 : _a.sorting) == null ? void 0 : _b.sortModel]);
  const stateRestorePreProcessing = React240.useCallback((params, context) => {
    var _a2;
    const sortModel = (_a2 = context.stateToRestore.sorting) == null ? void 0 : _a2.sortModel;
    if (sortModel == null) {
      return params;
    }
    apiRef.current.setState(mergeStateWithSortModel(sortModel, props.disableMultipleColumnsSorting));
    return _extends({}, params, {
      callbacks: [...params.callbacks, apiRef.current.applySorting]
    });
  }, [apiRef, props.disableMultipleColumnsSorting]);
  const flatSortingMethod = React240.useCallback((params) => {
    const rowTree = gridRowTreeSelector(apiRef);
    const rootGroupNode = rowTree[GRID_ROOT_GROUP_ID];
    const sortedChildren = params.sortRowList ? params.sortRowList(rootGroupNode.children.map((childId) => rowTree[childId])) : [...rootGroupNode.children];
    if (rootGroupNode.footerId != null) {
      sortedChildren.push(rootGroupNode.footerId);
    }
    return sortedChildren;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  useGridRegisterStrategyProcessor(apiRef, GRID_DEFAULT_STRATEGY, "sorting", flatSortingMethod);
  const handleColumnHeaderClick = React240.useCallback(({
    field,
    colDef
  }, event) => {
    if (!colDef.sortable || props.disableColumnSorting) {
      return;
    }
    const allowMultipleSorting = props.multipleColumnsSortingMode === "always" || event.shiftKey || event.metaKey || event.ctrlKey;
    sortColumn(field, void 0, allowMultipleSorting);
  }, [sortColumn, props.disableColumnSorting, props.multipleColumnsSortingMode]);
  const handleColumnHeaderKeyDown = React240.useCallback(({
    field,
    colDef
  }, event) => {
    if (!colDef.sortable || props.disableColumnSorting) {
      return;
    }
    if (event.key === "Enter" && !event.ctrlKey && !event.metaKey) {
      sortColumn(field, void 0, props.multipleColumnsSortingMode === "always" || event.shiftKey);
    }
  }, [sortColumn, props.disableColumnSorting, props.multipleColumnsSortingMode]);
  const handleColumnsChange = React240.useCallback(() => {
    const sortModel = gridSortModelSelector(apiRef);
    const latestColumns = gridColumnLookupSelector(apiRef);
    if (sortModel.length > 0) {
      const newModel = sortModel.filter((sortItem) => latestColumns[sortItem.field]);
      if (newModel.length < sortModel.length) {
        apiRef.current.setSortModel(newModel);
      }
    }
  }, [apiRef]);
  const handleStrategyProcessorChange = React240.useCallback((methodName) => {
    if (methodName === "sorting") {
      apiRef.current.applySorting();
    }
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuItem);
  useGridEvent(apiRef, "columnHeaderClick", handleColumnHeaderClick);
  useGridEvent(apiRef, "columnHeaderKeyDown", handleColumnHeaderKeyDown);
  useGridEvent(apiRef, "rowsSet", apiRef.current.applySorting);
  useGridEvent(apiRef, "columnsChange", handleColumnsChange);
  useGridEvent(apiRef, "activeStrategyProcessorChange", handleStrategyProcessorChange);
  useFirstRender(() => {
    if (props.signature === "DataGrid") {
      apiRef.current.applySorting();
    }
  });
  useEnhancedEffect_default(() => {
    if (props.sortModel !== void 0) {
      apiRef.current.setSortModel(props.sortModel);
    }
  }, [apiRef, props.sortModel]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/scroll/useGridScroll.js
var React241 = __toESM(require_react(), 1);
function scrollIntoView(dimensions) {
  const {
    containerSize,
    scrollPosition,
    elementSize,
    elementOffset
  } = dimensions;
  const elementEnd = elementOffset + elementSize;
  if (elementSize > containerSize) {
    return elementOffset;
  }
  if (elementEnd - containerSize > scrollPosition) {
    return elementEnd - containerSize;
  }
  if (elementOffset < scrollPosition) {
    return elementOffset;
  }
  return void 0;
}
var useGridScroll = (apiRef, props) => {
  const isRtl = useRtl();
  const logger = useGridLogger(apiRef, "useGridScroll");
  const colRef = apiRef.current.columnHeadersContainerRef;
  const virtualScrollerRef = apiRef.current.virtualScrollerRef;
  const scrollToIndexes = React241.useCallback((params) => {
    var _a, _b, _c;
    const dimensions = gridDimensionsSelector(apiRef);
    const totalRowCount = gridRowCountSelector(apiRef);
    const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
    const scrollToHeader = params.rowIndex == null;
    if (!scrollToHeader && totalRowCount === 0 || visibleColumns.length === 0) {
      return false;
    }
    logger.debug(`Scrolling to cell at row ${params.rowIndex}, col: ${params.colIndex} `);
    let scrollCoordinates = {};
    if (params.colIndex !== void 0 && visibleColumns[params.colIndex]) {
      const columnPositions = gridColumnPositionsSelector(apiRef);
      let cellWidth;
      if (typeof params.rowIndex !== "undefined") {
        const visibleSortedRows = gridExpandedSortedRowEntriesSelector(apiRef);
        const rowId = (_a = visibleSortedRows[params.rowIndex]) == null ? void 0 : _a.id;
        const cellColSpanInfo = apiRef.current.unstable_getCellColSpanInfo(rowId, params.colIndex);
        if (cellColSpanInfo && !cellColSpanInfo.spannedByColSpan) {
          cellWidth = cellColSpanInfo.cellProps.width;
        }
      }
      if (typeof cellWidth === "undefined") {
        cellWidth = visibleColumns[params.colIndex].computedWidth;
      }
      scrollCoordinates.left = scrollIntoView({
        containerSize: dimensions.viewportOuterSize.width,
        scrollPosition: Math.abs(((_b = virtualScrollerRef.current) == null ? void 0 : _b.scrollLeft) ?? 0),
        elementSize: cellWidth,
        elementOffset: columnPositions[params.colIndex]
      });
    }
    if (params.rowIndex !== void 0) {
      const rowsMeta = gridRowsMetaSelector(apiRef);
      const page = gridPageSelector(apiRef);
      const pageSize2 = gridPageSizeSelector(apiRef);
      const elementIndex = !props.pagination ? params.rowIndex : params.rowIndex - page * pageSize2;
      const targetOffsetHeight = rowsMeta.positions[elementIndex + 1] ? rowsMeta.positions[elementIndex + 1] - rowsMeta.positions[elementIndex] : rowsMeta.currentPageTotalHeight - rowsMeta.positions[elementIndex];
      scrollCoordinates.top = scrollIntoView({
        containerSize: dimensions.viewportInnerSize.height,
        scrollPosition: ((_c = virtualScrollerRef.current) == null ? void 0 : _c.scrollTop) ?? 0,
        elementSize: targetOffsetHeight,
        elementOffset: rowsMeta.positions[elementIndex]
      });
    }
    scrollCoordinates = apiRef.current.unstable_applyPipeProcessors("scrollToIndexes", scrollCoordinates, params);
    if (typeof scrollCoordinates.left !== "undefined" || typeof scrollCoordinates.top !== "undefined") {
      apiRef.current.scroll(scrollCoordinates);
      return true;
    }
    return false;
  }, [logger, apiRef, virtualScrollerRef, props.pagination]);
  const scroll = React241.useCallback((params) => {
    if (virtualScrollerRef.current && params.left !== void 0 && colRef.current) {
      const direction = isRtl ? -1 : 1;
      colRef.current.scrollLeft = params.left;
      virtualScrollerRef.current.scrollLeft = direction * params.left;
      logger.debug(`Scrolling left: ${params.left}`);
    }
    if (virtualScrollerRef.current && params.top !== void 0) {
      virtualScrollerRef.current.scrollTop = params.top;
      logger.debug(`Scrolling top: ${params.top}`);
    }
    logger.debug(`Scrolling, updating container, and viewport`);
  }, [virtualScrollerRef, isRtl, colRef, logger]);
  const getScrollPosition = React241.useCallback(() => {
    if (!(virtualScrollerRef == null ? void 0 : virtualScrollerRef.current)) {
      return {
        top: 0,
        left: 0
      };
    }
    return {
      top: virtualScrollerRef.current.scrollTop,
      left: virtualScrollerRef.current.scrollLeft
    };
  }, [virtualScrollerRef]);
  const scrollApi = {
    scroll,
    scrollToIndexes,
    getScrollPosition
  };
  useGridApiMethod(apiRef, scrollApi, "public");
};

// node_modules/@mui/x-data-grid/esm/hooks/features/events/useGridEvents.js
function useGridEvents(apiRef, props) {
  useGridEventPriority(apiRef, "columnHeaderClick", props.onColumnHeaderClick);
  useGridEventPriority(apiRef, "columnHeaderContextMenu", props.onColumnHeaderContextMenu);
  useGridEventPriority(apiRef, "columnHeaderDoubleClick", props.onColumnHeaderDoubleClick);
  useGridEventPriority(apiRef, "columnHeaderOver", props.onColumnHeaderOver);
  useGridEventPriority(apiRef, "columnHeaderOut", props.onColumnHeaderOut);
  useGridEventPriority(apiRef, "columnHeaderEnter", props.onColumnHeaderEnter);
  useGridEventPriority(apiRef, "columnHeaderLeave", props.onColumnHeaderLeave);
  useGridEventPriority(apiRef, "cellClick", props.onCellClick);
  useGridEventPriority(apiRef, "cellDoubleClick", props.onCellDoubleClick);
  useGridEventPriority(apiRef, "cellKeyDown", props.onCellKeyDown);
  useGridEventPriority(apiRef, "preferencePanelClose", props.onPreferencePanelClose);
  useGridEventPriority(apiRef, "preferencePanelOpen", props.onPreferencePanelOpen);
  useGridEventPriority(apiRef, "menuOpen", props.onMenuOpen);
  useGridEventPriority(apiRef, "menuClose", props.onMenuClose);
  useGridEventPriority(apiRef, "rowDoubleClick", props.onRowDoubleClick);
  useGridEventPriority(apiRef, "rowClick", props.onRowClick);
  useGridEventPriority(apiRef, "stateChange", props.onStateChange);
}

// node_modules/@mui/x-data-grid/esm/hooks/features/dimensions/useGridDimensions.js
var React242 = __toESM(require_react(), 1);
var EMPTY_SIZE = {
  width: 0,
  height: 0
};
var EMPTY_DIMENSIONS2 = {
  isReady: false,
  root: EMPTY_SIZE,
  viewportOuterSize: EMPTY_SIZE,
  viewportInnerSize: EMPTY_SIZE,
  contentSize: EMPTY_SIZE,
  minimumSize: EMPTY_SIZE,
  hasScrollX: false,
  hasScrollY: false,
  scrollbarSize: 0,
  headerHeight: 0,
  groupHeaderHeight: 0,
  headerFilterHeight: 0,
  rowWidth: 0,
  rowHeight: 0,
  columnsTotalWidth: 0,
  leftPinnedWidth: 0,
  rightPinnedWidth: 0,
  headersTotalHeight: 0,
  topContainerHeight: 0,
  bottomContainerHeight: 0,
  autoHeight: false,
  minimalContentHeight: void 0
};
var dimensionsStateInitializer = (state, props, apiRef) => {
  const dimensions = EMPTY_DIMENSIONS2;
  const density = gridDensityFactorSelector(apiRef);
  const dimensionsWithStatic = _extends({}, dimensions, getStaticDimensions(props, apiRef, density, gridVisiblePinnedColumnDefinitionsSelector(apiRef)));
  apiRef.current.store.state.dimensions = dimensionsWithStatic;
  return _extends({}, state, {
    dimensions: dimensionsWithStatic
  });
};
var columnsTotalWidthSelector2 = createSelector3(gridVisibleColumnDefinitionsSelector, gridColumnPositionsSelector, (visibleColumns, positions) => {
  const colCount = visibleColumns.length;
  if (colCount === 0) {
    return 0;
  }
  return roundToDecimalPlaces(positions[colCount - 1] + visibleColumns[colCount - 1].computedWidth, 1);
});
function useGridDimensions(apiRef, props) {
  const getRootDimensions = React242.useCallback(() => gridDimensionsSelector(apiRef), [apiRef]);
  const apiPublic = {
    getRootDimensions
  };
  const apiPrivate = {
    updateDimensions: () => {
      return apiRef.current.virtualizer.api.updateDimensions();
    },
    getViewportPageSize: () => {
      return apiRef.current.virtualizer.api.getViewportPageSize();
    }
  };
  useGridApiMethod(apiRef, apiPublic, "public");
  useGridApiMethod(apiRef, apiPrivate, "private");
  const handleRootMount = (root) => {
    setCSSVariables(root, gridDimensionsSelector(apiRef));
  };
  useGridEventPriority(apiRef, "rootMount", handleRootMount);
  useGridEventPriority(apiRef, "debouncedResize", props.onResize);
  if (true) {
    const logger = useGridLogger(apiRef, "useResizeContainer");
    const errorShown = React242.useRef(false);
    useGridEventPriority(apiRef, "resize", (size) => {
      if (!getRootDimensions().isReady) {
        return;
      }
      if (size.height === 0 && !errorShown.current && !props.autoHeight && !isJSDOM2) {
        logger.error(["The parent DOM element of the Data Grid has an empty height.", "Please make sure that this element has an intrinsic height.", "The grid displays with a height of 0px.", "", "More details: https://mui.com/r/x-data-grid-no-dimensions."].join("\n"));
        errorShown.current = true;
      }
      if (size.width === 0 && !errorShown.current && !isJSDOM2) {
        logger.error(["The parent DOM element of the Data Grid has an empty width.", "Please make sure that this element has an intrinsic width.", "The grid displays with a width of 0px.", "", "More details: https://mui.com/r/x-data-grid-no-dimensions."].join("\n"));
        errorShown.current = true;
      }
    });
  }
  useStoreEffect(apiRef.current.store, (s) => s.dimensions, (previous, next) => {
    if (!next.isReady) {
      return;
    }
    if (apiRef.current.rootElementRef.current) {
      setCSSVariables(apiRef.current.rootElementRef.current, next);
    }
    if (!areElementSizesEqual(next.viewportInnerSize, previous.viewportInnerSize)) {
      apiRef.current.publishEvent("viewportInnerSizeChange", next.viewportInnerSize);
    }
    apiRef.current.publishEvent("debouncedResize", next.root);
  });
}
function setCSSVariables(root, dimensions) {
  const set = (k2, v) => root.style.setProperty(k2, v);
  set("--DataGrid-hasScrollX", `${Number(dimensions.hasScrollX)}`);
  set("--DataGrid-hasScrollY", `${Number(dimensions.hasScrollY)}`);
  set("--DataGrid-scrollbarSize", `${dimensions.scrollbarSize}px`);
  set("--DataGrid-rowWidth", `${dimensions.rowWidth}px`);
  set("--DataGrid-columnsTotalWidth", `${dimensions.columnsTotalWidth}px`);
  set("--DataGrid-leftPinnedWidth", `${dimensions.leftPinnedWidth}px`);
  set("--DataGrid-rightPinnedWidth", `${dimensions.rightPinnedWidth}px`);
  set("--DataGrid-headerHeight", `${dimensions.headerHeight}px`);
  set("--DataGrid-headersTotalHeight", `${dimensions.headersTotalHeight}px`);
  set("--DataGrid-topContainerHeight", `${dimensions.topContainerHeight}px`);
  set("--DataGrid-bottomContainerHeight", `${dimensions.bottomContainerHeight}px`);
  set("--height", `${dimensions.rowHeight}px`);
}
function getStaticDimensions(props, apiRef, density, pinnedColumnns) {
  const validRowHeight = getValidRowHeight(props.rowHeight, DATA_GRID_PROPS_DEFAULT_VALUES.rowHeight, rowHeightWarning);
  return {
    rowHeight: Math.floor(validRowHeight * density),
    headerHeight: Math.floor(props.columnHeaderHeight * density),
    groupHeaderHeight: Math.floor((props.columnGroupHeaderHeight ?? props.columnHeaderHeight) * density),
    headerFilterHeight: Math.floor((props.headerFilterHeight ?? props.columnHeaderHeight) * density),
    columnsTotalWidth: columnsTotalWidthSelector2(apiRef),
    headersTotalHeight: getTotalHeaderHeight(apiRef, props),
    leftPinnedWidth: pinnedColumnns.left.reduce((w, col) => w + col.computedWidth, 0),
    rightPinnedWidth: pinnedColumnns.right.reduce((w, col) => w + col.computedWidth, 0)
  };
}
function areElementSizesEqual(a, b) {
  return a.width === b.width && a.height === b.height;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/statePersistence/useGridStatePersistence.js
var React243 = __toESM(require_react(), 1);
var useGridStatePersistence = (apiRef) => {
  const exportState = React243.useCallback((params = {}) => {
    const stateToExport = apiRef.current.unstable_applyPipeProcessors("exportState", {}, params);
    return stateToExport;
  }, [apiRef]);
  const restoreState = React243.useCallback((stateToRestore) => {
    const response = apiRef.current.unstable_applyPipeProcessors("restoreState", {
      callbacks: []
    }, {
      stateToRestore
    });
    response.callbacks.forEach((callback) => {
      callback();
    });
  }, [apiRef]);
  const statePersistenceApi = {
    exportState,
    restoreState
  };
  useGridApiMethod(apiRef, statePersistenceApi, "public");
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columns/useGridColumnSpanning.js
var useGridColumnSpanning = (apiRef) => {
  const resetColSpan = () => {
    return apiRef.current.virtualizer.api.resetColSpan();
  };
  const getCellColSpanInfo = (...params) => {
    return apiRef.current.virtualizer.api.getCellColSpanInfo(...params);
  };
  const calculateColSpan = (...params) => {
    apiRef.current.virtualizer.api.calculateColSpan(...params);
  };
  const columnSpanningPublicApi = {
    unstable_getCellColSpanInfo: getCellColSpanInfo
  };
  const columnSpanningPrivateApi = {
    resetColSpan,
    calculateColSpan
  };
  useGridApiMethod(apiRef, columnSpanningPublicApi, "public");
  useGridApiMethod(apiRef, columnSpanningPrivateApi, "private");
  useGridEvent(apiRef, "columnOrderChange", resetColSpan);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columnGrouping/useGridColumnGrouping.js
var React244 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnGrouping/gridColumnGroupsUtils.js
var _excluded66 = ["groupId", "children"];
var createGroupLookup = (columnGroupingModel) => {
  const groupLookup = {};
  for (let i2 = 0; i2 < columnGroupingModel.length; i2 += 1) {
    const node = columnGroupingModel[i2];
    if (isLeaf(node)) {
      continue;
    }
    const {
      groupId,
      children: children2
    } = node, other = _objectWithoutPropertiesLoose(node, _excluded66);
    if (!groupId) {
      throw new Error("MUI X: An element of the columnGroupingModel does not have either `field` or `groupId`.");
    }
    if (!children2) {
      console.warn(`MUI X: group groupId=${groupId} has no children.`);
    }
    const groupParam = _extends({}, other, {
      groupId
    });
    const subTreeLookup = createGroupLookup(children2);
    if (subTreeLookup[groupId] !== void 0 || groupLookup[groupId] !== void 0) {
      throw new Error(`MUI X: The groupId ${groupId} is used multiple times in the columnGroupingModel.`);
    }
    Object.assign(groupLookup, subTreeLookup);
    groupLookup[groupId] = groupParam;
  }
  return groupLookup;
};
var recurrentUnwrapGroupingColumnModel = (columnGroupNode, parents, unwrappedGroupingModelToComplete) => {
  if (isLeaf(columnGroupNode)) {
    if (unwrappedGroupingModelToComplete[columnGroupNode.field] !== void 0) {
      throw new Error([`MUI X: columnGroupingModel contains duplicated field`, `column field ${columnGroupNode.field} occurs two times in the grouping model:`, `- ${unwrappedGroupingModelToComplete[columnGroupNode.field].join(" > ")}`, `- ${parents.join(" > ")}`].join("\n"));
    }
    unwrappedGroupingModelToComplete[columnGroupNode.field] = parents;
    return;
  }
  const {
    groupId,
    children: children2
  } = columnGroupNode;
  children2.forEach((child) => {
    recurrentUnwrapGroupingColumnModel(child, [...parents, groupId], unwrappedGroupingModelToComplete);
  });
};
var unwrapGroupingColumnModel = (columnGroupingModel) => {
  if (!columnGroupingModel) {
    return {};
  }
  const unwrappedSubTree = {};
  columnGroupingModel.forEach((columnGroupNode) => {
    recurrentUnwrapGroupingColumnModel(columnGroupNode, [], unwrappedSubTree);
  });
  return unwrappedSubTree;
};
var getColumnGroupsHeaderStructure = (orderedColumns, unwrappedGroupingModel, pinnedFields) => {
  const getParents = (field) => unwrappedGroupingModel[field] ?? [];
  const groupingHeaderStructure = [];
  const maxDepth = Math.max(0, ...orderedColumns.map((field) => getParents(field).length));
  const haveSameParents = (field1, field2, depth) => {
    const a = getParents(field1);
    const b = getParents(field2);
    for (let i2 = 0; i2 <= depth; i2 += 1) {
      if (a[i2] !== b[i2]) {
        return false;
      }
    }
    return true;
  };
  const haveDifferentContainers = (field1, field2) => {
    const left2 = pinnedFields == null ? void 0 : pinnedFields.left;
    const right2 = pinnedFields == null ? void 0 : pinnedFields.right;
    const inLeft1 = !!(left2 == null ? void 0 : left2.includes(field1));
    const inLeft2 = !!(left2 == null ? void 0 : left2.includes(field2));
    const inRight1 = !!(right2 == null ? void 0 : right2.includes(field1));
    const inRight2 = !!(right2 == null ? void 0 : right2.includes(field2));
    return inLeft1 !== inLeft2 || inRight1 !== inRight2;
  };
  for (let depth = 0; depth < maxDepth; depth += 1) {
    const depthStructure = [];
    for (let i2 = 0; i2 < orderedColumns.length; i2 += 1) {
      const field = orderedColumns[i2];
      const groupId = getParents(field)[depth] ?? null;
      if (depthStructure.length === 0) {
        depthStructure.push({
          columnFields: [field],
          groupId
        });
        continue;
      }
      const lastGroup = depthStructure[depthStructure.length - 1];
      const prevField = lastGroup.columnFields[lastGroup.columnFields.length - 1];
      if (lastGroup.groupId !== groupId || !haveSameParents(prevField, field, depth) || haveDifferentContainers(prevField, field)) {
        depthStructure.push({
          columnFields: [field],
          groupId
        });
      } else {
        lastGroup.columnFields.push(field);
      }
    }
    groupingHeaderStructure.push(depthStructure);
  }
  return groupingHeaderStructure;
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columnGrouping/useGridColumnGrouping.js
var columnGroupsStateInitializer = (state, props, apiRef) => {
  apiRef.current.caches.columnGrouping = {
    lastColumnGroupingModel: props.columnGroupingModel
  };
  if (!props.columnGroupingModel) {
    return _extends({}, state, {
      columnGrouping: void 0
    });
  }
  const columnFields = gridColumnFieldsSelector(apiRef);
  const visibleColumnFields = gridVisibleColumnFieldsSelector(apiRef);
  const groupLookup = createGroupLookup(props.columnGroupingModel ?? []);
  const unwrappedGroupingModel = unwrapGroupingColumnModel(props.columnGroupingModel ?? []);
  const columnGroupsHeaderStructure = getColumnGroupsHeaderStructure(columnFields, unwrappedGroupingModel, apiRef.current.state.pinnedColumns ?? {});
  const maxDepth = visibleColumnFields.length === 0 ? 0 : Math.max(...visibleColumnFields.map((field) => {
    var _a;
    return ((_a = unwrappedGroupingModel[field]) == null ? void 0 : _a.length) ?? 0;
  }));
  return _extends({}, state, {
    columnGrouping: {
      lookup: groupLookup,
      unwrappedGroupingModel,
      headerStructure: columnGroupsHeaderStructure,
      maxDepth
    }
  });
};
var useGridColumnGrouping = (apiRef, props) => {
  const getColumnGroupPath = React244.useCallback((field) => {
    const unwrappedGroupingModel = gridColumnGroupsUnwrappedModelSelector(apiRef);
    return unwrappedGroupingModel[field] ?? [];
  }, [apiRef]);
  const getAllGroupDetails = React244.useCallback(() => {
    const columnGroupLookup = gridColumnGroupsLookupSelector(apiRef);
    return columnGroupLookup;
  }, [apiRef]);
  const columnGroupingApi = {
    getColumnGroupPath,
    getAllGroupDetails
  };
  useGridApiMethod(apiRef, columnGroupingApi, "public");
  const handleColumnIndexChange = React244.useCallback(() => {
    const unwrappedGroupingModel = unwrapGroupingColumnModel(props.columnGroupingModel ?? []);
    apiRef.current.setState((state) => {
      var _a;
      const orderedFields = ((_a = state.columns) == null ? void 0 : _a.orderedFields) ?? [];
      const pinnedColumns = state.pinnedColumns ?? {};
      const columnGroupsHeaderStructure = getColumnGroupsHeaderStructure(orderedFields, unwrappedGroupingModel, pinnedColumns);
      return _extends({}, state, {
        columnGrouping: _extends({}, state.columnGrouping, {
          headerStructure: columnGroupsHeaderStructure
        })
      });
    });
  }, [apiRef, props.columnGroupingModel]);
  const updateColumnGroupingState = React244.useCallback((columnGroupingModel) => {
    var _a, _b;
    if (!columnGroupingModel && !apiRef.current.caches.columnGrouping.lastColumnGroupingModel) {
      return;
    }
    apiRef.current.caches.columnGrouping.lastColumnGroupingModel = columnGroupingModel;
    const pinnedColumns = ((_b = (_a = apiRef.current).getPinnedColumns) == null ? void 0 : _b.call(_a)) ?? {};
    const columnFields = gridColumnFieldsSelector(apiRef);
    const visibleColumnFields = gridVisibleColumnFieldsSelector(apiRef);
    const groupLookup = createGroupLookup(columnGroupingModel ?? []);
    const unwrappedGroupingModel = unwrapGroupingColumnModel(columnGroupingModel ?? []);
    const columnGroupsHeaderStructure = getColumnGroupsHeaderStructure(columnFields, unwrappedGroupingModel, pinnedColumns);
    const maxDepth = visibleColumnFields.length === 0 ? 0 : Math.max(...visibleColumnFields.map((field) => {
      var _a2;
      return ((_a2 = unwrappedGroupingModel[field]) == null ? void 0 : _a2.length) ?? 0;
    }));
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        columnGrouping: {
          lookup: groupLookup,
          unwrappedGroupingModel,
          headerStructure: columnGroupsHeaderStructure,
          maxDepth
        }
      });
    });
  }, [apiRef]);
  useGridEvent(apiRef, "columnIndexChange", handleColumnIndexChange);
  useGridEvent(apiRef, "columnsChange", () => {
    updateColumnGroupingState(props.columnGroupingModel);
  });
  useGridEvent(apiRef, "columnVisibilityModelChange", () => {
    updateColumnGroupingState(props.columnGroupingModel);
  });
  React244.useEffect(() => {
    if (props.columnGroupingModel === apiRef.current.caches.columnGrouping.lastColumnGroupingModel) {
      return;
    }
    updateColumnGroupingState(props.columnGroupingModel);
  }, [apiRef, updateColumnGroupingState, props.columnGroupingModel]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/columnResize/useGridColumnResize.js
var React245 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/utils/createControllablePromise.js
function createControllablePromise() {
  let resolve;
  let reject;
  const promise = new Promise((_resolve, _reject) => {
    resolve = _resolve;
    reject = _reject;
  });
  promise.resolve = resolve;
  promise.reject = reject;
  return promise;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/columnResize/useGridColumnResize.js
function trackFinger(event, currentTouchId) {
  if (currentTouchId !== void 0 && event.changedTouches) {
    for (let i2 = 0; i2 < event.changedTouches.length; i2 += 1) {
      const touch = event.changedTouches[i2];
      if (touch.identifier === currentTouchId) {
        return {
          x: touch.clientX,
          y: touch.clientY
        };
      }
    }
    return false;
  }
  return {
    x: event.clientX,
    y: event.clientY
  };
}
function computeNewWidth(initialOffsetToSeparator, clickX, columnBounds, resizeDirection) {
  let newWidth = initialOffsetToSeparator;
  if (resizeDirection === "Right") {
    newWidth += clickX - columnBounds.left;
  } else {
    newWidth += columnBounds.right - clickX;
  }
  return Math.round(newWidth);
}
function computeOffsetToSeparator(clickX, columnBounds, resizeDirection) {
  if (resizeDirection === "Left") {
    return clickX - columnBounds.left;
  }
  return columnBounds.right - clickX;
}
function flipResizeDirection(side) {
  if (side === "Right") {
    return "Left";
  }
  return "Right";
}
function getResizeDirection(separator, isRtl) {
  const side = separator.classList.contains(gridClasses["columnSeparator--sideRight"]) ? "Right" : "Left";
  if (isRtl) {
    return flipResizeDirection(side);
  }
  return side;
}
function getPinnedWidthProperty(isRtl, pinnedPosition) {
  if (pinnedPosition === GridPinnedColumnPosition.LEFT) {
    return isRtl ? "--DataGrid-rightPinnedWidth" : "--DataGrid-leftPinnedWidth";
  }
  return isRtl ? "--DataGrid-leftPinnedWidth" : "--DataGrid-rightPinnedWidth";
}
function getPinnedWidth(dimensions, isRtl, pinnedPosition) {
  if (pinnedPosition === GridPinnedColumnPosition.LEFT) {
    return isRtl ? dimensions.rightPinnedWidth : dimensions.leftPinnedWidth;
  }
  return isRtl ? dimensions.leftPinnedWidth : dimensions.rightPinnedWidth;
}
function preventClick(event) {
  event.preventDefault();
  event.stopImmediatePropagation();
}
function useColumnVirtualizationDisabled(apiRef) {
  const promise = React245.useRef(void 0);
  const selector = () => gridVirtualizationColumnEnabledSelector(apiRef);
  const value = useGridSelector(apiRef, selector);
  React245.useEffect(() => {
    if (promise.current && value === false) {
      promise.current.resolve();
      promise.current = void 0;
    }
  });
  const asyncCheck = () => {
    if (!promise.current) {
      if (selector() === false) {
        return Promise.resolve();
      }
      promise.current = createControllablePromise();
    }
    return promise.current;
  };
  return asyncCheck;
}
function excludeOutliers(inputValues, factor) {
  if (inputValues.length < 4) {
    return inputValues;
  }
  const values3 = inputValues.slice();
  values3.sort((a, b) => a - b);
  const q1 = values3[Math.floor(values3.length * 0.25)];
  const q3 = values3[Math.floor(values3.length * 0.75) - 1];
  const iqr = q3 - q1;
  const deviation = iqr < 5 ? 5 : iqr * factor;
  return values3.filter((v) => v > q1 - deviation && v < q3 + deviation);
}
function extractColumnWidths(apiRef, options3, columns) {
  const widthByField = {};
  const root = apiRef.current.rootElementRef.current;
  root.classList.add(gridClasses.autosizing);
  const includeHeaderFilters = options3.includeHeaderFilters && gridHeaderFilteringEnabledSelector(apiRef);
  columns.forEach((column) => {
    const cells = findGridCells(apiRef.current, column.field);
    const widths = cells.map((cell) => {
      return cell.getBoundingClientRect().width ?? 0;
    });
    const filteredWidths = options3.includeOutliers ? widths : excludeOutliers(widths, options3.outliersFactor);
    if (options3.includeHeaders) {
      const header = findGridHeader(apiRef.current, column.field);
      if (header) {
        const titleContainer = header.querySelector(`.${gridClasses.columnHeaderTitleContainer}`);
        const children2 = Array.from(titleContainer.children);
        const menuContainer = header.querySelector(`.${gridClasses.menuIcon}`);
        const titleContainerStyle = window.getComputedStyle(titleContainer, null);
        const gap = parseInt(titleContainerStyle.gap, 10) || 0;
        const headerStyle = window.getComputedStyle(header, null);
        const paddingWidth = parseInt(headerStyle.paddingLeft, 10) + parseInt(headerStyle.paddingRight, 10);
        let totalChildren = 0;
        let childrenWidth = 0;
        for (let i2 = 0; i2 < children2.length; i2 += 1) {
          const child = children2[i2];
          if (child.clientWidth > 0) {
            totalChildren += 1;
            childrenWidth += child.scrollWidth;
          }
        }
        childrenWidth += 1;
        const width = childrenWidth + gap * (totalChildren - 1) + paddingWidth + ((menuContainer == null ? void 0 : menuContainer.clientWidth) ?? 0);
        filteredWidths.push(width);
      }
    }
    if (includeHeaderFilters) {
      const headerFilter = findGridHeaderFilter(apiRef.current, column.field);
      if (headerFilter) {
        const style = window.getComputedStyle(headerFilter, null);
        const paddingWidth = parseInt(style.paddingLeft, 10) + parseInt(style.paddingRight, 10);
        const contentWidth = headerFilter.scrollWidth;
        const width = contentWidth + paddingWidth;
        filteredWidths.push(width);
      }
    }
    const hasColumnMin = column.minWidth !== -Infinity && column.minWidth !== void 0;
    const hasColumnMax = column.maxWidth !== Infinity && column.maxWidth !== void 0;
    const min2 = hasColumnMin ? column.minWidth : 0;
    const max2 = hasColumnMax ? column.maxWidth : Infinity;
    const maxContent = filteredWidths.length === 0 ? 0 : Math.max(...filteredWidths);
    widthByField[column.field] = clamp(maxContent, min2, max2);
  });
  root.classList.remove(gridClasses.autosizing);
  return widthByField;
}
var columnResizeStateInitializer = (state) => _extends({}, state, {
  columnResize: {
    resizingColumnField: ""
  }
});
function createResizeRefs() {
  return {
    colDef: void 0,
    initialColWidth: 0,
    initialTotalWidth: 0,
    previousMouseClickEvent: void 0,
    columnHeaderElement: void 0,
    headerFilterElement: void 0,
    groupHeaderElements: [],
    cellElements: [],
    leftPinnedCellsAfter: [],
    rightPinnedCellsBefore: [],
    fillerLeft: void 0,
    fillerRight: void 0,
    leftPinnedHeadersAfter: [],
    rightPinnedHeadersBefore: []
  };
}
var useGridColumnResize = (apiRef, props) => {
  const isRtl = useRtl();
  const logger = useGridLogger(apiRef, "useGridColumnResize");
  const refs = useLazyRef(createResizeRefs).current;
  const initialOffsetToSeparator = React245.useRef(null);
  const resizeDirection = React245.useRef(null);
  const stopResizeEventTimeout = useTimeout();
  const touchId = React245.useRef(void 0);
  const updateWidth = (newWidth) => {
    var _a, _b, _c, _d, _e, _f;
    logger.debug(`Updating width to ${newWidth} for col ${refs.colDef.field}`);
    const prevWidth = refs.columnHeaderElement.offsetWidth;
    const widthDiff = newWidth - prevWidth;
    const columnWidthDiff = newWidth - refs.initialColWidth;
    if (columnWidthDiff > 0) {
      const newTotalWidth = refs.initialTotalWidth + columnWidthDiff;
      (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.style.setProperty("--DataGrid-rowWidth", `${newTotalWidth}px`);
    }
    refs.colDef.computedWidth = newWidth;
    refs.colDef.width = newWidth;
    refs.colDef.flex = 0;
    refs.columnHeaderElement.style.width = `${newWidth}px`;
    const headerFilterElement = refs.headerFilterElement;
    if (headerFilterElement) {
      headerFilterElement.style.width = `${newWidth}px`;
    }
    refs.groupHeaderElements.forEach((element) => {
      const div = element;
      let finalWidth;
      if (div.getAttribute("aria-colspan") === "1") {
        finalWidth = `${newWidth}px`;
      } else {
        finalWidth = `${div.offsetWidth + widthDiff}px`;
      }
      div.style.width = finalWidth;
    });
    refs.cellElements.forEach((element) => {
      const div = element;
      let finalWidth;
      if (div.getAttribute("aria-colspan") === "1") {
        finalWidth = `${newWidth}px`;
      } else {
        finalWidth = `${div.offsetWidth + widthDiff}px`;
      }
      div.style.setProperty("--width", finalWidth);
    });
    const dimensions = gridDimensionsSelector(apiRef);
    const pinnedPosition = apiRef.current.unstable_applyPipeProcessors("isColumnPinned", false, refs.colDef.field);
    if (pinnedPosition === GridPinnedColumnPosition.LEFT) {
      updateProperty(refs.fillerLeft, "width", widthDiff);
      refs.leftPinnedCellsAfter.forEach((cell) => {
        updateProperty(cell, "left", widthDiff);
      });
      refs.leftPinnedHeadersAfter.forEach((header) => {
        updateProperty(header, "left", widthDiff);
      });
      (_d = (_c = apiRef.current.rootElementRef) == null ? void 0 : _c.current) == null ? void 0 : _d.style.setProperty(getPinnedWidthProperty(isRtl, pinnedPosition), `${getPinnedWidth(dimensions, isRtl, pinnedPosition) + columnWidthDiff}px`);
    }
    if (pinnedPosition === GridPinnedColumnPosition.RIGHT) {
      updateProperty(refs.fillerRight, "width", widthDiff);
      refs.rightPinnedCellsBefore.forEach((cell) => {
        updateProperty(cell, "right", widthDiff);
      });
      refs.rightPinnedHeadersBefore.forEach((header) => {
        updateProperty(header, "right", widthDiff);
      });
      (_f = (_e = apiRef.current.rootElementRef) == null ? void 0 : _e.current) == null ? void 0 : _f.style.setProperty(getPinnedWidthProperty(isRtl, pinnedPosition), `${getPinnedWidth(dimensions, isRtl, pinnedPosition) + columnWidthDiff}px`);
    }
  };
  const finishResize = (nativeEvent) => {
    stopListening();
    if (refs.previousMouseClickEvent) {
      const prevEvent = refs.previousMouseClickEvent;
      const prevTimeStamp = prevEvent.timeStamp;
      const prevClientX = prevEvent.clientX;
      const prevClientY = prevEvent.clientY;
      if (nativeEvent.timeStamp - prevTimeStamp < 300 && nativeEvent.clientX === prevClientX && nativeEvent.clientY === prevClientY) {
        refs.previousMouseClickEvent = void 0;
        apiRef.current.publishEvent("columnResizeStop", null, nativeEvent);
        return;
      }
    }
    if (refs.colDef) {
      apiRef.current.setColumnWidth(refs.colDef.field, refs.colDef.width);
      logger.debug(`Updating col ${refs.colDef.field} with new width: ${refs.colDef.width}`);
      const columnsState = gridColumnsStateSelector(apiRef);
      refs.groupHeaderElements.forEach((element) => {
        const fields = getFieldsFromGroupHeaderElem(element);
        const div = element;
        const newWidth = fields.reduce((acc, field) => {
          if (columnsState.columnVisibilityModel[field] !== false) {
            return acc + columnsState.lookup[field].computedWidth;
          }
          return acc;
        }, 0);
        const finalWidth = `${newWidth}px`;
        div.style.width = finalWidth;
      });
    }
    stopResizeEventTimeout.start(0, () => {
      apiRef.current.publishEvent("columnResizeStop", null, nativeEvent);
    });
  };
  const setCellElementsRef = () => {
    if (refs.columnHeaderElement) {
      refs.cellElements = findGridCellElementsFromCol(refs.columnHeaderElement, apiRef.current);
    }
  };
  const storeReferences = (colDef, separator, xStart) => {
    var _a;
    const root = apiRef.current.rootElementRef.current;
    refs.initialColWidth = colDef.computedWidth;
    refs.initialTotalWidth = apiRef.current.getRootDimensions().rowWidth;
    refs.colDef = colDef;
    refs.columnHeaderElement = findHeaderElementFromField(apiRef.current.columnHeadersContainerRef.current, colDef.field);
    const headerFilterElement = root.querySelector(`.${gridClasses.headerFilterRow} [data-field="${escapeOperandAttributeSelector(colDef.field)}"]`);
    if (headerFilterElement) {
      refs.headerFilterElement = headerFilterElement;
    }
    refs.groupHeaderElements = findGroupHeaderElementsFromField((_a = apiRef.current.columnHeadersContainerRef) == null ? void 0 : _a.current, colDef.field);
    setCellElementsRef();
    refs.fillerLeft = findGridElement(apiRef.current, isRtl ? "filler--pinnedRight" : "filler--pinnedLeft");
    refs.fillerRight = findGridElement(apiRef.current, isRtl ? "filler--pinnedLeft" : "filler--pinnedRight");
    const pinnedPosition = apiRef.current.unstable_applyPipeProcessors("isColumnPinned", false, refs.colDef.field);
    refs.leftPinnedCellsAfter = pinnedPosition !== GridPinnedColumnPosition.LEFT ? [] : findLeftPinnedCellsAfterCol(apiRef.current, refs.columnHeaderElement, isRtl);
    refs.rightPinnedCellsBefore = pinnedPosition !== GridPinnedColumnPosition.RIGHT ? [] : findRightPinnedCellsBeforeCol(apiRef.current, refs.columnHeaderElement, isRtl);
    refs.leftPinnedHeadersAfter = pinnedPosition !== GridPinnedColumnPosition.LEFT ? [] : findLeftPinnedHeadersAfterCol(apiRef.current, refs.columnHeaderElement, isRtl);
    refs.rightPinnedHeadersBefore = pinnedPosition !== GridPinnedColumnPosition.RIGHT ? [] : findRightPinnedHeadersBeforeCol(apiRef.current, refs.columnHeaderElement, isRtl);
    resizeDirection.current = getResizeDirection(separator, isRtl);
    initialOffsetToSeparator.current = computeOffsetToSeparator(xStart, refs.columnHeaderElement.getBoundingClientRect(), resizeDirection.current);
  };
  const handleResizeMouseUp = useEventCallback_default(finishResize);
  const handleResizeMouseMove = useEventCallback_default((nativeEvent) => {
    if (nativeEvent.buttons === 0) {
      handleResizeMouseUp(nativeEvent);
      return;
    }
    let newWidth = computeNewWidth(initialOffsetToSeparator.current, nativeEvent.clientX, refs.columnHeaderElement.getBoundingClientRect(), resizeDirection.current);
    newWidth = clamp(newWidth, refs.colDef.minWidth, refs.colDef.maxWidth);
    updateWidth(newWidth);
    const params = {
      element: refs.columnHeaderElement,
      colDef: refs.colDef,
      width: newWidth
    };
    apiRef.current.publishEvent("columnResize", params, nativeEvent);
  });
  const handleTouchEnd = useEventCallback_default((nativeEvent) => {
    const finger = trackFinger(nativeEvent, touchId.current);
    if (!finger) {
      return;
    }
    finishResize(nativeEvent);
  });
  const handleTouchMove = useEventCallback_default((nativeEvent) => {
    const finger = trackFinger(nativeEvent, touchId.current);
    if (!finger) {
      return;
    }
    if (nativeEvent.type === "mousemove" && nativeEvent.buttons === 0) {
      handleTouchEnd(nativeEvent);
      return;
    }
    let newWidth = computeNewWidth(initialOffsetToSeparator.current, finger.x, refs.columnHeaderElement.getBoundingClientRect(), resizeDirection.current);
    newWidth = clamp(newWidth, refs.colDef.minWidth, refs.colDef.maxWidth);
    updateWidth(newWidth);
    const params = {
      element: refs.columnHeaderElement,
      colDef: refs.colDef,
      width: newWidth
    };
    apiRef.current.publishEvent("columnResize", params, nativeEvent);
  });
  const handleTouchStart = useEventCallback_default((event) => {
    const cellSeparator = findParentElementFromClassName(event.target, gridClasses["columnSeparator--resizable"]);
    if (!cellSeparator) {
      return;
    }
    const touch = event.changedTouches[0];
    if (touch != null) {
      touchId.current = touch.identifier;
    }
    const columnHeaderElement = findParentElementFromClassName(event.target, gridClasses.columnHeader);
    const field = getFieldFromHeaderElem(columnHeaderElement);
    const colDef = apiRef.current.getColumn(field);
    logger.debug(`Start Resize on col ${colDef.field}`);
    apiRef.current.publishEvent("columnResizeStart", {
      field
    }, event);
    storeReferences(colDef, cellSeparator, touch.clientX);
    const doc = ownerDocument(event.currentTarget);
    doc.addEventListener("touchmove", handleTouchMove);
    doc.addEventListener("touchend", handleTouchEnd);
  });
  const stopListening = React245.useCallback(() => {
    const doc = ownerDocument(apiRef.current.rootElementRef.current);
    doc.body.style.removeProperty("cursor");
    doc.removeEventListener("mousemove", handleResizeMouseMove);
    doc.removeEventListener("mouseup", handleResizeMouseUp);
    doc.removeEventListener("touchmove", handleTouchMove);
    doc.removeEventListener("touchend", handleTouchEnd);
    setTimeout(() => {
      doc.removeEventListener("click", preventClick, true);
    }, 100);
    if (refs.columnHeaderElement) {
      refs.columnHeaderElement.style.pointerEvents = "unset";
    }
  }, [apiRef, refs, handleResizeMouseMove, handleResizeMouseUp, handleTouchMove, handleTouchEnd]);
  const handleResizeStart = React245.useCallback(({
    field
  }) => {
    apiRef.current.setState((state) => _extends({}, state, {
      columnResize: _extends({}, state.columnResize, {
        resizingColumnField: field
      })
    }));
  }, [apiRef]);
  const handleResizeStop = React245.useCallback(() => {
    apiRef.current.setState((state) => _extends({}, state, {
      columnResize: _extends({}, state.columnResize, {
        resizingColumnField: ""
      })
    }));
  }, [apiRef]);
  const handleColumnResizeMouseDown = useEventCallback_default(({
    colDef
  }, event) => {
    if (event.button !== 0) {
      return;
    }
    if (!event.currentTarget.classList.contains(gridClasses["columnSeparator--resizable"])) {
      return;
    }
    event.preventDefault();
    logger.debug(`Start Resize on col ${colDef.field}`);
    apiRef.current.publishEvent("columnResizeStart", {
      field: colDef.field
    }, event);
    storeReferences(colDef, event.currentTarget, event.clientX);
    const doc = ownerDocument(apiRef.current.rootElementRef.current);
    doc.body.style.cursor = "col-resize";
    refs.previousMouseClickEvent = event.nativeEvent;
    doc.addEventListener("mousemove", handleResizeMouseMove);
    doc.addEventListener("mouseup", handleResizeMouseUp);
    doc.addEventListener("click", preventClick, true);
  });
  const handleColumnSeparatorDoubleClick = useEventCallback_default((params, event) => {
    if (props.disableAutosize) {
      return;
    }
    if (event.button !== 0) {
      return;
    }
    const column = apiRef.current.state.columns.lookup[params.field];
    if (column.resizable === false) {
      return;
    }
    apiRef.current.autosizeColumns(_extends({}, props.autosizeOptions, {
      disableColumnVirtualization: false,
      columns: [column.field]
    }));
  });
  const columnVirtualizationDisabled = useColumnVirtualizationDisabled(apiRef);
  const isAutosizingRef = React245.useRef(false);
  const autosizeColumns = React245.useCallback(async (userOptions) => {
    var _a;
    const root = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current;
    if (!root) {
      return;
    }
    if (isAutosizingRef.current) {
      return;
    }
    isAutosizingRef.current = true;
    const state = gridColumnsStateSelector(apiRef);
    const options3 = _extends({}, DEFAULT_GRID_AUTOSIZE_OPTIONS, userOptions, {
      columns: (userOptions == null ? void 0 : userOptions.columns) ?? state.orderedFields
    });
    options3.columns = options3.columns.filter((c) => state.columnVisibilityModel[c] !== false);
    const columns = options3.columns.map((c) => apiRef.current.state.columns.lookup[c]);
    try {
      if (!props.disableVirtualization && options3.disableColumnVirtualization) {
        apiRef.current.unstable_setColumnVirtualization(false);
        await columnVirtualizationDisabled();
      }
      const widthByField = extractColumnWidths(apiRef, options3, columns);
      const newColumns = columns.map((column) => _extends({}, column, {
        width: widthByField[column.field],
        computedWidth: widthByField[column.field],
        flex: 0
      }));
      if (options3.expand) {
        const visibleColumns = state.orderedFields.map((field) => state.lookup[field]).filter((c) => state.columnVisibilityModel[c.field] !== false);
        const totalWidth = visibleColumns.reduce((total, column) => total + (widthByField[column.field] ?? column.computedWidth ?? column.width), 0);
        const dimensions = apiRef.current.getRootDimensions();
        const availableWidth = dimensions.viewportInnerSize.width;
        const remainingWidth = availableWidth - totalWidth;
        if (remainingWidth > 0) {
          const widthPerColumn = remainingWidth / (newColumns.length || 1);
          newColumns.forEach((column) => {
            column.width += widthPerColumn;
            column.computedWidth += widthPerColumn;
          });
        }
      }
      apiRef.current.updateColumns(newColumns);
      newColumns.forEach((newColumn, index) => {
        if (newColumn.width !== columns[index].width) {
          const width = newColumn.width;
          apiRef.current.publishEvent("columnWidthChange", {
            element: apiRef.current.getColumnHeaderElement(newColumn.field),
            colDef: newColumn,
            width
          });
        }
      });
    } finally {
      if (!props.disableVirtualization) {
        apiRef.current.unstable_setColumnVirtualization(true);
      }
      isAutosizingRef.current = false;
    }
  }, [apiRef, columnVirtualizationDisabled, props.disableVirtualization]);
  React245.useEffect(() => stopListening, [stopListening]);
  useOnMount(() => {
    if (props.autosizeOnMount) {
      Promise.resolve().then(() => {
        apiRef.current.autosizeColumns(props.autosizeOptions);
      });
    }
  });
  useGridNativeEventListener(apiRef, () => {
    var _a;
    return (_a = apiRef.current.columnHeadersContainerRef) == null ? void 0 : _a.current;
  }, "touchstart", handleTouchStart, {
    passive: true
  });
  useGridApiMethod(apiRef, {
    autosizeColumns
  }, "public");
  useGridEvent(apiRef, "columnResizeStop", handleResizeStop);
  useGridEvent(apiRef, "columnResizeStart", handleResizeStart);
  useGridEvent(apiRef, "columnSeparatorMouseDown", handleColumnResizeMouseDown);
  useGridEvent(apiRef, "columnSeparatorDoubleClick", handleColumnSeparatorDoubleClick);
  useGridEvent(apiRef, "rowsSet", () => {
    if (gridResizingColumnFieldSelector(apiRef) !== "") {
      requestAnimationFrame(() => {
        setCellElementsRef();
      });
    }
  });
  useGridEventPriority(apiRef, "columnResize", props.onColumnResize);
  useGridEventPriority(apiRef, "columnWidthChange", props.onColumnWidthChange);
};
function updateProperty(element, property, delta) {
  if (!element) {
    return;
  }
  element.style[property] = `${Math.round(parseFloat(element.style[property])) + delta}px`;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowSpanning.js
var React246 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/gridRowSpanningUtils.js
function getUnprocessedRange(testRange, processedRange) {
  if (testRange.firstRowIndex >= processedRange.firstRowIndex && testRange.lastRowIndex <= processedRange.lastRowIndex) {
    return null;
  }
  if (testRange.firstRowIndex >= processedRange.firstRowIndex && testRange.lastRowIndex > processedRange.lastRowIndex) {
    return {
      firstRowIndex: processedRange.lastRowIndex,
      lastRowIndex: testRange.lastRowIndex
    };
  }
  if (testRange.firstRowIndex < processedRange.firstRowIndex && testRange.lastRowIndex <= processedRange.lastRowIndex) {
    return {
      firstRowIndex: testRange.firstRowIndex,
      lastRowIndex: processedRange.firstRowIndex - 1
    };
  }
  return testRange;
}
function isRowContextInitialized(renderContext) {
  return renderContext.firstRowIndex !== 0 || renderContext.lastRowIndex !== 0;
}
var getCellValue = (row, colDef, apiRef) => {
  if (!row) {
    return null;
  }
  const cellValue = row[colDef.field];
  if (colDef.rowSpanValueGetter) {
    return colDef.rowSpanValueGetter(cellValue, row, colDef, apiRef);
  }
  return getRowValue(row, colDef, apiRef);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/rows/useGridRowSpanning.js
var EMPTY_CACHES2 = {
  spannedCells: {},
  hiddenCells: {},
  hiddenCellOriginMap: {}
};
var EMPTY_RANGE2 = {
  firstRowIndex: 0,
  lastRowIndex: 0
};
var EMPTY_STATE = {
  caches: EMPTY_CACHES2,
  processedRange: EMPTY_RANGE2
};
var computeRowSpanningState = (apiRef, colDefs, visibleRows, range, rangeToProcess, resetState) => {
  const virtualizer = apiRef.current.virtualizer;
  const previousState = resetState ? EMPTY_STATE : Rowspan.selectors.state(virtualizer.store.state);
  const spannedCells = _extends({}, previousState.caches.spannedCells);
  const hiddenCells = _extends({}, previousState.caches.hiddenCells);
  const hiddenCellOriginMap = _extends({}, previousState.caches.hiddenCellOriginMap);
  const processedRange = {
    firstRowIndex: Math.min(previousState.processedRange.firstRowIndex, rangeToProcess.firstRowIndex),
    lastRowIndex: Math.max(previousState.processedRange.lastRowIndex, rangeToProcess.lastRowIndex)
  };
  colDefs.forEach((colDef, columnIndex) => {
    var _a;
    for (let index = rangeToProcess.firstRowIndex; index < rangeToProcess.lastRowIndex; index += 1) {
      const row = visibleRows[index];
      if ((_a = hiddenCells[row.id]) == null ? void 0 : _a[columnIndex]) {
        continue;
      }
      const cellValue = getCellValue(row.model, colDef, apiRef);
      if (cellValue == null) {
        continue;
      }
      let spannedRowId = row.id;
      let spannedRowIndex = index;
      let rowSpan = 0;
      const backwardsHiddenCells = [];
      if (index === rangeToProcess.firstRowIndex) {
        let prevIndex = index - 1;
        let prevRowEntry = visibleRows[prevIndex];
        while (prevIndex >= range.firstRowIndex && prevRowEntry && getCellValue(prevRowEntry.model, colDef, apiRef) === cellValue) {
          const currentRow = visibleRows[prevIndex + 1];
          if (hiddenCells[currentRow.id]) {
            hiddenCells[currentRow.id][columnIndex] = true;
          } else {
            hiddenCells[currentRow.id] = {
              [columnIndex]: true
            };
          }
          backwardsHiddenCells.push(index);
          rowSpan += 1;
          spannedRowId = prevRowEntry.id;
          spannedRowIndex = prevIndex;
          prevIndex -= 1;
          prevRowEntry = visibleRows[prevIndex];
        }
      }
      backwardsHiddenCells.forEach((hiddenCellIndex) => {
        if (hiddenCellOriginMap[hiddenCellIndex]) {
          hiddenCellOriginMap[hiddenCellIndex][columnIndex] = spannedRowIndex;
        } else {
          hiddenCellOriginMap[hiddenCellIndex] = {
            [columnIndex]: spannedRowIndex
          };
        }
      });
      let relativeIndex = index + 1;
      while (relativeIndex <= range.lastRowIndex && visibleRows[relativeIndex] && getCellValue(visibleRows[relativeIndex].model, colDef, apiRef) === cellValue) {
        const currentRow = visibleRows[relativeIndex];
        if (hiddenCells[currentRow.id]) {
          hiddenCells[currentRow.id][columnIndex] = true;
        } else {
          hiddenCells[currentRow.id] = {
            [columnIndex]: true
          };
        }
        if (hiddenCellOriginMap[relativeIndex]) {
          hiddenCellOriginMap[relativeIndex][columnIndex] = spannedRowIndex;
        } else {
          hiddenCellOriginMap[relativeIndex] = {
            [columnIndex]: spannedRowIndex
          };
        }
        relativeIndex += 1;
        rowSpan += 1;
      }
      if (rowSpan > 0) {
        if (spannedCells[spannedRowId]) {
          spannedCells[spannedRowId][columnIndex] = rowSpan + 1;
        } else {
          spannedCells[spannedRowId] = {
            [columnIndex]: rowSpan + 1
          };
        }
      }
    }
  });
  return {
    caches: {
      spannedCells,
      hiddenCells,
      hiddenCellOriginMap
    },
    processedRange
  };
};
var rowSpanningStateInitializer = (state) => {
  return _extends({}, state, {
    rowSpanning: EMPTY_STATE
  });
};
var useGridRowSpanning = (apiRef, props) => {
  const updateRowSpanningState = React246.useCallback((renderContext, resetState = false) => {
    const store = apiRef.current.virtualizer.store;
    const {
      range,
      rows: visibleRows
    } = getVisibleRows(apiRef);
    if (resetState) {
      store.set("rowSpanning", EMPTY_STATE);
    }
    if (range === null || !isRowContextInitialized(renderContext)) {
      return;
    }
    const previousState = resetState ? EMPTY_STATE : Rowspan.selectors.state(store.state);
    const rangeToProcess = getUnprocessedRange({
      firstRowIndex: renderContext.firstRowIndex,
      lastRowIndex: Math.min(renderContext.lastRowIndex, range.lastRowIndex - range.firstRowIndex + 1)
    }, previousState.processedRange);
    if (rangeToProcess === null) {
      return;
    }
    const colDefs = gridVisibleColumnDefinitionsSelector(apiRef);
    const newState = computeRowSpanningState(apiRef, colDefs, visibleRows, range, rangeToProcess, resetState);
    const newSpannedCellsCount = Object.keys(newState.caches.spannedCells).length;
    const newHiddenCellsCount = Object.keys(newState.caches.hiddenCells).length;
    const previousSpannedCellsCount = Object.keys(previousState.caches.spannedCells).length;
    const previousHiddenCellsCount = Object.keys(previousState.caches.hiddenCells).length;
    const shouldUpdateState = resetState || newSpannedCellsCount !== previousSpannedCellsCount || newHiddenCellsCount !== previousHiddenCellsCount;
    const hasNoSpannedCells = newSpannedCellsCount === 0 && previousSpannedCellsCount === 0;
    if (!shouldUpdateState || hasNoSpannedCells) {
      return;
    }
    store.set("rowSpanning", newState);
  }, [apiRef]);
  const {
    schedule: deferredUpdateRowSpanningState,
    cancel
  } = useRunOncePerLoop(updateRowSpanningState);
  const resetRowSpanningState = React246.useCallback(() => {
    const renderContext = gridRenderContextSelector(apiRef);
    if (!isRowContextInitialized(renderContext)) {
      return;
    }
    deferredUpdateRowSpanningState(renderContext, true);
  }, [apiRef, deferredUpdateRowSpanningState]);
  useGridEvent(apiRef, "renderedRowsIntervalChange", runIf(props.rowSpanning, (renderContext) => {
    const didHavePendingReset = cancel();
    updateRowSpanningState(renderContext, didHavePendingReset);
  }));
  useGridEvent(apiRef, "sortedRowsSet", runIf(props.rowSpanning, resetRowSpanningState));
  useGridEvent(apiRef, "paginationModelChange", runIf(props.rowSpanning, resetRowSpanningState));
  useGridEvent(apiRef, "filteredRowsSet", runIf(props.rowSpanning, resetRowSpanningState));
  useGridEvent(apiRef, "columnsChange", runIf(props.rowSpanning, resetRowSpanningState));
  useGridEvent(apiRef, "rowExpansionChange", runIf(props.rowSpanning, resetRowSpanningState));
  React246.useEffect(() => {
    var _a;
    const store = (_a = apiRef.current.virtualizer) == null ? void 0 : _a.store;
    if (!store) {
      return;
    }
    if (!props.rowSpanning) {
      if (store.state.rowSpanning !== EMPTY_STATE) {
        store.set("rowSpanning", EMPTY_STATE);
      }
    } else if (store.state.rowSpanning === EMPTY_STATE) {
      updateRowSpanningState(gridRenderContextSelector(apiRef));
    }
  }, [apiRef, props.rowSpanning, updateRowSpanningState]);
};

// node_modules/@mui/x-data-grid/esm/hooks/features/listView/useGridListView.js
var React247 = __toESM(require_react(), 1);
var listViewStateInitializer = (state, props, apiRef) => _extends({}, state, {
  listViewColumn: props.listViewColumn ? _extends({}, props.listViewColumn, {
    computedWidth: getListColumnWidth(apiRef)
  }) : void 0
});
function useGridListView(apiRef, props) {
  const updateListColumnWidth = () => {
    apiRef.current.setState((state) => {
      if (!state.listViewColumn) {
        return state;
      }
      return _extends({}, state, {
        listViewColumn: _extends({}, state.listViewColumn, {
          computedWidth: getListColumnWidth(apiRef)
        })
      });
    });
  };
  const prevInnerWidth = React247.useRef(null);
  const handleGridSizeChange = (viewportInnerSize) => {
    if (prevInnerWidth.current !== viewportInnerSize.width) {
      prevInnerWidth.current = viewportInnerSize.width;
      updateListColumnWidth();
    }
  };
  useGridEvent(apiRef, "viewportInnerSizeChange", handleGridSizeChange);
  useGridEvent(apiRef, "columnVisibilityModelChange", updateListColumnWidth);
  useEnhancedEffect_default(() => {
    const listColumn = props.listViewColumn;
    if (listColumn) {
      apiRef.current.setState((state) => {
        return _extends({}, state, {
          listViewColumn: _extends({}, listColumn, {
            computedWidth: getListColumnWidth(apiRef)
          })
        });
      });
    }
  }, [apiRef, props.listViewColumn]);
  React247.useEffect(() => {
    if (props.listView && !props.listViewColumn) {
      warnOnce(["MUI X: The `listViewColumn` prop must be set if `listView` is enabled.", 'To fix, pass a column definition to the `listViewColumn` prop, e.g. `{ field: "example", renderCell: (params) => <div>{params.row.id}</div> }`.', "For more details, see https://mui.com/x/react-data-grid/list-view/"]);
    }
  }, [props.listView, props.listViewColumn]);
}
function getListColumnWidth(apiRef) {
  return gridDimensionsSelector(apiRef).viewportInnerSize.width;
}

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/useGridDataSource.js
var React249 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/useGridDataSourceBase.js
var React248 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/gridDataSourceSelector.js
var gridGetRowsParamsSelector = createSelector3(gridFilterModelSelector, gridSortModelSelector, gridPaginationModelSelector, (filterModel, sortModel, paginationModel) => ({
  groupKeys: [],
  paginationModel,
  sortModel,
  filterModel,
  start: paginationModel.page * paginationModel.pageSize,
  end: paginationModel.page * paginationModel.pageSize + paginationModel.pageSize - 1
}));

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/utils.js
var DataSourceRowsUpdateStrategy = (function(DataSourceRowsUpdateStrategy2) {
  DataSourceRowsUpdateStrategy2["Default"] = "set-flat-rows";
  DataSourceRowsUpdateStrategy2["LazyLoading"] = "replace-row-range";
  DataSourceRowsUpdateStrategy2["GroupedData"] = "set-grouped-rows";
  return DataSourceRowsUpdateStrategy2;
})({});
var CacheChunkManager = class {
  /**
   * @param chunkSize The number of rows to store in each cache entry.
   * If not set, the whole array will be stored in a single cache entry.
   * Setting this value to smallest page size will result in better cache hit rate.
   * Has no effect if cursor pagination is used.
   */
  constructor(chunkSize) {
    __publicField(this, "getCacheKeys", (key) => {
      if (this.chunkSize < 1 || typeof key.start !== "number") {
        return [key];
      }
      const chunkedKeys = [];
      for (let i2 = key.start; i2 <= key.end; i2 += this.chunkSize) {
        const end2 = Math.min(i2 + this.chunkSize - 1, key.end);
        chunkedKeys.push(_extends({}, key, {
          start: i2,
          end: end2
        }));
      }
      return chunkedKeys;
    });
    __publicField(this, "splitResponse", (key, response) => {
      const cacheKeys = this.getCacheKeys(key);
      if (cacheKeys.length === 1) {
        return /* @__PURE__ */ new Map([[key, response]]);
      }
      const responses = /* @__PURE__ */ new Map();
      cacheKeys.forEach((chunkKey) => {
        var _a, _b, _c, _d;
        const isLastChunk = chunkKey.end === key.end;
        const responseSlice = _extends({}, response, {
          pageInfo: _extends({}, response.pageInfo, {
            // If the original response had page info, update that information for all but last chunk and keep the original value for the last chunk
            hasNextPage: ((_a = response.pageInfo) == null ? void 0 : _a.hasNextPage) !== void 0 && !isLastChunk ? true : (_b = response.pageInfo) == null ? void 0 : _b.hasNextPage,
            nextCursor: ((_c = response.pageInfo) == null ? void 0 : _c.nextCursor) !== void 0 && !isLastChunk ? response.rows[chunkKey.end + 1].id : (_d = response.pageInfo) == null ? void 0 : _d.nextCursor
          }),
          rows: typeof chunkKey.start !== "number" || typeof key.start !== "number" ? response.rows : response.rows.slice(chunkKey.start - key.start, chunkKey.end - key.start + 1)
        });
        responses.set(chunkKey, responseSlice);
      });
      return responses;
    });
    this.chunkSize = chunkSize;
  }
};
__publicField(CacheChunkManager, "mergeResponses", (responses) => {
  if (responses.length === 1) {
    return responses[0];
  }
  return responses.reduce((acc, response) => _extends({}, response, {
    rows: [...acc.rows, ...response.rows]
  }), {
    rows: [],
    rowCount: 0,
    pageInfo: {}
  });
});

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/useGridDataSourceBase.js
var _excluded67 = ["skipCache", "keepChildrenExpanded"];
var noopCache = {
  clear: () => {
  },
  get: () => void 0,
  set: () => {
  }
};
function getCache(cacheProp, options3 = {}) {
  if (cacheProp === null) {
    return noopCache;
  }
  return cacheProp ?? new GridDataSourceCacheDefault(options3);
}
var useGridDataSourceBase = (apiRef, props, options3 = {}) => {
  var _a, _b;
  const setStrategyAvailability2 = React248.useCallback(() => {
    apiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, DataSourceRowsUpdateStrategy.Default, props.dataSource ? () => true : () => false);
  }, [apiRef, props.dataSource]);
  const [currentStrategy, setCurrentStrategy] = React248.useState(apiRef.current.getActiveStrategy(GridStrategyGroup.DataSource));
  const standardRowsUpdateStrategyActive = React248.useMemo(() => {
    return currentStrategy === DataSourceRowsUpdateStrategy.Default || currentStrategy === DataSourceRowsUpdateStrategy.GroupedData;
  }, [currentStrategy]);
  const paginationModel = useGridSelector(apiRef, gridPaginationModelSelector);
  const lastRequestId = React248.useRef(0);
  const pollingIntervalRef = React248.useRef(null);
  const onDataSourceErrorProp = props.onDataSourceError;
  const revalidateMs = props.dataSourceRevalidateMs;
  const cacheChunkManager = useLazyRef(() => {
    if (!props.pagination) {
      return new CacheChunkManager(paginationModel.pageSize);
    }
    const sortedPageSizeOptions = props.pageSizeOptions.map((option) => typeof option === "number" ? option : option.value).sort((a, b) => a - b);
    const cacheChunkSize = Math.min(paginationModel.pageSize, sortedPageSizeOptions[0]);
    return new CacheChunkManager(cacheChunkSize);
  }).current;
  const [cache, setCache] = React248.useState(() => getCache(props.dataSourceCache, options3.cacheOptions));
  const fetchRows = React248.useCallback(async (parentId, params) => {
    var _a2, _b2, _c;
    const getRows = (_a2 = props.dataSource) == null ? void 0 : _a2.getRows;
    if (!getRows) {
      return;
    }
    if (parentId && parentId !== GRID_ROOT_GROUP_ID && props.signature !== "DataGrid") {
      (_b2 = options3.fetchRowChildren) == null ? void 0 : _b2.call(options3, [parentId]);
      return;
    }
    (_c = options3.clearDataSourceState) == null ? void 0 : _c.call(options3);
    const _ref = params || {}, {
      skipCache,
      keepChildrenExpanded
    } = _ref, getRowsParams = _objectWithoutPropertiesLoose(_ref, _excluded67);
    const fetchParams = _extends({}, gridGetRowsParamsSelector(apiRef), apiRef.current.unstable_applyPipeProcessors("getRowsParams", {}), getRowsParams);
    const cacheKeys = cacheChunkManager.getCacheKeys(fetchParams);
    const responses = cacheKeys.map((cacheKey) => cache.get(cacheKey));
    if (!skipCache && responses.every((response) => response !== void 0)) {
      apiRef.current.applyStrategyProcessor("dataSourceRowsUpdate", {
        response: CacheChunkManager.mergeResponses(responses),
        fetchParams,
        options: {
          skipCache,
          keepChildrenExpanded
        }
      });
      return;
    }
    if (standardRowsUpdateStrategyActive || apiRef.current.getRowsCount() === 0) {
      apiRef.current.setLoading(true);
    }
    const requestId = lastRequestId.current + 1;
    lastRequestId.current = requestId;
    try {
      const getRowsResponse = await getRows(fetchParams);
      const cacheResponses = cacheChunkManager.splitResponse(fetchParams, getRowsResponse);
      cacheResponses.forEach((response, key) => cache.set(key, response));
      if (lastRequestId.current === requestId) {
        apiRef.current.applyStrategyProcessor("dataSourceRowsUpdate", {
          response: getRowsResponse,
          fetchParams,
          options: {
            skipCache,
            keepChildrenExpanded
          }
        });
      }
    } catch (originalError) {
      if (lastRequestId.current === requestId) {
        apiRef.current.applyStrategyProcessor("dataSourceRowsUpdate", {
          error: originalError,
          fetchParams,
          options: {
            skipCache,
            keepChildrenExpanded
          }
        });
        if (typeof onDataSourceErrorProp === "function") {
          onDataSourceErrorProp(new GridGetRowsError({
            message: originalError == null ? void 0 : originalError.message,
            params: fetchParams,
            cause: originalError
          }));
        } else {
          warnOnce(["MUI X: A call to `dataSource.getRows()` threw an error which was not handled because `onDataSourceError()` is missing.", "To handle the error pass a callback to the `onDataSourceError` prop, for example `<DataGrid onDataSourceError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/server-side-data/#error-handling."], "error");
        }
      }
    } finally {
      if (standardRowsUpdateStrategyActive && lastRequestId.current === requestId) {
        apiRef.current.setLoading(false);
      }
    }
  }, [cacheChunkManager, cache, apiRef, standardRowsUpdateStrategyActive, (_a = props.dataSource) == null ? void 0 : _a.getRows, onDataSourceErrorProp, options3, props.signature]);
  const handleStrategyActivityChange = React248.useCallback(() => {
    setCurrentStrategy(apiRef.current.getActiveStrategy(GridStrategyGroup.DataSource));
  }, [apiRef]);
  const fetchRowChildrenOption = options3.fetchRowChildren;
  const revalidate = useEventCallback_default(async () => {
    var _a2;
    const getRows = (_a2 = props.dataSource) == null ? void 0 : _a2.getRows;
    if (!getRows || !standardRowsUpdateStrategyActive) {
      return;
    }
    const revalidateExpandedGroups = () => {
      if (currentStrategy !== DataSourceRowsUpdateStrategy.GroupedData || !fetchRowChildrenOption) {
        return;
      }
      const rowTree = gridRowTreeSelector(apiRef);
      const visibleRows = gridVisibleRowsSelector(apiRef).rows;
      const expandedGroupIds = visibleRows.reduce((acc, row) => {
        const node = rowTree[row.id];
        if (node.type === "group" && node.id !== GRID_ROOT_GROUP_ID && node.childrenExpanded === true) {
          acc.push(row.id);
        }
        return acc;
      }, []);
      if (expandedGroupIds.length > 0) {
        fetchRowChildrenOption(expandedGroupIds, {
          showChildrenLoading: false
        });
      }
    };
    const fetchParams = _extends({}, gridGetRowsParamsSelector(apiRef), apiRef.current.unstable_applyPipeProcessors("getRowsParams", {}));
    const cacheKeys = cacheChunkManager.getCacheKeys(fetchParams);
    const responses = cacheKeys.map((cacheKey) => cache.get(cacheKey));
    if (responses.every((response) => response !== void 0)) {
      revalidateExpandedGroups();
      return;
    }
    try {
      const response = await getRows(fetchParams);
      const currentParams = _extends({}, gridGetRowsParamsSelector(apiRef), apiRef.current.unstable_applyPipeProcessors("getRowsParams", {}));
      if (!isDeepEqual(fetchParams, currentParams)) {
        return;
      }
      const cacheResponses = cacheChunkManager.splitResponse(fetchParams, response);
      cacheResponses.forEach((cacheResponse, key) => cache.set(key, cacheResponse));
      apiRef.current.applyStrategyProcessor("dataSourceRowsUpdate", {
        response,
        fetchParams,
        options: {}
      });
      revalidateExpandedGroups();
    } catch {
    }
  });
  const stopPolling = React248.useCallback(() => {
    if (pollingIntervalRef.current !== null) {
      clearInterval(pollingIntervalRef.current);
      pollingIntervalRef.current = null;
    }
  }, []);
  const startPolling = useEventCallback_default(() => {
    stopPolling();
    if (revalidateMs <= 0 || !standardRowsUpdateStrategyActive) {
      return;
    }
    pollingIntervalRef.current = setInterval(revalidate, revalidateMs);
  });
  const handleDataUpdate = React248.useCallback((params) => {
    if ("error" in params) {
      apiRef.current.setRows([]);
      return;
    }
    const {
      response
    } = params;
    if (response.rowCount !== void 0) {
      apiRef.current.setRowCount(response.rowCount);
    }
    apiRef.current.setRows(response.rows);
    apiRef.current.unstable_applyPipeProcessors("processDataSourceRows", {
      params: params.fetchParams,
      response
    }, true);
    startPolling();
  }, [apiRef, startPolling]);
  const dataSourceUpdateRow = (_b = props.dataSource) == null ? void 0 : _b.updateRow;
  const handleEditRowOption = options3.handleEditRow;
  const editRow = React248.useCallback(async (params) => {
    if (!dataSourceUpdateRow) {
      return void 0;
    }
    try {
      const finalRowUpdate = await dataSourceUpdateRow(params);
      if (typeof handleEditRowOption === "function") {
        handleEditRowOption(params, finalRowUpdate);
        return finalRowUpdate;
      }
      if (finalRowUpdate && !isDeepEqual(finalRowUpdate, params.previousRow)) {
        apiRef.current.dataSource.cache.clear();
      }
      apiRef.current.updateNestedRows([finalRowUpdate], []);
      return finalRowUpdate;
    } catch (errorThrown) {
      if (typeof onDataSourceErrorProp === "function") {
        onDataSourceErrorProp(new GridUpdateRowError({
          message: errorThrown == null ? void 0 : errorThrown.message,
          params,
          cause: errorThrown
        }));
      } else {
        warnOnce(["MUI X: A call to `dataSource.updateRow()` threw an error which was not handled because `onDataSourceError()` is missing.", "To handle the error pass a callback to the `onDataSourceError` prop, for example `<DataGrid onDataSourceError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/server-side-data/#error-handling."], "error");
      }
      throw errorThrown;
    }
  }, [apiRef, dataSourceUpdateRow, onDataSourceErrorProp, handleEditRowOption]);
  const dataSourceApi = {
    dataSource: {
      fetchRows,
      cache,
      editRow
    }
  };
  const debouncedFetchRows = React248.useMemo(() => debounce(fetchRows, 0), [fetchRows]);
  const handleFetchRowsOnParamsChange = React248.useCallback(() => {
    apiRef.current.setRows([]);
    stopPolling();
    debouncedFetchRows();
  }, [stopPolling, debouncedFetchRows, apiRef]);
  const isFirstRender = React248.useRef(true);
  React248.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    if (props.dataSourceCache === void 0) {
      return;
    }
    const newCache = getCache(props.dataSourceCache, options3.cacheOptions);
    setCache((prevCache) => prevCache !== newCache ? newCache : prevCache);
  }, [props.dataSourceCache, options3.cacheOptions]);
  React248.useEffect(() => {
    if (!standardRowsUpdateStrategyActive) {
      stopPolling();
    }
  }, [standardRowsUpdateStrategyActive, stopPolling]);
  React248.useEffect(() => {
    if (revalidateMs <= 0) {
      stopPolling();
    }
  }, [revalidateMs, stopPolling]);
  React248.useEffect(() => stopPolling, [stopPolling]);
  React248.useEffect(() => {
    if (currentStrategy !== DataSourceRowsUpdateStrategy.Default && currentStrategy !== DataSourceRowsUpdateStrategy.LazyLoading && currentStrategy !== DataSourceRowsUpdateStrategy.GroupedData) {
      return void 0;
    }
    if (props.dataSource) {
      stopPolling();
      apiRef.current.setRows([]);
      apiRef.current.dataSource.cache.clear();
      apiRef.current.dataSource.fetchRows();
    }
    return () => {
      lastRequestId.current += 1;
    };
  }, [apiRef, props.dataSource, currentStrategy, stopPolling]);
  return {
    api: {
      public: dataSourceApi
    },
    debouncedFetchRows,
    strategyProcessor: {
      strategyName: DataSourceRowsUpdateStrategy.Default,
      group: "dataSourceRowsUpdate",
      processor: handleDataUpdate
    },
    setStrategyAvailability: setStrategyAvailability2,
    startPolling,
    stopPolling,
    cacheChunkManager,
    cache,
    events: {
      strategyAvailabilityChange: handleStrategyActivityChange,
      sortModelChange: runIf(standardRowsUpdateStrategyActive, handleFetchRowsOnParamsChange),
      filterModelChange: runIf(standardRowsUpdateStrategyActive, handleFetchRowsOnParamsChange),
      paginationModelChange: runIf(standardRowsUpdateStrategyActive, handleFetchRowsOnParamsChange)
    }
  };
};

// node_modules/@mui/x-data-grid/esm/hooks/features/dataSource/useGridDataSource.js
var useGridDataSource = (apiRef, props) => {
  const {
    api,
    strategyProcessor,
    events,
    setStrategyAvailability: setStrategyAvailability2
  } = useGridDataSourceBase(apiRef, props);
  useGridApiMethod(apiRef, api.public, "public");
  useGridRegisterStrategyProcessor(apiRef, strategyProcessor.strategyName, strategyProcessor.group, strategyProcessor.processor);
  Object.entries(events).forEach(([event, handler]) => {
    useGridEvent(apiRef, event, handler);
  });
  React249.useEffect(() => {
    setStrategyAvailability2();
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid/esm/DataGrid/useDataGridComponent.js
var useDataGridComponent = (apiRef, props, configuration4) => {
  useGridInitialization(apiRef, props);
  useGridRowSelectionPreProcessors(apiRef, props);
  useGridRowsPreProcessors(apiRef);
  useGridInitializeState(propsStateInitializer, apiRef, props);
  useGridInitializeState(rowSelectionStateInitializer, apiRef, props);
  useGridInitializeState(columnsStateInitializer, apiRef, props);
  useGridInitializeState(rowsStateInitializer, apiRef, props);
  useGridInitializeState(paginationStateInitializer, apiRef, props);
  useGridInitializeState(editingStateInitializer, apiRef, props);
  useGridInitializeState(focusStateInitializer, apiRef, props);
  useGridInitializeState(sortingStateInitializer, apiRef, props);
  useGridInitializeState(preferencePanelStateInitializer, apiRef, props);
  useGridInitializeState(filterStateInitializer, apiRef, props);
  useGridInitializeState(rowSpanningStateInitializer, apiRef, props);
  useGridInitializeState(densityStateInitializer, apiRef, props);
  useGridInitializeState(columnResizeStateInitializer, apiRef, props);
  useGridInitializeState(columnMenuStateInitializer, apiRef, props);
  useGridInitializeState(columnGroupsStateInitializer, apiRef, props);
  useGridInitializeState(virtualizationStateInitializer, apiRef, props);
  useGridInitializeState(dimensionsStateInitializer, apiRef, props);
  useGridInitializeState(rowsMetaStateInitializer, apiRef, props);
  useGridInitializeState(listViewStateInitializer, apiRef, props);
  useGridKeyboardNavigation(apiRef, props);
  useGridRowSelection(apiRef, props);
  useGridColumns(apiRef, props);
  useGridRows(apiRef, props, configuration4);
  useGridRowSpanning(apiRef, props);
  useGridParamsApi(apiRef, props, configuration4);
  useGridColumnSpanning(apiRef);
  useGridColumnGrouping(apiRef, props);
  useGridEditing(apiRef, props, configuration4);
  useGridFocus(apiRef, props);
  useGridPreferencesPanel(apiRef, props);
  useGridFilter(apiRef, props, configuration4);
  useGridSorting(apiRef, props);
  useGridDensity(apiRef, props);
  useGridColumnResize(apiRef, props);
  useGridPagination(apiRef, props);
  useGridScroll(apiRef, props);
  useGridColumnMenu(apiRef);
  useGridCsvExport(apiRef, props);
  useGridPrintExport(apiRef, props);
  useGridClipboard(apiRef, props);
  useGridDimensions(apiRef, props);
  useGridEvents(apiRef, props);
  useGridStatePersistence(apiRef);
  useGridVirtualization(apiRef, props);
  useGridListView(apiRef, props);
  useGridDataSource(apiRef, props);
  useFirstRender(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
  React250.useEffect(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
};

// node_modules/@mui/x-data-grid/esm/DataGrid/useDataGridProps.js
var React256 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/GridColumnHeaders.js
var React254 = __toESM(require_react(), 1);
var import_prop_types139 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid/esm/hooks/features/columnHeaders/useGridColumnHeaders.js
var React252 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridColumnGroupHeader.js
var React251 = __toESM(require_react(), 1);
var import_jsx_runtime176 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses94 = (ownerState) => {
  const {
    classes: classes2,
    headerAlign,
    isDragging,
    isLastColumn,
    showLeftBorder,
    showRightBorder,
    groupId,
    pinnedPosition
  } = ownerState;
  const slots = {
    root: ["columnHeader", headerAlign && `columnHeader--align${capitalize(headerAlign)}`, isDragging && "columnHeader--moving", showRightBorder && "columnHeader--withRightBorder", showLeftBorder && "columnHeader--withLeftBorder", "withBorderColor", groupId === null ? "columnHeader--emptyGroup" : "columnHeader--filledGroup", pinnedPosition === PinnedColumnPosition.LEFT && "columnHeader--pinnedLeft", pinnedPosition === PinnedColumnPosition.RIGHT && "columnHeader--pinnedRight", isLastColumn && "columnHeader--last"],
    draggableContainer: ["columnHeaderDraggableContainer"],
    titleContainer: ["columnHeaderTitleContainer", "withBorderColor"],
    titleContainerContent: ["columnHeaderTitleContainerContent"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridColumnGroupHeader(props) {
  var _a;
  const {
    groupId,
    width,
    depth,
    maxDepth,
    fields,
    height,
    colIndex,
    hasFocus,
    tabIndex,
    isLastColumn,
    pinnedPosition,
    pinnedOffset
  } = props;
  const rootProps = useGridRootProps();
  const isRtl = useRtl();
  const headerCellRef = React251.useRef(null);
  const apiRef = useGridApiContext();
  const columnGroupsLookup = useGridSelector(apiRef, gridColumnGroupsLookupSelector);
  const group = groupId ? columnGroupsLookup[groupId] : {};
  const {
    headerName = groupId ?? "",
    description = "",
    headerAlign = void 0
  } = group;
  let headerComponent;
  const render = groupId && ((_a = columnGroupsLookup[groupId]) == null ? void 0 : _a.renderHeaderGroup);
  const renderParams = React251.useMemo(() => ({
    groupId,
    headerName,
    description,
    depth,
    maxDepth,
    fields,
    colIndex,
    isLastColumn
  }), [groupId, headerName, description, depth, maxDepth, fields, colIndex, isLastColumn]);
  if (groupId && render) {
    headerComponent = render(renderParams);
  }
  const ownerState = _extends({}, props, {
    classes: rootProps.classes,
    headerAlign,
    depth,
    isDragging: false
  });
  const label = headerName ?? groupId;
  const id = useId();
  const elementId = groupId === null ? `empty-group-cell-${id}` : groupId;
  const classes2 = useUtilityClasses94(ownerState);
  React251.useLayoutEffect(() => {
    if (hasFocus) {
      const focusableElement = headerCellRef.current.querySelector('[tabindex="0"]');
      const elementToFocus = focusableElement || headerCellRef.current;
      if (!elementToFocus) {
        return;
      }
      if (doesSupportPreventScroll()) {
        elementToFocus.focus({
          preventScroll: true
        });
      } else {
        const scrollPosition = apiRef.current.getScrollPosition();
        elementToFocus.focus();
        apiRef.current.scroll(scrollPosition);
      }
    }
  }, [apiRef, hasFocus]);
  const publish = React251.useCallback(
    (eventName) => (event) => {
      if (isEventTargetInPortal(event)) {
        return;
      }
      apiRef.current.publishEvent(eventName, renderParams, event);
    },
    // For now this is stupid, because renderParams change all the time.
    // Need to move it's computation in the api, such that for a given depth+columnField, I can get the group parameters
    [apiRef, renderParams]
  );
  const mouseEventsHandlers = React251.useMemo(() => ({
    onKeyDown: publish("columnGroupHeaderKeyDown"),
    onFocus: publish("columnGroupHeaderFocus"),
    onBlur: publish("columnGroupHeaderBlur")
  }), [publish]);
  const headerClassName = typeof group.headerClassName === "function" ? group.headerClassName(renderParams) : group.headerClassName;
  const style = React251.useMemo(() => attachPinnedStyle(_extends({}, props.style), isRtl, pinnedPosition, pinnedOffset), [pinnedPosition, pinnedOffset, props.style, isRtl]);
  return (0, import_jsx_runtime176.jsx)(GridGenericColumnHeaderItem, _extends({
    ref: headerCellRef,
    classes: classes2,
    columnMenuOpen: false,
    colIndex,
    height,
    isResizing: false,
    sortDirection: null,
    hasFocus: false,
    tabIndex,
    isDraggable: false,
    headerComponent,
    headerClassName,
    description,
    elementId,
    width,
    columnMenuIconButton: null,
    columnTitleIconButtons: null,
    resizable: false,
    label,
    "aria-colspan": fields.length,
    "data-fields": `|-${fields.join("-|-")}-|`,
    style
  }, mouseEventsHandlers));
}

// node_modules/@mui/x-data-grid/esm/components/GridScrollbarFillerCell.js
var import_jsx_runtime177 = __toESM(require_jsx_runtime(), 1);
var classes = {
  root: gridClasses.scrollbarFiller,
  pinnedRight: gridClasses["scrollbarFiller--pinnedRight"]
};
function GridScrollbarFillerCell({
  pinnedRight
}) {
  return (0, import_jsx_runtime177.jsx)("div", {
    role: "presentation",
    className: clsx_default(classes.root, pinnedRight && classes.pinnedRight)
  });
}

// node_modules/@mui/x-data-grid/esm/hooks/features/columnHeaders/useGridColumnHeaders.js
var import_jsx_runtime178 = __toESM(require_jsx_runtime(), 1);
var GridColumnHeaderRow = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ColumnHeaderRow"
})({
  display: "flex"
});
var useGridColumnHeaders = (props) => {
  const {
    visibleColumns,
    sortColumnLookup,
    filterColumnLookup,
    columnHeaderTabIndexState,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure,
    hasOtherElementInTabSequence
  } = props;
  const [dragCol, setDragCol] = React252.useState("");
  const [resizeCol, setResizeCol] = React252.useState("");
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps();
  const columnGroupsModel = useGridSelector(apiRef, gridColumnGroupsUnwrappedModelSelector);
  const columnPositions = useGridSelector(apiRef, gridColumnPositionsSelector);
  const renderContext = useGridSelector(apiRef, gridRenderContextColumnsSelector);
  const pinnedColumns = useGridSelector(apiRef, gridVisiblePinnedColumnDefinitionsSelector);
  const columnsLookup = useGridSelector(apiRef, gridColumnLookupSelector);
  const offsetLeft = computeOffsetLeft(columnPositions, renderContext, pinnedColumns.left.length);
  const columnsTotalWidth = useGridSelector(apiRef, gridColumnsTotalWidthSelector);
  const gridHasFiller = useGridSelector(apiRef, gridHasFillerSelector);
  const headerHeight = useGridSelector(apiRef, gridHeaderHeightSelector);
  const groupHeaderHeight = useGridSelector(apiRef, gridGroupHeaderHeightSelector);
  const scrollbarWidth = useGridSelector(apiRef, gridVerticalScrollbarWidthSelector);
  const handleColumnResizeStart = React252.useCallback((params) => setResizeCol(params.field), []);
  const handleColumnResizeStop = React252.useCallback(() => setResizeCol(""), []);
  const handleColumnReorderStart = React252.useCallback((params) => setDragCol(params.field), []);
  const handleColumnReorderStop = React252.useCallback(() => setDragCol(""), []);
  const leftRenderContext = React252.useMemo(() => {
    return pinnedColumns.left.length ? {
      firstColumnIndex: 0,
      lastColumnIndex: pinnedColumns.left.length
    } : null;
  }, [pinnedColumns.left.length]);
  const rightRenderContext = React252.useMemo(() => {
    return pinnedColumns.right.length ? {
      firstColumnIndex: visibleColumns.length - pinnedColumns.right.length,
      lastColumnIndex: visibleColumns.length
    } : null;
  }, [pinnedColumns.right.length, visibleColumns.length]);
  useGridEvent(apiRef, "columnResizeStart", handleColumnResizeStart);
  useGridEvent(apiRef, "columnResizeStop", handleColumnResizeStop);
  useGridEvent(apiRef, "columnHeaderDragStart", handleColumnReorderStart);
  useGridEvent(apiRef, "columnHeaderDragEndNative", handleColumnReorderStop);
  const getColumnsToRender = (params) => {
    const {
      renderContext: currentContext = renderContext
    } = params || {};
    const firstColumnToRender = currentContext.firstColumnIndex;
    const lastColumnToRender = Math.min(currentContext.lastColumnIndex, visibleColumns.length);
    const renderedColumns = visibleColumns.slice(firstColumnToRender, lastColumnToRender);
    return {
      renderedColumns,
      firstColumnToRender,
      lastColumnToRender
    };
  };
  const getFillers = (params, children2, leftOverflow, borderBottom = false) => {
    const isPinnedRight = (params == null ? void 0 : params.position) === PinnedColumnPosition.RIGHT;
    const isNotPinned = (params == null ? void 0 : params.position) === void 0;
    const hasScrollbarFiller = pinnedColumns.right.length > 0 && isPinnedRight || pinnedColumns.right.length === 0 && isNotPinned;
    const leftOffsetWidth = offsetLeft - leftOverflow;
    return (0, import_jsx_runtime178.jsxs)(React252.Fragment, {
      children: [isNotPinned && (0, import_jsx_runtime178.jsx)("div", {
        role: "presentation",
        style: {
          width: leftOffsetWidth
        }
      }), children2, isNotPinned && (0, import_jsx_runtime178.jsx)("div", {
        role: "presentation",
        className: clsx_default(gridClasses.filler, borderBottom && gridClasses["filler--borderBottom"])
      }), hasScrollbarFiller && (0, import_jsx_runtime178.jsx)(GridScrollbarFillerCell, {
        pinnedRight: isPinnedRight
      })]
    });
  };
  if (true) getFillers.displayName = "getFillers";
  const getColumnHeaders = (params, other = {}) => {
    const {
      renderedColumns,
      firstColumnToRender
    } = getColumnsToRender(params);
    const columns = [];
    for (let i2 = 0; i2 < renderedColumns.length; i2 += 1) {
      const colDef = renderedColumns[i2];
      const columnIndex = firstColumnToRender + i2;
      const isFirstColumn = columnIndex === 0;
      const tabIndex = columnHeaderTabIndexState !== null && columnHeaderTabIndexState.field === colDef.field || isFirstColumn && !hasOtherElementInTabSequence ? 0 : -1;
      const hasFocus = columnHeaderFocus !== null && columnHeaderFocus.field === colDef.field;
      const open = columnMenuState.open && columnMenuState.field === colDef.field;
      const pinnedPosition = params == null ? void 0 : params.position;
      const pinnedOffset = getPinnedCellOffset(pinnedPosition, colDef.computedWidth, columnIndex, columnPositions, columnsTotalWidth, scrollbarWidth);
      const siblingWithBorderingSeparator = pinnedPosition === PinnedColumnPosition.RIGHT ? renderedColumns[i2 - 1] : renderedColumns[i2 + 1];
      const isSiblingFocused = siblingWithBorderingSeparator ? columnHeaderFocus !== null && columnHeaderFocus.field === siblingWithBorderingSeparator.field : false;
      const indexInSection = i2;
      const sectionLength = renderedColumns.length;
      const showLeftBorder = shouldCellShowLeftBorder(pinnedPosition, indexInSection, rootProps.showColumnVerticalBorder, rootProps.pinnedColumnsSectionSeparator);
      const showRightBorder = shouldCellShowRightBorder(pinnedPosition, indexInSection, sectionLength, rootProps.showColumnVerticalBorder, gridHasFiller, rootProps.pinnedColumnsSectionSeparator);
      columns.push((0, import_jsx_runtime178.jsx)(Memoized3, _extends({}, sortColumnLookup[colDef.field], {
        columnMenuOpen: open,
        filterItemsCounter: filterColumnLookup[colDef.field] && filterColumnLookup[colDef.field].length,
        headerHeight,
        isDragging: colDef.field === dragCol,
        colDef,
        colIndex: columnIndex,
        isResizing: resizeCol === colDef.field,
        isLast: columnIndex === columnPositions.length - 1,
        hasFocus,
        tabIndex,
        pinnedPosition,
        pinnedOffset,
        isSiblingFocused,
        showLeftBorder,
        showRightBorder
      }, other), colDef.field));
    }
    return getFillers(params, columns, 0);
  };
  const getColumnHeadersRow = () => {
    return (0, import_jsx_runtime178.jsxs)(GridColumnHeaderRow, {
      role: "row",
      "aria-rowindex": headerGroupingMaxDepth + 1,
      ownerState: rootProps,
      className: gridClasses["row--borderBottom"],
      style: {
        height: headerHeight
      },
      children: [leftRenderContext && getColumnHeaders({
        position: PinnedColumnPosition.LEFT,
        renderContext: leftRenderContext
      }, {
        disableReorder: true
      }), getColumnHeaders({
        renderContext
      }), rightRenderContext && getColumnHeaders({
        position: PinnedColumnPosition.RIGHT,
        renderContext: rightRenderContext
      }, {
        disableReorder: true,
        separatorSide: GridColumnHeaderSeparatorSides.Left
      })]
    });
  };
  if (true) getColumnHeadersRow.displayName = "getColumnHeadersRow";
  const getColumnGroupHeaders = ({
    depth,
    params
  }) => {
    var _a, _b;
    const columnsToRender = getColumnsToRender(params);
    if (columnsToRender.renderedColumns.length === 0) {
      return null;
    }
    const {
      firstColumnToRender,
      lastColumnToRender
    } = columnsToRender;
    const rowStructure = columnGroupsHeaderStructure[depth];
    const firstColumnFieldToRender = visibleColumns[firstColumnToRender].field;
    const firstGroupToRender = ((_a = columnGroupsModel[firstColumnFieldToRender]) == null ? void 0 : _a[depth]) ?? null;
    const firstGroupIndex = rowStructure.findIndex(({
      groupId,
      columnFields
    }) => groupId === firstGroupToRender && columnFields.includes(firstColumnFieldToRender));
    const lastColumnFieldToRender = visibleColumns[lastColumnToRender - 1].field;
    const lastGroupToRender = ((_b = columnGroupsModel[lastColumnFieldToRender]) == null ? void 0 : _b[depth]) ?? null;
    const lastGroupIndex = rowStructure.findIndex(({
      groupId,
      columnFields
    }) => groupId === lastGroupToRender && columnFields.includes(lastColumnFieldToRender));
    const visibleColumnGroupHeader = rowStructure.slice(firstGroupIndex, lastGroupIndex + 1).map((groupStructure) => {
      return _extends({}, groupStructure, {
        columnFields: groupStructure.columnFields.filter((field) => columnVisibility[field] !== false)
      });
    }).filter((groupStructure) => groupStructure.columnFields.length > 0);
    const firstVisibleColumnIndex = visibleColumnGroupHeader[0].columnFields.indexOf(firstColumnFieldToRender);
    const hiddenGroupColumns = visibleColumnGroupHeader[0].columnFields.slice(0, firstVisibleColumnIndex);
    const leftOverflow = hiddenGroupColumns.reduce((acc, field) => {
      const column = columnsLookup[field];
      return acc + (column.computedWidth ?? 0);
    }, 0);
    let columnIndex = firstColumnToRender;
    const children2 = visibleColumnGroupHeader.map(({
      groupId,
      columnFields
    }, index) => {
      const hasFocus = columnGroupHeaderFocus !== null && columnGroupHeaderFocus.depth === depth && columnFields.includes(columnGroupHeaderFocus.field);
      const tabIndex = columnGroupHeaderTabIndexState !== null && columnGroupHeaderTabIndexState.depth === depth && columnFields.includes(columnGroupHeaderTabIndexState.field) ? 0 : -1;
      const headerInfo = {
        groupId,
        width: columnFields.reduce((acc, field) => acc + columnsLookup[field].computedWidth, 0),
        fields: columnFields,
        colIndex: columnIndex,
        hasFocus,
        tabIndex
      };
      const pinnedPosition = params.position;
      const pinnedOffset = getPinnedCellOffset(pinnedPosition, headerInfo.width, columnIndex, columnPositions, columnsTotalWidth, scrollbarWidth);
      columnIndex += columnFields.length;
      let indexInSection = index;
      if (pinnedPosition === PinnedColumnPosition.LEFT) {
        indexInSection = columnIndex - 1;
      }
      return (0, import_jsx_runtime178.jsx)(GridColumnGroupHeader, {
        groupId,
        width: headerInfo.width,
        fields: headerInfo.fields,
        colIndex: headerInfo.colIndex,
        depth,
        isLastColumn: index === visibleColumnGroupHeader.length - 1,
        maxDepth: headerGroupingMaxDepth,
        height: groupHeaderHeight,
        hasFocus,
        tabIndex,
        pinnedPosition,
        pinnedOffset,
        showLeftBorder: shouldCellShowLeftBorder(pinnedPosition, indexInSection, rootProps.showColumnVerticalBorder, rootProps.pinnedColumnsSectionSeparator),
        showRightBorder: shouldCellShowRightBorder(pinnedPosition, indexInSection, visibleColumnGroupHeader.length, rootProps.showColumnVerticalBorder, gridHasFiller, rootProps.pinnedColumnsSectionSeparator)
      }, index);
    });
    return getFillers(params, children2, leftOverflow);
  };
  const getColumnGroupHeadersRows = () => {
    if (headerGroupingMaxDepth === 0) {
      return null;
    }
    const headerRows = [];
    for (let depth = 0; depth < headerGroupingMaxDepth; depth += 1) {
      headerRows.push((0, import_jsx_runtime178.jsxs)(GridColumnHeaderRow, {
        role: "row",
        "aria-rowindex": depth + 1,
        ownerState: rootProps,
        style: {
          height: groupHeaderHeight
        },
        children: [leftRenderContext && getColumnGroupHeaders({
          depth,
          params: {
            position: PinnedColumnPosition.LEFT,
            renderContext: leftRenderContext,
            maxLastColumn: leftRenderContext.lastColumnIndex
          }
        }), getColumnGroupHeaders({
          depth,
          params: {
            renderContext
          }
        }), rightRenderContext && getColumnGroupHeaders({
          depth,
          params: {
            position: PinnedColumnPosition.RIGHT,
            renderContext: rightRenderContext,
            maxLastColumn: rightRenderContext.lastColumnIndex
          }
        })]
      }, depth));
    }
    return headerRows;
  };
  return {
    renderContext,
    leftRenderContext,
    rightRenderContext,
    pinnedColumns,
    visibleColumns,
    columnPositions,
    getFillers,
    getColumnHeadersRow,
    getColumnsToRender,
    getColumnGroupHeadersRows,
    getPinnedCellOffset,
    isDragging: !!dragCol,
    getInnerProps: () => ({
      role: "rowgroup"
    })
  };
};

// node_modules/@mui/x-data-grid/esm/components/columnHeaders/GridBaseColumnHeaders.js
var React253 = __toESM(require_react(), 1);
var import_jsx_runtime179 = __toESM(require_jsx_runtime(), 1);
var _excluded68 = ["className"];
var useUtilityClasses95 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["columnHeaders"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridColumnHeadersRoot = styled_default("div", {
  name: "MuiDataGrid",
  slot: "ColumnHeaders"
})({
  display: "flex",
  flexDirection: "column",
  borderTopLeftRadius: "var(--unstable_DataGrid-radius)",
  borderTopRightRadius: "var(--unstable_DataGrid-radius)"
});
var GridBaseColumnHeaders = forwardRef2(function GridColumnHeaders(props, ref) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded68);
  const rootProps = useGridRootProps();
  const classes2 = useUtilityClasses95(rootProps);
  return (0, import_jsx_runtime179.jsx)(GridColumnHeadersRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState: rootProps
  }, other, {
    role: "presentation",
    ref
  }));
});
if (true) GridBaseColumnHeaders.displayName = "GridBaseColumnHeaders";

// node_modules/@mui/x-data-grid/esm/components/GridColumnHeaders.js
var import_jsx_runtime180 = __toESM(require_jsx_runtime(), 1);
var _excluded69 = ["className", "visibleColumns", "sortColumnLookup", "filterColumnLookup", "columnHeaderTabIndexState", "columnGroupHeaderTabIndexState", "columnHeaderFocus", "columnGroupHeaderFocus", "headerGroupingMaxDepth", "columnMenuState", "columnVisibility", "columnGroupsHeaderStructure", "hasOtherElementInTabSequence"];
var GridColumnHeaders2 = forwardRef2(function GridColumnHeaders3(props, ref) {
  const {
    visibleColumns,
    sortColumnLookup,
    filterColumnLookup,
    columnHeaderTabIndexState,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure,
    hasOtherElementInTabSequence
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded69);
  const {
    getInnerProps,
    getColumnHeadersRow,
    getColumnGroupHeadersRows
  } = useGridColumnHeaders({
    visibleColumns,
    sortColumnLookup,
    filterColumnLookup,
    columnHeaderTabIndexState,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure,
    hasOtherElementInTabSequence
  });
  return (0, import_jsx_runtime180.jsxs)(GridBaseColumnHeaders, _extends({}, other, getInnerProps(), {
    ref,
    children: [getColumnGroupHeadersRows(), getColumnHeadersRow()]
  }));
});
if (true) GridColumnHeaders2.displayName = "GridColumnHeaders";
true ? GridColumnHeaders2.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  columnGroupHeaderFocus: import_prop_types139.default.shape({
    depth: import_prop_types139.default.number.isRequired,
    field: import_prop_types139.default.string.isRequired
  }),
  columnGroupHeaderTabIndexState: import_prop_types139.default.shape({
    depth: import_prop_types139.default.number.isRequired,
    field: import_prop_types139.default.string.isRequired
  }),
  columnGroupsHeaderStructure: import_prop_types139.default.arrayOf(import_prop_types139.default.arrayOf(import_prop_types139.default.shape({
    columnFields: import_prop_types139.default.arrayOf(import_prop_types139.default.string).isRequired,
    groupId: import_prop_types139.default.string
  }))).isRequired,
  columnHeaderFocus: import_prop_types139.default.shape({
    field: import_prop_types139.default.string.isRequired
  }),
  columnHeaderTabIndexState: import_prop_types139.default.shape({
    field: import_prop_types139.default.string.isRequired
  }),
  columnMenuState: import_prop_types139.default.shape({
    field: import_prop_types139.default.string,
    open: import_prop_types139.default.bool.isRequired
  }).isRequired,
  columnVisibility: import_prop_types139.default.object.isRequired,
  filterColumnLookup: import_prop_types139.default.object.isRequired,
  hasOtherElementInTabSequence: import_prop_types139.default.bool.isRequired,
  headerGroupingMaxDepth: import_prop_types139.default.number.isRequired,
  sortColumnLookup: import_prop_types139.default.object.isRequired,
  visibleColumns: import_prop_types139.default.arrayOf(import_prop_types139.default.object).isRequired
} : void 0;
var MemoizedGridColumnHeaders = fastMemo(GridColumnHeaders2);

// node_modules/@mui/x-data-grid/esm/components/GridDetailPanels.js
function GridDetailPanels(_) {
  return null;
}

// node_modules/@mui/x-data-grid/esm/components/GridPinnedRows.js
function GridPinnedRows(_) {
  return null;
}

// node_modules/@mui/x-data-grid/esm/components/GridNoResultsOverlay.js
var import_jsx_runtime181 = __toESM(require_jsx_runtime(), 1);
var GridNoResultsOverlay = forwardRef2(function GridNoResultsOverlay2(props, ref) {
  const apiRef = useGridApiContext();
  const noResultsOverlayLabel = apiRef.current.getLocaleText("noResultsOverlayLabel");
  return (0, import_jsx_runtime181.jsx)(GridOverlay, _extends({}, props, {
    ref,
    children: noResultsOverlayLabel
  }));
});
if (true) GridNoResultsOverlay.displayName = "GridNoResultsOverlay";

// node_modules/@mui/x-data-grid/esm/components/virtualization/GridBottomContainer.js
var React255 = __toESM(require_react(), 1);
var import_jsx_runtime182 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses96 = () => {
  const slots = {
    root: ["bottomContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, {});
};
var Element4 = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "sticky",
  zIndex: 40,
  bottom: "calc(var(--DataGrid-hasScrollX) * var(--DataGrid-scrollbarSize))"
});
function GridBottomContainer(props) {
  const classes2 = useUtilityClasses96();
  return (0, import_jsx_runtime182.jsx)(Element4, _extends({}, props, {
    className: clsx_default(classes2.root, gridClasses["container--bottom"]),
    role: "presentation"
  }));
}

// node_modules/@mui/x-data-grid/esm/constants/defaultGridSlotsComponents.js
var DATA_GRID_DEFAULT_SLOTS_COMPONENTS = _extends({}, material_default, {
  cell: MemoizedGridCell,
  skeletonCell: Memoized2,
  columnHeaderFilterIconButton: GridColumnHeaderFilterIconButtonWrapped,
  columnHeaderSortIcon: GridColumnHeaderSortIcon,
  columnMenu: GridColumnMenu,
  columnHeaders: MemoizedGridColumnHeaders,
  detailPanels: GridDetailPanels,
  bottomContainer: GridBottomContainer,
  footer: GridFooter,
  footerRowCount: GridRowCount,
  toolbar: GridToolbar,
  pinnedRows: GridPinnedRows,
  loadingOverlay: GridLoadingOverlay,
  noResultsOverlay: GridNoResultsOverlay,
  noRowsOverlay: GridNoRowsOverlay,
  noColumnsOverlay: GridNoColumnsOverlay,
  pagination: GridPagination,
  filterPanel: GridFilterPanel,
  columnsPanel: GridColumnsPanel,
  columnsManagement: GridColumnsManagement,
  panel: GridPanel,
  row: MemoizedGridRow
});

// node_modules/@mui/x-data-grid/esm/DataGrid/useDataGridProps.js
var DATA_GRID_FORCED_PROPS = {
  disableMultipleColumnsFiltering: true,
  disableMultipleColumnsSorting: true,
  throttleRowsMs: void 0,
  hideFooterRowCount: false,
  pagination: true,
  checkboxSelectionVisibleOnly: false,
  disableColumnReorder: true,
  keepColumnPositionIfDraggedOutside: false,
  signature: "DataGrid",
  listView: false
};
var getDataGridForcedProps = (themedProps) => _extends({}, DATA_GRID_FORCED_PROPS, themedProps.dataSource ? {
  filterMode: "server",
  sortingMode: "server",
  paginationMode: "server"
} : {});
var defaultSlots = DATA_GRID_DEFAULT_SLOTS_COMPONENTS;
var useDataGridProps = (inProps) => {
  const theme = useTheme();
  const themedProps = React256.useMemo(() => getThemeProps({
    props: inProps,
    theme,
    name: "MuiDataGrid"
  }), [theme, inProps]);
  const localeText = React256.useMemo(() => _extends({}, GRID_DEFAULT_LOCALE_TEXT, themedProps.localeText), [themedProps.localeText]);
  const slots = React256.useMemo(() => computeSlots({
    defaultSlots,
    slots: themedProps.slots
  }), [themedProps.slots]);
  const injectDefaultProps = React256.useMemo(() => {
    return Object.keys(DATA_GRID_PROPS_DEFAULT_VALUES).reduce((acc, key) => {
      acc[key] = themedProps[key] ?? DATA_GRID_PROPS_DEFAULT_VALUES[key];
      return acc;
    }, {});
  }, [themedProps]);
  return React256.useMemo(() => _extends({}, themedProps, injectDefaultProps, {
    localeText,
    slots
  }, getDataGridForcedProps(themedProps)), [themedProps, localeText, slots, injectDefaultProps]);
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridApiInitialization.js
var React257 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/EventManager/EventManager.js
var EventManager = class {
  constructor() {
    __publicField(this, "maxListeners", 20);
    __publicField(this, "warnOnce", false);
    __publicField(this, "events", {});
  }
  on(eventName, listener, options3 = {}) {
    let collection = this.events[eventName];
    if (!collection) {
      collection = {
        highPriority: /* @__PURE__ */ new Map(),
        regular: /* @__PURE__ */ new Map()
      };
      this.events[eventName] = collection;
    }
    if (options3.isFirst) {
      collection.highPriority.set(listener, true);
    } else {
      collection.regular.set(listener, true);
    }
    if (true) {
      const collectionSize = collection.highPriority.size + collection.regular.size;
      if (collectionSize > this.maxListeners && !this.warnOnce) {
        this.warnOnce = true;
        console.warn([`Possible EventEmitter memory leak detected. ${collectionSize} ${eventName} listeners added.`].join("\n"));
      }
    }
  }
  removeListener(eventName, listener) {
    if (this.events[eventName]) {
      this.events[eventName].regular.delete(listener);
      this.events[eventName].highPriority.delete(listener);
    }
  }
  removeAllListeners() {
    this.events = {};
  }
  emit(eventName, ...args) {
    const collection = this.events[eventName];
    if (!collection) {
      return;
    }
    const highPriorityListeners = Array.from(collection.highPriority.keys());
    const regularListeners = Array.from(collection.regular.keys());
    for (let i2 = highPriorityListeners.length - 1; i2 >= 0; i2 -= 1) {
      const listener = highPriorityListeners[i2];
      if (collection.highPriority.has(listener)) {
        listener.apply(this, args);
      }
    }
    for (let i2 = 0; i2 < regularListeners.length; i2 += 1) {
      const listener = regularListeners[i2];
      if (collection.regular.has(listener)) {
        listener.apply(this, args);
      }
    }
  }
  once(eventName, listener) {
    const that = this;
    this.on(eventName, function oneTimeListener(...args) {
      that.removeListener(eventName, oneTimeListener);
      listener.apply(that, args);
    });
  }
};

// node_modules/@mui/x-data-grid/esm/hooks/core/useGridApiInitialization.js
var SYMBOL_API_PRIVATE = Symbol("mui.api_private");
var isSyntheticEvent = (event) => {
  return event.isPropagationStopped !== void 0;
};
var globalId = 0;
function createPrivateAPI(publicApiRef) {
  var _a;
  const existingPrivateApi = (_a = publicApiRef.current) == null ? void 0 : _a[SYMBOL_API_PRIVATE];
  if (existingPrivateApi) {
    return existingPrivateApi;
  }
  const state = {};
  const privateApi = {
    state,
    store: Store.create(state),
    instanceId: {
      id: globalId
    }
  };
  globalId += 1;
  privateApi.getPublicApi = () => publicApiRef.current;
  privateApi.register = (visibility, methods) => {
    Object.keys(methods).forEach((methodName) => {
      const method = methods[methodName];
      const currentPrivateMethod = privateApi[methodName];
      if ((currentPrivateMethod == null ? void 0 : currentPrivateMethod.spying) === true) {
        currentPrivateMethod.target = method;
      } else {
        privateApi[methodName] = method;
      }
      if (visibility === "public") {
        const publicApi = publicApiRef.current;
        const currentPublicMethod = publicApi[methodName];
        if ((currentPublicMethod == null ? void 0 : currentPublicMethod.spying) === true) {
          currentPublicMethod.target = method;
        } else {
          publicApi[methodName] = method;
        }
      }
    });
  };
  privateApi.register("private", {
    caches: {},
    eventManager: new EventManager()
  });
  return privateApi;
}
function createPublicAPI(privateApiRef) {
  const publicApi = {
    get state() {
      return privateApiRef.current.state;
    },
    get store() {
      return privateApiRef.current.store;
    },
    get instanceId() {
      return privateApiRef.current.instanceId;
    },
    [SYMBOL_API_PRIVATE]: privateApiRef.current
  };
  return publicApi;
}
function useGridApiInitialization(inputApiRef, props) {
  var _a;
  const publicApiRef = React257.useRef(null);
  const privateApiRef = React257.useRef(null);
  if (!privateApiRef.current) {
    privateApiRef.current = createPrivateAPI(publicApiRef);
  }
  if (!publicApiRef.current) {
    publicApiRef.current = createPublicAPI(privateApiRef);
  }
  const publishEvent = React257.useCallback((...args) => {
    const [name, params, event = {}] = args;
    event.defaultMuiPrevented = false;
    if (isSyntheticEvent(event) && event.isPropagationStopped()) {
      return;
    }
    const details = props.signature === GridSignature.DataGridPro || props.signature === GridSignature.DataGridPremium ? {
      api: privateApiRef.current.getPublicApi()
    } : {};
    privateApiRef.current.eventManager.emit(name, params, event, details);
  }, [privateApiRef, props.signature]);
  const subscribeEvent = React257.useCallback((event, handler, options3) => {
    privateApiRef.current.eventManager.on(event, handler, options3);
    const api = privateApiRef.current;
    return () => {
      api.eventManager.removeListener(event, handler);
    };
  }, [privateApiRef]);
  useGridApiMethod(privateApiRef, {
    subscribeEvent,
    publishEvent
  }, "public");
  if (inputApiRef && !((_a = inputApiRef.current) == null ? void 0 : _a.state)) {
    inputApiRef.current = publicApiRef.current;
  }
  React257.useImperativeHandle(inputApiRef, () => publicApiRef.current, [publicApiRef]);
  React257.useEffect(() => {
    const api = privateApiRef.current;
    return () => {
      api.publishEvent("unmount");
    };
  }, [privateApiRef]);
  return privateApiRef;
}

// node_modules/@mui/x-data-grid/esm/DataGrid/DataGrid.js
var import_jsx_runtime183 = __toESM(require_jsx_runtime(), 1);
var configuration = {
  hooks: {
    useCSSVariables: useMaterialCSSVariables,
    useGridAriaAttributes,
    useGridRowAriaAttributes,
    useGridRowsOverridableMethods,
    useGridParamsOverridableMethods,
    useIsCellEditable,
    useCellAggregationResult: () => null,
    useFilterValueGetter: (apiRef) => apiRef.current.getRowValue
  }
};
var DataGridRaw = function DataGrid(inProps, ref) {
  var _a;
  const props = useDataGridProps(inProps);
  const privateApiRef = useGridApiInitialization(props.apiRef, props);
  useDataGridComponent(privateApiRef, props, configuration);
  if (true) {
    validateProps(props, propValidatorsDataGrid);
  }
  return (0, import_jsx_runtime183.jsx)(GridContextProvider, {
    privateApiRef,
    configuration,
    props,
    children: (0, import_jsx_runtime183.jsx)(MemoizedGridRoot, _extends({
      className: props.className,
      style: props.style,
      sx: props.sx
    }, (_a = props.slotProps) == null ? void 0 : _a.root, {
      ref
    }))
  });
};
if (true) DataGridRaw.displayName = "DataGridRaw";
var DataGrid2 = React258.memo(forwardRef2(DataGridRaw));
if (true) DataGrid2.displayName = "DataGrid";
DataGridRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The ref object that allows Data Grid manipulation. Can be instantiated with `useGridApiRef()`.
   */
  apiRef: import_prop_types140.default.shape({
    current: import_prop_types140.default.object
  }),
  /**
   * The `aria-label` of the Data Grid.
   */
  "aria-label": import_prop_types140.default.string,
  /**
   * The `id` of the element containing a label for the Data Grid.
   */
  "aria-labelledby": import_prop_types140.default.string,
  /**
   * If `true`, the Data Grid height is dynamic and follows the number of rows in the Data Grid.
   * @default false
   * @deprecated Use flex parent container instead: https://mui.com/x/react-data-grid/layout/#flex-parent-container
   * @example
   * <div style={{ display: 'flex', flexDirection: 'column' }}>
   *   <DataGrid />
   * </div>
   */
  autoHeight: import_prop_types140.default.bool,
  /**
   * If `true`, the pageSize is calculated according to the container size and the max number of rows to avoid rendering a vertical scroll bar.
   * @default false
   */
  autoPageSize: import_prop_types140.default.bool,
  /**
   * If `true`, columns are autosized after the datagrid is mounted.
   * @default false
   */
  autosizeOnMount: import_prop_types140.default.bool,
  /**
   * The options for autosize when user-initiated.
   */
  autosizeOptions: import_prop_types140.default.shape({
    columns: import_prop_types140.default.arrayOf(import_prop_types140.default.string),
    disableColumnVirtualization: import_prop_types140.default.bool,
    expand: import_prop_types140.default.bool,
    includeHeaders: import_prop_types140.default.bool,
    includeOutliers: import_prop_types140.default.bool,
    outliersFactor: import_prop_types140.default.number
  }),
  /**
   * Controls the modes of the cells.
   */
  cellModesModel: import_prop_types140.default.object,
  /**
   * If `true`, the Data Grid will display an extra column with checkboxes for selecting rows.
   * @default false
   */
  checkboxSelection: import_prop_types140.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types140.default.object,
  className: import_prop_types140.default.string,
  /**
   * The character used to separate cell values when copying to the clipboard.
   * @default '\t'
   */
  clipboardCopyCellDelimiter: import_prop_types140.default.string,
  /**
   * Column region in pixels to render before/after the viewport
   * @default 150
   */
  columnBufferPx: import_prop_types140.default.number,
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering in the columns menu.
   * @default 150
   */
  columnFilterDebounceMs: import_prop_types140.default.number,
  /**
   * Sets the height in pixels of the column group headers in the Data Grid.
   * Inherits the `columnHeaderHeight` value if not set.
   */
  columnGroupHeaderHeight: import_prop_types140.default.number,
  columnGroupingModel: import_prop_types140.default.arrayOf(import_prop_types140.default.object),
  /**
   * Sets the height in pixel of the column headers in the Data Grid.
   * @default 56
   */
  columnHeaderHeight: import_prop_types140.default.number,
  /**
   * Set of columns of type [[GridColDef]][].
   */
  columns: import_prop_types140.default.arrayOf(import_prop_types140.default.object).isRequired,
  /**
   * Set the column visibility model of the Data Grid.
   * If defined, the Data Grid will ignore the `hide` property in [[GridColDef]].
   */
  columnVisibilityModel: import_prop_types140.default.object,
  /**
   * The data source object.
   */
  dataSource: import_prop_types140.default.shape({
    getRows: import_prop_types140.default.func.isRequired,
    updateRow: import_prop_types140.default.func
  }),
  /**
   * Data source cache object.
   */
  dataSourceCache: import_prop_types140.default.shape({
    clear: import_prop_types140.default.func.isRequired,
    get: import_prop_types140.default.func.isRequired,
    set: import_prop_types140.default.func.isRequired
  }),
  /**
   * If positive, the Data Grid will periodically revalidate data source rows by
   * re-fetching them from the server when the cache entry has expired.
   * Set to `0` to disable polling.
   * @default 0
   */
  dataSourceRevalidateMs: import_prop_types140.default.number,
  /**
   * Set the density of the Data Grid.
   * @default "standard"
   */
  density: import_prop_types140.default.oneOf(["comfortable", "compact", "standard"]),
  /**
   * If `true`, column autosizing on header separator double-click is disabled.
   * @default false
   */
  disableAutosize: import_prop_types140.default.bool,
  /**
   * If `true`, column filters are disabled.
   * @default false
   */
  disableColumnFilter: import_prop_types140.default.bool,
  /**
   * If `true`, the column menu is disabled.
   * @default false
   */
  disableColumnMenu: import_prop_types140.default.bool,
  /**
   * If `true`, resizing columns is disabled.
   * @default false
   */
  disableColumnResize: import_prop_types140.default.bool,
  /**
   * If `true`, hiding/showing columns is disabled.
   * @default false
   */
  disableColumnSelector: import_prop_types140.default.bool,
  /**
   * If `true`, the column sorting feature will be disabled.
   * @default false
   */
  disableColumnSorting: import_prop_types140.default.bool,
  /**
   * If `true`, the density selector is disabled.
   * @default false
   */
  disableDensitySelector: import_prop_types140.default.bool,
  /**
   * If `true`, `eval()` is not used for performance optimization.
   * @default false
   */
  disableEval: import_prop_types140.default.bool,
  /**
   * If `true`, multiple selection using the Ctrl/CMD or Shift key is disabled.
   * The MIT DataGrid will ignore this prop, unless `checkboxSelection` is enabled.
   * @default false (`!props.checkboxSelection` for MIT Data Grid)
   */
  disableMultipleRowSelection: import_prop_types140.default.bool,
  /**
   * If `true`, the Data Grid will not use the exclude model optimization when selecting all rows.
   * @default false
   */
  disableRowSelectionExcludeModel: import_prop_types140.default.bool,
  /**
   * If `true`, the selection on click on a row or cell is disabled.
   * @default false
   */
  disableRowSelectionOnClick: import_prop_types140.default.bool,
  /**
   * If `true`, the virtualization is disabled.
   * @default false
   */
  disableVirtualization: import_prop_types140.default.bool,
  /**
   * Controls whether to use the cell or row editing.
   * @default "cell"
   */
  editMode: import_prop_types140.default.oneOf(["cell", "row"]),
  /**
   * Use if the actual rowCount is not known upfront, but an estimation is available.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Applicable only with `paginationMode="server"` and when `rowCount="-1"`
   */
  estimatedRowCount: import_prop_types140.default.number,
  /**
   * Unstable features, breaking changes might be introduced.
   * For each feature, if the flag is not explicitly set to `true`, the feature will be fully disabled and any property / method call will not have any effect.
   */
  experimentalFeatures: import_prop_types140.default.shape({
    warnIfFocusStateIsNotSynced: import_prop_types140.default.bool
  }),
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering.
   * @default 150
   */
  filterDebounceMs: import_prop_types140.default.number,
  /**
   * Filtering can be processed on the server or client-side.
   * Set it to 'server' if you would like to handle filtering on the server-side.
   * @default "client"
   */
  filterMode: import_prop_types140.default.oneOf(["client", "server"]),
  /**
   * Set the filter model of the Data Grid.
   */
  filterModel: import_prop_types140.default.shape({
    items: import_prop_types140.default.arrayOf(import_prop_types140.default.shape({
      field: import_prop_types140.default.string.isRequired,
      id: import_prop_types140.default.oneOfType([import_prop_types140.default.number, import_prop_types140.default.string]),
      operator: import_prop_types140.default.string.isRequired,
      value: import_prop_types140.default.any
    })).isRequired,
    logicOperator: import_prop_types140.default.oneOf(["and", "or"]),
    quickFilterExcludeHiddenColumns: import_prop_types140.default.bool,
    quickFilterLogicOperator: import_prop_types140.default.oneOf(["and", "or"]),
    quickFilterValues: import_prop_types140.default.array
  }),
  /**
   * Function that applies CSS classes dynamically on cells.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {string} The CSS class to apply to the cell.
   */
  getCellClassName: import_prop_types140.default.func,
  /**
   * Function that returns the element to render in row detail.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {React.JSX.Element} The row detail element.
   */
  getDetailPanelContent: import_prop_types140.default.func,
  /**
   * Function that returns the estimated height for a row.
   * Only works if dynamic row height is used.
   * Once the row height is measured this value is discarded.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {number | null} The estimated row height value. If `null` or `undefined` then the default row height, based on the density, is applied.
   */
  getEstimatedRowHeight: import_prop_types140.default.func,
  /**
   * Function that applies CSS classes dynamically on rows.
   * @param {GridRowClassNameParams} params With all properties from [[GridRowClassNameParams]].
   * @returns {string} The CSS class to apply to the row.
   */
  getRowClassName: import_prop_types140.default.func,
  /**
   * Function that sets the row height per row.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {GridRowHeightReturnValue} The row height value. If `null` or `undefined` then the default row height is applied. If "auto" then the row height is calculated based on the content.
   */
  getRowHeight: import_prop_types140.default.func,
  /**
   * Return the id of a given [[GridRowModel]].
   * Ensure the reference of this prop is stable to avoid performance implications.
   * It could be done by either defining the prop outside of the component or by memoizing it.
   */
  getRowId: import_prop_types140.default.func,
  /**
   * Function that allows to specify the spacing between rows.
   * @param {GridRowSpacingParams} params With all properties from [[GridRowSpacingParams]].
   * @returns {GridRowSpacing} The row spacing values.
   */
  getRowSpacing: import_prop_types140.default.func,
  /**
   * If `true`, the footer component is hidden.
   * @default false
   */
  hideFooter: import_prop_types140.default.bool,
  /**
   * If `true`, the pagination component in the footer is hidden.
   * @default false
   */
  hideFooterPagination: import_prop_types140.default.bool,
  /**
   * If `true`, the selected row count in the footer is hidden.
   * @default false
   */
  hideFooterSelectedRowCount: import_prop_types140.default.bool,
  /**
   * If `true`, the diacritics (accents) are ignored when filtering or quick filtering.
   * E.g. when filter value is `cafe`, the rows with `café` will be visible.
   * @default false
   */
  ignoreDiacritics: import_prop_types140.default.bool,
  /**
   * If `true`, the Data Grid will not use `valueFormatter` when exporting to CSV or copying to clipboard.
   * If an object is provided, you can choose to ignore the `valueFormatter` for CSV export or clipboard export.
   * @default false
   */
  ignoreValueFormatterDuringExport: import_prop_types140.default.oneOfType([import_prop_types140.default.shape({
    clipboardExport: import_prop_types140.default.bool,
    csvExport: import_prop_types140.default.bool
  }), import_prop_types140.default.bool]),
  /**
   * The initial state of the DataGrid.
   * The data in it will be set in the state on initialization but will not be controlled.
   * If one of the data in `initialState` is also being controlled, then the control state wins.
   */
  initialState: import_prop_types140.default.object,
  /**
   * Callback fired when a cell is rendered, returns true if the cell is editable.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {boolean} A boolean indicating if the cell is editable.
   */
  isCellEditable: import_prop_types140.default.func,
  /**
   * Determines if a row can be selected.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {boolean} A boolean indicating if the row is selectable.
   */
  isRowSelectable: import_prop_types140.default.func,
  /**
   * If `true`, the selection model will retain selected rows that do not exist.
   * Useful when using server side pagination and row selections need to be retained
   * when changing pages.
   * @default false
   */
  keepNonExistentRowsSelected: import_prop_types140.default.bool,
  /**
   * The label of the Data Grid.
   * If the `showToolbar` prop is `true`, the label will be displayed in the toolbar and applied to the `aria-label` attribute of the grid.
   * If the `showToolbar` prop is `false`, the label will not be visible but will be applied to the `aria-label` attribute of the grid.
   */
  label: import_prop_types140.default.string,
  /**
   * If `true`, a loading overlay is displayed.
   * @default false
   */
  loading: import_prop_types140.default.bool,
  /**
   * Set the locale text of the Data Grid.
   * You can find all the translation keys supported in [the source](https://github.com/mui/mui-x/blob/HEAD/packages/x-data-grid/src/constants/localeTextConstants.ts) in the GitHub repository.
   */
  localeText: import_prop_types140.default.object,
  /**
   * Pass a custom logger in the components that implements the [[Logger]] interface.
   * @default console
   */
  logger: import_prop_types140.default.shape({
    debug: import_prop_types140.default.func.isRequired,
    error: import_prop_types140.default.func.isRequired,
    info: import_prop_types140.default.func.isRequired,
    warn: import_prop_types140.default.func.isRequired
  }),
  /**
   * Allows to pass the logging level or false to turn off logging.
   * @default "error" ("warn" in dev mode)
   */
  logLevel: import_prop_types140.default.oneOf(["debug", "error", "info", "warn", false]),
  /**
   * Nonce of the inline styles for [Content Security Policy](https://www.w3.org/TR/2016/REC-CSP2-20161215/#script-src-the-nonce-attribute).
   */
  nonce: import_prop_types140.default.string,
  /**
   * Callback fired when any cell is clicked.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellClick: import_prop_types140.default.func,
  /**
   * Callback fired when a double click event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellDoubleClick: import_prop_types140.default.func,
  /**
   * Callback fired when the cell turns to edit mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStart: import_prop_types140.default.func,
  /**
   * Callback fired when the cell turns to view mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStop: import_prop_types140.default.func,
  /**
   * Callback fired when a keydown event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellKeyDown: import_prop_types140.default.func,
  /**
   * Callback fired when the `cellModesModel` prop changes.
   * @param {GridCellModesModel} cellModesModel Object containing which cells are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellModesModelChange: import_prop_types140.default.func,
  /**
   * Callback called when the data is copied to the clipboard.
   * @param {string} data The data copied to the clipboard.
   */
  onClipboardCopy: import_prop_types140.default.func,
  /**
   * Callback fired when a click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderClick: import_prop_types140.default.func,
  /**
   * Callback fired when a contextmenu event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   */
  onColumnHeaderContextMenu: import_prop_types140.default.func,
  /**
   * Callback fired when a double click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderDoubleClick: import_prop_types140.default.func,
  /**
   * Callback fired when a mouse enter event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderEnter: import_prop_types140.default.func,
  /**
   * Callback fired when a mouse leave event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderLeave: import_prop_types140.default.func,
  /**
   * Callback fired when a mouseout event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOut: import_prop_types140.default.func,
  /**
   * Callback fired when a mouseover event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOver: import_prop_types140.default.func,
  /**
   * Callback fired when a column is reordered.
   * @param {GridColumnOrderChangeParams} params With all properties from [[GridColumnOrderChangeParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnOrderChange: import_prop_types140.default.func,
  /**
   * Callback fired while a column is being resized.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnResize: import_prop_types140.default.func,
  /**
   * Callback fired when the column visibility model changes.
   * @param {GridColumnVisibilityModel} model The new model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnVisibilityModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the width of a column is changed.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnWidthChange: import_prop_types140.default.func,
  /**
   * Callback fired when a data source request fails.
   * @param {GridGetRowsError | GridUpdateRowError} error The data source error object.
   */
  onDataSourceError: import_prop_types140.default.func,
  /**
   * Callback fired when the density changes.
   * @param {GridDensity} density New density value.
   */
  onDensityChange: import_prop_types140.default.func,
  /**
   * Callback fired when the Filter model changes before the filters are applied.
   * @param {GridFilterModel} model With all properties from [[GridFilterModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onFilterModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the menu is closed.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuClose: import_prop_types140.default.func,
  /**
   * Callback fired when the menu is opened.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuOpen: import_prop_types140.default.func,
  /**
   * Callback fired when the pagination meta has changed.
   * @param {GridPaginationMeta} paginationMeta Updated pagination meta.
   */
  onPaginationMetaChange: import_prop_types140.default.func,
  /**
   * Callback fired when the pagination model has changed.
   * @param {GridPaginationModel} model Updated pagination model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPaginationModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the preferences panel is closed.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelClose: import_prop_types140.default.func,
  /**
   * Callback fired when the preferences panel is opened.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelOpen: import_prop_types140.default.func,
  /**
   * Callback called when `processRowUpdate()` throws an error or rejects.
   * @param {any} error The error thrown.
   */
  onProcessRowUpdateError: import_prop_types140.default.func,
  /**
   * Callback fired when the Data Grid is resized.
   * @param {ElementSize} containerSize With all properties from [[ElementSize]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onResize: import_prop_types140.default.func,
  /**
   * Callback fired when a row is clicked.
   * Not called if the target clicked is an interactive element added by the built-in columns.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowClick: import_prop_types140.default.func,
  /**
   * Callback fired when the row count has changed.
   * @param {number} count Updated row count.
   */
  onRowCountChange: import_prop_types140.default.func,
  /**
   * Callback fired when a double click event comes from a row container element.
   * @param {GridRowParams} params With all properties from [[RowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowDoubleClick: import_prop_types140.default.func,
  /**
   * Callback fired when the row turns to edit mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStart: import_prop_types140.default.func,
  /**
   * Callback fired when the row turns to view mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStop: import_prop_types140.default.func,
  /**
   * Callback fired when the `rowModesModel` prop changes.
   * @param {GridRowModesModel} rowModesModel Object containing which rows are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowModesModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the selection state of one or multiple rows changes.
   * @param {GridRowSelectionModel} rowSelectionModel With all the row ids [[GridSelectionModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowSelectionModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the sort model changes before a column is sorted.
   * @param {GridSortModel} model With all properties from [[GridSortModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onSortModelChange: import_prop_types140.default.func,
  /**
   * Callback fired when the state of the Data Grid is updated.
   * @param {GridState} state The new state.
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @ignore - do not document.
   */
  onStateChange: import_prop_types140.default.func,
  /**
   * Select the pageSize dynamically using the component UI.
   * @default [25, 50, 100]
   */
  pageSizeOptions: import_prop_types140.default.arrayOf(import_prop_types140.default.oneOfType([import_prop_types140.default.number, import_prop_types140.default.shape({
    label: import_prop_types140.default.string.isRequired,
    value: import_prop_types140.default.number.isRequired
  })]).isRequired),
  pagination: import_prop_types140.default.oneOf([true]),
  /**
   * The extra information about the pagination state of the Data Grid.
   * Only applicable with `paginationMode="server"`.
   */
  paginationMeta: import_prop_types140.default.shape({
    hasNextPage: import_prop_types140.default.bool
  }),
  /**
   * Pagination can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle the pagination on the client-side.
   * Set it to 'server' if you would like to handle the pagination on the server-side.
   * @default "client"
   */
  paginationMode: import_prop_types140.default.oneOf(["client", "server"]),
  /**
   * The pagination model of type [[GridPaginationModel]] which refers to current `page` and `pageSize`.
   */
  paginationModel: import_prop_types140.default.shape({
    page: import_prop_types140.default.number.isRequired,
    pageSize: import_prop_types140.default.number.isRequired
  }),
  /**
   * Callback called before updating a row with new values in the row and cell editing.
   * @template R
   * @param {R} newRow Row object with the new values.
   * @param {R} oldRow Row object with the old values.
   * @param {{ rowId: GridRowId }} params Additional parameters.
   * @returns {Promise<R> | R} The final values to update the row.
   */
  processRowUpdate: import_prop_types140.default.func,
  /**
   * The milliseconds throttle delay for resizing the grid.
   * @default 60
   */
  resizeThrottleMs: import_prop_types140.default.number,
  /**
   * Row region in pixels to render before/after the viewport
   * @default 150
   */
  rowBufferPx: import_prop_types140.default.number,
  /**
   * Set the total number of rows, if it is different from the length of the value `rows` prop.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Only works with `paginationMode="server"`, ignored when `paginationMode="client"`.
   */
  rowCount: import_prop_types140.default.number,
  /**
   * Sets the height in pixel of a row in the Data Grid.
   * @default 52
   */
  rowHeight: import_prop_types140.default.number,
  /**
   * Controls the modes of the rows.
   */
  rowModesModel: import_prop_types140.default.object,
  /**
   * Set of rows of type [[GridRowsProp]].
   * @default []
   */
  rows: import_prop_types140.default.arrayOf(import_prop_types140.default.object),
  /**
   * If `false`, the row selection mode is disabled.
   * @default true
   */
  rowSelection: import_prop_types140.default.bool,
  /**
   * Sets the row selection model of the Data Grid.
   */
  rowSelectionModel: import_prop_types140.default.shape({
    ids: import_prop_types140.default.instanceOf(Set).isRequired,
    type: import_prop_types140.default.oneOf(["exclude", "include"]).isRequired
  }),
  /**
   * Sets the type of space between rows added by `getRowSpacing`.
   * @default "margin"
   */
  rowSpacingType: import_prop_types140.default.oneOf(["border", "margin"]),
  /**
   * If `true`, the Data Grid will auto span the cells over the rows having the same value.
   * @default false
   */
  rowSpanning: import_prop_types140.default.bool,
  /**
   * Override the height/width of the Data Grid inner scrollbar.
   */
  scrollbarSize: import_prop_types140.default.number,
  /**
   * If `true`, vertical borders will be displayed between cells.
   * @default false
   */
  showCellVerticalBorder: import_prop_types140.default.bool,
  /**
   * If `true`, vertical borders will be displayed between column header items.
   * @default false
   */
  showColumnVerticalBorder: import_prop_types140.default.bool,
  /**
   * If `true`, the toolbar is displayed.
   * @default false
   */
  showToolbar: import_prop_types140.default.bool,
  /**
   * Overridable components props dynamically passed to the component at rendering.
   */
  slotProps: import_prop_types140.default.object,
  /**
   * Overridable components.
   */
  slots: import_prop_types140.default.object,
  /**
   * Sorting can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle sorting on the client-side.
   * Set it to 'server' if you would like to handle sorting on the server-side.
   * @default "client"
   */
  sortingMode: import_prop_types140.default.oneOf(["client", "server"]),
  /**
   * The order of the sorting sequence.
   * @default ['asc', 'desc', null]
   */
  sortingOrder: import_prop_types140.default.arrayOf(import_prop_types140.default.oneOf(["asc", "desc"])),
  /**
   * Set the sort model of the Data Grid.
   */
  sortModel: import_prop_types140.default.arrayOf(import_prop_types140.default.shape({
    field: import_prop_types140.default.string.isRequired,
    sort: import_prop_types140.default.oneOf(["asc", "desc"])
  })),
  style: import_prop_types140.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types140.default.oneOfType([import_prop_types140.default.arrayOf(import_prop_types140.default.oneOfType([import_prop_types140.default.func, import_prop_types140.default.object, import_prop_types140.default.bool])), import_prop_types140.default.func, import_prop_types140.default.object]),
  /**
   * If `true`, the Data Grid enables column virtualization when `getRowHeight` is set to `() => 'auto'`.
   * By default, column virtualization is disabled when dynamic row height is enabled to measure the row height correctly.
   * For datasets with a large number of columns, this can cause performance issues.
   * The downside of enabling this prop is that the row height will be estimated based the cells that are currently rendered, which can cause row height change when scrolling horizontally.
   * @default false
   */
  virtualizeColumnsWithAutoRowHeight: import_prop_types140.default.bool
};

// node_modules/@mui/x-data-grid/esm/hooks/features/headerFiltering/useGridHeaderFiltering.js
var React259 = __toESM(require_react(), 1);
var headerFilteringStateInitializer = (state, props) => _extends({}, state, {
  headerFiltering: {
    enabled: props.headerFilters ?? false,
    editing: null,
    menuOpen: null
  }
});
var useGridHeaderFiltering = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridHeaderFiltering");
  const setHeaderFilterState = React259.useCallback((headerFilterState) => {
    apiRef.current.setState((state) => {
      if (props.signature === "DataGrid") {
        return state;
      }
      return _extends({}, state, {
        headerFiltering: {
          enabled: props.headerFilters ?? false,
          editing: headerFilterState.editing ?? null,
          menuOpen: headerFilterState.menuOpen ?? null
        }
      });
    });
  }, [apiRef, props.signature, props.headerFilters]);
  const startHeaderFilterEditMode = React259.useCallback((field) => {
    logger.debug(`Starting edit mode on header filter for field: ${field}`);
    apiRef.current.setHeaderFilterState({
      editing: field
    });
  }, [apiRef, logger]);
  const stopHeaderFilterEditMode = React259.useCallback(() => {
    logger.debug(`Stopping edit mode on header filter`);
    apiRef.current.setHeaderFilterState({
      editing: null
    });
  }, [apiRef, logger]);
  const showHeaderFilterMenu = React259.useCallback((field) => {
    logger.debug(`Opening header filter menu for field: ${field}`);
    apiRef.current.setHeaderFilterState({
      menuOpen: field
    });
  }, [apiRef, logger]);
  const hideHeaderFilterMenu = React259.useCallback(() => {
    logger.debug(`Hiding header filter menu for active field`);
    let fieldToFocus = apiRef.current.state.headerFiltering.menuOpen;
    if (fieldToFocus) {
      const columnLookup = gridColumnLookupSelector(apiRef);
      const columnVisibilityModel = gridColumnVisibilityModelSelector(apiRef);
      const orderedFields = gridColumnFieldsSelector(apiRef);
      if (!columnLookup[fieldToFocus]) {
        fieldToFocus = orderedFields[0];
      }
      if (columnVisibilityModel[fieldToFocus] === false) {
        const visibleOrderedFields = orderedFields.filter((field) => {
          if (field === fieldToFocus) {
            return true;
          }
          return columnVisibilityModel[field] !== false;
        });
        const fieldIndex = visibleOrderedFields.indexOf(fieldToFocus);
        fieldToFocus = visibleOrderedFields[fieldIndex + 1] || visibleOrderedFields[fieldIndex - 1];
      }
      apiRef.current.setHeaderFilterState({
        menuOpen: null
      });
      apiRef.current.setColumnHeaderFilterFocus(fieldToFocus);
    }
  }, [apiRef, logger]);
  const headerFilterPrivateApi = {
    setHeaderFilterState
  };
  const headerFilterApi = {
    startHeaderFilterEditMode,
    stopHeaderFilterEditMode,
    showHeaderFilterMenu,
    hideHeaderFilterMenu
  };
  useGridApiMethod(apiRef, headerFilterApi, "public");
  useGridApiMethod(apiRef, headerFilterPrivateApi, "private");
  const isFirstRender = React259.useRef(true);
  React259.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
    } else {
      apiRef.current.setHeaderFilterState({
        enabled: props.headerFilters ?? false
      });
    }
  }, [apiRef, props.headerFilters]);
};

// node_modules/@mui/x-data-grid/esm/internals/demo/TailwindDemoContainer.js
var React260 = __toESM(require_react(), 1);
var import_jsx_runtime184 = __toESM(require_jsx_runtime(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/utils/useGridRootProps.js
var useGridRootProps2 = useGridRootProps;

// node_modules/@mui/x-data-grid-pro/esm/hooks/utils/useGridApiContext.js
var useGridApiContext2 = useGridApiContext;

// node_modules/@mui/x-data-grid-pro/esm/components/GridTreeDataGroupingCell.js
var import_jsx_runtime185 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses97 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["treeDataGroupingCell"],
    toggle: ["treeDataGroupingCellToggle"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridTreeDataGroupingCell(props) {
  var _a;
  const {
    id,
    field,
    formattedValue,
    rowNode,
    hideDescendantCount,
    offsetMultiplier = 2
  } = props;
  const rootProps = useGridRootProps2();
  const apiRef = useGridApiContext2();
  const classes2 = useUtilityClasses97(rootProps);
  const filteredDescendantCountLookup = useGridSelector(apiRef, gridFilteredDescendantCountLookupSelector);
  const filteredDescendantCount = filteredDescendantCountLookup[rowNode.id] ?? 0;
  const Icon = rowNode.childrenExpanded ? rootProps.slots.treeDataCollapseIcon : rootProps.slots.treeDataExpandIcon;
  const handleClick = (event) => {
    apiRef.current.setRowChildrenExpansion(id, !rowNode.childrenExpanded);
    apiRef.current.setCellFocus(id, field);
    event.stopPropagation();
  };
  return (0, import_jsx_runtime185.jsxs)("div", {
    className: classes2.root,
    style: {
      marginLeft: vars.spacing(rowNode.depth * offsetMultiplier)
    },
    children: [(0, import_jsx_runtime185.jsx)("div", {
      className: classes2.toggle,
      children: filteredDescendantCount > 0 && (0, import_jsx_runtime185.jsx)(rootProps.slots.baseIconButton, _extends({
        size: "small",
        onClick: handleClick,
        tabIndex: -1,
        "aria-label": rowNode.childrenExpanded ? apiRef.current.getLocaleText("treeDataCollapse") : apiRef.current.getLocaleText("treeDataExpand")
      }, (_a = rootProps == null ? void 0 : rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
        children: (0, import_jsx_runtime185.jsx)(Icon, {
          fontSize: "inherit"
        })
      }))
    }), (0, import_jsx_runtime185.jsxs)("span", {
      children: [formattedValue === void 0 ? rowNode.groupingKey : formattedValue, !hideDescendantCount && filteredDescendantCount > 0 ? ` (${filteredDescendantCount})` : ""]
    })]
  });
}
true ? GridTreeDataGroupingCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types141.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types141.default.oneOf(["edit", "view"]).isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types141.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types141.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types141.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types141.default.bool.isRequired,
  hideDescendantCount: import_prop_types141.default.bool,
  /**
   * The grid row id.
   */
  id: import_prop_types141.default.oneOfType([import_prop_types141.default.number, import_prop_types141.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types141.default.bool,
  /**
   * The cell offset multiplier used for calculating cell offset (`rowNode.depth * offsetMultiplier` px).
   * @default 2
   */
  offsetMultiplier: import_prop_types141.default.number,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types141.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types141.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types141.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types141.default.any
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/components/GridColumnMenuPinningItem.js
var React262 = __toESM(require_react(), 1);
var import_prop_types142 = __toESM(require_prop_types(), 1);
var import_jsx_runtime186 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuPinningItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext2();
  const rootProps = useGridRootProps2();
  const isRtl = useRtl();
  const pinColumn = React262.useCallback((side2) => (event) => {
    apiRef.current.pinColumn(colDef.field, side2);
    onClick(event);
  }, [apiRef, colDef.field, onClick]);
  const unpinColumn = (event) => {
    apiRef.current.unpinColumn(colDef.field);
    onClick(event);
  };
  const pinToLeftMenuItem = (0, import_jsx_runtime186.jsx)(rootProps.slots.baseMenuItem, {
    onClick: pinColumn(GridPinnedColumnPosition.LEFT),
    iconStart: (0, import_jsx_runtime186.jsx)(rootProps.slots.columnMenuPinLeftIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("pinToLeft")
  });
  const pinToRightMenuItem = (0, import_jsx_runtime186.jsx)(rootProps.slots.baseMenuItem, {
    onClick: pinColumn(GridPinnedColumnPosition.RIGHT),
    iconStart: (0, import_jsx_runtime186.jsx)(rootProps.slots.columnMenuPinRightIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("pinToRight")
  });
  if (!colDef) {
    return null;
  }
  const side = apiRef.current.isColumnPinned(colDef.field);
  if (side) {
    const otherSide = side === GridPinnedColumnPosition.RIGHT ? GridPinnedColumnPosition.LEFT : GridPinnedColumnPosition.RIGHT;
    const label = otherSide === GridPinnedColumnPosition.RIGHT ? "pinToRight" : "pinToLeft";
    const Icon = side === GridPinnedColumnPosition.RIGHT ? rootProps.slots.columnMenuPinLeftIcon : rootProps.slots.columnMenuPinRightIcon;
    return (0, import_jsx_runtime186.jsxs)(React262.Fragment, {
      children: [(0, import_jsx_runtime186.jsx)(rootProps.slots.baseMenuItem, {
        onClick: pinColumn(otherSide),
        iconStart: (0, import_jsx_runtime186.jsx)(Icon, {
          fontSize: "small"
        }),
        children: apiRef.current.getLocaleText(label)
      }), (0, import_jsx_runtime186.jsx)(rootProps.slots.baseMenuItem, {
        onClick: unpinColumn,
        iconStart: (0, import_jsx_runtime186.jsx)("span", {}),
        children: apiRef.current.getLocaleText("unpin")
      })]
    });
  }
  if (isRtl) {
    return (0, import_jsx_runtime186.jsxs)(React262.Fragment, {
      children: [pinToRightMenuItem, pinToLeftMenuItem]
    });
  }
  return (0, import_jsx_runtime186.jsxs)(React262.Fragment, {
    children: [pinToLeftMenuItem, pinToRightMenuItem]
  });
}
true ? GridColumnMenuPinningItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types142.default.object.isRequired,
  onClick: import_prop_types142.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanelToggleCell.js
var React263 = __toESM(require_react(), 1);
var import_prop_types143 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/detailPanel/gridDetailPanelSelector.js
var gridDetailPanelStateSelector = createRootSelector((state) => state.detailPanel);
var gridDetailPanelExpandedRowIdsSelector = createSelector3(gridDetailPanelStateSelector, (detailPanelState) => detailPanelState.expandedRowIds);
var gridDetailPanelExpandedRowsContentCacheSelector = createSelector3(gridDetailPanelStateSelector, (detailPanelState) => detailPanelState.contentCache);
var gridDetailPanelRawHeightCacheSelector = createSelectorMemoized2(gridDetailPanelStateSelector, (detailPanelState) => detailPanelState.heightCache);

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanelToggleCell.js
var import_jsx_runtime187 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses98 = (ownerState) => {
  const {
    classes: classes2,
    isExpanded
  } = ownerState;
  const slots = {
    root: ["detailPanelToggleCell", isExpanded && "detailPanelToggleCell--expanded"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var isExpandedSelector = createSelector3(gridDetailPanelExpandedRowIdsSelector, (expandedRowIds, rowId) => {
  return expandedRowIds.has(rowId);
});
function GridDetailPanelToggleCell(props) {
  var _a;
  const {
    id,
    row,
    api
  } = props;
  const rowId = api.getRowId(row);
  const isExpanded = useGridSelector({
    current: api
  }, isExpandedSelector, rowId);
  const rootProps = useGridRootProps2();
  const apiRef = useGridApiContext2();
  const ownerState = {
    classes: rootProps.classes,
    isExpanded
  };
  const classes2 = useUtilityClasses98(ownerState);
  const contentCache = useGridSelector(apiRef, gridDetailPanelExpandedRowsContentCacheSelector);
  const hasContent = React263.isValidElement(contentCache[id]);
  const Icon = isExpanded ? rootProps.slots.detailPanelCollapseIcon : rootProps.slots.detailPanelExpandIcon;
  return (0, import_jsx_runtime187.jsx)(rootProps.slots.baseIconButton, _extends({
    size: "small",
    tabIndex: -1,
    disabled: !hasContent,
    className: classes2.root,
    "aria-expanded": isExpanded,
    "aria-label": isExpanded ? apiRef.current.getLocaleText("collapseDetailPanel") : apiRef.current.getLocaleText("expandDetailPanel")
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    children: (0, import_jsx_runtime187.jsx)(Icon, {
      fontSize: "inherit"
    })
  }));
}
true ? GridDetailPanelToggleCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types143.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types143.default.oneOf(["edit", "view"]).isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types143.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types143.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types143.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types143.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types143.default.oneOfType([import_prop_types143.default.number, import_prop_types143.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types143.default.bool,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types143.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types143.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types143.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types143.default.any
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/components/GridRowReorderCell.js
var React264 = __toESM(require_react(), 1);
var import_prop_types144 = __toESM(require_prop_types(), 1);
var import_jsx_runtime188 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses99 = (ownerState) => {
  const {
    isDraggable,
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["rowReorderCell", isDraggable && "rowReorderCell--draggable"],
    placeholder: ["rowReorderCellPlaceholder"],
    icon: ["rowReorderIcon"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var RowReorderIcon = styled_default2("svg", {
  name: "MuiDataGrid",
  slot: "RowReorderIcon"
})({
  color: vars.colors.foreground.muted
});
function GridRowReorderCell(params) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps2();
  const sortModel = useGridSelector(apiRef, gridSortModelSelector);
  const editRowsState = useGridSelector(apiRef, gridEditRowsStateSelector);
  const cellValue = (
    // eslint-disable-next-line no-underscore-dangle
    params.row.__reorder__ || (params.rowNode.type === "group" ? params.rowNode.groupingKey ?? params.id : params.id)
  );
  const cellRef = React264.useRef(null);
  const listenerNodeRef = React264.useRef(null);
  const isRowReorderable = rootProps.isRowReorderable;
  const isDraggable = React264.useMemo(() => {
    const baseCondition = !!rootProps.rowReordering && !sortModel.length && Object.keys(editRowsState).length === 0;
    if (!baseCondition) {
      return false;
    }
    if (isRowReorderable) {
      return isRowReorderable({
        row: params.row,
        rowNode: params.rowNode
      });
    }
    return true;
  }, [rootProps.rowReordering, isRowReorderable, sortModel, editRowsState, params.row, params.rowNode]);
  const ownerState = {
    isDraggable,
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses99(ownerState);
  const publish = React264.useCallback((eventName, propHandler) => (event) => {
    if (isEventTargetInPortal(event)) {
      return;
    }
    if (!apiRef.current.getRow(params.id)) {
      return;
    }
    apiRef.current.publishEvent(eventName, apiRef.current.getRowParams(params.id), event);
    if (propHandler) {
      propHandler(event);
    }
  }, [apiRef, params.id]);
  const handleMouseDown = React264.useCallback(() => {
    var _a, _b;
    (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.classList.add(gridClasses["root--disableUserSelection"]);
  }, [apiRef]);
  const handleMouseUp = React264.useCallback(() => {
    var _a, _b;
    (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.classList.remove(gridClasses["root--disableUserSelection"]);
  }, [apiRef]);
  const handleDragEnd = React264.useCallback((event) => {
    handleMouseUp();
    if (apiRef.current.getRow(params.id)) {
      apiRef.current.publishEvent("rowDragEnd", apiRef.current.getRowParams(params.id), event);
    }
    listenerNodeRef.current.removeEventListener("dragend", handleDragEnd);
    listenerNodeRef.current = null;
  }, [apiRef, params.id, handleMouseUp]);
  const handleDragStart = React264.useCallback((event) => {
    if (!cellRef.current) {
      return;
    }
    publish("rowDragStart")(event);
    cellRef.current.addEventListener("dragend", handleDragEnd);
    listenerNodeRef.current = cellRef.current;
  }, [publish, handleDragEnd]);
  const draggableEventHandlers = isDraggable ? {
    onDragStart: handleDragStart,
    onDragOver: publish("rowDragOver"),
    onMouseDown: handleMouseDown,
    onMouseUp: handleMouseUp
  } : null;
  if (params.rowNode.type === "footer") {
    return null;
  }
  return (0, import_jsx_runtime188.jsxs)("div", _extends({
    ref: cellRef,
    className: classes2.root,
    draggable: isDraggable
  }, draggableEventHandlers, {
    children: [(0, import_jsx_runtime188.jsx)(RowReorderIcon, {
      as: rootProps.slots.rowReorderIcon,
      ownerState,
      className: classes2.icon,
      fontSize: "small"
    }), (0, import_jsx_runtime188.jsx)("div", {
      className: classes2.placeholder,
      children: cellValue
    })]
  }));
}
true ? GridRowReorderCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * GridApi that let you manipulate the grid.
   */
  api: import_prop_types144.default.object.isRequired,
  /**
   * The mode of the cell.
   */
  cellMode: import_prop_types144.default.oneOf(["edit", "view"]).isRequired,
  /**
   * The column of the row that the current cell belongs to.
   */
  colDef: import_prop_types144.default.object.isRequired,
  /**
   * The column field of the cell that triggered the event.
   */
  field: import_prop_types144.default.string.isRequired,
  /**
   * The cell value formatted with the column valueFormatter.
   */
  formattedValue: import_prop_types144.default.any,
  /**
   * If true, the cell is the active element.
   */
  hasFocus: import_prop_types144.default.bool.isRequired,
  /**
   * The grid row id.
   */
  id: import_prop_types144.default.oneOfType([import_prop_types144.default.number, import_prop_types144.default.string]).isRequired,
  /**
   * If true, the cell is editable.
   */
  isEditable: import_prop_types144.default.bool,
  /**
   * The row model of the row that the current cell belongs to.
   */
  row: import_prop_types144.default.any.isRequired,
  /**
   * The node of the row that the current cell belongs to.
   */
  rowNode: import_prop_types144.default.object.isRequired,
  /**
   * the tabIndex value.
   */
  tabIndex: import_prop_types144.default.oneOf([-1, 0]).isRequired,
  /**
   * The cell value.
   * If the column has `valueGetter`, use `params.row` to directly access the fields.
   */
  value: import_prop_types144.default.any
} : void 0;
var renderRowReorderCell = (params) => {
  if (params.rowNode.type === "footer" || params.rowNode.type === "pinnedRow") {
    return null;
  }
  return (0, import_jsx_runtime188.jsx)(GridRowReorderCell, _extends({}, params));
};
if (true) renderRowReorderCell.displayName = "renderRowReorderCell";

// node_modules/@mui/x-data-grid-pro/esm/material/icons.js
var import_jsx_runtime189 = __toESM(require_jsx_runtime(), 1);
var GridPushPinRightIcon = createSvgIcon2((0, import_jsx_runtime189.jsx)("g", {
  transform: "rotate(-30 15 10)",
  children: (0, import_jsx_runtime189.jsx)("path", {
    d: "M16,9V4l1,0c0.55,0,1-0.45,1-1v0c0-0.55-0.45-1-1-1H7C6.45,2,6,2.45,6,3v0 c0,0.55,0.45,1,1,1l1,0v5c0,1.66-1.34,3-3,3h0v2h5.97v7l1,1l1-1v-7H19v-2h0C17.34,12,16,10.66,16,9z",
    fillRule: "evenodd"
  })
}), "PushPinRight");
var GridPushPinLeftIcon = createSvgIcon2((0, import_jsx_runtime189.jsx)("g", {
  transform: "rotate(30 8 12)",
  children: (0, import_jsx_runtime189.jsx)("path", {
    d: "M16,9V4l1,0c0.55,0,1-0.45,1-1v0c0-0.55-0.45-1-1-1H7C6.45,2,6,2.45,6,3v0 c0,0.55,0.45,1,1,1l1,0v5c0,1.66-1.34,3-3,3h0v2h5.97v7l1,1l1-1v-7H19v-2h0C17.34,12,16,10.66,16,9z",
    fillRule: "evenodd"
  })
}), "PushPinLeft");

// node_modules/@mui/x-data-grid-pro/esm/components/headerFiltering/GridHeaderFilterMenuContainer.js
var React265 = __toESM(require_react(), 1);
var import_prop_types145 = __toESM(require_prop_types(), 1);
var import_jsx_runtime190 = __toESM(require_jsx_runtime(), 1);
var _excluded70 = ["operators", "item", "field", "buttonRef", "headerFilterMenuRef", "disabled", "showClearItem", "clearFilterItem"];
function GridHeaderFilterMenuContainer(props) {
  var _a;
  const {
    operators,
    item,
    field,
    buttonRef,
    headerFilterMenuRef,
    disabled = false,
    showClearItem,
    clearFilterItem
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded70);
  const buttonId = useId();
  const menuId = useId();
  const rootProps = useGridRootProps2();
  const apiRef = useGridApiContext();
  const menuOpenField = useGridSelector(apiRef, gridHeaderFilteringMenuSelector);
  const open = Boolean(menuOpenField === field && headerFilterMenuRef.current);
  const handleClick = (event) => {
    headerFilterMenuRef.current = event.currentTarget;
    apiRef.current.showHeaderFilterMenu(field);
  };
  if (!rootProps.slots.headerFilterMenu) {
    return null;
  }
  const label = apiRef.current.getLocaleText("filterPanelOperator");
  const labelString = label ? String(label) : void 0;
  return (0, import_jsx_runtime190.jsxs)(React265.Fragment, {
    children: [(0, import_jsx_runtime190.jsx)(rootProps.slots.baseIconButton, _extends({
      id: buttonId,
      ref: buttonRef,
      "aria-label": labelString,
      title: labelString,
      "aria-controls": menuId,
      "aria-expanded": open ? "true" : void 0,
      "aria-haspopup": "true",
      tabIndex: -1,
      size: "small",
      onClick: handleClick,
      disabled
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
      children: (0, import_jsx_runtime190.jsx)(rootProps.slots.baseBadge, {
        color: "primary",
        variant: "dot",
        badgeContent: showClearItem ? 1 : 0,
        children: (0, import_jsx_runtime190.jsx)(rootProps.slots.openFilterButtonIcon, {
          fontSize: "inherit"
        })
      })
    })), (0, import_jsx_runtime190.jsx)(rootProps.slots.headerFilterMenu, _extends({
      field,
      open,
      item,
      target: headerFilterMenuRef.current,
      operators,
      labelledBy: buttonId,
      id: menuId,
      clearFilterItem,
      showClearItem
    }, other))]
  });
}
true ? GridHeaderFilterMenuContainer.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  applyFilterChanges: import_prop_types145.default.func.isRequired,
  buttonRef: refType_default,
  clearFilterItem: import_prop_types145.default.func,
  disabled: import_prop_types145.default.bool,
  field: import_prop_types145.default.string.isRequired,
  headerFilterMenuRef: import_prop_types145.default.shape({
    current: import_prop_types145.default.object
  }).isRequired,
  item: import_prop_types145.default.shape({
    field: import_prop_types145.default.string.isRequired,
    id: import_prop_types145.default.oneOfType([import_prop_types145.default.number, import_prop_types145.default.string]),
    operator: import_prop_types145.default.string.isRequired,
    value: import_prop_types145.default.any
  }).isRequired,
  operators: import_prop_types145.default.arrayOf(import_prop_types145.default.shape({
    getApplyFilterFn: import_prop_types145.default.func.isRequired,
    getValueAsString: import_prop_types145.default.func,
    headerLabel: import_prop_types145.default.string,
    InputComponent: import_prop_types145.default.elementType,
    InputComponentProps: import_prop_types145.default.shape({
      apiRef: import_prop_types145.default.shape({
        current: import_prop_types145.default.object.isRequired
      }),
      applyValue: import_prop_types145.default.func,
      className: import_prop_types145.default.string,
      clearButton: import_prop_types145.default.node,
      disabled: import_prop_types145.default.bool,
      focusElementRef: import_prop_types145.default.oneOfType([import_prop_types145.default.func, import_prop_types145.default.shape({
        current: import_prop_types145.default.any.isRequired
      })]),
      headerFilterMenu: import_prop_types145.default.node,
      inputRef: import_prop_types145.default.oneOfType([import_prop_types145.default.func, import_prop_types145.default.shape({
        current: (props, propName) => {
          if (props[propName] == null) {
            return null;
          }
          if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
            return new Error(`Expected prop '${propName}' to be of type Element`);
          }
          return null;
        }
      })]),
      isFilterActive: import_prop_types145.default.bool,
      item: import_prop_types145.default.shape({
        field: import_prop_types145.default.string.isRequired,
        id: import_prop_types145.default.oneOfType([import_prop_types145.default.number, import_prop_types145.default.string]),
        operator: import_prop_types145.default.string.isRequired,
        value: import_prop_types145.default.any
      }),
      onBlur: import_prop_types145.default.func,
      onFocus: import_prop_types145.default.func,
      slotProps: import_prop_types145.default.object,
      tabIndex: import_prop_types145.default.number
    }),
    label: import_prop_types145.default.string,
    requiresFilterValue: import_prop_types145.default.bool,
    value: import_prop_types145.default.string.isRequired
  })).isRequired,
  showClearItem: import_prop_types145.default.bool
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/components/headerFiltering/GridHeaderFilterCell.js
var React266 = __toESM(require_react(), 1);
var import_prop_types146 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-pro/esm/components/headerFiltering/GridHeaderFilterClearButton.js
var import_jsx_runtime191 = __toESM(require_jsx_runtime(), 1);
function GridHeaderFilterClearButton(props) {
  var _a;
  const rootProps = useGridRootProps2();
  return (0, import_jsx_runtime191.jsx)(rootProps.slots.baseIconButton, _extends({
    tabIndex: -1,
    "aria-label": "Clear filter",
    size: "small"
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, props, {
    children: (0, import_jsx_runtime191.jsx)(rootProps.slots.columnMenuClearIcon, {
      fontSize: "inherit"
    })
  }));
}

// node_modules/@mui/x-data-grid-pro/esm/components/headerFiltering/GridHeaderFilterCell.js
var import_jsx_runtime192 = __toESM(require_jsx_runtime(), 1);
var _excluded71 = ["colIndex", "height", "hasFocus", "width", "headerClassName", "colDef", "item", "headerFilterMenuRef", "InputComponentProps", "showClearIcon", "pinnedPosition", "pinnedOffset", "style", "showLeftBorder", "showRightBorder"];
var StyledInputComponent = styled_default2(GridFilterInputValue, {
  name: "MuiDataGrid",
  slot: "ColumnHeaderFilterInput"
})({
  flex: 1,
  marginRight: vars.spacing(0.5),
  marginBottom: vars.spacing(-0.25),
  '& input[type="date"], & input[type="datetime-local"]': {
    '&[value=""]:not(:focus)': {
      color: "transparent"
    }
  },
  [`& .${inputBaseClasses_default.input}`]: {
    fontSize: "14px"
  },
  [`.${gridClasses["root--densityCompact"]} & .${inputBaseClasses_default.input}`]: {
    paddingTop: vars.spacing(0.25),
    paddingBottom: vars.spacing(0.25),
    height: 20
  }
});
var OperatorLabel = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "ColumnHeaderFilterOperatorLabel"
})({
  flex: 1,
  marginRight: vars.spacing(0.5),
  color: vars.colors.foreground.muted,
  whiteSpace: "nowrap",
  textOverflow: "ellipsis",
  overflow: "hidden"
});
var useUtilityClasses100 = (ownerState) => {
  const {
    colDef,
    classes: classes2,
    showRightBorder,
    showLeftBorder,
    pinnedPosition
  } = ownerState;
  const slots = {
    root: ["columnHeader", "columnHeader--filter", colDef.headerAlign && `columnHeader--align${capitalize(colDef.headerAlign)}`, "withBorderColor", showRightBorder && "columnHeader--withRightBorder", showLeftBorder && "columnHeader--withLeftBorder", pinnedPosition === PinnedColumnPosition.LEFT && "columnHeader--pinnedLeft", pinnedPosition === PinnedColumnPosition.RIGHT && "columnHeader--pinnedRight"],
    input: ["columnHeaderFilterInput"],
    operatorLabel: ["columnHeaderFilterOperatorLabel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var DEFAULT_INPUT_COMPONENTS = {
  string: GridFilterInputValue,
  number: GridFilterInputValue,
  date: GridFilterInputDate,
  dateTime: GridFilterInputDate,
  boolean: GridFilterInputBoolean,
  singleSelect: GridFilterInputSingleSelect,
  actions: null,
  custom: null,
  longText: GridFilterInputValue
};
var GridHeaderFilterCell = forwardRef2((props, ref) => {
  const {
    colIndex,
    height,
    hasFocus,
    width,
    headerClassName,
    colDef,
    item,
    headerFilterMenuRef,
    InputComponentProps,
    showClearIcon = false,
    pinnedPosition,
    pinnedOffset,
    style: styleProp,
    showLeftBorder,
    showRightBorder
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded71);
  const apiRef = useGridPrivateApiContext();
  const isRtl = useRtl();
  const columnFields = useGridSelector(apiRef, gridVisibleColumnFieldsSelector);
  const rootProps = useGridRootProps2();
  const cellRef = React266.useRef(null);
  const handleRef = useForkRef(ref, cellRef);
  const inputRef = React266.useRef(null);
  const buttonRef = React266.useRef(null);
  const editingField = useGridSelector(apiRef, gridHeaderFilteringEditFieldSelector);
  const isEditing = editingField === colDef.field;
  const menuOpenField = useGridSelector(apiRef, gridHeaderFilteringMenuSelector);
  const isMenuOpen = menuOpenField === colDef.field;
  const filterOperators = React266.useMemo(() => {
    if (!colDef.filterOperators) {
      return [];
    }
    return colDef.filterOperators.filter((operator) => operator.value !== "isAnyOf");
  }, [colDef.filterOperators]);
  const filterModel = useGridSelector(apiRef, gridFilterModelSelector);
  const filterableColumnsLookup = useGridSelector(apiRef, gridFilterableColumnLookupSelector);
  const isFilterReadOnly = React266.useMemo(() => {
    if (!(filterModel == null ? void 0 : filterModel.items.length)) {
      return false;
    }
    const filterModelItem = filterModel.items.find((it) => it.field === colDef.field);
    return filterModelItem ? !filterableColumnsLookup[filterModelItem.field] : false;
  }, [colDef.field, filterModel, filterableColumnsLookup]);
  const currentOperator = React266.useMemo(() => filterOperators.find((operator) => operator.value === item.operator) ?? filterOperators[0], [item.operator, filterOperators]);
  const InputComponent = colDef.filterable || isFilterReadOnly ? currentOperator.InputComponent ?? DEFAULT_INPUT_COMPONENTS[colDef.type] : null;
  const clearFilterItem = React266.useCallback(() => {
    apiRef.current.deleteFilterItem(item);
  }, [apiRef, item]);
  let headerFilterComponent;
  if (colDef.renderHeaderFilter) {
    headerFilterComponent = colDef.renderHeaderFilter(_extends({}, props, {
      inputRef
    }));
  }
  React266.useLayoutEffect(() => {
    if (hasFocus && !isMenuOpen) {
      let focusableElement = cellRef.current.querySelector('[tabindex="0"]');
      if (isEditing && InputComponent) {
        focusableElement = inputRef.current;
      }
      const elementToFocus = focusableElement || cellRef.current;
      const scrollPosition = apiRef.current.getScrollPosition();
      elementToFocus == null ? void 0 : elementToFocus.focus();
      apiRef.current.scroll(scrollPosition);
    }
  }, [InputComponent, apiRef, hasFocus, isEditing, isMenuOpen]);
  const onKeyDown = React266.useCallback((event) => {
    if (isMenuOpen || isNavigationKey(event.key) || isFilterReadOnly) {
      return;
    }
    switch (event.key) {
      case "Escape":
        if (isEditing) {
          apiRef.current.stopHeaderFilterEditMode();
        }
        break;
      case "Enter":
        if (isEditing) {
          if (!event.defaultPrevented) {
            apiRef.current.stopHeaderFilterEditMode();
            break;
          }
        }
        if (event.metaKey || event.ctrlKey) {
          headerFilterMenuRef.current = buttonRef.current;
          apiRef.current.showHeaderFilterMenu(colDef.field);
          break;
        }
        apiRef.current.startHeaderFilterEditMode(colDef.field);
        break;
      case "Tab": {
        if (isEditing) {
          const fieldToFocus = columnFields[colIndex + (event.shiftKey ? -1 : 1)] ?? null;
          if (fieldToFocus) {
            apiRef.current.startHeaderFilterEditMode(fieldToFocus);
            apiRef.current.setColumnHeaderFilterFocus(fieldToFocus, event);
          }
        }
        break;
      }
      default:
        if (isEditing || event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) {
          break;
        }
        apiRef.current.startHeaderFilterEditMode(colDef.field);
        break;
    }
  }, [apiRef, colDef.field, colIndex, columnFields, headerFilterMenuRef, isEditing, isFilterReadOnly, isMenuOpen]);
  const publish = React266.useCallback((eventName, propHandler) => (event) => {
    apiRef.current.publishEvent(eventName, apiRef.current.getColumnHeaderParams(colDef.field), event);
    if (propHandler) {
      propHandler(event);
    }
  }, [apiRef, colDef.field]);
  const onMouseDown = React266.useCallback((event) => {
    var _a, _b;
    if (!hasFocus) {
      if ((_b = (_a = inputRef.current) == null ? void 0 : _a.contains) == null ? void 0 : _b.call(_a, event.target)) {
        inputRef.current.focus();
      }
      apiRef.current.setColumnHeaderFilterFocus(colDef.field, event);
    }
  }, [apiRef, colDef.field, hasFocus]);
  const mouseEventsHandlers = React266.useMemo(() => ({
    onKeyDown: publish("headerFilterKeyDown", onKeyDown),
    onClick: publish("headerFilterClick"),
    onMouseDown: publish("headerFilterMouseDown", onMouseDown),
    onBlur: publish("headerFilterBlur")
  }), [onMouseDown, onKeyDown, publish]);
  const ownerState = _extends({}, rootProps, {
    pinnedPosition,
    colDef,
    showLeftBorder,
    showRightBorder
  });
  const classes2 = useUtilityClasses100(ownerState);
  const label = currentOperator.headerLabel ?? apiRef.current.getLocaleText(`headerFilterOperator${capitalize(item.operator)}`);
  const isNoInputOperator = currentOperator.requiresFilterValue === false;
  const isApplied = (item == null ? void 0 : item.value) !== void 0 || isNoInputOperator;
  const isFilterActive = isApplied || hasFocus;
  const headerFilterMenu = (0, import_jsx_runtime192.jsx)(GridHeaderFilterMenuContainer, {
    operators: filterOperators,
    item,
    field: colDef.field,
    disabled: isFilterReadOnly,
    applyFilterChanges: apiRef.current.upsertFilterItem,
    headerFilterMenuRef,
    buttonRef,
    showClearItem: !showClearIcon && isApplied,
    clearFilterItem
  });
  const clearButton = showClearIcon && isApplied ? (0, import_jsx_runtime192.jsx)(GridHeaderFilterClearButton, {
    onClick: clearFilterItem,
    disabled: isFilterReadOnly
  }) : null;
  return (0, import_jsx_runtime192.jsxs)("div", _extends({
    className: clsx_default(classes2.root, headerClassName),
    style: attachPinnedStyle(_extends({
      height,
      width
    }, styleProp), isRtl, pinnedPosition, pinnedOffset),
    role: "columnheader",
    "aria-colindex": colIndex + 1,
    "aria-label": headerFilterComponent == null ? colDef.headerName ?? colDef.field : void 0
  }, other, mouseEventsHandlers, {
    ref: handleRef,
    children: [headerFilterComponent, headerFilterComponent === void 0 ? (0, import_jsx_runtime192.jsxs)(React266.Fragment, {
      children: [isNoInputOperator ? (0, import_jsx_runtime192.jsxs)(React266.Fragment, {
        children: [(0, import_jsx_runtime192.jsx)(OperatorLabel, {
          className: classes2.operatorLabel,
          children: label
        }), clearButton, headerFilterMenu]
      }) : null, InputComponent && !isNoInputOperator ? (0, import_jsx_runtime192.jsx)(StyledInputComponent, _extends({
        as: InputComponent,
        className: classes2.input,
        apiRef,
        item,
        inputRef,
        applyValue: apiRef.current.upsertFilterItem,
        onFocus: () => apiRef.current.startHeaderFilterEditMode(colDef.field),
        onBlur: (event) => {
          var _a;
          apiRef.current.stopHeaderFilterEditMode();
          if (!((_a = event.relatedTarget) == null ? void 0 : _a.className.includes("columnHeader"))) {
            apiRef.current.setState((state) => _extends({}, state, {
              focus: {
                cell: null,
                columnHeader: null,
                columnHeaderFilter: null,
                columnGroupHeader: null
              }
            }));
          }
        },
        isFilterActive,
        headerFilterMenu,
        clearButton,
        disabled: isFilterReadOnly || isNoInputOperator,
        tabIndex: -1,
        slotProps: {
          root: {
            size: "small",
            label: capitalize(label),
            placeholder: ""
          }
        }
      }, isNoInputOperator ? {
        value: ""
      } : {}, currentOperator == null ? void 0 : currentOperator.InputComponentProps, InputComponentProps)) : null]
    }) : null]
  }));
});
if (true) GridHeaderFilterCell.displayName = "GridHeaderFilterCell";
true ? GridHeaderFilterCell.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types146.default.object.isRequired,
  colIndex: import_prop_types146.default.number.isRequired,
  hasFocus: import_prop_types146.default.bool,
  /**
   * Class name added to the column header cell.
   */
  headerClassName: import_prop_types146.default.oneOfType([import_prop_types146.default.func, import_prop_types146.default.string]),
  headerFilterMenuRef: import_prop_types146.default.shape({
    current: import_prop_types146.default.object
  }).isRequired,
  height: import_prop_types146.default.number.isRequired,
  InputComponentProps: import_prop_types146.default.shape({
    apiRef: import_prop_types146.default.shape({
      current: import_prop_types146.default.object.isRequired
    }),
    applyValue: import_prop_types146.default.func,
    className: import_prop_types146.default.string,
    clearButton: import_prop_types146.default.node,
    disabled: import_prop_types146.default.bool,
    focusElementRef: import_prop_types146.default.oneOfType([import_prop_types146.default.func, import_prop_types146.default.shape({
      current: import_prop_types146.default.any.isRequired
    })]),
    headerFilterMenu: import_prop_types146.default.node,
    inputRef: import_prop_types146.default.oneOfType([import_prop_types146.default.func, import_prop_types146.default.shape({
      current: (props, propName) => {
        if (props[propName] == null) {
          return null;
        }
        if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
          return new Error(`Expected prop '${propName}' to be of type Element`);
        }
        return null;
      }
    })]),
    isFilterActive: import_prop_types146.default.bool,
    item: import_prop_types146.default.shape({
      field: import_prop_types146.default.string.isRequired,
      id: import_prop_types146.default.oneOfType([import_prop_types146.default.number, import_prop_types146.default.string]),
      operator: import_prop_types146.default.string.isRequired,
      value: import_prop_types146.default.any
    }),
    onBlur: import_prop_types146.default.func,
    onFocus: import_prop_types146.default.func,
    slotProps: import_prop_types146.default.object,
    tabIndex: import_prop_types146.default.number
  }),
  item: import_prop_types146.default.shape({
    field: import_prop_types146.default.string.isRequired,
    id: import_prop_types146.default.oneOfType([import_prop_types146.default.number, import_prop_types146.default.string]),
    operator: import_prop_types146.default.string.isRequired,
    value: import_prop_types146.default.any
  }).isRequired,
  pinnedOffset: import_prop_types146.default.number,
  pinnedPosition: import_prop_types146.default.oneOf([0, 1, 2, 3]),
  showClearIcon: import_prop_types146.default.bool,
  showLeftBorder: import_prop_types146.default.bool.isRequired,
  showRightBorder: import_prop_types146.default.bool.isRequired,
  sortIndex: import_prop_types146.default.number,
  style: import_prop_types146.default.object,
  tabIndex: import_prop_types146.default.oneOf([-1, 0]).isRequired,
  width: import_prop_types146.default.number.isRequired
} : void 0;
var Memoized4 = fastMemo(GridHeaderFilterCell);

// node_modules/@mui/x-data-grid-pro/esm/components/headerFiltering/GridHeaderFilterMenu.js
var React267 = __toESM(require_react(), 1);
var import_prop_types147 = __toESM(require_prop_types(), 1);
var import_jsx_runtime193 = __toESM(require_jsx_runtime(), 1);
function GridHeaderFilterMenu({
  open,
  field,
  target,
  applyFilterChanges,
  operators,
  item,
  id,
  labelledBy,
  showClearItem,
  clearFilterItem
}) {
  const apiRef = useGridApiContext();
  const rootProps = useGridRootProps();
  const hideMenu = React267.useCallback(() => {
    apiRef.current.hideHeaderFilterMenu();
  }, [apiRef]);
  if (!target) {
    return null;
  }
  return (0, import_jsx_runtime193.jsx)(GridMenu, {
    position: "bottom-end",
    open,
    target,
    onClose: hideMenu,
    children: (0, import_jsx_runtime193.jsxs)(rootProps.slots.baseMenuList, {
      "aria-labelledby": labelledBy,
      id,
      children: [showClearItem && [(0, import_jsx_runtime193.jsx)(rootProps.slots.baseMenuItem, {
        iconStart: (0, import_jsx_runtime193.jsx)(rootProps.slots.columnMenuClearIcon, {
          fontSize: "small"
        }),
        onClick: () => {
          clearFilterItem();
          hideMenu();
        },
        children: apiRef.current.getLocaleText("headerFilterClear")
      }, "filter-menu-clear-filter"), (0, import_jsx_runtime193.jsx)(rootProps.slots.baseDivider, {}, "filter-menu-divider")], operators.map((op) => {
        const selected = op.value === item.operator;
        const label = (op == null ? void 0 : op.headerLabel) ?? apiRef.current.getLocaleText(`headerFilterOperator${capitalize(op.value)}`);
        return (0, import_jsx_runtime193.jsx)(rootProps.slots.baseMenuItem, {
          iconStart: selected ? (0, import_jsx_runtime193.jsx)(rootProps.slots.menuItemCheckIcon, {
            fontSize: "small"
          }) : (0, import_jsx_runtime193.jsx)("span", {}),
          onClick: () => {
            applyFilterChanges(_extends({}, item, {
              operator: op.value
            }));
            hideMenu();
          },
          autoFocus: selected ? open : false,
          children: label
        }, `${field}-${op.value}`);
      })]
    })
  });
}
true ? GridHeaderFilterMenu.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  applyFilterChanges: import_prop_types147.default.func.isRequired,
  clearFilterItem: import_prop_types147.default.func.isRequired,
  field: import_prop_types147.default.string.isRequired,
  id: import_prop_types147.default.string,
  item: import_prop_types147.default.shape({
    field: import_prop_types147.default.string.isRequired,
    id: import_prop_types147.default.oneOfType([import_prop_types147.default.number, import_prop_types147.default.string]),
    operator: import_prop_types147.default.string.isRequired,
    value: import_prop_types147.default.any
  }).isRequired,
  labelledBy: import_prop_types147.default.string,
  open: import_prop_types147.default.bool.isRequired,
  operators: import_prop_types147.default.arrayOf(import_prop_types147.default.shape({
    getApplyFilterFn: import_prop_types147.default.func.isRequired,
    getValueAsString: import_prop_types147.default.func,
    headerLabel: import_prop_types147.default.string,
    InputComponent: import_prop_types147.default.elementType,
    InputComponentProps: import_prop_types147.default.shape({
      apiRef: import_prop_types147.default.shape({
        current: import_prop_types147.default.object.isRequired
      }),
      applyValue: import_prop_types147.default.func,
      className: import_prop_types147.default.string,
      clearButton: import_prop_types147.default.node,
      disabled: import_prop_types147.default.bool,
      focusElementRef: import_prop_types147.default.oneOfType([import_prop_types147.default.func, import_prop_types147.default.shape({
        current: import_prop_types147.default.any.isRequired
      })]),
      headerFilterMenu: import_prop_types147.default.node,
      inputRef: import_prop_types147.default.oneOfType([import_prop_types147.default.func, import_prop_types147.default.shape({
        current: (props, propName) => {
          if (props[propName] == null) {
            return null;
          }
          if (typeof props[propName] !== "object" || props[propName].nodeType !== 1) {
            return new Error(`Expected prop '${propName}' to be of type Element`);
          }
          return null;
        }
      })]),
      isFilterActive: import_prop_types147.default.bool,
      item: import_prop_types147.default.shape({
        field: import_prop_types147.default.string.isRequired,
        id: import_prop_types147.default.oneOfType([import_prop_types147.default.number, import_prop_types147.default.string]),
        operator: import_prop_types147.default.string.isRequired,
        value: import_prop_types147.default.any
      }),
      onBlur: import_prop_types147.default.func,
      onFocus: import_prop_types147.default.func,
      slotProps: import_prop_types147.default.object,
      tabIndex: import_prop_types147.default.number
    }),
    label: import_prop_types147.default.string,
    requiresFilterValue: import_prop_types147.default.bool,
    value: import_prop_types147.default.string.isRequired
  })).isRequired,
  showClearItem: import_prop_types147.default.bool.isRequired,
  target: HTMLElementType
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/columnReorder/columnReorderSelector.js
var gridColumnReorderSelector = createRootSelector((state) => state.columnReorder);
var gridColumnReorderDragColSelector = createSelector3(gridColumnReorderSelector, (columnReorder) => columnReorder.dragCol);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/gridRowReorderColDef.js
var GRID_REORDER_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "custom",
  field: "__reorder__",
  sortable: false,
  filterable: false,
  width: 50,
  align: "center",
  headerAlign: "center",
  disableColumnMenu: true,
  disableExport: true,
  disableReorder: true,
  resizable: false,
  // @ts-ignore
  aggregable: false,
  chartable: false,
  renderHeader: () => " ",
  renderCell: renderRowReorderCell,
  rowSpanValueGetter: (_, row, __, apiRef) => gridRowIdSelector(apiRef, row)
});

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/gridTreeDataGroupColDef.js
var GRID_TREE_DATA_GROUPING_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "custom",
  sortable: false,
  filterable: false,
  disableColumnMenu: true,
  disableReorder: true,
  align: "left",
  width: 200,
  valueGetter: (value, row, column, apiRef) => {
    const rowId = gridRowIdSelector(apiRef, row);
    const rowNode = gridRowNodeSelector(apiRef, rowId);
    return (rowNode == null ? void 0 : rowNode.type) === "group" || (rowNode == null ? void 0 : rowNode.type) === "leaf" ? rowNode.groupingKey : void 0;
  }
});
var GRID_TREE_DATA_GROUPING_COL_DEF_FORCED_PROPERTIES = {
  field: GRID_TREE_DATA_GROUPING_FIELD,
  editable: false,
  groupable: false
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/detailPanel/gridDetailPanelToggleColDef.js
var import_jsx_runtime194 = __toESM(require_jsx_runtime(), 1);
var GRID_DETAIL_PANEL_TOGGLE_COL_DEF = _extends({}, GRID_STRING_COL_DEF, {
  type: "custom",
  field: GRID_DETAIL_PANEL_TOGGLE_FIELD,
  editable: false,
  sortable: false,
  filterable: false,
  resizable: false,
  // @ts-ignore
  aggregable: false,
  chartable: false,
  disableColumnMenu: true,
  disableReorder: true,
  disableExport: true,
  align: "left",
  width: 40,
  valueGetter: (value, row, column, apiRef) => {
    const rowId = gridRowIdSelector(apiRef, row);
    const expandedRowIds = gridDetailPanelExpandedRowIdsSelector(apiRef);
    return expandedRowIds.has(rowId);
  },
  rowSpanValueGetter: (_, row, __, apiRef) => gridRowIdSelector(apiRef, row),
  renderCell: (params) => (0, import_jsx_runtime194.jsx)(GridDetailPanelToggleCell, _extends({}, params)),
  renderHeader: ({
    colDef
  }) => (0, import_jsx_runtime194.jsx)("div", {
    "aria-label": colDef.headerName,
    role: "presentation"
  })
});

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/utils.js
var getGroupRowIdFromPath = (path) => {
  const pathStr = path.map((groupingCriteria) => `${groupingCriteria.field}/${groupingCriteria.key}`).join("-");
  return `auto-generated-row-${pathStr}`;
};
var getNodePathInTree = ({
  id,
  tree
}) => {
  const path = [];
  let node = tree[id];
  while (node.id !== GRID_ROOT_GROUP_ID) {
    path.push({
      field: node.type === "leaf" ? null : node.groupingField,
      key: node.groupingKey
    });
    node = tree[node.parent];
  }
  path.reverse();
  return path;
};
var checkGroupChildrenExpansion = (node, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault, prevChildrenExpanded) => {
  if (maxDepth !== void 0 && node.depth >= maxDepth) {
    return false;
  }
  if (prevChildrenExpanded !== void 0) {
    return prevChildrenExpanded;
  }
  let childrenExpanded;
  if (node.id === GRID_ROOT_GROUP_ID) {
    childrenExpanded = true;
  } else if (isGroupExpandedByDefault) {
    childrenExpanded = isGroupExpandedByDefault(node);
  } else {
    childrenExpanded = defaultGroupingExpansionDepth === -1 || defaultGroupingExpansionDepth > node.depth;
  }
  return childrenExpanded;
};
var updateGroupDefaultExpansion = (node, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault, prevChildrenExpanded) => {
  const childrenExpanded = checkGroupChildrenExpansion(node, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault, prevChildrenExpanded);
  node.childrenExpanded = childrenExpanded;
  return node;
};
var insertNodeInTree = (node, tree, treeDepths, previousTree) => {
  var _a;
  tree[node.id] = node;
  treeDepths[node.depth] = (treeDepths[node.depth] ?? 0) + 1;
  const parentNode = tree[node.parent];
  if (node.type === "group" || node.type === "leaf") {
    const groupingFieldName = node.groupingField ?? "__no_field__";
    const groupingKeyName = node.groupingKey ?? "__no_key__";
    const groupingField = (_a = parentNode.childrenFromPath) == null ? void 0 : _a[groupingFieldName];
    if (previousTree !== null && previousTree[parentNode.id] === tree[parentNode.id]) {
      parentNode.children = [...parentNode.children, node.id];
    } else {
      parentNode.children.push(node.id);
    }
    if (groupingField == null) {
      parentNode.childrenFromPath[groupingFieldName] = {
        [groupingKeyName.toString()]: node.id
      };
    } else {
      groupingField[groupingKeyName.toString()] = node.id;
    }
  } else if (node.type === "footer") {
    parentNode.footerId = node.id;
  }
};
var removeNodeFromTree = ({
  node,
  tree,
  treeDepths
}) => {
  delete tree[node.id];
  const nodeDepth = node.depth;
  const currentNodeCount = treeDepths[nodeDepth];
  if (currentNodeCount === 1) {
    delete treeDepths[nodeDepth];
  } else {
    treeDepths[nodeDepth] = currentNodeCount - 1;
  }
  const parentNode = tree[node.parent];
  if (node.type === "footer") {
    tree[parentNode.id] = _extends({}, parentNode, {
      footerId: null
    });
  } else {
    const groupingField = node.groupingField ?? "__no_field__";
    const groupingKey = node.groupingKey ?? "__no_key__";
    const children2 = parentNode.children.filter((childId) => childId !== node.id);
    const childrenFromPath = parentNode.childrenFromPath;
    delete childrenFromPath[groupingField][groupingKey.toString()];
    tree[parentNode.id] = _extends({}, parentNode, {
      children: children2,
      childrenFromPath
    });
  }
};
var updateGroupNodeIdAndAutoGenerated = ({
  node,
  updatedNode,
  previousTree,
  tree,
  treeDepths
}) => {
  node.children.forEach((childId) => {
    tree[childId] = _extends({}, tree[childId], {
      parent: updatedNode.id
    });
  });
  removeNodeFromTree({
    node,
    tree,
    treeDepths
  });
  const groupNode = _extends({}, node, updatedNode);
  insertNodeInTree(groupNode, tree, treeDepths, previousTree);
};
var getVisibleRowsLookup2 = ({
  tree,
  filteredRowsLookup
}) => {
  if (!filteredRowsLookup) {
    return {};
  }
  const visibleRowsLookup = {};
  const handleTreeNode = (node, areAncestorsExpanded) => {
    const isPassingFiltering = filteredRowsLookup[node.id] !== false;
    if (node.type === "group") {
      node.children.forEach((childId) => {
        const childNode = tree[childId];
        handleTreeNode(childNode, areAncestorsExpanded && !!node.childrenExpanded);
      });
    }
    const isVisible = isPassingFiltering && areAncestorsExpanded;
    if (!isVisible) {
      visibleRowsLookup[node.id] = isVisible;
    }
    if (node.type === "group" && node.footerId != null) {
      const isFooterVisible = isPassingFiltering && areAncestorsExpanded && !!node.childrenExpanded;
      if (!isFooterVisible) {
        visibleRowsLookup[node.footerId] = isFooterVisible;
      }
    }
  };
  const nodes = Object.values(tree);
  for (let i2 = 0; i2 < nodes.length; i2 += 1) {
    const node = nodes[i2];
    if (node.depth === 0) {
      handleTreeNode(node, true);
    }
  }
  return visibleRowsLookup;
};

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/DataGrid.js
function DataGrid3() {
  if (false) {
    return null;
  }
  throw new Error(["You try to import Data Grid from @mui/x-data-grid-premium but this module isn't exported from this npm package.", "", "Instead, you can do `import { DataGridPremium } from '@mui/x-data-grid-premium'`.", ""].join("\n"));
}
function DataGridPro() {
  if (false) {
    return null;
  }
  throw new Error(["You try to import Data Grid Pro from @mui/x-data-grid-premium but this module isn't exported from this npm package.", "", "Instead, you can do `import { DataGridPremium } from '@mui/x-data-grid-premium'`.", ""].join("\n"));
}

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/DataGridPremium.js
var React371 = __toESM(require_react(), 1);
var import_prop_types163 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-license/esm/encoding/md5.js
var k = [];
var i = 0;
for (; i < 64; ) {
  k[i] = 0 | Math.sin(++i % Math.PI) * 4294967296;
}
function md5(s) {
  const words = [];
  let b, c, d, j = unescape(encodeURI(s)) + "", a = j.length;
  const h = [b = 1732584193, c = 4023233417, ~b, ~c];
  s = --a / 4 + 2 | 15;
  words[--s] = a * 8;
  for (; ~a; ) {
    words[a >> 2] |= j.charCodeAt(a) << 8 * a--;
  }
  for (i = j = 0; i < s; i += 16) {
    a = h;
    for (; j < 64; a = [d = a[3], b + ((d = a[0] + [b & c | ~b & d, d & b | ~d & c, b ^ c ^ d, c ^ (b | ~d)][a = j >> 4] + k[j] + ~~words[i | [j, 5 * j + 1, 3 * j + 5, 7 * j][a] & 15]) << (a = [7, 12, 17, 22, 5, 9, 14, 20, 4, 11, 16, 23, 6, 10, 15, 21][4 * a + j++ % 4]) | d >>> -a), b, c]) {
      b = a[1] | 0;
      c = a[2];
    }
    for (j = 4; j; ) h[--j] += a[j];
  }
  for (s = ""; j < 32; ) {
    s += (h[j >> 3] >> (1 ^ j++) * 4 & 15).toString(16);
  }
  return s;
}

// node_modules/@mui/x-license/esm/encoding/base64.js
var _keyStr = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
var base64Decode = (input) => {
  let output = "";
  let chr1, chr2, chr3;
  let enc1, enc2, enc3, enc4;
  let i2 = 0;
  input = input.replace(/[^A-Za-z0-9\+\/\=]/g, "");
  while (i2 < input.length) {
    enc1 = _keyStr.indexOf(input.charAt(i2++));
    enc2 = _keyStr.indexOf(input.charAt(i2++));
    enc3 = _keyStr.indexOf(input.charAt(i2++));
    enc4 = _keyStr.indexOf(input.charAt(i2++));
    chr1 = enc1 << 2 | enc2 >> 4;
    chr2 = (enc2 & 15) << 4 | enc3 >> 2;
    chr3 = (enc3 & 3) << 6 | enc4;
    output = output + String.fromCharCode(chr1);
    if (enc3 != 64) {
      output = output + String.fromCharCode(chr2);
    }
    if (enc4 != 64) {
      output = output + String.fromCharCode(chr3);
    }
  }
  return output;
};

// node_modules/@mui/x-license/esm/utils/plan.js
var PLAN_SCOPES = ["pro", "premium"];

// node_modules/@mui/x-license/esm/utils/licenseModel.js
var LICENSE_MODELS = [
  /**
   * A license is outdated if the current version of the software was released after the expiry date of the license.
   * But the license can be used indefinitely with an older version of the software.
   */
  "perpetual",
  /**
   * On development, a license is outdated if the expiry date has been reached
   * On production, a license is outdated if the current version of the software was released after the expiry date of the license (see "perpetual")
   */
  "annual",
  /**
   * Legacy. The previous name for 'annual'.
   * Can be removed once old license keys generated with 'subscription' are no longer supported.
   * To support for a while. We need more years of backward support and we sell multi year licenses.
   */
  "subscription"
];

// node_modules/@mui/x-license/esm/utils/licenseErrorMessageUtils.js
var isCodeSandbox = typeof window !== "undefined" && window.location.hostname.endsWith(".csb.app");
function showError(message) {
  const logger = isCodeSandbox ? console.log : console.error;
  logger(["*************************************************************", "", ...message, "", "*************************************************************"].join("\n"));
}
function showInvalidLicenseKeyError() {
  showError(["MUI X: Invalid license key.", "", "Your MUI X license key format isn't valid. It could be because the license key is missing a character or has a typo.", "", "To solve the issue, you need to double check that `setLicenseKey()` is called with the right argument", "Please check the license key installation https://mui.com/r/x-license-key-installation."]);
}
function showLicenseKeyPlanMismatchError({
  packageName
}) {
  const rootPackageName = packageName.replace(/-(premium|pro)$/, "");
  showError(["MUI X: License key plan mismatch.", "", "Your use of MUI X is not compatible with the plan of your license key. The feature you are trying to use is not included in the plan of your license key. This happens if you try to use Data Grid Premium with a license key for the Pro plan.", "", "To solve the issue, you can upgrade your plan from Pro to Premium at https://mui.com/r/x-get-license?scope=premium.", `Or if you didn't intend to use Premium features, you can replace the import of \`${rootPackageName}-premium\` with \`${rootPackageName}-pro\`.`]);
}
function showNotAvailableInInitialProPlanError() {
  showError(["MUI X: Component not included in your license.", "", "The component you are trying to use is not included in the Pro Plan you purchased.", "", "Your license is from an old version of the Pro Plan that is only compatible with the `@mui/x-data-grid-pro` and `@mui/x-date-pickers-pro` commercial packages.", "", "To start using another Pro package, please consider reaching to our sales team to upgrade your license or visit https://mui.com/r/x-get-license to get a new license key."]);
}
function showMissingLicenseKeyError({
  plan,
  packageName
}) {
  showError(["MUI X: Missing license key.", "", `The license key is missing. You might not be allowed to use \`${packageName}\` which is part of MUI X ${plan}.`, "", "To solve the issue, you can check the free trial conditions: https://mui.com/r/x-license-trial.", "If you are eligible no actions are required. If you are not eligible to the free trial, you need to purchase a license https://mui.com/r/x-get-license or stop using the software immediately."]);
}
function showExpiredPackageVersionError({
  packageName
}) {
  showError(["MUI X: Expired package version.", "", `You have installed a version of \`${packageName}\` that is outside of the maintenance plan of your license key. By default, commercial licenses provide access to new versions released during the first year after the purchase.`, "", "To solve the issue, you can renew your license https://mui.com/r/x-get-license or install an older version of the npm package that is compatible with your license key."]);
}
function showExpiredAnnualGraceLicenseKeyError({
  plan,
  licenseKey,
  expiryTimestamp
}) {
  showError(["MUI X: Expired license key.", "", `Your annual license key to use MUI X ${plan} in non-production environments has expired. If you are seeing this development console message, you might be close to breach the license terms by making direct or indirect changes to the frontend of an app that render a MUI X ${plan} component (more details in https://mui.com/r/x-license-annual).`, "", "To solve the problem you can either:", "", "- Renew your license https://mui.com/r/x-get-license and use the new key", `- Stop making changes to code depending directly or indirectly on MUI X ${plan}'s APIs`, "", "Note that your license is perpetual in production environments with any version released before your license term ends.", "", `- License key expiry timestamp: ${new Date(expiryTimestamp)}`, `- Installed license key: ${licenseKey}`, ""]);
}
function showExpiredAnnualLicenseKeyError({
  plan,
  licenseKey,
  expiryTimestamp
}) {
  throw new Error(["MUI X: Expired license key.", "", `Your annual license key to use MUI X ${plan} in non-production environments has expired. If you are seeing this development console message, you might be close to breach the license terms by making direct or indirect changes to the frontend of an app that render a MUI X ${plan} component (more details in https://mui.com/r/x-license-annual).`, "", "To solve the problem you can either:", "", "- Renew your license https://mui.com/r/x-get-license and use the new key", `- Stop making changes to code depending directly or indirectly on MUI X ${plan}'s APIs`, "", "Note that your license is perpetual in production environments with any version released before your license term ends.", "", `- License key expiry timestamp: ${new Date(expiryTimestamp)}`, `- Installed license key: ${licenseKey}`, ""].join("\n"));
}

// node_modules/@mui/x-license/esm/utils/licenseInfo.js
globalThis.__MUI_LICENSE_INFO__ = globalThis.__MUI_LICENSE_INFO__ || {
  key: void 0
};
var LicenseInfo = class _LicenseInfo {
  static getLicenseInfo() {
    return globalThis.__MUI_LICENSE_INFO__;
  }
  static getLicenseKey() {
    return _LicenseInfo.getLicenseInfo().key;
  }
  static setLicenseKey(key) {
    const licenseInfo = _LicenseInfo.getLicenseInfo();
    licenseInfo.key = key;
  }
};

// node_modules/@mui/x-license/esm/utils/licenseStatus.js
var LICENSE_STATUS = (function(LICENSE_STATUS2) {
  LICENSE_STATUS2["NotFound"] = "NotFound";
  LICENSE_STATUS2["Invalid"] = "Invalid";
  LICENSE_STATUS2["ExpiredAnnual"] = "ExpiredAnnual";
  LICENSE_STATUS2["ExpiredAnnualGrace"] = "ExpiredAnnualGrace";
  LICENSE_STATUS2["ExpiredVersion"] = "ExpiredVersion";
  LICENSE_STATUS2["Valid"] = "Valid";
  LICENSE_STATUS2["OutOfScope"] = "OutOfScope";
  LICENSE_STATUS2["NotAvailableInInitialProPlan"] = "NotAvailableInInitialProPlan";
  return LICENSE_STATUS2;
})({});

// node_modules/@mui/x-license/esm/verifyLicense/verifyLicense.js
function isPlanScopeSufficient(packageName, planScope) {
  let acceptedScopes;
  if (packageName.includes("-pro")) {
    acceptedScopes = ["pro", "premium"];
  } else if (packageName.includes("-premium")) {
    acceptedScopes = ["premium"];
  } else {
    acceptedScopes = [];
  }
  return acceptedScopes.includes(planScope);
}
var expiryReg = /^.*EXPIRY=([0-9]+),.*$/;
var orderReg = /^.*ORDER:([0-9]+),.*$/;
var PRO_PACKAGES_AVAILABLE_IN_INITIAL_PRO_PLAN = ["x-data-grid-pro", "x-date-pickers-pro"];
function decodeLicenseVersion1(license) {
  let expiryTimestamp;
  let orderId;
  try {
    expiryTimestamp = parseInt(license.match(expiryReg)[1], 10);
    if (!expiryTimestamp || Number.isNaN(expiryTimestamp)) {
      expiryTimestamp = null;
    }
    orderId = parseInt(license.match(orderReg)[1], 10);
    if (!orderId || Number.isNaN(orderId)) {
      orderId = null;
    }
  } catch (err) {
    expiryTimestamp = null;
    orderId = null;
  }
  return {
    version: 1,
    licenseModel: "perpetual",
    planScope: "pro",
    planVersion: "initial",
    expiryTimestamp,
    expiryDate: expiryTimestamp ? new Date(expiryTimestamp) : null,
    orderId
  };
}
function decodeLicenseVersion2(license) {
  const licenseInfo = {
    version: 2,
    licenseModel: null,
    planScope: null,
    planVersion: "initial",
    expiryTimestamp: null,
    expiryDate: null,
    orderId: null
  };
  license.split(",").map((token) => token.split("=")).filter((el) => el.length === 2).forEach(([key, value]) => {
    if (key === "S") {
      licenseInfo.planScope = value;
    }
    if (key === "LM") {
      licenseInfo.licenseModel = value;
    }
    if (key === "E") {
      const expiryTimestamp = parseInt(value, 10);
      if (expiryTimestamp && !Number.isNaN(expiryTimestamp)) {
        licenseInfo.expiryTimestamp = expiryTimestamp;
        licenseInfo.expiryDate = new Date(expiryTimestamp);
      }
    }
    if (key === "PV") {
      licenseInfo.planVersion = value;
    }
    if (key === "O") {
      const orderNum = parseInt(value, 10);
      if (orderNum && !Number.isNaN(orderNum)) {
        licenseInfo.orderId = orderNum;
      }
    }
  });
  return licenseInfo;
}
function decodeLicense(encodedLicense) {
  const license = base64Decode(encodedLicense);
  if (license.includes("KEYVERSION=1")) {
    return decodeLicenseVersion1(license);
  }
  if (license.includes("KV=2")) {
    return decodeLicenseVersion2(license);
  }
  return null;
}
function verifyLicense({
  releaseInfo: releaseInfo3,
  licenseKey,
  packageName
}) {
  if (false) {
    return {
      status: LICENSE_STATUS.Valid
    };
  }
  if (!releaseInfo3) {
    throw new Error("MUI X: The release information is missing. Not able to validate license.");
  }
  if (!licenseKey) {
    return {
      status: LICENSE_STATUS.NotFound
    };
  }
  const hash4 = licenseKey.substr(0, 32);
  const encoded = licenseKey.substr(32);
  if (hash4 !== md5(encoded)) {
    return {
      status: LICENSE_STATUS.Invalid
    };
  }
  const license = decodeLicense(encoded);
  if (license == null) {
    console.error("MUI X: Error checking license. Key version not found!");
    return {
      status: LICENSE_STATUS.Invalid
    };
  }
  if (license.licenseModel == null || !LICENSE_MODELS.includes(license.licenseModel)) {
    console.error("MUI X: Error checking license. License model not found or invalid!");
    return {
      status: LICENSE_STATUS.Invalid
    };
  }
  if (license.expiryTimestamp == null) {
    console.error("MUI X: Error checking license. Expiry timestamp not found or invalid!");
    return {
      status: LICENSE_STATUS.Invalid
    };
  }
  if (license.licenseModel === "perpetual" || false) {
    const pkgTimestamp = parseInt(base64Decode(releaseInfo3), 10);
    if (Number.isNaN(pkgTimestamp)) {
      throw new Error("MUI X: The release information is invalid. Not able to validate license.");
    }
    if (license.expiryTimestamp < pkgTimestamp) {
      return {
        status: LICENSE_STATUS.ExpiredVersion
      };
    }
  } else if (license.licenseModel === "subscription" || license.licenseModel === "annual") {
    if ((/* @__PURE__ */ new Date()).getTime() > license.expiryTimestamp) {
      if (
        // 30 days grace
        (/* @__PURE__ */ new Date()).getTime() < license.expiryTimestamp + 1e3 * 3600 * 24 * 30 || false
      ) {
        return {
          status: LICENSE_STATUS.ExpiredAnnualGrace,
          meta: {
            expiryTimestamp: license.expiryTimestamp,
            licenseKey
          }
        };
      }
      return {
        status: LICENSE_STATUS.ExpiredAnnual,
        meta: {
          expiryTimestamp: license.expiryTimestamp,
          licenseKey
        }
      };
    }
  }
  if (license.planScope == null || !PLAN_SCOPES.includes(license.planScope)) {
    console.error("MUI X: Error checking license. planScope not found or invalid!");
    return {
      status: LICENSE_STATUS.Invalid
    };
  }
  if (!isPlanScopeSufficient(packageName, license.planScope)) {
    return {
      status: LICENSE_STATUS.OutOfScope
    };
  }
  if (license.planVersion === "initial" && license.planScope === "pro" && !PRO_PACKAGES_AVAILABLE_IN_INITIAL_PRO_PLAN.includes(packageName)) {
    return {
      status: LICENSE_STATUS.NotAvailableInInitialProPlan
    };
  }
  return {
    status: LICENSE_STATUS.Valid
  };
}

// node_modules/@mui/x-license/esm/useLicenseVerifier/useLicenseVerifier.js
var React269 = __toESM(require_react(), 1);

// node_modules/@mui/x-telemetry/esm/runtime/events.js
var muiXTelemetryEvents = {
  licenseVerification: false ? noop : (context, payload) => ({
    eventName: "licenseVerification",
    payload,
    context
  })
};
var events_default = muiXTelemetryEvents;

// node_modules/@mui/x-telemetry/esm/runtime/config.js
var envEnabledValues = ["1", "true", "yes", "y"];
var envDisabledValues = ["0", "false", "no", "n"];
function getBooleanEnv(value) {
  if (!value) {
    return void 0;
  }
  if (envEnabledValues.includes(value)) {
    return true;
  }
  if (envDisabledValues.includes(value)) {
    return false;
  }
  return void 0;
}
function getBooleanEnvFromEnvObject(envKey, envObj) {
  var _a;
  const keys2 = Object.keys(envObj);
  for (let i2 = 0; i2 < keys2.length; i2 += 1) {
    const key = keys2[i2];
    if (!key.endsWith(envKey)) {
      continue;
    }
    const value = getBooleanEnv((_a = envObj[key]) == null ? void 0 : _a.toLowerCase());
    if (typeof value === "boolean") {
      return value;
    }
  }
  return void 0;
}
function getIsTelemetryCollecting() {
  const globalValue = globalThis.__MUI_X_TELEMETRY_DISABLED__;
  if (typeof globalValue === "boolean") {
    return !globalValue;
  }
  try {
    if (typeof process !== "undefined" && process.env && typeof process.env === "object") {
      const result = getBooleanEnvFromEnvObject("MUI_X_TELEMETRY_DISABLED", process.env);
      if (typeof result === "boolean") {
        return !result;
      }
    }
  } catch (_) {
  }
  try {
    const envValue = process.env.MUI_X_TELEMETRY_DISABLED || process.env.NEXT_PUBLIC_MUI_X_TELEMETRY_DISABLED || process.env.GATSBY_MUI_X_TELEMETRY_DISABLED || process.env.REACT_APP_MUI_X_TELEMETRY_DISABLED || process.env.PUBLIC_MUI_X_TELEMETRY_DISABLED;
    const result = getBooleanEnv(envValue);
    if (typeof result === "boolean") {
      return !result;
    }
  } catch (_) {
  }
  return void 0;
}
function getIsDebugModeEnabled() {
  try {
    const globalValue = globalThis.__MUI_X_TELEMETRY_DEBUG__;
    if (typeof globalValue === "boolean") {
      return globalValue;
    }
    if (typeof process !== "undefined" && process.env && typeof process.env === "object") {
      const result = getBooleanEnvFromEnvObject("MUI_X_TELEMETRY_DEBUG", process.env);
      if (typeof result === "boolean") {
        return result;
      }
    }
    if (process.env.MUI_X_TELEMETRY_DEBUG) {
      const result = getBooleanEnv(process.env.MUI_X_TELEMETRY_DEBUG);
      if (typeof result === "boolean") {
        return result;
      }
    }
  } catch (_) {
  }
  try {
    const envValue = process.env.MUI_X_TELEMETRY_DEBUG || process.env.NEXT_PUBLIC_MUI_X_TELEMETRY_DEBUG || process.env.GATSBY_MUI_X_TELEMETRY_DEBUG || process.env.REACT_APP_MUI_X_TELEMETRY_DEBUG || process.env.PUBLIC_MUI_X_TELEMETRY_DEBUG;
    const result = getBooleanEnv(envValue);
    if (typeof result === "boolean") {
      return result;
    }
  } catch (_) {
  }
  return false;
}
function getNodeEnv() {
  try {
    return "development";
  } catch (_) {
    return "<unknown>";
  }
}
var cachedEnv = null;
function getTelemetryEnvConfig(skipCache = false) {
  if (skipCache || !cachedEnv) {
    cachedEnv = {
      NODE_ENV: getNodeEnv(),
      IS_COLLECTING: getIsTelemetryCollecting(),
      DEBUG: getIsDebugModeEnabled()
    };
  }
  return cachedEnv;
}
function getTelemetryEnvConfigValue(key) {
  return getTelemetryEnvConfig()[key];
}

// node_modules/@mui/x-telemetry/esm/runtime/fetcher.js
async function fetchWithRetry(url, options3, retries = 3) {
  try {
    const response = await fetch(url, options3);
    if (response.ok) {
      return response;
    }
    throw new Error(`Request failed with status ${response.status}`);
  } catch (error) {
    if (retries === 0) {
      throw error;
    }
    return new Promise((resolve) => {
      setTimeout(() => {
        resolve(fetchWithRetry(url, options3, retries - 1));
      }, Math.random() * 3e3);
    });
  }
}

// node_modules/@mui/x-telemetry/esm/runtime/sender.js
var sendMuiXTelemetryRetries = 3;
function shouldSendTelemetry(telemetryContext) {
  if (typeof window === "undefined") {
    return false;
  }
  const envIsCollecting = getTelemetryEnvConfigValue("IS_COLLECTING");
  if (typeof envIsCollecting === "boolean") {
    return envIsCollecting;
  }
  if (telemetryContext.traits.isCI) {
    return false;
  }
  return false;
}
async function sendMuiXTelemetryEvent(event) {
  try {
    if (false) {
      return;
    }
    const {
      default: getTelemetryContext
    } = await import("./get-context-5TBX6PM3.js");
    const telemetryContext = await getTelemetryContext();
    if (!event || !shouldSendTelemetry(telemetryContext)) {
      return;
    }
    const eventPayload = _extends({}, event, {
      context: _extends({}, telemetryContext.traits, event.context)
    });
    if (getTelemetryEnvConfigValue("DEBUG")) {
      console.log("[mui-x-telemetry] event", JSON.stringify(eventPayload, null, 2));
      return;
    }
    await fetchWithRetry("https://x-telemetry.mui.com/v2/telemetry/record", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "X-Telemetry-Client-Version": "8.20.0",
        "X-Telemetry-Node-Env": "development"
      },
      body: JSON.stringify([eventPayload])
    }, sendMuiXTelemetryRetries);
  } catch (_) {
    console.log("[mui-x-telemetry] error", _);
  }
}
var sender_default = sendMuiXTelemetryEvent;

// node_modules/@mui/x-telemetry/esm/index.js
var sendMuiXTelemetryEvent2 = false ? noop : sender_default;

// node_modules/@mui/x-license/esm/Unstable_LicenseInfoProvider/MuiLicenseInfoContext.js
var React268 = __toESM(require_react(), 1);
var MuiLicenseInfoContext = React268.createContext({
  key: void 0
});
if (true) MuiLicenseInfoContext.displayName = "MuiLicenseInfoContext";
var MuiLicenseInfoContext_default = MuiLicenseInfoContext;

// node_modules/@mui/x-license/esm/useLicenseVerifier/useLicenseVerifier.js
var sharedLicenseStatuses = {};
function useLicenseVerifier(packageName, releaseInfo3) {
  const {
    key: contextKey
  } = React269.useContext(MuiLicenseInfoContext_default);
  return React269.useMemo(() => {
    const licenseKey = contextKey ?? LicenseInfo.getLicenseKey();
    if (sharedLicenseStatuses[packageName] && sharedLicenseStatuses[packageName].key === licenseKey) {
      return sharedLicenseStatuses[packageName].licenseVerifier;
    }
    const plan = packageName.includes("premium") ? "Premium" : "Pro";
    const licenseStatus = verifyLicense({
      releaseInfo: releaseInfo3,
      licenseKey,
      packageName
    });
    const fullPackageName = `@mui/${packageName}`;
    sendMuiXTelemetryEvent2(events_default.licenseVerification({
      licenseKey
    }, {
      packageName,
      packageReleaseInfo: releaseInfo3,
      licenseStatus: licenseStatus == null ? void 0 : licenseStatus.status
    }));
    if (licenseStatus.status === LICENSE_STATUS.Valid) {
    } else if (licenseStatus.status === LICENSE_STATUS.Invalid) {
      showInvalidLicenseKeyError();
    } else if (licenseStatus.status === LICENSE_STATUS.NotAvailableInInitialProPlan) {
      showNotAvailableInInitialProPlanError();
    } else if (licenseStatus.status === LICENSE_STATUS.OutOfScope) {
      showLicenseKeyPlanMismatchError({
        packageName: fullPackageName
      });
    } else if (licenseStatus.status === LICENSE_STATUS.NotFound) {
      showMissingLicenseKeyError({
        plan,
        packageName: fullPackageName
      });
    } else if (licenseStatus.status === LICENSE_STATUS.ExpiredAnnualGrace) {
      showExpiredAnnualGraceLicenseKeyError(_extends({
        plan
      }, licenseStatus.meta));
    } else if (licenseStatus.status === LICENSE_STATUS.ExpiredAnnual) {
      showExpiredAnnualLicenseKeyError(_extends({
        plan
      }, licenseStatus.meta));
    } else if (licenseStatus.status === LICENSE_STATUS.ExpiredVersion) {
      showExpiredPackageVersionError({
        packageName: fullPackageName
      });
    } else if (true) {
      throw new Error("missing status handler");
    }
    sharedLicenseStatuses[packageName] = {
      key: licenseKey,
      licenseVerifier: licenseStatus
    };
    return licenseStatus;
  }, [packageName, releaseInfo3, contextKey]);
}

// node_modules/@mui/x-license/esm/Watermark/Watermark.js
var import_jsx_runtime195 = __toESM(require_jsx_runtime(), 1);
function getLicenseErrorMessage(licenseStatus) {
  switch (licenseStatus) {
    case LICENSE_STATUS.ExpiredAnnualGrace:
    case LICENSE_STATUS.ExpiredAnnual:
      return "MUI X Expired license key";
    case LICENSE_STATUS.ExpiredVersion:
      return "MUI X Expired package version";
    case LICENSE_STATUS.Invalid:
      return "MUI X Invalid license key";
    case LICENSE_STATUS.OutOfScope:
      return "MUI X License key plan mismatch";
    case LICENSE_STATUS.NotAvailableInInitialProPlan:
      return "MUI X Product not covered by plan";
    case LICENSE_STATUS.NotFound:
      return "MUI X Missing license key";
    default:
      throw new Error("Unhandled MUI X license status.");
  }
}
function Watermark(props) {
  const {
    packageName,
    releaseInfo: releaseInfo3
  } = props;
  const licenseStatus = useLicenseVerifier(packageName, releaseInfo3);
  if (licenseStatus.status === LICENSE_STATUS.Valid) {
    return null;
  }
  return (0, import_jsx_runtime195.jsx)("div", {
    style: {
      position: "absolute",
      pointerEvents: "none",
      color: "#8282829e",
      zIndex: 1e5,
      width: "100%",
      textAlign: "center",
      bottom: "50%",
      right: 0,
      letterSpacing: 5,
      fontSize: 24
    },
    children: getLicenseErrorMessage(licenseStatus.status)
  });
}
var MemoizedWatermark = fastMemo(Watermark);

// node_modules/@mui/x-license/esm/Unstable_LicenseInfoProvider/LicenseInfoProvider.js
var React270 = __toESM(require_react(), 1);
var import_jsx_runtime196 = __toESM(require_jsx_runtime(), 1);

// node_modules/@mui/x-data-grid-pro/esm/DataGridPro/DataGridPro.js
var React300 = __toESM(require_react(), 1);
var import_prop_types149 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rows/useGridRowsOverridableMethods.js
var React271 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/reorderExecutor.js
var BaseReorderOperation = class {
};
var RowReorderExecutor = class {
  constructor(operations) {
    this.operations = operations;
  }
  async execute(ctx) {
    for (const operation of this.operations) {
      const detectedOperation = operation.detectOperation(ctx);
      if (detectedOperation) {
        await operation.executeOperation(detectedOperation, ctx);
        return;
      }
    }
    warnOnce(["MUI X: The parameters provided to the API method resulted in a no-op.", "Consider looking at the documentation at https://mui.com/x/react-data-grid/row-ordering/"], "warning");
  }
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/utils.js
var utils_exports = {};
__export(utils_exports, {
  BatchRowUpdater: () => BatchRowUpdater,
  calculateTargetIndex: () => calculateTargetIndex,
  collectAllDescendants: () => collectAllDescendants,
  collectAllLeafDescendants: () => collectAllLeafDescendants,
  determineOperationType: () => determineOperationType,
  findCellElement: () => findCellElement,
  findExistingGroupWithSameKey: () => findExistingGroupWithSameKey,
  getNodePathInTree: () => getNodePathInTree,
  handleProcessRowUpdateError: () => handleProcessRowUpdateError,
  isDescendantOf: () => isDescendantOf,
  removeEmptyAncestors: () => removeEmptyAncestors,
  updateDescendantDepths: () => updateDescendantDepths
});
function findCellElement(target) {
  const element = target;
  if (!element) {
    return element;
  }
  if (element instanceof Element && element.classList.contains(gridClasses.cell)) {
    return element;
  }
  const cellElement = element.closest(`[class*="${gridClasses.cell}"]`);
  return cellElement || element;
}
function determineOperationType(sourceNode, targetNode) {
  if (sourceNode.parent === targetNode.parent) {
    return "same-parent-swap";
  }
  if (sourceNode.type === "leaf") {
    return "cross-parent-leaf";
  }
  return "cross-parent-group";
}
function calculateTargetIndex(sourceNode, targetNode, isLastChild, rowTree) {
  if (sourceNode.parent === targetNode.parent && !isLastChild) {
    const parent = rowTree[sourceNode.parent];
    return parent.children.findIndex((id) => id === targetNode.id);
  }
  if (isLastChild) {
    const targetParent2 = rowTree[targetNode.parent];
    return targetParent2.children.length;
  }
  const targetParent = rowTree[targetNode.parent];
  const targetIndex = targetParent.children.findIndex((id) => id === targetNode.id);
  return targetIndex >= 0 ? targetIndex : 0;
}
var collectAllLeafDescendants = (groupNode, tree) => {
  const leafIds = [];
  const collectFromNode = (nodeId) => {
    const node = tree[nodeId];
    if (node.type === "leaf") {
      leafIds.push(nodeId);
    } else if (node.type === "group") {
      node.children.forEach(collectFromNode);
    }
  };
  groupNode.children.forEach(collectFromNode);
  return leafIds;
};
var collectAllDescendants = (groupNode, tree) => {
  const descendants = [];
  const collectFromNode = (nodeId) => {
    const node = tree[nodeId];
    if (node) {
      descendants.push(node);
      if (node.type === "group") {
        node.children.forEach(collectFromNode);
      }
    }
  };
  groupNode.children.forEach(collectFromNode);
  return descendants;
};
var isDescendantOf = (possibleDescendant, ancestor, tree) => {
  let current = possibleDescendant;
  while (current && current.id !== GRID_ROOT_GROUP_ID) {
    if (current.id === ancestor.id) {
      return true;
    }
    current = tree[current.parent];
  }
  return false;
};
var updateDescendantDepths = (group, tree, depthDiff) => {
  const updateNodeDepth = (nodeId) => {
    const node = tree[nodeId];
    if (node) {
      tree[nodeId] = _extends({}, node, {
        depth: node.depth + depthDiff
      });
      if (node.type === "group") {
        node.children.forEach(updateNodeDepth);
      }
    }
  };
  group.children.forEach(updateNodeDepth);
};
function findExistingGroupWithSameKey(parentNode, groupingKey, groupingField, tree) {
  for (const childId of parentNode.children) {
    const childNode = tree[childId];
    if (childNode && childNode.type === "group" && childNode.groupingKey === groupingKey && childNode.groupingField === groupingField) {
      return childNode;
    }
  }
  return null;
}
function removeEmptyAncestors(groupId, tree, removedGroups) {
  let rootLevelRemovals = 0;
  let currentGroupId = groupId;
  while (currentGroupId && currentGroupId !== GRID_ROOT_GROUP_ID) {
    const group = tree[currentGroupId];
    if (!group) {
      break;
    }
    const remainingChildren = group.children.filter((childId) => !removedGroups.has(childId));
    if (remainingChildren.length > 0) {
      break;
    }
    if (group.depth === 0) {
      rootLevelRemovals += 1;
    }
    removedGroups.add(currentGroupId);
    currentGroupId = group.parent;
  }
  return rootLevelRemovals;
}
function handleProcessRowUpdateError(error, onProcessRowUpdateError) {
  if (onProcessRowUpdateError) {
    onProcessRowUpdateError(error);
  } else {
    warnOnce(["MUI X: A call to `processRowUpdate()` threw an error which was not handled because `onProcessRowUpdateError()` is missing.", "To handle the error pass a callback to the `onProcessRowUpdateError()` prop, for example `<DataGrid onProcessRowUpdateError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/editing/persistence/."], "error");
  }
}
var BatchRowUpdater = class {
  constructor(apiRef, processRowUpdate, onProcessRowUpdateError) {
    __publicField(this, "rowsToUpdate", /* @__PURE__ */ (() => /* @__PURE__ */ new Map())());
    __publicField(this, "originalRows", /* @__PURE__ */ (() => /* @__PURE__ */ new Map())());
    __publicField(this, "successfulRowIds", /* @__PURE__ */ (() => /* @__PURE__ */ new Set())());
    __publicField(this, "failedRowIds", /* @__PURE__ */ (() => /* @__PURE__ */ new Set())());
    __publicField(this, "pendingRowUpdates", []);
    this.apiRef = apiRef;
    this.processRowUpdate = processRowUpdate;
    this.onProcessRowUpdateError = onProcessRowUpdateError;
  }
  queueUpdate(rowId, originalRow, updatedRow) {
    this.originalRows.set(rowId, originalRow);
    this.rowsToUpdate.set(rowId, updatedRow);
  }
  async executeAll() {
    const rowIds = Array.from(this.rowsToUpdate.keys());
    if (rowIds.length === 0) {
      return {
        successful: [],
        failed: [],
        updates: []
      };
    }
    const handleRowUpdate = async (rowId) => {
      const newRow = this.rowsToUpdate.get(rowId);
      const oldRow = this.originalRows.get(rowId);
      try {
        if (typeof this.processRowUpdate === "function") {
          const params = {
            rowId,
            previousRow: oldRow,
            updatedRow: newRow
          };
          const finalRow = await this.processRowUpdate(newRow, oldRow, params);
          this.pendingRowUpdates.push(finalRow || newRow);
          this.successfulRowIds.add(rowId);
        } else {
          this.pendingRowUpdates.push(newRow);
          this.successfulRowIds.add(rowId);
        }
      } catch (error) {
        this.failedRowIds.add(rowId);
        handleProcessRowUpdateError(error, this.onProcessRowUpdateError);
      }
    };
    const promises = rowIds.map((rowId) => {
      return new Promise((resolve) => {
        handleRowUpdate(rowId).then(resolve).catch(resolve);
      });
    });
    this.apiRef.current.setLoading(true);
    await Promise.all(promises);
    this.apiRef.current.setLoading(false);
    return {
      successful: Array.from(this.successfulRowIds),
      failed: Array.from(this.failedRowIds),
      updates: this.pendingRowUpdates
    };
  }
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/utils.js
var buildTreeDataPath = (node, tree) => {
  const path = [];
  let current = node;
  while (current && current.id !== GRID_ROOT_GROUP_ID) {
    if ((current.type === "leaf" || current.type === "group") && current.groupingKey !== null) {
      path.unshift(String(current.groupingKey));
    }
    current = tree[current.parent];
  }
  return path;
};
function displaySetTreeDataPathWarning(operationName) {
  warnOnce(`MUI X: ${operationName} requires \`setTreeDataPath()\` prop to update row data paths. Please provide a \`setTreeDataPath()\` function to enable this feature.`, "warning");
}
function removeNodeFromSourceParent(updatedTree, sourceNode) {
  const sourceParent = updatedTree[sourceNode.parent];
  const sourceChildren = sourceParent.children.filter((id) => id !== sourceNode.id);
  if (sourceChildren.length === 0) {
    updatedTree[sourceNode.parent] = _extends({}, sourceParent, {
      type: "leaf",
      children: void 0
    });
  } else {
    updatedTree[sourceNode.parent] = _extends({}, sourceParent, {
      children: sourceChildren
    });
  }
}
async function updateLeafPath(sourceNode, targetPath, ctx) {
  const {
    apiRef,
    setTreeDataPath,
    processRowUpdate,
    onProcessRowUpdateError
  } = ctx;
  const dataRowIdToModelLookup = gridRowsLookupSelector(apiRef);
  const leafKey = sourceNode.type === "leaf" ? sourceNode.groupingKey : null;
  const newPath = leafKey !== null ? [...targetPath, String(leafKey)] : targetPath;
  const originalRow = dataRowIdToModelLookup[sourceNode.id];
  const updatedRow = setTreeDataPath(newPath, originalRow);
  const updater = new BatchRowUpdater(apiRef, processRowUpdate, onProcessRowUpdateError);
  updater.queueUpdate(sourceNode.id, originalRow, updatedRow);
  const {
    successful,
    updates
  } = await updater.executeAll();
  if (successful.length === 0) {
    return null;
  }
  return updates[0];
}
async function updateGroupHierarchyPaths(sourceNode, sourceBasePath, targetPath, ctx) {
  const {
    apiRef,
    setTreeDataPath,
    processRowUpdate,
    onProcessRowUpdateError
  } = ctx;
  const rowTree = gridRowTreeSelector(apiRef);
  const dataRowIdToModelLookup = gridRowsLookupSelector(apiRef);
  const nodesToUpdate = collectAllDescendants(sourceNode, rowTree);
  nodesToUpdate.unshift(sourceNode);
  const sourceDepth = sourceBasePath.length;
  const updater = new BatchRowUpdater(apiRef, processRowUpdate, onProcessRowUpdateError);
  for (const node of nodesToUpdate) {
    const originalRow = dataRowIdToModelLookup[node.id];
    const currentPath = buildTreeDataPath(node, rowTree);
    const relativePath = currentPath.slice(sourceDepth);
    const newPath = [...targetPath, ...relativePath];
    const updatedRow = setTreeDataPath(newPath, originalRow);
    updater.queueUpdate(node.id, originalRow, updatedRow);
  }
  const {
    successful,
    updates
  } = await updater.executeAll();
  if (successful.length === 0) {
    return [];
  }
  return updates;
}
function updateNodeParentAndDepth(updatedTree, node, newParentId, newDepth) {
  updatedTree[node.id] = _extends({}, node, {
    parent: newParentId,
    depth: newDepth
  });
  if (node.type === "group") {
    const depthDiff = newDepth - node.depth;
    updateDescendantDepths(node, updatedTree, depthDiff);
  }
}

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/treeDataReorderExecutor.js
var SameParentSwapOperation = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "same-parent-swap");
  }
  detectOperation(ctx) {
    if (ctx.dropPosition === "inside") {
      return null;
    }
    const {
      sourceRowId,
      placeholderIndex,
      sortedFilteredRowIds,
      sortedFilteredRowIndexLookup,
      rowTree,
      apiRef
    } = ctx;
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type === "footer") {
      return null;
    }
    let targetIndex = placeholderIndex;
    const sourceIndex = sortedFilteredRowIndexLookup[sourceRowId];
    if (targetIndex === sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
      targetIndex -= 1;
    }
    let targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
    if (placeholderIndex > sourceIndex && sourceNode.parent === targetNode.parent) {
      targetIndex = placeholderIndex - 1;
      targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
      if (targetNode && targetNode.depth !== sourceNode.depth) {
        while (targetNode.depth > sourceNode.depth && targetIndex >= 0) {
          targetIndex -= 1;
          targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
        }
      }
      if (targetIndex === -1) {
        return null;
      }
    }
    let isLastChild = false;
    if (!targetNode) {
      if (placeholderIndex >= sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
        targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[sortedFilteredRowIds.length - 1]);
        isLastChild = true;
      } else {
        return null;
      }
    }
    let adjustedTargetNode = targetNode;
    if (targetNode.type === "group" && sourceNode.parent !== targetNode.parent && sourceNode.depth > targetNode.depth) {
      let i2 = targetIndex - 1;
      while (i2 >= 0) {
        const node = gridRowNodeSelector(apiRef, sortedFilteredRowIds[i2]);
        if (node && node.depth < sourceNode.depth) {
          return null;
        }
        if (node && node.depth === sourceNode.depth) {
          targetIndex = i2;
          adjustedTargetNode = node;
          break;
        }
        i2 -= 1;
      }
    }
    const isBelowPosition = ctx.dropPosition === "below";
    if (isBelowPosition && sourceNode.parent !== adjustedTargetNode.parent) {
      const unAdjustedTargetIndex = placeholderIndex - 1;
      const unAdjustedTargetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[unAdjustedTargetIndex]);
      if (unAdjustedTargetNode && unAdjustedTargetNode.parent === sourceNode.parent) {
        adjustedTargetNode = unAdjustedTargetNode;
        isLastChild = true;
      }
    }
    if (sourceNode.parent !== adjustedTargetNode.parent) {
      return null;
    }
    const actualTargetIndex = calculateTargetIndex(sourceNode, adjustedTargetNode, isLastChild, rowTree);
    targetNode = adjustedTargetNode;
    return {
      sourceNode,
      targetNode,
      actualTargetIndex,
      isLastChild,
      operationType: this.operationType
    };
  }
  executeOperation(operation, ctx) {
    const {
      sourceNode,
      actualTargetIndex
    } = operation;
    const {
      apiRef,
      sourceRowId
    } = ctx;
    apiRef.current.setState((state) => {
      const group = gridRowTreeSelector(apiRef)[sourceNode.parent];
      const currentChildren = [...group.children];
      const oldIndex = currentChildren.findIndex((row) => row === sourceRowId);
      if (oldIndex === -1 || actualTargetIndex === -1 || oldIndex === actualTargetIndex) {
        return state;
      }
      currentChildren.splice(actualTargetIndex, 0, currentChildren.splice(oldIndex, 1)[0]);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: _extends({}, state.rows.tree, {
            [sourceNode.parent]: _extends({}, group, {
              children: currentChildren
            })
          })
        })
      });
    });
    apiRef.current.publishEvent("rowsSet");
  }
};
var CrossParentLeafOperation = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "cross-parent-leaf");
  }
  detectOperation(ctx) {
    if (ctx.dropPosition === "inside") {
      return null;
    }
    const {
      sourceRowId,
      placeholderIndex,
      sortedFilteredRowIds,
      rowTree,
      apiRef,
      setTreeDataPath
    } = ctx;
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type !== "leaf") {
      return null;
    }
    if (!setTreeDataPath) {
      displaySetTreeDataPathWarning("Cross-parent reordering");
    }
    let targetIndex = placeholderIndex;
    if (targetIndex === sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
      targetIndex = sortedFilteredRowIds.length - 1;
    }
    if (targetIndex < 0) {
      return null;
    }
    const targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
    if (!targetNode) {
      return null;
    }
    if (sourceNode.parent === targetNode.parent) {
      return null;
    }
    const actualTargetIndex = calculateTargetIndex(sourceNode, targetNode, placeholderIndex >= sortedFilteredRowIds.length, rowTree);
    return {
      sourceNode,
      targetNode,
      actualTargetIndex,
      isLastChild: placeholderIndex >= sortedFilteredRowIds.length,
      operationType: this.operationType
    };
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode,
      actualTargetIndex
    } = operation;
    const {
      apiRef
    } = ctx;
    const rowTree = gridRowTreeSelector(apiRef);
    const targetParentNode = rowTree[targetNode.parent];
    const targetPath = buildTreeDataPath(targetParentNode, rowTree);
    const updatedRow = await updateLeafPath(sourceNode, targetPath, ctx);
    if (!updatedRow) {
      return;
    }
    apiRef.current.setState((state) => {
      const updatedTree = _extends({}, state.rows.tree);
      removeNodeFromSourceParent(updatedTree, sourceNode);
      const targetParent = updatedTree[targetNode.parent];
      const targetChildren = [...targetParent.children];
      targetChildren.splice(actualTargetIndex, 0, sourceNode.id);
      updatedTree[targetNode.parent] = _extends({}, targetParent, {
        children: targetChildren
      });
      const parentNode = updatedTree[targetNode.parent];
      updateNodeParentAndDepth(updatedTree, sourceNode, targetNode.parent, parentNode.depth + 1);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: updatedTree
        })
      });
    });
    apiRef.current.updateRows([updatedRow]);
    apiRef.current.publishEvent("rowsSet");
  }
};
var DropOnLeafOperation = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "drop-on-leaf");
  }
  detectOperation(ctx) {
    const {
      sourceRowId,
      dropPosition,
      placeholderIndex,
      sortedFilteredRowIds,
      apiRef,
      setTreeDataPath
    } = ctx;
    if (dropPosition !== "inside") {
      return null;
    }
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type === "footer") {
      return null;
    }
    if (!setTreeDataPath) {
      displaySetTreeDataPathWarning("Drop on leaf reordering");
    }
    let targetIndex = placeholderIndex;
    if (targetIndex === sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
      targetIndex = sortedFilteredRowIds.length - 1;
    }
    if (targetIndex < 0) {
      return null;
    }
    const targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
    if (!targetNode || targetNode.type !== "leaf") {
      return null;
    }
    const actualTargetIndex = 0;
    return {
      sourceNode,
      targetNode,
      actualTargetIndex,
      isLastChild: false,
      operationType: this.operationType
    };
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode
    } = operation;
    const {
      apiRef
    } = ctx;
    const rowTree = gridRowTreeSelector(apiRef);
    const targetPath = buildTreeDataPath(targetNode, rowTree);
    let rowsToUpdate = [];
    if (sourceNode.type === "leaf") {
      const updatedRow = await updateLeafPath(sourceNode, targetPath, ctx);
      if (!updatedRow) {
        return;
      }
      rowsToUpdate.push(updatedRow);
    } else {
      const sourceParentNode = rowTree[sourceNode.parent];
      const sourceBasePath = buildTreeDataPath(sourceParentNode, rowTree);
      rowsToUpdate = await updateGroupHierarchyPaths(sourceNode, sourceBasePath, targetPath, ctx);
      if (rowsToUpdate.length === 0) {
        return;
      }
    }
    apiRef.current.setState((state) => {
      const updatedTree = _extends({}, state.rows.tree);
      removeNodeFromSourceParent(updatedTree, sourceNode);
      updatedTree[targetNode.id] = _extends({}, targetNode, {
        type: "group",
        children: [sourceNode.id],
        childrenFromPath: {},
        groupingField: null,
        isAutoGenerated: false,
        childrenExpanded: true
      });
      updateNodeParentAndDepth(updatedTree, sourceNode, targetNode.id, targetNode.depth + 1);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: updatedTree
        })
      });
    });
    apiRef.current.updateRows(rowsToUpdate);
    apiRef.current.publishEvent("rowsSet");
  }
};
var DropOnGroupOperation = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "drop-on-group");
  }
  detectOperation(ctx) {
    const {
      sourceRowId,
      dropPosition,
      placeholderIndex,
      sortedFilteredRowIds,
      apiRef,
      setTreeDataPath,
      rowTree
    } = ctx;
    if (dropPosition !== "inside") {
      return null;
    }
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type === "footer") {
      return null;
    }
    if (!setTreeDataPath) {
      displaySetTreeDataPathWarning("Drop on group reordering");
    }
    let targetIndex = placeholderIndex;
    if (targetIndex === sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
      targetIndex = sortedFilteredRowIds.length - 1;
    }
    if (targetIndex < 0) {
      return null;
    }
    const targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
    if (!targetNode || targetNode.type !== "group") {
      return null;
    }
    if (isDescendantOf(targetNode, sourceNode, rowTree)) {
      return null;
    }
    const actualTargetIndex = 0;
    return {
      sourceNode,
      targetNode,
      actualTargetIndex,
      isLastChild: false,
      operationType: this.operationType
    };
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode
    } = operation;
    const {
      apiRef
    } = ctx;
    const rowTree = gridRowTreeSelector(apiRef);
    const targetPath = buildTreeDataPath(targetNode, rowTree);
    let rowsToUpdate = [];
    if (sourceNode.type === "leaf") {
      const updatedRow = await updateLeafPath(sourceNode, targetPath, ctx);
      if (!updatedRow) {
        return;
      }
      rowsToUpdate.push(updatedRow);
    } else {
      const sourceParentNode = rowTree[sourceNode.parent];
      const sourceBasePath = buildTreeDataPath(sourceParentNode, rowTree);
      rowsToUpdate = await updateGroupHierarchyPaths(sourceNode, sourceBasePath, targetPath, ctx);
      if (rowsToUpdate.length === 0) {
        return;
      }
    }
    apiRef.current.setState((state) => {
      const updatedTree = _extends({}, state.rows.tree);
      removeNodeFromSourceParent(updatedTree, sourceNode);
      const targetGroup = updatedTree[targetNode.id];
      const targetChildren = [sourceNode.id, ...targetGroup.children];
      updatedTree[targetNode.id] = _extends({}, targetGroup, {
        children: targetChildren
      });
      updateNodeParentAndDepth(updatedTree, sourceNode, targetNode.id, targetNode.depth + 1);
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          tree: updatedTree
        })
      });
    });
    apiRef.current.updateRows(rowsToUpdate);
    apiRef.current.publishEvent("rowsSet");
  }
};
var CrossParentGroupOperation = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "cross-parent-group");
  }
  detectOperation(ctx) {
    if (ctx.dropPosition === "inside") {
      return null;
    }
    const {
      sourceRowId,
      placeholderIndex,
      sortedFilteredRowIds,
      rowTree,
      apiRef,
      setTreeDataPath
    } = ctx;
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type !== "group") {
      return null;
    }
    if (!setTreeDataPath) {
      displaySetTreeDataPathWarning("Cross-parent reordering");
    }
    let targetIndex = placeholderIndex;
    if (targetIndex === sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
      targetIndex = sortedFilteredRowIds.length - 1;
    }
    if (targetIndex < 0) {
      return null;
    }
    const targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[targetIndex]);
    if (!targetNode) {
      return null;
    }
    if (sourceNode.parent === targetNode.parent) {
      return null;
    }
    if (isDescendantOf(targetNode, sourceNode, rowTree)) {
      return null;
    }
    const actualTargetIndex = calculateTargetIndex(sourceNode, targetNode, placeholderIndex >= sortedFilteredRowIds.length, rowTree);
    return {
      sourceNode,
      targetNode,
      actualTargetIndex,
      isLastChild: placeholderIndex >= sortedFilteredRowIds.length,
      operationType: this.operationType
    };
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode,
      actualTargetIndex
    } = operation;
    const {
      apiRef
    } = ctx;
    const rowTree = gridRowTreeSelector(apiRef);
    const targetParentNode = rowTree[targetNode.parent];
    const newBasePath = buildTreeDataPath(targetParentNode, rowTree);
    const sourceParentNode = rowTree[sourceNode.parent];
    const sourceBasePath = buildTreeDataPath(sourceParentNode, rowTree);
    const updates = await updateGroupHierarchyPaths(sourceNode, sourceBasePath, newBasePath, ctx);
    if (updates.length > 0) {
      apiRef.current.setState((state) => {
        const updatedTree = _extends({}, state.rows.tree);
        removeNodeFromSourceParent(updatedTree, sourceNode);
        const targetParent = updatedTree[targetNode.parent];
        const targetChildren = [...targetParent.children];
        targetChildren.splice(actualTargetIndex, 0, sourceNode.id);
        updatedTree[targetNode.parent] = _extends({}, targetParent, {
          children: targetChildren
        });
        const newParentNode = updatedTree[targetNode.parent];
        const newGroupDepth = newParentNode.depth + 1;
        updateNodeParentAndDepth(updatedTree, sourceNode, targetNode.parent, newGroupDepth);
        return _extends({}, state, {
          rows: _extends({}, state.rows, {
            tree: updatedTree
          })
        });
      });
      apiRef.current.updateRows(updates);
      apiRef.current.publishEvent("rowsSet");
    }
  }
};
var treeDataReorderExecutor = new RowReorderExecutor([new SameParentSwapOperation(), new CrossParentLeafOperation(), new DropOnLeafOperation(), new DropOnGroupOperation(), new CrossParentGroupOperation()]);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rows/useGridRowsOverridableMethods.js
var useGridRowsOverridableMethods2 = (apiRef, props) => {
  const {
    processRowUpdate,
    onProcessRowUpdateError,
    setTreeDataPath
  } = props;
  const {
    setRowIndex: setRowIndexFlat,
    setRowPosition: setRowPositionFlat
  } = useGridRowsOverridableMethods(apiRef);
  const flatTree = useGridSelector(apiRef, gridRowMaximumTreeDepthSelector) === 1;
  const setRowPosition = React271.useCallback(async (sourceRowId, targetRowId, position) => {
    const sortedFilteredRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const sortedFilteredRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    const targetNode = gridRowNodeSelector(apiRef, targetRowId);
    if (!sourceNode) {
      throw new Error(`MUI X: No row with id #${sourceRowId} found.`);
    }
    if (!targetNode) {
      throw new Error(`MUI X: No row with id #${targetRowId} found.`);
    }
    if (sourceNode.type === "footer") {
      throw new Error(`MUI X: The row reordering do not support reordering of footer rows.`);
    }
    const targetIndexUnadjusted = sortedFilteredRowIndexLookup[targetRowId];
    if (targetIndexUnadjusted === void 0) {
      throw new Error(`MUI X: Target row with id #${targetRowId} not found in current view.`);
    }
    const targetIndex = position === "below" ? targetIndexUnadjusted + 1 : targetIndexUnadjusted;
    const executionContext = {
      sourceRowId,
      dropPosition: position,
      placeholderIndex: targetIndex,
      sortedFilteredRowIds,
      sortedFilteredRowIndexLookup,
      rowTree,
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError,
      setTreeDataPath
    };
    return treeDataReorderExecutor.execute(executionContext);
  }, [apiRef, processRowUpdate, onProcessRowUpdateError, setTreeDataPath]);
  const setRowIndex = React271.useCallback(async () => {
    throw new Error(`MUI X: \`setRowIndex()\` is not supported for tree data. Use \`setRowPosition()\` instead.`);
  }, []);
  return {
    setRowIndex: flatTree ? setRowIndexFlat : setRowIndex,
    setRowPosition: flatTree ? setRowPositionFlat : setRowPosition
  };
};

// node_modules/@mui/x-data-grid-pro/esm/DataGridPro/useDataGridProComponent.js
var React298 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/infiniteLoader/useGridInfiniteLoader.js
var useGridInfiniteLoader = (apiRef, props) => {
  const isEnabled = props.rowsLoadingMode === "client" && !!props.onRowsScrollEnd;
  const handleLoadMoreRows = useEventCallback_default(() => {
    const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
    const currentPage = getVisibleRows(apiRef);
    const viewportPageSize = apiRef.current.getViewportPageSize();
    const rowScrollEndParams = {
      visibleColumns,
      viewportPageSize,
      visibleRowsCount: currentPage.rows.length
    };
    apiRef.current.publishEvent("rowsScrollEnd", rowScrollEndParams);
  });
  useGridEventPriority(apiRef, "rowsScrollEnd", props.onRowsScrollEnd);
  useGridEvent(apiRef, "rowsScrollEndIntersection", runIf(isEnabled, handleLoadMoreRows));
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/columnReorder/useGridColumnReorder.js
var React272 = __toESM(require_react(), 1);
var CURSOR_MOVE_DIRECTION_LEFT = "left";
var CURSOR_MOVE_DIRECTION_RIGHT = "right";
var getCursorMoveDirectionX = (currentCoordinates, nextCoordinates) => {
  return currentCoordinates.x <= nextCoordinates.x ? CURSOR_MOVE_DIRECTION_RIGHT : CURSOR_MOVE_DIRECTION_LEFT;
};
var hasCursorPositionChanged = (currentCoordinates, nextCoordinates) => currentCoordinates.x !== nextCoordinates.x || currentCoordinates.y !== nextCoordinates.y;
var useUtilityClasses101 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    columnHeaderDragging: ["columnHeader--dragging"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var columnReorderStateInitializer = (state) => _extends({}, state, {
  columnReorder: {
    dragCol: ""
  }
});
var useGridColumnReorder = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridColumnReorder");
  const dragColNode = React272.useRef(null);
  const cursorPosition2 = React272.useRef({
    x: 0,
    y: 0
  });
  const originColumnIndex = React272.useRef(null);
  const forbiddenIndexes = React272.useRef({});
  const removeDnDStylesTimeout = React272.useRef(void 0);
  const ownerState = {
    classes: props.classes
  };
  const classes2 = useUtilityClasses101(ownerState);
  const isRtl = useRtl();
  React272.useEffect(() => {
    return () => {
      clearTimeout(removeDnDStylesTimeout.current);
    };
  }, []);
  const handleDragEnd = React272.useCallback((params, event) => {
    const dragColField = gridColumnReorderDragColSelector(apiRef);
    if (props.disableColumnReorder || !dragColField) {
      return;
    }
    logger.debug("End dragging col");
    event.preventDefault();
    event.stopPropagation();
    clearTimeout(removeDnDStylesTimeout.current);
    if (dragColNode.current.classList.contains(classes2.columnHeaderDragging)) {
      dragColNode.current.classList.remove(classes2.columnHeaderDragging);
    }
    dragColNode.current = null;
    if (event.dataTransfer.dropEffect === "none" && !props.keepColumnPositionIfDraggedOutside) {
      apiRef.current.setColumnIndex(dragColField, originColumnIndex.current);
      originColumnIndex.current = null;
    } else {
      const columnOrderChangeParams = {
        column: apiRef.current.getColumn(dragColField),
        targetIndex: apiRef.current.getColumnIndexRelativeToVisibleColumns(dragColField),
        oldIndex: originColumnIndex.current
      };
      apiRef.current.publishEvent("columnOrderChange", columnOrderChangeParams);
    }
    apiRef.current.setState((state) => _extends({}, state, {
      columnReorder: _extends({}, state.columnReorder, {
        dragCol: ""
      })
    }));
  }, [apiRef, props.disableColumnReorder, props.keepColumnPositionIfDraggedOutside, logger, classes2.columnHeaderDragging]);
  const handleDragStart = React272.useCallback((params, event) => {
    if (props.disableColumnReorder || params.colDef.disableReorder) {
      return;
    }
    logger.debug(`Start dragging col ${params.field}`);
    event.stopPropagation();
    dragColNode.current = event.currentTarget;
    dragColNode.current.classList.add(classes2.columnHeaderDragging);
    const handleDragEndEvent = (dragEndEvent) => {
      dragColNode.current.removeEventListener("dragend", handleDragEndEvent);
      apiRef.current.publishEvent("columnHeaderDragEndNative", params, dragEndEvent);
    };
    dragColNode.current.addEventListener("dragend", handleDragEndEvent);
    if (event.dataTransfer) {
      event.dataTransfer.effectAllowed = "move";
    }
    apiRef.current.setState((state) => _extends({}, state, {
      columnReorder: _extends({}, state.columnReorder, {
        dragCol: params.field
      })
    }));
    removeDnDStylesTimeout.current = setTimeout(() => {
      dragColNode.current.classList.remove(classes2.columnHeaderDragging);
    });
    originColumnIndex.current = apiRef.current.getColumnIndex(params.field, false);
    const draggingColumnGroupPath = apiRef.current.getColumnGroupPath(params.field);
    const columnIndex = originColumnIndex.current;
    const allColumns = apiRef.current.getAllColumns();
    const groupsLookup = apiRef.current.getAllGroupDetails();
    const getGroupPathFromColumnIndex = (colIndex) => {
      const field = allColumns[colIndex].field;
      return apiRef.current.getColumnGroupPath(field);
    };
    let limitingGroupId = null;
    draggingColumnGroupPath.forEach((groupId) => {
      var _a;
      if (!((_a = groupsLookup[groupId]) == null ? void 0 : _a.freeReordering)) {
        if (columnIndex > 0 && getGroupPathFromColumnIndex(columnIndex - 1).includes(groupId)) {
          limitingGroupId = groupId;
        } else if (columnIndex + 1 < allColumns.length && getGroupPathFromColumnIndex(columnIndex + 1).includes(groupId)) {
          limitingGroupId = groupId;
        }
      }
    });
    forbiddenIndexes.current = {};
    for (let indexToForbid = 0; indexToForbid < allColumns.length; indexToForbid += 1) {
      const leftIndex = indexToForbid <= columnIndex ? indexToForbid - 1 : indexToForbid;
      const rightIndex = indexToForbid < columnIndex ? indexToForbid : indexToForbid + 1;
      if (limitingGroupId !== null) {
        let allowIndex = false;
        if (leftIndex >= 0 && getGroupPathFromColumnIndex(leftIndex).includes(limitingGroupId)) {
          allowIndex = true;
        } else if (rightIndex < allColumns.length && getGroupPathFromColumnIndex(rightIndex).includes(limitingGroupId)) {
          allowIndex = true;
        }
        if (!allowIndex) {
          forbiddenIndexes.current[indexToForbid] = true;
        }
      }
      if (leftIndex >= 0 && rightIndex < allColumns.length) {
        getGroupPathFromColumnIndex(rightIndex).forEach((groupId) => {
          var _a;
          if (getGroupPathFromColumnIndex(leftIndex).includes(groupId)) {
            if (!draggingColumnGroupPath.includes(groupId)) {
              if (!((_a = groupsLookup[groupId]) == null ? void 0 : _a.freeReordering)) {
                forbiddenIndexes.current[indexToForbid] = true;
              }
            }
          }
        });
      }
    }
  }, [props.disableColumnReorder, classes2.columnHeaderDragging, logger, apiRef]);
  const handleDragEnter = React272.useCallback((params, event) => {
    event.preventDefault();
    event.stopPropagation();
  }, []);
  const handleDragOver = React272.useCallback((params, event) => {
    const dragColField = gridColumnReorderDragColSelector(apiRef);
    if (!dragColField) {
      return;
    }
    logger.debug(`Dragging over col ${params.field}`);
    event.preventDefault();
    event.stopPropagation();
    const coordinates = {
      x: event.clientX,
      y: event.clientY
    };
    if (params.field !== dragColField && hasCursorPositionChanged(cursorPosition2.current, coordinates)) {
      const targetColIndex = apiRef.current.getColumnIndex(params.field, false);
      const targetColVisibleIndex = apiRef.current.getColumnIndex(params.field, true);
      const targetCol = apiRef.current.getColumn(params.field);
      const dragColIndex = apiRef.current.getColumnIndex(dragColField, false);
      const visibleColumns = apiRef.current.getVisibleColumns();
      const allColumns = apiRef.current.getAllColumns();
      const cursorMoveDirectionX = getCursorMoveDirectionX(cursorPosition2.current, coordinates);
      const hasMovedLeft = cursorMoveDirectionX === CURSOR_MOVE_DIRECTION_LEFT && (isRtl ? dragColIndex < targetColIndex : targetColIndex < dragColIndex);
      const hasMovedRight = cursorMoveDirectionX === CURSOR_MOVE_DIRECTION_RIGHT && (isRtl ? targetColIndex < dragColIndex : dragColIndex < targetColIndex);
      if (hasMovedLeft || hasMovedRight) {
        let canBeReordered;
        let indexOffsetInHiddenColumns = 0;
        if (!targetCol.disableReorder) {
          canBeReordered = true;
        } else if (hasMovedLeft) {
          canBeReordered = targetColVisibleIndex > 0 && !visibleColumns[targetColVisibleIndex - 1].disableReorder;
        } else {
          canBeReordered = targetColVisibleIndex < visibleColumns.length - 1 && !visibleColumns[targetColVisibleIndex + 1].disableReorder;
        }
        if (forbiddenIndexes.current[targetColIndex]) {
          let nextVisibleColumnField;
          let indexWithOffset = targetColIndex + indexOffsetInHiddenColumns;
          if (hasMovedLeft) {
            nextVisibleColumnField = targetColVisibleIndex > 0 ? visibleColumns[targetColVisibleIndex - 1].field : null;
            while (indexWithOffset > 0 && allColumns[indexWithOffset].field !== nextVisibleColumnField && forbiddenIndexes.current[indexWithOffset]) {
              indexOffsetInHiddenColumns -= 1;
              indexWithOffset = targetColIndex + indexOffsetInHiddenColumns;
            }
          } else {
            nextVisibleColumnField = targetColVisibleIndex + 1 < visibleColumns.length ? visibleColumns[targetColVisibleIndex + 1].field : null;
            while (indexWithOffset < allColumns.length - 1 && allColumns[indexWithOffset].field !== nextVisibleColumnField && forbiddenIndexes.current[indexWithOffset]) {
              indexOffsetInHiddenColumns += 1;
              indexWithOffset = targetColIndex + indexOffsetInHiddenColumns;
            }
          }
          if (forbiddenIndexes.current[indexWithOffset] || allColumns[indexWithOffset].field === nextVisibleColumnField) {
            canBeReordered = false;
          }
        }
        const canBeReorderedProcessed = apiRef.current.unstable_applyPipeProcessors("canBeReordered", canBeReordered, {
          targetIndex: targetColVisibleIndex
        });
        if (canBeReorderedProcessed) {
          apiRef.current.setColumnIndex(dragColField, targetColIndex + indexOffsetInHiddenColumns);
        }
      }
      cursorPosition2.current = coordinates;
    }
  }, [apiRef, logger, isRtl]);
  React272.useEffect(() => {
    if (!props.keepColumnPositionIfDraggedOutside) {
      return () => {
      };
    }
    const doc = ownerDocument(apiRef.current.rootElementRef.current);
    const listener = (event) => {
      if (event.dataTransfer) {
        event.preventDefault();
        event.dataTransfer.dropEffect = "move";
      }
    };
    doc.addEventListener("dragover", listener);
    return () => {
      doc.removeEventListener("dragover", listener);
    };
  }, [apiRef, props.keepColumnPositionIfDraggedOutside]);
  useGridEvent(apiRef, "columnHeaderDragStart", handleDragStart);
  useGridEvent(apiRef, "columnHeaderDragEnter", handleDragEnter);
  useGridEvent(apiRef, "columnHeaderDragOver", handleDragOver);
  useGridEvent(apiRef, "columnHeaderDragEndNative", handleDragEnd);
  useGridEvent(apiRef, "cellDragEnter", handleDragEnter);
  useGridEvent(apiRef, "cellDragOver", handleDragOver);
  useGridEventPriority(apiRef, "columnOrderChange", props.onColumnOrderChange);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/useGridTreeData.js
var React273 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/reorderValidator.js
var RowReorderValidator = class {
  constructor(rules) {
    this.rules = rules;
  }
  validate(context) {
    for (const rule of this.rules) {
      if (rule.applies(context) && rule.isInvalid(context)) {
        return false;
      }
    }
    return true;
  }
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/commonReorderConditions.js
var commonReorderConditions = {
  // Node type checks
  isGroupToGroup: (ctx) => ctx.sourceNode.type === "group" && ctx.targetNode.type === "group",
  isLeafToLeaf: (ctx) => ctx.sourceNode.type === "leaf" && ctx.targetNode.type === "leaf",
  isLeafToGroup: (ctx) => ctx.sourceNode.type === "leaf" && ctx.targetNode.type === "group",
  isGroupToLeaf: (ctx) => ctx.sourceNode.type === "group" && ctx.targetNode.type === "leaf",
  // Drop position checks
  isDropAbove: (ctx) => ctx.dropPosition === "above",
  isDropBelow: (ctx) => ctx.dropPosition === "below",
  // Depth checks
  sameDepth: (ctx) => ctx.sourceNode.depth === ctx.targetNode.depth,
  sourceDepthGreater: (ctx) => ctx.sourceNode.depth > ctx.targetNode.depth,
  targetDepthIsSourceMinusOne: (ctx) => ctx.targetNode.depth === ctx.sourceNode.depth - 1,
  // Parent checks
  sameParent: (ctx) => ctx.sourceNode.parent === ctx.targetNode.parent,
  // Node state checks
  targetGroupExpanded: (ctx) => (ctx.targetNode.type === "group" && ctx.targetNode.childrenExpanded) ?? false,
  targetGroupCollapsed: (ctx) => ctx.targetNode.type === "group" && !ctx.targetNode.childrenExpanded,
  // Previous/Next node checks
  hasPrevNode: (ctx) => ctx.prevNode !== null,
  hasNextNode: (ctx) => ctx.nextNode !== null,
  prevIsLeaf: (ctx) => {
    var _a;
    return ((_a = ctx.prevNode) == null ? void 0 : _a.type) === "leaf";
  },
  prevIsGroup: (ctx) => {
    var _a;
    return ((_a = ctx.prevNode) == null ? void 0 : _a.type) === "group";
  },
  nextIsLeaf: (ctx) => {
    var _a;
    return ((_a = ctx.nextNode) == null ? void 0 : _a.type) === "leaf";
  },
  nextIsGroup: (ctx) => {
    var _a;
    return ((_a = ctx.nextNode) == null ? void 0 : _a.type) === "group";
  },
  prevDepthEquals: (ctx, depth) => {
    var _a;
    return ((_a = ctx.prevNode) == null ? void 0 : _a.depth) === depth;
  },
  prevDepthEqualsSource: (ctx) => {
    var _a;
    return ((_a = ctx.prevNode) == null ? void 0 : _a.depth) === ctx.sourceNode.depth;
  },
  // Complex checks
  prevBelongsToSource: (ctx) => {
    if (!ctx.prevNode) {
      return false;
    }
    let currentId = ctx.prevNode.parent;
    while (currentId) {
      if (currentId === ctx.sourceNode.id) {
        return true;
      }
      const node = gridRowTreeSelector(ctx.apiRef)[currentId];
      if (!node) {
        break;
      }
      currentId = node.parent;
    }
    return false;
  },
  // Position checks
  isAdjacentPosition: (ctx) => {
    const expandedSortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(ctx.apiRef);
    const sourceRowIndex = expandedSortedRowIndexLookup[ctx.sourceNode.id];
    const targetRowIndex = expandedSortedRowIndexLookup[ctx.targetNode.id];
    const dropPosition = ctx.dropPosition;
    return dropPosition === "above" && targetRowIndex === sourceRowIndex + 1 || dropPosition === "below" && targetRowIndex === sourceRowIndex - 1;
  },
  // First child check
  targetFirstChildIsGroupWithSourceDepth: (ctx) => {
    var _a;
    if (ctx.targetNode.type !== "group") {
      return false;
    }
    const rowTree = gridRowTreeSelector(ctx.apiRef);
    const targetGroup = ctx.targetNode;
    const firstChild = ((_a = targetGroup.children) == null ? void 0 : _a[0]) ? rowTree[targetGroup.children[0]] : null;
    return (firstChild == null ? void 0 : firstChild.type) === "group" && firstChild.depth === ctx.sourceNode.depth;
  },
  targetFirstChildDepthEqualsSource: (ctx) => {
    var _a;
    if (ctx.targetNode.type !== "group") {
      return false;
    }
    const rowTree = gridRowTreeSelector(ctx.apiRef);
    const targetGroup = ctx.targetNode;
    const firstChild = ((_a = targetGroup.children) == null ? void 0 : _a[0]) ? rowTree[targetGroup.children[0]] : null;
    return firstChild ? firstChild.depth === ctx.sourceNode.depth : false;
  }
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/treeDataReorderValidator.js
var validationRules = [{
  name: "same-position",
  applies: (ctx) => ctx.sourceNode.id === ctx.targetNode.id,
  isInvalid: () => true,
  message: "Source and target are the same"
}, {
  name: "adjacent-position",
  applies: (ctx) => commonReorderConditions.isAdjacentPosition(ctx),
  isInvalid: () => true,
  message: "Source and target are adjacent"
}, {
  name: "to-descendent",
  applies: (ctx) => commonReorderConditions.isGroupToLeaf(ctx) || commonReorderConditions.isGroupToGroup(ctx),
  isInvalid: (ctx) => {
    let currentNode = ctx.targetNode;
    const rowTree = gridRowTreeSelector(ctx.apiRef);
    while (currentNode.parent) {
      currentNode = rowTree[currentNode.parent];
      if (currentNode.id === ctx.sourceNode.id) {
        return true;
      }
    }
    return false;
  },
  message: "Cannot drop group on one of its descendents"
}, {
  name: "group-to-group-above-leaf-belongs-to-source",
  applies: (ctx) => commonReorderConditions.isGroupToGroup(ctx) && commonReorderConditions.isDropAbove(ctx) && commonReorderConditions.prevIsLeaf(ctx),
  isInvalid: commonReorderConditions.prevBelongsToSource,
  message: "Previous leaf belongs to source group or its descendants"
}];
var treeDataReorderValidator = new RowReorderValidator(validationRules);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/useGridTreeData.js
var useGridTreeData = (apiRef, props) => {
  const handleCellKeyDown = React273.useCallback((params, event) => {
    const cellParams = apiRef.current.getCellParams(params.id, params.field);
    if (cellParams.colDef.field === GRID_TREE_DATA_GROUPING_FIELD && (event.key === " " || event.key === "Enter") && !event.shiftKey) {
      if (params.rowNode.type !== "group") {
        return;
      }
      if (props.dataSource && !params.rowNode.childrenExpanded) {
        apiRef.current.dataSource.fetchRows(params.id);
        return;
      }
      apiRef.current.setRowChildrenExpansion(params.id, !params.rowNode.childrenExpanded);
    }
  }, [apiRef, props.dataSource]);
  const isValidRowReorderProp = props.isValidRowReorder;
  const isRowReorderValid = React273.useCallback((initialValue, {
    sourceRowId,
    targetRowId,
    dropPosition,
    dragDirection
  }) => {
    if (gridRowMaximumTreeDepthSelector(apiRef) === 1 || !props.treeData) {
      return initialValue;
    }
    const expandedSortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const expandedSortedRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const targetRowIndex = expandedSortedRowIndexLookup[targetRowId];
    const sourceNode = rowTree[sourceRowId];
    const targetNode = rowTree[targetRowId];
    const prevNode = targetRowIndex > 0 ? rowTree[expandedSortedRowIds[targetRowIndex - 1]] : null;
    const nextNode = targetRowIndex < expandedSortedRowIds.length - 1 ? rowTree[expandedSortedRowIds[targetRowIndex + 1]] : null;
    if (!sourceNode || !targetNode) {
      return false;
    }
    const context = {
      apiRef,
      sourceNode,
      targetNode,
      prevNode,
      nextNode,
      dropPosition,
      dragDirection
    };
    let isValid = treeDataReorderValidator.validate(context);
    if (isValid && isValidRowReorderProp) {
      isValid = isValidRowReorderProp(context);
    }
    return isValid;
  }, [apiRef, props.treeData, isValidRowReorderProp]);
  useGridRegisterPipeProcessor(apiRef, "isRowReorderValid", isRowReorderValid);
  useGridEvent(apiRef, "cellKeyDown", handleCellKeyDown);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/useGridTreeDataPreProcessors.js
var React297 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/gridTreeDataUtils.js
var TreeDataStrategy = (function(TreeDataStrategy2) {
  TreeDataStrategy2["Default"] = "tree-data";
  TreeDataStrategy2["DataSource"] = "tree-data-source";
  return TreeDataStrategy2;
})({});
var filterRowTreeFromTreeData = (params) => {
  const {
    apiRef,
    rowTree,
    disableChildrenFiltering,
    isRowMatchingFilters
  } = params;
  const filteredRowsLookup = {};
  const filteredChildrenCountLookup = {};
  const filteredDescendantCountLookup = {};
  const filterCache = {};
  const filterResults = {
    passingFilterItems: null,
    passingQuickFilterValues: null
  };
  const filterTreeNode = (node, isParentMatchingFilters, areAncestorsExpanded) => {
    const shouldSkipFilters = disableChildrenFiltering && node.depth > 0;
    let isMatchingFilters;
    if (shouldSkipFilters) {
      isMatchingFilters = null;
    } else if (!isRowMatchingFilters || node.type === "footer") {
      isMatchingFilters = true;
    } else {
      const row = apiRef.current.getRow(node.id);
      isRowMatchingFilters(row, void 0, filterResults);
      isMatchingFilters = passFilterLogic([filterResults.passingFilterItems], [filterResults.passingQuickFilterValues], params.filterModel, params.filterValueGetter, params.apiRef, filterCache);
    }
    let filteredChildrenCount = 0;
    let filteredDescendantCount = 0;
    if (node.type === "group") {
      node.children.forEach((childId) => {
        const childNode = rowTree[childId];
        const childSubTreeSize = filterTreeNode(childNode, isMatchingFilters ?? isParentMatchingFilters, areAncestorsExpanded && !!node.childrenExpanded);
        filteredDescendantCount += childSubTreeSize;
        if (childSubTreeSize > 0) {
          filteredChildrenCount += 1;
        }
      });
    }
    let shouldPassFilters;
    switch (isMatchingFilters) {
      case true: {
        shouldPassFilters = true;
        break;
      }
      case false: {
        shouldPassFilters = filteredDescendantCount > 0;
        break;
      }
      default: {
        shouldPassFilters = isParentMatchingFilters;
        break;
      }
    }
    if (!shouldPassFilters) {
      filteredRowsLookup[node.id] = false;
    }
    if (!shouldPassFilters) {
      return 0;
    }
    filteredChildrenCountLookup[node.id] = filteredChildrenCount;
    filteredDescendantCountLookup[node.id] = filteredDescendantCount;
    if (node.type === "footer") {
      return filteredDescendantCount;
    }
    return filteredDescendantCount + 1;
  };
  const nodes = Object.values(rowTree);
  for (let i2 = 0; i2 < nodes.length; i2 += 1) {
    const node = nodes[i2];
    if (node.depth === 0) {
      filterTreeNode(node, true, true);
    }
  }
  return {
    filteredRowsLookup,
    filteredChildrenCountLookup,
    filteredDescendantCountLookup
  };
};

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/insertDataRowInTree.js
var insertDataRowInTree = ({
  id,
  path,
  previousTree,
  tree,
  treeDepths,
  onDuplicatePath,
  isGroupExpandedByDefault,
  defaultGroupingExpansionDepth,
  serverChildrenCount,
  groupsToFetch,
  maxDepth
}) => {
  var _a, _b, _c, _d;
  let parentNodeId = GRID_ROOT_GROUP_ID;
  for (let depth = 0; depth < path.length; depth += 1) {
    const {
      key,
      field
    } = path[depth];
    const fieldWithDefaultValue = field ?? "__no_field__";
    const keyWithDefaultValue = key ?? "__no_key__";
    const existingNodeIdWithPartialPath = (_b = (_a = tree[parentNodeId].childrenFromPath) == null ? void 0 : _a[fieldWithDefaultValue]) == null ? void 0 : _b[keyWithDefaultValue.toString()];
    if (depth === path.length - 1) {
      if (existingNodeIdWithPartialPath == null) {
        let node;
        if (serverChildrenCount !== void 0 && serverChildrenCount !== 0) {
          node = {
            type: "group",
            id,
            parent: parentNodeId,
            path: path.map((step) => step.key),
            depth,
            isAutoGenerated: false,
            groupingKey: key,
            groupingField: field,
            children: [],
            childrenFromPath: {},
            childrenExpanded: false,
            serverChildrenCount
          };
          const shouldFetchChildren = checkGroupChildrenExpansion(node, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault);
          if (shouldFetchChildren) {
            groupsToFetch == null ? void 0 : groupsToFetch.add(id);
          }
        } else {
          node = {
            type: "leaf",
            id,
            depth,
            parent: parentNodeId,
            groupingKey: key
          };
        }
        insertNodeInTree(node, tree, treeDepths, previousTree);
      } else {
        const existingNodeWithPartialPath = tree[existingNodeIdWithPartialPath];
        if (existingNodeWithPartialPath.type === "group" && existingNodeWithPartialPath.isAutoGenerated) {
          updateGroupNodeIdAndAutoGenerated({
            tree,
            previousTree,
            treeDepths,
            node: existingNodeWithPartialPath,
            updatedNode: {
              id,
              isAutoGenerated: false
            }
          });
        } else {
          onDuplicatePath == null ? void 0 : onDuplicatePath(existingNodeIdWithPartialPath, id, path);
        }
      }
    } else if (existingNodeIdWithPartialPath == null) {
      const nodeId = getGroupRowIdFromPath(path.slice(0, depth + 1));
      const autoGeneratedGroupNode = {
        type: "group",
        id: nodeId,
        parent: parentNodeId,
        depth,
        isAutoGenerated: true,
        groupingKey: key,
        groupingField: field,
        children: [],
        childrenFromPath: {},
        childrenExpanded: false
      };
      insertNodeInTree(updateGroupDefaultExpansion(autoGeneratedGroupNode, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault, (_c = previousTree == null ? void 0 : previousTree[nodeId]) == null ? void 0 : _c.childrenExpanded), tree, treeDepths, previousTree);
      parentNodeId = nodeId;
    } else {
      const currentGroupNode = tree[existingNodeIdWithPartialPath];
      if (currentGroupNode.type !== "group") {
        const groupNode = {
          type: "group",
          id: currentGroupNode.id,
          parent: currentGroupNode.parent,
          depth: currentGroupNode.depth,
          isAutoGenerated: false,
          groupingKey: key,
          groupingField: field,
          children: [],
          childrenFromPath: {},
          childrenExpanded: false
        };
        tree[existingNodeIdWithPartialPath] = updateGroupDefaultExpansion(groupNode, defaultGroupingExpansionDepth, maxDepth, isGroupExpandedByDefault, (_d = previousTree == null ? void 0 : previousTree[currentGroupNode.id]) == null ? void 0 : _d.childrenExpanded);
      }
      parentNodeId = currentGroupNode.id;
    }
  }
};

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/createRowTree.js
var createRowTree = (params) => {
  const dataRowIds = [];
  const tree = {
    [GRID_ROOT_GROUP_ID]: buildRootGroup()
  };
  const treeDepths = {};
  const groupsToFetch = /* @__PURE__ */ new Set();
  for (let i2 = 0; i2 < params.nodes.length; i2 += 1) {
    const node = params.nodes[i2];
    dataRowIds.push(node.id);
    insertDataRowInTree({
      tree,
      previousTree: params.previousTree,
      id: node.id,
      path: node.path,
      serverChildrenCount: node.serverChildrenCount,
      onDuplicatePath: params.onDuplicatePath,
      treeDepths,
      isGroupExpandedByDefault: params.isGroupExpandedByDefault,
      defaultGroupingExpansionDepth: params.defaultGroupingExpansionDepth,
      groupsToFetch,
      maxDepth: params.maxDepth
    });
  }
  return {
    tree,
    treeDepths,
    groupingName: params.groupingName,
    dataRowIds,
    groupsToFetch: Array.from(groupsToFetch)
  };
};

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/sortRowTree.js
var Node2 = class {
  constructor(data, next) {
    this.next = next;
    this.data = data;
  }
  insertAfter(list) {
    if (!list.first || !list.last) {
      return;
    }
    const next = this.next;
    this.next = list.first;
    list.last.next = next;
  }
};
var List4 = class _List {
  constructor(first, last) {
    this.first = first;
    this.last = last;
  }
  data() {
    const array = [];
    this.forEach((node) => {
      array.push(node.data);
    });
    return array;
  }
  forEach(fn2) {
    let current = this.first;
    while (current !== null) {
      fn2(current);
      current = current.next;
    }
  }
  static from(array) {
    if (array.length === 0) {
      return new _List(null, null);
    }
    let index = 0;
    const first = new Node2(array[index], null);
    let current = first;
    while (index + 1 < array.length) {
      index += 1;
      const node = new Node2(array[index], null);
      current.next = node;
      current = node;
    }
    return new _List(first, current);
  }
};
var sortRowTree = (params) => {
  const {
    rowTree,
    disableChildrenSorting,
    sortRowList,
    shouldRenderGroupBelowLeaves
  } = params;
  const sortedGroupedByParentRows = /* @__PURE__ */ new Map();
  const sortGroup = (node) => {
    const shouldSortGroup = !!sortRowList && (!disableChildrenSorting || node.depth === -1);
    let sortedRowIds;
    if (shouldSortGroup) {
      for (let i2 = 0; i2 < node.children.length; i2 += 1) {
        const childNode = rowTree[node.children[i2]];
        if (childNode.type === "group") {
          sortGroup(childNode);
        }
      }
      sortedRowIds = sortRowList(node.children.map((childId) => rowTree[childId]));
    } else if (shouldRenderGroupBelowLeaves) {
      const childrenLeaves = [];
      const childrenGroups = [];
      for (let i2 = 0; i2 < node.children.length; i2 += 1) {
        const childId = node.children[i2];
        const childNode = rowTree[childId];
        if (childNode.type === "group") {
          sortGroup(childNode);
          childrenGroups.push(childId);
        } else if (childNode.type === "leaf") {
          childrenLeaves.push(childId);
        }
      }
      sortedRowIds = [...childrenLeaves, ...childrenGroups];
    } else {
      for (let i2 = 0; i2 < node.children.length; i2 += 1) {
        const childNode = rowTree[node.children[i2]];
        if (childNode.type === "group") {
          sortGroup(childNode);
        }
      }
      sortedRowIds = [...node.children];
    }
    if (node.footerId != null) {
      sortedRowIds.push(node.footerId);
    }
    sortedGroupedByParentRows.set(node.id, sortedRowIds);
  };
  sortGroup(rowTree[GRID_ROOT_GROUP_ID]);
  const rootList = List4.from(sortedGroupedByParentRows.get(GRID_ROOT_GROUP_ID));
  rootList.forEach((node) => {
    const children2 = sortedGroupedByParentRows.get(node.data);
    if (children2 == null ? void 0 : children2.length) {
      node.insertAfter(List4.from(children2));
    }
  });
  return rootList.data();
};

// node_modules/@mui/x-data-grid-pro/esm/components/GridColumnHeaders.js
var React275 = __toESM(require_react(), 1);
var import_prop_types148 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/columnHeaders/useGridColumnHeaders.js
var React274 = __toESM(require_react(), 1);
var import_jsx_runtime197 = __toESM(require_jsx_runtime(), 1);
var _excluded73 = ["getColumnsToRender", "getPinnedCellOffset", "renderContext", "leftRenderContext", "rightRenderContext", "pinnedColumns", "visibleColumns", "columnPositions"];
var useUtilityClasses102 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  return React274.useMemo(() => {
    const slots = {
      headerFilterRow: ["headerFilterRow"]
    };
    return composeClasses(slots, getDataGridUtilityClass, classes2);
  }, [classes2]);
};
var useGridColumnHeadersPro = (props) => {
  const apiRef = useGridPrivateApiContext();
  const {
    headerGroupingMaxDepth,
    hasOtherElementInTabSequence
  } = props;
  const columnHeaderFilterTabIndexState = useGridSelector(apiRef, gridTabIndexColumnHeaderFilterSelector);
  const _useGridColumnHeaders = useGridColumnHeaders(_extends({}, props, {
    hasOtherElementInTabSequence: hasOtherElementInTabSequence || columnHeaderFilterTabIndexState !== null
  })), {
    getColumnsToRender,
    getPinnedCellOffset: getPinnedCellOffset2,
    renderContext,
    leftRenderContext,
    rightRenderContext,
    pinnedColumns,
    visibleColumns,
    columnPositions
  } = _useGridColumnHeaders, otherProps = _objectWithoutPropertiesLoose(_useGridColumnHeaders, _excluded73);
  const headerFiltersRef = React274.useRef(null);
  apiRef.current.register("private", {
    headerFiltersElementRef: headerFiltersRef
  });
  const headerFilterMenuRef = React274.useRef(null);
  const rootProps = useGridRootProps2();
  const classes2 = useUtilityClasses102(rootProps);
  const disableHeaderFiltering = !rootProps.headerFilters;
  const filterModel = useGridSelector(apiRef, gridFilterModelSelector);
  const columnsTotalWidth = useGridSelector(apiRef, gridColumnsTotalWidthSelector);
  const gridHasFiller = useGridSelector(apiRef, gridHasFillerSelector);
  const headerFilterHeight = useGridSelector(apiRef, gridHeaderFilterHeightSelector);
  const scrollbarWidth = useGridSelector(apiRef, gridVerticalScrollbarWidthSelector);
  const columnHeaderFilterFocus = useGridSelector(apiRef, gridFocusColumnHeaderFilterSelector);
  const filterItemsCache = React274.useRef(/* @__PURE__ */ Object.create(null)).current;
  const getFilterItem = React274.useCallback(
    (colDef) => {
      const filterModelItem = filterModel == null ? void 0 : filterModel.items.find((it) => it.field === colDef.field && it.operator !== "isAnyOf");
      if (filterModelItem != null) {
        return filterModelItem;
      }
      const defaultCachedItem = filterItemsCache[colDef.field];
      if (defaultCachedItem != null) {
        return defaultCachedItem;
      }
      const defaultItem = getGridFilter(colDef);
      filterItemsCache[colDef.field] = defaultItem;
      return defaultItem;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [filterModel]
  );
  const getColumnFilters = (params) => {
    var _a;
    const {
      renderedColumns,
      firstColumnToRender
    } = getColumnsToRender(params);
    const filters = [];
    for (let i2 = 0; i2 < renderedColumns.length; i2 += 1) {
      const colDef = renderedColumns[i2];
      const columnIndex = firstColumnToRender + i2;
      const hasFocus = (columnHeaderFilterFocus == null ? void 0 : columnHeaderFilterFocus.field) === colDef.field;
      const isFirstColumn = columnIndex === 0;
      const tabIndexField = columnHeaderFilterTabIndexState == null ? void 0 : columnHeaderFilterTabIndexState.field;
      const tabIndex = tabIndexField === colDef.field || isFirstColumn && !props.hasOtherElementInTabSequence ? 0 : -1;
      const headerClassName = typeof colDef.headerClassName === "function" ? colDef.headerClassName({
        field: colDef.field,
        colDef
      }) : colDef.headerClassName;
      const item = getFilterItem(colDef);
      const pinnedPosition = params == null ? void 0 : params.position;
      const pinnedOffset = getPinnedCellOffset2(pinnedPosition, colDef.computedWidth, columnIndex, columnPositions, columnsTotalWidth, scrollbarWidth);
      const indexInSection = i2;
      const sectionLength = renderedColumns.length;
      const showLeftBorder = shouldCellShowLeftBorder(pinnedPosition, indexInSection, rootProps.showColumnVerticalBorder, rootProps.pinnedColumnsSectionSeparator);
      const showRightBorder = shouldCellShowRightBorder(pinnedPosition, indexInSection, sectionLength, rootProps.showColumnVerticalBorder, gridHasFiller, rootProps.pinnedColumnsSectionSeparator);
      filters.push((0, import_jsx_runtime197.jsx)(rootProps.slots.headerFilterCell, _extends({
        colIndex: columnIndex,
        height: headerFilterHeight,
        width: colDef.computedWidth,
        colDef,
        hasFocus,
        tabIndex,
        headerFilterMenuRef,
        headerClassName,
        "data-field": colDef.field,
        item,
        pinnedPosition,
        pinnedOffset,
        showLeftBorder,
        showRightBorder
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.headerFilterCell), `${colDef.field}-filter`));
    }
    return otherProps.getFillers(params, filters, 0, true);
  };
  const getColumnFiltersRow = () => {
    if (disableHeaderFiltering) {
      return null;
    }
    return (0, import_jsx_runtime197.jsxs)(GridColumnHeaderRow, {
      ref: headerFiltersRef,
      className: classes2.headerFilterRow,
      role: "row",
      "aria-rowindex": headerGroupingMaxDepth + 2,
      ownerState: rootProps,
      children: [leftRenderContext && getColumnFilters({
        position: PinnedColumnPosition.LEFT,
        renderContext: leftRenderContext,
        maxLastColumn: leftRenderContext.lastColumnIndex
      }), getColumnFilters({
        renderContext,
        maxLastColumn: visibleColumns.length - pinnedColumns.right.length
      }), rightRenderContext && getColumnFilters({
        position: PinnedColumnPosition.RIGHT,
        renderContext: rightRenderContext,
        maxLastColumn: rightRenderContext.lastColumnIndex
      })]
    });
  };
  if (true) getColumnFiltersRow.displayName = "getColumnFiltersRow";
  return _extends({}, otherProps, {
    getColumnFiltersRow
  });
};

// node_modules/@mui/x-data-grid-pro/esm/components/GridColumnHeaders.js
var import_jsx_runtime198 = __toESM(require_jsx_runtime(), 1);
var _excluded74 = ["style", "className", "visibleColumns", "sortColumnLookup", "filterColumnLookup", "columnHeaderTabIndexState", "columnGroupHeaderTabIndexState", "columnHeaderFocus", "columnGroupHeaderFocus", "headerGroupingMaxDepth", "columnMenuState", "columnVisibility", "columnGroupsHeaderStructure", "hasOtherElementInTabSequence"];
var Filler2 = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  flex: 1,
  backgroundColor: vars.header.background.base
});
var GridColumnHeaders4 = forwardRef2(function GridColumnHeaders5(props, ref) {
  const {
    className,
    visibleColumns,
    sortColumnLookup,
    filterColumnLookup,
    columnHeaderTabIndexState,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure,
    hasOtherElementInTabSequence
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded74);
  const {
    getInnerProps,
    getColumnHeadersRow,
    getColumnFiltersRow,
    getColumnGroupHeadersRows
  } = useGridColumnHeadersPro({
    visibleColumns,
    sortColumnLookup,
    filterColumnLookup,
    columnHeaderTabIndexState,
    hasOtherElementInTabSequence,
    columnGroupHeaderTabIndexState,
    columnHeaderFocus,
    columnGroupHeaderFocus,
    headerGroupingMaxDepth,
    columnMenuState,
    columnVisibility,
    columnGroupsHeaderStructure
  });
  return (0, import_jsx_runtime198.jsxs)(GridBaseColumnHeaders, _extends({
    className
  }, other, getInnerProps(), {
    ref,
    children: [getColumnGroupHeadersRows(), getColumnHeadersRow(), getColumnFiltersRow(), (0, import_jsx_runtime198.jsx)(Filler2, {})]
  }));
});
if (true) GridColumnHeaders4.displayName = "GridColumnHeaders";
true ? GridColumnHeaders4.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  columnGroupHeaderFocus: import_prop_types148.default.shape({
    depth: import_prop_types148.default.number.isRequired,
    field: import_prop_types148.default.string.isRequired
  }),
  columnGroupHeaderTabIndexState: import_prop_types148.default.shape({
    depth: import_prop_types148.default.number.isRequired,
    field: import_prop_types148.default.string.isRequired
  }),
  columnGroupsHeaderStructure: import_prop_types148.default.arrayOf(import_prop_types148.default.arrayOf(import_prop_types148.default.shape({
    columnFields: import_prop_types148.default.arrayOf(import_prop_types148.default.string).isRequired,
    groupId: import_prop_types148.default.string
  }))).isRequired,
  columnHeaderFocus: import_prop_types148.default.shape({
    field: import_prop_types148.default.string.isRequired
  }),
  columnHeaderTabIndexState: import_prop_types148.default.shape({
    field: import_prop_types148.default.string.isRequired
  }),
  columnMenuState: import_prop_types148.default.shape({
    field: import_prop_types148.default.string,
    open: import_prop_types148.default.bool.isRequired
  }).isRequired,
  columnVisibility: import_prop_types148.default.object.isRequired,
  filterColumnLookup: import_prop_types148.default.object.isRequired,
  hasOtherElementInTabSequence: import_prop_types148.default.bool.isRequired,
  headerGroupingMaxDepth: import_prop_types148.default.number.isRequired,
  sortColumnLookup: import_prop_types148.default.object.isRequired,
  visibleColumns: import_prop_types148.default.arrayOf(import_prop_types148.default.object).isRequired
} : void 0;

// node_modules/@mui/x-data-grid-pro/esm/components/GridProColumnMenu.js
var import_jsx_runtime199 = __toESM(require_jsx_runtime(), 1);
var GRID_COLUMN_MENU_SLOTS_PRO = _extends({}, GRID_COLUMN_MENU_SLOTS, {
  columnMenuPinningItem: GridColumnMenuPinningItem
});
var GRID_COLUMN_MENU_SLOT_PROPS_PRO = _extends({}, GRID_COLUMN_MENU_SLOT_PROPS, {
  columnMenuPinningItem: {
    displayOrder: 15
  }
});
var GridProColumnMenu = forwardRef2(function GridProColumnMenu2(props, ref) {
  return (0, import_jsx_runtime199.jsx)(GridGenericColumnMenu, _extends({}, props, {
    defaultSlots: GRID_COLUMN_MENU_SLOTS_PRO,
    defaultSlotProps: GRID_COLUMN_MENU_SLOT_PROPS_PRO,
    ref
  }));
});
if (true) GridProColumnMenu.displayName = "GridProColumnMenu";
GridProColumnMenu.defaultSlots = GRID_COLUMN_MENU_SLOTS_PRO;
GridProColumnMenu.defaultSlotProps = GRID_COLUMN_MENU_SLOT_PROPS_PRO;

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanels.js
var React278 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/utils/useGridPrivateApiContext.js
var useGridPrivateApiContext2 = useGridPrivateApiContext;

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanel.js
var React277 = __toESM(require_react(), 1);

// node_modules/@mui/x-internals/esm/useResizeObserver/useResizeObserver.js
var React276 = __toESM(require_react(), 1);
var isDevEnvironment = true;
var noop6 = () => {
};
function useResizeObserver(ref, fn2, enabled) {
  const fnRef = React276.useRef(null);
  fnRef.current = fn2;
  useEnhancedEffect_default(() => {
    if (enabled === false || typeof ResizeObserver === "undefined") {
      return noop6;
    }
    let frameID = 0;
    const target = ref.current;
    const observer = new ResizeObserver((entries) => {
      if (isDevEnvironment) {
        frameID = requestAnimationFrame(() => {
          fnRef.current(entries);
        });
      } else {
        fnRef.current(entries);
      }
    });
    if (target) {
      observer.observe(target);
    }
    return () => {
      if (frameID) {
        cancelAnimationFrame(frameID);
      }
      observer.disconnect();
    };
  }, [ref, enabled]);
}

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanel.js
var import_jsx_runtime200 = __toESM(require_jsx_runtime(), 1);
var DetailPanel = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "DetailPanel"
})({
  width: "calc(var(--DataGrid-rowWidth) - var(--DataGrid-hasScrollY) * var(--DataGrid-scrollbarSize))",
  backgroundColor: vars.colors.background.base,
  overflow: "auto"
});
function GridDetailPanel(props) {
  const {
    rowId,
    height,
    className,
    children: children2
  } = props;
  const apiRef = useGridPrivateApiContext2();
  const ref = React277.useRef(null);
  const rootProps = useGridRootProps2();
  const ownerState = rootProps;
  const hasAutoHeight = height === "auto";
  const rowNode = gridRowNodeSelector(apiRef, rowId);
  React277.useLayoutEffect(() => {
    if (hasAutoHeight && typeof ResizeObserver === "undefined") {
      apiRef.current.storeDetailPanelHeight(rowId, ref.current.clientHeight);
    }
  }, [apiRef, hasAutoHeight, rowId]);
  useResizeObserver(ref, (entries) => {
    const [entry] = entries;
    const observedHeight = entry.borderBoxSize && entry.borderBoxSize.length > 0 ? entry.borderBoxSize[0].blockSize : entry.contentRect.height;
    apiRef.current.storeDetailPanelHeight(rowId, observedHeight);
  }, hasAutoHeight);
  if ((rowNode == null ? void 0 : rowNode.type) === "skeletonRow") {
    return null;
  }
  return (0, import_jsx_runtime200.jsx)(DetailPanel, {
    ref,
    ownerState,
    role: "presentation",
    style: {
      height
    },
    className,
    children: children2
  });
}

// node_modules/@mui/x-data-grid-pro/esm/components/GridDetailPanels.js
var import_jsx_runtime201 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses103 = () => {
  const slots = {
    detailPanel: ["detailPanel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, {});
};
function GridDetailPanels2(props) {
  const rootProps = useGridRootProps2();
  if (!rootProps.getDetailPanelContent) {
    return null;
  }
  return React278.createElement(GridDetailPanelsImpl, props);
}
function GridDetailPanelsImpl({
  virtualScroller
}) {
  const apiRef = useGridPrivateApiContext2();
  const classes2 = useUtilityClasses103();
  const {
    setPanels
  } = virtualScroller;
  const expandedRowIds = useGridSelector(apiRef, gridDetailPanelExpandedRowIdsSelector);
  const detailPanelsContent = useGridSelector(apiRef, gridDetailPanelExpandedRowsContentCacheSelector);
  const detailPanelsHeights = useGridSelector(apiRef, gridDetailPanelRawHeightCacheSelector);
  const getDetailPanel = React278.useCallback((rowId) => {
    const content = detailPanelsContent[rowId];
    const rowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(rowId);
    const exists = rowIndex !== void 0;
    if (!React278.isValidElement(content) || !exists) {
      return null;
    }
    const heightCache = detailPanelsHeights[rowId];
    const height = heightCache.autoHeight ? "auto" : heightCache.height;
    return (0, import_jsx_runtime201.jsx)(GridDetailPanel, {
      rowId,
      height,
      className: classes2.detailPanel,
      children: content
    }, `panel-${rowId}`);
  }, [apiRef, classes2.detailPanel, detailPanelsHeights, detailPanelsContent]);
  React278.useEffect(() => {
    const map = /* @__PURE__ */ new Map();
    for (const rowId of expandedRowIds) {
      map.set(rowId, getDetailPanel(rowId));
    }
    setPanels(map);
  }, [expandedRowIds, setPanels, getDetailPanel]);
  return null;
}

// node_modules/@mui/x-data-grid-pro/esm/components/GridPinnedRows.js
var React279 = __toESM(require_react(), 1);
var import_jsx_runtime202 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses104 = () => {
  const slots = {
    root: ["pinnedRows"]
  };
  return composeClasses(slots, getDataGridUtilityClass, {});
};
function GridPinnedRows2({
  position,
  virtualScroller
}) {
  const classes2 = useUtilityClasses104();
  const apiRef = useGridPrivateApiContext();
  const pinnedRowsData = useGridSelector(apiRef, gridPinnedRowsSelector);
  const rows = pinnedRowsData[position];
  const {
    getRows
  } = virtualScroller;
  const pinnedRenderContext = React279.useMemo(() => ({
    firstRowIndex: 0,
    lastRowIndex: rows.length,
    firstColumnIndex: -1,
    lastColumnIndex: -1
  }), [rows]);
  if (rows.length === 0) {
    return null;
  }
  const pinnedRows = getRows({
    position,
    rows,
    renderContext: pinnedRenderContext
  }, gridRowTreeSelector(apiRef));
  return (0, import_jsx_runtime202.jsx)("div", {
    className: clsx_default(classes2.root, gridClasses[`pinnedRows--${position}`]),
    role: "presentation",
    children: pinnedRows
  });
}

// node_modules/@mui/x-data-grid-pro/esm/material/index.js
var iconSlots2 = {
  columnMenuPinRightIcon: GridPushPinRightIcon,
  columnMenuPinLeftIcon: GridPushPinLeftIcon
};
var materialSlots2 = _extends({}, iconSlots2);
var material_default2 = materialSlots2;

// node_modules/@mui/x-data-grid-pro/esm/constants/dataGridProDefaultSlotsComponents.js
var DATA_GRID_PRO_DEFAULT_SLOTS_COMPONENTS = _extends({}, DATA_GRID_DEFAULT_SLOTS_COMPONENTS, material_default2, {
  columnMenu: GridProColumnMenu,
  columnHeaders: GridColumnHeaders4,
  detailPanels: GridDetailPanels2,
  headerFilterCell: Memoized4,
  headerFilterMenu: GridHeaderFilterMenu,
  pinnedRows: GridPinnedRows2
});

// node_modules/@mui/x-data-grid-pro/esm/hooks/utils/useGridAriaAttributes.js
var useGridAriaAttributesPro = () => {
  const ariaAttributesCommunity = useGridAriaAttributes();
  const rootProps = useGridRootProps2();
  const ariaAttributesPro = rootProps.treeData ? {
    role: "treegrid"
  } : {};
  return _extends({}, ariaAttributesCommunity, ariaAttributesPro);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rows/useGridRowAriaAttributes.js
var React280 = __toESM(require_react(), 1);
var useGridRowAriaAttributesPro = (addTreeDataAttributes) => {
  const apiRef = useGridPrivateApiContext2();
  const props = useGridRootProps2();
  const getRowAriaAttributesCommunity = useGridRowAriaAttributes();
  const filteredTopLevelRowCount = useGridSelector(apiRef, gridFilteredTopLevelRowCountSelector);
  const filteredChildrenCountLookup = useGridSelector(apiRef, gridFilteredChildrenCountLookupSelector);
  const sortedVisibleRowPositionsLookup = useGridSelector(apiRef, gridExpandedSortedRowTreeLevelPositionLookupSelector);
  return React280.useCallback((rowNode, index) => {
    const ariaAttributes = getRowAriaAttributesCommunity(rowNode, index);
    if (!rowNode || !(props.treeData || addTreeDataAttributes)) {
      return ariaAttributes;
    }
    if (rowNode.type === "footer" || rowNode.type === "pinnedRow") {
      return ariaAttributes;
    }
    ariaAttributes["aria-level"] = rowNode.depth + 1;
    const filteredChildrenCount = filteredChildrenCountLookup[rowNode.id] ?? 0;
    if (rowNode.type === "group" && filteredChildrenCount > 0) {
      ariaAttributes["aria-expanded"] = Boolean(rowNode.childrenExpanded);
    }
    if (rowNode.parent !== null) {
      ariaAttributes["aria-setsize"] = rowNode.parent === GRID_ROOT_GROUP_ID ? filteredTopLevelRowCount : filteredChildrenCountLookup[rowNode.parent];
      ariaAttributes["aria-posinset"] = sortedVisibleRowPositionsLookup[rowNode.id];
    }
    return ariaAttributes;
  }, [props.treeData, addTreeDataAttributes, filteredTopLevelRowCount, filteredChildrenCountLookup, sortedVisibleRowPositionsLookup, getRowAriaAttributesCommunity]);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/columnPinning/useGridColumnPinning.js
var React281 = __toESM(require_react(), 1);
var columnPinningStateInitializer = (state, props, apiRef) => {
  var _a;
  apiRef.current.caches.columnPinning = {
    orderedFieldsBeforePinningColumns: null
  };
  let model;
  if (props.pinnedColumns) {
    model = props.pinnedColumns;
  } else if ((_a = props.initialState) == null ? void 0 : _a.pinnedColumns) {
    model = props.initialState.pinnedColumns;
  } else {
    model = {};
  }
  return _extends({}, state, {
    pinnedColumns: model
  });
};
var useGridColumnPinning = (apiRef, props) => {
  var _a;
  const pinnedColumns = useGridSelector(apiRef, gridPinnedColumnsSelector);
  const calculateScrollLeft = React281.useCallback((initialValue, params) => {
    const visiblePinnedColumns = gridVisiblePinnedColumnDefinitionsSelector(apiRef);
    if (!params.colIndex || visiblePinnedColumns.left.length === 0 && visiblePinnedColumns.right.length === 0) {
      return initialValue;
    }
    const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
    const columnsTotalWidth = gridColumnsTotalWidthSelector(apiRef);
    const columnPositions = gridColumnPositionsSelector(apiRef);
    const clientWidth = apiRef.current.virtualScrollerRef.current.clientWidth;
    const scrollLeft = Math.abs(apiRef.current.virtualScrollerRef.current.scrollLeft);
    const offsetWidth = visibleColumns[params.colIndex].computedWidth;
    const offsetLeft = columnPositions[params.colIndex];
    const leftPinnedColumnsWidth = columnPositions[visiblePinnedColumns.left.length];
    const rightPinnedColumnsWidth = columnsTotalWidth - columnPositions[columnPositions.length - visiblePinnedColumns.right.length];
    const elementBottom = offsetLeft + offsetWidth;
    if (elementBottom - (clientWidth - rightPinnedColumnsWidth) > scrollLeft) {
      const left2 = elementBottom - (clientWidth - rightPinnedColumnsWidth);
      return _extends({}, initialValue, {
        left: left2
      });
    }
    if (offsetLeft < scrollLeft + leftPinnedColumnsWidth) {
      const left2 = offsetLeft - leftPinnedColumnsWidth;
      return _extends({}, initialValue, {
        left: left2
      });
    }
    return initialValue;
  }, [apiRef]);
  const addColumnMenuItems = React281.useCallback((columnMenuItems, colDef) => {
    if (props.disableColumnPinning) {
      return columnMenuItems;
    }
    if (colDef.pinnable === false) {
      return columnMenuItems;
    }
    return [...columnMenuItems, "columnMenuPinningItem"];
  }, [props.disableColumnPinning]);
  const checkIfCanBeReordered = React281.useCallback((initialValue, {
    targetIndex
  }) => {
    const visiblePinnedColumns = gridVisiblePinnedColumnDefinitionsSelector(apiRef);
    if (visiblePinnedColumns.left.length === 0 && visiblePinnedColumns.right.length === 0) {
      return initialValue;
    }
    if (visiblePinnedColumns.left.length > 0 && targetIndex < visiblePinnedColumns.left.length) {
      return false;
    }
    if (visiblePinnedColumns.right.length > 0) {
      const visibleColumns = gridVisibleColumnDefinitionsSelector(apiRef);
      const firstRightPinnedColumnIndex = visibleColumns.length - visiblePinnedColumns.right.length;
      return targetIndex >= firstRightPinnedColumnIndex ? false : initialValue;
    }
    return initialValue;
  }, [apiRef]);
  const stateExportPreProcessing = React281.useCallback((prevState, context) => {
    var _a2;
    const pinnedColumnsToExport = gridPinnedColumnsSelector(apiRef);
    const shouldExportPinnedColumns = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the model is controlled
      props.pinnedColumns != null || // Always export if the model has been initialized
      ((_a2 = props.initialState) == null ? void 0 : _a2.pinnedColumns) != null || // Export if the model is not empty
      (pinnedColumnsToExport.left ?? []).length > 0 || (pinnedColumnsToExport.right ?? []).length > 0
    );
    if (!shouldExportPinnedColumns) {
      return prevState;
    }
    return _extends({}, prevState, {
      pinnedColumns: pinnedColumnsToExport
    });
  }, [apiRef, props.pinnedColumns, (_a = props.initialState) == null ? void 0 : _a.pinnedColumns]);
  const stateRestorePreProcessing = React281.useCallback((params, context) => {
    const newPinnedColumns = context.stateToRestore.pinnedColumns;
    if (newPinnedColumns != null) {
      apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns = null;
      setState(apiRef, newPinnedColumns);
      return _extends({}, params, {
        callbacks: [...params.callbacks, () => apiRef.current.requestPipeProcessorsApplication("hydrateColumns")]
      });
    }
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "scrollToIndexes", calculateScrollLeft);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuItems);
  useGridRegisterPipeProcessor(apiRef, "canBeReordered", checkIfCanBeReordered);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  apiRef.current.registerControlState({
    stateId: "pinnedColumns",
    propModel: props.pinnedColumns,
    propOnChange: props.onPinnedColumnsChange,
    stateSelector: gridPinnedColumnsSelector,
    changeEvent: "pinnedColumnsChange"
  });
  const pinColumn = React281.useCallback((field, side) => {
    if (apiRef.current.isColumnPinned(field) === side) {
      return;
    }
    const otherSide = side === GridPinnedColumnPosition.RIGHT ? GridPinnedColumnPosition.LEFT : GridPinnedColumnPosition.RIGHT;
    const newPinnedColumns = {
      [side]: [...pinnedColumns[side] || [], field],
      [otherSide]: (pinnedColumns[otherSide] || []).filter((column) => column !== field)
    };
    apiRef.current.setPinnedColumns(newPinnedColumns);
  }, [apiRef, pinnedColumns]);
  const unpinColumn = React281.useCallback((field) => {
    apiRef.current.setPinnedColumns({
      left: (pinnedColumns.left || []).filter((column) => column !== field),
      right: (pinnedColumns.right || []).filter((column) => column !== field)
    });
  }, [apiRef, pinnedColumns.left, pinnedColumns.right]);
  const getPinnedColumns = React281.useCallback(() => {
    return gridPinnedColumnsSelector(apiRef);
  }, [apiRef]);
  const setPinnedColumns = React281.useCallback((newPinnedColumns) => {
    setState(apiRef, newPinnedColumns);
    apiRef.current.requestPipeProcessorsApplication("hydrateColumns");
  }, [apiRef]);
  const isColumnPinned = React281.useCallback((field) => {
    const leftPinnedColumns = pinnedColumns.left || [];
    if (leftPinnedColumns.includes(field)) {
      return GridPinnedColumnPosition.LEFT;
    }
    const rightPinnedColumns = pinnedColumns.right || [];
    if (rightPinnedColumns.includes(field)) {
      return GridPinnedColumnPosition.RIGHT;
    }
    return false;
  }, [pinnedColumns.left, pinnedColumns.right]);
  const columnPinningApi = {
    pinColumn,
    unpinColumn,
    getPinnedColumns,
    setPinnedColumns,
    isColumnPinned
  };
  useGridApiMethod(apiRef, columnPinningApi, "public");
  const handleColumnOrderChange = (params) => {
    if (!apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns) {
      return;
    }
    const {
      column,
      targetIndex,
      oldIndex
    } = params;
    const delta = targetIndex > oldIndex ? 1 : -1;
    const latestColumnFields = gridColumnFieldsSelector(apiRef);
    const siblingField = latestColumnFields[targetIndex - delta];
    const newOrderedFieldsBeforePinningColumns = [...apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns];
    let i2 = newOrderedFieldsBeforePinningColumns.findIndex((currentColumn) => currentColumn === column.field);
    let j = i2 + delta;
    const stop = newOrderedFieldsBeforePinningColumns.findIndex((currentColumn) => currentColumn === siblingField);
    while (delta > 0 ? i2 < stop : i2 > stop) {
      while (apiRef.current.isColumnPinned(newOrderedFieldsBeforePinningColumns[j])) {
        j += delta;
      }
      const temp = newOrderedFieldsBeforePinningColumns[i2];
      newOrderedFieldsBeforePinningColumns[i2] = newOrderedFieldsBeforePinningColumns[j];
      newOrderedFieldsBeforePinningColumns[j] = temp;
      i2 = j;
      j = i2 + delta;
    }
    apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns = newOrderedFieldsBeforePinningColumns;
  };
  useGridEvent(apiRef, "columnOrderChange", handleColumnOrderChange);
  React281.useEffect(() => {
    if (props.pinnedColumns) {
      apiRef.current.setPinnedColumns(props.pinnedColumns);
    }
  }, [apiRef, props.pinnedColumns]);
};
function setState(apiRef, model) {
  apiRef.current.setState((state) => _extends({}, state, {
    pinnedColumns: model
  }));
}

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/columnPinning/useGridColumnPinningPreProcessors.js
var React282 = __toESM(require_react(), 1);
var useGridColumnPinningPreProcessors = (apiRef, props) => {
  const {
    disableColumnPinning
  } = props;
  const prevAllPinnedColumns = React282.useRef([]);
  const reorderPinnedColumns = React282.useCallback((columnsState) => {
    if (columnsState.orderedFields.length === 0 || disableColumnPinning) {
      return columnsState;
    }
    const savedState = apiRef.current.state;
    apiRef.current.state = _extends({}, savedState, {
      columns: columnsState
    });
    const pinnedColumns = gridExistingPinnedColumnSelector(apiRef);
    apiRef.current.state = savedState;
    const leftPinnedColumns = pinnedColumns.left;
    const rightPinnedColumns = pinnedColumns.right;
    let newOrderedFields;
    const allPinnedColumns = [...leftPinnedColumns, ...rightPinnedColumns];
    const {
      orderedFieldsBeforePinningColumns
    } = apiRef.current.caches.columnPinning;
    if (orderedFieldsBeforePinningColumns) {
      newOrderedFields = new Array(columnsState.orderedFields.length).fill(null);
      const newOrderedFieldsBeforePinningColumns = [...newOrderedFields];
      const remainingFields = [...columnsState.orderedFields];
      prevAllPinnedColumns.current.forEach((field) => {
        if (!allPinnedColumns.includes(field) && columnsState.lookup[field]) {
          const index = orderedFieldsBeforePinningColumns.indexOf(field);
          newOrderedFields[index] = field;
          newOrderedFieldsBeforePinningColumns[index] = field;
          remainingFields.splice(remainingFields.indexOf(field), 1);
        }
      });
      allPinnedColumns.forEach((field) => {
        let index = orderedFieldsBeforePinningColumns.indexOf(field);
        if (index === -1 || index >= newOrderedFieldsBeforePinningColumns.length) {
          index = columnsState.orderedFields.indexOf(field);
        }
        if (newOrderedFieldsBeforePinningColumns[index] !== null) {
          index = 0;
          while (newOrderedFieldsBeforePinningColumns[index] !== null) {
            index += 1;
          }
        }
        newOrderedFields[index] = field;
        newOrderedFieldsBeforePinningColumns[index] = field;
        remainingFields.splice(remainingFields.indexOf(field), 1);
      });
      let i2 = 0;
      remainingFields.forEach((field) => {
        while (newOrderedFieldsBeforePinningColumns[i2] !== null) {
          i2 += 1;
        }
        newOrderedFieldsBeforePinningColumns[i2] = field;
        newOrderedFields[i2] = field;
      });
      apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns = newOrderedFieldsBeforePinningColumns;
    } else {
      if (allPinnedColumns.length === 0) {
        prevAllPinnedColumns.current = allPinnedColumns;
        return columnsState;
      }
      newOrderedFields = [...columnsState.orderedFields];
      apiRef.current.caches.columnPinning.orderedFieldsBeforePinningColumns = [...columnsState.orderedFields];
    }
    prevAllPinnedColumns.current = allPinnedColumns;
    const centerColumns = newOrderedFields.filter((field) => {
      return !leftPinnedColumns.includes(field) && !rightPinnedColumns.includes(field);
    });
    return _extends({}, columnsState, {
      orderedFields: [...leftPinnedColumns, ...centerColumns, ...rightPinnedColumns]
    });
  }, [apiRef, disableColumnPinning]);
  useGridRegisterPipeProcessor(apiRef, "hydrateColumns", reorderPinnedColumns);
  const isColumnPinned = React282.useCallback((initialValue, field) => apiRef.current.isColumnPinned(field), [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "isColumnPinned", isColumnPinned);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/serverSideTreeData/useGridDataSourceTreeDataPreProcessors.js
var React284 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/serverSideTreeData/utils.js
function skipFiltering(rowTree) {
  const filteredChildrenCountLookup = {};
  const nodes = Object.values(rowTree);
  for (let i2 = 0; i2 < nodes.length; i2 += 1) {
    const node = nodes[i2];
    filteredChildrenCountLookup[node.id] = node.serverChildrenCount ?? 0;
  }
  return {
    filteredRowsLookup: defaultGridFilterLookup.filteredRowsLookup,
    filteredChildrenCountLookup,
    filteredDescendantCountLookup: defaultGridFilterLookup.filteredDescendantCountLookup
  };
}
function skipSorting(rowTree) {
  return getTreeNodeDescendants(rowTree, GRID_ROOT_GROUP_ID, false);
}
function getParentPath(rowId, treeCreationParams) {
  var _a;
  if (treeCreationParams.updates.type !== "full" || !((_a = treeCreationParams.previousTree) == null ? void 0 : _a[rowId]) || treeCreationParams.previousTree[rowId].depth < 1) {
    return [];
  }
  const node = treeCreationParams.previousTree[rowId];
  const parentId = node.parent;
  if (parentId == null) {
    return [];
  }
  const parentNode = treeCreationParams.previousTree[parentId];
  if (parentNode && "path" in parentNode) {
    return parentNode.path || [];
  }
  return [];
}

// node_modules/@mui/x-data-grid-pro/esm/components/GridDataSourceTreeDataGroupingCell.js
var React283 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/gridDataSourceSelector.js
var gridDataSourceStateSelector = createRootSelector((state) => state.dataSource);
var gridDataSourceLoadingSelector = createSelector3(gridDataSourceStateSelector, (dataSource) => dataSource.loading);
var gridDataSourceLoadingIdSelector = createSelector3(gridDataSourceStateSelector, (dataSource, id) => dataSource.loading[id] ?? false);
var gridDataSourceErrorsSelector = createSelector3(gridDataSourceStateSelector, (dataSource) => dataSource.errors);
var gridDataSourceErrorSelector = createSelector3(gridDataSourceStateSelector, (dataSource, id) => dataSource.errors[id]);

// node_modules/@mui/x-data-grid-pro/esm/components/GridDataSourceTreeDataGroupingCell.js
var import_jsx_runtime203 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses105 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["treeDataGroupingCell"],
    toggle: ["treeDataGroupingCellToggle"],
    loadingContainer: ["treeDataGroupingCellLoadingContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridTreeDataGroupingCellIcon(props) {
  var _a;
  const apiRef = useGridPrivateApiContext2();
  const rootProps = useGridRootProps2();
  const classes2 = useUtilityClasses105(rootProps);
  const {
    rowNode,
    id,
    field,
    descendantCount
  } = props;
  const isDataLoading = useGridSelector(apiRef, gridDataSourceLoadingIdSelector, id);
  const error = useGridSelector(apiRef, gridDataSourceErrorSelector, id);
  const handleClick = (event) => {
    if (!rowNode.childrenExpanded) {
      apiRef.current.dataSource.fetchRows(id);
    } else {
      apiRef.current.setRowChildrenExpansion(id, false);
      apiRef.current.removeChildrenRows(id);
    }
    apiRef.current.setCellFocus(id, field);
    event.stopPropagation();
  };
  const Icon = rowNode.childrenExpanded ? rootProps.slots.treeDataCollapseIcon : rootProps.slots.treeDataExpandIcon;
  if (isDataLoading) {
    return (0, import_jsx_runtime203.jsx)("div", {
      className: classes2.loadingContainer,
      children: (0, import_jsx_runtime203.jsx)(rootProps.slots.baseCircularProgress, {
        size: "1rem",
        color: "inherit"
      })
    });
  }
  return descendantCount === -1 || descendantCount > 0 ? (0, import_jsx_runtime203.jsx)(rootProps.slots.baseIconButton, _extends({
    size: "small",
    onClick: handleClick,
    tabIndex: -1,
    "aria-label": rowNode.childrenExpanded ? apiRef.current.getLocaleText("treeDataCollapse") : apiRef.current.getLocaleText("treeDataExpand")
  }, (_a = rootProps == null ? void 0 : rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    children: (0, import_jsx_runtime203.jsx)(rootProps.slots.baseTooltip, {
      title: (error == null ? void 0 : error.message) ?? null,
      children: (0, import_jsx_runtime203.jsx)(rootProps.slots.baseBadge, {
        variant: "dot",
        color: "error",
        invisible: !error,
        children: (0, import_jsx_runtime203.jsx)(Icon, {
          fontSize: "inherit"
        })
      })
    })
  })) : null;
}
function GridDataSourceTreeDataGroupingCell(props) {
  var _a, _b;
  const {
    id,
    field,
    formattedValue,
    rowNode,
    hideDescendantCount,
    offsetMultiplier = 2
  } = props;
  const rootProps = useGridRootProps2();
  const apiRef = useGridPrivateApiContext2();
  const row = useGridSelector(apiRef, gridRowSelector, id);
  const classes2 = useUtilityClasses105(rootProps);
  let descendantCount = 0;
  if (row) {
    descendantCount = ((_b = (_a = rootProps.dataSource) == null ? void 0 : _a.getChildrenCount) == null ? void 0 : _b.call(_a, row)) ?? 0;
  }
  return (0, import_jsx_runtime203.jsxs)("div", {
    className: classes2.root,
    style: {
      marginLeft: vars.spacing(rowNode.depth * offsetMultiplier)
    },
    children: [(0, import_jsx_runtime203.jsx)("div", {
      className: classes2.toggle,
      children: (0, import_jsx_runtime203.jsx)(GridTreeDataGroupingCellIcon, {
        id,
        field,
        rowNode,
        row,
        descendantCount
      })
    }), (0, import_jsx_runtime203.jsxs)("span", {
      children: [formattedValue === void 0 ? rowNode.groupingKey : formattedValue, !hideDescendantCount && descendantCount > 0 ? ` (${descendantCount})` : ""]
    })]
  });
}

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/serverSideTreeData/useGridDataSourceTreeDataPreProcessors.js
var import_jsx_runtime204 = __toESM(require_jsx_runtime(), 1);
var _excluded75 = ["hideDescendantCount"];
var useGridDataSourceTreeDataPreProcessors = (privateApiRef, props) => {
  const setStrategyAvailability2 = React284.useCallback(() => {
    privateApiRef.current.setStrategyAvailability(GridStrategyGroup.RowTree, TreeDataStrategy.DataSource, props.treeData && props.dataSource ? () => true : () => false);
  }, [privateApiRef, props.treeData, props.dataSource]);
  const getGroupingColDef = React284.useCallback(() => {
    const groupingColDefProp = props.groupingColDef;
    let colDefOverride;
    if (typeof groupingColDefProp === "function") {
      const params = {
        groupingName: TreeDataStrategy.DataSource,
        fields: []
      };
      colDefOverride = groupingColDefProp(params);
    } else {
      colDefOverride = groupingColDefProp;
    }
    const _ref = colDefOverride ?? {}, {
      hideDescendantCount
    } = _ref, colDefOverrideProperties = _objectWithoutPropertiesLoose(_ref, _excluded75);
    const commonProperties = _extends({}, GRID_TREE_DATA_GROUPING_COL_DEF, {
      renderCell: (params) => (0, import_jsx_runtime204.jsx)(GridDataSourceTreeDataGroupingCell, _extends({}, params, {
        hideDescendantCount
      })),
      headerName: privateApiRef.current.getLocaleText("treeDataGroupingHeaderName")
    });
    return _extends({}, commonProperties, colDefOverrideProperties, GRID_TREE_DATA_GROUPING_COL_DEF_FORCED_PROPERTIES);
  }, [privateApiRef, props.groupingColDef]);
  const updateGroupingColumn = React284.useCallback((columnsState) => {
    if (!props.dataSource) {
      return columnsState;
    }
    const groupingColDefField = GRID_TREE_DATA_GROUPING_COL_DEF_FORCED_PROPERTIES.field;
    const shouldHaveGroupingColumn = props.treeData;
    const prevGroupingColumn = columnsState.lookup[groupingColDefField];
    if (shouldHaveGroupingColumn) {
      const newGroupingColumn = getGroupingColDef();
      if (prevGroupingColumn) {
        newGroupingColumn.width = prevGroupingColumn.width;
        newGroupingColumn.flex = prevGroupingColumn.flex;
      }
      columnsState.lookup[groupingColDefField] = newGroupingColumn;
      if (prevGroupingColumn == null) {
        columnsState.orderedFields = [groupingColDefField, ...columnsState.orderedFields];
      }
    } else if (!shouldHaveGroupingColumn && prevGroupingColumn) {
      delete columnsState.lookup[groupingColDefField];
      columnsState.orderedFields = columnsState.orderedFields.filter((field) => field !== groupingColDefField);
    }
    return columnsState;
  }, [props.treeData, props.dataSource, getGroupingColDef]);
  const createRowTreeForTreeData = React284.useCallback((params) => {
    var _a, _b;
    const getGroupKey = (_a = props.dataSource) == null ? void 0 : _a.getGroupKey;
    if (!getGroupKey) {
      throw new Error("MUI X: No `getGroupKey` method provided with the dataSource.");
    }
    const getChildrenCount = (_b = props.dataSource) == null ? void 0 : _b.getChildrenCount;
    if (!getChildrenCount) {
      throw new Error("MUI X: No `getChildrenCount` method provided with the dataSource.");
    }
    const getRowTreeBuilderNode = (rowId) => {
      const parentPath = params.updates.groupKeys ?? getParentPath(rowId, params);
      const count = getChildrenCount(params.dataRowIdToModelLookup[rowId]);
      return {
        id: rowId,
        path: [...parentPath, getGroupKey(params.dataRowIdToModelLookup[rowId])].map((key) => ({
          key,
          field: null
        })),
        serverChildrenCount: count
      };
    };
    const onDuplicatePath = (firstId, secondId, path) => {
      throw new Error(["MUI X: The values returned by `getGroupKey` for all the sibling rows should be unique.", `The rows with id #${firstId} and #${secondId} have the same.`, `Path: ${JSON.stringify(path.map((step) => step.key))}.`].join("\n"));
    };
    if (params.updates.type === "full") {
      return createRowTree({
        previousTree: params.previousTree,
        nodes: params.updates.rows.map(getRowTreeBuilderNode),
        defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
        isGroupExpandedByDefault: props.isGroupExpandedByDefault,
        groupingName: TreeDataStrategy.DataSource,
        onDuplicatePath
      });
    }
    return updateRowTree({
      nodes: {
        inserted: params.updates.actions.insert.map(getRowTreeBuilderNode),
        modified: params.updates.actions.modify.map(getRowTreeBuilderNode),
        removed: params.updates.actions.remove
      },
      previousTree: params.previousTree,
      previousGroupsToFetch: params.previousGroupsToFetch,
      previousTreeDepth: params.previousTreeDepths,
      defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
      isGroupExpandedByDefault: props.isGroupExpandedByDefault,
      groupingName: TreeDataStrategy.DataSource
    });
  }, [props.dataSource, props.defaultGroupingExpansionDepth, props.isGroupExpandedByDefault]);
  const filterRows = React284.useCallback(() => {
    const rowTree = gridRowTreeSelector(privateApiRef);
    return skipFiltering(rowTree);
  }, [privateApiRef]);
  const sortRows = React284.useCallback(() => {
    const rowTree = gridRowTreeSelector(privateApiRef);
    return skipSorting(rowTree);
  }, [privateApiRef]);
  useGridRegisterPipeProcessor(privateApiRef, "hydrateColumns", updateGroupingColumn);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.DataSource, "rowTreeCreation", createRowTreeForTreeData);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.DataSource, "filtering", filterRows);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.DataSource, "sorting", sortRows);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.DataSource, "visibleRowsLookupCreation", getVisibleRowsLookup2);
  useFirstRender(() => {
    setStrategyAvailability2();
  });
  const isFirstRender = React284.useRef(true);
  React284.useEffect(() => {
    if (!isFirstRender.current) {
      setStrategyAvailability2();
    } else {
      isFirstRender.current = false;
    }
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/detailPanel/useGridDetailPanel.js
var React285 = __toESM(require_react(), 1);
var emptySet = /* @__PURE__ */ new Set();
var detailPanelStateInitializer = (state, props) => {
  var _a, _b;
  return _extends({}, state, {
    detailPanel: {
      heightCache: {},
      expandedRowIds: props.detailPanelExpandedRowIds ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.detailPanel) == null ? void 0 : _b.expandedRowIds) ?? emptySet
    }
  });
};
function cacheContentAndHeight(apiRef, getDetailPanelContent, getDetailPanelHeight, previousHeightCache) {
  var _a;
  if (typeof getDetailPanelContent !== "function") {
    return {};
  }
  const rowIds = gridDataRowIdsSelector(apiRef);
  const contentCache = {};
  const heightCache = {};
  for (let i2 = 0; i2 < rowIds.length; i2 += 1) {
    const id = rowIds[i2];
    const params = apiRef.current.getRowParams(id);
    const content = getDetailPanelContent(params);
    contentCache[id] = content;
    if (content == null) {
      continue;
    }
    const height = getDetailPanelHeight(params);
    const autoHeight = height === "auto";
    heightCache[id] = {
      autoHeight,
      height: autoHeight ? (_a = previousHeightCache[id]) == null ? void 0 : _a.height : height
    };
  }
  return {
    contentCache,
    heightCache
  };
}
var useGridDetailPanel = (apiRef, props) => {
  const handleCellClick = React285.useCallback((params, event) => {
    if (params.field !== GRID_DETAIL_PANEL_TOGGLE_FIELD || props.getDetailPanelContent == null) {
      return;
    }
    const contentCache = gridDetailPanelExpandedRowsContentCacheSelector(apiRef);
    const content = contentCache[params.id];
    if (!React285.isValidElement(content)) {
      return;
    }
    if (event.target === event.currentTarget) {
      return;
    }
    apiRef.current.toggleDetailPanel(params.id);
  }, [apiRef, props.getDetailPanelContent]);
  const handleCellKeyDown = React285.useCallback((params, event) => {
    if (props.getDetailPanelContent == null) {
      return;
    }
    if (params.field === GRID_DETAIL_PANEL_TOGGLE_FIELD && event.key === " ") {
      apiRef.current.toggleDetailPanel(params.id);
    }
  }, [apiRef, props.getDetailPanelContent]);
  useGridEvent(apiRef, "cellClick", handleCellClick);
  useGridEvent(apiRef, "cellKeyDown", handleCellKeyDown);
  apiRef.current.registerControlState({
    stateId: "detailPanels",
    propModel: props.detailPanelExpandedRowIds,
    propOnChange: props.onDetailPanelExpandedRowIdsChange,
    stateSelector: gridDetailPanelExpandedRowIdsSelector,
    changeEvent: "detailPanelsExpandedRowIdsChange"
  });
  const toggleDetailPanel = React285.useCallback((id) => {
    if (props.getDetailPanelContent == null) {
      return;
    }
    const contentCache = gridDetailPanelExpandedRowsContentCacheSelector(apiRef);
    const content = contentCache[id];
    if (!React285.isValidElement(content)) {
      return;
    }
    const ids = apiRef.current.getExpandedDetailPanels();
    const newIds = new Set(ids);
    if (ids.has(id)) {
      newIds.delete(id);
    } else {
      newIds.add(id);
    }
    apiRef.current.setExpandedDetailPanels(newIds);
  }, [apiRef, props.getDetailPanelContent]);
  const getExpandedDetailPanels = React285.useCallback(() => gridDetailPanelExpandedRowIdsSelector(apiRef), [apiRef]);
  const setExpandedDetailPanels = React285.useCallback((ids) => {
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        detailPanel: _extends({}, state.detailPanel, {
          expandedRowIds: ids
        })
      });
    });
    apiRef.current.requestPipeProcessorsApplication("rowHeight");
  }, [apiRef]);
  const storeDetailPanelHeight = React285.useCallback((id, height) => {
    const heightCache = gridDetailPanelRawHeightCacheSelector(apiRef);
    if (!heightCache[id] || heightCache[id].height === height) {
      return;
    }
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        detailPanel: _extends({}, state.detailPanel, {
          heightCache: _extends({}, heightCache, {
            [id]: _extends({}, heightCache[id], {
              height
            })
          })
        })
      });
    });
    apiRef.current.requestPipeProcessorsApplication("rowHeight");
  }, [apiRef]);
  const detailPanelPubicApi = {
    toggleDetailPanel,
    getExpandedDetailPanels,
    setExpandedDetailPanels
  };
  const detailPanelPrivateApi = {
    storeDetailPanelHeight
  };
  useGridApiMethod(apiRef, detailPanelPubicApi, "public");
  useGridApiMethod(apiRef, detailPanelPrivateApi, "private");
  React285.useEffect(() => {
    if (props.detailPanelExpandedRowIds) {
      const currentModel = gridDetailPanelExpandedRowIdsSelector(apiRef);
      if (currentModel !== props.detailPanelExpandedRowIds) {
        apiRef.current.setExpandedDetailPanels(props.detailPanelExpandedRowIds);
      }
    }
  }, [apiRef, props.detailPanelExpandedRowIds]);
  const updateCaches = React285.useCallback(() => {
    if (!props.getDetailPanelContent) {
      return;
    }
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        detailPanel: _extends({}, state.detailPanel, cacheContentAndHeight(apiRef, props.getDetailPanelContent, props.getDetailPanelHeight, state.detailPanel.heightCache))
      });
    });
  }, [apiRef, props.getDetailPanelContent, props.getDetailPanelHeight]);
  useGridEvent(apiRef, "sortedRowsSet", updateCaches);
  const previousGetDetailPanelContentProp = React285.useRef(void 0);
  const previousGetDetailPanelHeightProp = React285.useRef(void 0);
  const updateCachesIfNeeded = React285.useCallback(() => {
    if (props.getDetailPanelContent === previousGetDetailPanelContentProp.current && props.getDetailPanelHeight === previousGetDetailPanelHeightProp.current || !props.getDetailPanelContent) {
      return;
    }
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        detailPanel: _extends({}, state.detailPanel, cacheContentAndHeight(apiRef, props.getDetailPanelContent, props.getDetailPanelHeight, state.detailPanel.heightCache))
      });
    });
    previousGetDetailPanelContentProp.current = props.getDetailPanelContent;
    previousGetDetailPanelHeightProp.current = props.getDetailPanelHeight;
  }, [apiRef, props.getDetailPanelContent, props.getDetailPanelHeight]);
  const addDetailHeight = React285.useCallback((initialValue, row) => {
    var _a;
    const expandedRowIds = gridDetailPanelExpandedRowIdsSelector(apiRef);
    if (!expandedRowIds || !expandedRowIds.has(row.id)) {
      initialValue.detail = 0;
      return initialValue;
    }
    updateCachesIfNeeded();
    const heightCache = gridDetailPanelRawHeightCacheSelector(apiRef);
    initialValue.detail = ((_a = heightCache[row.id]) == null ? void 0 : _a.height) ?? 0;
    return initialValue;
  }, [apiRef, updateCachesIfNeeded]);
  const enabled = props.getDetailPanelContent !== void 0;
  useGridRegisterPipeProcessor(apiRef, "rowHeight", addDetailHeight, enabled);
  const isFirstRender = React285.useRef(true);
  if (isFirstRender.current) {
    updateCachesIfNeeded();
  }
  React285.useEffect(() => {
    if (!isFirstRender.current) {
      updateCachesIfNeeded();
    }
    isFirstRender.current = false;
  }, [apiRef, updateCachesIfNeeded]);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/detailPanel/useGridDetailPanelPreProcessors.js
var React286 = __toESM(require_react(), 1);
var useGridDetailPanelPreProcessors = (privateApiRef, props) => {
  const addToggleColumn = React286.useCallback((columnsState) => {
    const detailPanelToggleColumn = _extends({}, GRID_DETAIL_PANEL_TOGGLE_COL_DEF, {
      headerName: privateApiRef.current.getLocaleText("detailPanelToggle")
    });
    const shouldHaveToggleColumn = !!props.getDetailPanelContent;
    const hasToggleColumn = columnsState.lookup[GRID_DETAIL_PANEL_TOGGLE_FIELD] != null;
    if (shouldHaveToggleColumn && !hasToggleColumn) {
      columnsState.lookup[GRID_DETAIL_PANEL_TOGGLE_FIELD] = detailPanelToggleColumn;
      columnsState.orderedFields = [GRID_DETAIL_PANEL_TOGGLE_FIELD, ...columnsState.orderedFields];
    } else if (!shouldHaveToggleColumn && hasToggleColumn) {
      delete columnsState.lookup[GRID_DETAIL_PANEL_TOGGLE_FIELD];
      columnsState.orderedFields = columnsState.orderedFields.filter((field) => field !== GRID_DETAIL_PANEL_TOGGLE_FIELD);
    } else if (shouldHaveToggleColumn && hasToggleColumn) {
      columnsState.lookup[GRID_DETAIL_PANEL_TOGGLE_FIELD] = _extends({}, detailPanelToggleColumn, columnsState.lookup[GRID_DETAIL_PANEL_TOGGLE_FIELD]);
      if (!props.columns.some((col) => col.field === GRID_DETAIL_PANEL_TOGGLE_FIELD)) {
        columnsState.orderedFields = [GRID_DETAIL_PANEL_TOGGLE_FIELD, ...columnsState.orderedFields.filter((field) => field !== GRID_DETAIL_PANEL_TOGGLE_FIELD)];
      }
    }
    return columnsState;
  }, [privateApiRef, props.columns, props.getDetailPanelContent]);
  const addExpandedClassToRow = React286.useCallback((classes2, id) => {
    if (props.getDetailPanelContent == null) {
      return classes2;
    }
    const expandedRowIds = gridDetailPanelExpandedRowIdsSelector(privateApiRef);
    if (!expandedRowIds.has(id)) {
      return classes2;
    }
    return [...classes2, gridClasses["row--detailPanelExpanded"]];
  }, [privateApiRef, props.getDetailPanelContent]);
  useGridRegisterPipeProcessor(privateApiRef, "hydrateColumns", addToggleColumn);
  useGridRegisterPipeProcessor(privateApiRef, "rowClassName", addExpandedClassToRow);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/useGridRowReorder.js
var React287 = __toESM(require_react(), 1);
var EMPTY_REORDER_STATE = {
  previousTargetId: null,
  dragDirection: null,
  previousDropPosition: null
};
var TIMEOUT_CLEAR_BUFFER_PX = 5;
var EMPTY_TIMEOUT_INFO = {
  rowId: null
};
var useUtilityClasses106 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    rowDragging: ["row--dragging"],
    rowBeingDragged: ["row--beingDragged"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var rowReorderStateInitializer = (state) => _extends({}, state, {
  rowReorder: {
    isActive: false
  }
});
var useGridRowReorder = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridRowReorder");
  const sortModel = useGridSelector(apiRef, gridSortModelSelector);
  const dragRowNode = React287.useRef(null);
  const originRowIndex = React287.useRef(null);
  const removeDnDStylesTimeout = React287.useRef(void 0);
  const ownerState = {
    classes: props.classes
  };
  const classes2 = useUtilityClasses106(ownerState);
  const [dragRowId, setDragRowId] = React287.useState("");
  const timeoutInfoRef = React287.useRef(EMPTY_TIMEOUT_INFO);
  const timeout2 = useTimeout();
  const previousReorderState = React287.useRef(EMPTY_REORDER_STATE);
  const dropTarget = React287.useRef({
    targetRowId: null,
    targetRowIndex: null,
    dropPosition: null
  });
  React287.useEffect(() => {
    return () => {
      clearTimeout(removeDnDStylesTimeout.current);
    };
  }, []);
  const isRowReorderDisabled = React287.useMemo(() => {
    return !props.rowReordering || !!sortModel.length;
  }, [props.rowReordering, sortModel]);
  const calculateDropPosition = React287.useCallback((event) => {
    const targetElement = props.treeData ? findCellElement(event.target) : event.target;
    const targetRect = targetElement.getBoundingClientRect();
    const relativeY = Math.floor(event.clientY - targetRect.top);
    if (props.treeData) {
      const topThreshold = targetRect.height * 0.2;
      const bottomThreshold = targetRect.height * 0.8;
      if (relativeY < topThreshold) {
        return "above";
      }
      if (relativeY > bottomThreshold) {
        return "below";
      }
      return "inside";
    }
    const midPoint = targetRect.height / 2;
    return relativeY < midPoint ? "above" : "below";
  }, [props.treeData]);
  const applyDraggedState = React287.useCallback((rowId, isDragged) => {
    var _a, _b;
    if (rowId) {
      const draggedRow = (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.querySelector(`[data-id="${rowId}"]`);
      if (draggedRow) {
        if (isDragged) {
          draggedRow.classList.add(classes2.rowBeingDragged);
        } else {
          draggedRow.classList.remove(classes2.rowBeingDragged);
        }
      }
    }
  }, [apiRef, classes2.rowBeingDragged]);
  const applyRowAnimation = React287.useCallback(async (callback) => {
    var _a;
    const rootElement = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current;
    if (!rootElement) {
      return;
    }
    const visibleRows = rootElement.querySelectorAll("[data-id]");
    if (!visibleRows.length) {
      return;
    }
    const rowsArray = Array.from(visibleRows);
    const initialPositions = /* @__PURE__ */ new Map();
    rowsArray.forEach((row) => {
      const rowId = row.getAttribute("data-id");
      if (rowId) {
        initialPositions.set(rowId, row.getBoundingClientRect());
      }
    });
    await callback();
    requestAnimationFrame(() => {
      const newRows = rootElement.querySelectorAll("[data-id]");
      const animations = [];
      newRows.forEach((row) => {
        const rowId = row.getAttribute("data-id");
        if (!rowId) {
          return;
        }
        const prevRect = initialPositions.get(rowId);
        if (!prevRect) {
          return;
        }
        const currentRect = row.getBoundingClientRect();
        const deltaY = prevRect.top - currentRect.top;
        if (Math.abs(deltaY) > 1) {
          const animation = row.animate([{
            transform: `translateY(${deltaY}px)`
          }, {
            transform: "translateY(0)"
          }], {
            duration: 200,
            easing: "ease-in-out",
            fill: "forwards"
          });
          animations.push(animation);
        }
      });
      if (animations.length > 0) {
        Promise.allSettled(animations.map((a) => a.finished)).then(() => {
        });
      }
    });
  }, [apiRef]);
  const handleDragStart = React287.useCallback((params, event) => {
    const editRowsState = gridEditRowsStateSelector(apiRef);
    event.dataTransfer.effectAllowed = "copy";
    if (isRowReorderDisabled || Object.keys(editRowsState).length !== 0) {
      return;
    }
    if (timeoutInfoRef.current) {
      timeout2.clear();
      timeoutInfoRef.current = EMPTY_TIMEOUT_INFO;
    }
    logger.debug(`Start dragging row ${params.id}`);
    event.stopPropagation();
    apiRef.current.setState((state) => _extends({}, state, {
      rowReorder: _extends({}, state.rowReorder, {
        isActive: true,
        draggedRowId: params.id
      })
    }));
    dragRowNode.current = event.currentTarget;
    dragRowNode.current.classList.add(classes2.rowDragging);
    setDragRowId(params.id);
    applyDraggedState(params.id, true);
    removeDnDStylesTimeout.current = setTimeout(() => {
      dragRowNode.current.classList.remove(classes2.rowDragging);
    });
    const sortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    originRowIndex.current = sortedRowIndexLookup[params.id];
    apiRef.current.setCellFocus(params.id, GRID_REORDER_COL_DEF.field);
  }, [apiRef, isRowReorderDisabled, logger, classes2.rowDragging, applyDraggedState, timeout2]);
  const handleDragOver = React287.useCallback((params, event) => {
    if (dragRowId === "") {
      return;
    }
    const targetNode = gridRowNodeSelector(apiRef, params.id);
    const sourceNode = gridRowNodeSelector(apiRef, dragRowId);
    if (!sourceNode || !targetNode || targetNode.type === "footer" || targetNode.type === "pinnedRow" || !event.target) {
      return;
    }
    logger.debug(`Dragging over row ${params.id}`);
    event.preventDefault();
    event.stopPropagation();
    if (timeoutInfoRef.current && (timeoutInfoRef.current.rowId !== params.id || // Avoid accidental opening of node when the user is moving over a row
    event.clientY > timeoutInfoRef.current.clientY + TIMEOUT_CLEAR_BUFFER_PX || event.clientY < timeoutInfoRef.current.clientY - TIMEOUT_CLEAR_BUFFER_PX || event.clientX > timeoutInfoRef.current.clientX + TIMEOUT_CLEAR_BUFFER_PX || event.clientX < timeoutInfoRef.current.clientX - TIMEOUT_CLEAR_BUFFER_PX)) {
      timeout2.clear();
      timeoutInfoRef.current = EMPTY_TIMEOUT_INFO;
    }
    const dropPosition = calculateDropPosition(event);
    if (targetNode.type === "group" && !targetNode.childrenExpanded && !timeoutInfoRef.current.rowId && targetNode.id !== sourceNode.id && (dropPosition === "inside" || targetNode.depth < sourceNode.depth)) {
      timeout2.start(500, () => {
        const rowNode = gridRowNodeSelector(apiRef, params.id);
        apiRef.current.setRowChildrenExpansion(params.id, !rowNode.childrenExpanded);
      });
      timeoutInfoRef.current = {
        rowId: params.id,
        clientY: event.clientY,
        clientX: event.clientX
      };
      return;
    }
    const sortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const targetRowIndex = sortedRowIndexLookup[params.id];
    const sourceRowIndex = sortedRowIndexLookup[dragRowId];
    const currentReorderState = {
      dragDirection: targetRowIndex < sourceRowIndex ? "up" : "down",
      previousTargetId: params.id,
      previousDropPosition: dropPosition
    };
    if (previousReorderState.current.previousTargetId !== params.id || previousReorderState.current.previousDropPosition !== dropPosition) {
      const isSameNode = targetRowIndex === sourceRowIndex;
      const isAdjacentPosition = dropPosition === "above" && targetRowIndex === sourceRowIndex + 1 || dropPosition === "below" && targetRowIndex === sourceRowIndex - 1;
      const isRowReorderValid2 = apiRef.current.unstable_applyPipeProcessors("isRowReorderValid", false, {
        sourceRowId: dragRowId,
        targetRowId: params.id,
        dropPosition,
        dragDirection: currentReorderState.dragDirection
      });
      if (isRowReorderValid2 || isAdjacentPosition || isSameNode) {
        dropTarget.current = {
          targetRowId: params.id,
          targetRowIndex,
          dropPosition
        };
        apiRef.current.setState((state) => _extends({}, state, {
          rowReorder: _extends({}, state.rowReorder, {
            dropTarget: {
              rowId: params.id,
              position: dropPosition
            }
          })
        }));
      } else {
        dropTarget.current = {
          targetRowId: null,
          targetRowIndex: null,
          dropPosition: null
        };
        apiRef.current.setState((state) => _extends({}, state, {
          rowReorder: _extends({}, state.rowReorder, {
            dropTarget: void 0
          })
        }));
      }
      previousReorderState.current = currentReorderState;
    }
    if (dropTarget.current.targetRowId === null) {
      event.dataTransfer.dropEffect = "none";
    } else {
      event.dataTransfer.dropEffect = "copy";
    }
  }, [dragRowId, apiRef, logger, timeout2, calculateDropPosition]);
  const handleDragEnd = React287.useCallback(async (_, event) => {
    const editRowsState = gridEditRowsStateSelector(apiRef);
    if (dragRowId === "" || isRowReorderDisabled || Object.keys(editRowsState).length !== 0) {
      return;
    }
    if (timeoutInfoRef.current) {
      timeout2.clear();
      timeoutInfoRef.current = EMPTY_TIMEOUT_INFO;
    }
    logger.debug("End dragging row");
    event.preventDefault();
    event.stopPropagation();
    clearTimeout(removeDnDStylesTimeout.current);
    dragRowNode.current = null;
    const dragDirection = previousReorderState.current.dragDirection;
    previousReorderState.current = EMPTY_REORDER_STATE;
    if (!event.dataTransfer || event.dataTransfer.dropEffect === "none") {
      dropTarget.current = {
        targetRowId: null,
        targetRowIndex: null,
        dropPosition: null
      };
      originRowIndex.current = null;
      setDragRowId("");
      applyDraggedState(dragRowId, false);
      apiRef.current.setState((state) => _extends({}, state, {
        rowReorder: {
          isActive: false,
          draggedRowId: null
        }
      }));
      return;
    }
    if (dropTarget.current.targetRowIndex !== null && dropTarget.current.targetRowId !== null) {
      const isRowReorderValid2 = apiRef.current.unstable_applyPipeProcessors("isRowReorderValid", false, {
        sourceRowId: dragRowId,
        targetRowId: dropTarget.current.targetRowId,
        dropPosition: dropTarget.current.dropPosition,
        dragDirection
      });
      if (isRowReorderValid2) {
        try {
          const rowTree = gridRowTreeSelector(apiRef);
          const sourceNode = gridRowNodeSelector(apiRef, dragRowId);
          if (!sourceNode) {
            throw new Error(`MUI X: No row node found for id #${dragRowId}`);
          }
          const oldParent = sourceNode.parent;
          const oldParentNode = rowTree[oldParent];
          const oldIndexInParent = oldParentNode.children.indexOf(dragRowId) ?? originRowIndex.current;
          await applyRowAnimation(async () => {
            await apiRef.current.setRowPosition(dragRowId, dropTarget.current.targetRowId, dropTarget.current.dropPosition);
            const updatedTree = gridRowTreeSelector(apiRef);
            const updatedNode = updatedTree[dragRowId];
            if (!updatedNode) {
              throw new Error(`MUI X: Row node for id #${dragRowId} not found after move`);
            }
            const newParent = updatedNode.parent;
            const newParentNode = updatedTree[newParent];
            const newIndexInParent = newParentNode.children.indexOf(dragRowId);
            const rowOrderChangeParams = {
              row: apiRef.current.getRow(dragRowId),
              oldIndex: oldIndexInParent,
              targetIndex: newIndexInParent,
              oldParent: oldParent === GRID_ROOT_GROUP_ID ? null : oldParent,
              newParent: newParent === GRID_ROOT_GROUP_ID ? null : newParent
            };
            applyDraggedState(dragRowId, false);
            apiRef.current.setState((state) => _extends({}, state, {
              rowReorder: {
                isActive: false,
                draggedRowId: null
              }
            }));
            apiRef.current.publishEvent("rowOrderChange", rowOrderChangeParams);
          });
        } catch (error) {
          applyDraggedState(dragRowId, false);
          apiRef.current.setState((state) => _extends({}, state, {
            rowReorder: {
              isActive: false,
              draggedRowId: null
            }
          }));
        }
      } else {
        applyDraggedState(dragRowId, false);
        apiRef.current.setState((state) => _extends({}, state, {
          rowReorder: _extends({}, state.rowReorder, {
            dropTarget: void 0
          })
        }));
      }
    }
    dropTarget.current = {
      targetRowId: null,
      targetRowIndex: null,
      dropPosition: null
    };
    setDragRowId("");
  }, [apiRef, dragRowId, isRowReorderDisabled, logger, applyDraggedState, timeout2, applyRowAnimation]);
  const isValidRowReorderProp = props.isValidRowReorder;
  const isRowReorderValid = React287.useCallback((initialValue, {
    sourceRowId,
    targetRowId,
    dropPosition,
    dragDirection
  }) => {
    if (gridRowMaximumTreeDepthSelector(apiRef) > 1) {
      return initialValue;
    }
    const sortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const targetRowIndex = sortedRowIndexLookup[targetRowId];
    const sourceRowIndex = sortedRowIndexLookup[sourceRowId];
    const isAdjacentNode = dropPosition === "above" && targetRowIndex === sourceRowIndex + 1 || // dragging to immediately below (above next row)
    dropPosition === "below" && targetRowIndex === sourceRowIndex - 1;
    if (isAdjacentNode || sourceRowIndex === targetRowIndex) {
      return false;
    }
    if (isValidRowReorderProp) {
      const expandedSortedRowIds = gridExpandedSortedRowIdsSelector(apiRef);
      const rowTree = gridRowTreeSelector(apiRef);
      const sourceNode = rowTree[sourceRowId];
      const targetNode = rowTree[targetRowId];
      const prevNode = targetRowIndex > 0 ? rowTree[expandedSortedRowIds[targetRowIndex - 1]] : null;
      const nextNode = targetRowIndex < expandedSortedRowIds.length - 1 ? rowTree[expandedSortedRowIds[targetRowIndex + 1]] : null;
      const context = {
        apiRef,
        sourceNode,
        targetNode,
        prevNode,
        nextNode,
        dropPosition,
        dragDirection
      };
      if (!isValidRowReorderProp(context)) {
        return false;
      }
    }
    return true;
  }, [apiRef, isValidRowReorderProp]);
  useGridRegisterPipeProcessor(apiRef, "isRowReorderValid", isRowReorderValid);
  useGridEvent(apiRef, "rowDragStart", handleDragStart);
  useGridEvent(apiRef, "rowDragOver", handleDragOver);
  useGridEvent(apiRef, "rowDragEnd", handleDragEnd);
  useGridEvent(apiRef, "cellDragOver", handleDragOver);
  useGridEventPriority(apiRef, "rowOrderChange", props.onRowOrderChange);
  const setRowDragActive = React287.useCallback((isActive) => {
    apiRef.current.setState((state) => _extends({}, state, {
      rowReorder: _extends({}, state.rowReorder, {
        isActive
      })
    }));
  }, [apiRef]);
  useGridApiMethod(apiRef, {
    setRowDragActive
  }, "private");
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowReorder/useGridRowReorderPreProcessors.js
var React288 = __toESM(require_react(), 1);
var useUtilityClasses107 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  return React288.useMemo(() => {
    const slots = {
      rowReorderCellContainer: ["rowReorderCellContainer"],
      columnHeaderReorder: ["columnHeaderReorder"]
    };
    return composeClasses(slots, getDataGridUtilityClass, classes2);
  }, [classes2]);
};
var useGridRowReorderPreProcessors = (privateApiRef, props) => {
  const ownerState = {
    classes: props.classes
  };
  const classes2 = useUtilityClasses107(ownerState);
  const updateReorderColumn = React288.useCallback((columnsState) => {
    const reorderColumn = _extends({}, GRID_REORDER_COL_DEF, {
      cellClassName: classes2.rowReorderCellContainer,
      headerClassName: classes2.columnHeaderReorder,
      headerName: privateApiRef.current.getLocaleText("rowReorderingHeaderName")
    });
    const shouldHaveReorderColumn = props.rowReordering;
    const hasReorderColumn = columnsState.lookup[reorderColumn.field] != null;
    if (shouldHaveReorderColumn && !hasReorderColumn) {
      columnsState.lookup[reorderColumn.field] = reorderColumn;
      columnsState.orderedFields = [reorderColumn.field, ...columnsState.orderedFields];
    } else if (!shouldHaveReorderColumn && hasReorderColumn) {
      delete columnsState.lookup[reorderColumn.field];
      columnsState.orderedFields = columnsState.orderedFields.filter((field) => field !== reorderColumn.field);
    } else if (shouldHaveReorderColumn && hasReorderColumn) {
      columnsState.lookup[reorderColumn.field] = _extends({}, reorderColumn, columnsState.lookup[reorderColumn.field]);
      if (!props.columns.some((col) => col.field === GRID_REORDER_COL_DEF.field)) {
        columnsState.orderedFields = [reorderColumn.field, ...columnsState.orderedFields.filter((field) => field !== reorderColumn.field)];
      }
    }
    return columnsState;
  }, [privateApiRef, classes2, props.columns, props.rowReordering]);
  useGridRegisterPipeProcessor(privateApiRef, "hydrateColumns", updateReorderColumn);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowPinning/useGridRowPinning.js
var React289 = __toESM(require_react(), 1);
function createPinnedRowsInternalCache(pinnedRows, getRowId) {
  var _a, _b;
  const cache = {
    topIds: [],
    bottomIds: [],
    idLookup: {}
  };
  (_a = pinnedRows == null ? void 0 : pinnedRows.top) == null ? void 0 : _a.forEach((rowModel) => {
    const id = getRowIdFromRowModel(rowModel, getRowId);
    cache.topIds.push(id);
    cache.idLookup[id] = rowModel;
  });
  (_b = pinnedRows == null ? void 0 : pinnedRows.bottom) == null ? void 0 : _b.forEach((rowModel) => {
    const id = getRowIdFromRowModel(rowModel, getRowId);
    cache.bottomIds.push(id);
    cache.idLookup[id] = rowModel;
  });
  return cache;
}
var rowPinningStateInitializer = (state, props, apiRef) => {
  var _a;
  apiRef.current.caches.pinnedRows = createPinnedRowsInternalCache(props.pinnedRows, props.getRowId);
  return _extends({}, state, {
    rows: _extends({}, state.rows, {
      additionalRowGroups: _extends({}, (_a = state.rows) == null ? void 0 : _a.additionalRowGroups, {
        pinnedRows: {
          top: [],
          bottom: []
        }
      })
    })
  });
};
var useGridRowPinning = (apiRef, props) => {
  const setPinnedRows = React289.useCallback((newPinnedRows) => {
    apiRef.current.caches.pinnedRows = createPinnedRowsInternalCache(newPinnedRows, props.getRowId);
    apiRef.current.requestPipeProcessorsApplication("hydrateRows");
  }, [apiRef, props.getRowId]);
  useGridApiMethod(apiRef, {
    unstable_setPinnedRows: setPinnedRows
  }, "public");
  const isFirstRender = React289.useRef(true);
  React289.useEffect(() => {
    if (isFirstRender.current) {
      isFirstRender.current = false;
      return;
    }
    apiRef.current.unstable_setPinnedRows(props.pinnedRows);
  }, [apiRef, props.pinnedRows]);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/rowPinning/useGridRowPinningPreProcessors.js
var React290 = __toESM(require_react(), 1);
function addPinnedRow({
  groupingParams,
  rowModel,
  rowId,
  position,
  apiRef,
  isAutoGenerated
}) {
  var _a, _b, _c, _d;
  const dataRowIdToModelLookup = _extends({}, groupingParams.dataRowIdToModelLookup);
  const tree = _extends({}, groupingParams.tree);
  const treeDepths = _extends({}, groupingParams.treeDepths);
  const node = {
    type: "pinnedRow",
    id: rowId,
    depth: 0,
    parent: GRID_ROOT_GROUP_ID,
    isAutoGenerated
  };
  insertNodeInTree(node, tree, treeDepths, null);
  if (!isAutoGenerated) {
    dataRowIdToModelLookup[rowId] = rowModel;
  }
  apiRef.current.caches.rows.dataRowIdToModelLookup[rowId] = _extends({}, rowModel);
  const previousPinnedRows = ((_a = groupingParams.additionalRowGroups) == null ? void 0 : _a.pinnedRows) || {};
  const newPinnedRow = {
    id: rowId,
    model: rowModel
  };
  if ((_d = (_c = (_b = groupingParams.additionalRowGroups) == null ? void 0 : _b.pinnedRows) == null ? void 0 : _c[position]) == null ? void 0 : _d.includes(newPinnedRow)) {
    return _extends({}, groupingParams, {
      dataRowIdToModelLookup,
      tree,
      treeDepths
    });
  }
  return _extends({}, groupingParams, {
    dataRowIdToModelLookup,
    tree,
    treeDepths,
    additionalRowGroups: _extends({}, groupingParams.additionalRowGroups, {
      pinnedRows: _extends({}, previousPinnedRows, {
        [position]: [...previousPinnedRows[position] || [], newPinnedRow]
      })
    })
  });
}
var useGridRowPinningPreProcessors = (apiRef) => {
  const prevPinnedRowsCacheRef = React290.useRef(null);
  const addPinnedRows = React290.useCallback((groupingParams) => {
    var _a, _b, _c, _d, _e, _f;
    const pinnedRowsCache = apiRef.current.caches.pinnedRows || {};
    const prevPinnedRowsCache = prevPinnedRowsCacheRef.current;
    prevPinnedRowsCacheRef.current = pinnedRowsCache;
    let newGroupingParams = _extends({}, groupingParams, {
      additionalRowGroups: _extends({}, groupingParams.additionalRowGroups, {
        // reset pinned rows state
        pinnedRows: {}
      })
    });
    if (prevPinnedRowsCache) {
      const pinnedRowCleanup = (rowId) => {
        const node = newGroupingParams.tree[rowId];
        if ((node == null ? void 0 : node.type) === "pinnedRow") {
          delete newGroupingParams.tree[rowId];
          delete newGroupingParams.dataRowIdToModelLookup[rowId];
          delete apiRef.current.caches.rows.dataRowIdToModelLookup[rowId];
        }
      };
      (_a = prevPinnedRowsCache.topIds) == null ? void 0 : _a.forEach(pinnedRowCleanup);
      (_b = prevPinnedRowsCache.bottomIds) == null ? void 0 : _b.forEach(pinnedRowCleanup);
    }
    (_c = pinnedRowsCache.topIds) == null ? void 0 : _c.forEach((rowId) => {
      newGroupingParams = addPinnedRow({
        groupingParams: newGroupingParams,
        rowModel: pinnedRowsCache.idLookup[rowId],
        rowId,
        position: "top",
        apiRef,
        isAutoGenerated: false
      });
    });
    (_d = pinnedRowsCache.bottomIds) == null ? void 0 : _d.forEach((rowId) => {
      newGroupingParams = addPinnedRow({
        groupingParams: newGroupingParams,
        rowModel: pinnedRowsCache.idLookup[rowId],
        rowId,
        position: "bottom",
        apiRef,
        isAutoGenerated: false
      });
    });
    if (((_e = pinnedRowsCache.bottomIds) == null ? void 0 : _e.length) || ((_f = pinnedRowsCache.topIds) == null ? void 0 : _f.length)) {
      const shouldKeepRow = (rowId) => {
        if (newGroupingParams.tree[rowId] && newGroupingParams.tree[rowId].type === "pinnedRow") {
          return false;
        }
        return true;
      };
      const rootGroupNode = newGroupingParams.tree[GRID_ROOT_GROUP_ID];
      newGroupingParams.tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroupNode, {
        children: rootGroupNode.children.filter(shouldKeepRow)
      });
      newGroupingParams.dataRowIds = newGroupingParams.dataRowIds.filter(shouldKeepRow);
    }
    return newGroupingParams;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "hydrateRows", addPinnedRows);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/lazyLoader/useGridLazyLoader.js
var React291 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/lazyLoader/utils.js
var adjustRowParams = (params, options3) => {
  if (typeof params.start !== "number") {
    return params;
  }
  const {
    pageSize: pageSize2,
    rowCount
  } = options3;
  const adjustedStart = params.start - params.start % pageSize2;
  const pageAlignedEnd = params.end + pageSize2 - params.end % pageSize2 - 1;
  const maxEnd = rowCount !== void 0 && rowCount !== -1 ? Math.max(0, rowCount - 1) : Infinity;
  return _extends({}, params, {
    start: adjustedStart,
    end: Math.min(maxEnd, pageAlignedEnd)
  });
};
var findSkeletonRowsSection = ({
  apiRef,
  visibleRows,
  range
}) => {
  var _a, _b;
  let {
    firstRowIndex,
    lastRowIndex
  } = range;
  const visibleRowsSection = visibleRows.slice(range.firstRowIndex, range.lastRowIndex);
  let startIndex = 0;
  let endIndex = visibleRowsSection.length - 1;
  let isSkeletonSectionFound = false;
  while (!isSkeletonSectionFound && firstRowIndex < lastRowIndex) {
    const isStartingWithASkeletonRow = ((_a = gridRowNodeSelector(apiRef, visibleRowsSection[startIndex].id)) == null ? void 0 : _a.type) === "skeletonRow";
    const isEndingWithASkeletonRow = ((_b = gridRowNodeSelector(apiRef, visibleRowsSection[endIndex].id)) == null ? void 0 : _b.type) === "skeletonRow";
    if (isStartingWithASkeletonRow && isEndingWithASkeletonRow) {
      isSkeletonSectionFound = true;
    }
    if (!isStartingWithASkeletonRow) {
      startIndex += 1;
      firstRowIndex += 1;
    }
    if (!isEndingWithASkeletonRow) {
      endIndex -= 1;
      lastRowIndex -= 1;
    }
  }
  return isSkeletonSectionFound ? {
    firstRowIndex,
    lastRowIndex
  } : void 0;
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/lazyLoader/useGridLazyLoader.js
var useGridLazyLoader = (privateApiRef, props) => {
  const sortModel = useGridSelector(privateApiRef, gridSortModelSelector);
  const filterModel = useGridSelector(privateApiRef, gridFilterModelSelector);
  const renderedRowsIntervalCache = React291.useRef({
    firstRowToRender: 0,
    lastRowToRender: 0
  });
  const isDisabled = props.rowsLoadingMode !== "server";
  const handleRenderedRowsIntervalChange = React291.useCallback((params) => {
    if (isDisabled) {
      return;
    }
    const fetchRowsParams = {
      firstRowToRender: params.firstRowIndex,
      lastRowToRender: params.lastRowIndex,
      sortModel,
      filterModel
    };
    if (renderedRowsIntervalCache.current.firstRowToRender === params.firstRowIndex && renderedRowsIntervalCache.current.lastRowToRender === params.lastRowIndex) {
      return;
    }
    renderedRowsIntervalCache.current = {
      firstRowToRender: params.firstRowIndex,
      lastRowToRender: params.lastRowIndex
    };
    if (sortModel.length === 0 && filterModel.items.length === 0) {
      const currentVisibleRows = getVisibleRows(privateApiRef, {
        pagination: props.pagination,
        paginationMode: props.paginationMode
      });
      const skeletonRowsSection = findSkeletonRowsSection({
        apiRef: privateApiRef,
        visibleRows: currentVisibleRows.rows,
        range: {
          firstRowIndex: params.firstRowIndex,
          lastRowIndex: params.lastRowIndex
        }
      });
      if (!skeletonRowsSection) {
        return;
      }
      fetchRowsParams.firstRowToRender = skeletonRowsSection.firstRowIndex;
      fetchRowsParams.lastRowToRender = skeletonRowsSection.lastRowIndex;
    }
    privateApiRef.current.publishEvent("fetchRows", fetchRowsParams);
  }, [privateApiRef, isDisabled, props.pagination, props.paginationMode, sortModel, filterModel]);
  const handleGridSortModelChange = React291.useCallback((newSortModel) => {
    if (isDisabled) {
      return;
    }
    privateApiRef.current.requestPipeProcessorsApplication("hydrateRows");
    const renderContext = gridRenderContextSelector(privateApiRef);
    const fetchRowsParams = {
      firstRowToRender: renderContext.firstRowIndex,
      lastRowToRender: renderContext.lastRowIndex,
      sortModel: newSortModel,
      filterModel
    };
    privateApiRef.current.publishEvent("fetchRows", fetchRowsParams);
  }, [privateApiRef, isDisabled, filterModel]);
  const handleGridFilterModelChange = React291.useCallback((newFilterModel) => {
    if (isDisabled) {
      return;
    }
    privateApiRef.current.requestPipeProcessorsApplication("hydrateRows");
    const renderContext = gridRenderContextSelector(privateApiRef);
    const fetchRowsParams = {
      firstRowToRender: renderContext.firstRowIndex,
      lastRowToRender: renderContext.lastRowIndex,
      sortModel,
      filterModel: newFilterModel
    };
    privateApiRef.current.publishEvent("fetchRows", fetchRowsParams);
  }, [privateApiRef, isDisabled, sortModel]);
  useGridEvent(privateApiRef, "renderedRowsIntervalChange", handleRenderedRowsIntervalChange);
  useGridEvent(privateApiRef, "sortModelChange", handleGridSortModelChange);
  useGridEvent(privateApiRef, "filterModelChange", handleGridFilterModelChange);
  useGridEventPriority(privateApiRef, "fetchRows", props.onFetchRows);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/lazyLoader/useGridLazyLoaderPreProcessors.js
var React292 = __toESM(require_react(), 1);
var GRID_SKELETON_ROW_ROOT_ID = "auto-generated-skeleton-row-root";
var getSkeletonRowId = (index) => `${GRID_SKELETON_ROW_ROOT_ID}-${index}`;
var useGridLazyLoaderPreProcessors = (privateApiRef, props) => {
  const addSkeletonRows = React292.useCallback((groupingParams) => {
    const rootGroup = groupingParams.tree[GRID_ROOT_GROUP_ID];
    if (props.rowsLoadingMode !== "server" || !props.rowCount || rootGroup.children.length >= props.rowCount) {
      return groupingParams;
    }
    const tree = _extends({}, groupingParams.tree);
    const rootGroupChildren = [...rootGroup.children];
    for (let i2 = 0; i2 < props.rowCount - rootGroup.children.length; i2 += 1) {
      const skeletonId = getSkeletonRowId(i2);
      rootGroupChildren.push(skeletonId);
      const skeletonRowNode = {
        type: "skeletonRow",
        id: skeletonId,
        parent: GRID_ROOT_GROUP_ID,
        depth: 0
      };
      tree[skeletonId] = skeletonRowNode;
    }
    tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
      children: rootGroupChildren
    });
    return _extends({}, groupingParams, {
      tree
    });
  }, [props.rowCount, props.rowsLoadingMode]);
  useGridRegisterPipeProcessor(privateApiRef, "hydrateRows", addSkeletonRows);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/serverSideLazyLoader/useGridDataSourceLazyLoader.js
var React293 = __toESM(require_react(), 1);
var LoadingTrigger = (function(LoadingTrigger2) {
  LoadingTrigger2[LoadingTrigger2["VIEWPORT"] = 0] = "VIEWPORT";
  LoadingTrigger2[LoadingTrigger2["SCROLL_END"] = 1] = "SCROLL_END";
  return LoadingTrigger2;
})(LoadingTrigger || {});
var INTERVAL_CACHE_INITIAL_STATE = {
  firstRowToRender: 0,
  lastRowToRender: 0
};
var getSkeletonRowId2 = (index) => `${GRID_SKELETON_ROW_ROOT_ID}-${index}`;
var useGridDataSourceLazyLoader = (privateApiRef, props) => {
  const setStrategyAvailability2 = React293.useCallback(() => {
    privateApiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, DataSourceRowsUpdateStrategy.LazyLoading, props.dataSource && props.lazyLoading ? () => true : () => false);
  }, [privateApiRef, props.lazyLoading, props.dataSource]);
  const [lazyLoadingRowsUpdateStrategyActive, setLazyLoadingRowsUpdateStrategyActive] = React293.useState(false);
  const renderedRowsIntervalCache = React293.useRef(INTERVAL_CACHE_INITIAL_STATE);
  const previousLastRowIndex = React293.useRef(0);
  const loadingTrigger = React293.useRef(null);
  const rowsStale = React293.useRef(false);
  const draggedRowId = React293.useRef(null);
  const pollingIntervalRef = React293.useRef(null);
  const fetchRows = React293.useCallback((params) => {
    privateApiRef.current.dataSource.fetchRows(GRID_ROOT_GROUP_ID, params);
  }, [privateApiRef]);
  const debouncedFetchRows = React293.useMemo(() => debounce(fetchRows, 0), [fetchRows]);
  const revalidate = useEventCallback_default((params) => {
    if (rowsStale.current) {
      return;
    }
    const cache = privateApiRef.current.dataSource.cache;
    const cachedResponse = cache.get(params);
    if (cachedResponse !== void 0) {
      return;
    }
    debouncedFetchRows(params);
  });
  const stopPolling = React293.useCallback(() => {
    if (pollingIntervalRef.current !== null) {
      clearInterval(pollingIntervalRef.current);
      pollingIntervalRef.current = null;
    }
  }, []);
  const startPolling = useEventCallback_default((params) => {
    stopPolling();
    if (props.dataSourceRevalidateMs <= 0) {
      return;
    }
    pollingIntervalRef.current = setInterval(() => {
      revalidate(params);
    }, props.dataSourceRevalidateMs);
  });
  const resetGrid = React293.useCallback(() => {
    privateApiRef.current.setLoading(true);
    privateApiRef.current.dataSource.cache.clear();
    rowsStale.current = true;
    previousLastRowIndex.current = 0;
    const paginationModel = gridPaginationModelSelector(privateApiRef);
    const sortModel = gridSortModelSelector(privateApiRef);
    const filterModel = gridFilterModelSelector(privateApiRef);
    const getRowsParams = {
      start: 0,
      end: paginationModel.pageSize - 1,
      sortModel,
      filterModel
    };
    fetchRows(getRowsParams);
  }, [privateApiRef, fetchRows]);
  const ensureValidRowCount = React293.useCallback((previousLoadingTrigger, newLoadingTrigger) => {
    if (previousLoadingTrigger === LoadingTrigger.VIEWPORT && newLoadingTrigger === LoadingTrigger.SCROLL_END) {
      resetGrid();
      return;
    }
    const tree = privateApiRef.current.state.rows.tree;
    const rootGroup = tree[GRID_ROOT_GROUP_ID];
    const rootGroupChildren = [...rootGroup.children];
    const pageRowCount = privateApiRef.current.state.pagination.rowCount;
    const rootChildrenCount = rootGroupChildren.length;
    if (rootChildrenCount > pageRowCount) {
      resetGrid();
    }
  }, [privateApiRef, resetGrid]);
  const addSkeletonRows = React293.useCallback(() => {
    const tree = privateApiRef.current.state.rows.tree;
    const rootGroup = tree[GRID_ROOT_GROUP_ID];
    const rootGroupChildren = [...rootGroup.children];
    const pageRowCount = privateApiRef.current.state.pagination.rowCount;
    const rootChildrenCount = rootGroupChildren.length;
    if (rootChildrenCount === 0) {
      return;
    }
    let hasChanged = false;
    if (loadingTrigger.current === LoadingTrigger.VIEWPORT) {
      for (let i2 = 0; i2 < pageRowCount - rootChildrenCount; i2 += 1) {
        const skeletonId = getSkeletonRowId2(i2 + rootChildrenCount);
        rootGroupChildren.push(skeletonId);
        const skeletonRowNode = {
          type: "skeletonRow",
          id: skeletonId,
          parent: GRID_ROOT_GROUP_ID,
          depth: 0
        };
        tree[skeletonId] = skeletonRowNode;
        hasChanged = true;
      }
    }
    if (!hasChanged) {
      return;
    }
    tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
      children: rootGroupChildren
    });
    privateApiRef.current.setState((state) => _extends({}, state, {
      rows: _extends({}, state.rows, {
        tree
      })
    }), "addSkeletonRows");
  }, [privateApiRef]);
  const updateLoadingTrigger = React293.useCallback((rowCount) => {
    const newLoadingTrigger = rowCount === -1 ? LoadingTrigger.SCROLL_END : LoadingTrigger.VIEWPORT;
    if (loadingTrigger.current !== null) {
      ensureValidRowCount(loadingTrigger.current, newLoadingTrigger);
    }
    if (loadingTrigger.current !== newLoadingTrigger) {
      loadingTrigger.current = newLoadingTrigger;
    }
  }, [ensureValidRowCount]);
  const handleDataUpdate = React293.useCallback((params) => {
    var _a, _b;
    if ("error" in params) {
      return;
    }
    const {
      response,
      fetchParams
    } = params;
    const pageRowCount = privateApiRef.current.state.pagination.rowCount;
    const tree = privateApiRef.current.state.rows.tree;
    const dataRowIdToModelLookup = privateApiRef.current.state.rows.dataRowIdToModelLookup;
    if (response.rowCount !== void 0 || pageRowCount === void 0) {
      privateApiRef.current.setRowCount(response.rowCount === void 0 ? -1 : response.rowCount);
    }
    if (rowsStale.current && params.fetchParams.start === 0) {
      privateApiRef.current.scroll({
        top: 0
      });
      privateApiRef.current.setRows(response.rows);
    } else {
      const rootGroup = tree[GRID_ROOT_GROUP_ID];
      const rootGroupChildren = [...rootGroup.children];
      const filteredSortedRowIds = gridFilteredSortedRowIdsSelector(privateApiRef);
      const startingIndex = typeof fetchParams.start === "string" ? Math.max(filteredSortedRowIds.indexOf(fetchParams.start), 0) : fetchParams.start;
      const firstTargetRow = rootGroupChildren[startingIndex];
      const isRevalidation = firstTargetRow && ((_a = tree[firstTargetRow]) == null ? void 0 : _a.type) !== "skeletonRow";
      if (isRevalidation) {
        const newRowIds = response.rows.map((row) => gridRowIdSelector(privateApiRef, row));
        const existingRowIds = rootGroupChildren.slice(startingIndex, startingIndex + response.rows.length);
        const sameRowIds = existingRowIds.length === newRowIds.length && existingRowIds.every((id, i2) => id === newRowIds[i2]);
        if (sameRowIds) {
          const changedRows = response.rows.filter((newRow, i2) => {
            const existingRow = dataRowIdToModelLookup[existingRowIds[i2]];
            return !isDeepEqual(newRow, existingRow);
          });
          if (changedRows.length === 0) {
            privateApiRef.current.setLoading(false);
            return;
          }
          privateApiRef.current.updateRows(changedRows);
        } else {
          for (let i2 = startingIndex; i2 < startingIndex + response.rows.length && i2 < rootGroupChildren.length; i2 += 1) {
            const oldRowId = rootGroupChildren[i2];
            if (oldRowId && ((_b = tree[oldRowId]) == null ? void 0 : _b.type) !== "skeletonRow") {
              delete tree[oldRowId];
              delete dataRowIdToModelLookup[oldRowId];
              const skeletonId = getSkeletonRowId2(i2);
              rootGroupChildren[i2] = skeletonId;
              tree[skeletonId] = {
                type: "skeletonRow",
                id: skeletonId,
                parent: GRID_ROOT_GROUP_ID,
                depth: 0
              };
            }
          }
          let duplicateRowCount = 0;
          response.rows.forEach((row) => {
            const rowId = gridRowIdSelector(privateApiRef, row);
            if (tree[rowId] || dataRowIdToModelLookup[rowId]) {
              const index = rootGroupChildren.indexOf(rowId);
              if (index !== -1) {
                const skeletonId = getSkeletonRowId2(index);
                rootGroupChildren[index] = skeletonId;
                tree[skeletonId] = {
                  type: "skeletonRow",
                  id: skeletonId,
                  parent: GRID_ROOT_GROUP_ID,
                  depth: 0
                };
              }
              delete tree[rowId];
              delete dataRowIdToModelLookup[rowId];
              duplicateRowCount += 1;
            }
          });
          if (duplicateRowCount > 0) {
            tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
              children: rootGroupChildren
            });
            privateApiRef.current.setState((state) => _extends({}, state, {
              rows: _extends({}, state.rows, {
                tree,
                dataRowIdToModelLookup
              })
            }));
          }
          privateApiRef.current.unstable_replaceRows(startingIndex, response.rows);
        }
      } else {
        let duplicateRowCount = 0;
        response.rows.forEach((row) => {
          const rowId = gridRowIdSelector(privateApiRef, row);
          if (tree[rowId] || dataRowIdToModelLookup[rowId]) {
            const index = rootGroupChildren.indexOf(rowId);
            if (index !== -1) {
              const skeletonId = getSkeletonRowId2(index);
              rootGroupChildren[index] = skeletonId;
              tree[skeletonId] = {
                type: "skeletonRow",
                id: skeletonId,
                parent: GRID_ROOT_GROUP_ID,
                depth: 0
              };
            }
            delete tree[rowId];
            delete dataRowIdToModelLookup[rowId];
            duplicateRowCount += 1;
          }
        });
        if (duplicateRowCount > 0) {
          tree[GRID_ROOT_GROUP_ID] = _extends({}, rootGroup, {
            children: rootGroupChildren
          });
          privateApiRef.current.setState((state) => _extends({}, state, {
            rows: _extends({}, state.rows, {
              tree,
              dataRowIdToModelLookup
            })
          }));
        }
        privateApiRef.current.unstable_replaceRows(startingIndex, response.rows);
      }
    }
    rowsStale.current = false;
    if (loadingTrigger.current === null) {
      updateLoadingTrigger(privateApiRef.current.state.pagination.rowCount);
    }
    addSkeletonRows();
    privateApiRef.current.setLoading(false);
    privateApiRef.current.unstable_applyPipeProcessors("processDataSourceRows", {
      params: params.fetchParams,
      response
    }, false);
    if (loadingTrigger.current === LoadingTrigger.VIEWPORT) {
      startPolling(params.fetchParams);
    }
    privateApiRef.current.requestPipeProcessorsApplication("hydrateRows");
  }, [privateApiRef, updateLoadingTrigger, addSkeletonRows, startPolling]);
  const handleRowCountChange = React293.useCallback(() => {
    if (rowsStale.current || loadingTrigger.current === null) {
      return;
    }
    updateLoadingTrigger(privateApiRef.current.state.pagination.rowCount);
    addSkeletonRows();
    privateApiRef.current.requestPipeProcessorsApplication("hydrateRows");
  }, [privateApiRef, updateLoadingTrigger, addSkeletonRows]);
  const handleIntersection = useEventCallback_default(() => {
    if (rowsStale.current || loadingTrigger.current !== LoadingTrigger.SCROLL_END) {
      return;
    }
    const renderContext = gridRenderContextSelector(privateApiRef);
    if (previousLastRowIndex.current >= renderContext.lastRowIndex) {
      return;
    }
    previousLastRowIndex.current = renderContext.lastRowIndex;
    const paginationModel = gridPaginationModelSelector(privateApiRef);
    const sortModel = gridSortModelSelector(privateApiRef);
    const filterModel = gridFilterModelSelector(privateApiRef);
    const getRowsParams = {
      start: renderContext.lastRowIndex,
      end: renderContext.lastRowIndex + paginationModel.pageSize - 1,
      sortModel,
      filterModel
    };
    privateApiRef.current.setLoading(true);
    fetchRows(adjustRowParams(getRowsParams, {
      pageSize: paginationModel.pageSize,
      rowCount: privateApiRef.current.state.pagination.rowCount
    }));
  });
  const handleRenderedRowsIntervalChange = React293.useCallback((renderContext) => {
    if (rowsStale.current) {
      return;
    }
    const sortModel = gridSortModelSelector(privateApiRef);
    const filterModel = gridFilterModelSelector(privateApiRef);
    const getRowsParams = {
      start: renderContext.firstRowIndex,
      end: renderContext.lastRowIndex - 1,
      sortModel,
      filterModel
    };
    if (renderedRowsIntervalCache.current.firstRowToRender === renderContext.firstRowIndex && renderedRowsIntervalCache.current.lastRowToRender === renderContext.lastRowIndex) {
      return;
    }
    renderedRowsIntervalCache.current = {
      firstRowToRender: renderContext.firstRowIndex,
      lastRowToRender: renderContext.lastRowIndex
    };
    const currentVisibleRows = getVisibleRows(privateApiRef);
    const skeletonRowsSection = findSkeletonRowsSection({
      apiRef: privateApiRef,
      visibleRows: currentVisibleRows.rows,
      range: renderContext
    });
    const paginationModel = gridPaginationModelSelector(privateApiRef);
    if (!skeletonRowsSection) {
      if (loadingTrigger.current === LoadingTrigger.VIEWPORT) {
        const adjustedParams = adjustRowParams(getRowsParams, {
          pageSize: paginationModel.pageSize,
          rowCount: privateApiRef.current.state.pagination.rowCount
        });
        revalidate(adjustedParams);
        startPolling(adjustedParams);
      }
      return;
    }
    getRowsParams.start = skeletonRowsSection.firstRowIndex;
    getRowsParams.end = skeletonRowsSection.lastRowIndex;
    fetchRows(adjustRowParams(getRowsParams, {
      pageSize: paginationModel.pageSize,
      rowCount: privateApiRef.current.state.pagination.rowCount
    }));
  }, [privateApiRef, fetchRows, revalidate, startPolling]);
  const throttledHandleRenderedRowsIntervalChange = React293.useMemo(() => throttle(handleRenderedRowsIntervalChange, props.lazyLoadingRequestThrottleMs), [props.lazyLoadingRequestThrottleMs, handleRenderedRowsIntervalChange]);
  React293.useEffect(() => {
    return () => {
      throttledHandleRenderedRowsIntervalChange.clear();
      stopPolling();
    };
  }, [throttledHandleRenderedRowsIntervalChange, stopPolling]);
  React293.useEffect(() => {
    if (props.dataSourceRevalidateMs <= 0) {
      stopPolling();
    }
  }, [props.dataSourceRevalidateMs, stopPolling]);
  React293.useEffect(() => stopPolling, [stopPolling]);
  const handleGridSortModelChange = React293.useCallback((newSortModel) => {
    rowsStale.current = true;
    throttledHandleRenderedRowsIntervalChange.clear();
    stopPolling();
    previousLastRowIndex.current = 0;
    const paginationModel = gridPaginationModelSelector(privateApiRef);
    const filterModel = gridFilterModelSelector(privateApiRef);
    const getRowsParams = {
      start: 0,
      end: paginationModel.pageSize - 1,
      sortModel: newSortModel,
      filterModel
    };
    privateApiRef.current.setLoading(true);
    debouncedFetchRows(getRowsParams);
  }, [privateApiRef, debouncedFetchRows, throttledHandleRenderedRowsIntervalChange, stopPolling]);
  const handleGridFilterModelChange = React293.useCallback((newFilterModel) => {
    rowsStale.current = true;
    throttledHandleRenderedRowsIntervalChange.clear();
    stopPolling();
    previousLastRowIndex.current = 0;
    const paginationModel = gridPaginationModelSelector(privateApiRef);
    const sortModel = gridSortModelSelector(privateApiRef);
    const getRowsParams = {
      start: 0,
      end: paginationModel.pageSize - 1,
      sortModel,
      filterModel: newFilterModel
    };
    privateApiRef.current.setLoading(true);
    debouncedFetchRows(getRowsParams);
  }, [privateApiRef, debouncedFetchRows, throttledHandleRenderedRowsIntervalChange, stopPolling]);
  const handleDragStart = React293.useCallback((row) => {
    draggedRowId.current = row.id;
  }, []);
  const handleDragEnd = React293.useCallback(() => {
    draggedRowId.current = null;
  }, []);
  const handleStrategyActivityChange = React293.useCallback(() => {
    setLazyLoadingRowsUpdateStrategyActive(privateApiRef.current.getActiveStrategy(GridStrategyGroup.DataSource) === DataSourceRowsUpdateStrategy.LazyLoading);
  }, [privateApiRef]);
  useGridRegisterStrategyProcessor(privateApiRef, DataSourceRowsUpdateStrategy.LazyLoading, "dataSourceRowsUpdate", handleDataUpdate);
  useGridEvent(privateApiRef, "strategyAvailabilityChange", handleStrategyActivityChange);
  useGridEvent(privateApiRef, "rowCountChange", runIf(lazyLoadingRowsUpdateStrategyActive, handleRowCountChange));
  useGridEvent(privateApiRef, "rowsScrollEndIntersection", runIf(lazyLoadingRowsUpdateStrategyActive, handleIntersection));
  useGridEvent(privateApiRef, "renderedRowsIntervalChange", runIf(lazyLoadingRowsUpdateStrategyActive, throttledHandleRenderedRowsIntervalChange));
  useGridEvent(privateApiRef, "sortModelChange", runIf(lazyLoadingRowsUpdateStrategyActive, handleGridSortModelChange));
  useGridEvent(privateApiRef, "filterModelChange", runIf(lazyLoadingRowsUpdateStrategyActive, handleGridFilterModelChange));
  useGridEvent(privateApiRef, "rowDragStart", runIf(lazyLoadingRowsUpdateStrategyActive, handleDragStart));
  useGridEvent(privateApiRef, "rowDragEnd", runIf(lazyLoadingRowsUpdateStrategyActive, handleDragEnd));
  React293.useEffect(() => {
    setStrategyAvailability2();
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/serverSideLazyLoader/useGridInfiniteLoadingIntersection.js
var React294 = __toESM(require_react(), 1);
var import_jsx_runtime205 = __toESM(require_jsx_runtime(), 1);
var InfiniteLoadingTriggerElement = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "sticky",
  left: 0,
  width: 0,
  height: 0
});
var useGridInfiniteLoadingIntersection = (apiRef, props) => {
  const observer = React294.useRef(null);
  const updateTargetTimeout = useTimeout();
  const triggerElement = React294.useRef(null);
  const isEnabledClientSide = props.rowsLoadingMode === "client" && !!props.onRowsScrollEnd;
  const isEnabledServerSide = props.dataSource && props.lazyLoading;
  const isEnabled = isEnabledClientSide || isEnabledServerSide;
  const isEnabledAndReady = useGridSelector(apiRef, () => isEnabled && gridDimensionsSelector(apiRef).isReady);
  const handleIntersectionChange = useEventCallback_default(([entry]) => {
    var _a;
    const currentRatio = entry.intersectionRatio;
    const isIntersecting = entry.isIntersecting;
    if (isIntersecting && currentRatio === 1) {
      (_a = observer.current) == null ? void 0 : _a.disconnect();
      triggerElement.current = null;
      apiRef.current.publishEvent("rowsScrollEndIntersection");
    }
  });
  React294.useEffect(() => {
    var _a;
    const virtualScroller = apiRef.current.virtualScrollerRef.current;
    if (!isEnabledAndReady || !virtualScroller) {
      return;
    }
    (_a = observer.current) == null ? void 0 : _a.disconnect();
    const horizontalScrollbarHeight = gridHorizontalScrollbarHeightSelector(apiRef);
    const marginBottom = props.scrollEndThreshold - horizontalScrollbarHeight;
    observer.current = new IntersectionObserver(handleIntersectionChange, {
      threshold: 1,
      root: virtualScroller,
      rootMargin: `0px 0px ${marginBottom}px 0px`
    });
    if (triggerElement.current) {
      observer.current.observe(triggerElement.current);
    }
  }, [apiRef, handleIntersectionChange, isEnabledAndReady, props.scrollEndThreshold]);
  const updateTarget = (node) => {
    var _a, _b;
    if (triggerElement.current !== node) {
      (_a = observer.current) == null ? void 0 : _a.disconnect();
      triggerElement.current = node;
      if (triggerElement.current) {
        (_b = observer.current) == null ? void 0 : _b.observe(triggerElement.current);
      }
    }
  };
  const triggerRef = React294.useCallback((node) => {
    if (!isEnabled) {
      return;
    }
    updateTargetTimeout.start(0, () => updateTarget(node));
  }, [isEnabled, updateTargetTimeout]);
  const getInfiniteLoadingTriggerElement = React294.useCallback(({
    lastRowId
  }) => {
    if (!isEnabled) {
      return null;
    }
    return (0, import_jsx_runtime205.jsx)(InfiniteLoadingTriggerElement, {
      ref: triggerRef,
      role: "presentation"
    }, `trigger-${lastRowId}`);
  }, [isEnabled, triggerRef]);
  const infiniteLoaderPrivateApi = {
    getInfiniteLoadingTriggerElement
  };
  useGridApiMethod(apiRef, infiniteLoaderPrivateApi, "private");
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/useGridDataSourcePro.js
var React296 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/useGridDataSourceBasePro.js
var React295 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/utils.js
var MAX_CONCURRENT_REQUESTS = Infinity;
var RequestStatus = (function(RequestStatus2) {
  RequestStatus2[RequestStatus2["QUEUED"] = 0] = "QUEUED";
  RequestStatus2[RequestStatus2["PENDING"] = 1] = "PENDING";
  RequestStatus2[RequestStatus2["SETTLED"] = 2] = "SETTLED";
  RequestStatus2[RequestStatus2["UNKNOWN"] = 3] = "UNKNOWN";
  return RequestStatus2;
})({});
var NestedDataManager = class {
  constructor(privateApiRef, maxConcurrentRequests = MAX_CONCURRENT_REQUESTS) {
    __publicField(this, "pendingRequests", /* @__PURE__ */ (() => /* @__PURE__ */ new Set())());
    __publicField(this, "queuedRequests", /* @__PURE__ */ (() => /* @__PURE__ */ new Set())());
    __publicField(this, "settledRequests", /* @__PURE__ */ (() => /* @__PURE__ */ new Set())());
    __publicField(this, "processQueue", async () => {
      if (this.queuedRequests.size === 0 || this.pendingRequests.size >= this.maxConcurrentRequests) {
        return;
      }
      const loopLength = Math.min(this.maxConcurrentRequests - this.pendingRequests.size, this.queuedRequests.size);
      if (loopLength === 0) {
        return;
      }
      const fetchQueue = Array.from(this.queuedRequests);
      for (let i2 = 0; i2 < loopLength; i2 += 1) {
        const id = fetchQueue[i2];
        this.queuedRequests.delete(id);
        this.pendingRequests.add(id);
        this.api.fetchRowChildren(id);
      }
    });
    __publicField(this, "queue", async (ids, options3 = {}) => {
      const {
        showChildrenLoading = true
      } = options3;
      const loadingIds = {};
      ids.forEach((id) => {
        this.queuedRequests.add(id);
        if (showChildrenLoading) {
          loadingIds[id] = true;
        }
      });
      if (showChildrenLoading) {
        this.api.setState((state) => _extends({}, state, {
          dataSource: _extends({}, state.dataSource, {
            loading: _extends({}, state.dataSource.loading, loadingIds)
          })
        }));
      }
      this.processQueue();
    });
    __publicField(this, "setRequestSettled", (id) => {
      this.pendingRequests.delete(id);
      this.settledRequests.add(id);
      this.processQueue();
    });
    __publicField(this, "clear", () => {
      this.queuedRequests.clear();
      Array.from(this.pendingRequests).forEach((id) => this.clearPendingRequest(id));
    });
    __publicField(this, "clearPendingRequest", (id) => {
      this.api.dataSource.setChildrenLoading(id, false);
      this.pendingRequests.delete(id);
      this.processQueue();
    });
    __publicField(this, "getRequestStatus", (id) => {
      if (this.pendingRequests.has(id)) {
        return RequestStatus.PENDING;
      }
      if (this.queuedRequests.has(id)) {
        return RequestStatus.QUEUED;
      }
      if (this.settledRequests.has(id)) {
        return RequestStatus.SETTLED;
      }
      return RequestStatus.UNKNOWN;
    });
    __publicField(this, "getActiveRequestsCount", () => this.pendingRequests.size + this.queuedRequests.size);
    this.api = privateApiRef.current;
    this.maxConcurrentRequests = maxConcurrentRequests;
  }
};
var getGroupKeys = (tree, rowId) => {
  const rowNode = tree[rowId];
  let currentNodeId = rowNode.parent;
  const groupKeys = [];
  while (currentNodeId && currentNodeId !== GRID_ROOT_GROUP_ID) {
    const currentNode = tree[currentNodeId];
    groupKeys.push(currentNode.groupingKey ?? "");
    currentNodeId = currentNode.parent;
  }
  return groupKeys.reverse();
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/useGridDataSourceBasePro.js
var INITIAL_STATE = {
  loading: {},
  errors: {}
};
var useGridDataSourceBasePro = (apiRef, props, options3 = {}) => {
  var _a;
  const groupsToAutoFetch = useGridSelector(apiRef, gridRowGroupsToFetchSelector);
  const nestedDataManager = useLazyRef(() => new NestedDataManager(apiRef)).current;
  const scheduledGroups = React295.useRef(0);
  const clearDataSourceState = React295.useCallback(() => {
    nestedDataManager.clear();
    scheduledGroups.current = 0;
    const dataSourceState = apiRef.current.state.dataSource;
    if (dataSourceState !== INITIAL_STATE) {
      apiRef.current.resetDataSourceState();
    }
    return null;
  }, [apiRef, nestedDataManager]);
  const handleEditRow = React295.useCallback((params, updatedRow) => {
    if (updatedRow && !isDeepEqual(updatedRow, params.previousRow)) {
      apiRef.current.dataSource.cache.clear();
    }
    const groupKeys = getGroupKeys(gridRowTreeSelector(apiRef), params.rowId);
    apiRef.current.updateNestedRows([updatedRow], groupKeys);
  }, [apiRef]);
  const {
    api,
    debouncedFetchRows,
    strategyProcessor: flatTreeStrategyProcessor,
    events,
    startPolling,
    cacheChunkManager,
    cache
  } = useGridDataSourceBase(apiRef, props, _extends({
    fetchRowChildren: nestedDataManager.queue,
    clearDataSourceState,
    handleEditRow
  }, options3));
  const setStrategyAvailability2 = React295.useCallback(() => {
    const currentStrategy = props.treeData ? DataSourceRowsUpdateStrategy.GroupedData : DataSourceRowsUpdateStrategy.Default;
    const prevStrategy = currentStrategy === DataSourceRowsUpdateStrategy.GroupedData ? DataSourceRowsUpdateStrategy.Default : DataSourceRowsUpdateStrategy.GroupedData;
    apiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, prevStrategy, () => false);
    apiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, currentStrategy, props.dataSource && !props.lazyLoading ? () => true : () => false);
  }, [apiRef, props.dataSource, props.lazyLoading, props.treeData]);
  const onDataSourceErrorProp = props.onDataSourceError;
  const replaceGroupRows = React295.useCallback((groupId, groupPath, rows) => {
    const tree = gridRowTreeSelector(apiRef);
    const rowsLookup = gridRowsLookupSelector(apiRef);
    const fetchedRowIds = new Set(rows.map((row) => gridRowIdSelector(apiRef, row)));
    const currentGroupRows = getTreeNodeDescendants(tree, groupId, false, true);
    const rowsToDelete = [];
    currentGroupRows.forEach((rowId) => {
      if (fetchedRowIds.has(rowId)) {
        return;
      }
      const descendants = getTreeNodeDescendants(tree, rowId, false, false);
      for (let i2 = descendants.length - 1; i2 >= 0; i2 -= 1) {
        const descendantId = descendants[i2];
        if (fetchedRowIds.has(descendantId)) {
          continue;
        }
        rowsToDelete.push(_extends({}, rowsLookup[descendantId], {
          _action: "delete"
        }));
      }
      rowsToDelete.push(_extends({}, rowsLookup[rowId], {
        _action: "delete"
      }));
    });
    if (rowsToDelete.length > 0) {
      apiRef.current.updateNestedRows(rowsToDelete, groupPath);
    }
    apiRef.current.updateNestedRows(rows, groupPath);
  }, [apiRef]);
  const fetchRowChildren = React295.useCallback(async (id) => {
    var _a2, _b;
    const pipedParams = apiRef.current.unstable_applyPipeProcessors("getRowsParams", {});
    if (!props.treeData && (((_a2 = pipedParams.groupFields) == null ? void 0 : _a2.length) ?? 0) === 0) {
      nestedDataManager.clearPendingRequest(id);
      return;
    }
    const getRows = (_b = props.dataSource) == null ? void 0 : _b.getRows;
    if (!getRows) {
      nestedDataManager.clearPendingRequest(id);
      return;
    }
    const rowNode = apiRef.current.getRowNode(id);
    if (!rowNode) {
      nestedDataManager.clearPendingRequest(id);
      return;
    }
    const fetchParams = _extends({}, gridGetRowsParamsSelector(apiRef), pipedParams, {
      groupKeys: rowNode.path
    });
    const cacheKeys = cacheChunkManager.getCacheKeys(fetchParams);
    const responses = cacheKeys.map((cacheKey) => cache.get(cacheKey));
    const cachedData = responses.some((response) => response === void 0) ? void 0 : CacheChunkManager.mergeResponses(responses);
    if (cachedData !== void 0) {
      const rows = cachedData.rows;
      nestedDataManager.setRequestSettled(id);
      replaceGroupRows(id, rowNode.path, rows);
      if (cachedData.rowCount !== void 0) {
        apiRef.current.setRowCount(cachedData.rowCount);
      }
      apiRef.current.setRowChildrenExpansion(id, true);
      apiRef.current.dataSource.setChildrenLoading(id, false);
      return;
    }
    const existingError = gridDataSourceErrorsSelector(apiRef)[id] ?? null;
    if (existingError) {
      apiRef.current.dataSource.setChildrenFetchError(id, null);
    }
    try {
      const getRowsResponse = await getRows(fetchParams);
      if (!apiRef.current.getRowNode(id)) {
        nestedDataManager.clearPendingRequest(id);
        return;
      }
      if (nestedDataManager.getRequestStatus(id) === RequestStatus.UNKNOWN) {
        apiRef.current.dataSource.setChildrenLoading(id, false);
        return;
      }
      nestedDataManager.setRequestSettled(id);
      const cacheResponses = cacheChunkManager.splitResponse(fetchParams, getRowsResponse);
      cacheResponses.forEach((response, key) => {
        cache.set(key, response);
      });
      if (getRowsResponse.rowCount !== void 0) {
        apiRef.current.setRowCount(getRowsResponse.rowCount);
      }
      replaceGroupRows(id, rowNode.path, getRowsResponse.rows);
      apiRef.current.setRowChildrenExpansion(id, true);
    } catch (error) {
      const childrenFetchError = error;
      apiRef.current.dataSource.setChildrenFetchError(id, childrenFetchError);
      if (typeof onDataSourceErrorProp === "function") {
        onDataSourceErrorProp(new GridGetRowsError({
          message: childrenFetchError.message,
          params: fetchParams,
          cause: childrenFetchError
        }));
      } else {
        warnOnce(["MUI X: A call to `dataSource.getRows()` threw an error which was not handled because `onDataSourceError()` is missing.", "To handle the error pass a callback to the `onDataSourceError` prop, for example `<DataGrid onDataSourceError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/server-side-data/#error-handling."], "error");
      }
    } finally {
      apiRef.current.dataSource.setChildrenLoading(id, false);
      nestedDataManager.setRequestSettled(id);
    }
  }, [nestedDataManager, cacheChunkManager, cache, onDataSourceErrorProp, replaceGroupRows, apiRef, props.treeData, (_a = props.dataSource) == null ? void 0 : _a.getRows]);
  const setChildrenLoading = React295.useCallback((parentId, isLoading) => {
    apiRef.current.setState((state) => {
      if (!state.dataSource.loading[parentId] && isLoading === false) {
        return state;
      }
      const newLoadingState = _extends({}, state.dataSource.loading);
      if (isLoading === false) {
        delete newLoadingState[parentId];
      } else {
        newLoadingState[parentId] = isLoading;
      }
      return _extends({}, state, {
        dataSource: _extends({}, state.dataSource, {
          loading: newLoadingState
        })
      });
    });
  }, [apiRef]);
  const setChildrenFetchError = React295.useCallback((parentId, error) => {
    apiRef.current.setState((state) => {
      const newErrorsState = _extends({}, state.dataSource.errors);
      if (error === null && newErrorsState[parentId] !== void 0) {
        delete newErrorsState[parentId];
      } else {
        newErrorsState[parentId] = error;
      }
      return _extends({}, state, {
        dataSource: _extends({}, state.dataSource, {
          errors: newErrorsState
        })
      });
    });
  }, [apiRef]);
  const resetDataSourceState = React295.useCallback(() => {
    apiRef.current.setState((state) => {
      return _extends({}, state, {
        dataSource: INITIAL_STATE
      });
    });
  }, [apiRef]);
  const removeChildrenRows = React295.useCallback((parentId) => {
    const rowsLookup = gridRowsLookupSelector(apiRef);
    const rowNode = gridRowNodeSelector(apiRef, parentId);
    if (!rowNode || rowNode.type !== "group" || rowNode.children.length === 0) {
      return;
    }
    const removedRows = [];
    const traverse = (nodeId) => {
      const node = gridRowNodeSelector(apiRef, nodeId);
      if (!node) {
        return;
      }
      if (node.type === "group" && node.children.length > 0) {
        node.children.forEach(traverse);
      }
      removedRows.push(_extends({}, rowsLookup[nodeId], {
        _action: "delete"
      }));
    };
    rowNode.children.forEach(traverse);
    if (removedRows.length > 0) {
      apiRef.current.updateNestedRows(removedRows, rowNode.path);
    }
  }, [apiRef]);
  const handleGroupedDataUpdate = React295.useCallback((params) => {
    if ("error" in params) {
      apiRef.current.setRows([]);
      return;
    }
    const {
      response,
      options: {
        keepChildrenExpanded
      }
    } = params;
    if (response.rowCount !== void 0) {
      apiRef.current.setRowCount(response.rowCount);
    }
    if (keepChildrenExpanded === false) {
      apiRef.current.setRows(response.rows);
    } else {
      const tree = gridRowTreeSelector(apiRef);
      const rowsLookup = gridRowsLookupSelector(apiRef);
      const parentRowsToDelete = new Set(getTreeNodeDescendants(tree, GRID_ROOT_GROUP_ID, false, true));
      response.rows.forEach((row) => {
        parentRowsToDelete.delete(gridRowIdSelector(apiRef, row));
      });
      const rowsToDelete = [];
      if (parentRowsToDelete.size > 0) {
        parentRowsToDelete.forEach((parentRowId) => {
          const descendants = getTreeNodeDescendants(tree, parentRowId, false, false);
          for (let i2 = descendants.length - 1; i2 >= 0; i2 -= 1) {
            rowsToDelete.push(_extends({}, rowsLookup[descendants[i2]], {
              _action: "delete"
            }));
          }
          rowsToDelete.push(_extends({}, rowsLookup[parentRowId], {
            _action: "delete"
          }));
        });
      }
      apiRef.current.updateRows(response.rows.concat(rowsToDelete));
    }
    apiRef.current.unstable_applyPipeProcessors("processDataSourceRows", {
      params: params.fetchParams,
      response
    }, true);
    startPolling();
  }, [apiRef, startPolling]);
  const dataSourceApi = {
    dataSource: _extends({}, api.public.dataSource, {
      setChildrenLoading,
      setChildrenFetchError
    })
  };
  const dataSourcePrivateApi = {
    fetchRowChildren,
    resetDataSourceState,
    removeChildrenRows
  };
  React295.useEffect(() => {
    if (groupsToAutoFetch && groupsToAutoFetch.length && scheduledGroups.current < groupsToAutoFetch.length) {
      const groupsToSchedule = groupsToAutoFetch.slice(scheduledGroups.current);
      nestedDataManager.queue(groupsToSchedule);
      scheduledGroups.current = groupsToAutoFetch.length;
    }
  }, [apiRef, nestedDataManager, groupsToAutoFetch]);
  return {
    api: {
      public: dataSourceApi,
      private: dataSourcePrivateApi
    },
    debouncedFetchRows,
    flatTreeStrategyProcessor,
    groupedDataStrategyProcessor: {
      strategyName: DataSourceRowsUpdateStrategy.GroupedData,
      group: "dataSourceRowsUpdate",
      processor: handleGroupedDataUpdate
    },
    events,
    setStrategyAvailability: setStrategyAvailability2,
    cacheChunkManager,
    cache
  };
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/dataSource/useGridDataSourcePro.js
function getKeyPro(params) {
  return JSON.stringify([params.filterModel, params.sortModel, params.groupKeys, params.start, params.end]);
}
var dataSourceStateInitializer = (state) => {
  return _extends({}, state, {
    dataSource: INITIAL_STATE
  });
};
var options = {
  cacheOptions: {
    getKey: getKeyPro
  }
};
var useGridDataSourcePro = (apiRef, props) => {
  const {
    api,
    flatTreeStrategyProcessor,
    groupedDataStrategyProcessor,
    events,
    setStrategyAvailability: setStrategyAvailability2
  } = useGridDataSourceBasePro(apiRef, props, options);
  useGridApiMethod(apiRef, api.public, "public");
  useGridApiMethod(apiRef, api.private, "private");
  useGridRegisterStrategyProcessor(apiRef, flatTreeStrategyProcessor.strategyName, flatTreeStrategyProcessor.group, flatTreeStrategyProcessor.processor);
  useGridRegisterStrategyProcessor(apiRef, groupedDataStrategyProcessor.strategyName, groupedDataStrategyProcessor.group, groupedDataStrategyProcessor.processor);
  Object.entries(events).forEach(([event, handler]) => {
    useGridEvent(apiRef, event, handler);
  });
  React296.useEffect(() => {
    setStrategyAvailability2();
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid-pro/esm/internals/propValidation.js
var propValidatorsDataGridPro = [...propValidatorsDataGrid, (props) => props.pagination && props.hideFooterRowCount && "MUI X: The `hideFooterRowCount` prop has no effect when the pagination is enabled." || void 0, (props) => props.treeData && props.filterMode === "server" && !props.dataSource && 'MUI X: The `filterMode="server"` prop is not available when the `treeData` is enabled.' || void 0, (props) => !props.pagination && props.checkboxSelectionVisibleOnly && "MUI X: The `checkboxSelectionVisibleOnly` prop has no effect when the pagination is not enabled." || void 0, (props) => props.signature !== GridSignature.DataGrid && props.paginationMode === "client" && props.rowsLoadingMode !== "server" && isNumber(props.rowCount) && 'MUI X: Usage of the `rowCount` prop with client side pagination (`paginationMode="client"`) has no effect. `rowCount` is only meant to be used with `paginationMode="server"`.' || void 0, (props) => props.signature !== GridSignature.DataGrid && (props.rowsLoadingMode === "server" || props.onRowsScrollEnd) && props.lazyLoading && 'MUI X: Usage of the client side lazy loading (`rowsLoadingMode="server"` or `onRowsScrollEnd=...`) cannot be used together with server side lazy loading `lazyLoading="true"`.' || void 0];

// node_modules/@mui/x-data-grid-pro/esm/internals/index.js
var RowGroupingStrategy = (function(RowGroupingStrategy2) {
  RowGroupingStrategy2["Default"] = "grouping-columns";
  RowGroupingStrategy2["DataSource"] = "grouping-columns-data-source";
  return RowGroupingStrategy2;
})({});

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/removeDataRowFromTree.js
var removeNode = ({
  node,
  tree,
  treeDepths
}) => {
  removeNodeFromTree({
    node,
    tree,
    treeDepths
  });
  if (node.type === "group" && node.footerId != null) {
    removeNodeFromTree({
      node: tree[node.footerId],
      tree,
      treeDepths
    });
  }
};
var removeNodeAndCleanParent = ({
  node,
  tree,
  treeDepths
}) => {
  removeNode({
    node,
    tree,
    treeDepths
  });
  const parentNode = tree[node.parent];
  const shouldDeleteGroup = parentNode.id !== GRID_ROOT_GROUP_ID && parentNode.children.length === 0;
  if (shouldDeleteGroup) {
    if (parentNode.isAutoGenerated) {
      removeNodeAndCleanParent({
        node: parentNode,
        tree,
        treeDepths
      });
    } else {
      tree[parentNode.id] = {
        type: "leaf",
        id: parentNode.id,
        depth: parentNode.depth,
        parent: parentNode.parent,
        groupingKey: parentNode.groupingKey
      };
    }
  }
};
var replaceDataGroupWithAutoGeneratedGroup = ({
  node,
  tree,
  treeDepths
}) => {
  updateGroupNodeIdAndAutoGenerated({
    previousTree: null,
    tree,
    treeDepths,
    node,
    updatedNode: {
      id: getGroupRowIdFromPath(getNodePathInTree({
        id: node.id,
        tree
      })),
      isAutoGenerated: true
    }
  });
};
var removeDataRowFromTree = ({
  id,
  tree,
  treeDepths,
  groupingName
}) => {
  const node = tree[id];
  if (node.type === "group" && node.children.length > 0) {
    replaceDataGroupWithAutoGeneratedGroup({
      node,
      tree,
      treeDepths
    });
  } else if (groupingName === TreeDataStrategy.Default || groupingName === RowGroupingStrategy.Default) {
    removeNodeAndCleanParent({
      node,
      tree,
      treeDepths
    });
  } else {
    removeNode({
      node,
      tree,
      treeDepths
    });
  }
};

// node_modules/@mui/x-data-grid-pro/esm/utils/tree/updateRowTree.js
var updateRowTree = (params) => {
  const tree = _extends({}, params.previousTree);
  const treeDepths = _extends({}, params.previousTreeDepth);
  const groupsToFetch = params.previousGroupsToFetch ? /* @__PURE__ */ new Set([...params.previousGroupsToFetch]) : /* @__PURE__ */ new Set([]);
  for (let i2 = 0; i2 < params.nodes.inserted.length; i2 += 1) {
    const {
      id,
      path,
      serverChildrenCount
    } = params.nodes.inserted[i2];
    insertDataRowInTree({
      previousTree: params.previousTree,
      tree,
      treeDepths,
      id,
      path,
      serverChildrenCount,
      onDuplicatePath: params.onDuplicatePath,
      isGroupExpandedByDefault: params.isGroupExpandedByDefault,
      defaultGroupingExpansionDepth: params.defaultGroupingExpansionDepth,
      groupsToFetch,
      maxDepth: params.maxDepth
    });
  }
  for (let i2 = 0; i2 < params.nodes.removed.length; i2 += 1) {
    const nodeId = params.nodes.removed[i2];
    removeDataRowFromTree({
      tree,
      treeDepths,
      id: nodeId,
      groupingName: params.groupingName
    });
  }
  for (let i2 = 0; i2 < params.nodes.modified.length; i2 += 1) {
    const {
      id,
      path,
      serverChildrenCount
    } = params.nodes.modified[i2];
    const pathInPreviousTree = getNodePathInTree({
      tree,
      id
    });
    const isInSameGroup = isDeepEqual(pathInPreviousTree, path);
    if (!isInSameGroup) {
      removeDataRowFromTree({
        tree,
        treeDepths,
        id,
        groupingName: params.groupingName
      });
      insertDataRowInTree({
        previousTree: params.previousTree,
        tree,
        treeDepths,
        id,
        path,
        serverChildrenCount,
        onDuplicatePath: params.onDuplicatePath,
        isGroupExpandedByDefault: params.isGroupExpandedByDefault,
        defaultGroupingExpansionDepth: params.defaultGroupingExpansionDepth,
        groupsToFetch,
        maxDepth: params.maxDepth
      });
    }
  }
  const dataRowIds = getTreeNodeDescendants(tree, GRID_ROOT_GROUP_ID, true);
  return {
    tree,
    treeDepths,
    groupingName: params.groupingName,
    dataRowIds,
    groupsToFetch: Array.from(groupsToFetch)
  };
};

// node_modules/@mui/x-data-grid-pro/esm/hooks/features/treeData/useGridTreeDataPreProcessors.js
var import_jsx_runtime206 = __toESM(require_jsx_runtime(), 1);
var _excluded76 = ["hideDescendantCount"];
var useGridTreeDataPreProcessors = (privateApiRef, props) => {
  const setStrategyAvailability2 = React297.useCallback(() => {
    privateApiRef.current.setStrategyAvailability(GridStrategyGroup.RowTree, TreeDataStrategy.Default, props.treeData && !props.dataSource ? () => true : () => false);
  }, [privateApiRef, props.treeData, props.dataSource]);
  const getGroupingColDef = React297.useCallback(() => {
    const groupingColDefProp = props.groupingColDef;
    let colDefOverride;
    if (typeof groupingColDefProp === "function") {
      const params = {
        groupingName: TreeDataStrategy.Default,
        fields: []
      };
      colDefOverride = groupingColDefProp(params);
    } else {
      colDefOverride = groupingColDefProp;
    }
    const _ref = colDefOverride ?? {}, {
      hideDescendantCount
    } = _ref, colDefOverrideProperties = _objectWithoutPropertiesLoose(_ref, _excluded76);
    const commonProperties = _extends({}, GRID_TREE_DATA_GROUPING_COL_DEF, {
      renderCell: (params) => (0, import_jsx_runtime206.jsx)(GridTreeDataGroupingCell, _extends({}, params, {
        hideDescendantCount
      })),
      headerName: privateApiRef.current.getLocaleText("treeDataGroupingHeaderName")
    });
    return _extends({}, commonProperties, colDefOverrideProperties, GRID_TREE_DATA_GROUPING_COL_DEF_FORCED_PROPERTIES);
  }, [privateApiRef, props.groupingColDef]);
  const updateGroupingColumn = React297.useCallback((columnsState) => {
    if (props.dataSource) {
      return columnsState;
    }
    const groupingColDefField = GRID_TREE_DATA_GROUPING_COL_DEF_FORCED_PROPERTIES.field;
    const shouldHaveGroupingColumn = props.treeData;
    const prevGroupingColumn = columnsState.lookup[groupingColDefField];
    if (shouldHaveGroupingColumn) {
      const newGroupingColumn = getGroupingColDef();
      if (prevGroupingColumn) {
        newGroupingColumn.width = prevGroupingColumn.width;
        newGroupingColumn.flex = prevGroupingColumn.flex;
      }
      columnsState.lookup[groupingColDefField] = newGroupingColumn;
      if (prevGroupingColumn == null) {
        columnsState.orderedFields = [groupingColDefField, ...columnsState.orderedFields];
      }
    } else if (!shouldHaveGroupingColumn && prevGroupingColumn) {
      delete columnsState.lookup[groupingColDefField];
      columnsState.orderedFields = columnsState.orderedFields.filter((field) => field !== groupingColDefField);
    }
    return columnsState;
  }, [props.treeData, props.dataSource, getGroupingColDef]);
  const createRowTreeForTreeData = React297.useCallback((params) => {
    if (!props.getTreeDataPath) {
      throw new Error("MUI X: No getTreeDataPath given.");
    }
    const getRowTreeBuilderNode = (rowId) => ({
      id: rowId,
      path: props.getTreeDataPath(params.dataRowIdToModelLookup[rowId]).map((key) => ({
        key,
        field: null
      }))
    });
    const onDuplicatePath = (firstId, secondId, path) => {
      throw new Error(["MUI X: The path returned by `getTreeDataPath` should be unique.", `The rows with id #${firstId} and #${secondId} have the same.`, `Path: ${JSON.stringify(path.map((step) => step.key))}.`].join("\n"));
    };
    if (params.updates.type === "full") {
      return createRowTree({
        previousTree: params.previousTree,
        nodes: params.updates.rows.map(getRowTreeBuilderNode),
        defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
        isGroupExpandedByDefault: props.isGroupExpandedByDefault,
        groupingName: TreeDataStrategy.Default,
        onDuplicatePath
      });
    }
    return updateRowTree({
      nodes: {
        inserted: params.updates.actions.insert.map(getRowTreeBuilderNode),
        modified: params.updates.actions.modify.map(getRowTreeBuilderNode),
        removed: params.updates.actions.remove
      },
      previousTree: params.previousTree,
      previousTreeDepth: params.previousTreeDepths,
      defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
      isGroupExpandedByDefault: props.isGroupExpandedByDefault,
      groupingName: TreeDataStrategy.Default
    });
  }, [props.getTreeDataPath, props.defaultGroupingExpansionDepth, props.isGroupExpandedByDefault]);
  const filterRows = React297.useCallback((params) => {
    const rowTree = gridRowTreeSelector(privateApiRef);
    return filterRowTreeFromTreeData({
      rowTree,
      isRowMatchingFilters: params.isRowMatchingFilters,
      disableChildrenFiltering: props.disableChildrenFiltering,
      filterModel: params.filterModel,
      filterValueGetter: params.filterValueGetter,
      apiRef: privateApiRef
    });
  }, [privateApiRef, props.disableChildrenFiltering]);
  const sortRows = React297.useCallback((params) => {
    const rowTree = gridRowTreeSelector(privateApiRef);
    return sortRowTree({
      rowTree,
      sortRowList: params.sortRowList,
      disableChildrenSorting: props.disableChildrenSorting,
      shouldRenderGroupBelowLeaves: false
    });
  }, [privateApiRef, props.disableChildrenSorting]);
  useGridRegisterPipeProcessor(privateApiRef, "hydrateColumns", updateGroupingColumn);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.Default, "rowTreeCreation", createRowTreeForTreeData);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.Default, "filtering", filterRows);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.Default, "sorting", sortRows);
  useGridRegisterStrategyProcessor(privateApiRef, TreeDataStrategy.Default, "visibleRowsLookupCreation", getVisibleRowsLookup2);
  useFirstRender(() => {
    setStrategyAvailability2();
  });
  const isFirstRender = React297.useRef(true);
  React297.useEffect(() => {
    if (!isFirstRender.current) {
      setStrategyAvailability2();
    } else {
      isFirstRender.current = false;
    }
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid-pro/esm/DataGridPro/useDataGridProComponent.js
var useDataGridProComponent = (apiRef, props, configuration4) => {
  useGridInitialization(apiRef, props);
  useGridDetailPanelPreProcessors(apiRef, props);
  useGridTreeDataPreProcessors(apiRef, props);
  useGridDataSourceTreeDataPreProcessors(apiRef, props);
  useGridRowSelectionPreProcessors(apiRef, props);
  useGridLazyLoaderPreProcessors(apiRef, props);
  useGridRowPinningPreProcessors(apiRef);
  useGridRowReorderPreProcessors(apiRef, props);
  useGridColumnPinningPreProcessors(apiRef, props);
  useGridRowsPreProcessors(apiRef);
  useGridInitializeState(propsStateInitializer, apiRef, props);
  useGridInitializeState(headerFilteringStateInitializer, apiRef, props);
  useGridInitializeState(rowSelectionStateInitializer, apiRef, props);
  useGridInitializeState(rowReorderStateInitializer, apiRef, props);
  useGridInitializeState(detailPanelStateInitializer, apiRef, props);
  useGridInitializeState(columnPinningStateInitializer, apiRef, props);
  useGridInitializeState(columnsStateInitializer, apiRef, props);
  useGridInitializeState(rowPinningStateInitializer, apiRef, props);
  useGridInitializeState(rowsStateInitializer, apiRef, props);
  useGridInitializeState(paginationStateInitializer, apiRef, props);
  useGridInitializeState(editingStateInitializer, apiRef, props);
  useGridInitializeState(focusStateInitializer, apiRef, props);
  useGridInitializeState(sortingStateInitializer, apiRef, props);
  useGridInitializeState(preferencePanelStateInitializer, apiRef, props);
  useGridInitializeState(filterStateInitializer, apiRef, props);
  useGridInitializeState(rowSpanningStateInitializer, apiRef, props);
  useGridInitializeState(densityStateInitializer, apiRef, props);
  useGridInitializeState(columnReorderStateInitializer, apiRef, props);
  useGridInitializeState(columnResizeStateInitializer, apiRef, props);
  useGridInitializeState(columnMenuStateInitializer, apiRef, props);
  useGridInitializeState(columnGroupsStateInitializer, apiRef, props);
  useGridInitializeState(virtualizationStateInitializer, apiRef, props);
  useGridInitializeState(dataSourceStateInitializer, apiRef, props);
  useGridInitializeState(dimensionsStateInitializer, apiRef, props);
  useGridInitializeState(rowsMetaStateInitializer, apiRef, props);
  useGridInitializeState(listViewStateInitializer, apiRef, props);
  useGridHeaderFiltering(apiRef, props);
  useGridTreeData(apiRef, props);
  useGridKeyboardNavigation(apiRef, props);
  useGridRowSelection(apiRef, props);
  useGridColumnPinning(apiRef, props);
  useGridRowPinning(apiRef, props);
  useGridColumns(apiRef, props);
  useGridRows(apiRef, props, configuration4);
  useGridRowSpanning(apiRef, props);
  useGridParamsApi(apiRef, props, configuration4);
  useGridDetailPanel(apiRef, props);
  useGridColumnSpanning(apiRef);
  useGridColumnGrouping(apiRef, props);
  useGridEditing(apiRef, props, configuration4);
  useGridFocus(apiRef, props);
  useGridPreferencesPanel(apiRef, props);
  useGridFilter(apiRef, props, configuration4);
  useGridSorting(apiRef, props);
  useGridDensity(apiRef, props);
  useGridColumnReorder(apiRef, props);
  useGridColumnResize(apiRef, props);
  useGridPagination(apiRef, props);
  useGridRowReorder(apiRef, props);
  useGridScroll(apiRef, props);
  useGridInfiniteLoader(apiRef, props);
  useGridLazyLoader(apiRef, props);
  useGridDataSourceLazyLoader(apiRef, props);
  useGridInfiniteLoadingIntersection(apiRef, props);
  useGridColumnMenu(apiRef);
  useGridCsvExport(apiRef, props);
  useGridPrintExport(apiRef, props);
  useGridClipboard(apiRef, props);
  useGridDimensions(apiRef, props);
  useGridEvents(apiRef, props);
  useGridStatePersistence(apiRef);
  useGridVirtualization(apiRef, props);
  useGridDataSourcePro(apiRef, props);
  useGridListView(apiRef, props);
  useFirstRender(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
  React298.useEffect(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
};

// node_modules/@mui/x-data-grid-pro/esm/DataGridPro/useDataGridProProps.js
var React299 = __toESM(require_react(), 1);
var getDataGridProForcedProps = (themedProps) => _extends({
  signature: "DataGridPro"
}, themedProps.dataSource ? {
  filterMode: "server",
  sortingMode: "server",
  paginationMode: "server"
} : {});
var DATA_GRID_PRO_PROPS_DEFAULT_VALUES = _extends({}, DATA_GRID_PROPS_DEFAULT_VALUES, {
  autosizeOnMount: false,
  defaultGroupingExpansionDepth: 0,
  disableAutosize: false,
  disableChildrenFiltering: false,
  disableChildrenSorting: false,
  disableColumnPinning: false,
  getDetailPanelHeight: () => 500,
  headerFilters: false,
  keepColumnPositionIfDraggedOutside: false,
  rowSelectionPropagation: ROW_SELECTION_PROPAGATION_DEFAULT,
  rowReordering: false,
  rowsLoadingMode: "client",
  scrollEndThreshold: 80,
  treeData: false,
  lazyLoading: false,
  lazyLoadingRequestThrottleMs: 500,
  dataSourceRevalidateMs: 0,
  listView: false,
  multipleColumnsSortingMode: "withModifierKey",
  pinnedColumnsSectionSeparator: "border-and-shadow",
  pinnedRowsSectionSeparator: "border-and-shadow"
});
var defaultSlots2 = DATA_GRID_PRO_DEFAULT_SLOTS_COMPONENTS;
var useDataGridProProps = (inProps) => {
  const theme = useTheme();
  const themedProps = React299.useMemo(() => getThemeProps({
    props: inProps,
    theme,
    name: "MuiDataGrid"
  }), [theme, inProps]);
  const localeText = React299.useMemo(() => _extends({}, GRID_DEFAULT_LOCALE_TEXT, themedProps.localeText), [themedProps.localeText]);
  const slots = React299.useMemo(() => computeSlots({
    defaultSlots: defaultSlots2,
    slots: themedProps.slots
  }), [themedProps.slots]);
  return React299.useMemo(() => _extends({}, DATA_GRID_PRO_PROPS_DEFAULT_VALUES, themedProps, {
    localeText,
    slots
  }, getDataGridProForcedProps(themedProps)), [themedProps, localeText, slots]);
};

// node_modules/@mui/x-data-grid-pro/esm/DataGridPro/DataGridPro.js
var import_jsx_runtime207 = __toESM(require_jsx_runtime(), 1);
var configuration2 = {
  hooks: {
    useCSSVariables: useMaterialCSSVariables,
    useGridAriaAttributes: useGridAriaAttributesPro,
    useGridRowAriaAttributes: useGridRowAriaAttributesPro,
    useGridRowsOverridableMethods: useGridRowsOverridableMethods2,
    useGridParamsOverridableMethods,
    useIsCellEditable,
    useCellAggregationResult: () => null,
    useFilterValueGetter: (apiRef) => apiRef.current.getRowValue
  }
};
var releaseInfo = "MTc3Mzk2NDgwMDAwMA==";
var watermark = (0, import_jsx_runtime207.jsx)(MemoizedWatermark, {
  packageName: "x-data-grid-pro",
  releaseInfo
});
var DataGridProRaw = forwardRef2(function DataGridPro2(inProps, ref) {
  var _a;
  const props = useDataGridProProps(inProps);
  const privateApiRef = useGridApiInitialization(props.apiRef, props);
  useDataGridProComponent(privateApiRef, props, configuration2);
  useLicenseVerifier("x-data-grid-pro", releaseInfo);
  if (true) {
    validateProps(props, propValidatorsDataGridPro);
  }
  return (0, import_jsx_runtime207.jsx)(GridContextProvider, {
    privateApiRef,
    configuration: configuration2,
    props,
    children: (0, import_jsx_runtime207.jsx)(MemoizedGridRoot, _extends({
      className: props.className,
      style: props.style,
      sx: props.sx
    }, (_a = props.slotProps) == null ? void 0 : _a.root, {
      ref,
      children: watermark
    }))
  });
});
if (true) DataGridProRaw.displayName = "DataGridProRaw";
var DataGridPro3 = React300.memo(DataGridProRaw);
if (true) DataGridPro3.displayName = "DataGridPro";
DataGridProRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The ref object that allows grid manipulation. Can be instantiated with `useGridApiRef()`.
   */
  apiRef: import_prop_types149.default.shape({
    current: import_prop_types149.default.object
  }),
  /**
   * The `aria-label` of the Data Grid.
   */
  "aria-label": import_prop_types149.default.string,
  /**
   * The `id` of the element containing a label for the Data Grid.
   */
  "aria-labelledby": import_prop_types149.default.string,
  /**
   * If `true`, the Data Grid height is dynamic and takes as much space as it needs to display all rows.
   * Use it instead of a flex parent container approach, if:
   * - you don't need to set a minimum or maximum height for the Data Grid
   * - you want to avoid the scrollbar flickering when the content changes
   * @default false
   */
  autoHeight: import_prop_types149.default.bool,
  /**
   * If `true`, the pageSize is calculated according to the container size and the max number of rows to avoid rendering a vertical scroll bar.
   * @default false
   */
  autoPageSize: import_prop_types149.default.bool,
  /**
   * If `true`, columns are autosized after the datagrid is mounted.
   * @default false
   */
  autosizeOnMount: import_prop_types149.default.bool,
  /**
   * The options for autosize when user-initiated.
   */
  autosizeOptions: import_prop_types149.default.shape({
    columns: import_prop_types149.default.arrayOf(import_prop_types149.default.string),
    disableColumnVirtualization: import_prop_types149.default.bool,
    expand: import_prop_types149.default.bool,
    includeHeaderFilters: import_prop_types149.default.bool,
    includeHeaders: import_prop_types149.default.bool,
    includeOutliers: import_prop_types149.default.bool,
    outliersFactor: import_prop_types149.default.number
  }),
  /**
   * Controls the modes of the cells.
   */
  cellModesModel: import_prop_types149.default.object,
  /**
   * If `true`, the Data Grid will display an extra column with checkboxes for selecting rows.
   * @default false
   */
  checkboxSelection: import_prop_types149.default.bool,
  /**
   * If `true`, the "Select All" header checkbox selects only the rows on the current page. To be used in combination with `checkboxSelection`.
   * It only works if the pagination is enabled.
   * @default false
   */
  checkboxSelectionVisibleOnly: import_prop_types149.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types149.default.object,
  className: import_prop_types149.default.string,
  /**
   * The character used to separate cell values when copying to the clipboard.
   * @default '\t'
   */
  clipboardCopyCellDelimiter: import_prop_types149.default.string,
  /**
   * Column region in pixels to render before/after the viewport
   * @default 150
   */
  columnBufferPx: import_prop_types149.default.number,
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering in the columns menu.
   * @default 150
   */
  columnFilterDebounceMs: import_prop_types149.default.number,
  /**
   * Sets the height in pixels of the column group headers in the Data Grid.
   * Inherits the `columnHeaderHeight` value if not set.
   */
  columnGroupHeaderHeight: import_prop_types149.default.number,
  columnGroupingModel: import_prop_types149.default.arrayOf(import_prop_types149.default.object),
  /**
   * Sets the height in pixel of the column headers in the Data Grid.
   * @default 56
   */
  columnHeaderHeight: import_prop_types149.default.number,
  /**
   * Set of columns of type [[GridColDef]][].
   */
  columns: import_prop_types149.default.arrayOf(import_prop_types149.default.object).isRequired,
  /**
   * Set the column visibility model of the Data Grid.
   * If defined, the Data Grid will ignore the `hide` property in [[GridColDef]].
   */
  columnVisibilityModel: import_prop_types149.default.object,
  /**
   * The data source of the Data Grid Pro.
   */
  dataSource: import_prop_types149.default.shape({
    getChildrenCount: import_prop_types149.default.func,
    getGroupKey: import_prop_types149.default.func,
    getRows: import_prop_types149.default.func.isRequired,
    updateRow: import_prop_types149.default.func
  }),
  /**
   * Data source cache object.
   */
  dataSourceCache: import_prop_types149.default.shape({
    clear: import_prop_types149.default.func.isRequired,
    get: import_prop_types149.default.func.isRequired,
    set: import_prop_types149.default.func.isRequired
  }),
  /**
   * If positive, the Data Grid will periodically revalidate data source rows by re-fetching them from the server when the cache entry has expired.
   * If the refetched rows are different from the current rows, the grid will update the rows.
   * Set to `0` to disable polling.
   * @default 0
   */
  dataSourceRevalidateMs: import_prop_types149.default.number,
  /**
   * If above 0, the row children will be expanded up to this depth.
   * If equal to -1, all the row children will be expanded.
   * @default 0
   */
  defaultGroupingExpansionDepth: import_prop_types149.default.number,
  /**
   * Set the density of the Data Grid.
   * @default "standard"
   */
  density: import_prop_types149.default.oneOf(["comfortable", "compact", "standard"]),
  /**
   * The row ids to show the detail panel.
   */
  detailPanelExpandedRowIds: import_prop_types149.default.instanceOf(Set),
  /**
   * If `true`, column autosizing on header separator double-click is disabled.
   * @default false
   */
  disableAutosize: import_prop_types149.default.bool,
  /**
   * If `true`, the filtering will only be applied to the top level rows when grouping rows with the `treeData` prop.
   * @default false
   */
  disableChildrenFiltering: import_prop_types149.default.bool,
  /**
   * If `true`, the sorting will only be applied to the top level rows when grouping rows with the `treeData` prop.
   * @default false
   */
  disableChildrenSorting: import_prop_types149.default.bool,
  /**
   * If `true`, column filters are disabled.
   * @default false
   */
  disableColumnFilter: import_prop_types149.default.bool,
  /**
   * If `true`, the column menu is disabled.
   * @default false
   */
  disableColumnMenu: import_prop_types149.default.bool,
  /**
   * If `true`, the column pinning is disabled.
   * @default false
   */
  disableColumnPinning: import_prop_types149.default.bool,
  /**
   * If `true`, reordering columns is disabled.
   * @default false
   */
  disableColumnReorder: import_prop_types149.default.bool,
  /**
   * If `true`, resizing columns is disabled.
   * @default false
   */
  disableColumnResize: import_prop_types149.default.bool,
  /**
   * If `true`, hiding/showing columns is disabled.
   * @default false
   */
  disableColumnSelector: import_prop_types149.default.bool,
  /**
   * If `true`, the column sorting feature will be disabled.
   * @default false
   */
  disableColumnSorting: import_prop_types149.default.bool,
  /**
   * If `true`, the density selector is disabled.
   * @default false
   */
  disableDensitySelector: import_prop_types149.default.bool,
  /**
   * If `true`, `eval()` is not used for performance optimization.
   * @default false
   */
  disableEval: import_prop_types149.default.bool,
  /**
   * If `true`, filtering with multiple columns is disabled.
   * @default false
   */
  disableMultipleColumnsFiltering: import_prop_types149.default.bool,
  /**
   * If `true`, the sorting with multiple columns is disabled.
   * @default false
   */
  disableMultipleColumnsSorting: import_prop_types149.default.bool,
  /**
   * If `true`, multiple selection using the Ctrl/CMD or Shift key is disabled.
   * The MIT DataGrid will ignore this prop, unless `checkboxSelection` is enabled.
   * @default false (`!props.checkboxSelection` for MIT Data Grid)
   */
  disableMultipleRowSelection: import_prop_types149.default.bool,
  /**
   * If `true`, the Data Grid will not use the exclude model optimization when selecting all rows.
   * @default false
   */
  disableRowSelectionExcludeModel: import_prop_types149.default.bool,
  /**
   * If `true`, the selection on click on a row or cell is disabled.
   * @default false
   */
  disableRowSelectionOnClick: import_prop_types149.default.bool,
  /**
   * If `true`, the virtualization is disabled.
   * @default false
   */
  disableVirtualization: import_prop_types149.default.bool,
  /**
   * Controls whether to use the cell or row editing.
   * @default "cell"
   */
  editMode: import_prop_types149.default.oneOf(["cell", "row"]),
  /**
   * Use if the actual rowCount is not known upfront, but an estimation is available.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Applicable only with `paginationMode="server"` and when `rowCount="-1"`
   */
  estimatedRowCount: import_prop_types149.default.number,
  /**
   * Unstable features, breaking changes might be introduced.
   * For each feature, if the flag is not explicitly set to `true`, the feature will be fully disabled and any property / method call will not have any effect.
   */
  experimentalFeatures: import_prop_types149.default.shape({
    warnIfFocusStateIsNotSynced: import_prop_types149.default.bool
  }),
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering.
   * @default 150
   */
  filterDebounceMs: import_prop_types149.default.number,
  /**
   * Filtering can be processed on the server or client-side.
   * Set it to 'server' if you would like to handle filtering on the server-side.
   * @default "client"
   */
  filterMode: import_prop_types149.default.oneOf(["client", "server"]),
  /**
   * Set the filter model of the Data Grid.
   */
  filterModel: import_prop_types149.default.shape({
    items: import_prop_types149.default.arrayOf(import_prop_types149.default.shape({
      field: import_prop_types149.default.string.isRequired,
      id: import_prop_types149.default.oneOfType([import_prop_types149.default.number, import_prop_types149.default.string]),
      operator: import_prop_types149.default.string.isRequired,
      value: import_prop_types149.default.any
    })).isRequired,
    logicOperator: import_prop_types149.default.oneOf(["and", "or"]),
    quickFilterExcludeHiddenColumns: import_prop_types149.default.bool,
    quickFilterLogicOperator: import_prop_types149.default.oneOf(["and", "or"]),
    quickFilterValues: import_prop_types149.default.array
  }),
  /**
   * Function that applies CSS classes dynamically on cells.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {string} The CSS class to apply to the cell.
   */
  getCellClassName: import_prop_types149.default.func,
  /**
   * Function that returns the element to render in row detail.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {React.JSX.Element} The row detail element.
   */
  getDetailPanelContent: import_prop_types149.default.func,
  /**
   * Function that returns the height of the row detail panel.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {number | string} The height in pixels or "auto" to use the content height.
   * @default "() => 500"
   */
  getDetailPanelHeight: import_prop_types149.default.func,
  /**
   * Function that returns the estimated height for a row.
   * Only works if dynamic row height is used.
   * Once the row height is measured this value is discarded.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {number | null} The estimated row height value. If `null` or `undefined` then the default row height, based on the density, is applied.
   */
  getEstimatedRowHeight: import_prop_types149.default.func,
  /**
   * Function that applies CSS classes dynamically on rows.
   * @param {GridRowClassNameParams} params With all properties from [[GridRowClassNameParams]].
   * @returns {string} The CSS class to apply to the row.
   */
  getRowClassName: import_prop_types149.default.func,
  /**
   * Function that sets the row height per row.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {GridRowHeightReturnValue} The row height value. If `null` or `undefined` then the default row height is applied. If "auto" then the row height is calculated based on the content.
   */
  getRowHeight: import_prop_types149.default.func,
  /**
   * Return the id of a given [[GridRowModel]].
   * Ensure the reference of this prop is stable to avoid performance implications.
   * It could be done by either defining the prop outside of the component or by memoizing it.
   */
  getRowId: import_prop_types149.default.func,
  /**
   * Function that allows to specify the spacing between rows.
   * @param {GridRowSpacingParams} params With all properties from [[GridRowSpacingParams]].
   * @returns {GridRowSpacing} The row spacing values.
   */
  getRowSpacing: import_prop_types149.default.func,
  /**
   * Determines the path of a row in the tree data.
   * For instance, a row with the path ["A", "B"] is the child of the row with the path ["A"].
   * Note that all paths must contain at least one element.
   * @template R
   * @param {R} row The row from which we want the path.
   * @returns {string[]} The path to the row.
   */
  getTreeDataPath: import_prop_types149.default.func,
  /**
   * The grouping column used by the tree data.
   */
  groupingColDef: import_prop_types149.default.oneOfType([import_prop_types149.default.func, import_prop_types149.default.object]),
  /**
   * Override the height of the header filters.
   */
  headerFilterHeight: import_prop_types149.default.number,
  /**
   * If `true`, the header filters feature is enabled.
   * @default false
   */
  headerFilters: import_prop_types149.default.bool,
  /**
   * If `true`, the footer component is hidden.
   * @default false
   */
  hideFooter: import_prop_types149.default.bool,
  /**
   * If `true`, the pagination component in the footer is hidden.
   * @default false
   */
  hideFooterPagination: import_prop_types149.default.bool,
  /**
   * If `true`, the row count in the footer is hidden.
   * It has no effect if the pagination is enabled.
   * @default false
   */
  hideFooterRowCount: import_prop_types149.default.bool,
  /**
   * If `true`, the selected row count in the footer is hidden.
   * @default false
   */
  hideFooterSelectedRowCount: import_prop_types149.default.bool,
  /**
   * If `true`, the diacritics (accents) are ignored when filtering or quick filtering.
   * E.g. when filter value is `cafe`, the rows with `café` will be visible.
   * @default false
   */
  ignoreDiacritics: import_prop_types149.default.bool,
  /**
   * If `true`, the Data Grid will not use `valueFormatter` when exporting to CSV or copying to clipboard.
   * If an object is provided, you can choose to ignore the `valueFormatter` for CSV export or clipboard export.
   * @default false
   */
  ignoreValueFormatterDuringExport: import_prop_types149.default.oneOfType([import_prop_types149.default.shape({
    clipboardExport: import_prop_types149.default.bool,
    csvExport: import_prop_types149.default.bool
  }), import_prop_types149.default.bool]),
  /**
   * The initial state of the DataGridPro.
   * The data in it will be set in the state on initialization but will not be controlled.
   * If one of the data in `initialState` is also being controlled, then the control state wins.
   */
  initialState: import_prop_types149.default.object,
  /**
   * Callback fired when a cell is rendered, returns true if the cell is editable.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {boolean} A boolean indicating if the cell is editable.
   */
  isCellEditable: import_prop_types149.default.func,
  /**
   * Determines if a group should be expanded after its creation.
   * This prop takes priority over the `defaultGroupingExpansionDepth` prop.
   * @param {GridGroupNode} node The node of the group to test.
   * @returns {boolean} A boolean indicating if the group is expanded.
   */
  isGroupExpandedByDefault: import_prop_types149.default.func,
  /**
   * Indicates whether a row is reorderable.
   * @param {object} params With all properties from the row.
   * @param {R} params.row The row model of the row that the current cell belongs to.
   * @param {GridTreeNode} params.rowNode The node of the row that the current cell belongs to.
   * @returns {boolean} A boolean indicating if the row is reorderable.
   */
  isRowReorderable: import_prop_types149.default.func,
  /**
   * Determines if a row can be selected.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {boolean} A boolean indicating if the row is selectable.
   */
  isRowSelectable: import_prop_types149.default.func,
  /**
   * Indicates if a row reorder attempt is valid.
   * Can be used to disable certain row reorder operations based on the context.
   * The internal validation is still applied, preventing unsupported use-cases.
   * Use `isValidRowReorder()` to add additional validation rules to the default ones.
   * @param {ReorderValidationContext} context The context object containing all information about the reorder operation.
   * @returns {boolean} A boolean indicating if the reorder operation should go through.
   */
  isValidRowReorder: import_prop_types149.default.func,
  /**
   * If `true`, moving the mouse pointer outside the grid before releasing the mouse button
   * in a column re-order action will not cause the column to jump back to its original position.
   * @default false
   */
  keepColumnPositionIfDraggedOutside: import_prop_types149.default.bool,
  /**
   * If `true`, the selection model will retain selected rows that do not exist.
   * Useful when using server side pagination and row selections need to be retained
   * when changing pages.
   * @default false
   */
  keepNonExistentRowsSelected: import_prop_types149.default.bool,
  /**
   * The label of the Data Grid.
   * If the `showToolbar` prop is `true`, the label will be displayed in the toolbar and applied to the `aria-label` attribute of the grid.
   * If the `showToolbar` prop is `false`, the label will not be visible but will be applied to the `aria-label` attribute of the grid.
   */
  label: import_prop_types149.default.string,
  /**
   * Used together with `dataSource` to enable lazy loading.
   * If enabled, the grid stops adding `paginationModel` to the data requests (`getRows`)
   * and starts sending `start` and `end` values depending on the loading mode and the scroll position.
   * @default false
   */
  lazyLoading: import_prop_types149.default.bool,
  /**
   * If positive, the Data Grid will throttle data source requests on rendered rows interval change.
   * @default 500
   */
  lazyLoadingRequestThrottleMs: import_prop_types149.default.number,
  /**
   * If `true`, displays the data in a list view.
   * Use in combination with `listViewColumn`.
   */
  listView: import_prop_types149.default.bool,
  /**
   * Definition of the column rendered when the `listView` prop is enabled.
   */
  listViewColumn: import_prop_types149.default.shape({
    align: import_prop_types149.default.oneOf(["center", "left", "right"]),
    cellClassName: import_prop_types149.default.oneOfType([import_prop_types149.default.func, import_prop_types149.default.string]),
    display: import_prop_types149.default.oneOf(["flex", "text"]),
    field: import_prop_types149.default.string.isRequired,
    renderCell: import_prop_types149.default.func
  }),
  /**
   * If `true`, a loading overlay is displayed.
   * @default false
   */
  loading: import_prop_types149.default.bool,
  /**
   * Set the locale text of the Data Grid.
   * You can find all the translation keys supported in [the source](https://github.com/mui/mui-x/blob/HEAD/packages/x-data-grid/src/constants/localeTextConstants.ts) in the GitHub repository.
   */
  localeText: import_prop_types149.default.object,
  /**
   * Pass a custom logger in the components that implements the [[Logger]] interface.
   * @default console
   */
  logger: import_prop_types149.default.shape({
    debug: import_prop_types149.default.func.isRequired,
    error: import_prop_types149.default.func.isRequired,
    info: import_prop_types149.default.func.isRequired,
    warn: import_prop_types149.default.func.isRequired
  }),
  /**
   * Allows to pass the logging level or false to turn off logging.
   * @default "error" ("warn" in dev mode)
   */
  logLevel: import_prop_types149.default.oneOf(["debug", "error", "info", "warn", false]),
  /**
   * If set to "always", the multi-sorting is applied without modifier key.
   * Otherwise, the modifier key is required for multi-sorting to be applied.
   * @see See https://mui.com/x/react-data-grid/sorting/#multi-sorting
   * @default "withModifierKey"
   */
  multipleColumnsSortingMode: import_prop_types149.default.oneOf(["always", "withModifierKey"]),
  /**
   * Nonce of the inline styles for [Content Security Policy](https://www.w3.org/TR/2016/REC-CSP2-20161215/#script-src-the-nonce-attribute).
   */
  nonce: import_prop_types149.default.string,
  /**
   * Callback fired when any cell is clicked.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellClick: import_prop_types149.default.func,
  /**
   * Callback fired when a double click event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellDoubleClick: import_prop_types149.default.func,
  /**
   * Callback fired when the cell turns to edit mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStart: import_prop_types149.default.func,
  /**
   * Callback fired when the cell turns to view mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStop: import_prop_types149.default.func,
  /**
   * Callback fired when a keydown event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellKeyDown: import_prop_types149.default.func,
  /**
   * Callback fired when the `cellModesModel` prop changes.
   * @param {GridCellModesModel} cellModesModel Object containing which cells are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellModesModelChange: import_prop_types149.default.func,
  /**
   * Callback called when the data is copied to the clipboard.
   * @param {string} data The data copied to the clipboard.
   */
  onClipboardCopy: import_prop_types149.default.func,
  /**
   * Callback fired when a click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderClick: import_prop_types149.default.func,
  /**
   * Callback fired when a contextmenu event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   */
  onColumnHeaderContextMenu: import_prop_types149.default.func,
  /**
   * Callback fired when a double click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderDoubleClick: import_prop_types149.default.func,
  /**
   * Callback fired when a mouse enter event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderEnter: import_prop_types149.default.func,
  /**
   * Callback fired when a mouse leave event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderLeave: import_prop_types149.default.func,
  /**
   * Callback fired when a mouseout event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOut: import_prop_types149.default.func,
  /**
   * Callback fired when a mouseover event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOver: import_prop_types149.default.func,
  /**
   * Callback fired when a column is reordered.
   * @param {GridColumnOrderChangeParams} params With all properties from [[GridColumnOrderChangeParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnOrderChange: import_prop_types149.default.func,
  /**
   * Callback fired while a column is being resized.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnResize: import_prop_types149.default.func,
  /**
   * Callback fired when the column visibility model changes.
   * @param {GridColumnVisibilityModel} model The new model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnVisibilityModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when the width of a column is changed.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnWidthChange: import_prop_types149.default.func,
  /**
   * Callback fired when a data source request fails.
   * @param {GridGetRowsError | GridUpdateRowError} error The data source error object.
   */
  onDataSourceError: import_prop_types149.default.func,
  /**
   * Callback fired when the density changes.
   * @param {GridDensity} density New density value.
   */
  onDensityChange: import_prop_types149.default.func,
  /**
   * Callback fired when the detail panel of a row is opened or closed.
   * @param {GridRowId[]} ids The ids of the rows which have the detail panel open.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onDetailPanelExpandedRowIdsChange: import_prop_types149.default.func,
  /**
   * Callback fired when rowCount is set and the next batch of virtualized rows is rendered.
   * @param {GridFetchRowsParams} params With all properties from [[GridFetchRowsParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#viewport-loading Server-side data-Viewport loading} instead.
   */
  onFetchRows: import_prop_types149.default.func,
  /**
   * Callback fired when the Filter model changes before the filters are applied.
   * @param {GridFilterModel} model With all properties from [[GridFilterModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onFilterModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when the menu is closed.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuClose: import_prop_types149.default.func,
  /**
   * Callback fired when the menu is opened.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuOpen: import_prop_types149.default.func,
  /**
   * Callback fired when the pagination meta has changed.
   * @param {GridPaginationMeta} paginationMeta Updated pagination meta.
   */
  onPaginationMetaChange: import_prop_types149.default.func,
  /**
   * Callback fired when the pagination model has changed.
   * @param {GridPaginationModel} model Updated pagination model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPaginationModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when the pinned columns have changed.
   * @param {GridPinnedColumnFields} pinnedColumns The changed pinned columns.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPinnedColumnsChange: import_prop_types149.default.func,
  /**
   * Callback fired when the preferences panel is closed.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelClose: import_prop_types149.default.func,
  /**
   * Callback fired when the preferences panel is opened.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelOpen: import_prop_types149.default.func,
  /**
   * Callback called when `processRowUpdate()` throws an error or rejects.
   * @param {any} error The error thrown.
   */
  onProcessRowUpdateError: import_prop_types149.default.func,
  /**
   * Callback fired when the Data Grid is resized.
   * @param {ElementSize} containerSize With all properties from [[ElementSize]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onResize: import_prop_types149.default.func,
  /**
   * Callback fired when a row is clicked.
   * Not called if the target clicked is an interactive element added by the built-in columns.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowClick: import_prop_types149.default.func,
  /**
   * Callback fired when the row count has changed.
   * @param {number} count Updated row count.
   */
  onRowCountChange: import_prop_types149.default.func,
  /**
   * Callback fired when a double click event comes from a row container element.
   * @param {GridRowParams} params With all properties from [[RowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowDoubleClick: import_prop_types149.default.func,
  /**
   * Callback fired when the row turns to edit mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStart: import_prop_types149.default.func,
  /**
   * Callback fired when the row turns to view mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStop: import_prop_types149.default.func,
  /**
   * Callback fired when the `rowModesModel` prop changes.
   * @param {GridRowModesModel} rowModesModel Object containing which rows are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowModesModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when a row is being reordered.
   * @param {GridRowOrderChangeParams} params With all properties from [[GridRowOrderChangeParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowOrderChange: import_prop_types149.default.func,
  /**
   * Callback fired when the selection state of one or multiple rows changes.
   * @param {GridRowSelectionModel} rowSelectionModel With all the row ids [[GridSelectionModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowSelectionModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when scrolling to the bottom of the grid viewport.
   * @param {GridRowScrollEndParams} params With all properties from [[GridRowScrollEndParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#infinite-loading Server-side data-Infinite loading} instead.
   */
  onRowsScrollEnd: import_prop_types149.default.func,
  /**
   * Callback fired when the sort model changes before a column is sorted.
   * @param {GridSortModel} model With all properties from [[GridSortModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onSortModelChange: import_prop_types149.default.func,
  /**
   * Callback fired when the state of the Data Grid is updated.
   * @param {GridState} state The new state.
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @ignore - do not document.
   */
  onStateChange: import_prop_types149.default.func,
  /**
   * Select the pageSize dynamically using the component UI.
   * @default [25, 50, 100]
   */
  pageSizeOptions: import_prop_types149.default.arrayOf(import_prop_types149.default.oneOfType([import_prop_types149.default.number, import_prop_types149.default.shape({
    label: import_prop_types149.default.string.isRequired,
    value: import_prop_types149.default.number.isRequired
  })]).isRequired),
  /**
   * If `true`, pagination is enabled.
   * @default false
   */
  pagination: import_prop_types149.default.bool,
  /**
   * The extra information about the pagination state of the Data Grid.
   * Only applicable with `paginationMode="server"`.
   */
  paginationMeta: import_prop_types149.default.shape({
    hasNextPage: import_prop_types149.default.bool
  }),
  /**
   * Pagination can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle the pagination on the client-side.
   * Set it to 'server' if you would like to handle the pagination on the server-side.
   * @default "client"
   */
  paginationMode: import_prop_types149.default.oneOf(["client", "server"]),
  /**
   * The pagination model of type [[GridPaginationModel]] which refers to current `page` and `pageSize`.
   */
  paginationModel: import_prop_types149.default.shape({
    page: import_prop_types149.default.number.isRequired,
    pageSize: import_prop_types149.default.number.isRequired
  }),
  /**
   * The column fields to display pinned to left or right.
   */
  pinnedColumns: import_prop_types149.default.object,
  /**
   * Sets the type of separator between pinned columns and non-pinned columns.
   * @default 'border-and-shadow'
   */
  pinnedColumnsSectionSeparator: import_prop_types149.default.oneOf(["border-and-shadow", "border", "shadow"]),
  /**
   * Rows data to pin on top or bottom.
   */
  pinnedRows: import_prop_types149.default.shape({
    bottom: import_prop_types149.default.arrayOf(import_prop_types149.default.object),
    top: import_prop_types149.default.arrayOf(import_prop_types149.default.object)
  }),
  /**
   * Sets the type of separator between pinned rows and non-pinned rows.
   * @default 'border-and-shadow'
   */
  pinnedRowsSectionSeparator: import_prop_types149.default.oneOf(["border-and-shadow", "border"]),
  /**
   * Callback called before updating a row with new values in the row and cell editing.
   * @template R
   * @param {R} newRow Row object with the new values.
   * @param {R} oldRow Row object with the old values.
   * @param {{ rowId: GridRowId }} params Additional parameters.
   * @returns {Promise<R> | R} The final values to update the row.
   */
  processRowUpdate: import_prop_types149.default.func,
  /**
   * The milliseconds throttle delay for resizing the grid.
   * @default 60
   */
  resizeThrottleMs: import_prop_types149.default.number,
  /**
   * Row region in pixels to render before/after the viewport
   * @default 150
   */
  rowBufferPx: import_prop_types149.default.number,
  /**
   * Set the total number of rows, if it is different from the length of the value `rows` prop.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Only works with `paginationMode="server"`, ignored when `paginationMode="client"`.
   */
  rowCount: import_prop_types149.default.number,
  /**
   * Sets the height in pixel of a row in the Data Grid.
   * @default 52
   */
  rowHeight: import_prop_types149.default.number,
  /**
   * Controls the modes of the rows.
   */
  rowModesModel: import_prop_types149.default.object,
  /**
   * If `true`, the reordering of rows is enabled.
   * @default false
   */
  rowReordering: import_prop_types149.default.bool,
  /**
   * Set of rows of type [[GridRowsProp]].
   * @default []
   */
  rows: import_prop_types149.default.arrayOf(import_prop_types149.default.object),
  /**
   * If `false`, the row selection mode is disabled.
   * @default true
   */
  rowSelection: import_prop_types149.default.bool,
  /**
   * Sets the row selection model of the Data Grid.
   */
  rowSelectionModel: import_prop_types149.default.shape({
    ids: import_prop_types149.default.instanceOf(Set).isRequired,
    type: import_prop_types149.default.oneOf(["exclude", "include"]).isRequired
  }),
  /**
   * When `rowSelectionPropagation.descendants` is set to `true`.
   * - Selecting a parent selects all its filtered descendants automatically.
   * - Deselecting a parent row deselects all its filtered descendants automatically.
   *
   * When `rowSelectionPropagation.parents` is set to `true`
   * - Selecting all the filtered descendants of a parent selects the parent automatically.
   * - Deselecting a descendant of a selected parent deselects the parent automatically.
   *
   * Works with tree data and row grouping on the client-side only.
   * @default { parents: true, descendants: true }
   */
  rowSelectionPropagation: import_prop_types149.default.shape({
    descendants: import_prop_types149.default.bool,
    parents: import_prop_types149.default.bool
  }),
  /**
   * Loading rows can be processed on the server or client-side.
   * Set it to 'client' if you would like enable infnite loading.
   * Set it to 'server' if you would like to enable lazy loading.
   * @default "client"
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#viewport-loading Server-side data-Viewport loading} instead.
   */
  rowsLoadingMode: import_prop_types149.default.oneOf(["client", "server"]),
  /**
   * Sets the type of space between rows added by `getRowSpacing`.
   * @default "margin"
   */
  rowSpacingType: import_prop_types149.default.oneOf(["border", "margin"]),
  /**
   * If `true`, the Data Grid will auto span the cells over the rows having the same value.
   * @default false
   */
  rowSpanning: import_prop_types149.default.bool,
  /**
   * Override the height/width of the Data Grid inner scrollbar.
   */
  scrollbarSize: import_prop_types149.default.number,
  /**
   * Set the area in `px` at the bottom of the grid viewport where onRowsScrollEnd is called.
   * If combined with `lazyLoading`, it defines the area where the next data request is triggered.
   * @default 80
   */
  scrollEndThreshold: import_prop_types149.default.number,
  /**
   * Updates the tree path in a row model.
   * Used when reordering rows across different parents in tree data.
   * @template R
   * @param {string[]} path The new path for the row.
   * @param {R} row The row model to update.
   * @returns {R} The updated row model with the new path.
   */
  setTreeDataPath: import_prop_types149.default.func,
  /**
   * If `true`, vertical borders will be displayed between cells.
   * @default false
   */
  showCellVerticalBorder: import_prop_types149.default.bool,
  /**
   * If `true`, vertical borders will be displayed between column header items.
   * @default false
   */
  showColumnVerticalBorder: import_prop_types149.default.bool,
  /**
   * If `true`, the toolbar is displayed.
   * @default false
   */
  showToolbar: import_prop_types149.default.bool,
  /**
   * Overridable components props dynamically passed to the component at rendering.
   */
  slotProps: import_prop_types149.default.object,
  /**
   * Overridable components.
   */
  slots: import_prop_types149.default.object,
  /**
   * Sorting can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle sorting on the client-side.
   * Set it to 'server' if you would like to handle sorting on the server-side.
   * @default "client"
   */
  sortingMode: import_prop_types149.default.oneOf(["client", "server"]),
  /**
   * The order of the sorting sequence.
   * @default ['asc', 'desc', null]
   */
  sortingOrder: import_prop_types149.default.arrayOf(import_prop_types149.default.oneOf(["asc", "desc"])),
  /**
   * Set the sort model of the Data Grid.
   */
  sortModel: import_prop_types149.default.arrayOf(import_prop_types149.default.shape({
    field: import_prop_types149.default.string.isRequired,
    sort: import_prop_types149.default.oneOf(["asc", "desc"])
  })),
  style: import_prop_types149.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types149.default.oneOfType([import_prop_types149.default.arrayOf(import_prop_types149.default.oneOfType([import_prop_types149.default.func, import_prop_types149.default.object, import_prop_types149.default.bool])), import_prop_types149.default.func, import_prop_types149.default.object]),
  /**
   * Sets the tab navigation behavior for the Data Grid.
   * - "none": No Data Grid specific tab navigation. Pressing the tab key will move the focus to the next element in the tab sequence.
   * - "content": Pressing the tab key will move the focus to the next cell in the same row or the first cell in the next row. Shift+Tab will move the focus to the previous cell in the same row or the last cell in the previous row. Tab navigation is not enabled for the header.
   * - "header": Pressing the tab key will move the focus to the next column group, column header or header filter. Shift+Tab will move the focus to the previous column group, column header or header filter. Tab navigation is not enabled for the content.
   * - "all": Combines the "content" and "header" behavior.
   * @default "none"
   */
  tabNavigation: import_prop_types149.default.oneOf(["all", "content", "header", "none"]),
  /**
   * If positive, the Data Grid will throttle updates coming from `apiRef.current.updateRows` and `apiRef.current.setRows`.
   * It can be useful if you have a high update rate but do not want to do heavy work like filtering / sorting or rendering on each  individual update.
   * @default 0
   */
  throttleRowsMs: import_prop_types149.default.number,
  /**
   * If `true`, the rows will be gathered in a tree structure according to the `getTreeDataPath` prop.
   * @default false
   */
  treeData: import_prop_types149.default.bool,
  /**
   * If `true`, the Data Grid enables column virtualization when `getRowHeight` is set to `() => 'auto'`.
   * By default, column virtualization is disabled when dynamic row height is enabled to measure the row height correctly.
   * For datasets with a large number of columns, this can cause performance issues.
   * The downside of enabling this prop is that the row height will be estimated based the cells that are currently rendered, which can cause row height change when scrolling horizontally.
   * @default false
   */
  virtualizeColumnsWithAutoRowHeight: import_prop_types149.default.bool
};

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/useDataGridPremiumComponent.js
var React362 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/dataSource/useGridDataSourcePremium.js
var React301 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/sidebar/gridSidebarInterfaces.js
var GridSidebarValue = (function(GridSidebarValue2) {
  GridSidebarValue2["Pivot"] = "pivot";
  GridSidebarValue2["Charts"] = "charts";
  return GridSidebarValue2;
})({});

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/sidebar/gridSidebarSelector.js
var gridSidebarStateSelector = createRootSelector((state) => state.sidebar);
var gridSidebarOpenSelector = createSelector3(gridSidebarStateSelector, (state) => state.open);
var gridSidebarContentSelector = createSelector3(gridSidebarStateSelector, ({
  sidebarId,
  labelId,
  value
}) => ({
  sidebarId,
  labelId,
  value
}));

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/pivoting/gridPivotingSelectors.js
var gridPivotingStateSelector2 = createRootSelector((state) => state.pivoting);
var gridPivotPanelOpenSelector = createSelector3(gridSidebarStateSelector, (sidebar) => sidebar.value === GridSidebarValue.Pivot && sidebar.open);
var gridPivotModelSelector = createSelector3(gridPivotingStateSelector2, (pivoting) => pivoting == null ? void 0 : pivoting.model);
var gridPivotPropsOverridesSelector = createSelector3(gridPivotingStateSelector2, (pivoting) => (pivoting == null ? void 0 : pivoting.active) ? pivoting.propsOverrides : void 0);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/dataSource/utils.js
var getPropsOverrides = (pivotColumns, pivotingColDef, pivotModel, initialColumns, apiRef) => {
  const visiblePivotColumns = pivotModel.columns.filter((column) => !column.hidden);
  const visiblePivotValues = pivotModel.values.filter((value) => !value.hidden);
  const columns = Array.from(initialColumns.values());
  const columnGroupingModel = [];
  const columnGroupingModelLookup = /* @__PURE__ */ new Map();
  const newColumns = {};
  const aggregationModel = {};
  const uniquePaths = [];
  const processPath = (currentPath, remainingColumns, level) => {
    if (level === visiblePivotColumns.length) {
      uniquePaths.push([...currentPath]);
      return;
    }
    remainingColumns.forEach((column) => {
      processPath([...currentPath, {
        key: column.key,
        field: visiblePivotColumns[level].field,
        value: column.group
      }], column.children || [], level + 1);
    });
  };
  processPath([], pivotColumns, 0);
  const columnGroupPathValues = [];
  uniquePaths.forEach((columnPath) => {
    const columnPathKeys = columnPath.map((path) => path.key);
    const columnPathValues = columnPath.map((path) => path.value);
    visiblePivotValues.forEach((pivotValue) => {
      const originalColumn = initialColumns.get(pivotValue.field);
      const overrides = pivotingColDef(pivotValue.field, columnPathKeys);
      const newColumnDef = _extends({}, originalColumn, overrides, {
        aggregable: false,
        groupable: false,
        filterable: false,
        hideable: false,
        editable: false,
        disableReorder: true
      });
      const pivotFieldName = newColumnDef.field;
      newColumns[pivotFieldName] = newColumnDef;
      aggregationModel[pivotFieldName] = pivotValue.aggFunc;
      const combinedPathValues = [...columnPathValues, pivotValue.field].map((path, index) => typeof path === "string" ? path : apiRef.current.getRowValue(path, initialColumns.get(visiblePivotColumns[index].field)));
      columnGroupPathValues.push({
        field: pivotFieldName,
        pathValues: combinedPathValues.slice(0, -1),
        pathValuesRaw: columnPathValues
      });
      for (let i2 = 0; i2 < combinedPathValues.length - 1; i2 += 1) {
        const currentField = visiblePivotColumns[i2].field;
        const groupPath = combinedPathValues.slice(0, i2 + 1);
        const groupId = groupPath.join("-");
        let headerName = columnPathValues[groupPath.length - 1];
        if (typeof headerName !== "string") {
          headerName = apiRef.current.getRowFormattedValue(headerName, initialColumns.get(currentField));
        }
        if (typeof headerName === "number") {
          headerName = String(headerName);
        }
        if (typeof headerName !== "string") {
          throw new Error(`MUI X: Header name for a column group based on ${currentField} cannot be converted to a string.`);
        }
        if (!columnGroupingModelLookup.has(groupId)) {
          const columnGroup = {
            groupId,
            headerName,
            children: []
          };
          columnGroupingModelLookup.set(groupId, columnGroup);
          if (i2 === 0) {
            columnGroupingModel.push(columnGroup);
          } else {
            const parentGroupId2 = groupPath.slice(0, -1).join("-");
            const parentGroup2 = columnGroupingModelLookup.get(parentGroupId2);
            if (parentGroup2) {
              parentGroup2.children.push(columnGroup);
            }
          }
        }
      }
      const parentGroupId = combinedPathValues.slice(0, -1).join("-");
      const parentGroup = columnGroupingModelLookup.get(parentGroupId);
      if (parentGroup) {
        parentGroup.children.push({
          field: pivotFieldName
        });
      }
    });
  });
  for (let i2 = visiblePivotColumns.length - 1; i2 >= 0; i2 -= 1) {
    const sort = visiblePivotColumns[i2].sort;
    if (!sort) {
      continue;
    }
    columnGroupPathValues.sort((a, b) => {
      if (typeof a.pathValuesRaw[i2] === "string" && typeof b.pathValuesRaw[i2] === "string") {
        return 0;
      }
      return (sort === "asc" ? 1 : -1) * gridStringOrNumberComparator(a.pathValues[i2], b.pathValues[i2], {}, {});
    });
  }
  if (visiblePivotColumns.length > 0) {
    for (let i2 = 0; i2 < columnGroupPathValues.length; i2 += 1) {
      columns.push(newColumns[columnGroupPathValues[i2].field]);
    }
  }
  return {
    columns,
    columnGroupingModel,
    aggregationModel
  };
};
var fetchParents = (rowTree, rowId, fetchHandler) => {
  const parents = [];
  let currentId = rowId;
  while (currentId !== void 0 && currentId !== GRID_ROOT_GROUP_ID) {
    const parentId = rowTree[currentId].parent;
    parents.push(parentId);
    currentId = parentId;
  }
  return Promise.all(parents.reverse().map(fetchHandler));
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/gridRowGroupingSelector.js
var gridRowGroupingStateSelector = createRootSelector((state) => state.rowGrouping);
var gridRowGroupingModelSelector = createSelector3(gridRowGroupingStateSelector, (rowGrouping) => rowGrouping.model);
var gridRowGroupingSanitizedModelSelector = createSelectorMemoized2(gridRowGroupingModelSelector, gridColumnLookupSelector, (model, columnsLookup) => model.filter((field) => !!columnsLookup[field]));

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/gridAggregationSelectors.js
var gridAggregationStateSelector = createRootSelector((state) => state.aggregation);
var gridAggregationModelSelector = createSelector3(gridAggregationStateSelector, (aggregationState) => aggregationState.model);
var gridAggregationLookupSelector = createSelector3(gridAggregationStateSelector, (aggregationState) => aggregationState.lookup);
var gridCellAggregationResultSelector = createSelector3(gridRowTreeSelector, gridAggregationLookupSelector, (rowTree, aggregationLookup, {
  id,
  field
}) => {
  var _a;
  if (!aggregationLookup) {
    return null;
  }
  let cellAggregationPosition = null;
  const rowNode = rowTree[id];
  if (!rowNode) {
    return null;
  }
  if (rowNode.type === "group") {
    cellAggregationPosition = "inline";
  } else if (id.toString().startsWith("auto-generated-group-footer-")) {
    cellAggregationPosition = "footer";
  }
  if (cellAggregationPosition == null) {
    return null;
  }
  const groupId = cellAggregationPosition === "inline" ? id : rowNode.parent ?? "";
  const aggregationResult = (_a = aggregationLookup == null ? void 0 : aggregationLookup[groupId]) == null ? void 0 : _a[field];
  if (!aggregationResult || aggregationResult.position !== cellAggregationPosition) {
    return null;
  }
  return aggregationResult;
});

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/dataSource/useGridDataSourcePremium.js
function getKeyPremium(params) {
  return JSON.stringify([params.filterModel, params.sortModel, params.groupKeys, params.groupFields, params.start, params.end, params.pivotModel ? {} : params.aggregationModel, params.pivotModel]);
}
var options2 = {
  cacheOptions: {
    getKey: getKeyPremium
  }
};
var useGridDataSourcePremium = (apiRef, props) => {
  const aggregationModel = gridAggregationModelSelector(apiRef);
  const groupingModelSize = gridRowGroupingSanitizedModelSelector(apiRef).length;
  const setStrategyAvailability2 = React301.useCallback(() => {
    const currentStrategy = props.treeData || !props.disableRowGrouping && groupingModelSize > 0 ? DataSourceRowsUpdateStrategy.GroupedData : DataSourceRowsUpdateStrategy.Default;
    const prevStrategy = currentStrategy === DataSourceRowsUpdateStrategy.GroupedData ? DataSourceRowsUpdateStrategy.Default : DataSourceRowsUpdateStrategy.GroupedData;
    apiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, prevStrategy, () => false);
    apiRef.current.setStrategyAvailability(GridStrategyGroup.DataSource, currentStrategy, props.dataSource && !props.lazyLoading ? () => true : () => false);
  }, [apiRef, props.dataSource, props.lazyLoading, props.treeData, props.disableRowGrouping, groupingModelSize]);
  const handleEditRowWithAggregation = React301.useCallback((params, updatedRow) => {
    const rowTree = gridRowTreeSelector(apiRef);
    if (updatedRow && !isDeepEqual(updatedRow, params.previousRow)) {
      apiRef.current.dataSource.cache.clear();
    }
    const groupKeys = getGroupKeys(rowTree, params.rowId);
    apiRef.current.updateNestedRows([updatedRow], groupKeys);
    fetchParents(rowTree, params.rowId, apiRef.current.dataSource.fetchRows);
  }, [apiRef]);
  const {
    api,
    debouncedFetchRows,
    flatTreeStrategyProcessor,
    groupedDataStrategyProcessor,
    events
  } = useGridDataSourceBasePro(apiRef, props, _extends({}, !props.disableAggregation && Object.keys(aggregationModel).length > 0 ? {
    handleEditRow: handleEditRowWithAggregation
  } : {}, options2));
  const aggregateRowRef = React301.useRef({});
  const initialColumns = gridPivotInitialColumnsSelector(apiRef);
  const pivotActive = gridPivotActiveSelector(apiRef);
  const pivotModel = gridPivotModelSelector(apiRef);
  const processDataSourceRows = React301.useCallback(({
    params,
    response
  }, applyRowHydration) => {
    if (response.aggregateRow) {
      aggregateRowRef.current = response.aggregateRow;
    }
    if (Object.keys(params.aggregationModel || {}).length > 0) {
      if (applyRowHydration) {
        apiRef.current.requestPipeProcessorsApplication("hydrateRows");
      }
      apiRef.current.applyAggregation();
    }
    if (response.pivotColumns) {
      const pivotingColDef = props.pivotingColDef;
      if (!pivotingColDef || typeof pivotingColDef !== "function") {
        throw new Error("MUI X: No `pivotingColDef()` prop provided with to the Data Grid, but response contains `pivotColumns`.\n\n            You need a callback to return at least a field column prop for each generated pivot column.\n\n            See [server-side pivoting](https://mui.com/x/react-data-grid/server-side-data/pivoting/) documentation for more details.");
      }
      const partialPropsOverrides = getPropsOverrides(response.pivotColumns, pivotingColDef, pivotModel, initialColumns, apiRef);
      apiRef.current.setState((state) => {
        return _extends({}, state, {
          pivoting: _extends({}, state.pivoting, {
            propsOverrides: _extends({}, state.pivoting.propsOverrides, partialPropsOverrides)
          })
        });
      });
    }
    return {
      params,
      response
    };
  }, [apiRef, props.pivotingColDef, initialColumns, pivotModel]);
  const resolveGroupAggregation = React301.useCallback((groupId, field) => {
    var _a, _b, _c, _d;
    if (groupId === GRID_ROOT_GROUP_ID) {
      return (_b = (_a = props.dataSource) == null ? void 0 : _a.getAggregatedValue) == null ? void 0 : _b.call(_a, aggregateRowRef.current, field);
    }
    const row = apiRef.current.getRow(groupId);
    return (_d = (_c = props.dataSource) == null ? void 0 : _c.getAggregatedValue) == null ? void 0 : _d.call(_c, row, field);
  }, [apiRef, props.dataSource]);
  const privateApi = _extends({}, api.private, {
    resolveGroupAggregation
  });
  useGridApiMethod(apiRef, api.public, "public");
  useGridApiMethod(apiRef, privateApi, "private");
  useGridRegisterStrategyProcessor(apiRef, flatTreeStrategyProcessor.strategyName, flatTreeStrategyProcessor.group, flatTreeStrategyProcessor.processor);
  useGridRegisterStrategyProcessor(apiRef, groupedDataStrategyProcessor.strategyName, groupedDataStrategyProcessor.group, groupedDataStrategyProcessor.processor);
  useGridRegisterPipeProcessor(apiRef, "processDataSourceRows", processDataSourceRows);
  Object.entries(events).forEach(([event, handler]) => {
    useGridEvent(apiRef, event, handler);
  });
  useGridEvent(apiRef, "rowGroupingModelChange", runIf(!pivotActive, () => debouncedFetchRows()));
  useGridEvent(apiRef, "aggregationModelChange", runIf(!pivotActive, () => debouncedFetchRows()));
  useGridEvent(apiRef, "pivotModeChange", runIf(!pivotActive, () => debouncedFetchRows()));
  useGridEvent(apiRef, "pivotModelChange", runIf(pivotActive, () => debouncedFetchRows()));
  React301.useEffect(() => {
    setStrategyAvailability2();
  }, [setStrategyAvailability2]);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/useGridAggregation.js
var React302 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/gridAggregationUtils.js
var GRID_AGGREGATION_ROOT_FOOTER_ROW_ID = "auto-generated-group-footer-root";
var getAggregationFooterRowIdFromGroupId = (groupId) => {
  if (groupId == null) {
    return GRID_AGGREGATION_ROOT_FOOTER_ROW_ID;
  }
  return `auto-generated-group-footer-${groupId}`;
};
var isClientSideAggregateFunction = (aggregationFunction) => !!aggregationFunction && "apply" in aggregationFunction;
var canColumnHaveAggregationFunction = ({
  colDef,
  aggregationFunctionName,
  aggregationFunction,
  isDataSource
}) => {
  if (!colDef) {
    return false;
  }
  if (!isClientSideAggregateFunction(aggregationFunction) && !isDataSource) {
    return false;
  }
  if (colDef.availableAggregationFunctions != null) {
    return colDef.availableAggregationFunctions.includes(aggregationFunctionName);
  }
  if (!(aggregationFunction == null ? void 0 : aggregationFunction.columnTypes)) {
    return true;
  }
  return aggregationFunction.columnTypes.includes(colDef.type);
};
var getAvailableAggregationFunctions = ({
  aggregationFunctions,
  colDef,
  isDataSource
}) => Object.keys(aggregationFunctions).filter((aggregationFunctionName) => canColumnHaveAggregationFunction({
  colDef,
  aggregationFunctionName,
  aggregationFunction: aggregationFunctions[aggregationFunctionName],
  isDataSource
}));
var mergeStateWithAggregationModel = (aggregationModel) => (state) => _extends({}, state, {
  aggregation: _extends({}, state.aggregation, {
    model: aggregationModel
  })
});
var getAggregationRules = (columnsLookup, aggregationModel, aggregationFunctions, isDataSource) => {
  const aggregationRules = {};
  for (const field in aggregationModel) {
    const columnItem = aggregationModel[field];
    if (columnsLookup[field] && canColumnHaveAggregationFunction({
      colDef: columnsLookup[field],
      aggregationFunctionName: columnItem,
      aggregationFunction: aggregationFunctions[columnItem],
      isDataSource
    })) {
      aggregationRules[field] = {
        aggregationFunctionName: columnItem,
        aggregationFunction: aggregationFunctions[columnItem]
      };
    }
  }
  return aggregationRules;
};
var addFooterRows = ({
  groupingParams,
  apiRef,
  getAggregationPosition,
  hasAggregationRule
}) => {
  let newGroupingParams = _extends({}, groupingParams, {
    tree: _extends({}, groupingParams.tree),
    treeDepths: _extends({}, groupingParams.treeDepths)
  });
  const updateChildGroupFooter = (groupNode) => {
    const shouldHaveFooter = hasAggregationRule && getAggregationPosition(groupNode) === "footer";
    if (shouldHaveFooter) {
      const footerId = getAggregationFooterRowIdFromGroupId(groupNode.id);
      if (groupNode.footerId !== footerId) {
        if (groupNode.footerId != null) {
          removeNodeFromTree({
            node: newGroupingParams.tree[groupNode.footerId],
            tree: newGroupingParams.tree,
            treeDepths: newGroupingParams.treeDepths
          });
        }
        const footerNode = {
          id: footerId,
          parent: groupNode.id,
          depth: groupNode ? groupNode.depth + 1 : 0,
          type: "footer"
        };
        insertNodeInTree(footerNode, newGroupingParams.tree, newGroupingParams.treeDepths, null);
      }
    } else if (groupNode.footerId != null) {
      removeNodeFromTree({
        node: newGroupingParams.tree[groupNode.footerId],
        tree: newGroupingParams.tree,
        treeDepths: newGroupingParams.treeDepths
      });
      newGroupingParams.tree[groupNode.id] = _extends({}, newGroupingParams.tree[groupNode.id], {
        footerId: null
      });
    }
  };
  const updateRootGroupFooter = (groupNode) => {
    const shouldHaveFooter = hasAggregationRule && getAggregationPosition(groupNode) === "footer" && groupNode.children.length > 0;
    if (shouldHaveFooter) {
      const rowId = getAggregationFooterRowIdFromGroupId(null);
      newGroupingParams = addPinnedRow({
        groupingParams: newGroupingParams,
        rowModel: {
          [GRID_ID_AUTOGENERATED]: rowId
        },
        rowId,
        position: "bottom",
        apiRef,
        isAutoGenerated: true
      });
    }
  };
  const updateGroupFooter = (groupNode) => {
    if (groupNode.id === GRID_ROOT_GROUP_ID) {
      updateRootGroupFooter(groupNode);
    } else {
      updateChildGroupFooter(groupNode);
    }
    groupNode.children.forEach((childId) => {
      const childNode = newGroupingParams.tree[childId];
      if (childNode.type === "group") {
        updateGroupFooter(childNode);
      }
    });
  };
  updateGroupFooter(newGroupingParams.tree[GRID_ROOT_GROUP_ID]);
  return newGroupingParams;
};
var areAggregationRulesEqual = (previousValue, newValue) => {
  const previousFields = Object.keys(previousValue ?? {});
  const newFields = Object.keys(newValue);
  if (!isDeepEqual(previousFields, newFields)) {
    return false;
  }
  return newFields.every((field) => {
    const previousRule = previousValue == null ? void 0 : previousValue[field];
    const newRule = newValue[field];
    if ((previousRule == null ? void 0 : previousRule.aggregationFunction) !== (newRule == null ? void 0 : newRule.aggregationFunction)) {
      return false;
    }
    if ((previousRule == null ? void 0 : previousRule.aggregationFunctionName) !== (newRule == null ? void 0 : newRule.aggregationFunctionName)) {
      return false;
    }
    return true;
  });
};
var getAggregationFunctionLabel = ({
  apiRef,
  aggregationRule
}) => {
  if (aggregationRule.aggregationFunction.label != null) {
    return aggregationRule.aggregationFunction.label;
  }
  try {
    return apiRef.current.getLocaleText(`aggregationFunctionLabel${capitalize(aggregationRule.aggregationFunctionName)}`);
  } catch {
    return aggregationRule.aggregationFunctionName;
  }
};
var defaultGetAggregationPosition = (groupNode) => groupNode.depth === -1 ? "footer" : "inline";

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/createAggregationLookup.js
var shouldApplySorting = (aggregationRules, aggregatedFields) => {
  return aggregatedFields.some((field) => aggregationRules[field].aggregationFunction.applySorting);
};
var getGroupAggregatedValue = (groupId, apiRef, aggregationRowsScope, aggregatedFields, aggregationRules, position, applySorting, valueGetters, publicApi, groupAggregatedValuesLookup, columnsLookup) => {
  const groupAggregationLookup = {};
  const aggregatedValues = [];
  for (let i2 = 0; i2 < aggregatedFields.length; i2 += 1) {
    aggregatedValues[i2] = {
      aggregatedField: aggregatedFields[i2],
      values: []
    };
  }
  const rowTree = gridRowTreeSelector(apiRef);
  const rowLookup = gridRowsLookupSelector(apiRef);
  const isPivotActive = apiRef.current.state.pivoting.active;
  const rowIds = apiRef.current.getRowGroupChildren({
    groupId,
    applySorting,
    directChildrenOnly: true,
    skipAutoGeneratedRows: false,
    applyFiltering: aggregationRowsScope === "filtered"
  });
  for (let i2 = 0; i2 < rowIds.length; i2 += 1) {
    const rowId = rowIds[i2];
    const rowNode = rowTree[rowId];
    if (rowNode.type === "group") {
      const childGroupValues = groupAggregatedValuesLookup.get(rowId);
      if (childGroupValues) {
        for (let j = 0; j < aggregatedFields.length; j += 1) {
          aggregatedValues[j].values = aggregatedValues[j].values.concat(childGroupValues[j].values);
        }
      }
      continue;
    }
    const row = rowLookup[rowId];
    if (!row) {
      continue;
    }
    for (let j = 0; j < aggregatedFields.length; j += 1) {
      const aggregatedField = aggregatedFields[j];
      const columnAggregationRules = aggregationRules[aggregatedField];
      const aggregationFunction = columnAggregationRules.aggregationFunction;
      const field = aggregatedField;
      let value;
      if (typeof aggregationFunction.getCellValue === "function") {
        value = aggregationFunction.getCellValue({
          field,
          row
        });
      } else if (isPivotActive) {
        value = row[field];
      } else {
        const valueGetter = valueGetters[aggregatedField];
        value = valueGetter(row);
      }
      if (value !== void 0) {
        aggregatedValues[j].values.push(value);
      }
    }
  }
  for (let i2 = 0; i2 < aggregatedValues.length; i2 += 1) {
    const {
      aggregatedField,
      values: values3
    } = aggregatedValues[i2];
    const aggregationFunction = aggregationRules[aggregatedField].aggregationFunction;
    const value = aggregationFunction.apply({
      values: values3,
      groupId,
      field: aggregatedField
      // Added per user request in https://github.com/mui/mui-x/issues/6995#issuecomment-1327423455
    }, publicApi);
    const rowForFormatter = rowLookup[groupId] || {
      id: groupId,
      [aggregatedField]: value
    };
    const formattedValue = aggregationFunction.valueFormatter ? aggregationFunction.valueFormatter(value, rowForFormatter, columnsLookup[aggregatedField], apiRef) : void 0;
    if (position !== null) {
      groupAggregationLookup[aggregatedField] = {
        position,
        value,
        formattedValue
      };
    }
  }
  return {
    groupAggregationLookup,
    aggregatedValues
  };
};
var getGroupAggregatedValueDataSource = (groupId, apiRef, aggregatedFields, position) => {
  var _a, _b;
  const groupAggregationLookup = {};
  for (let j = 0; j < aggregatedFields.length; j += 1) {
    const aggregatedField = aggregatedFields[j];
    groupAggregationLookup[aggregatedField] = {
      position,
      value: ((_b = (_a = apiRef.current).resolveGroupAggregation) == null ? void 0 : _b.call(_a, groupId, aggregatedField)) ?? ""
    };
  }
  return groupAggregationLookup;
};
var createAggregationLookup = ({
  apiRef,
  aggregationRules,
  aggregatedFields,
  aggregationRowsScope,
  getAggregationPosition,
  isDataSource,
  applySorting = false
}) => {
  if (aggregatedFields.length === 0) {
    return {};
  }
  const columnsLookup = gridColumnLookupSelector(apiRef);
  const valueGetters = {};
  for (let i2 = 0; i2 < aggregatedFields.length; i2 += 1) {
    const field = aggregatedFields[i2];
    const column = columnsLookup[field];
    const valueGetter = (row) => apiRef.current.getRowValue(row, column);
    valueGetters[field] = valueGetter;
  }
  const aggregationLookup = {};
  const rowTree = gridRowTreeSelector(apiRef);
  const groupAggregatedValuesLookup = /* @__PURE__ */ new Map();
  const {
    rowIdToIndexMap
  } = getVisibleRows(apiRef);
  const createGroupAggregationLookup = (groupNode) => {
    let children2 = groupNode.children;
    if (applySorting) {
      children2 = children2.toSorted((a, b) => rowIdToIndexMap.get(a) - rowIdToIndexMap.get(b));
    }
    for (let i2 = 0; i2 < children2.length; i2 += 1) {
      const childId = children2[i2];
      const childNode = rowTree[childId];
      if (childNode.type === "group") {
        createGroupAggregationLookup(childNode);
      }
    }
    const position = getAggregationPosition(groupNode);
    if (isDataSource) {
      if (position !== null) {
        aggregationLookup[groupNode.id] = getGroupAggregatedValueDataSource(groupNode.id, apiRef, aggregatedFields, position);
      }
    } else if (groupNode.children.length) {
      const result = getGroupAggregatedValue(groupNode.id, apiRef, aggregationRowsScope, aggregatedFields, aggregationRules, position, applySorting, valueGetters, apiRef.current, groupAggregatedValuesLookup, columnsLookup);
      groupAggregatedValuesLookup.set(groupNode.id, result.aggregatedValues);
      if (position !== null) {
        aggregationLookup[groupNode.id] = result.groupAggregationLookup;
      }
    }
  };
  createGroupAggregationLookup(rowTree[GRID_ROOT_GROUP_ID]);
  return aggregationLookup;
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/useGridAggregation.js
var aggregationStateInitializer = (state, props, apiRef) => {
  var _a, _b;
  apiRef.current.caches.aggregation = {
    rulesOnLastColumnHydration: {},
    rulesOnLastRowHydration: {}
  };
  return _extends({}, state, {
    aggregation: {
      model: props.aggregationModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.aggregation) == null ? void 0 : _b.model) ?? {}
    }
  });
};
var useGridAggregation = (apiRef, props) => {
  apiRef.current.registerControlState({
    stateId: "aggregation",
    propModel: props.aggregationModel,
    propOnChange: props.onAggregationModelChange,
    stateSelector: gridAggregationModelSelector,
    changeEvent: "aggregationModelChange"
  });
  const setAggregationModel = React302.useCallback((model) => {
    const currentModel = gridAggregationModelSelector(apiRef);
    if (currentModel !== model) {
      apiRef.current.setState(mergeStateWithAggregationModel(model));
    }
  }, [apiRef]);
  const abortControllerRef = React302.useRef(null);
  const applyAggregation = React302.useCallback(() => {
    if (abortControllerRef.current) {
      abortControllerRef.current.abort();
    }
    const abortController = new AbortController();
    abortControllerRef.current = abortController;
    const aggregationRules = getAggregationRules(gridColumnLookupSelector(apiRef), gridAggregationModelSelector(apiRef), props.aggregationFunctions, !!props.dataSource);
    const aggregatedFields = Object.keys(aggregationRules);
    const currentAggregationLookup = gridAggregationLookupSelector(apiRef);
    const renderContext = gridRenderContextSelector(apiRef);
    const visibleColumns = gridVisibleColumnFieldsSelector(apiRef);
    const chunks = [];
    const sortedAggregatedFields = gridSortModelSelector(apiRef).map((s) => s.field).filter((field) => aggregatedFields.includes(field));
    const visibleAggregatedFields = visibleColumns.slice(renderContext.firstColumnIndex, renderContext.lastColumnIndex + 1).filter((field) => aggregatedFields.includes(field));
    const visibleAggregatedFieldsWithSort = [...visibleAggregatedFields, ...sortedAggregatedFields.filter((field) => !visibleAggregatedFields.includes(field))];
    const hasAggregatedSortedField = gridRowMaximumTreeDepthSelector(apiRef) > 1 && sortedAggregatedFields.length > 0;
    if (visibleAggregatedFieldsWithSort.length > 0) {
      chunks.push(visibleAggregatedFieldsWithSort);
    }
    const otherAggregatedFields = aggregatedFields.filter((field) => !visibleAggregatedFieldsWithSort.includes(field));
    const chunkSize = 20;
    for (let i2 = 0; i2 < otherAggregatedFields.length; i2 += chunkSize) {
      chunks.push(otherAggregatedFields.slice(i2, i2 + chunkSize));
    }
    let chunkIndex = 0;
    const aggregationLookup = {};
    let chunkStartTime = performance.now();
    const timeLimit = 1e3 / 120;
    const processChunk = () => {
      if (abortController.signal.aborted) {
        return;
      }
      const currentChunk = chunks[chunkIndex];
      if (!currentChunk) {
        apiRef.current.publishEvent("aggregationLookupSet");
        abortControllerRef.current = null;
        return;
      }
      const applySorting = shouldApplySorting(aggregationRules, currentChunk);
      const partialLookup = createAggregationLookup({
        apiRef,
        getAggregationPosition: props.getAggregationPosition,
        aggregatedFields: currentChunk,
        aggregationRules,
        aggregationRowsScope: props.aggregationRowsScope,
        isDataSource: !!props.dataSource,
        applySorting
      });
      for (const key of Object.keys(partialLookup)) {
        for (const field of Object.keys(partialLookup[key])) {
          aggregationLookup[key] ?? (aggregationLookup[key] = {});
          aggregationLookup[key][field] = partialLookup[key][field];
        }
      }
      apiRef.current.setState((state) => _extends({}, state, {
        aggregation: _extends({}, state.aggregation, {
          lookup: _extends({}, aggregationLookup)
        })
      }));
      if (chunkIndex === 0 && hasAggregatedSortedField) {
        apiRef.current.applySorting();
      }
      chunkIndex += 1;
      if (performance.now() - chunkStartTime < timeLimit) {
        processChunk();
        return;
      }
      setTimeout(() => {
        chunkStartTime = performance.now();
        processChunk();
      }, 0);
    };
    processChunk();
    if (aggregatedFields.length === 0 && !isObjectEmpty(currentAggregationLookup)) {
      apiRef.current.setState((state) => _extends({}, state, {
        aggregation: _extends({}, state.aggregation, {
          lookup: {}
        })
      }));
    }
  }, [apiRef, props.getAggregationPosition, props.aggregationFunctions, props.aggregationRowsScope, props.dataSource]);
  React302.useEffect(() => {
    return () => {
      if (abortControllerRef.current) {
        abortControllerRef.current.abort();
        abortControllerRef.current = null;
      }
    };
  }, []);
  const {
    schedule: deferredApplyAggregation
  } = useRunOncePerLoop(applyAggregation);
  const aggregationApi = {
    setAggregationModel
  };
  const aggregationPrivateApi = {
    applyAggregation
  };
  useGridApiMethod(apiRef, aggregationApi, "public");
  useGridApiMethod(apiRef, aggregationPrivateApi, "private");
  const addGetRowsParams = React302.useCallback((params) => {
    return _extends({}, params, {
      aggregationModel: gridAggregationModelSelector(apiRef)
    });
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "getRowsParams", addGetRowsParams);
  const checkAggregationRulesDiff = React302.useCallback(() => {
    const pivotingActive = gridPivotActiveSelector(apiRef);
    const {
      rulesOnLastRowHydration,
      rulesOnLastColumnHydration
    } = apiRef.current.caches.aggregation;
    const aggregationRules = props.disableAggregation ? {} : getAggregationRules(gridColumnLookupSelector(apiRef), gridAggregationModelSelector(apiRef), props.aggregationFunctions, !!props.dataSource);
    if ((!props.dataSource || pivotingActive) && !areAggregationRulesEqual(rulesOnLastRowHydration, aggregationRules)) {
      apiRef.current.requestPipeProcessorsApplication("hydrateRows");
      deferredApplyAggregation();
    }
    if (!areAggregationRulesEqual(rulesOnLastColumnHydration, aggregationRules)) {
      apiRef.current.requestPipeProcessorsApplication("hydrateColumns");
    }
  }, [apiRef, deferredApplyAggregation, props.aggregationFunctions, props.disableAggregation, props.dataSource]);
  useGridEvent(apiRef, "aggregationModelChange", checkAggregationRulesDiff);
  useGridEvent(apiRef, "columnsChange", checkAggregationRulesDiff);
  useGridEvent(apiRef, "filteredRowsSet", deferredApplyAggregation);
  const lastSortModel = React302.useRef(gridSortModelSelector(apiRef));
  useGridEvent(apiRef, "sortedRowsSet", () => {
    const sortModel = gridSortModelSelector(apiRef);
    if (lastSortModel.current === sortModel) {
      return;
    }
    lastSortModel.current = sortModel;
    const aggregationRules = getAggregationRules(gridColumnLookupSelector(apiRef), gridAggregationModelSelector(apiRef), props.aggregationFunctions, !!props.dataSource);
    const aggregatedFields = Object.keys(aggregationRules);
    if (!aggregatedFields.length) {
      return;
    }
    const needsSorting = shouldApplySorting(aggregationRules, aggregatedFields);
    if (!needsSorting) {
      return;
    }
    deferredApplyAggregation();
  });
  React302.useEffect(() => {
    if (props.aggregationModel !== void 0) {
      apiRef.current.setAggregationModel(props.aggregationModel);
    }
  }, [apiRef, props.aggregationModel]);
  React302.useEffect(() => {
    if (props.getAggregationPosition !== void 0) {
      deferredApplyAggregation();
    }
  }, [deferredApplyAggregation, props.getAggregationPosition]);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/useGridAggregationPreProcessors.js
var React303 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridApiContext.js
var useGridApiContext3 = useGridApiContext;

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridRootProps.js
var useGridRootProps3 = useGridRootProps2;

// node_modules/@mui/x-data-grid-premium/esm/components/GridAggregationHeader.js
var import_jsx_runtime208 = __toESM(require_jsx_runtime(), 1);
var _excluded77 = ["renderHeader"];
var GridAggregationHeaderRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AggregationColumnHeader",
  overridesResolver: (props, styles5) => {
    const {
      ownerState
    } = props;
    return [styles5.aggregationColumnHeader, ownerState.colDef.headerAlign && styles5[`aggregationColumnHeader--align${capitalize(ownerState.colDef.headerAlign)}`]];
  }
})({
  display: "flex",
  flexDirection: "column",
  [`&.${gridClasses["aggregationColumnHeader--alignRight"]}`]: {
    alignItems: "flex-end"
  },
  [`&.${gridClasses["aggregationColumnHeader--alignCenter"]}`]: {
    alignItems: "center"
  }
});
var GridAggregationFunctionLabel = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AggregationColumnHeaderLabel"
})({
  font: vars.typography.font.small,
  lineHeight: "normal",
  color: vars.colors.foreground.muted,
  marginTop: -1
});
var useUtilityClasses108 = (ownerState) => {
  const {
    classes: classes2,
    colDef
  } = ownerState;
  const slots = {
    root: ["aggregationColumnHeader", colDef.headerAlign && `aggregationColumnHeader--align${capitalize(colDef.headerAlign)}`],
    aggregationLabel: ["aggregationColumnHeaderLabel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridAggregationHeader(props) {
  const {
    renderHeader
  } = props, params = _objectWithoutPropertiesLoose(props, _excluded77);
  const {
    colDef,
    aggregation
  } = params;
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const ownerState = _extends({}, rootProps, {
    classes: rootProps.classes,
    colDef
  });
  const classes2 = useUtilityClasses108(ownerState);
  if (!aggregation) {
    return null;
  }
  const aggregationLabel = getAggregationFunctionLabel({
    apiRef,
    aggregationRule: aggregation.aggregationRule
  });
  return (0, import_jsx_runtime208.jsxs)(GridAggregationHeaderRoot, {
    ownerState,
    className: classes2.root,
    children: [renderHeader ? renderHeader(params) : (0, import_jsx_runtime208.jsx)(GridColumnHeaderTitle, {
      label: colDef.headerName ?? colDef.field,
      description: colDef.description,
      columnWidth: colDef.computedWidth
    }), (0, import_jsx_runtime208.jsx)(GridAggregationFunctionLabel, {
      ownerState,
      className: classes2.aggregationLabel,
      children: aggregationLabel
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/wrapColumnWithAggregation.js
var import_jsx_runtime209 = __toESM(require_jsx_runtime(), 1);
var _excluded78 = ["aggregationWrappedProperties"];
var getAggregationValueWrappedRenderCell = ({
  value: renderCell,
  aggregationRule,
  getCellAggregationResult,
  apiRef
}) => {
  const pivotActive = gridPivotActiveSelector(apiRef);
  const wrappedRenderCell = (params) => {
    const cellAggregationResult = getCellAggregationResult(params.id, params.field);
    if (cellAggregationResult != null) {
      if (!renderCell) {
        if (cellAggregationResult.position === "footer") {
          return (0, import_jsx_runtime209.jsx)(GridFooterCell, _extends({}, params));
        }
        if (pivotActive && cellAggregationResult.value === 0) {
          return null;
        }
        return params.formattedValue;
      }
      if (pivotActive && cellAggregationResult.value === 0) {
        return null;
      }
      const aggregationMeta = {
        hasCellUnit: aggregationRule.aggregationFunction.hasCellUnit ?? true,
        aggregationFunctionName: aggregationRule.aggregationFunctionName,
        position: cellAggregationResult.position
      };
      return renderCell(_extends({}, params, {
        aggregation: aggregationMeta
      }));
    }
    if (!renderCell) {
      return params.formattedValue;
    }
    return renderCell(params);
  };
  return wrappedRenderCell;
};
var getWrappedRenderHeader = ({
  value: renderHeader,
  aggregationRule
}) => {
  const wrappedRenderHeader = (params) => {
    if (!params.colDef) {
      return null;
    }
    return (0, import_jsx_runtime209.jsx)(GridAggregationHeader, _extends({}, params, {
      aggregation: {
        aggregationRule
      },
      renderHeader
    }));
  };
  if (true) wrappedRenderHeader.displayName = "wrappedRenderHeader";
  return wrappedRenderHeader;
};
var wrapColumnWithAggregationValue = (column, aggregationRule, apiRef) => {
  const getCellAggregationResult = (id, field) => {
    var _a, _b;
    let cellAggregationPosition = null;
    const rowNode = gridRowNodeSelector(apiRef, id);
    if (!rowNode) {
      return null;
    }
    if (rowNode.type === "group") {
      cellAggregationPosition = "inline";
    } else if (id.toString().startsWith("auto-generated-group-footer-")) {
      cellAggregationPosition = "footer";
    }
    if (cellAggregationPosition == null) {
      return null;
    }
    const groupId = cellAggregationPosition === "inline" ? id : rowNode.parent ?? "";
    const aggregationResult = (_b = (_a = gridAggregationLookupSelector(apiRef)) == null ? void 0 : _a[groupId]) == null ? void 0 : _b[field];
    if (!aggregationResult || aggregationResult.position !== cellAggregationPosition) {
      return null;
    }
    return aggregationResult;
  };
  let didWrapSomeProperty = false;
  const wrappedColumn = _extends({}, column, {
    aggregationWrappedProperties: []
  });
  const wrapColumnProperty = (property, wrapper) => {
    const originalValue = column[property];
    const wrappedProperty = wrapper({
      apiRef,
      value: originalValue,
      colDef: column,
      aggregationRule,
      getCellAggregationResult
    });
    if (wrappedProperty !== originalValue) {
      didWrapSomeProperty = true;
      wrappedColumn[property] = wrappedProperty;
      wrappedColumn.aggregationWrappedProperties.push({
        name: property,
        originalValue,
        wrappedValue: wrappedProperty
      });
    }
  };
  wrapColumnProperty("renderCell", getAggregationValueWrappedRenderCell);
  wrapColumnProperty("renderHeader", getWrappedRenderHeader);
  if (!didWrapSomeProperty) {
    return column;
  }
  return wrappedColumn;
};
var isColumnWrappedWithAggregation = (column) => {
  return typeof column.aggregationWrappedProperties !== "undefined";
};
var unwrapColumnFromAggregation = (column) => {
  if (!isColumnWrappedWithAggregation(column)) {
    return column;
  }
  const _ref = column, {
    aggregationWrappedProperties
  } = _ref, unwrappedColumn = _objectWithoutPropertiesLoose(_ref, _excluded78);
  aggregationWrappedProperties.forEach(({
    name,
    originalValue,
    wrappedValue
  }) => {
    if (wrappedValue !== unwrappedColumn[name]) {
      return;
    }
    unwrappedColumn[name] = originalValue;
  });
  return unwrappedColumn;
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/useGridAggregationPreProcessors.js
var useGridAggregationPreProcessors = (apiRef, props) => {
  const rulesOnLastColumnHydration = React303.useRef({});
  const updateAggregatedColumns = React303.useCallback((columnsState) => {
    const aggregationRules = props.disableAggregation ? {} : getAggregationRules(columnsState.lookup, gridAggregationModelSelector(apiRef), props.aggregationFunctions, !!props.dataSource);
    columnsState.orderedFields.forEach((field) => {
      const shouldHaveAggregationValue = !!aggregationRules[field];
      const haveAggregationColumnValue = !!rulesOnLastColumnHydration.current[field];
      let column = columnsState.lookup[field];
      if (haveAggregationColumnValue) {
        column = unwrapColumnFromAggregation(column);
      }
      if (shouldHaveAggregationValue) {
        column = wrapColumnWithAggregationValue(column, aggregationRules[field], apiRef);
      }
      columnsState.lookup[field] = column;
    });
    rulesOnLastColumnHydration.current = aggregationRules;
    apiRef.current.caches.aggregation.rulesOnLastColumnHydration = aggregationRules;
    return columnsState;
  }, [apiRef, props.aggregationFunctions, props.disableAggregation, props.dataSource]);
  const addGroupFooterRows = React303.useCallback((value) => {
    const aggregationRules = props.disableAggregation ? {} : getAggregationRules(gridColumnLookupSelector(apiRef), gridAggregationModelSelector(apiRef), props.aggregationFunctions, !!props.dataSource);
    const hasAggregationRule = Object.keys(aggregationRules).length > 0;
    if (Object.keys(apiRef.current.caches.aggregation.rulesOnLastRowHydration).length === 0 && !hasAggregationRule) {
      return value;
    }
    apiRef.current.caches.aggregation.rulesOnLastRowHydration = aggregationRules;
    return addFooterRows({
      apiRef,
      groupingParams: value,
      getAggregationPosition: props.getAggregationPosition,
      hasAggregationRule
    });
  }, [apiRef, props.disableAggregation, props.getAggregationPosition, props.aggregationFunctions, props.dataSource]);
  const addColumnMenuButtons = React303.useCallback((columnMenuItems, colDef) => {
    if (props.disableAggregation || !colDef.aggregable) {
      return columnMenuItems;
    }
    const availableAggregationFunctions = getAvailableAggregationFunctions({
      aggregationFunctions: props.aggregationFunctions,
      colDef,
      isDataSource: !!props.dataSource
    });
    if (availableAggregationFunctions.length === 0) {
      return columnMenuItems;
    }
    return [...columnMenuItems, "columnMenuAggregationItem"];
  }, [props.aggregationFunctions, props.disableAggregation, props.dataSource]);
  const stateExportPreProcessing = React303.useCallback((prevState) => {
    if (props.disableAggregation) {
      return prevState;
    }
    const aggregationModelToExport = gridAggregationModelSelector(apiRef);
    if (Object.values(aggregationModelToExport).length === 0) {
      return prevState;
    }
    return _extends({}, prevState, {
      aggregation: {
        model: aggregationModelToExport
      }
    });
  }, [apiRef, props.disableAggregation]);
  const stateRestorePreProcessing = React303.useCallback((params, context) => {
    var _a;
    if (props.disableAggregation) {
      return params;
    }
    const aggregationModel = (_a = context.stateToRestore.aggregation) == null ? void 0 : _a.model;
    if (aggregationModel != null) {
      apiRef.current.setState(mergeStateWithAggregationModel(aggregationModel));
    }
    return params;
  }, [apiRef, props.disableAggregation]);
  useGridRegisterPipeProcessor(apiRef, "hydrateColumns", updateAggregatedColumns);
  useGridRegisterPipeProcessor(apiRef, "hydrateRows", addGroupFooterRows);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuButtons);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/useGridRowGrouping.js
var React304 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/gridRowGroupingUtils.js
var getRowGroupingFieldFromGroupingCriteria = (groupingCriteria) => {
  if (groupingCriteria === null) {
    return GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD;
  }
  return `__row_group_by_columns_group_${groupingCriteria}__`;
};
var shouldApplyFilterItemOnGroup = (columnField, node) => {
  if (columnField === GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD) {
    return true;
  }
  const groupingCriteriaField = getRowGroupingCriteriaFromGroupingField(columnField);
  return groupingCriteriaField === node.groupingField;
};
var filterRowTreeFromGroupingColumns = (params) => {
  const {
    apiRef,
    rowTree,
    isRowMatchingFilters,
    filterModel,
    filterValueGetter
  } = params;
  const filteredRowsLookup = {};
  const filteredChildrenCountLookup = {};
  const filteredDescendantCountLookup = {};
  const filterCache = {};
  const filterTreeNode = (node, areAncestorsExpanded, ancestorsResults) => {
    const filterResults = {
      passingFilterItems: null,
      passingQuickFilterValues: null
    };
    let isPassingFiltering = false;
    if (isRowMatchingFilters && node.type !== "footer") {
      const shouldApplyItem = node.type === "group" && node.isAutoGenerated ? (columnField) => shouldApplyFilterItemOnGroup(columnField, node) : void 0;
      const row = apiRef.current.getRow(node.id);
      isRowMatchingFilters(row, shouldApplyItem, filterResults);
    } else {
      isPassingFiltering = true;
    }
    let filteredChildrenCount = 0;
    let filteredDescendantCount = 0;
    if (node.type === "group") {
      node.children.forEach((childId) => {
        const childNode = rowTree[childId];
        const childSubTreeSize = filterTreeNode(childNode, areAncestorsExpanded && !!node.childrenExpanded, [...ancestorsResults, filterResults]);
        filteredDescendantCount += childSubTreeSize;
        if (childSubTreeSize > 0) {
          filteredChildrenCount += 1;
        }
      });
    }
    if (isPassingFiltering === false) {
      if (node.type === "group") {
        isPassingFiltering = filteredDescendantCount > 0;
      } else {
        const allResults = [...ancestorsResults, filterResults];
        isPassingFiltering = passFilterLogic(allResults.map((result) => result.passingFilterItems), allResults.map((result) => result.passingQuickFilterValues), filterModel, filterValueGetter, params.apiRef, filterCache);
      }
    }
    if (!isPassingFiltering) {
      filteredRowsLookup[node.id] = false;
    }
    if (!isPassingFiltering) {
      return 0;
    }
    filteredChildrenCountLookup[node.id] = filteredChildrenCount;
    filteredDescendantCountLookup[node.id] = filteredDescendantCount;
    if (node.type !== "group") {
      return filteredDescendantCount + 1;
    }
    return filteredDescendantCount;
  };
  const nodes = Object.values(rowTree);
  for (let i2 = 0; i2 < nodes.length; i2 += 1) {
    const node = nodes[i2];
    if (node.depth === 0) {
      filterTreeNode(node, true, []);
    }
  }
  return {
    filteredRowsLookup,
    filteredChildrenCountLookup,
    filteredDescendantCountLookup
  };
};
var getColDefOverrides = (groupingColDefProp, fields, strategy) => {
  if (typeof groupingColDefProp === "function") {
    return groupingColDefProp({
      groupingName: strategy ?? RowGroupingStrategy.Default,
      fields
    });
  }
  return groupingColDefProp;
};
var mergeStateWithRowGroupingModel = (rowGroupingModel) => (state) => _extends({}, state, {
  rowGrouping: _extends({}, state.rowGrouping, {
    model: rowGroupingModel
  })
});
var setStrategyAvailability = (privateApiRef, disableRowGrouping, dataSource) => {
  const strategy = dataSource ? RowGroupingStrategy.DataSource : RowGroupingStrategy.Default;
  if (privateApiRef.current.getActiveStrategy(GridStrategyGroup.RowTree) === strategy) {
    return;
  }
  let isAvailable;
  if (disableRowGrouping) {
    isAvailable = () => false;
  } else {
    isAvailable = () => {
      const rowGroupingSanitizedModel = gridRowGroupingSanitizedModelSelector(privateApiRef);
      return rowGroupingSanitizedModel.length > 0;
    };
  }
  privateApiRef.current.setStrategyAvailability(GridStrategyGroup.RowTree, strategy, isAvailable);
};
var getCellGroupingCriteria = ({
  row,
  colDef,
  groupingRule,
  apiRef
}) => {
  let key;
  if (groupingRule.groupingValueGetter) {
    key = groupingRule.groupingValueGetter(row[groupingRule.field], row, colDef, apiRef);
  } else {
    key = getRowValue(row, colDef, apiRef);
  }
  return {
    key,
    field: groupingRule.field
  };
};
var getGroupingRules = ({
  sanitizedRowGroupingModel,
  columnsLookup
}) => sanitizedRowGroupingModel.map((field) => {
  var _a, _b;
  return {
    field,
    groupingValueGetter: (_a = columnsLookup[field]) == null ? void 0 : _a.groupingValueGetter,
    groupingValueSetter: (_b = columnsLookup[field]) == null ? void 0 : _b.groupingValueSetter
  };
});
var areGroupingRulesEqual = (newValue, previousValue) => {
  if (previousValue.length !== newValue.length) {
    return false;
  }
  return newValue.every((newRule, newRuleIndex) => {
    const previousRule = previousValue[newRuleIndex];
    if (previousRule.groupingValueGetter !== newRule.groupingValueGetter) {
      return false;
    }
    if (previousRule.groupingValueSetter !== newRule.groupingValueSetter) {
      return false;
    }
    if (previousRule.field !== newRule.field) {
      return false;
    }
    return true;
  });
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowReorder/rowGroupingReorderValidator.js
var validationRules2 = [
  // ===== Basic invalid cases =====
  {
    name: "same-position",
    applies: (ctx) => ctx.sourceNode.id === ctx.targetNode.id,
    isInvalid: () => true,
    message: "Source and target are the same"
  },
  {
    name: "adjacent-position",
    applies: (ctx) => commonReorderConditions.isAdjacentPosition(ctx),
    isInvalid: () => true,
    message: "Source and target are adjacent"
  },
  {
    name: "group-to-leaf",
    applies: commonReorderConditions.isGroupToLeaf,
    isInvalid: () => true,
    message: "Cannot drop group on leaf"
  },
  // ===== Group to Group Rules =====
  {
    name: "group-to-group-above-leaf-belongs-to-source",
    applies: (ctx) => commonReorderConditions.isGroupToGroup(ctx) && commonReorderConditions.isDropAbove(ctx) && commonReorderConditions.prevIsLeaf(ctx),
    isInvalid: commonReorderConditions.prevBelongsToSource,
    message: "Previous leaf belongs to source group or its descendants"
  },
  {
    name: "group-to-group-above-invalid-depth",
    applies: (ctx) => commonReorderConditions.isGroupToGroup(ctx) && commonReorderConditions.isDropAbove(ctx) && !commonReorderConditions.sameDepth(ctx) && !(ctx.targetNode.depth < ctx.sourceNode.depth && (commonReorderConditions.prevIsLeaf(ctx) || commonReorderConditions.prevIsGroup(ctx) && commonReorderConditions.prevDepthEqualsSource(ctx))),
    isInvalid: () => true,
    message: "Invalid depth configuration for group above group"
  },
  {
    name: "group-to-group-above-different-parent-depth",
    applies: (ctx) => commonReorderConditions.isGroupToGroup(ctx) && commonReorderConditions.isDropAbove(ctx) && commonReorderConditions.prevIsGroup(ctx) && commonReorderConditions.prevDepthEqualsSource(ctx) && commonReorderConditions.targetGroupExpanded(ctx),
    isInvalid: (ctx) => ctx.prevNode.depth !== ctx.sourceNode.depth,
    message: "Cannot reorder groups with different depths"
  },
  {
    name: "group-to-group-below-invalid-config",
    applies: (ctx) => commonReorderConditions.isGroupToGroup(ctx) && commonReorderConditions.isDropBelow(ctx),
    isInvalid: (ctx) => {
      if (commonReorderConditions.sameDepth(ctx) && commonReorderConditions.targetGroupCollapsed(ctx)) {
        return false;
      }
      if (commonReorderConditions.targetDepthIsSourceMinusOne(ctx) && commonReorderConditions.targetGroupExpanded(ctx) && commonReorderConditions.targetFirstChildIsGroupWithSourceDepth(ctx)) {
        return false;
      }
      return true;
    },
    message: "Invalid group below group configuration"
  },
  // ===== Leaf to Leaf Rules =====
  {
    name: "leaf-to-leaf-different-depth",
    applies: (ctx) => commonReorderConditions.isLeafToLeaf(ctx) && !commonReorderConditions.sameDepth(ctx),
    isInvalid: () => true,
    message: "Leaves at different depths cannot be reordered"
  },
  {
    name: "leaf-to-leaf-invalid-below",
    applies: (ctx) => commonReorderConditions.isLeafToLeaf(ctx) && commonReorderConditions.sameDepth(ctx) && !commonReorderConditions.sameParent(ctx) && commonReorderConditions.isDropBelow(ctx),
    isInvalid: (ctx) => !(commonReorderConditions.nextIsGroup(ctx) && ctx.sourceNode.depth > ctx.nextNode.depth) && !commonReorderConditions.nextIsLeaf(ctx),
    message: "Invalid leaf below leaf configuration"
  },
  // ===== Leaf to Group Rules =====
  {
    name: "leaf-to-group-above-no-prev-leaf",
    applies: (ctx) => commonReorderConditions.isLeafToGroup(ctx) && commonReorderConditions.isDropAbove(ctx),
    isInvalid: (ctx) => !commonReorderConditions.hasPrevNode(ctx) || !commonReorderConditions.prevIsLeaf(ctx),
    message: "No valid previous leaf for leaf above group"
  },
  {
    name: "leaf-to-group-above-depth-mismatch",
    applies: (ctx) => commonReorderConditions.isLeafToGroup(ctx) && commonReorderConditions.isDropAbove(ctx) && commonReorderConditions.prevIsLeaf(ctx) && !(ctx.sourceNode.depth > ctx.targetNode.depth && ctx.targetNode.depth === 0),
    isInvalid: (ctx) => ctx.prevNode.depth !== ctx.sourceNode.depth,
    message: "Previous node depth mismatch for leaf above group"
  },
  {
    name: "leaf-to-group-below-collapsed",
    applies: (ctx) => commonReorderConditions.isLeafToGroup(ctx) && commonReorderConditions.isDropBelow(ctx),
    isInvalid: commonReorderConditions.targetGroupCollapsed,
    message: "Cannot drop below collapsed group"
  },
  {
    name: "leaf-to-group-below-invalid-depth",
    applies: (ctx) => commonReorderConditions.isLeafToGroup(ctx) && commonReorderConditions.isDropBelow(ctx) && commonReorderConditions.targetGroupExpanded(ctx),
    isInvalid: (ctx) => {
      if (ctx.sourceNode.depth > ctx.targetNode.depth && ctx.targetNode.depth === ctx.sourceNode.depth - 1) {
        return false;
      }
      if (commonReorderConditions.targetFirstChildDepthEqualsSource(ctx)) {
        return false;
      }
      return true;
    },
    message: "Invalid depth configuration for leaf below group"
  }
];
var rowGroupingReorderValidator = new RowReorderValidator(validationRules2);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/useGridRowGrouping.js
var rowGroupingStateInitializer = (state, props, apiRef) => {
  var _a, _b;
  apiRef.current.caches.rowGrouping = {
    rulesOnLastRowTreeCreation: []
  };
  return _extends({}, state, {
    rowGrouping: {
      model: props.rowGroupingModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.rowGrouping) == null ? void 0 : _b.model) ?? []
    }
  });
};
var useGridRowGrouping = (apiRef, props) => {
  var _a, _b;
  apiRef.current.registerControlState({
    stateId: "rowGrouping",
    propModel: props.rowGroupingModel,
    propOnChange: props.onRowGroupingModelChange,
    stateSelector: gridRowGroupingModelSelector,
    changeEvent: "rowGroupingModelChange"
  });
  const setRowGroupingModel = React304.useCallback((model) => {
    const currentModel = gridRowGroupingModelSelector(apiRef);
    if (currentModel !== model) {
      apiRef.current.setState(mergeStateWithRowGroupingModel(model));
      setStrategyAvailability(apiRef, props.disableRowGrouping);
    }
  }, [apiRef, props.disableRowGrouping]);
  const addRowGroupingCriteria = React304.useCallback((field, groupingIndex) => {
    const currentModel = gridRowGroupingModelSelector(apiRef);
    if (currentModel.includes(field)) {
      return;
    }
    const cleanGroupingIndex = groupingIndex ?? currentModel.length;
    const updatedModel = [...currentModel.slice(0, cleanGroupingIndex), field, ...currentModel.slice(cleanGroupingIndex)];
    apiRef.current.setRowGroupingModel(updatedModel);
  }, [apiRef]);
  const removeRowGroupingCriteria = React304.useCallback((field) => {
    const currentModel = gridRowGroupingModelSelector(apiRef);
    if (!currentModel.includes(field)) {
      return;
    }
    apiRef.current.setRowGroupingModel(currentModel.filter((el) => el !== field));
  }, [apiRef]);
  const setRowGroupingCriteriaIndex = React304.useCallback((field, targetIndex) => {
    const currentModel = gridRowGroupingModelSelector(apiRef);
    const currentTargetIndex = currentModel.indexOf(field);
    if (currentTargetIndex === -1) {
      return;
    }
    const updatedModel = [...currentModel];
    updatedModel.splice(targetIndex, 0, updatedModel.splice(currentTargetIndex, 1)[0]);
    apiRef.current.setRowGroupingModel(updatedModel);
  }, [apiRef]);
  const rowGroupingApi = {
    setRowGroupingModel,
    addRowGroupingCriteria,
    removeRowGroupingCriteria,
    setRowGroupingCriteriaIndex
  };
  useGridApiMethod(apiRef, rowGroupingApi, "public");
  const addColumnMenuButtons = React304.useCallback((columnMenuItems, colDef) => {
    if (props.disableRowGrouping) {
      return columnMenuItems;
    }
    if (isGroupingColumn(colDef.field) || colDef.groupable) {
      return [...columnMenuItems, "columnMenuGroupingItem"];
    }
    return columnMenuItems;
  }, [props.disableRowGrouping]);
  const addGetRowsParams = React304.useCallback((params) => {
    return _extends({}, params, {
      groupFields: gridRowGroupingModelSelector(apiRef)
    });
  }, [apiRef]);
  const stateExportPreProcessing = React304.useCallback((prevState, context) => {
    var _a2, _b2;
    const rowGroupingModelToExport = gridRowGroupingModelSelector(apiRef);
    const shouldExportRowGroupingModel = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the model is controlled
      props.rowGroupingModel != null || // Always export if the model has been initialized
      ((_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.rowGrouping) == null ? void 0 : _b2.model) != null || // Export if the model is not empty
      Object.keys(rowGroupingModelToExport).length > 0
    );
    if (!shouldExportRowGroupingModel) {
      return prevState;
    }
    return _extends({}, prevState, {
      rowGrouping: {
        model: rowGroupingModelToExport
      }
    });
  }, [apiRef, props.rowGroupingModel, (_b = (_a = props.initialState) == null ? void 0 : _a.rowGrouping) == null ? void 0 : _b.model]);
  const stateRestorePreProcessing = React304.useCallback((params, context) => {
    var _a2;
    if (props.disableRowGrouping) {
      return params;
    }
    const rowGroupingModel = (_a2 = context.stateToRestore.rowGrouping) == null ? void 0 : _a2.model;
    if (rowGroupingModel != null) {
      apiRef.current.setState(mergeStateWithRowGroupingModel(rowGroupingModel));
    }
    return params;
  }, [apiRef, props.disableRowGrouping]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuButtons);
  useGridRegisterPipeProcessor(apiRef, "getRowsParams", addGetRowsParams);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  const handleCellKeyDown = React304.useCallback((params, event) => {
    const cellParams = apiRef.current.getCellParams(params.id, params.field);
    if (isGroupingColumn(cellParams.field) && event.key === " " && !event.shiftKey) {
      event.stopPropagation();
      event.preventDefault();
      if (params.rowNode.type !== "group") {
        return;
      }
      const isOnGroupingCell = props.rowGroupingColumnMode === "single" || getRowGroupingFieldFromGroupingCriteria(params.rowNode.groupingField) === params.field;
      if (!isOnGroupingCell) {
        return;
      }
      if (props.dataSource && !params.rowNode.childrenExpanded) {
        apiRef.current.dataSource.fetchRows(params.id);
        return;
      }
      apiRef.current.setRowChildrenExpansion(params.id, !params.rowNode.childrenExpanded);
    }
  }, [apiRef, props.rowGroupingColumnMode, props.dataSource]);
  const checkGroupingColumnsModelDiff = React304.useCallback(() => {
    const sanitizedRowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const rulesOnLastRowTreeCreation = apiRef.current.caches.rowGrouping.rulesOnLastRowTreeCreation || [];
    const groupingRules = getGroupingRules({
      sanitizedRowGroupingModel,
      columnsLookup: gridColumnLookupSelector(apiRef)
    });
    if (!areGroupingRulesEqual(rulesOnLastRowTreeCreation, groupingRules)) {
      apiRef.current.caches.rowGrouping.rulesOnLastRowTreeCreation = groupingRules;
      apiRef.current.requestPipeProcessorsApplication("hydrateColumns");
      setStrategyAvailability(apiRef, props.disableRowGrouping);
      if (apiRef.current.getActiveStrategy(GridStrategyGroup.RowTree) === RowGroupingStrategy.Default) {
        apiRef.current.publishEvent("activeStrategyProcessorChange", "rowTreeCreation");
      }
    }
  }, [apiRef, props.disableRowGrouping]);
  const isValidRowReorderProp = props.isValidRowReorder;
  const isRowReorderValid = React304.useCallback((initialValue, {
    sourceRowId,
    targetRowId,
    dropPosition,
    dragDirection
  }) => {
    if (gridRowMaximumTreeDepthSelector(apiRef) === 1 || props.treeData) {
      return initialValue;
    }
    const expandedSortedRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const expandedSortedRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const targetRowIndex = expandedSortedRowIndexLookup[targetRowId];
    const sourceNode = rowTree[sourceRowId];
    const targetNode = rowTree[targetRowId];
    const prevNode = targetRowIndex > 0 ? rowTree[expandedSortedRowIds[targetRowIndex - 1]] : null;
    const nextNode = targetRowIndex < expandedSortedRowIds.length - 1 ? rowTree[expandedSortedRowIds[targetRowIndex + 1]] : null;
    if (!sourceNode || !targetNode) {
      return false;
    }
    const context = {
      apiRef,
      sourceNode,
      targetNode,
      prevNode,
      nextNode,
      dropPosition,
      dragDirection
    };
    let isValid = rowGroupingReorderValidator.validate(context);
    if (isValid && isValidRowReorderProp) {
      isValid = isValidRowReorderProp(context);
    }
    if (isValid) {
      return true;
    }
    return false;
  }, [apiRef, props.treeData, isValidRowReorderProp]);
  useGridRegisterPipeProcessor(apiRef, "isRowReorderValid", isRowReorderValid);
  useGridEvent(apiRef, "cellKeyDown", handleCellKeyDown);
  useGridEvent(apiRef, "columnsChange", checkGroupingColumnsModelDiff);
  useGridEvent(apiRef, "rowGroupingModelChange", checkGroupingColumnsModelDiff);
  React304.useEffect(() => {
    if (props.rowGroupingModel !== void 0) {
      apiRef.current.setRowGroupingModel(props.rowGroupingModel);
    }
  }, [apiRef, props.rowGroupingModel]);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/useGridRowGroupingPreProcessors.js
var React307 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/GridGroupingColumnFooterCell.js
var import_jsx_runtime210 = __toESM(require_jsx_runtime(), 1);
function GridGroupingColumnFooterCell(props) {
  const rootProps = useGridRootProps3();
  const sx = {
    ml: 0
  };
  if (props.rowNode.parent == null) {
    sx.ml = 0;
  } else if (rootProps.rowGroupingColumnMode === "multiple") {
    sx.ml = 2;
  } else {
    sx.ml = `calc(var(--DataGrid-cellOffsetMultiplier) * ${vars.spacing(props.rowNode.depth)})`;
  }
  return (0, import_jsx_runtime210.jsx)(GridFooterCell, _extends({
    sx
  }, props));
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridGroupingCriteriaCell.js
var React305 = __toESM(require_react(), 1);
var import_jsx_runtime211 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses109 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["groupingCriteriaCell"],
    toggle: ["groupingCriteriaCellToggle"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridGroupingCriteriaCell(props) {
  var _a;
  const {
    id,
    field,
    rowNode,
    hideDescendantCount,
    formattedValue
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses109(ownerState);
  const filteredDescendantCountLookup = useGridSelector(apiRef, gridFilteredDescendantCountLookupSelector);
  const filteredDescendantCount = filteredDescendantCountLookup[rowNode.id] ?? 0;
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const maxTreeDepth = gridRowMaximumTreeDepthSelector(apiRef);
  const shouldShowToggleContainer = !pivotActive || maxTreeDepth > 2;
  const shouldShowToggleButton = !pivotActive || rowNode.depth < maxTreeDepth - 2;
  const Icon = rowNode.childrenExpanded ? rootProps.slots.groupingCriteriaCollapseIcon : rootProps.slots.groupingCriteriaExpandIcon;
  const handleKeyDown = (event) => {
    if (event.key === " ") {
      event.stopPropagation();
    }
    apiRef.current.publishEvent("cellKeyDown", props, event);
  };
  const handleClick = (event) => {
    apiRef.current.setRowChildrenExpansion(id, !rowNode.childrenExpanded);
    apiRef.current.setCellFocus(id, field);
    event.stopPropagation();
  };
  let cellContent;
  const colDef = apiRef.current.getColumn(rowNode.groupingField);
  if (typeof colDef.renderCell === "function") {
    cellContent = colDef.renderCell(props);
  } else if (typeof formattedValue !== "undefined") {
    cellContent = (0, import_jsx_runtime211.jsx)("span", {
      children: formattedValue
    });
  } else {
    cellContent = (0, import_jsx_runtime211.jsx)("span", {
      children: rowNode.groupingKey
    });
  }
  return (0, import_jsx_runtime211.jsxs)("div", {
    className: classes2.root,
    style: {
      marginLeft: rootProps.rowGroupingColumnMode === "multiple" ? 0 : `calc(var(--DataGrid-cellOffsetMultiplier) * ${rowNode.depth} * ${vars.spacing(1)})`
    },
    children: [shouldShowToggleContainer ? (0, import_jsx_runtime211.jsx)("div", {
      className: classes2.toggle,
      children: shouldShowToggleButton && filteredDescendantCount > 0 && (0, import_jsx_runtime211.jsx)(rootProps.slots.baseIconButton, _extends({
        size: "small",
        onClick: handleClick,
        onKeyDown: handleKeyDown,
        tabIndex: -1,
        "aria-label": rowNode.childrenExpanded ? apiRef.current.getLocaleText("treeDataCollapse") : apiRef.current.getLocaleText("treeDataExpand")
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
        children: (0, import_jsx_runtime211.jsx)(Icon, {
          fontSize: "inherit"
        })
      }))
    }) : null, cellContent, !hideDescendantCount && filteredDescendantCount > 0 ? (0, import_jsx_runtime211.jsxs)("span", {
      style: {
        whiteSpace: "pre"
      },
      children: [" (", filteredDescendantCount, ")"]
    }) : null]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridDataSourceGroupingCriteriaCell.js
var React306 = __toESM(require_react(), 1);
var import_jsx_runtime212 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses110 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["groupingCriteriaCell"],
    toggle: ["groupingCriteriaCellToggle"],
    loadingContainer: ["groupingCriteriaCellLoadingContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
function GridGroupingCriteriaCellIcon(props) {
  var _a;
  const apiRef = useGridPrivateApiContext();
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses110(rootProps);
  const {
    rowNode,
    id,
    field,
    descendantCount
  } = props;
  const isDataLoading = useGridSelector(apiRef, gridDataSourceLoadingIdSelector, id);
  const error = useGridSelector(apiRef, gridDataSourceErrorSelector, id);
  const handleClick = (event) => {
    if (!rowNode.childrenExpanded) {
      apiRef.current.dataSource.fetchRows(id);
    } else {
      apiRef.current.setRowChildrenExpansion(id, false);
      apiRef.current.removeChildrenRows(id);
    }
    apiRef.current.setCellFocus(id, field);
    event.stopPropagation();
  };
  const Icon = rowNode.childrenExpanded ? rootProps.slots.groupingCriteriaCollapseIcon : rootProps.slots.groupingCriteriaExpandIcon;
  if (isDataLoading) {
    return (0, import_jsx_runtime212.jsx)("div", {
      className: classes2.loadingContainer,
      children: (0, import_jsx_runtime212.jsx)(rootProps.slots.baseCircularProgress, {
        size: "1rem",
        color: "inherit"
      })
    });
  }
  return descendantCount === -1 || descendantCount > 0 ? (0, import_jsx_runtime212.jsx)(rootProps.slots.baseIconButton, _extends({
    size: "small",
    onClick: handleClick,
    tabIndex: -1,
    "aria-label": rowNode.childrenExpanded ? apiRef.current.getLocaleText("treeDataCollapse") : apiRef.current.getLocaleText("treeDataExpand")
  }, (_a = rootProps == null ? void 0 : rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    children: (0, import_jsx_runtime212.jsx)(rootProps.slots.baseTooltip, {
      title: (error == null ? void 0 : error.message) ?? null,
      children: (0, import_jsx_runtime212.jsx)(rootProps.slots.baseBadge, {
        variant: "dot",
        color: "error",
        invisible: !error,
        children: (0, import_jsx_runtime212.jsx)(Icon, {
          fontSize: "inherit"
        })
      })
    })
  })) : null;
}
function GridDataSourceGroupingCriteriaCell(props) {
  var _a, _b;
  const {
    id,
    field,
    rowNode,
    hideDescendantCount,
    formattedValue
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const row = useGridSelector(apiRef, gridRowSelector, id);
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const rowGroupingModelLength = useGridSelector(apiRef, gridRowGroupingModelSelector).length;
  const classes2 = useUtilityClasses110(rootProps);
  const shouldShowToggleContainer = !pivotActive || rowGroupingModelLength > 1;
  const shouldShowToggleButton = !pivotActive || rowNode.depth < rowGroupingModelLength - 1;
  let descendantCount = 0;
  if (row) {
    descendantCount = ((_b = (_a = rootProps.dataSource) == null ? void 0 : _a.getChildrenCount) == null ? void 0 : _b.call(_a, row)) ?? 0;
  }
  let cellContent;
  const colDef = apiRef.current.getColumn(rowNode.groupingField);
  if (typeof (colDef == null ? void 0 : colDef.renderCell) === "function") {
    cellContent = colDef.renderCell(props);
  } else if (typeof formattedValue !== "undefined") {
    cellContent = (0, import_jsx_runtime212.jsx)("span", {
      children: formattedValue
    });
  } else {
    cellContent = (0, import_jsx_runtime212.jsx)("span", {
      children: rowNode.groupingKey
    });
  }
  return (0, import_jsx_runtime212.jsxs)("div", {
    className: classes2.root,
    style: {
      marginLeft: rootProps.rowGroupingColumnMode === "multiple" ? 0 : `calc(var(--DataGrid-cellOffsetMultiplier) * ${vars.spacing(rowNode.depth)})`
    },
    children: [shouldShowToggleContainer && (0, import_jsx_runtime212.jsx)("div", {
      className: classes2.toggle,
      children: shouldShowToggleButton && (0, import_jsx_runtime212.jsx)(GridGroupingCriteriaCellIcon, {
        id,
        field,
        rowNode,
        row,
        descendantCount
      })
    }), cellContent, !hideDescendantCount && descendantCount > 0 ? (0, import_jsx_runtime212.jsxs)("span", {
      style: {
        whiteSpace: "pre"
      },
      children: [" (", descendantCount, ")"]
    }) : null]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridGroupingColumnLeafCell.js
var import_jsx_runtime213 = __toESM(require_jsx_runtime(), 1);
function GridGroupingColumnLeafCell(props) {
  const {
    rowNode
  } = props;
  const rootProps = useGridRootProps3();
  return (0, import_jsx_runtime213.jsx)("div", {
    style: {
      marginLeft: rootProps.rowGroupingColumnMode === "multiple" ? vars.spacing(1) : `calc(var(--DataGrid-cellOffsetMultiplier) * ${vars.spacing(rowNode.depth)})`
    },
    children: props.formattedValue ?? props.value
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/createGroupingColDef.js
var import_jsx_runtime214 = __toESM(require_jsx_runtime(), 1);
var _excluded79 = ["leafField", "mainGroupingCriteria", "hideDescendantCount"];
var _excluded218 = ["leafField", "mainGroupingCriteria", "hideDescendantCount"];
var GROUPING_COL_DEF_DEFAULT_PROPERTIES = _extends({}, GRID_STRING_COL_DEF, {
  type: "custom",
  disableReorder: true,
  chartable: false,
  aggregable: false
});
var GROUPING_COL_DEF_FORCED_PROPERTIES_DEFAULT = {
  editable: false,
  groupable: false
};
var GROUPING_COL_DEF_FORCED_PROPERTIES_DATA_SOURCE = _extends({}, GROUPING_COL_DEF_FORCED_PROPERTIES_DEFAULT, {
  // TODO: Support these features on the grouping column(s)
  filterable: false,
  sortable: false
});
var groupingFieldIndexComparator = (v1, v2, cellParams1, cellParams2) => {
  const model = gridRowGroupingSanitizedModelSelector({
    current: cellParams1.api
  });
  const groupingField1 = cellParams1.rowNode.groupingField ?? null;
  const groupingField2 = cellParams2.rowNode.groupingField ?? null;
  if (groupingField1 === groupingField2) {
    return 0;
  }
  if (groupingField1 == null) {
    return -1;
  }
  if (groupingField2 == null) {
    return 1;
  }
  if (model.indexOf(groupingField1) < model.indexOf(groupingField2)) {
    return -1;
  }
  return 1;
};
var getLeafProperties = (leafColDef) => ({
  headerName: leafColDef.headerName ?? leafColDef.field,
  sortable: leafColDef.sortable,
  filterable: leafColDef.filterable,
  valueOptions: isSingleSelectColDef(leafColDef) ? leafColDef.valueOptions : void 0,
  filterOperators: leafColDef.filterOperators,
  sortComparator: (v1, v2, cellParams1, cellParams2) => {
    if (cellParams1.rowNode.type === "leaf" && cellParams2.rowNode.type === "leaf") {
      return leafColDef.sortComparator(v1, v2, cellParams1, cellParams2);
    }
    return groupingFieldIndexComparator(v1, v2, cellParams1, cellParams2);
  }
});
var groupedByColValueFormatter = (groupedByColDef) => (value, row, _, apiRef) => {
  const rowId = gridRowIdSelector(apiRef, row);
  const rowNode = gridRowNodeSelector(apiRef, rowId);
  if (rowNode.type === "group" && rowNode.groupingField === groupedByColDef.field) {
    return groupedByColDef.valueFormatter(value, row, groupedByColDef, apiRef);
  }
  return value;
};
function getGroupingCriteriaProperties(groupedByColDef, rowGroupingColumnMode, rowGroupingModel = [], columnsLookup = {}) {
  let valueFormatter;
  if (rowGroupingColumnMode === "single" && rowGroupingModel.length > 1) {
    valueFormatter = (value, row, column, apiRef) => {
      const rowId = gridRowIdSelector(apiRef, row);
      const rowNode = gridRowNodeSelector(apiRef, rowId);
      if ((rowNode == null ? void 0 : rowNode.type) === "group") {
        const originalColDef = rowNode.groupingField ? columnsLookup[rowNode.groupingField] : null;
        if ((originalColDef == null ? void 0 : originalColDef.type) === "singleSelect") {
          return value;
        }
        const columnValueFormatter = originalColDef == null ? void 0 : originalColDef.valueFormatter;
        if (typeof columnValueFormatter === "function") {
          return columnValueFormatter(value, row, column, apiRef);
        }
      }
      return value;
    };
  } else {
    valueFormatter = groupedByColDef.valueFormatter ? groupedByColValueFormatter(groupedByColDef) : void 0;
  }
  const properties = {
    sortable: groupedByColDef.sortable,
    filterable: groupedByColDef.filterable,
    valueFormatter,
    valueOptions: isSingleSelectColDef(groupedByColDef) ? groupedByColDef.valueOptions : void 0,
    sortComparator: (v1, v2, cellParams1, cellParams2) => {
      if (cellParams1.rowNode.type === "group" && cellParams2.rowNode.type === "group" && cellParams1.rowNode.groupingField === cellParams2.rowNode.groupingField) {
        const colDef = cellParams1.api.getColumn(cellParams1.rowNode.groupingField);
        return colDef.sortComparator(v1, v2, cellParams1, cellParams2);
      }
      return groupingFieldIndexComparator(v1, v2, cellParams1, cellParams2);
    },
    filterOperators: groupedByColDef.filterOperators
  };
  const applyHeaderName = !(rowGroupingColumnMode === "single" && rowGroupingModel.length > 1);
  if (applyHeaderName) {
    properties.headerName = groupedByColDef.headerName ?? groupedByColDef.field;
  }
  return properties;
}
var createGroupingColDefForOneGroupingCriteria = ({
  columnsLookup,
  groupedByColDef,
  groupingCriteria,
  colDefOverride,
  strategy = RowGroupingStrategy.Default
}) => {
  const _ref = colDefOverride ?? {}, {
    leafField,
    mainGroupingCriteria,
    hideDescendantCount
  } = _ref, colDefOverrideProperties = _objectWithoutPropertiesLoose(_ref, _excluded79);
  const leafColDef = leafField ? columnsLookup[leafField] : null;
  const CriteriaCell = strategy === RowGroupingStrategy.Default ? GridGroupingCriteriaCell : GridDataSourceGroupingCriteriaCell;
  const commonProperties = {
    width: Math.max((groupedByColDef.width ?? GRID_STRING_COL_DEF.width) + 40, (leafColDef == null ? void 0 : leafColDef.width) ?? 0),
    renderCell: (params) => {
      if (params.rowNode.type === "footer" || params.rowNode.type === "pinnedRow") {
        return (0, import_jsx_runtime214.jsx)(GridGroupingColumnFooterCell, _extends({}, params));
      }
      if (params.rowNode.type === "leaf") {
        if (leafColDef) {
          const leafParams = _extends({}, params.api.getCellParams(params.id, leafField), {
            api: params.api,
            hasFocus: params.hasFocus
          });
          if (leafColDef.renderCell) {
            return leafColDef.renderCell(leafParams);
          }
          return (0, import_jsx_runtime214.jsx)(GridGroupingColumnLeafCell, _extends({}, leafParams));
        }
        return "";
      }
      if (params.rowNode.groupingField === groupingCriteria) {
        return (0, import_jsx_runtime214.jsx)(CriteriaCell, _extends({}, params, {
          hideDescendantCount
        }));
      }
      return "";
    },
    valueGetter: (value, row, column, apiRef) => {
      const rowId = gridRowIdSelector(apiRef, row);
      const rowNode = gridRowNodeSelector(apiRef, rowId);
      if (!rowNode || rowNode.type === "footer" || rowNode.type === "pinnedRow") {
        return void 0;
      }
      if (rowNode.type === "leaf") {
        if (leafColDef) {
          return apiRef.current.getCellValue(rowId, leafField);
        }
        return void 0;
      }
      if (rowNode.groupingField === groupingCriteria) {
        return rowNode.groupingKey;
      }
      return void 0;
    }
  };
  let sourceProperties;
  if (mainGroupingCriteria && mainGroupingCriteria === groupingCriteria) {
    sourceProperties = getGroupingCriteriaProperties(groupedByColDef, "multiple");
  } else if (leafColDef) {
    sourceProperties = getLeafProperties(leafColDef);
  } else {
    sourceProperties = getGroupingCriteriaProperties(groupedByColDef, "multiple");
  }
  const forcedProperties = _extends({
    field: getRowGroupingFieldFromGroupingCriteria(groupingCriteria)
  }, GROUPING_COL_DEF_FORCED_PROPERTIES_DEFAULT);
  return _extends({}, GROUPING_COL_DEF_DEFAULT_PROPERTIES, commonProperties, sourceProperties, colDefOverrideProperties, forcedProperties);
};
var createGroupingColDefForAllGroupingCriteria = ({
  apiRef,
  columnsLookup,
  rowGroupingModel,
  colDefOverride,
  strategy = RowGroupingStrategy.Default
}) => {
  const _ref2 = colDefOverride ?? {}, {
    leafField,
    mainGroupingCriteria,
    hideDescendantCount
  } = _ref2, colDefOverrideProperties = _objectWithoutPropertiesLoose(_ref2, _excluded218);
  const leafColDef = leafField ? columnsLookup[leafField] : null;
  const CriteriaCell = strategy === RowGroupingStrategy.Default ? GridGroupingCriteriaCell : GridDataSourceGroupingCriteriaCell;
  const commonProperties = {
    headerName: apiRef.current.getLocaleText("groupingColumnHeaderName"),
    width: Math.max(...rowGroupingModel.map((field) => (columnsLookup[field].width ?? GRID_STRING_COL_DEF.width) + 40), (leafColDef == null ? void 0 : leafColDef.width) ?? 0),
    renderCell: (params) => {
      if (params.rowNode.type === "footer" || params.rowNode.type === "pinnedRow") {
        return (0, import_jsx_runtime214.jsx)(GridGroupingColumnFooterCell, _extends({}, params));
      }
      if (params.rowNode.type === "leaf") {
        if (leafColDef) {
          const leafParams = _extends({}, params.api.getCellParams(params.id, leafField), {
            api: params.api,
            hasFocus: params.hasFocus
          });
          if (leafColDef.renderCell) {
            return leafColDef.renderCell(leafParams);
          }
          return (0, import_jsx_runtime214.jsx)(GridGroupingColumnLeafCell, _extends({}, leafParams));
        }
        return "";
      }
      return (0, import_jsx_runtime214.jsx)(CriteriaCell, _extends({}, params, {
        hideDescendantCount
      }));
    },
    valueGetter: (value, row) => {
      const rowId = gridRowIdSelector(apiRef, row);
      const rowNode = gridRowNodeSelector(apiRef, rowId);
      if (!rowNode || rowNode.type === "footer" || rowNode.type === "pinnedRow") {
        return void 0;
      }
      if (rowNode.type === "leaf") {
        if (leafColDef) {
          return apiRef.current.getCellValue(rowId, leafField);
        }
        return void 0;
      }
      return rowNode.groupingKey;
    }
  };
  if (true) commonProperties.renderCell.displayName = "commonProperties.renderCell";
  let sourceProperties;
  if (mainGroupingCriteria && rowGroupingModel.includes(mainGroupingCriteria)) {
    sourceProperties = getGroupingCriteriaProperties(columnsLookup[mainGroupingCriteria], "single", rowGroupingModel, columnsLookup);
  } else if (leafColDef) {
    sourceProperties = getLeafProperties(leafColDef);
  } else {
    sourceProperties = getGroupingCriteriaProperties(columnsLookup[rowGroupingModel[0]], "single", rowGroupingModel, columnsLookup);
  }
  const forcedProperties = _extends({
    field: GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD
  }, strategy === RowGroupingStrategy.Default ? GROUPING_COL_DEF_FORCED_PROPERTIES_DEFAULT : GROUPING_COL_DEF_FORCED_PROPERTIES_DATA_SOURCE);
  return _extends({}, GROUPING_COL_DEF_DEFAULT_PROPERTIES, commonProperties, sourceProperties, colDefOverrideProperties, forcedProperties);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/useGridRowGroupingPreProcessors.js
var useGridRowGroupingPreProcessors = (apiRef, props) => {
  const getGroupingColDefs = React307.useCallback((columnsState) => {
    if (props.disableRowGrouping) {
      return [];
    }
    const strategy = props.dataSource ? RowGroupingStrategy.DataSource : RowGroupingStrategy.Default;
    const groupingColDefProp = props.groupingColDef;
    const rowGroupingModel = gridRowGroupingModelSelector(apiRef).filter((field) => !!columnsState.lookup[field]);
    if (rowGroupingModel.length === 0) {
      return [];
    }
    switch (props.rowGroupingColumnMode) {
      case "single": {
        return [createGroupingColDefForAllGroupingCriteria({
          apiRef,
          rowGroupingModel,
          colDefOverride: getColDefOverrides(groupingColDefProp, rowGroupingModel, strategy),
          columnsLookup: columnsState.lookup,
          strategy
        })];
      }
      case "multiple": {
        return rowGroupingModel.map((groupingCriteria) => createGroupingColDefForOneGroupingCriteria({
          groupingCriteria,
          colDefOverride: getColDefOverrides(groupingColDefProp, [groupingCriteria]),
          groupedByColDef: columnsState.lookup[groupingCriteria],
          columnsLookup: columnsState.lookup,
          strategy
        }));
      }
      default: {
        return [];
      }
    }
  }, [apiRef, props.groupingColDef, props.rowGroupingColumnMode, props.disableRowGrouping, props.dataSource]);
  const updateGroupingColumn = React307.useCallback((columnsState) => {
    const groupingColDefs = getGroupingColDefs(columnsState);
    let newColumnFields = [];
    const newColumnsLookup = {};
    columnsState.orderedFields.forEach((field) => {
      if (!isGroupingColumn(field)) {
        newColumnFields.push(field);
        newColumnsLookup[field] = columnsState.lookup[field];
      }
    });
    groupingColDefs.forEach((groupingColDef) => {
      const matchingGroupingColDef = columnsState.lookup[groupingColDef.field];
      if (matchingGroupingColDef) {
        groupingColDef.width = matchingGroupingColDef.width;
        groupingColDef.flex = matchingGroupingColDef.flex;
      }
      newColumnsLookup[groupingColDef.field] = groupingColDef;
    });
    newColumnFields = [...groupingColDefs.map((colDef) => colDef.field), ...newColumnFields];
    columnsState.orderedFields = newColumnFields;
    columnsState.lookup = newColumnsLookup;
    return columnsState;
  }, [getGroupingColDefs]);
  const createRowTreeForRowGrouping = React307.useCallback((params) => {
    const sanitizedRowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const columnsLookup = gridColumnLookupSelector(apiRef);
    const groupingRules = getGroupingRules({
      sanitizedRowGroupingModel,
      columnsLookup
    });
    apiRef.current.caches.rowGrouping.rulesOnLastRowTreeCreation = groupingRules;
    const getRowTreeBuilderNode = (rowId) => {
      const row = params.dataRowIdToModelLookup[rowId];
      const parentPath = groupingRules.map((groupingRule) => getCellGroupingCriteria({
        row,
        groupingRule,
        colDef: columnsLookup[groupingRule.field],
        apiRef
      })).filter((cell) => cell.key != null);
      const leafGroupingCriteria = {
        key: rowId.toString(),
        field: null
      };
      return {
        path: [...parentPath, leafGroupingCriteria],
        id: rowId
      };
    };
    if (params.updates.type === "full") {
      return createRowTree({
        previousTree: params.previousTree,
        nodes: params.updates.rows.map(getRowTreeBuilderNode),
        defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
        isGroupExpandedByDefault: props.isGroupExpandedByDefault,
        groupingName: RowGroupingStrategy.Default
      });
    }
    return updateRowTree({
      nodes: {
        inserted: params.updates.actions.insert.map(getRowTreeBuilderNode),
        modified: params.updates.actions.modify.map(getRowTreeBuilderNode),
        removed: params.updates.actions.remove
      },
      previousTree: params.previousTree,
      previousTreeDepth: params.previousTreeDepths,
      defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
      isGroupExpandedByDefault: props.isGroupExpandedByDefault,
      groupingName: RowGroupingStrategy.Default
    });
  }, [apiRef, props.defaultGroupingExpansionDepth, props.isGroupExpandedByDefault]);
  const filterRows = React307.useCallback((params) => {
    const rowTree = gridRowTreeSelector(apiRef);
    return filterRowTreeFromGroupingColumns({
      rowTree,
      isRowMatchingFilters: params.isRowMatchingFilters,
      filterModel: params.filterModel,
      filterValueGetter: params.filterValueGetter,
      apiRef
    });
  }, [apiRef]);
  const sortRows = React307.useCallback((params) => {
    const rowTree = gridRowTreeSelector(apiRef);
    return sortRowTree({
      rowTree,
      sortRowList: params.sortRowList,
      disableChildrenSorting: false,
      shouldRenderGroupBelowLeaves: true
    });
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "hydrateColumns", updateGroupingColumn);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.Default, "rowTreeCreation", createRowTreeForRowGrouping);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.Default, "filtering", filterRows);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.Default, "sorting", sortRows);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.Default, "visibleRowsLookupCreation", getVisibleRowsLookup2);
  useFirstRender(() => {
    setStrategyAvailability(apiRef, props.disableRowGrouping, props.dataSource);
  });
  const isFirstRender = React307.useRef(true);
  React307.useEffect(() => {
    if (!isFirstRender.current) {
      setStrategyAvailability(apiRef, props.disableRowGrouping, props.dataSource);
    } else {
      isFirstRender.current = false;
    }
  }, [apiRef, props.disableRowGrouping, props.dataSource]);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowGrouping/useGridDataSourceRowGroupingPreProcessors.js
var React308 = __toESM(require_react(), 1);
var useGridDataSourceRowGroupingPreProcessors = (apiRef, props) => {
  const createRowTreeForRowGrouping = React308.useCallback((params) => {
    var _a, _b;
    const getGroupKey = (_a = props.dataSource) == null ? void 0 : _a.getGroupKey;
    if (!getGroupKey) {
      throw new Error("MUI X: No `getGroupKey` method provided with the dataSource.");
    }
    const getChildrenCount = (_b = props.dataSource) == null ? void 0 : _b.getChildrenCount;
    if (!getChildrenCount) {
      throw new Error("MUI X: No `getChildrenCount` method provided with the dataSource.");
    }
    const pivotingActive = gridPivotActiveSelector(apiRef);
    const sanitizedRowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const maxDepth = pivotingActive ? sanitizedRowGroupingModel.length - 1 : void 0;
    const columnsLookup = gridColumnLookupSelector(apiRef);
    const groupingRules = getGroupingRules({
      sanitizedRowGroupingModel,
      columnsLookup
    });
    apiRef.current.caches.rowGrouping.rulesOnLastRowTreeCreation = groupingRules;
    const getRowTreeBuilderNode = (rowId) => {
      const parentPath = params.updates.groupKeys ?? getParentPath(rowId, params);
      const leafKey = getGroupKey(params.dataRowIdToModelLookup[rowId]);
      return {
        id: rowId,
        path: [...parentPath, leafKey ?? rowId.toString()].map((key, i2) => {
          var _a2;
          return {
            key,
            field: ((_a2 = groupingRules[i2]) == null ? void 0 : _a2.field) ?? null
          };
        }),
        serverChildrenCount: getChildrenCount(params.dataRowIdToModelLookup[rowId]) ?? 0
      };
    };
    if (params.updates.type === "full") {
      return createRowTree({
        previousTree: params.previousTree,
        nodes: params.updates.rows.map(getRowTreeBuilderNode),
        defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
        isGroupExpandedByDefault: props.isGroupExpandedByDefault,
        groupingName: RowGroupingStrategy.DataSource,
        maxDepth
      });
    }
    return updateRowTree({
      nodes: {
        inserted: params.updates.actions.insert.map(getRowTreeBuilderNode),
        modified: params.updates.actions.modify.map(getRowTreeBuilderNode),
        removed: params.updates.actions.remove
      },
      previousTree: params.previousTree,
      previousGroupsToFetch: params.previousGroupsToFetch,
      previousTreeDepth: params.previousTreeDepths,
      defaultGroupingExpansionDepth: props.defaultGroupingExpansionDepth,
      isGroupExpandedByDefault: props.isGroupExpandedByDefault,
      groupingName: RowGroupingStrategy.DataSource,
      maxDepth
    });
  }, [apiRef, props.dataSource, props.defaultGroupingExpansionDepth, props.isGroupExpandedByDefault]);
  const filterRows = React308.useCallback(() => {
    const rowTree = gridRowTreeSelector(apiRef);
    return skipFiltering(rowTree);
  }, [apiRef]);
  const sortRows = React308.useCallback(() => {
    const rowTree = gridRowTreeSelector(apiRef);
    return skipSorting(rowTree);
  }, [apiRef]);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.DataSource, "rowTreeCreation", createRowTreeForRowGrouping);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.DataSource, "filtering", filterRows);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.DataSource, "sorting", sortRows);
  useGridRegisterStrategyProcessor(apiRef, RowGroupingStrategy.DataSource, "visibleRowsLookupCreation", getVisibleRowsLookup2);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/export/useGridExcelExport.js
var React355 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/export/serializer/utils.js
var getExcelJs = async () => {
  const excelJsModule = await import("./exceljs.min-67EGHQ2Z.js");
  return excelJsModule.default ?? excelJsModule;
};
var addColumnGroupingHeaders = (worksheet, columns, columnGroupPaths, columnGroupDetails) => {
  const maxDepth = Math.max(...columns.map(({
    key
  }) => {
    var _a;
    return ((_a = columnGroupPaths[key]) == null ? void 0 : _a.length) ?? 0;
  }));
  if (maxDepth === 0) {
    return;
  }
  for (let rowIndex = 0; rowIndex < maxDepth; rowIndex += 1) {
    const row = columns.map(({
      key
    }) => {
      const groupingPath = columnGroupPaths[key];
      if (groupingPath.length <= rowIndex) {
        return {
          groupId: null,
          parents: groupingPath
        };
      }
      return _extends({}, columnGroupDetails[groupingPath[rowIndex]], {
        parents: groupingPath.slice(0, rowIndex)
      });
    });
    const newRow = worksheet.addRow(row.map((group) => group.groupId === null ? null : (group == null ? void 0 : group.headerName) ?? group.groupId));
    const lastRowIndex = newRow.worksheet.rowCount;
    let leftIndex = 0;
    let rightIndex = 1;
    while (rightIndex < columns.length) {
      const {
        groupId: leftGroupId,
        parents: leftParents
      } = row[leftIndex];
      const {
        groupId: rightGroupId,
        parents: rightParents
      } = row[rightIndex];
      const areInSameGroup = leftGroupId === rightGroupId && leftParents.length === rightParents.length && leftParents.every((leftParent, index) => rightParents[index] === leftParent);
      if (areInSameGroup) {
        rightIndex += 1;
      } else {
        if (rightIndex - leftIndex > 1) {
          worksheet.mergeCells(lastRowIndex, leftIndex + 1, lastRowIndex, rightIndex);
        }
        leftIndex = rightIndex;
        rightIndex += 1;
      }
    }
    if (rightIndex - leftIndex > 1) {
      worksheet.mergeCells(lastRowIndex, leftIndex + 1, lastRowIndex, rightIndex);
    }
  }
};
function addSerializedRowToWorksheet(serializedRow, worksheet) {
  const {
    row,
    dataValidation,
    outlineLevel,
    mergedCells
  } = serializedRow;
  const newRow = worksheet.addRow(row);
  Object.keys(dataValidation).forEach((field) => {
    newRow.getCell(field).dataValidation = _extends({}, dataValidation[field]);
  });
  if (outlineLevel) {
    newRow.outlineLevel = outlineLevel;
  }
  const lastRowIndex = newRow.worksheet.rowCount;
  mergedCells.forEach((mergedCell) => {
    worksheet.mergeCells(lastRowIndex, mergedCell.leftIndex, lastRowIndex, mergedCell.rightIndex);
  });
}
async function createValueOptionsSheetIfNeeded(valueOptionsData, sheetName, workbook) {
  if (Object.keys(valueOptionsData).length === 0) {
    return;
  }
  const valueOptionsWorksheet = workbook.addWorksheet(sheetName);
  valueOptionsWorksheet.columns = Object.keys(valueOptionsData).map((key) => ({
    key
  }));
  Object.entries(valueOptionsData).forEach(([field, {
    values: values3
  }]) => {
    valueOptionsWorksheet.getColumn(field).values = values3;
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/export/serializer/excelSerializer.js
var getFormattedValueOptions = (colDef, row, valueOptions, api, callback) => {
  if (!colDef.valueOptions) {
    return;
  }
  const valueFormatter = colDef.valueFormatter;
  for (let i2 = 0; i2 < valueOptions.length; i2 += 1) {
    const option = valueOptions[i2];
    let value;
    if (valueFormatter) {
      if (typeof option === "object") {
        value = option.label;
      } else {
        value = String(colDef.valueFormatter(option, row, colDef, {
          current: api
        }));
      }
    } else {
      value = typeof option === "object" ? option.label : option;
    }
    callback(value, i2);
  }
};
var commaRegex = /,/g;
var commaReplacement = "CHAR(44)";
var serializeRowUnsafe = (id, columns, apiRef, defaultValueOptionsFormulae, options3) => {
  const serializedRow = {};
  const dataValidation = {};
  const mergedCells = [];
  const row = apiRef.current.getRow(id);
  const rowNode = apiRef.current.getRowNode(id);
  if (!row || !rowNode) {
    throw new Error(`No row with id #${id} found`);
  }
  const outlineLevel = rowNode.depth;
  const hasColSpan = gridHasColSpanSelector(apiRef);
  if (hasColSpan) {
    apiRef.current.calculateColSpan(id, 0, columns.length, columns);
  }
  columns.forEach((column, colIndex) => {
    const colSpanInfo = hasColSpan ? apiRef.current.unstable_getCellColSpanInfo(id, colIndex) : void 0;
    if (colSpanInfo && colSpanInfo.spannedByColSpan) {
      return;
    }
    if (colSpanInfo && colSpanInfo.cellProps.colSpan > 1) {
      mergedCells.push({
        leftIndex: colIndex + 1,
        rightIndex: colIndex + colSpanInfo.cellProps.colSpan
      });
    }
    let cellValue;
    switch (column.type) {
      case "singleSelect": {
        const castColumn = column;
        if (typeof castColumn.valueOptions === "function") {
          const valueOptions = castColumn.valueOptions({
            id,
            row,
            field: column.field
          });
          let formulae = '"';
          getFormattedValueOptions(castColumn, row, valueOptions, apiRef.current, (value, index) => {
            const formatted = value.toString().replace(commaRegex, commaReplacement);
            formulae += formatted;
            if (index < valueOptions.length - 1) {
              formulae += ",";
            }
          });
          formulae += '"';
          dataValidation[castColumn.field] = {
            type: "list",
            allowBlank: true,
            formulae: [formulae]
          };
        } else {
          const address = defaultValueOptionsFormulae[column.field].address;
          dataValidation[castColumn.field] = {
            type: "list",
            allowBlank: true,
            formulae: [address]
          };
        }
        const formattedValue = apiRef.current.getRowFormattedValue(row, castColumn);
        if (true) {
          if (String(formattedValue) === "[object Object]") {
            warnOnce(["MUI X: When the value of a field is an object or a `renderCell` is provided, the Excel export might not display the value correctly.", "You can provide a `valueFormatter` with a string representation to be used."]);
          }
        }
        if (isObject(formattedValue)) {
          serializedRow[castColumn.field] = formattedValue == null ? void 0 : formattedValue.label;
        } else {
          serializedRow[castColumn.field] = formattedValue;
        }
        break;
      }
      case "boolean":
      case "number":
        cellValue = apiRef.current.getRowValue(row, column);
        break;
      case "date":
      case "dateTime": {
        const value = apiRef.current.getRowValue(row, column);
        if (!value) {
          break;
        }
        const utcDate = new Date(Date.UTC(value.getFullYear(), value.getMonth(), value.getDate(), value.getHours(), value.getMinutes(), value.getSeconds()));
        serializedRow[column.field] = utcDate;
        break;
      }
      case "actions":
        break;
      default:
        cellValue = apiRef.current.getRowFormattedValue(row, column);
        if (true) {
          if (String(cellValue) === "[object Object]") {
            warnOnce(["MUI X: When the value of a field is an object or a `renderCell` is provided, the Excel export might not display the value correctly.", "You can provide a `valueFormatter` with a string representation to be used."]);
          }
        }
        break;
    }
    if (typeof cellValue === "string" && options3.escapeFormulas) {
      if (["=", "+", "-", "@", "	", "\r"].includes(cellValue[0])) {
        cellValue = `'${cellValue}`;
      }
    }
    if (typeof cellValue !== "undefined") {
      serializedRow[column.field] = cellValue;
    }
  });
  return {
    row: serializedRow,
    dataValidation,
    outlineLevel,
    mergedCells
  };
};
var defaultColumnsStyles = {
  [GRID_DATE_COL_DEF.type]: {
    numFmt: "dd.mm.yyyy"
  },
  [GRID_DATETIME_COL_DEF.type]: {
    numFmt: "dd.mm.yyyy hh:mm"
  }
};
var serializeColumn = (column, columnsStyles) => {
  const {
    field,
    type
  } = column;
  return {
    key: field,
    headerText: column.headerName ?? column.field,
    // Excel width must stay between 0 and 255 (https://support.microsoft.com/en-us/office/change-the-column-width-and-row-height-72f5e3cc-994d-43e8-ae58-9774a0905f46)
    // From the example of column width behavior (https://docs.microsoft.com/en-US/office/troubleshoot/excel/determine-column-widths#example-of-column-width-behavior)
    // a value of 10 corresponds to 75px. This is an approximation, because column width depends on the font-size
    width: Math.min(255, column.width ? column.width / 7.5 : 8.43),
    style: _extends({}, type && (defaultColumnsStyles == null ? void 0 : defaultColumnsStyles[type]), columnsStyles == null ? void 0 : columnsStyles[field])
  };
};
function serializeColumns(columns, styles5) {
  return columns.map((column) => serializeColumn(column, styles5));
}
async function getDataForValueOptionsSheet(columns, valueOptionsSheetName, api) {
  const excelJS = await getExcelJs();
  const workbook = new excelJS.Workbook();
  const worksheet = workbook.addWorksheet("Sheet1");
  const record = {};
  const worksheetColumns = [];
  for (let i2 = 0; i2 < columns.length; i2 += 1) {
    const column = columns[i2];
    const isCandidateColumn = isSingleSelectColDef(column) && Array.isArray(column.valueOptions);
    if (!isCandidateColumn) {
      continue;
    }
    worksheetColumns.push({
      key: column.field
    });
    worksheet.columns = worksheetColumns;
    const header = column.headerName ?? column.field;
    const values3 = [header];
    getFormattedValueOptions(column, {}, column.valueOptions, api, (value) => {
      values3.push(value);
    });
    const letter = worksheet.getColumn(column.field).letter;
    const address = `${valueOptionsSheetName}!$${letter}$2:$${letter}$${values3.length}`;
    record[column.field] = {
      values: values3,
      address
    };
  }
  return record;
}
async function buildExcel(options3, apiRef) {
  const {
    columns,
    rowIds,
    includeHeaders,
    includeColumnGroupsHeaders,
    valueOptionsSheetName = "Options",
    exceljsPreProcess,
    exceljsPostProcess,
    columnsStyles = {}
  } = options3;
  const excelJS = await getExcelJs();
  const workbook = new excelJS.Workbook();
  const worksheet = workbook.addWorksheet("Sheet1");
  const serializedColumns = serializeColumns(columns, columnsStyles);
  worksheet.columns = serializedColumns;
  if (exceljsPreProcess) {
    await exceljsPreProcess({
      workbook,
      worksheet
    });
  }
  if (includeColumnGroupsHeaders) {
    const columnGroupPaths = columns.reduce((acc, column) => {
      acc[column.field] = apiRef.current.getColumnGroupPath(column.field);
      return acc;
    }, {});
    addColumnGroupingHeaders(worksheet, serializedColumns, columnGroupPaths, apiRef.current.getAllGroupDetails());
  }
  if (includeHeaders) {
    worksheet.addRow(columns.map((column) => column.headerName ?? column.field));
  }
  const valueOptionsData = await getDataForValueOptionsSheet(columns, valueOptionsSheetName, apiRef.current);
  createValueOptionsSheetIfNeeded(valueOptionsData, valueOptionsSheetName, workbook);
  apiRef.current.resetColSpan();
  rowIds.forEach((id) => {
    const serializedRow = serializeRowUnsafe(id, columns, apiRef, valueOptionsData, options3);
    addSerializedRowToWorksheet(serializedRow, worksheet);
  });
  apiRef.current.resetColSpan();
  if (exceljsPostProcess) {
    await exceljsPostProcess({
      workbook,
      worksheet
    });
  }
  return workbook;
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridExcelExportMenuItem.js
var import_prop_types150 = __toESM(require_prop_types(), 1);
var import_jsx_runtime215 = __toESM(require_jsx_runtime(), 1);
var _excluded80 = ["hideMenu", "options"];
function GridExcelExportMenuItem(props) {
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const {
    hideMenu,
    options: options3
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded80);
  return (0, import_jsx_runtime215.jsx)(rootProps.slots.baseMenuItem, _extends({
    onClick: () => {
      apiRef.current.exportDataAsExcel(options3);
      hideMenu == null ? void 0 : hideMenu();
    }
  }, other, {
    children: apiRef.current.getLocaleText("toolbarExportExcel")
  }));
}
true ? GridExcelExportMenuItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  hideMenu: import_prop_types150.default.func,
  options: import_prop_types150.default.shape({
    allColumns: import_prop_types150.default.bool,
    columnsStyles: import_prop_types150.default.object,
    disableToolbarButton: import_prop_types150.default.bool,
    escapeFormulas: import_prop_types150.default.bool,
    exceljsPostProcess: import_prop_types150.default.func,
    exceljsPreProcess: import_prop_types150.default.func,
    fields: import_prop_types150.default.arrayOf(import_prop_types150.default.string),
    fileName: import_prop_types150.default.string,
    getRowsToExport: import_prop_types150.default.func,
    includeColumnGroupsHeaders: import_prop_types150.default.bool,
    includeHeaders: import_prop_types150.default.bool,
    valueOptionsSheetName: import_prop_types150.default.string,
    worker: import_prop_types150.default.func
  })
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/material/icons.js
var import_jsx_runtime216 = __toESM(require_jsx_runtime(), 1);
var GridWorkspacesIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("g", {
  children: (0, import_jsx_runtime216.jsx)("path", {
    d: "M6,13c-2.2,0-4,1.8-4,4s1.8,4,4,4s4-1.8,4-4S8.2,13,6,13z M12,3C9.8,3,8,4.8,8,7s1.8,4,4,4s4-1.8,4-4S14.2,3,12,3z M18,13 c-2.2,0-4,1.8-4,4s1.8,4,4,4s4-1.8,4-4S20.2,13,18,13z"
  })
}), "Workspaces");
var GridGroupWorkIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zM8 17.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5zM9.5 8c0-1.38 1.12-2.5 2.5-2.5s2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5S9.5 9.38 9.5 8zm6.5 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"
}), "GroupWork");
var GridFunctionsIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M18 4H6v2l6.5 6L6 18v2h12v-3h-7l5-5-5-5h7z"
}), "Functions");
var GridSendIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M2.01 21 23 12 2.01 3 2 10l15 2-15 2z"
}), "Send");
var GridMicIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M12 14c1.66 0 2.99-1.34 2.99-3L15 5c0-1.66-1.34-3-3-3S9 3.34 9 5v6c0 1.66 1.34 3 3 3m5.3-3c0 3-2.54 5.1-5.3 5.1S6.7 14 6.7 11H5c0 3.41 2.72 6.23 6 6.72V21h2v-3.28c3.28-.48 6-3.3 6-6.72z"
}), "Mic");
var GridMicOffIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M19 11h-1.7c0 .74-.16 1.43-.43 2.05l1.23 1.23c.56-.98.9-2.09.9-3.28m-4.02.17c0-.06.02-.11.02-.17V5c0-1.66-1.34-3-3-3S9 3.34 9 5v.18zM4.27 3 3 4.27l6.01 6.01V11c0 1.66 1.33 3 2.99 3 .22 0 .44-.03.65-.08l1.66 1.66c-.71.33-1.5.52-2.31.52-2.76 0-5.3-2.1-5.3-5.1H5c0 3.41 2.72 6.23 6 6.72V21h2v-3.28c.91-.13 1.77-.45 2.54-.9L19.73 21 21 19.73z"
}), "MicOff");
var GridMoveToTopIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "m7.41 18.205 4.59-4.59 4.59 4.59 1.41-1.41-6-6-6 6 1.41 1.41ZM6 7.795v-2h12v2H6Z"
}), "MoveToTop");
var GridMoveToBottomIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M16.59 5.795 12 10.385l-4.59-4.59L6 7.205l6 6 6-6-1.41-1.41ZM18 16.205v2H6v-2h12Z"
}), "MoveToBottom");
var GridExpandLessIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "m12 8.295-6 6 1.41 1.41 4.59-4.58 4.59 4.58 1.41-1.41-6-6Z"
}), "ExpandLess");
var GridPivotIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M10 8h11V5c0-1.1-.9-2-2-2h-9zM3 8h5V3H5c-1.1 0-2 .9-2 2zm2 13h3V10H3v9c0 1.1.9 2 2 2m8 1-4-4 4-4zm1-9 4-4 4 4zm.58 6H13v-2h1.58c1.33 0 2.42-1.08 2.42-2.42V13h2v1.58c0 2.44-1.98 4.42-4.42 4.42"
}), "Pivot");
var GridAssistantIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M19 2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h4l3 3 3-3h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2m-5.12 10.88L12 17l-1.88-4.12L6 11l4.12-1.88L12 5l1.88 4.12L18 11z"
}), "Assistant");
var GridPromptIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "m19 9 1.25-2.75L23 5l-2.75-1.25L19 1l-1.25 2.75L15 5l2.75 1.25zm-7.5.5L9 4 6.5 9.5 1 12l5.5 2.5L9 20l2.5-5.5L17 12zM19 15l-1.25 2.75L15 19l2.75 1.25L19 23l1.25-2.75L23 19l-2.75-1.25z"
}), "Prompt");
var GridRerunIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M12 5V1L7 6l5 5V7c3.31 0 6 2.69 6 6s-2.69 6-6 6-6-2.69-6-6H4c0 4.42 3.58 8 8 8s8-3.58 8-8-3.58-8-8-8"
}), "Rerun");
var GridHistoryIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M13 3c-4.97 0-9 4.03-9 9H1l3.89 3.89.07.14L9 12H6c0-3.87 3.13-7 7-7s7 3.13 7 7-3.13 7-7 7c-1.93 0-3.68-.79-4.94-2.06l-1.42 1.42C8.27 19.99 10.51 21 13 21c4.97 0 9-4.03 9-9s-4.03-9-9-9m-1 5v5l4.28 2.54.72-1.21-3.5-2.08V8z"
}), "History");
var GridChartsIcon = createSvgIcon2((0, import_jsx_runtime216.jsxs)("g", {
  children: [(0, import_jsx_runtime216.jsx)("path", {
    d: "M0 0h24v24H0z",
    fill: "none"
  }), (0, import_jsx_runtime216.jsx)("path", {
    d: "M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zM9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z"
  })]
}), "Charts");
var GridSyncIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M10 4.26v2.09C7.67 7.18 6 9.39 6 12c0 1.77.78 3.34 2 4.44V14h2v6H4v-2h2.73C5.06 16.54 4 14.4 4 12c0-3.73 2.55-6.85 6-7.74M20 4h-6v6h2V7.56c1.22 1.1 2 2.67 2 4.44h2c0-2.4-1.06-4.54-2.73-6H20zm0 13v-1c0-1.1-.9-2-2-2s-2 .9-2 2v1c-.55 0-1 .45-1 1v3c0 .55.45 1 1 1h4c.55 0 1-.45 1-1v-3c0-.55-.45-1-1-1m-1 0h-2v-1c0-.55.45-1 1-1s1 .45 1 1z"
}), "Sync");
var GridSyncDisabledIcon = createSvgIcon2((0, import_jsx_runtime216.jsx)("path", {
  d: "M10 6.35V4.26c-.8.21-1.55.54-2.23.96l1.46 1.46c.25-.12.5-.24.77-.33m-7.14-.94 2.36 2.36C4.45 8.99 4 10.44 4 12c0 2.21.91 4.2 2.36 5.64L4 20h6v-6l-2.24 2.24C6.68 15.15 6 13.66 6 12c0-1 .25-1.94.68-2.77l8.08 8.08c-.25.13-.5.25-.77.34v2.09c.8-.21 1.55-.54 2.23-.96l2.36 2.36 1.27-1.27L4.14 4.14zM20 4h-6v6l2.24-2.24C17.32 8.85 18 10.34 18 12c0 1-.25 1.94-.68 2.77l1.46 1.46C19.55 15.01 20 13.56 20 12c0-2.21-.91-4.2-2.36-5.64z"
}), "SyncDisabled");

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuAggregationItem.js
var React309 = __toESM(require_react(), 1);
var import_prop_types151 = __toESM(require_prop_types(), 1);
var import_jsx_runtime217 = __toESM(require_jsx_runtime(), 1);
var import_react12 = __toESM(require_react(), 1);
var _excluded81 = ["native"];
function GridColumnMenuAggregationItem(props) {
  var _a, _b;
  const {
    colDef
  } = props;
  const apiRef = useGridApiContext3();
  const inputRef = React309.useRef(null);
  const rootProps = useGridRootProps3();
  const id = useId();
  const aggregationModel = useGridSelector(apiRef, gridAggregationModelSelector);
  const availableAggregationFunctions = React309.useMemo(() => getAvailableAggregationFunctions({
    aggregationFunctions: rootProps.aggregationFunctions,
    colDef,
    isDataSource: !!rootProps.dataSource
  }), [colDef, rootProps.aggregationFunctions, rootProps.dataSource]);
  const _ref = ((_a = rootProps.slotProps) == null ? void 0 : _a.baseSelect) || {}, {
    native: isBaseSelectNative = false
  } = _ref, baseSelectProps = _objectWithoutPropertiesLoose(_ref, _excluded81);
  const baseSelectOptionProps = ((_b = rootProps.slotProps) == null ? void 0 : _b.baseSelectOption) || {};
  const selectedAggregationRule = React309.useMemo(() => {
    if (!colDef || !aggregationModel[colDef.field]) {
      return "";
    }
    const aggregationFunctionName = aggregationModel[colDef.field];
    if (canColumnHaveAggregationFunction({
      colDef,
      aggregationFunctionName,
      aggregationFunction: rootProps.aggregationFunctions[aggregationFunctionName],
      isDataSource: !!rootProps.dataSource
    })) {
      return aggregationFunctionName;
    }
    return "";
  }, [rootProps.aggregationFunctions, rootProps.dataSource, aggregationModel, colDef]);
  const handleAggregationItemChange = (event) => {
    var _a2;
    const newAggregationItem = ((_a2 = event.target) == null ? void 0 : _a2.value) || void 0;
    const currentModel = gridAggregationModelSelector(apiRef);
    const _colDef$field = colDef.field, otherColumnItems = _objectWithoutPropertiesLoose(currentModel, [_colDef$field].map(toPropertyKey));
    const newModel = newAggregationItem == null ? otherColumnItems : _extends({}, otherColumnItems, {
      [colDef == null ? void 0 : colDef.field]: newAggregationItem
    });
    apiRef.current.setAggregationModel(newModel);
    apiRef.current.hideColumnMenu();
  };
  const label = apiRef.current.getLocaleText("aggregationMenuItemHeader");
  const handleMenuItemKeyDown = React309.useCallback((event) => {
    if (event.key === "Enter" || event.key === " ") {
      inputRef.current.focus();
    }
  }, []);
  const handleSelectKeyDown = React309.useCallback((event) => {
    if (event.key === "ArrowDown" || event.key === "ArrowUp" || event.key === " ") {
      event.stopPropagation();
    }
  }, []);
  return (0, import_jsx_runtime217.jsx)(rootProps.slots.baseMenuItem, {
    inert: true,
    iconStart: (0, import_jsx_runtime217.jsx)(rootProps.slots.columnMenuAggregationIcon, {
      fontSize: "small"
    }),
    onKeyDown: handleMenuItemKeyDown,
    children: (0, import_jsx_runtime217.jsxs)(rootProps.slots.baseSelect, _extends({
      labelId: `${id}-label`,
      id: `${id}-input`,
      value: selectedAggregationRule,
      label,
      onChange: handleAggregationItemChange,
      onKeyDown: handleSelectKeyDown,
      onBlur: (event) => event.stopPropagation(),
      native: isBaseSelectNative,
      fullWidth: true,
      size: "small",
      style: {
        minWidth: 150
      },
      slotProps: {
        htmlInput: {
          ref: inputRef
        }
      }
    }, baseSelectProps, {
      children: [(0, import_jsx_runtime217.jsx)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        native: isBaseSelectNative,
        value: "",
        children: "..."
      })), availableAggregationFunctions.map((aggFunc) => (0, import_react12.createElement)(rootProps.slots.baseSelectOption, _extends({}, baseSelectOptionProps, {
        key: aggFunc,
        value: aggFunc,
        native: isBaseSelectNative
      }), getAggregationFunctionLabel({
        apiRef,
        aggregationRule: {
          aggregationFunctionName: aggFunc,
          aggregationFunction: rootProps.aggregationFunctions[aggFunc]
        }
      })))]
    }))
  });
}
true ? GridColumnMenuAggregationItem.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  colDef: import_prop_types151.default.object.isRequired,
  onClick: import_prop_types151.default.func.isRequired
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptField.js
var React311 = __toESM(require_react(), 1);
var import_prop_types152 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptFieldContext.js
var React310 = __toESM(require_react(), 1);
var PromptFieldContext = React310.createContext(void 0);
if (true) PromptFieldContext.displayName = "PromptFieldContext";
function usePromptFieldContext() {
  const context = React310.useContext(PromptFieldContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context. Prompt Field subcomponents must be placed within a <PromptField /> component.");
  }
  return context;
}

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptField.js
var import_jsx_runtime218 = __toESM(require_jsx_runtime(), 1);
var _excluded83 = ["render", "className", "lang", "onRecordError", "onSubmit"];
var PromptField = forwardRef2(function PromptField2(props, ref) {
  const {
    render,
    className,
    lang,
    onRecordError,
    onSubmit
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded83);
  const [value, setValue] = React311.useState("");
  const [recording, setRecording] = React311.useState(false);
  const [disabled, setDisabled] = React311.useState(false);
  const state = React311.useMemo(() => ({
    value,
    recording,
    disabled
  }), [value, recording, disabled]);
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleOnSubmit = React311.useCallback(async (prompt) => {
    setDisabled(true);
    setValue("");
    await onSubmit(prompt);
    setDisabled(false);
  }, [onSubmit]);
  const contextValue = React311.useMemo(() => ({
    state,
    lang,
    onValueChange: setValue,
    onRecordingChange: setRecording,
    onSubmit: handleOnSubmit,
    onError: onRecordError
  }), [state, lang, onRecordError, handleOnSubmit]);
  const element = useComponentRenderer("div", render, _extends({
    className: resolvedClassName
  }, other, {
    ref
  }), state);
  return (0, import_jsx_runtime218.jsx)(PromptFieldContext.Provider, {
    value: contextValue,
    children: element
  });
});
if (true) PromptField.displayName = "PromptField";
true ? PromptField.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types152.default.oneOfType([import_prop_types152.default.func, import_prop_types152.default.string]),
  /**
   * Called when an speech recognition error occurs.
   * @param {string} error The error message
   */
  onRecordError: import_prop_types152.default.func,
  /**
   * Called when the user submits the prompt.
   * @param {string} prompt The prompt
   */
  onSubmit: import_prop_types152.default.func.isRequired,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types152.default.oneOfType([import_prop_types152.default.element, import_prop_types152.default.func])
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptFieldControl.js
var React312 = __toESM(require_react(), 1);
var import_prop_types153 = __toESM(require_prop_types(), 1);
var import_jsx_runtime219 = __toESM(require_jsx_runtime(), 1);
var _excluded84 = ["render", "className", "onChange", "onKeyDown"];
var PromptFieldControl = forwardRef2(function PromptFieldControl2(props, ref) {
  var _a;
  const {
    render,
    className,
    onChange,
    onKeyDown
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded84);
  const rootProps = useGridRootProps3();
  const {
    state,
    onValueChange,
    onSubmit
  } = usePromptFieldContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleChange = (event) => {
    onValueChange(event.target.value);
    onChange == null ? void 0 : onChange(event);
  };
  const handleKeyDown = (event) => {
    if (event.key === "Enter" && state.value.trim()) {
      onSubmit(state.value);
    }
    onKeyDown == null ? void 0 : onKeyDown(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseTextField, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, {
    value: state.value,
    className: resolvedClassName
  }, other, {
    onChange: handleChange,
    onKeyDown: handleKeyDown,
    ref
  }), state);
  return (0, import_jsx_runtime219.jsx)(React312.Fragment, {
    children: element
  });
});
if (true) PromptFieldControl.displayName = "PromptFieldControl";
true ? PromptFieldControl.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  autoComplete: import_prop_types153.default.string,
  autoFocus: import_prop_types153.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types153.default.oneOfType([import_prop_types153.default.func, import_prop_types153.default.string]),
  color: import_prop_types153.default.oneOf(["error", "primary"]),
  disabled: import_prop_types153.default.bool,
  error: import_prop_types153.default.bool,
  fullWidth: import_prop_types153.default.bool,
  helperText: import_prop_types153.default.string,
  id: import_prop_types153.default.string,
  inputRef: import_prop_types153.default.oneOfType([import_prop_types153.default.func, import_prop_types153.default.shape({
    current: import_prop_types153.default.object
  })]),
  label: import_prop_types153.default.node,
  multiline: import_prop_types153.default.bool,
  placeholder: import_prop_types153.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types153.default.oneOfType([import_prop_types153.default.element, import_prop_types153.default.func]),
  role: import_prop_types153.default.string,
  size: import_prop_types153.default.oneOf(["medium", "small"]),
  slotProps: import_prop_types153.default.object,
  style: import_prop_types153.default.object,
  tabIndex: import_prop_types153.default.number,
  type: import_prop_types153.default.string,
  value: import_prop_types153.default.string
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptFieldRecord.js
var React313 = __toESM(require_react(), 1);
var import_prop_types154 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-premium/esm/utils/speechRecognition.js
var BrowserSpeechRecognition = globalThis.SpeechRecognition || globalThis.webkitSpeechRecognition;
var IS_SPEECH_RECOGNITION_SUPPORTED = !!BrowserSpeechRecognition;

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptFieldRecord.js
var import_jsx_runtime220 = __toESM(require_jsx_runtime(), 1);
var _excluded85 = ["render", "className", "onClick"];
var PromptFieldRecord = forwardRef2(function PromptFieldRecord2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded85);
  const rootProps = useGridRootProps3();
  const {
    state,
    lang,
    onRecordingChange,
    onValueChange,
    onSubmit,
    onError
  } = usePromptFieldContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const recognition = useLazyRef(() => {
    if (!BrowserSpeechRecognition) {
      return {
        start: () => {
        },
        abort: () => {
        }
      };
    }
    const timeout2 = new Timeout();
    const instance = new BrowserSpeechRecognition();
    instance.continuous = true;
    instance.interimResults = true;
    instance.lang = lang;
    let finalResult = "";
    let interimResult = "";
    function start2(options3) {
      if (state.recording) {
        return;
      }
      onRecordingChange(true);
      instance.onresult = (event) => {
        finalResult = "";
        interimResult = "";
        if (typeof event.results === "undefined") {
          instance.stop();
          return;
        }
        for (let i2 = event.resultIndex; i2 < event.results.length; i2 += 1) {
          if (event.results[i2].isFinal) {
            finalResult += event.results[i2][0].transcript;
          } else {
            interimResult += event.results[i2][0].transcript;
          }
        }
        if (finalResult === "") {
          options3.onUpdate(interimResult);
        }
        timeout2.start(1e3, () => instance.stop());
      };
      instance.onsoundend = () => {
        instance.stop();
      };
      instance.onend = () => {
        options3.onDone(finalResult);
        onRecordingChange(false);
      };
      instance.onerror = (error) => {
        options3.onError(error.message);
        instance.stop();
        onRecordingChange(false);
      };
      instance.start();
    }
    function abort() {
      instance.abort();
    }
    return {
      start: start2,
      abort
    };
  }).current;
  const handleClick = (event) => {
    if (!state.recording) {
      recognition.start({
        onDone: onSubmit,
        onUpdate: onValueChange,
        onError: onError ?? (() => {
        })
      });
      return;
    }
    recognition.abort();
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseIconButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    className: resolvedClassName,
    disabled: state.disabled
  }, other, {
    ref,
    onClick: handleClick
  }), state);
  return (0, import_jsx_runtime220.jsx)(React313.Fragment, {
    children: element
  });
});
if (true) PromptFieldRecord.displayName = "PromptFieldRecord";
true ? PromptFieldRecord.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types154.default.oneOfType([import_prop_types154.default.func, import_prop_types154.default.string]),
  color: import_prop_types154.default.oneOf(["default", "inherit", "primary"]),
  disabled: import_prop_types154.default.bool,
  edge: import_prop_types154.default.oneOf(["end", "start", false]),
  id: import_prop_types154.default.string,
  label: import_prop_types154.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types154.default.oneOfType([import_prop_types154.default.element, import_prop_types154.default.func]),
  role: import_prop_types154.default.string,
  size: import_prop_types154.default.oneOf(["large", "medium", "small"]),
  style: import_prop_types154.default.object,
  tabIndex: import_prop_types154.default.number,
  title: import_prop_types154.default.string,
  touchRippleRef: import_prop_types154.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/PromptFieldSend.js
var React314 = __toESM(require_react(), 1);
var import_prop_types155 = __toESM(require_prop_types(), 1);
var import_jsx_runtime221 = __toESM(require_jsx_runtime(), 1);
var _excluded86 = ["render", "className", "onClick"];
var PromptFieldSend = forwardRef2(function PromptFieldSend2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded86);
  const rootProps = useGridRootProps3();
  const {
    state,
    onSubmit
  } = usePromptFieldContext();
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleClick = (event) => {
    onSubmit(state.value);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseIconButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
    className: resolvedClassName,
    disabled: state.disabled || state.recording || !state.value.trim()
  }, other, {
    onClick: handleClick,
    ref
  }), state);
  return (0, import_jsx_runtime221.jsx)(React314.Fragment, {
    children: element
  });
});
if (true) PromptFieldSend.displayName = "PromptFieldSend";
true ? PromptFieldSend.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override or extend the styles applied to the component.
   */
  className: import_prop_types155.default.oneOfType([import_prop_types155.default.func, import_prop_types155.default.string]),
  color: import_prop_types155.default.oneOf(["default", "inherit", "primary"]),
  disabled: import_prop_types155.default.bool,
  edge: import_prop_types155.default.oneOf(["end", "start", false]),
  id: import_prop_types155.default.string,
  label: import_prop_types155.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types155.default.oneOfType([import_prop_types155.default.element, import_prop_types155.default.func]),
  role: import_prop_types155.default.string,
  size: import_prop_types155.default.oneOf(["large", "medium", "small"]),
  style: import_prop_types155.default.object,
  tabIndex: import_prop_types155.default.number,
  title: import_prop_types155.default.string,
  touchRippleRef: import_prop_types155.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuRowGroupItem.js
var React315 = __toESM(require_react(), 1);
var import_jsx_runtime222 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuRowGroupItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext3();
  const rowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  const columnsLookup = useGridSelector(apiRef, gridColumnLookupSelector);
  const rootProps = useGridRootProps3();
  const renderUnGroupingMenuItem = (field) => {
    const ungroupColumn = (event) => {
      apiRef.current.removeRowGroupingCriteria(field);
      onClick(event);
    };
    const groupedColumn = columnsLookup[field];
    const name = groupedColumn.headerName ?? field;
    return (0, import_jsx_runtime222.jsx)(rootProps.slots.baseMenuItem, {
      onClick: ungroupColumn,
      disabled: !groupedColumn.groupable,
      iconStart: (0, import_jsx_runtime222.jsx)(rootProps.slots.columnMenuUngroupIcon, {
        fontSize: "small"
      }),
      children: apiRef.current.getLocaleText("unGroupColumn")(name)
    }, field);
  };
  if (true) renderUnGroupingMenuItem.displayName = "renderUnGroupingMenuItem";
  if (!colDef || !isGroupingColumn(colDef.field)) {
    return null;
  }
  if (colDef.field === GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD) {
    return (0, import_jsx_runtime222.jsx)(React315.Fragment, {
      children: rowGroupingModel.map(renderUnGroupingMenuItem)
    });
  }
  return renderUnGroupingMenuItem(getRowGroupingCriteriaFromGroupingField(colDef.field));
}

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuRowUngroupItem.js
var React316 = __toESM(require_react(), 1);
var import_jsx_runtime223 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuRowUngroupItem(props) {
  const {
    colDef,
    onClick
  } = props;
  const apiRef = useGridApiContext3();
  const rowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  const columnsLookup = useGridSelector(apiRef, gridColumnLookupSelector);
  const rootProps = useGridRootProps3();
  if (!colDef.groupable) {
    return null;
  }
  const ungroupColumn = (event) => {
    apiRef.current.removeRowGroupingCriteria(colDef.field);
    onClick(event);
  };
  const groupColumn = (event) => {
    apiRef.current.addRowGroupingCriteria(colDef.field);
    onClick(event);
  };
  const name = columnsLookup[colDef.field].headerName ?? colDef.field;
  if (rowGroupingModel.includes(colDef.field)) {
    return (0, import_jsx_runtime223.jsx)(rootProps.slots.baseMenuItem, {
      onClick: ungroupColumn,
      iconStart: (0, import_jsx_runtime223.jsx)(rootProps.slots.columnMenuUngroupIcon, {
        fontSize: "small"
      }),
      children: apiRef.current.getLocaleText("unGroupColumn")(name)
    });
  }
  return (0, import_jsx_runtime223.jsx)(rootProps.slots.baseMenuItem, {
    onClick: groupColumn,
    iconStart: (0, import_jsx_runtime223.jsx)(rootProps.slots.columnMenuGroupIcon, {
      fontSize: "small"
    }),
    children: apiRef.current.getLocaleText("groupColumn")(name)
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuManagePanelItem.js
var React319 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuPivotItem.js
var React317 = __toESM(require_react(), 1);
var import_jsx_runtime224 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuPivotItem(props) {
  const {
    onClick
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const isPivotPanelOpen = useGridSelector(apiRef, gridPivotPanelOpenSelector);
  const openPivotSettings = (event) => {
    onClick(event);
    apiRef.current.showSidebar(GridSidebarValue.Pivot);
  };
  if (rootProps.disablePivoting) {
    return null;
  }
  return (0, import_jsx_runtime224.jsx)(rootProps.slots.baseMenuItem, {
    onClick: openPivotSettings,
    iconStart: (0, import_jsx_runtime224.jsx)(rootProps.slots.pivotIcon, {
      fontSize: "small"
    }),
    disabled: isPivotPanelOpen,
    children: apiRef.current.getLocaleText("columnMenuManagePivot")
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuChartsItem.js
var React318 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/chartsIntegration/gridChartsIntegrationSelectors.js
var gridChartsIntegrationStateSelector = createRootSelector((state) => state.chartsIntegration);
var gridChartsIntegrationActiveChartIdSelector = createSelector3(gridChartsIntegrationStateSelector, (chartsIntegration) => chartsIntegration.activeChartId);
var gridChartsIntegrationChartsLookupSelector = createSelector3(gridChartsIntegrationStateSelector, (chartsIntegration) => chartsIntegration.charts);
var gridChartsPanelOpenSelector = createSelector3(gridSidebarStateSelector, (sidebar) => sidebar.value === GridSidebarValue.Charts && sidebar.open);
var gridChartableColumnsSelector = createSelectorMemoized2(gridColumnLookupSelector, gridPivotActiveSelector, gridPivotModelSelector, (columns, pivotActive, pivotModel) => {
  let chartableColumns = Object.values(columns).filter((column) => column.chartable);
  if (pivotActive) {
    const pivotColumns = pivotModel.columns.filter((column) => column.hidden !== true).map((column) => column.field);
    const pivotValues = pivotModel.values.filter((value) => value.hidden !== true).map((value) => value.field);
    if (pivotColumns.length > 0) {
      chartableColumns = chartableColumns.filter((column) => !pivotColumns.includes(column.field) && !pivotValues.includes(column.field));
    }
  }
  return chartableColumns.reduce((acc, column) => {
    acc[column.field] = column;
    return acc;
  }, {});
});
var gridChartsDimensionsSelector = createSelector3(gridChartsIntegrationStateSelector, (chartsIntegration, chartId) => {
  var _a;
  return ((_a = chartsIntegration.charts[chartId]) == null ? void 0 : _a.dimensions) || [];
});
var gridChartsValuesSelector = createSelector3(gridChartsIntegrationStateSelector, (chartsIntegration, chartId) => {
  var _a;
  return ((_a = chartsIntegration.charts[chartId]) == null ? void 0 : _a.values) || [];
});

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuChartsItem.js
var import_jsx_runtime225 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuChartsItem(props) {
  var _a;
  const {
    onClick
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const isChartsPanelOpen = useGridSelector(apiRef, gridChartsPanelOpenSelector);
  const openChartsSettings = (event) => {
    onClick(event);
    apiRef.current.setChartsPanelOpen(true);
  };
  if (!((_a = rootProps.experimentalFeatures) == null ? void 0 : _a.charts) || !rootProps.chartsIntegration) {
    return null;
  }
  return (0, import_jsx_runtime225.jsx)(rootProps.slots.baseMenuItem, {
    onClick: openChartsSettings,
    iconStart: (0, import_jsx_runtime225.jsx)(rootProps.slots.chartsIcon, {
      fontSize: "small"
    }),
    disabled: isChartsPanelOpen,
    children: apiRef.current.getLocaleText("columnMenuManageCharts")
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/columnMenu/menuItems/GridColumnMenuManagePanelItem.js
var import_jsx_runtime226 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuManagePanelItem(props) {
  return (0, import_jsx_runtime226.jsxs)(React319.Fragment, {
    children: [(0, import_jsx_runtime226.jsx)(GridColumnMenuPivotItem, _extends({}, props)), (0, import_jsx_runtime226.jsx)(GridColumnMenuChartsItem, _extends({}, props))]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridPremiumColumnMenu.js
var import_jsx_runtime227 = __toESM(require_jsx_runtime(), 1);
function GridColumnMenuGroupingItem(props) {
  const {
    colDef
  } = props;
  if (isGroupingColumn(colDef.field)) {
    return (0, import_jsx_runtime227.jsx)(GridColumnMenuRowGroupItem, _extends({}, props));
  }
  return (0, import_jsx_runtime227.jsx)(GridColumnMenuRowUngroupItem, _extends({}, props));
}
var GRID_COLUMN_MENU_SLOTS_PREMIUM = _extends({}, GRID_COLUMN_MENU_SLOTS_PRO, {
  columnMenuAggregationItem: GridColumnMenuAggregationItem,
  columnMenuGroupingItem: GridColumnMenuGroupingItem,
  columnMenuManagePanelItem: GridColumnMenuManagePanelItem
});
var GRID_COLUMN_MENU_SLOT_PROPS_PREMIUM = _extends({}, GRID_COLUMN_MENU_SLOT_PROPS_PRO, {
  columnMenuAggregationItem: {
    displayOrder: 23
  },
  columnMenuGroupingItem: {
    displayOrder: 27
  },
  columnMenuManagePanelItem: {
    displayOrder: 28
  }
});
var GridPremiumColumnMenu = forwardRef2(function GridPremiumColumnMenuSimple(props, ref) {
  return (0, import_jsx_runtime227.jsx)(GridGenericColumnMenu, _extends({}, props, {
    defaultSlots: GRID_COLUMN_MENU_SLOTS_PREMIUM,
    defaultSlotProps: GRID_COLUMN_MENU_SLOT_PROPS_PREMIUM,
    ref
  }));
});
if (true) GridPremiumColumnMenu.displayName = "GridPremiumColumnMenu";
GridPremiumColumnMenu.defaultSlots = GRID_COLUMN_MENU_SLOTS_PREMIUM;
GridPremiumColumnMenu.defaultSlotProps = GRID_COLUMN_MENU_SLOT_PROPS_PREMIUM;

// node_modules/@mui/x-data-grid-premium/esm/components/export/ExportExcel.js
var React320 = __toESM(require_react(), 1);
var import_prop_types156 = __toESM(require_prop_types(), 1);
var import_jsx_runtime228 = __toESM(require_jsx_runtime(), 1);
var _excluded87 = ["render", "options", "onClick"];
var ExportExcel = forwardRef2(function ExportExcel2(props, ref) {
  var _a;
  const {
    render,
    options: options3,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded87);
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const handleClick = (event) => {
    apiRef.current.exportDataAsExcel(options3);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({
    onClick: handleClick
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, other, {
    ref
  }));
  return (0, import_jsx_runtime228.jsx)(React320.Fragment, {
    children: element
  });
});
if (true) ExportExcel.displayName = "ExportExcel";
true ? ExportExcel.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  className: import_prop_types156.default.string,
  disabled: import_prop_types156.default.bool,
  id: import_prop_types156.default.string,
  /**
   * The options to apply on the Excel export.
   * @demos
   *   - [Excel export](/x/react-data-grid/export/#excel-export)
   */
  options: import_prop_types156.default.shape({
    allColumns: import_prop_types156.default.bool,
    columnsStyles: import_prop_types156.default.object,
    escapeFormulas: import_prop_types156.default.bool,
    exceljsPostProcess: import_prop_types156.default.func,
    exceljsPreProcess: import_prop_types156.default.func,
    fields: import_prop_types156.default.arrayOf(import_prop_types156.default.string),
    fileName: import_prop_types156.default.string,
    getRowsToExport: import_prop_types156.default.func,
    includeColumnGroupsHeaders: import_prop_types156.default.bool,
    includeHeaders: import_prop_types156.default.bool,
    valueOptionsSheetName: import_prop_types156.default.string,
    worker: import_prop_types156.default.func
  }),
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types156.default.oneOfType([import_prop_types156.default.element, import_prop_types156.default.func]),
  role: import_prop_types156.default.string,
  size: import_prop_types156.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types156.default.node,
  style: import_prop_types156.default.object,
  tabIndex: import_prop_types156.default.number,
  title: import_prop_types156.default.string,
  touchRippleRef: import_prop_types156.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/GridEmptyPivotOverlay.js
var import_prop_types157 = __toESM(require_prop_types(), 1);
var import_jsx_runtime229 = __toESM(require_jsx_runtime(), 1);
var GridEmptyPivotOverlay = forwardRef2(function GridEmptyPivotOverlay2(props, ref) {
  const apiRef = useGridApiContext3();
  return (0, import_jsx_runtime229.jsx)(GridOverlay, _extends({}, props, {
    ref,
    children: apiRef.current.getLocaleText("emptyPivotOverlayLabel")
  }));
});
if (true) GridEmptyPivotOverlay.displayName = "GridEmptyPivotOverlay";
true ? GridEmptyPivotOverlay.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  sx: import_prop_types157.default.oneOfType([import_prop_types157.default.arrayOf(import_prop_types157.default.oneOfType([import_prop_types157.default.func, import_prop_types157.default.object, import_prop_types157.default.bool])), import_prop_types157.default.func, import_prop_types157.default.object])
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/PivotPanelTrigger.js
var React321 = __toESM(require_react(), 1);
var import_prop_types158 = __toESM(require_prop_types(), 1);
var import_jsx_runtime230 = __toESM(require_jsx_runtime(), 1);
var _excluded88 = ["render", "className", "onClick", "onPointerUp"];
var PivotPanelTrigger = forwardRef2(function PivotPanelTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded88);
  const rootProps = useGridRootProps3();
  const buttonId = useId();
  const panelId = useId();
  const apiRef = useGridApiContext3();
  const open = useGridSelector(apiRef, gridPivotPanelOpenSelector);
  const active = useGridSelector(apiRef, gridPivotActiveSelector);
  const state = {
    open,
    active
  };
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleClick = (event) => {
    if (open) {
      apiRef.current.hideSidebar();
    } else {
      apiRef.current.showSidebar(GridSidebarValue.Pivot, panelId, buttonId);
    }
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    id: buttonId,
    // TODO: Hook up the panel/trigger IDs to the pivot panel
    "aria-haspopup": "true",
    "aria-expanded": open ? "true" : void 0,
    "aria-controls": open ? panelId : void 0,
    onClick: handleClick,
    className: resolvedClassName
  }, other, {
    ref
  }), state);
  return (0, import_jsx_runtime230.jsx)(React321.Fragment, {
    children: element
  });
});
if (true) PivotPanelTrigger.displayName = "PivotPanelTrigger";
true ? PivotPanelTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * A function to customize rendering of the component.
   */
  className: import_prop_types158.default.oneOfType([import_prop_types158.default.func, import_prop_types158.default.string]),
  disabled: import_prop_types158.default.bool,
  id: import_prop_types158.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types158.default.oneOfType([import_prop_types158.default.element, import_prop_types158.default.func]),
  role: import_prop_types158.default.string,
  size: import_prop_types158.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types158.default.node,
  style: import_prop_types158.default.object,
  tabIndex: import_prop_types158.default.number,
  title: import_prop_types158.default.string,
  touchRippleRef: import_prop_types158.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanel.js
var React337 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelHeader.js
var React329 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/sidebar/Sidebar.js
var React326 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/resizablePanel/ResizablePanel.js
var React323 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/resizablePanel/ResizablePanelContext.js
var React322 = __toESM(require_react(), 1);
var ResizablePanelContext = React322.createContext(void 0);
if (true) ResizablePanelContext.displayName = "ResizablePanelContext";
function useResizablePanelContext() {
  const context = React322.useContext(ResizablePanelContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context. ResizablePanel subcomponents must be placed within a <ResizablePanel /> component.");
  }
  return context;
}

// node_modules/@mui/x-data-grid-premium/esm/components/resizablePanel/ResizablePanel.js
var import_jsx_runtime231 = __toESM(require_jsx_runtime(), 1);
var _excluded89 = ["className", "children", "direction"];
var useUtilityClasses111 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["resizablePanel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var ResizablePanelRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ResizablePanel"
})({
  position: "relative"
});
function ResizablePanel(props) {
  const {
    className,
    children: children2,
    direction = "horizontal"
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded89);
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses111(rootProps);
  const ref = React323.useRef(null);
  const contextValue = React323.useMemo(() => ({
    rootRef: ref,
    direction
  }), [direction]);
  return (0, import_jsx_runtime231.jsx)(ResizablePanelContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime231.jsx)(ResizablePanelRoot, _extends({
      className: clsx_default(classes2.root, className),
      ownerState: rootProps
    }, other, {
      ref,
      children: children2
    }))
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/resizablePanel/ResizablePanelHandle.js
var React325 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useResize.js
var React324 = __toESM(require_react(), 1);
var useResize = (options3) => {
  const resizeHandleRef = React324.useRef(null);
  const optionsRef = React324.useRef(options3);
  React324.useEffect(() => {
    optionsRef.current = options3;
  }, [options3]);
  React324.useEffect(() => {
    const handle = resizeHandleRef.current;
    if (!handle) {
      return void 0;
    }
    const {
      onSizeChange,
      getInitialSize,
      direction = "horizontal"
    } = optionsRef.current;
    let startPosition = null;
    let startSize = null;
    const handlePointerMove = (event) => {
      event.preventDefault();
      if (startPosition === null || startSize === null) {
        return;
      }
      const delta = direction === "horizontal" ? startPosition - event.clientX : startPosition - event.clientY;
      const newSize = startSize + delta;
      onSizeChange(newSize, handle);
    };
    const handlePointerUp = (event) => {
      startPosition = null;
      startSize = null;
      handle.removeEventListener("pointermove", handlePointerMove);
      handle.releasePointerCapture(event.pointerId);
    };
    const handlePointerDown = (event) => {
      startPosition = direction === "horizontal" ? event.clientX : event.clientY;
      startSize = getInitialSize(handle);
      handle.addEventListener("pointermove", handlePointerMove);
      handle.setPointerCapture(event.pointerId);
    };
    handle.addEventListener("pointerdown", handlePointerDown);
    handle.addEventListener("pointerup", handlePointerUp);
    return () => {
      handle.removeEventListener("pointerdown", handlePointerDown);
      handle.removeEventListener("pointerup", handlePointerUp);
      handle.removeEventListener("pointermove", handlePointerMove);
    };
  }, []);
  return {
    ref: resizeHandleRef
  };
};

// node_modules/@mui/x-data-grid-premium/esm/components/resizablePanel/ResizablePanelHandle.js
var import_jsx_runtime232 = __toESM(require_jsx_runtime(), 1);
var _excluded90 = ["className", "children"];
var useUtilityClasses112 = (ownerState) => {
  const {
    classes: classes2,
    direction
  } = ownerState;
  const slots = {
    root: ["resizablePanelHandle", `resizablePanelHandle--${direction}`]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var ResizablePanelHandleRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ResizablePanelHandle",
  overridesResolver: (props, styles5) => [{
    [`&.${gridClasses["resizablePanelHandle--horizontal"]}`]: styles5["resizablePanelHandle--horizontal"]
  }, {
    [`&.${gridClasses["resizablePanelHandle--vertical"]}`]: styles5["resizablePanelHandle--vertical"]
  }, styles5.resizablePanelHandle]
})({
  position: "absolute",
  zIndex: 3,
  top: 0,
  left: 0,
  userSelect: "none",
  transition: vars.transition(["border-color"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  }),
  "&:hover": {
    borderWidth: 2,
    borderColor: vars.colors.interactive.selected
  },
  variants: [{
    props: {
      direction: "horizontal"
    },
    style: {
      height: "100%",
      width: 8,
      cursor: "ew-resize",
      borderLeft: `1px solid ${vars.colors.border.base}`,
      touchAction: "pan-x"
    }
  }, {
    props: {
      direction: "vertical"
    },
    style: {
      width: "100%",
      height: 8,
      cursor: "ns-resize",
      borderTop: `1px solid ${vars.colors.border.base}`,
      touchAction: "pan-y"
    }
  }]
});
function ResizablePanelHandle(props) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded90);
  const {
    rootRef,
    direction
  } = useResizablePanelContext();
  const rootProps = useGridRootProps3();
  const ownerState = {
    classes: rootProps.classes,
    direction
  };
  const classes2 = useUtilityClasses112(ownerState);
  const {
    ref
  } = useResize({
    direction,
    getInitialSize: () => {
      return direction === "horizontal" ? rootRef.current.offsetWidth : rootRef.current.offsetHeight;
    },
    onSizeChange: (newSize) => {
      if (direction === "horizontal") {
        rootRef.current.style.width = `${newSize}px`;
      } else {
        rootRef.current.style.height = `${newSize}px`;
      }
    }
  });
  return (0, import_jsx_runtime232.jsx)(ResizablePanelHandleRoot, _extends({
    className: clsx_default(classes2.root, className),
    ownerState,
    direction
  }, other, {
    ref
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/sidebar/Sidebar.js
var import_jsx_runtime233 = __toESM(require_jsx_runtime(), 1);
var _excluded91 = ["className", "children"];
var useUtilityClasses113 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["sidebar"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var SidebarRoot = styled_default2(ResizablePanel, {
  name: "MuiDataGrid",
  slot: "Sidebar"
})({
  display: "flex",
  flexDirection: "column",
  width: 300,
  minWidth: 260,
  maxWidth: 400,
  overflow: "hidden"
});
function Sidebar(props) {
  const {
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded91);
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses113(rootProps);
  const {
    value,
    sidebarId,
    labelId
  } = useGridSelector(apiRef, gridSidebarContentSelector);
  if (!value) {
    return null;
  }
  const sidebarContent = apiRef.current.unstable_applyPipeProcessors("sidebar", null, value);
  if (!sidebarContent) {
    return null;
  }
  return (0, import_jsx_runtime233.jsxs)(SidebarRoot, _extends({
    id: sidebarId,
    className: clsx_default(className, classes2.root),
    ownerState: rootProps,
    "aria-labelledby": labelId
  }, other, {
    children: [(0, import_jsx_runtime233.jsx)(ResizablePanelHandle, {}), sidebarContent]
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/sidebar/SidebarHeader.js
var React327 = __toESM(require_react(), 1);
var import_jsx_runtime234 = __toESM(require_jsx_runtime(), 1);
var _excluded93 = ["className", "children"];
var useUtilityClasses114 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["sidebarHeader"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var SidebarHeaderRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "SidebarHeader"
})({
  position: "sticky",
  top: 0,
  borderBottom: `1px solid ${vars.colors.border.base}`
});
function SidebarHeader(props) {
  const {
    className,
    children: children2
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded93);
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses114(rootProps);
  return (0, import_jsx_runtime234.jsx)(SidebarHeaderRoot, _extends({
    className: clsx_default(className, classes2.root),
    ownerState: rootProps
  }, other, {
    children: children2
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelSearch.js
var React328 = __toESM(require_react(), 1);
var import_jsx_runtime235 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses115 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    container: ["pivotPanelSearchContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPivotPanelSearchContainer = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelSearchContainer"
})({
  padding: vars.spacing(0, 1, 1)
});
function GridPivotPanelSearch(props) {
  var _a;
  const {
    onClear,
    value,
    onChange
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const classes2 = useUtilityClasses115(rootProps);
  const handleKeyDown = (event) => {
    if (event.key === "Escape") {
      onClear();
    }
  };
  return (0, import_jsx_runtime235.jsx)(GridPivotPanelSearchContainer, {
    ownerState: rootProps,
    className: classes2.container,
    children: (0, import_jsx_runtime235.jsx)(rootProps.slots.baseTextField, _extends({
      size: "small",
      "aria-label": apiRef.current.getLocaleText("pivotSearchControlLabel"),
      placeholder: apiRef.current.getLocaleText("pivotSearchControlPlaceholder"),
      onKeyDown: handleKeyDown,
      fullWidth: true,
      slotProps: {
        input: {
          startAdornment: (0, import_jsx_runtime235.jsx)(rootProps.slots.pivotSearchIcon, {
            fontSize: "small"
          }),
          endAdornment: value ? (0, import_jsx_runtime235.jsx)(rootProps.slots.baseIconButton, {
            edge: "end",
            size: "small",
            onClick: onClear,
            "aria-label": apiRef.current.getLocaleText("pivotSearchControlClear"),
            children: (0, import_jsx_runtime235.jsx)(rootProps.slots.pivotSearchClearIcon, {
              fontSize: "small"
            })
          }) : null
        },
        htmlInput: {
          role: "searchbox"
        }
      }
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, {
      value,
      onChange
    }))
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelHeader.js
var import_jsx_runtime236 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses116 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["pivotPanelHeader"],
    switch: ["pivotPanelSwitch"],
    label: ["pivotPanelSwitchLabel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPivotPanelHeaderRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelHeader"
})({
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(1),
  padding: vars.spacing(0, 0.75, 0, 1),
  boxSizing: "border-box",
  height: 52
});
var GridPivotPanelSwitch = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "PivotPanelSwitch"
})({
  marginRight: "auto"
});
var GridPivotPanelSwitchLabel = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "PivotPanelSwitchLabel"
})({
  font: vars.typography.font.large,
  fontWeight: vars.typography.fontWeight.medium
});
function GridPivotPanelHeader(props) {
  var _a, _b;
  const {
    searchValue,
    onSearchValueChange
  } = props;
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const classes2 = useUtilityClasses116(rootProps);
  const rows = useGridSelector(apiRef, gridRowCountSelector);
  const isEmptyPivot = pivotActive && rows === 0;
  return (0, import_jsx_runtime236.jsxs)(SidebarHeader, {
    children: [(0, import_jsx_runtime236.jsxs)(GridPivotPanelHeaderRoot, {
      ownerState: rootProps,
      className: classes2.root,
      children: [(0, import_jsx_runtime236.jsx)(GridPivotPanelSwitch, _extends({
        as: rootProps.slots.baseSwitch,
        ownerState: rootProps,
        className: classes2.switch,
        checked: pivotActive,
        onChange: (event) => apiRef.current.setPivotActive(event.target.checked),
        size: "small",
        label: (0, import_jsx_runtime236.jsx)(GridPivotPanelSwitchLabel, {
          ownerState: rootProps,
          className: classes2.label,
          children: apiRef.current.getLocaleText("pivotToggleLabel")
        })
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseSwitch)), (0, import_jsx_runtime236.jsx)(rootProps.slots.baseIconButton, _extends({
        onClick: () => {
          apiRef.current.hideSidebar();
          if (isEmptyPivot) {
            apiRef.current.setPivotActive(false);
          }
        },
        "aria-label": apiRef.current.getLocaleText("pivotCloseButton")
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseIconButton, {
        children: (0, import_jsx_runtime236.jsx)(rootProps.slots.sidebarCloseIcon, {
          fontSize: "small"
        })
      }))]
    }), (0, import_jsx_runtime236.jsx)(GridPivotPanelSearch, {
      value: searchValue,
      onClear: () => onSearchValueChange(""),
      onChange: (event) => onSearchValueChange(event.target.value)
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelBody.js
var React336 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelField.js
var React331 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelFieldMenu.js
var React330 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridPrivateApiContext.js
var useGridPrivateApiContext3 = useGridPrivateApiContext;

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelFieldMenu.js
var import_jsx_runtime237 = __toESM(require_jsx_runtime(), 1);
function GridPivotPanelFieldMenu(props) {
  var _a, _b;
  const {
    field,
    modelKey
  } = props;
  const rootProps = useGridRootProps3();
  const [open, setOpen] = React330.useState(false);
  const apiRef = useGridPrivateApiContext3();
  const isAvailableField = modelKey === null;
  const pivotModel = useGridSelector(apiRef, gridPivotModelSelector);
  const fieldIndexInModel = !isAvailableField ? pivotModel[modelKey].findIndex((item) => item.field === field) : -1;
  const modelLength = !isAvailableField ? pivotModel[modelKey].length : 0;
  const canMoveUp = fieldIndexInModel > 0;
  const canMoveDown = !isAvailableField && fieldIndexInModel < modelLength - 1;
  const menuId = useId();
  const triggerId = useId();
  const triggerRef = React330.useRef(null);
  const getMenuItems = React330.useCallback(() => {
    if (isAvailableField) {
      return [{
        key: "rows",
        label: apiRef.current.getLocaleText("pivotMenuAddToRows")
      }, {
        key: "columns",
        label: apiRef.current.getLocaleText("pivotMenuAddToColumns")
      }, {
        key: "values",
        label: apiRef.current.getLocaleText("pivotMenuAddToValues")
      }];
    }
    return [{
      key: "up",
      label: apiRef.current.getLocaleText("pivotMenuMoveUp"),
      icon: (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuMoveUpIcon, {}),
      disabled: !canMoveUp
    }, {
      key: "down",
      label: apiRef.current.getLocaleText("pivotMenuMoveDown"),
      icon: (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuMoveDownIcon, {}),
      disabled: !canMoveDown
    }, {
      divider: true
    }, {
      key: "top",
      label: apiRef.current.getLocaleText("pivotMenuMoveToTop"),
      icon: (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuMoveToTopIcon, {}),
      disabled: !canMoveUp
    }, {
      key: "bottom",
      label: apiRef.current.getLocaleText("pivotMenuMoveToBottom"),
      icon: (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuMoveToBottomIcon, {}),
      disabled: !canMoveDown
    }, {
      divider: true
    }, {
      key: "rows",
      label: apiRef.current.getLocaleText("pivotMenuRows"),
      icon: modelKey === "rows" ? (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuCheckIcon, {}) : (0, import_jsx_runtime237.jsx)("span", {})
    }, {
      key: "columns",
      label: apiRef.current.getLocaleText("pivotMenuColumns"),
      icon: modelKey === "columns" ? (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuCheckIcon, {}) : (0, import_jsx_runtime237.jsx)("span", {})
    }, {
      key: "values",
      label: apiRef.current.getLocaleText("pivotMenuValues"),
      icon: modelKey === "values" ? (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuCheckIcon, {}) : (0, import_jsx_runtime237.jsx)("span", {})
    }, {
      divider: true
    }, {
      key: null,
      label: apiRef.current.getLocaleText("pivotMenuRemove"),
      icon: (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuRemoveIcon, {})
    }];
  }, [isAvailableField, apiRef, rootProps, canMoveUp, canMoveDown, modelKey]);
  const handleClick = () => {
    setOpen(!open);
  };
  const handleClose = () => {
    setOpen(false);
  };
  const handleMove = (to) => {
    handleClose();
    if (to === modelKey) {
      return;
    }
    let targetField;
    let targetFieldPosition = null;
    let targetSection = modelKey;
    switch (to) {
      case "up":
        targetField = pivotModel[modelKey][fieldIndexInModel - 1].field;
        targetFieldPosition = "top";
        break;
      case "down":
        targetField = pivotModel[modelKey][fieldIndexInModel + 1].field;
        targetFieldPosition = "bottom";
        break;
      case "top":
        targetField = pivotModel[modelKey][0].field;
        targetFieldPosition = "top";
        break;
      case "bottom":
        targetField = pivotModel[modelKey][modelLength - 1].field;
        targetFieldPosition = "bottom";
        break;
      case "rows":
      case "columns":
      case "values":
      case null:
        targetSection = to;
        break;
      default:
        break;
    }
    apiRef.current.updatePivotModel({
      field,
      targetField,
      targetFieldPosition,
      targetSection,
      originSection: modelKey
    });
  };
  return (0, import_jsx_runtime237.jsxs)(React330.Fragment, {
    children: [(0, import_jsx_runtime237.jsx)(rootProps.slots.baseIconButton, _extends({
      size: "small"
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
      id: triggerId,
      "aria-haspopup": "true",
      "aria-controls": open ? menuId : void 0,
      "aria-expanded": open ? "true" : void 0,
      "aria-label": apiRef.current.getLocaleText("pivotMenuOptions"),
      onClick: handleClick,
      ref: triggerRef,
      children: isAvailableField ? (0, import_jsx_runtime237.jsx)(rootProps.slots.pivotMenuAddIcon, {
        fontSize: "small"
      }) : (0, import_jsx_runtime237.jsx)(rootProps.slots.columnMenuIcon, {
        fontSize: "small"
      })
    })), (0, import_jsx_runtime237.jsx)(GridMenu, {
      target: triggerRef.current,
      open,
      onClose: handleClose,
      position: "bottom-start",
      children: (0, import_jsx_runtime237.jsx)(rootProps.slots.baseMenuList, _extends({
        id: menuId,
        "aria-labelledby": triggerId,
        autoFocusItem: true
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseMenuList, {
        children: getMenuItems().map((item, index) => {
          var _a2;
          return "divider" in item ? (0, import_jsx_runtime237.jsx)(rootProps.slots.baseDivider, {}, `divider-${index}`) : (0, import_jsx_runtime237.jsx)(rootProps.slots.baseMenuItem, _extends({
            disabled: item.disabled,
            onClick: () => handleMove(item.key),
            iconStart: item.icon
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem, {
            children: item.label
          }), item.key);
        })
      }))
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelField.js
var import_jsx_runtime238 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses117 = (ownerState) => {
  const {
    classes: classes2,
    modelKey
  } = ownerState;
  const sorted = modelKey === "columns" && ownerState.modelValue.sort;
  const slots = {
    root: ["pivotPanelField", sorted && "pivotPanelField--sorted"],
    name: ["pivotPanelFieldName"],
    actionContainer: ["pivotPanelFieldActionContainer"],
    dragIcon: ["pivotPanelFieldDragIcon"],
    checkbox: ["pivotPanelFieldCheckbox"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPivotPanelFieldRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelField",
  overridesResolver: (props, styles5) => [{
    [`&.${gridClasses["pivotPanelField--sorted"]}`]: styles5["pivotPanelField--sorted"]
  }, styles5.pivotPanelField]
})({
  flexShrink: 0,
  position: "relative",
  padding: vars.spacing(0, 1, 0, 2),
  height: 32,
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(0.5),
  borderWidth: 0,
  borderTopWidth: 2,
  borderBottomWidth: 2,
  borderStyle: "solid",
  borderColor: "transparent",
  margin: "-1px 0",
  // collapse vertical borders
  cursor: "grab",
  variants: [{
    props: {
      dropPosition: "top"
    },
    style: {
      borderTopColor: vars.colors.interactive.selected
    }
  }, {
    props: {
      dropPosition: "bottom"
    },
    style: {
      borderBottomColor: vars.colors.interactive.selected
    }
  }, {
    props: {
      section: null
    },
    style: {
      borderTopColor: "transparent",
      borderBottomColor: "transparent"
    }
  }],
  "&:hover": {
    backgroundColor: vars.colors.interactive.hover
  }
});
var GridPivotPanelFieldName = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "PivotPanelFieldName"
})({
  flex: 1,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
});
var GridPivotPanelFieldActionContainer = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelFieldActionContainer"
})({
  display: "flex",
  alignItems: "center"
});
var GridPivotPanelFieldDragIcon = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelFieldDragIcon"
})({
  position: "absolute",
  left: -1,
  width: 16,
  display: "flex",
  justifyContent: "center",
  color: vars.colors.foreground.base,
  opacity: 0,
  '[draggable="true"]:hover > &': {
    opacity: 0.3
  }
});
var GridPivotPanelFieldCheckbox = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "PivotPanelFieldCheckbox"
})({
  flex: 1,
  position: "relative",
  margin: vars.spacing(0, 0, 0, -1),
  cursor: "grab"
});
function AggregationSelect({
  aggFunc,
  field
}) {
  var _a;
  const rootProps = useGridRootProps3();
  const [aggregationMenuOpen, setAggregationMenuOpen] = React331.useState(false);
  const aggregationMenuTriggerRef = React331.useRef(null);
  const aggregationMenuTriggerId = useId();
  const aggregationMenuId = useId();
  const apiRef = useGridApiContext3();
  const initialColumns = useGridSelector(apiRef, gridPivotInitialColumnsSelector);
  const colDef = initialColumns.get(field);
  const availableAggregationFunctions = React331.useMemo(() => getAvailableAggregationFunctions({
    aggregationFunctions: rootProps.aggregationFunctions,
    colDef,
    isDataSource: !!rootProps.dataSource
  }), [colDef, rootProps.aggregationFunctions, rootProps.dataSource]);
  const handleClick = (func) => {
    apiRef.current.setPivotModel((prev) => {
      return _extends({}, prev, {
        values: prev.values.map((col) => {
          if (col.field === field) {
            return _extends({}, col, {
              aggFunc: func
            });
          }
          return col;
        })
      });
    });
    setAggregationMenuOpen(false);
  };
  return availableAggregationFunctions.length > 0 ? (0, import_jsx_runtime238.jsxs)(React331.Fragment, {
    children: [(0, import_jsx_runtime238.jsx)(rootProps.slots.baseChip, {
      label: getAggregationFunctionLabel({
        apiRef,
        aggregationRule: {
          aggregationFunctionName: aggFunc,
          aggregationFunction: rootProps.aggregationFunctions[aggFunc]
        }
      }),
      size: "small",
      variant: "outlined",
      ref: aggregationMenuTriggerRef,
      id: aggregationMenuTriggerId,
      "aria-haspopup": "true",
      "aria-controls": aggregationMenuOpen ? aggregationMenuId : void 0,
      "aria-expanded": aggregationMenuOpen ? "true" : void 0,
      onClick: () => setAggregationMenuOpen(!aggregationMenuOpen)
    }), (0, import_jsx_runtime238.jsx)(GridMenu, {
      open: aggregationMenuOpen,
      onClose: () => setAggregationMenuOpen(false),
      target: aggregationMenuTriggerRef.current,
      position: "bottom-start",
      children: (0, import_jsx_runtime238.jsx)(rootProps.slots.baseMenuList, _extends({
        id: aggregationMenuId,
        "aria-labelledby": aggregationMenuTriggerId,
        autoFocusItem: true
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseMenuList, {
        children: availableAggregationFunctions.map((func) => {
          var _a2;
          return (0, import_jsx_runtime238.jsx)(rootProps.slots.baseMenuItem, _extends({
            selected: aggFunc === func,
            onClick: () => handleClick(func)
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem, {
            children: getAggregationFunctionLabel({
              apiRef,
              aggregationRule: {
                aggregationFunctionName: func,
                aggregationFunction: rootProps.aggregationFunctions[func]
              }
            })
          }), func);
        })
      }))
    })]
  }) : null;
}
function GridPivotPanelField(props) {
  var _a;
  const {
    children: children2,
    field,
    onDragStart,
    onDragEnd
  } = props;
  const rootProps = useGridRootProps3();
  const [dropPosition, setDropPosition] = React331.useState(null);
  const section = props.modelKey;
  const ownerState = _extends({}, props, {
    classes: rootProps.classes,
    dropPosition,
    section
  });
  const classes2 = useUtilityClasses117(ownerState);
  const apiRef = useGridPrivateApiContext3();
  const handleDragStart = React331.useCallback((event) => {
    const data = {
      field,
      modelKey: section
    };
    event.dataTransfer.setData("text/plain", JSON.stringify(data));
    event.dataTransfer.dropEffect = "move";
    onDragStart(section);
  }, [field, onDragStart, section]);
  const getDropPosition = React331.useCallback((event) => {
    const rect = event.target.getBoundingClientRect();
    const y = event.clientY - rect.top;
    if (y < rect.height / 2) {
      return "top";
    }
    return "bottom";
  }, []);
  const handleDragOver = React331.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDropPosition(getDropPosition(event));
    }
  }, [getDropPosition]);
  const handleDragLeave = React331.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDropPosition(null);
    }
  }, []);
  const handleDrop = React331.useCallback((event) => {
    setDropPosition(null);
    if (!event.currentTarget.contains(event.relatedTarget)) {
      event.preventDefault();
      const position = getDropPosition(event);
      const {
        field: droppedField,
        modelKey: originSection
      } = JSON.parse(event.dataTransfer.getData("text/plain"));
      apiRef.current.updatePivotModel({
        field: droppedField,
        targetField: field,
        targetFieldPosition: position,
        originSection,
        targetSection: section
      });
    }
  }, [getDropPosition, apiRef, field, section]);
  const handleSort = () => {
    const currentSort = section === "columns" ? props.modelValue.sort : null;
    let newValue;
    if (currentSort === "asc") {
      newValue = "desc";
    } else if (currentSort === "desc") {
      newValue = void 0;
    } else {
      newValue = "asc";
    }
    apiRef.current.setPivotModel((prev) => {
      return _extends({}, prev, {
        columns: prev.columns.map((col) => {
          if (col.field === field) {
            return _extends({}, col, {
              sort: newValue
            });
          }
          return col;
        })
      });
    });
  };
  const handleVisibilityChange = (event) => {
    if (section) {
      apiRef.current.setPivotModel((prev) => {
        return _extends({}, prev, {
          [section]: prev[section].map((col) => {
            if (col.field === field) {
              return _extends({}, col, {
                hidden: !event.target.checked
              });
            }
            return col;
          })
        });
      });
    }
  };
  const hideable = section !== null;
  return (0, import_jsx_runtime238.jsxs)(GridPivotPanelFieldRoot, {
    ownerState,
    className: classes2.root,
    onDragOver: handleDragOver,
    onDragLeave: handleDragLeave,
    onDrop: handleDrop,
    onDragStart: handleDragStart,
    onDragEnd,
    draggable: "true",
    children: [(0, import_jsx_runtime238.jsx)(GridPivotPanelFieldDragIcon, {
      ownerState,
      className: classes2.dragIcon,
      children: (0, import_jsx_runtime238.jsx)(rootProps.slots.columnReorderIcon, {
        fontSize: "small"
      })
    }), hideable ? (0, import_jsx_runtime238.jsx)(GridPivotPanelFieldCheckbox, _extends({
      ownerState,
      className: classes2.checkbox,
      as: rootProps.slots.baseCheckbox,
      size: "small",
      density: "compact"
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseCheckbox, {
      checked: !props.modelValue.hidden || false,
      onChange: handleVisibilityChange,
      label: children2
    })) : (0, import_jsx_runtime238.jsx)(GridPivotPanelFieldName, {
      ownerState,
      className: classes2.name,
      children: children2
    }), (0, import_jsx_runtime238.jsxs)(GridPivotPanelFieldActionContainer, {
      ownerState,
      className: classes2.actionContainer,
      children: [section === "columns" && (0, import_jsx_runtime238.jsx)(GridColumnSortButton, {
        field,
        direction: props.modelValue.sort,
        sortingOrder: rootProps.sortingOrder,
        onClick: handleSort
      }), section === "values" && (0, import_jsx_runtime238.jsx)(AggregationSelect, {
        aggFunc: props.modelValue.aggFunc,
        field
      }), (0, import_jsx_runtime238.jsx)(GridPivotPanelFieldMenu, {
        field,
        modelKey: section
      })]
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/collapsible/Collapsible.js
var React333 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/collapsible/CollapsibleContext.js
var React332 = __toESM(require_react(), 1);
var CollapsibleContext = React332.createContext(void 0);
if (true) CollapsibleContext.displayName = "CollapsibleContext";
function useCollapsibleContext() {
  const context = React332.useContext(CollapsibleContext);
  if (context === void 0) {
    throw new Error("MUI X: Missing context. Collapsible subcomponents must be placed within a <Collapsible /> component.");
  }
  return context;
}

// node_modules/@mui/x-data-grid-premium/esm/components/collapsible/Collapsible.js
var import_jsx_runtime239 = __toESM(require_jsx_runtime(), 1);
var _excluded94 = ["className", "children", "initiallyOpen"];
var useUtilityClasses118 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["collapsible"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var CollapsibleRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "Collapsible"
})(({
  ownerState
}) => ({
  display: "flex",
  flexDirection: "column",
  flex: ownerState.open ? "1 0 auto" : "0 0 auto",
  borderRadius: vars.radius.base
}));
function Collapsible(props) {
  const {
    className,
    children: children2,
    initiallyOpen = true
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded94);
  const [open, setOpen] = React333.useState(initiallyOpen);
  const panelId = useId();
  const rootProps = useGridRootProps3();
  const ownerState = {
    classes: rootProps.classes,
    open
  };
  const classes2 = useUtilityClasses118(ownerState);
  const contextValue = React333.useMemo(() => ({
    open,
    onOpenChange: setOpen,
    panelId
  }), [open, setOpen, panelId]);
  return (0, import_jsx_runtime239.jsx)(CollapsibleContext.Provider, {
    value: contextValue,
    children: (0, import_jsx_runtime239.jsx)(CollapsibleRoot, _extends({
      ownerState,
      className: clsx_default(classes2.root, className)
    }, other, {
      children: children2
    }))
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/collapsible/CollapsibleTrigger.js
var React334 = __toESM(require_react(), 1);
var import_jsx_runtime240 = __toESM(require_jsx_runtime(), 1);
var _excluded95 = ["children", "className"];
var useUtilityClasses119 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["collapsibleTrigger"],
    icon: ["collapsibleIcon"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var CollapsibleTriggerRoot = styled_default2("button", {
  name: "MuiDataGrid",
  slot: "CollapsibleTrigger"
})(({
  ownerState
}) => ({
  position: "relative",
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
  height: 40,
  padding: vars.spacing(0, 1.5),
  border: `1px solid ${vars.colors.border.base}`,
  background: "none",
  outline: "none",
  borderTopLeftRadius: vars.radius.base,
  borderTopRightRadius: vars.radius.base,
  borderBottomLeftRadius: ownerState.open ? 0 : vars.radius.base,
  borderBottomRightRadius: ownerState.open ? 0 : vars.radius.base,
  "&:hover": {
    backgroundColor: vars.colors.interactive.hover,
    cursor: "pointer"
  },
  "&:focus-visible": {
    outline: `2px solid ${vars.colors.interactive.selected}`,
    outlineOffset: -2
  }
}));
var CollapsibleIcon = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "CollapsibleIcon"
})(({
  ownerState
}) => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  color: vars.colors.foreground.muted,
  transform: ownerState.open ? "none" : "rotate(180deg)",
  transition: vars.transition(["transform"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  })
}));
function CollapsibleTrigger(props) {
  const {
    children: children2,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded95);
  const rootProps = useGridRootProps3();
  const {
    open,
    onOpenChange,
    panelId
  } = useCollapsibleContext();
  const ownerState = {
    classes: rootProps.classes,
    open
  };
  const classes2 = useUtilityClasses119(ownerState);
  return (0, import_jsx_runtime240.jsxs)(CollapsibleTriggerRoot, _extends({
    ownerState,
    className: clsx_default(classes2.root, className),
    tabIndex: 0,
    "aria-controls": open ? panelId : void 0,
    "aria-expanded": !open,
    onClick: () => onOpenChange(!open)
  }, other, {
    children: [children2, (0, import_jsx_runtime240.jsx)(CollapsibleIcon, {
      ownerState,
      className: classes2.icon,
      children: (0, import_jsx_runtime240.jsx)(rootProps.slots.collapsibleIcon, {})
    })]
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/collapsible/CollapsiblePanel.js
var React335 = __toESM(require_react(), 1);
var import_jsx_runtime241 = __toESM(require_jsx_runtime(), 1);
var _excluded96 = ["aria-label", "children", "className"];
var useUtilityClasses120 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["collapsiblePanel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var CollapsiblePanelRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "CollapsiblePanel"
})({
  border: `1px solid ${vars.colors.border.base}`,
  borderTop: "none",
  borderBottomLeftRadius: vars.radius.base,
  borderBottomRightRadius: vars.radius.base,
  flex: 1,
  overflow: "hidden"
});
function CollapsiblePanel(props) {
  const {
    children: children2,
    className
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded96);
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses120(rootProps);
  const id = useId();
  const {
    open
  } = useCollapsibleContext();
  if (!open) {
    return null;
  }
  return (0, import_jsx_runtime241.jsx)(CollapsiblePanelRoot, _extends({
    ownerState: rootProps,
    className: clsx_default(classes2.root, className),
    id
  }, other, {
    children: children2
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanelBody.js
var import_jsx_runtime242 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses121 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["pivotPanelBody"],
    availableFields: ["pivotPanelAvailableFields"],
    sections: ["pivotPanelSections"],
    scrollArea: ["pivotPanelScrollArea"],
    section: ["pivotPanelSection"],
    sectionTitle: ["pivotPanelSectionTitle"],
    fieldList: ["pivotPanelFieldList"],
    placeholder: ["pivotPanelPlaceholder"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridPivotPanelBodyRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelBody"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  overflow: "hidden"
});
var GridPivotPanelAvailableFields = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "PivotPanelAvailableFields"
})({
  flex: 1,
  minHeight: 84,
  transition: vars.transition(["background-color"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  }),
  '&[data-drag-over="true"]': {
    backgroundColor: vars.colors.interactive.hover
  }
});
var GridPivotPanelSections = styled_default2(ResizablePanel, {
  name: "MuiDataGrid",
  slot: "PivotPanelSections"
})({
  position: "relative",
  minHeight: 158,
  overflow: "hidden",
  display: "flex",
  flexDirection: "column"
});
var GridPivotPanelScrollArea = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "PivotPanelScrollArea"
})({
  height: "100%"
});
var GridPivotPanelSection = styled_default2(Collapsible, {
  name: "MuiDataGrid",
  slot: "PivotPanelSection"
})({
  margin: vars.spacing(0.5, 1),
  transition: vars.transition(["border-color", "background-color"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  }),
  '&[data-drag-over="true"]': {
    backgroundColor: vars.colors.interactive.hover,
    outline: `2px solid ${vars.colors.interactive.selected}`
  }
});
var GridPivotPanelSectionTitle = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelSectionTitle"
})({
  flex: 1,
  marginRight: vars.spacing(1.75),
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: vars.spacing(1),
  font: vars.typography.font.body,
  fontWeight: vars.typography.fontWeight.medium
});
var GridPivotPanelFieldList = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelFieldList"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  padding: vars.spacing(0.5, 0)
});
var GridPivotPanelPlaceholder = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "PivotPanelPlaceholder"
})({
  flex: 1,
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  textWrap: "balance",
  textAlign: "center",
  minHeight: 38,
  height: "100%",
  padding: vars.spacing(0, 1),
  color: vars.colors.foreground.muted,
  font: vars.typography.font.body
});
var INITIAL_DRAG_STATE = {
  active: false,
  dropZone: null,
  initialModelKey: null
};
function GridPivotPanelBody({
  searchValue
}) {
  const apiRef = useGridPrivateApiContext3();
  const initialColumns = useGridSelector(apiRef, gridPivotInitialColumnsSelector);
  const fields = React336.useMemo(() => Array.from(initialColumns.keys()), [initialColumns]);
  const rootProps = useGridRootProps3();
  const [drag, setDrag] = React336.useState(INITIAL_DRAG_STATE);
  const pivotModel = useGridSelector(apiRef, gridPivotModelSelector);
  const classes2 = useUtilityClasses121(rootProps);
  const getColumnName = React336.useCallback((field) => {
    const column = initialColumns.get(field);
    return (column == null ? void 0 : column.headerName) || field;
  }, [initialColumns]);
  const pivotModelFields = React336.useMemo(() => {
    const pivotModelArray = pivotModel.rows.concat(pivotModel.columns, pivotModel.values);
    return new Set(pivotModelArray.map((item) => item.field));
  }, [pivotModel]);
  const availableFields = React336.useMemo(() => {
    return fields.filter((field) => {
      var _a;
      if (pivotModelFields.has(field)) {
        return false;
      }
      if (((_a = initialColumns.get(field)) == null ? void 0 : _a.pivotable) === false) {
        return false;
      }
      if (searchValue) {
        const fieldName = getColumnName(field);
        return fieldName.toLowerCase().includes(searchValue.toLowerCase());
      }
      return true;
    });
  }, [searchValue, fields, getColumnName, pivotModelFields, initialColumns]);
  const handleDragStart = (modelKey) => {
    setDrag({
      active: true,
      initialModelKey: modelKey,
      dropZone: null
    });
  };
  const handleDragEnd = () => {
    setDrag(INITIAL_DRAG_STATE);
  };
  const handleDrop = (event) => {
    setDrag(INITIAL_DRAG_STATE);
    if (event.defaultPrevented) {
      return;
    }
    event.preventDefault();
    const {
      field,
      modelKey: originSection
    } = JSON.parse(event.dataTransfer.getData("text/plain"));
    const targetSection = event.currentTarget.getAttribute("data-section");
    if (originSection === targetSection) {
      return;
    }
    apiRef.current.updatePivotModel({
      field,
      targetSection,
      originSection
    });
  };
  const handleDragOver = React336.useCallback((event) => {
    event.preventDefault();
    event.dataTransfer.dropEffect = "move";
  }, []);
  const handleDragEnter = React336.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      const dropZone = event.currentTarget.getAttribute("data-section");
      setDrag((v) => _extends({}, v, {
        active: true,
        dropZone
      }));
    }
  }, []);
  const handleDragLeave = React336.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDrag((v) => _extends({}, v, {
        active: true,
        dropZone: v.initialModelKey
      }));
    }
  }, []);
  const rowsLabel = apiRef.current.getLocaleText("pivotRows");
  const columnsLabel = apiRef.current.getLocaleText("pivotColumns");
  const valuesLabel = apiRef.current.getLocaleText("pivotValues");
  return (0, import_jsx_runtime242.jsxs)(GridPivotPanelBodyRoot, {
    ownerState: rootProps,
    className: classes2.root,
    "data-dragging": drag.active,
    onDragLeave: handleDragLeave,
    children: [(0, import_jsx_runtime242.jsxs)(GridPivotPanelAvailableFields, {
      ownerState: rootProps,
      className: classes2.availableFields,
      onDrop: handleDrop,
      onDragEnter: handleDragEnter,
      onDragOver: handleDragOver,
      "data-section": null,
      "data-drag-over": drag.active && drag.dropZone === null,
      children: [availableFields.length === 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelPlaceholder, {
        ownerState: rootProps,
        className: classes2.placeholder,
        children: apiRef.current.getLocaleText("pivotNoFields")
      }), availableFields.length > 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelFieldList, {
        ownerState: rootProps,
        className: classes2.fieldList,
        children: availableFields.map((field) => (0, import_jsx_runtime242.jsx)(GridPivotPanelField, {
          field,
          modelKey: null,
          onDragStart: handleDragStart,
          onDragEnd: handleDragEnd,
          children: getColumnName(field)
        }, field))
      })]
    }), (0, import_jsx_runtime242.jsxs)(GridPivotPanelSections, {
      ownerState: rootProps,
      className: classes2.sections,
      direction: "vertical",
      children: [(0, import_jsx_runtime242.jsx)(ResizablePanelHandle, {}), (0, import_jsx_runtime242.jsxs)(GridPivotPanelScrollArea, {
        ownerState: rootProps,
        className: classes2.scrollArea,
        children: [(0, import_jsx_runtime242.jsxs)(GridPivotPanelSection, {
          ownerState: rootProps,
          className: classes2.section,
          onDrop: handleDrop,
          onDragEnter: handleDragEnter,
          onDragOver: handleDragOver,
          "data-section": "rows",
          "data-drag-over": drag.dropZone === "rows",
          children: [(0, import_jsx_runtime242.jsx)(CollapsibleTrigger, {
            "aria-label": apiRef.current.getLocaleText("pivotRows"),
            children: (0, import_jsx_runtime242.jsxs)(GridPivotPanelSectionTitle, {
              ownerState: rootProps,
              className: classes2.sectionTitle,
              children: [rowsLabel, pivotModel.rows.length > 0 && (0, import_jsx_runtime242.jsx)(rootProps.slots.baseBadge, {
                badgeContent: pivotModel.rows.length
              })]
            })
          }), (0, import_jsx_runtime242.jsxs)(CollapsiblePanel, {
            children: [pivotModel.rows.length === 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelPlaceholder, {
              ownerState: rootProps,
              className: classes2.placeholder,
              children: apiRef.current.getLocaleText("pivotDragToRows")
            }), pivotModel.rows.length > 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelFieldList, {
              ownerState: rootProps,
              className: classes2.fieldList,
              children: pivotModel.rows.map((modelValue) => (0, import_jsx_runtime242.jsx)(GridPivotPanelField, {
                field: modelValue.field,
                modelKey: "rows",
                modelValue,
                "data-field": modelValue.field,
                onDragStart: handleDragStart,
                onDragEnd: handleDragEnd,
                children: getColumnName(modelValue.field)
              }, modelValue.field))
            })]
          })]
        }), (0, import_jsx_runtime242.jsxs)(GridPivotPanelSection, {
          ownerState: rootProps,
          className: classes2.section,
          onDrop: handleDrop,
          onDragEnter: handleDragEnter,
          onDragOver: handleDragOver,
          "data-section": "columns",
          "data-drag-over": drag.dropZone === "columns",
          children: [(0, import_jsx_runtime242.jsx)(CollapsibleTrigger, {
            "aria-label": apiRef.current.getLocaleText("pivotColumns"),
            children: (0, import_jsx_runtime242.jsxs)(GridPivotPanelSectionTitle, {
              ownerState: rootProps,
              className: classes2.sectionTitle,
              children: [columnsLabel, pivotModel.columns.length > 0 && (0, import_jsx_runtime242.jsx)(rootProps.slots.baseBadge, {
                badgeContent: pivotModel.columns.length
              })]
            })
          }), (0, import_jsx_runtime242.jsxs)(CollapsiblePanel, {
            children: [pivotModel.columns.length === 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelPlaceholder, {
              ownerState: rootProps,
              className: classes2.placeholder,
              children: apiRef.current.getLocaleText("pivotDragToColumns")
            }), pivotModel.columns.length > 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelFieldList, {
              ownerState: rootProps,
              className: classes2.fieldList,
              children: pivotModel.columns.map((modelValue) => (0, import_jsx_runtime242.jsx)(GridPivotPanelField, {
                field: modelValue.field,
                modelKey: "columns",
                modelValue,
                onDragStart: handleDragStart,
                onDragEnd: handleDragEnd,
                children: getColumnName(modelValue.field)
              }, modelValue.field))
            })]
          })]
        }), (0, import_jsx_runtime242.jsxs)(GridPivotPanelSection, {
          ownerState: rootProps,
          className: classes2.section,
          onDrop: handleDrop,
          onDragEnter: handleDragEnter,
          onDragOver: handleDragOver,
          "data-section": "values",
          "data-drag-over": drag.dropZone === "values",
          children: [(0, import_jsx_runtime242.jsx)(CollapsibleTrigger, {
            "aria-label": apiRef.current.getLocaleText("pivotValues"),
            children: (0, import_jsx_runtime242.jsxs)(GridPivotPanelSectionTitle, {
              ownerState: rootProps,
              className: classes2.sectionTitle,
              children: [valuesLabel, pivotModel.values.length > 0 && (0, import_jsx_runtime242.jsx)(rootProps.slots.baseBadge, {
                badgeContent: pivotModel.values.length
              })]
            })
          }), (0, import_jsx_runtime242.jsxs)(CollapsiblePanel, {
            children: [pivotModel.values.length === 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelPlaceholder, {
              ownerState: rootProps,
              className: classes2.placeholder,
              children: apiRef.current.getLocaleText("pivotDragToValues")
            }), pivotModel.values.length > 0 && (0, import_jsx_runtime242.jsx)(GridPivotPanelFieldList, {
              ownerState: rootProps,
              className: classes2.fieldList,
              children: pivotModel.values.map((modelValue) => (0, import_jsx_runtime242.jsx)(GridPivotPanelField, {
                field: modelValue.field,
                modelKey: "values",
                modelValue,
                onDragStart: handleDragStart,
                onDragEnd: handleDragEnd,
                children: getColumnName(modelValue.field)
              }, modelValue.field))
            })]
          })]
        })]
      })]
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/pivotPanel/GridPivotPanel.js
var import_jsx_runtime243 = __toESM(require_jsx_runtime(), 1);
function GridPivotPanel() {
  const [searchValue, setSearchValue] = React337.useState("");
  return (0, import_jsx_runtime243.jsxs)(React337.Fragment, {
    children: [(0, import_jsx_runtime243.jsx)(GridPivotPanelHeader, {
      searchValue,
      onSearchValueChange: setSearchValue
    }), (0, import_jsx_runtime243.jsx)(GridPivotPanelBody, {
      searchValue
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/AiAssistantPanelTrigger.js
var React338 = __toESM(require_react(), 1);
var import_prop_types159 = __toESM(require_prop_types(), 1);
var import_jsx_runtime244 = __toESM(require_jsx_runtime(), 1);
var _excluded97 = ["render", "className", "onClick", "onPointerUp"];
var AiAssistantPanelTrigger = forwardRef2(function AiAssistantPanelTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick,
    onPointerUp
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded97);
  const rootProps = useGridRootProps3();
  const buttonId = useId();
  const panelId = useId();
  const apiRef = useGridApiContext3();
  const panelState = useGridSelector(apiRef, gridPreferencePanelStateSelector);
  const open = panelState.open && panelState.openedPanelValue === GridPreferencePanelsValue.aiAssistant;
  const state = {
    open
  };
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const {
    aiAssistantPanelTriggerRef
  } = useGridPanelContext();
  const handleRef = useForkRef_default(ref, aiAssistantPanelTriggerRef);
  const handleClick = (event) => {
    if (open) {
      apiRef.current.hidePreferences();
    } else {
      apiRef.current.showPreferences(GridPreferencePanelsValue.aiAssistant, panelId, buttonId);
    }
    onClick == null ? void 0 : onClick(event);
  };
  const handlePointerUp = (event) => {
    if (open) {
      event.stopPropagation();
    }
    onPointerUp == null ? void 0 : onPointerUp(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    id: buttonId,
    "aria-haspopup": "true",
    "aria-expanded": open ? "true" : void 0,
    "aria-controls": open ? panelId : void 0,
    className: resolvedClassName
  }, other, {
    onClick: handleClick,
    onPointerUp: handlePointerUp,
    ref: handleRef
  }), state);
  return (0, import_jsx_runtime244.jsx)(React338.Fragment, {
    children: element
  });
});
if (true) AiAssistantPanelTrigger.displayName = "AiAssistantPanelTrigger";
true ? AiAssistantPanelTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * A function to customize rendering of the component.
   */
  className: import_prop_types159.default.oneOfType([import_prop_types159.default.func, import_prop_types159.default.string]),
  disabled: import_prop_types159.default.bool,
  id: import_prop_types159.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types159.default.oneOfType([import_prop_types159.default.element, import_prop_types159.default.func]),
  role: import_prop_types159.default.string,
  size: import_prop_types159.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types159.default.node,
  style: import_prop_types159.default.object,
  tabIndex: import_prop_types159.default.number,
  title: import_prop_types159.default.string,
  touchRippleRef: import_prop_types159.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanel.js
var React342 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aiAssistant/gridAiAssistantSelectors.js
var gridAiAssistantStateSelector = createRootSelector((state) => state.aiAssistant);
var gridAiAssistantActiveConversationIndexSelector = createSelector3(gridAiAssistantStateSelector, (aiAssistant) => aiAssistant == null ? void 0 : aiAssistant.activeConversationIndex);
var gridAiAssistantConversationsSelector = createSelector3(gridAiAssistantStateSelector, (aiAssistant) => aiAssistant == null ? void 0 : aiAssistant.conversations);
var gridAiAssistantActiveConversationSelector = createSelectorMemoized2(gridAiAssistantConversationsSelector, gridAiAssistantActiveConversationIndexSelector, (conversations, index) => conversations[index]);

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanelConversation.js
var React340 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/prompt/GridPrompt.js
var React339 = __toESM(require_react(), 1);
var import_jsx_runtime245 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses122 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["prompt"],
    iconContainer: ["promptIconContainer"],
    icon: ["promptIcon"],
    text: ["promptText"],
    content: ["promptContent"],
    action: ["promptAction"],
    feedback: ["promptFeedback"],
    changeList: ["promptChangeList"],
    changesToggle: ["promptChangesToggle"],
    changesToggleIcon: ["promptChangesToggleIcon"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var fadeIn = keyframes({
  from: {
    opacity: 0
  },
  to: {
    opacity: 1
  }
});
var fadeInUp = keyframes({
  from: {
    opacity: 0,
    transform: "translateY(5px)"
  },
  to: {
    opacity: 1,
    transform: "translateY(0)"
  }
});
var PromptItem = styled_default("li", {
  name: "MuiDataGrid",
  slot: "Prompt"
})`
  display: flex;
  padding: ${vars.spacing(1, 1.25)};
  align-items: flex-start;
  overflow: hidden;
  .${gridClasses.promptAction} {
    opacity: 0;
    transition: ${vars.transition(["opacity"], {
  duration: vars.transitions.duration.short
})};
  }
  &:hover .${gridClasses.promptAction}, & .${gridClasses.promptAction}:focus-visible {
    opacity: 1;
  }
  @media (prefers-reduced-motion: no-preference) {
    animation: ${fadeInUp} ${vars.transitions.duration.long} ${vars.transitions.easing.easeInOut};
  }
`;
var PromptContent = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PromptContent"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  alignItems: "flex-start",
  overflow: "hidden"
});
var PromptText = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PromptText"
})({
  font: vars.typography.font.body
});
var PromptIconContainer = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PromptIconContainer"
})({
  flexShrink: 0,
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  width: 36,
  height: 36,
  marginRight: vars.spacing(1.5)
});
var PromptIcon = styled_default("svg", {
  name: "MuiDataGrid",
  slot: "PromptIcon"
})`
  color: ${({
  ownerState
}) => ownerState.variant === "error" ? vars.colors.foreground.error : vars.colors.foreground.muted};
  @media (prefers-reduced-motion: no-preference) {
    animation: ${fadeIn} ${vars.transitions.duration.short} ${vars.transitions.easing.easeInOut};
  }
`;
var PromptFeedback = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PromptFeedback"
})({
  font: vars.typography.font.small,
  color: vars.colors.foreground.muted,
  variants: [{
    props: {
      variant: "error"
    },
    style: {
      color: vars.colors.foreground.error
    }
  }]
});
var PromptChangeList = styled_default("div", {
  name: "MuiDataGrid",
  slot: "PromptChangeList"
})({
  display: "flex",
  flexWrap: "wrap",
  gap: vars.spacing(0.5),
  width: "100%",
  marginTop: vars.spacing(1),
  overflow: "hidden"
});
var PromptChangesToggle = styled_default("button", {
  name: "MuiDataGrid",
  slot: "PromptChangesToggle"
})({
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(0.25),
  padding: 0,
  font: vars.typography.font.small,
  color: vars.colors.foreground.accent,
  fontWeight: vars.typography.fontWeight.medium,
  cursor: "pointer",
  border: "none",
  background: "none",
  outline: "none",
  "&:hover, &:focus-visible": {
    textDecoration: "underline"
  }
});
var PromptChangesToggleIcon = styled_default("svg", {
  name: "MuiDataGrid",
  slot: "PromptChangesToggleIcon"
})({
  variants: [{
    props: {
      showChanges: true
    },
    style: {
      transform: "rotate(180deg)"
    }
  }]
});
function GridPrompt(props) {
  const {
    value,
    response,
    helperText,
    variant,
    onRerun
  } = props;
  const rootProps = useGridRootProps3();
  const [showChanges, setShowChanges] = React339.useState(false);
  const ownerState = {
    classes: rootProps.classes,
    variant,
    showChanges
  };
  const classes2 = useUtilityClasses122(ownerState);
  const apiRef = useGridApiContext3();
  const columns = useGridSelector(apiRef, gridColumnLookupSelector);
  const changesListId = useId();
  const getColumnName = React339.useCallback((column) => {
    var _a;
    return ((_a = columns[column]) == null ? void 0 : _a.headerName) ?? column;
  }, [columns]);
  const getGroupingChanges = React339.useCallback((grouping) => {
    return grouping.map((group) => ({
      label: getColumnName(group.column),
      description: apiRef.current.getLocaleText("promptChangeGroupDescription")(getColumnName(group.column)),
      icon: rootProps.slots.promptGroupIcon
    }));
  }, [apiRef, getColumnName, rootProps.slots.promptGroupIcon]);
  const getAggregationChanges = React339.useCallback((aggregation) => {
    return Object.keys(aggregation).map((column) => ({
      label: apiRef.current.getLocaleText("promptChangeAggregationLabel")(getColumnName(column), aggregation[column]),
      description: apiRef.current.getLocaleText("promptChangeAggregationDescription")(getColumnName(column), aggregation[column]),
      icon: rootProps.slots.promptAggregationIcon
    }));
  }, [apiRef, getColumnName, rootProps.slots.promptAggregationIcon]);
  const getFilterChanges = React339.useCallback((filters) => {
    return filters.map((filter) => {
      const filterOperator = apiRef.current.getLocaleText(`filterOperator${capitalize(filter.operator)}`);
      let filterValue = filter.value;
      if (isSingleSelectColDef(columns[filter.column])) {
        const allOptions = getValueOptions(columns[filter.column]) ?? [];
        const colDef = columns[filter.column];
        const getOptionLabel = colDef.getOptionLabel ?? ((option) => typeof option === "object" ? option.label : String(option));
        const getOptionValue = colDef.getOptionValue ?? ((option) => typeof option === "object" ? option.value : option);
        if (Array.isArray(filterValue)) {
          filterValue = filterValue.map((filterVal) => {
            const option = allOptions.find((opt) => String(getOptionValue(opt)) === String(filterVal));
            return option ? getOptionLabel(option) : String(filterVal);
          }).join(", ");
        } else {
          const option = allOptions.find((opt) => String(getOptionValue(opt)) === String(filterValue));
          filterValue = option ? getOptionLabel(option) : String(filterValue);
        }
      }
      return {
        label: apiRef.current.getLocaleText("promptChangeFilterLabel")(getColumnName(filter.column), filterOperator, filterValue),
        description: apiRef.current.getLocaleText("promptChangeFilterDescription")(getColumnName(filter.column), filterOperator, filterValue),
        icon: rootProps.slots.promptFilterIcon
      };
    });
  }, [apiRef, columns, getColumnName, rootProps.slots.promptFilterIcon]);
  const getSortingChanges = React339.useCallback((sorting) => {
    return sorting.map((sort) => ({
      label: getColumnName(sort.column),
      description: apiRef.current.getLocaleText("promptChangeSortDescription")(getColumnName(sort.column), sort.direction),
      icon: sort.direction === "asc" ? rootProps.slots.promptSortAscIcon : rootProps.slots.promptSortDescIcon
    }));
  }, [apiRef, getColumnName, rootProps.slots.promptSortAscIcon, rootProps.slots.promptSortDescIcon]);
  const getPivotingChanges = React339.useCallback((pivoting) => {
    if (!("columns" in pivoting)) {
      return [];
    }
    const changes = [{
      label: apiRef.current.getLocaleText("promptChangePivotEnableLabel"),
      icon: rootProps.slots.promptPivotIcon,
      description: apiRef.current.getLocaleText("promptChangePivotEnableDescription")
    }];
    if (pivoting.columns.length) {
      changes.push({
        label: apiRef.current.getLocaleText("promptChangePivotColumnsLabel")(pivoting.columns.length),
        icon: rootProps.slots.columnMenuManageColumnsIcon,
        description: pivoting.columns.map((column) => apiRef.current.getLocaleText("promptChangePivotColumnsDescription")(getColumnName(column.column), column.direction)).join(`, `)
      });
    }
    if (pivoting.rows.length) {
      changes.push({
        label: apiRef.current.getLocaleText("promptChangePivotRowsLabel")(pivoting.rows.length),
        icon: rootProps.slots.densityStandardIcon,
        description: pivoting.rows.map((column) => getColumnName(column)).join(`, `)
      });
    }
    if (pivoting.values.length) {
      changes.push({
        label: apiRef.current.getLocaleText("promptChangePivotValuesLabel")(pivoting.values.length),
        icon: rootProps.slots.promptAggregationIcon,
        description: pivoting.values.map((aggregation) => Object.keys(aggregation).map((column) => apiRef.current.getLocaleText("promptChangePivotValuesDescription")(getColumnName(column), aggregation[column]))).join(`, `)
      });
    }
    return changes;
  }, [apiRef, getColumnName, rootProps.slots]);
  const getChartChanges = React339.useCallback((chart) => {
    return {
      label: apiRef.current.getLocaleText("toolbarCharts"),
      description: apiRef.current.getLocaleText("promptChangeChartsLabel")(chart.dimensions.length, chart.values.length),
      icon: rootProps.slots.promptChartsIcon
    };
  }, [apiRef, rootProps.slots.promptChartsIcon]);
  const changeList = React339.useMemo(() => {
    if (!response) {
      return [];
    }
    const changes = [];
    if (response.grouping.length) {
      changes.push(...getGroupingChanges(response.grouping));
    }
    if (response.aggregation && Object.keys(response.aggregation).length) {
      changes.push(...getAggregationChanges(response.aggregation));
    }
    if (response.filters.length) {
      changes.push(...getFilterChanges(response.filters));
    }
    if (response.sorting.length) {
      changes.push(...getSortingChanges(response.sorting));
    }
    if (response.pivoting && "columns" in response.pivoting) {
      changes.push(...getPivotingChanges(response.pivoting));
    }
    if (response.chart) {
      changes.push(getChartChanges(response.chart));
    }
    return changes;
  }, [response, getGroupingChanges, getAggregationChanges, getFilterChanges, getSortingChanges, getPivotingChanges, getChartChanges]);
  return (0, import_jsx_runtime245.jsxs)(PromptItem, {
    ownerState,
    className: classes2.root,
    children: [(0, import_jsx_runtime245.jsx)(PromptIconContainer, {
      ownerState,
      className: classes2.iconContainer,
      children: !response && variant !== "error" ? (0, import_jsx_runtime245.jsx)(rootProps.slots.baseCircularProgress, {
        size: 20
      }) : (0, import_jsx_runtime245.jsx)(PromptIcon, {
        as: rootProps.slots.promptIcon,
        ownerState,
        className: classes2.icon,
        fontSize: "small"
      })
    }), (0, import_jsx_runtime245.jsxs)(PromptContent, {
      ownerState,
      className: classes2.content,
      children: [(0, import_jsx_runtime245.jsx)(PromptText, {
        ownerState,
        className: classes2.text,
        children: value
      }), (0, import_jsx_runtime245.jsx)(PromptFeedback, {
        ownerState,
        className: classes2.feedback,
        children: helperText
      }), changeList.length > 0 ? (0, import_jsx_runtime245.jsxs)(React339.Fragment, {
        children: [(0, import_jsx_runtime245.jsxs)(PromptChangesToggle, {
          ownerState,
          className: classes2.changesToggle,
          "aria-expanded": showChanges,
          "aria-controls": changesListId,
          onClick: () => setShowChanges(!showChanges),
          children: [apiRef.current.getLocaleText("promptAppliedChanges"), (0, import_jsx_runtime245.jsx)(PromptChangesToggleIcon, {
            as: rootProps.slots.promptChangesToggleIcon,
            ownerState,
            fontSize: "small"
          })]
        }), showChanges && (0, import_jsx_runtime245.jsx)(PromptChangeList, {
          id: changesListId,
          ownerState,
          className: classes2.changeList,
          children: changeList.map((change) => (0, import_jsx_runtime245.jsx)(rootProps.slots.baseTooltip, {
            title: change.description,
            children: (0, import_jsx_runtime245.jsx)(rootProps.slots.baseChip, {
              label: change.label,
              icon: (0, import_jsx_runtime245.jsx)(change.icon, {}),
              size: "small"
            })
          }, change.label))
        })]
      }) : null]
    }), (0, import_jsx_runtime245.jsx)(rootProps.slots.baseTooltip, {
      title: apiRef.current.getLocaleText("promptRerun"),
      enterDelay: 500,
      children: (0, import_jsx_runtime245.jsx)(rootProps.slots.baseIconButton, {
        size: "small",
        className: classes2.action,
        onClick: onRerun,
        children: (0, import_jsx_runtime245.jsx)(rootProps.slots.promptRerunIcon, {
          fontSize: "small"
        })
      })
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanelConversation.js
var import_jsx_runtime246 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses123 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["aiAssistantPanelConversation"],
    list: ["aiAssistantPanelConversationList"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var AiAssistantPanelConversationRoot = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelConversation"
})({
  flexShrink: 0,
  height: "100%"
});
var AiAssistantPanelConversationList = styled_default2("ol", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelConversationList"
})({
  flex: 1,
  padding: 0,
  margin: 0
});
function GridAiAssistantPanelConversation(props) {
  const {
    conversation
  } = props;
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses123(rootProps);
  const ref = React340.useRef(null);
  const apiRef = useGridApiContext3();
  React340.useEffect(() => {
    var _a, _b;
    (_b = ref.current) == null ? void 0 : _b.scrollTo({
      top: (_a = ref.current) == null ? void 0 : _a.scrollHeight,
      behavior: "smooth"
    });
  }, [conversation]);
  return (0, import_jsx_runtime246.jsx)(AiAssistantPanelConversationRoot, {
    className: classes2.root,
    ownerState: rootProps,
    ref,
    children: (0, import_jsx_runtime246.jsx)(AiAssistantPanelConversationList, {
      className: classes2.list,
      ownerState: rootProps,
      children: conversation.prompts.map((item) => (0, import_jsx_runtime246.jsx)(GridPrompt, _extends({}, item, {
        onRerun: () => apiRef.current.aiAssistant.processPrompt(item.value)
      }), item.createdAt.toISOString()))
    })
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/promptField/GridPromptField.js
var import_jsx_runtime247 = __toESM(require_jsx_runtime(), 1);
var _excluded98 = ["ref"];
function GridPromptField(props) {
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  let placeholder = apiRef.current.getLocaleText("promptFieldPlaceholder");
  if (IS_SPEECH_RECOGNITION_SUPPORTED) {
    placeholder = apiRef.current.getLocaleText("promptFieldPlaceholderWithRecording");
  }
  return (0, import_jsx_runtime247.jsx)(PromptField, _extends({}, props, {
    children: (0, import_jsx_runtime247.jsx)(PromptFieldControl, {
      onKeyDown: (event) => {
        if (event.key === "Enter") {
          event.preventDefault();
        }
      },
      render: (_ref, state) => {
        var _a;
        let {
          ref
        } = _ref, controlProps = _objectWithoutPropertiesLoose(_ref, _excluded98);
        return (0, import_jsx_runtime247.jsx)(rootProps.slots.baseTextField, _extends({}, controlProps, {
          fullWidth: true,
          inputRef: ref,
          "aria-label": apiRef.current.getLocaleText("promptFieldLabel"),
          placeholder: state.recording ? apiRef.current.getLocaleText("promptFieldPlaceholderListening") : placeholder,
          size: "small",
          multiline: true,
          autoFocus: true,
          slotProps: _extends({
            input: _extends({
              startAdornment: IS_SPEECH_RECOGNITION_SUPPORTED ? (0, import_jsx_runtime247.jsx)(rootProps.slots.baseTooltip, {
                title: state.recording ? apiRef.current.getLocaleText("promptFieldStopRecording") : apiRef.current.getLocaleText("promptFieldRecord"),
                children: (0, import_jsx_runtime247.jsx)(PromptFieldRecord, {
                  size: "small",
                  edge: "start",
                  color: state.recording ? "primary" : "default",
                  children: (0, import_jsx_runtime247.jsx)(rootProps.slots.promptSpeechRecognitionIcon, {
                    fontSize: "small"
                  })
                })
              }) : (0, import_jsx_runtime247.jsx)(rootProps.slots.baseTooltip, {
                title: apiRef.current.getLocaleText("promptFieldSpeechRecognitionNotSupported"),
                children: (0, import_jsx_runtime247.jsx)(rootProps.slots.promptSpeechRecognitionOffIcon, {
                  fontSize: "small"
                })
              }),
              endAdornment: (0, import_jsx_runtime247.jsx)(rootProps.slots.baseTooltip, {
                title: apiRef.current.getLocaleText("promptFieldSend"),
                children: (0, import_jsx_runtime247.jsx)("span", {
                  children: (0, import_jsx_runtime247.jsx)(PromptFieldSend, {
                    size: "small",
                    edge: "end",
                    color: "primary",
                    children: (0, import_jsx_runtime247.jsx)(rootProps.slots.promptSendIcon, {
                      fontSize: "small"
                    })
                  })
                })
              })
            }, (_a = controlProps.slotProps) == null ? void 0 : _a.input)
          }, controlProps.slotProps)
        }));
      }
    })
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanelSuggestions.js
var import_jsx_runtime248 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses124 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["aiAssistantPanelSuggestions"],
    list: ["aiAssistantPanelSuggestionsList"],
    item: ["aiAssistantPanelSuggestionsItem"],
    label: ["aiAssistantPanelSuggestionsLabel"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var AiAssistantPanelSuggestionsRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelSuggestions"
})({
  display: "flex",
  flexDirection: "column",
  gap: vars.spacing(0.75)
});
var AiAssistantPanelSuggestionsList = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelSuggestionsList"
})({
  display: "flex",
  gap: vars.spacing(0.75),
  overflow: "auto",
  padding: vars.spacing(1),
  margin: vars.spacing(-1),
  scrollbarWidth: "thin"
});
var AiAssistantPanelSuggestionsLabel = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelSuggestionsLabel"
})({
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(1),
  font: vars.typography.font.body,
  color: vars.colors.foreground.muted,
  paddingLeft: vars.spacing(0.5)
});
function GridAiAssistantPanelSuggestions(props) {
  const {
    suggestions
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const ownerState = {
    classes: rootProps.classes
  };
  const classes2 = useUtilityClasses124(ownerState);
  return (0, import_jsx_runtime248.jsxs)(AiAssistantPanelSuggestionsRoot, {
    className: classes2.root,
    ownerState,
    children: [(0, import_jsx_runtime248.jsx)(AiAssistantPanelSuggestionsLabel, {
      className: classes2.label,
      ownerState,
      children: apiRef.current.getLocaleText("aiAssistantSuggestions")
    }), (0, import_jsx_runtime248.jsx)(AiAssistantPanelSuggestionsList, {
      className: classes2.list,
      ownerState,
      children: suggestions.map((suggestion) => (0, import_jsx_runtime248.jsx)(rootProps.slots.baseChip, {
        label: suggestion.value,
        className: classes2.item,
        onClick: () => apiRef.current.aiAssistant.processPrompt(suggestion.value),
        variant: "outlined"
      }, suggestion.value))
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanelConversationsMenu.js
var React341 = __toESM(require_react(), 1);
var import_jsx_runtime249 = __toESM(require_jsx_runtime(), 1);
function GridAiAssistantPanelConversationsMenu() {
  var _a, _b;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const activeConversationIndex = useGridSelector(apiRef, gridAiAssistantActiveConversationIndexSelector);
  const conversations = useGridSelector(apiRef, gridAiAssistantConversationsSelector);
  const [open, setOpen] = React341.useState(false);
  const menuId = useId();
  const triggerId = useId();
  const triggerRef = React341.useRef(null);
  const handleOpen = () => {
    setOpen(!open);
  };
  const handleClose = () => {
    setOpen(false);
  };
  const sortedConversations = React341.useMemo(() => {
    return [...conversations].sort((a, b) => {
      if (!a.prompts.length) {
        return -1;
      }
      if (!b.prompts.length) {
        return 1;
      }
      return b.prompts[b.prompts.length - 1].createdAt.getTime() - a.prompts[a.prompts.length - 1].createdAt.getTime();
    });
  }, [conversations]);
  return (0, import_jsx_runtime249.jsxs)(React341.Fragment, {
    children: [(0, import_jsx_runtime249.jsx)(rootProps.slots.baseTooltip, {
      title: apiRef.current.getLocaleText("aiAssistantPanelConversationHistory"),
      enterDelay: 500,
      children: (0, import_jsx_runtime249.jsx)("span", {
        children: (0, import_jsx_runtime249.jsx)(rootProps.slots.baseIconButton, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
          disabled: conversations.length === 0,
          id: triggerId,
          "aria-haspopup": "true",
          "aria-controls": open ? menuId : void 0,
          "aria-expanded": open ? "true" : void 0,
          "aria-label": apiRef.current.getLocaleText("aiAssistantPanelConversationHistory"),
          onClick: handleOpen,
          ref: triggerRef,
          children: (0, import_jsx_runtime249.jsx)(rootProps.slots.aiAssistantPanelHistoryIcon, {
            fontSize: "small"
          })
        }))
      })
    }), (0, import_jsx_runtime249.jsx)(GridMenu, {
      target: triggerRef.current,
      open,
      onClose: handleClose,
      position: "bottom-end",
      children: (0, import_jsx_runtime249.jsx)(rootProps.slots.baseMenuList, _extends({
        id: menuId,
        "aria-labelledby": triggerId,
        autoFocusItem: true
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseMenuList, {
        children: sortedConversations.map((conversation, sortedIndex) => {
          const conversationIndex = conversations.findIndex((c) => c === conversation);
          return (0, import_jsx_runtime249.jsx)(rootProps.slots.baseMenuItem, {
            selected: conversationIndex === activeConversationIndex,
            onClick: () => {
              apiRef.current.aiAssistant.setActiveConversationIndex(conversationIndex);
              handleClose();
            },
            children: conversation.title
          }, `${conversation.id}-${sortedIndex}`);
        })
      }))
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/aiAssistantPanel/GridAiAssistantPanel.js
var import_jsx_runtime250 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses125 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["aiAssistantPanel"],
    header: ["aiAssistantPanelHeader"],
    title: ["aiAssistantPanelTitle"],
    titleContainer: ["aiAssistantPanelTitleContainer"],
    conversationTitle: ["aiAssistantPanelConversationTitle"],
    body: ["aiAssistantPanelBody"],
    emptyText: ["aiAssistantPanelEmptyText"],
    footer: ["aiAssistantPanelFooter"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var AiAssistantPanelRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanel"
})({
  flexDirection: "column",
  width: 380,
  maxHeight: "none",
  overflow: "hidden"
});
var AiAssistantPanelHeader = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelHeader"
})({
  flexShrink: 0,
  display: "flex",
  alignItems: "center",
  width: "100%",
  boxSizing: "border-box",
  borderBottom: `1px solid ${vars.colors.border.base}`,
  height: 52,
  padding: vars.spacing(0, 0.75, 0, 2)
});
var AiAssistantPanelTitleContainer = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelTitleContainer"
})({
  display: "flex",
  flexDirection: "column",
  flex: 1,
  overflow: "hidden"
});
var AiAssistantPanelTitle = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelTitle"
})({
  font: vars.typography.font.body,
  fontWeight: vars.typography.fontWeight.medium,
  marginTop: vars.spacing(0.25)
});
var AiAssistantPanelConversationTitle = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelConversationTitle"
})({
  font: vars.typography.font.small,
  color: vars.colors.foreground.muted,
  marginTop: vars.spacing(-0.25),
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
});
var AiAssistantPanelBody = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelBody"
})({
  flexGrow: 0,
  flexShrink: 0,
  height: 260,
  display: "flex",
  justifyContent: "center",
  alignItems: "center"
});
var AiAssistantPanelEmptyText = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelEmptyText"
})({
  font: vars.typography.font.body,
  color: vars.colors.foreground.muted
});
var AiAssistantPanelFooter = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "AiAssistantPanelFooter"
})({
  flexShrink: 0,
  display: "flex",
  flexDirection: "column",
  gap: vars.spacing(1),
  borderTop: `1px solid ${vars.colors.border.base}`,
  padding: vars.spacing(1)
});
function GridAiAssistantPanel() {
  var _a, _b;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const classes2 = useUtilityClasses125(rootProps);
  const activeConversation = useGridSelector(apiRef, gridAiAssistantActiveConversationSelector);
  const conversations = useGridSelector(apiRef, gridAiAssistantConversationsSelector);
  const conversationTitle = (activeConversation == null ? void 0 : activeConversation.title) || apiRef.current.getLocaleText("aiAssistantPanelNewConversation");
  const createConversation = React342.useCallback(() => {
    const newConversation = conversations.findIndex((conversation) => !conversation.prompts.length);
    if (newConversation !== -1) {
      apiRef.current.aiAssistant.setActiveConversationIndex(newConversation);
    } else {
      apiRef.current.aiAssistant.setConversations((newConversations) => [...newConversations, {
        title: apiRef.current.getLocaleText("aiAssistantPanelNewConversation"),
        prompts: []
      }]);
      apiRef.current.aiAssistant.setActiveConversationIndex(conversations.length);
    }
  }, [apiRef, conversations]);
  return (0, import_jsx_runtime250.jsxs)(AiAssistantPanelRoot, {
    className: classes2.root,
    ownerState: rootProps,
    children: [(0, import_jsx_runtime250.jsxs)(AiAssistantPanelHeader, {
      className: classes2.header,
      ownerState: rootProps,
      children: [(0, import_jsx_runtime250.jsxs)(AiAssistantPanelTitleContainer, {
        className: classes2.titleContainer,
        ownerState: rootProps,
        children: [(0, import_jsx_runtime250.jsx)(AiAssistantPanelTitle, {
          className: classes2.title,
          ownerState: rootProps,
          children: apiRef.current.getLocaleText("aiAssistantPanelTitle")
        }), (0, import_jsx_runtime250.jsx)(AiAssistantPanelConversationTitle, {
          className: classes2.conversationTitle,
          ownerState: rootProps,
          title: conversationTitle,
          children: conversationTitle
        })]
      }), (0, import_jsx_runtime250.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("aiAssistantPanelNewConversation"),
        enterDelay: 500,
        children: (0, import_jsx_runtime250.jsx)("span", {
          children: (0, import_jsx_runtime250.jsx)(rootProps.slots.baseIconButton, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
            disabled: !conversations.length || !(activeConversation == null ? void 0 : activeConversation.prompts.length),
            onClick: createConversation,
            children: (0, import_jsx_runtime250.jsx)(rootProps.slots.aiAssistantPanelNewConversationIcon, {
              fontSize: "small"
            })
          }))
        })
      }), (0, import_jsx_runtime250.jsx)(GridAiAssistantPanelConversationsMenu, {}), (0, import_jsx_runtime250.jsx)(rootProps.slots.baseIconButton, _extends({}, (_b = rootProps.slotProps) == null ? void 0 : _b.baseIconButton, {
        "aria-label": apiRef.current.getLocaleText("aiAssistantPanelClose"),
        onClick: apiRef.current.hidePreferences,
        children: (0, import_jsx_runtime250.jsx)(rootProps.slots.aiAssistantPanelCloseIcon, {
          fontSize: "small"
        })
      }))]
    }), (0, import_jsx_runtime250.jsx)(AiAssistantPanelBody, {
      className: classes2.body,
      ownerState: rootProps,
      children: activeConversation && activeConversation.prompts.length > 0 ? (0, import_jsx_runtime250.jsx)(GridAiAssistantPanelConversation, {
        conversation: activeConversation
      }) : (0, import_jsx_runtime250.jsx)(AiAssistantPanelEmptyText, {
        ownerState: rootProps,
        className: classes2.emptyText,
        children: apiRef.current.getLocaleText("aiAssistantPanelEmptyConversation")
      })
    }), (0, import_jsx_runtime250.jsxs)(AiAssistantPanelFooter, {
      className: classes2.footer,
      ownerState: rootProps,
      children: [(0, import_jsx_runtime250.jsx)(GridPromptField, {
        onSubmit: apiRef.current.aiAssistant.processPrompt
      }), rootProps.aiAssistantSuggestions && rootProps.aiAssistantSuggestions.length > 0 && (0, import_jsx_runtime250.jsx)(GridAiAssistantPanelSuggestions, {
        suggestions: rootProps.aiAssistantSuggestions
      })]
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/GridChartsPanel.js
var React353 = __toESM(require_react(), 1);
var import_prop_types161 = __toESM(require_prop_types(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/chart/GridChartsPanelChart.js
var import_prop_types160 = __toESM(require_prop_types(), 1);
var import_jsx_runtime251 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses126 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["chartsManagement"],
    chartTypeRoot: ["chartTypeRoot"],
    button: ["chartTypeSelectorButton"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridChartsManagementRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsManagement"
})({
  display: "flex",
  flexDirection: "column",
  justifyContent: "space-between",
  height: "100%"
});
var GridChartTypeRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartTypeRoot"
})({
  display: "grid",
  gridTemplateColumns: "1fr 1fr 1fr",
  gap: vars.spacing(1),
  padding: vars.spacing(1)
});
var GridChartTypeButton = styled_default2("button", {
  name: "MuiDataGrid",
  slot: "ChartTypeSelectorButton",
  shouldForwardProp: (prop) => prop !== "isSelected"
})(({
  isSelected
}) => {
  return {
    backgroundColor: isSelected ? `color-mix(in srgb, ${vars.colors.interactive.selected} calc(${vars.colors.interactive.selectedOpacity} * 100%), ${vars.colors.background.base})` : vars.colors.background.base,
    color: isSelected ? vars.colors.interactive.selected : vars.colors.foreground.muted,
    cursor: "pointer",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    gap: vars.spacing(0.5),
    padding: vars.spacing(1.5, 1, 1),
    border: `1px solid ${isSelected ? vars.colors.interactive.selected : vars.colors.border.base}`,
    font: vars.typography.font.small,
    fontWeight: vars.typography.fontWeight.medium,
    borderRadius: vars.radius.base,
    transition: vars.transition(["border-color", "background-color", "color"], {
      duration: vars.transitions.duration.short,
      easing: vars.transitions.easing.easeInOut
    }),
    "&:hover": {
      backgroundColor: isSelected ? `color-mix(in srgb, ${vars.colors.interactive.selected} calc(${vars.colors.interactive.selectedOpacity} * 100%), ${vars.colors.background.base})` : vars.colors.interactive.hover
    }
  };
});
function GridChartsPanelChart(props) {
  const {
    schema,
    selectedChartType,
    onChartTypeChange
  } = props;
  const rootProps = useGridRootProps3();
  const classes2 = useUtilityClasses126(rootProps);
  return (0, import_jsx_runtime251.jsx)(GridChartsManagementRoot, {
    ownerState: rootProps,
    className: classes2.root,
    children: (0, import_jsx_runtime251.jsx)(GridChartTypeRoot, {
      className: classes2.chartTypeRoot,
      children: Object.entries(schema).map(([type, config]) => {
        var _a;
        return (0, import_jsx_runtime251.jsxs)(GridChartTypeButton, _extends({
          className: classes2.button,
          isSelected: type === selectedChartType,
          onClick: () => onChartTypeChange(type)
        }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
          children: [(0, import_jsx_runtime251.jsx)(config.icon, {
            style: {
              width: 32,
              height: 32
            }
          }), config.label]
        }), type);
      })
    })
  });
}
true ? GridChartsPanelChart.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  schema: import_prop_types160.default.object
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/customize/GridChartsPanelCustomize.js
var React346 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridChartIntegration.js
var React344 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsIntegration/GridChartsIntegrationContext.js
var React343 = __toESM(require_react(), 1);
var GridChartsIntegrationContext = React343.createContext(void 0);
if (true) GridChartsIntegrationContext.displayName = "GridChartsIntegrationContext";

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridChartIntegration.js
var useGridChartsIntegrationContext = (ignoreError = false) => {
  const context = React344.useContext(GridChartsIntegrationContext);
  if (!context && !ignoreError) {
    throw new Error(["MUI X: Could not find the Data Grid charts integration context.", "It looks like you rendered your component outside of a DataGrid, DataGridPro or DataGridPremium parent component.", "This can also happen if you are bundling multiple versions of the Data Grid."].join("\n"));
  }
  return context;
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/chartsIntegration/useGridChartsIntegration.js
var React345 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/constants/columnGroups.js
var COLUMN_GROUP_ID_SEPARATOR = ">->";

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/chartsIntegration/utils.js
var getBlockedSections = (column, rowGroupingModel, pivotModel) => {
  if (pivotModel) {
    const sections = [];
    const rows = pivotModel.rows.filter((item) => item.hidden !== true).map((item) => item.field);
    const values3 = pivotModel.values.filter((item) => item.hidden !== true).map((item) => item.field);
    const unwrappedFieldName = column.field.split(COLUMN_GROUP_ID_SEPARATOR).pop();
    if (values3.includes(unwrappedFieldName)) {
      sections.push("dimensions");
    }
    if (rows.includes(column.field)) {
      sections.push("values");
    }
    return sections;
  }
  if (rowGroupingModel.length > 0) {
    return rowGroupingModel.includes(column.field) ? ["values"] : [];
  }
  if (column.type !== "number") {
    return ["values"];
  }
  return [];
};
var isBlockedForSection = (column, section, rowGroupingModel, pivotModel) => getBlockedSections(column, rowGroupingModel, pivotModel).includes(section);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/chartsIntegration/useGridChartsIntegration.js
var import_jsx_runtime252 = __toESM(require_jsx_runtime(), 1);
var EMPTY_CHART_INTEGRATION_CONTEXT = {
  chartStateLookup: {},
  setChartState: () => {
  }
};
var EMPTY_CHART_INTEGRATION_CONTEXT_STATE = {
  synced: true,
  dimensions: [],
  values: [],
  type: "",
  configuration: {}
};
var chartsIntegrationStateInitializer = (state, props) => {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i;
  if (!props.chartsIntegration || !((_a = props.experimentalFeatures) == null ? void 0 : _a.charts)) {
    return _extends({}, state, {
      chartsIntegration: {
        activeChartId: "",
        charts: {}
      }
    });
  }
  const rowGroupingModel = (((_b = state.rowGrouping) == null ? void 0 : _b.model) ?? []).filter((item) => item !== void 0);
  const pivotModel = ((_c = state.pivoting) == null ? void 0 : _c.active) ? (_d = state.pivoting) == null ? void 0 : _d.model : void 0;
  const columnsLookup = ((_e = state.columns) == null ? void 0 : _e.lookup) ?? {};
  const charts = Object.fromEntries(Object.entries(((_g = (_f = props.initialState) == null ? void 0 : _f.chartsIntegration) == null ? void 0 : _g.charts) || {}).map(([chartId, chart]) => [chartId, {
    dimensions: (chart.dimensions || []).map((dimension) => typeof dimension === "string" ? {
      field: dimension,
      hidden: false
    } : dimension).filter((dimension) => {
      var _a2;
      return ((_a2 = columnsLookup[dimension.field]) == null ? void 0 : _a2.chartable) === true && !isBlockedForSection(columnsLookup[dimension.field], "dimensions", rowGroupingModel, pivotModel);
    }),
    values: (chart.values || []).map((value) => typeof value === "string" ? {
      field: value,
      hidden: false
    } : value).filter((value) => {
      var _a2;
      return ((_a2 = columnsLookup[value.field]) == null ? void 0 : _a2.chartable) === true && !isBlockedForSection(columnsLookup[value.field], "values", rowGroupingModel, pivotModel);
    })
  }]));
  return _extends({}, state, {
    chartsIntegration: {
      activeChartId: props.activeChartId ?? ((_i = (_h = props.initialState) == null ? void 0 : _h.chartsIntegration) == null ? void 0 : _i.activeChartId) ?? "",
      charts
    }
  });
};
var useGridChartsIntegration = (apiRef, props) => {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
  const visibleDimensions = React345.useRef({});
  const visibleValues = React345.useRef({});
  const schema = React345.useMemo(() => {
    var _a2, _b2;
    return ((_b2 = (_a2 = props.slotProps) == null ? void 0 : _a2.chartsPanel) == null ? void 0 : _b2.schema) || {};
  }, [(_b = (_a = props.slotProps) == null ? void 0 : _a.chartsPanel) == null ? void 0 : _b.schema]);
  const context = useGridChartsIntegrationContext(true);
  const isChartsIntegrationAvailable = !!props.chartsIntegration && !!((_c = props.experimentalFeatures) == null ? void 0 : _c.charts) && !!context;
  const activeChartId = gridChartsIntegrationActiveChartIdSelector(apiRef);
  const aggregationModel = gridAggregationModelSelector(apiRef);
  const pivotActive = gridPivotActiveSelector(apiRef);
  const pivotModel = gridPivotModelSelector(apiRef);
  const {
    chartStateLookup,
    setChartState
  } = context || EMPTY_CHART_INTEGRATION_CONTEXT;
  const availableChartIds = React345.useMemo(() => {
    const ids = Object.keys(chartStateLookup);
    Object.keys(visibleDimensions.current).forEach((chartId) => {
      if (!ids.includes(chartId)) {
        delete visibleDimensions.current[chartId];
        delete visibleValues.current[chartId];
      }
    });
    return ids;
  }, [chartStateLookup]);
  const syncedChartIds = React345.useMemo(() => availableChartIds.filter((chartId) => chartStateLookup[chartId].synced !== false), [availableChartIds, chartStateLookup]);
  const getColumnName = React345.useCallback((field) => {
    var _a2, _b2, _c2;
    const customFieldName = (_c2 = (_b2 = (_a2 = props.slotProps) == null ? void 0 : _a2.chartsPanel) == null ? void 0 : _b2.getColumnName) == null ? void 0 : _c2.call(_b2, field);
    if (customFieldName) {
      return customFieldName;
    }
    const columns = gridColumnLookupSelector(apiRef);
    const columnGroupPath = gridColumnGroupsUnwrappedModelSelector(apiRef)[field] ?? [];
    const columnGroupLookup = gridColumnGroupsLookupSelector(apiRef);
    const column = columns[field];
    const columnName = (column == null ? void 0 : column.headerName) || field;
    if (!pivotActive || !columnGroupPath) {
      return columnName;
    }
    const groupNames = columnGroupPath.map((group) => columnGroupLookup[group].headerName || group);
    return [columnName, ...groupNames].join(" - ");
  }, [apiRef, pivotActive, (_d = props.slotProps) == null ? void 0 : _d.chartsPanel]);
  const getValueDatasetLabel = React345.useCallback((field) => {
    var _a2, _b2, _c2;
    const customFieldName = (_c2 = (_b2 = (_a2 = props.slotProps) == null ? void 0 : _a2.chartsPanel) == null ? void 0 : _b2.getColumnName) == null ? void 0 : _c2.call(_b2, field);
    if (customFieldName) {
      return customFieldName;
    }
    const columnName = getColumnName(field);
    const fieldAggregation = gridAggregationModelSelector(apiRef)[field];
    const suffix = fieldAggregation ? ` (${getAggregationFunctionLabel({
      apiRef,
      aggregationRule: {
        aggregationFunctionName: fieldAggregation,
        aggregationFunction: props.aggregationFunctions[fieldAggregation] || {}
      }
    })})` : "";
    return `${columnName}${suffix}`;
  }, [apiRef, props.aggregationFunctions, (_e = props.slotProps) == null ? void 0 : _e.chartsPanel, getColumnName]);
  apiRef.current.registerControlState({
    stateId: "activeChartId",
    propModel: props.activeChartId,
    propOnChange: props.onActiveChartIdChange,
    stateSelector: gridChartsIntegrationActiveChartIdSelector,
    changeEvent: "activeChartIdChange"
  });
  const updateOtherModels = React345.useCallback(() => {
    var _a2;
    const rowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    if (((_a2 = visibleDimensions.current[activeChartId]) == null ? void 0 : _a2.length) > 0 && // if there was row grouping or if we are adding more than one dimension, set the new grouping model
    (rowGroupingModel.length > 0 || visibleDimensions.current[activeChartId].length > 1) && // if row grouping model starts with dimensions in the same order, we don't have to do anything
    visibleDimensions.current[activeChartId].some((item, index) => item.field !== rowGroupingModel[index])) {
      const newGroupingModel = visibleDimensions.current[activeChartId].map((item) => item.field);
      if (pivotActive) {
        apiRef.current.setPivotModel((prev) => _extends({}, prev, {
          rows: newGroupingModel.map((item) => ({
            field: item,
            hidden: false
          }))
        }));
      } else {
        apiRef.current.setRowGroupingModel(newGroupingModel);
        apiRef.current.setColumnVisibilityModel(_extends({}, apiRef.current.state.columns.columnVisibilityModel, Object.fromEntries(rowGroupingModel.map((item) => [item, true])), Object.fromEntries(newGroupingModel.map((item) => [item, false]))));
      }
    }
    if (!pivotActive && visibleValues.current[activeChartId] && rowGroupingModel.length > 0) {
      const aggregatedFields = Object.keys(aggregationModel);
      const aggregationsToAdd = {};
      visibleValues.current[activeChartId].forEach((item) => {
        const hasAggregation = aggregatedFields.includes(item.field);
        if (!hasAggregation) {
          aggregationsToAdd[item.field] = getAvailableAggregationFunctions({
            aggregationFunctions: props.aggregationFunctions,
            colDef: item,
            isDataSource: !!props.dataSource
          })[0];
        }
      });
      if (Object.keys(aggregationsToAdd).length > 0) {
        apiRef.current.setAggregationModel(_extends({}, aggregationModel, aggregationsToAdd));
      }
    }
  }, [apiRef, props.aggregationFunctions, props.dataSource, activeChartId, pivotActive, aggregationModel]);
  const handleRowDataUpdate = React345.useCallback((chartIds) => {
    var _a2;
    if (chartIds.length === 0 || chartIds.some((chartId) => !visibleDimensions.current[chartId] || !visibleValues.current[chartId])) {
      return;
    }
    const orderedFields = gridColumnFieldsSelector(apiRef);
    const rowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const rowsPerDepth = gridFilteredSortedDepthRowEntriesSelector(apiRef);
    const currentChartId = gridChartsIntegrationActiveChartIdSelector(apiRef);
    const defaultDepth = Math.max(0, (((_a2 = visibleDimensions.current[currentChartId]) == null ? void 0 : _a2.length) ?? 0) - 1);
    const rowsAtDefaultDepth = (rowsPerDepth[defaultDepth] ?? []).length;
    const dataColumns = [.../* @__PURE__ */ new Set([...Object.values(visibleDimensions.current).flat(), ...Object.values(visibleValues.current).flat()])].map((column) => {
      const isColumnGrouped = rowGroupingModel.includes(column.field);
      if (isColumnGrouped) {
        const groupedFieldName = isColumnGrouped ? getRowGroupingFieldFromGroupingCriteria(orderedFields.includes(GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD) ? null : column.field) : column.field;
        const columnDefinition = apiRef.current.getColumn(groupedFieldName);
        return _extends({}, columnDefinition, {
          dataFieldName: column.field,
          depth: rowGroupingModel.indexOf(column.field)
        });
      }
      return _extends({}, column, {
        dataFieldName: column.field,
        depth: defaultDepth
      });
    });
    const data = Object.fromEntries(dataColumns.map((column) => [column.dataFieldName, []]));
    const dataColumnsCount = dataColumns.length;
    for (let i2 = 0; i2 < rowsAtDefaultDepth; i2 += 1) {
      for (let j = 0; j < dataColumnsCount; j += 1) {
        let targetRow = rowsPerDepth[defaultDepth][i2].model;
        for (let d = defaultDepth; d > dataColumns[j].depth; d -= 1) {
          const rowId = gridRowIdSelector(apiRef, targetRow);
          targetRow = gridRowNodeSelector(apiRef, rowTree[rowId].parent);
        }
        const value = apiRef.current.getRowValue(targetRow, dataColumns[j]);
        if (value !== null) {
          data[dataColumns[j].dataFieldName].push(typeof value === "object" && "label" in value ? value.label : value);
        }
      }
    }
    chartIds.forEach((chartId) => {
      setChartState(chartId, {
        dimensions: visibleDimensions.current[chartId].map((dimension) => ({
          id: dimension.field,
          label: getColumnName(dimension.field),
          data: data[dimension.field] || []
        })),
        values: visibleValues.current[chartId].map((value) => ({
          id: value.field,
          label: getValueDatasetLabel(value.field),
          data: data[value.field] || []
        }))
      });
    });
  }, [apiRef, getColumnName, getValueDatasetLabel, setChartState]);
  const debouncedHandleRowDataUpdate = React345.useMemo(() => debounce(handleRowDataUpdate, 0), [handleRowDataUpdate]);
  const handleColumnDataUpdate = React345.useCallback((chartIds, updatedChartStateLookup) => {
    if (chartIds.length === 0) {
      return;
    }
    const rowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const chartableColumns = gridChartableColumnsSelector(apiRef);
    const selectedFields = chartIds.reduce((acc, chartId) => {
      const values4 = gridChartsValuesSelector(apiRef, chartId);
      const dimensions2 = gridChartsDimensionsSelector(apiRef, chartId);
      return _extends({}, acc, {
        [chartId]: {
          values: values4,
          dimensions: dimensions2
        }
      });
    }, {});
    const values3 = {};
    const dimensions = {};
    chartIds.forEach((chartId) => {
      dimensions[chartId] = [];
      values3[chartId] = [];
      const chartState = (updatedChartStateLookup == null ? void 0 : updatedChartStateLookup[chartId]) || chartStateLookup[chartId];
      const dimensionsSize = (chartState == null ? void 0 : chartState.maxDimensions) ? Math.min(chartState.maxDimensions, selectedFields[chartId].dimensions.length) : selectedFields[chartId].dimensions.length;
      const valuesSize = (chartState == null ? void 0 : chartState.maxValues) ? Math.min(chartState.maxValues, selectedFields[chartId].values.length) : selectedFields[chartId].values.length;
      for (let i2 = 0; i2 < valuesSize; i2 += 1) {
        if (chartableColumns[selectedFields[chartId].values[i2].field] && !isBlockedForSection(chartableColumns[selectedFields[chartId].values[i2].field], "values", rowGroupingModel, pivotActive ? pivotModel : void 0)) {
          if (!values3[chartId]) {
            values3[chartId] = [];
          }
          values3[chartId].push(selectedFields[chartId].values[i2]);
        }
      }
      for (let i2 = 0; i2 < dimensionsSize; i2 += 1) {
        const item = selectedFields[chartId].dimensions[i2];
        if (!selectedFields[chartId].values.some((valueItem) => valueItem.field === item.field) && chartableColumns[item.field] && !isBlockedForSection(chartableColumns[item.field], "dimensions", rowGroupingModel, pivotActive ? pivotModel : void 0)) {
          if (!dimensions[chartId]) {
            dimensions[chartId] = [];
          }
          dimensions[chartId].push(item);
        }
      }
      if (dimensions[chartId] && selectedFields[chartId].dimensions.length !== dimensions[chartId].length) {
        apiRef.current.updateChartDimensionsData(chartId, dimensions[chartId]);
      }
      if (values3[chartId] && selectedFields[chartId].values.length !== values3[chartId].length) {
        apiRef.current.updateChartValuesData(chartId, values3[chartId]);
      }
      visibleDimensions.current[chartId] = dimensions[chartId].filter((dimension) => dimension.hidden !== true).map((dimension) => chartableColumns[dimension.field]);
      visibleValues.current[chartId] = values3[chartId].filter((value) => value.hidden !== true).map((value) => chartableColumns[value.field]);
      if (visibleDimensions.current[chartId].length === 0 || visibleValues.current[chartId].length === 0) {
        visibleDimensions.current[chartId] = [];
        visibleValues.current[chartId] = [];
      }
    });
    updateOtherModels();
    debouncedHandleRowDataUpdate(chartIds);
  }, [apiRef, chartStateLookup, pivotActive, pivotModel, debouncedHandleRowDataUpdate, updateOtherModels]);
  const debouncedHandleColumnDataUpdate = React345.useMemo(() => debounce(handleColumnDataUpdate, 0), [handleColumnDataUpdate]);
  const setChartsPanelOpen = React345.useCallback((callback) => {
    if (!isChartsIntegrationAvailable) {
      return;
    }
    const panelOpen = gridChartsPanelOpenSelector(apiRef);
    const newPanelOpen = typeof callback === "function" ? callback(panelOpen) : callback;
    if (panelOpen === newPanelOpen) {
      return;
    }
    if (newPanelOpen) {
      apiRef.current.showSidebar(GridSidebarValue.Charts);
    } else {
      apiRef.current.hideSidebar();
    }
  }, [apiRef, isChartsIntegrationAvailable]);
  const updateChartDimensionsData = React345.useCallback((chartId, dimensions) => {
    if (!isChartsIntegrationAvailable) {
      return;
    }
    apiRef.current.setState((state) => {
      const newDimensions = typeof dimensions === "function" ? dimensions(state.chartsIntegration.charts[chartId].dimensions) : dimensions;
      return _extends({}, state, {
        chartsIntegration: _extends({}, state.chartsIntegration, {
          charts: _extends({}, state.chartsIntegration.charts, {
            [chartId]: _extends({}, state.chartsIntegration.charts[chartId], {
              dimensions: newDimensions
            })
          })
        })
      });
    });
    debouncedHandleColumnDataUpdate(syncedChartIds);
  }, [apiRef, isChartsIntegrationAvailable, syncedChartIds, debouncedHandleColumnDataUpdate]);
  const updateChartValuesData = React345.useCallback((chartId, values3) => {
    if (!isChartsIntegrationAvailable) {
      return;
    }
    apiRef.current.setState((state) => {
      const newValues = typeof values3 === "function" ? values3(state.chartsIntegration.charts[chartId].values) : values3;
      return _extends({}, state, {
        chartsIntegration: _extends({}, state.chartsIntegration, {
          charts: _extends({}, state.chartsIntegration.charts, {
            [chartId]: _extends({}, state.chartsIntegration.charts[chartId], {
              values: newValues
            })
          })
        })
      });
    });
    debouncedHandleColumnDataUpdate(syncedChartIds);
  }, [apiRef, isChartsIntegrationAvailable, syncedChartIds, debouncedHandleColumnDataUpdate]);
  const setActiveChartId = React345.useCallback((chartId) => {
    if (!isChartsIntegrationAvailable) {
      return;
    }
    apiRef.current.setState((state) => _extends({}, state, {
      chartsIntegration: _extends({}, state.chartsIntegration, {
        activeChartId: chartId
      })
    }));
  }, [apiRef, isChartsIntegrationAvailable]);
  const setChartType = React345.useCallback((chartId, type) => {
    var _a2, _b2, _c2, _d2;
    if (!isChartsIntegrationAvailable || !chartStateLookup[chartId]) {
      return;
    }
    const stateUpdate = {
      type,
      dimensionsLabel: (_a2 = schema[type]) == null ? void 0 : _a2.dimensionsLabel,
      valuesLabel: (_b2 = schema[type]) == null ? void 0 : _b2.valuesLabel,
      maxDimensions: (_c2 = schema[type]) == null ? void 0 : _c2.maxDimensions,
      maxValues: (_d2 = schema[type]) == null ? void 0 : _d2.maxValues
    };
    const updatedChartStateLookup = _extends({}, chartStateLookup, {
      [chartId]: _extends({}, chartStateLookup[chartId], stateUpdate)
    });
    handleColumnDataUpdate([chartId], updatedChartStateLookup);
    setChartState(chartId, stateUpdate);
  }, [isChartsIntegrationAvailable, chartStateLookup, schema, setChartState, handleColumnDataUpdate]);
  const setChartSynchronizationState = React345.useCallback((chartId, synced) => {
    if (!isChartsIntegrationAvailable || !chartStateLookup[chartId]) {
      return;
    }
    const stateUpdate = {
      synced
    };
    const updatedChartStateLookup = _extends({}, chartStateLookup, {
      [chartId]: _extends({}, chartStateLookup[chartId], stateUpdate)
    });
    setChartState(chartId, stateUpdate);
    apiRef.current.publishEvent("chartSynchronizationStateChange", {
      chartId,
      synced
    });
    if (synced) {
      debouncedHandleColumnDataUpdate([chartId], updatedChartStateLookup);
    }
  }, [apiRef, isChartsIntegrationAvailable, chartStateLookup, setChartState, debouncedHandleColumnDataUpdate]);
  const updateDataReference = React345.useCallback((field, originSection, targetSection, targetField, placementRelativeToTargetField) => {
    var _a2, _b2;
    const columns = gridColumnLookupSelector(apiRef);
    const dimensions = gridChartsDimensionsSelector(apiRef, activeChartId);
    const values3 = gridChartsValuesSelector(apiRef, activeChartId);
    const rowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    if (targetSection) {
      if (isBlockedForSection(columns[field], targetSection, rowGroupingModel, pivotActive ? pivotModel : void 0)) {
        return;
      }
      const currentTargetItems = targetSection === "dimensions" ? dimensions : values3;
      const currentMaxItems = targetSection === "dimensions" ? (_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.maxDimensions : (_b2 = chartStateLookup[activeChartId]) == null ? void 0 : _b2.maxValues;
      if (currentMaxItems && currentTargetItems.length >= currentMaxItems) {
        return;
      }
    }
    let hidden;
    if (originSection) {
      const method = originSection === "dimensions" ? updateChartDimensionsData : updateChartValuesData;
      const currentItems = originSection === "dimensions" ? [...dimensions] : [...values3];
      const fieldIndex = currentItems.findIndex((item) => item.field === field);
      if (fieldIndex !== -1) {
        hidden = currentItems[fieldIndex].hidden;
      }
      if (targetSection !== originSection) {
        currentItems.splice(fieldIndex, 1);
        method(activeChartId, currentItems);
      }
    }
    if (targetSection) {
      const method = targetSection === "dimensions" ? updateChartDimensionsData : updateChartValuesData;
      const currentItems = targetSection === "dimensions" ? dimensions : values3;
      const remainingItems = targetSection === originSection ? currentItems.filter((item) => item.field !== field) : [...currentItems];
      if (rowGroupingModel.length > 0 && targetSection === "values") {
        const hasAggregation = Object.keys(aggregationModel).includes(field);
        if (!hasAggregation) {
          apiRef.current.setAggregationModel(_extends({}, aggregationModel, {
            [field]: getAvailableAggregationFunctions({
              aggregationFunctions: props.aggregationFunctions,
              colDef: columns[field],
              isDataSource: !!props.dataSource
            })[0]
          }));
        }
      }
      if (targetField) {
        const targetFieldIndex = remainingItems.findIndex((item) => item.field === targetField);
        const targetIndex = placementRelativeToTargetField === "top" ? targetFieldIndex : targetFieldIndex + 1;
        remainingItems.splice(targetIndex, 0, {
          field,
          hidden
        });
        method(activeChartId, remainingItems);
      } else {
        method(activeChartId, [...remainingItems, {
          field,
          hidden
        }]);
      }
    }
  }, [apiRef, props.aggregationFunctions, props.dataSource, activeChartId, chartStateLookup, updateChartDimensionsData, updateChartValuesData, aggregationModel, pivotActive, pivotModel]);
  const addColumnMenuButton = React345.useCallback((menuItems) => {
    if (isChartsIntegrationAvailable) {
      return [...menuItems, "columnMenuManagePanelItem"];
    }
    return menuItems;
  }, [isChartsIntegrationAvailable]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuButton);
  const addChartsPanel = React345.useCallback((initialValue, value) => {
    var _a2;
    if (props.slots.chartsPanel && isChartsIntegrationAvailable && value === GridSidebarValue.Charts) {
      return (0, import_jsx_runtime252.jsx)(props.slots.chartsPanel, _extends({}, (_a2 = props.slotProps) == null ? void 0 : _a2.chartsPanel));
    }
    return initialValue;
  }, [props, isChartsIntegrationAvailable]);
  useGridRegisterPipeProcessor(apiRef, "sidebar", addChartsPanel);
  useGridApiMethod(apiRef, {
    chartsIntegration: {
      updateDataReference,
      getColumnName
    }
  }, "private");
  useGridApiMethod(apiRef, ((_f = props.experimentalFeatures) == null ? void 0 : _f.charts) ? {
    setChartsPanelOpen,
    setActiveChartId,
    setChartType,
    setChartSynchronizationState,
    updateChartDimensionsData,
    updateChartValuesData
  } : {}, "public");
  useGridEvent(apiRef, "columnsChange", runIf(isChartsIntegrationAvailable, () => debouncedHandleColumnDataUpdate(syncedChartIds)));
  useGridEvent(apiRef, "pivotModeChange", runIf(isChartsIntegrationAvailable, () => debouncedHandleColumnDataUpdate(syncedChartIds)));
  useGridEvent(apiRef, "filteredRowsSet", runIf(isChartsIntegrationAvailable, () => debouncedHandleRowDataUpdate(syncedChartIds)));
  useGridEvent(apiRef, "sortedRowsSet", runIf(isChartsIntegrationAvailable, () => debouncedHandleRowDataUpdate(syncedChartIds)));
  useGridEvent(apiRef, "aggregationLookupSet", runIf(isChartsIntegrationAvailable, () => debouncedHandleRowDataUpdate(syncedChartIds)));
  const stateExportPreProcessing = React345.useCallback((prevState, exportContext) => {
    var _a2, _b2;
    if (!props.chartsIntegration || !((_a2 = props.experimentalFeatures) == null ? void 0 : _a2.charts)) {
      return prevState;
    }
    const currentActiveChartId = gridChartsIntegrationActiveChartIdSelector(apiRef);
    const chartsLookup = gridChartsIntegrationChartsLookupSelector(apiRef);
    const integrationContextToExport = Object.fromEntries(Object.entries(chartStateLookup).map(([chartId, chartState]) => [
      chartId,
      // keep only the state that is controlled by the user, drop the data and labels
      {
        synced: chartState.synced,
        type: chartState.type,
        configuration: chartState.configuration
      }
    ]));
    const shouldExportChartState = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !exportContext.exportOnlyDirtyModels || // Always export if the chart state has been initialized
      ((_b2 = props.initialState) == null ? void 0 : _b2.chartsIntegration) != null || // Export if the chart model or context is not empty
      Object.keys(chartsLookup).length > 0 || Object.keys(integrationContextToExport).length > 0
    );
    if (!shouldExportChartState) {
      return prevState;
    }
    const chartStateToExport = {
      activeChartId: currentActiveChartId,
      charts: chartsLookup,
      // add a custom prop to keep the integration context in the exported state
      integrationContext: integrationContextToExport
    };
    return _extends({}, prevState, {
      chartsIntegration: chartStateToExport
    });
  }, [apiRef, chartStateLookup, props.chartsIntegration, (_g = props.experimentalFeatures) == null ? void 0 : _g.charts, (_h = props.initialState) == null ? void 0 : _h.chartsIntegration]);
  const stateRestorePreProcessing = React345.useCallback((params, restoreContext) => {
    const chartsRestoreState = restoreContext.stateToRestore.chartsIntegration;
    if (!chartsRestoreState) {
      return params;
    }
    const {
      activeChartId: activeChartIdToRestore,
      charts: chartsToRestore,
      integrationContext
    } = chartsRestoreState;
    if (activeChartIdToRestore === void 0 || chartsToRestore === void 0 || Object.keys(chartsToRestore).length === 0) {
      return params;
    }
    apiRef.current.setState(_extends({}, apiRef.current.state, {
      chartsIntegration: {
        activeChartId: activeChartIdToRestore,
        charts: chartsToRestore
      }
    }));
    Object.entries(integrationContext).forEach(([chartId, chartContextState]) => {
      setChartState(chartId, chartContextState);
    });
    return params;
  }, [apiRef, setChartState]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  React345.useEffect(() => {
    if (!activeChartId && availableChartIds.length > 0) {
      setActiveChartId(availableChartIds[0]);
    }
  }, [availableChartIds, activeChartId, setActiveChartId]);
  const isInitialized = React345.useRef(false);
  React345.useEffect(() => {
    if (isInitialized.current) {
      return;
    }
    if (availableChartIds.length === 0) {
      return;
    }
    isInitialized.current = true;
    availableChartIds.forEach((chartId) => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2, _h2, _i2, _j2, _k, _l;
      const chartType = ((_d2 = (_c2 = (_b2 = (_a2 = props.initialState) == null ? void 0 : _a2.chartsIntegration) == null ? void 0 : _b2.charts) == null ? void 0 : _c2[chartId]) == null ? void 0 : _d2.chartType) || "";
      setChartState(chartId, {
        type: chartType,
        maxDimensions: (_e2 = schema[chartType]) == null ? void 0 : _e2.maxDimensions,
        maxValues: (_f2 = schema[chartType]) == null ? void 0 : _f2.maxValues,
        dimensionsLabel: (_g2 = schema[chartType]) == null ? void 0 : _g2.dimensionsLabel,
        valuesLabel: (_h2 = schema[chartType]) == null ? void 0 : _h2.valuesLabel,
        configuration: ((_l = (_k = (_j2 = (_i2 = props.initialState) == null ? void 0 : _i2.chartsIntegration) == null ? void 0 : _j2.charts) == null ? void 0 : _k[chartId]) == null ? void 0 : _l.configuration) || {}
      });
    });
    debouncedHandleColumnDataUpdate(syncedChartIds);
  }, [schema, availableChartIds, syncedChartIds, (_j = (_i = props.initialState) == null ? void 0 : _i.chartsIntegration) == null ? void 0 : _j.charts, setChartState, debouncedHandleColumnDataUpdate]);
};

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/customize/GridChartsPanelCustomize.js
var import_jsx_runtime253 = __toESM(require_jsx_runtime(), 1);
var GridChartsPanelCustomizeRoot = styled_default2(GridShadowScrollArea)({
  height: "100%"
});
var GridChartsPanelCustomizeSection = styled_default2(Collapsible, {
  name: "MuiDataGrid",
  slot: "ChartsPanelCustomizeSection"
})({
  margin: vars.spacing(0.5, 1)
});
var GridChartsPanelCustomizePanel = styled_default2(CollapsiblePanel, {
  name: "MuiDataGrid",
  slot: "chartsPanelSection"
})({
  display: "flex",
  flexDirection: "column",
  padding: vars.spacing(2, 1.5),
  gap: vars.spacing(3)
});
var GridChartsPanelCustomizePanelTitle = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelCustomizePanelTitle"
})({
  font: vars.typography.font.body,
  fontWeight: vars.typography.fontWeight.medium
});
function GridChartsPanelCustomize(props) {
  const {
    activeChartId,
    sections
  } = props;
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const {
    chartStateLookup,
    setChartState
  } = useGridChartsIntegrationContext();
  const {
    type: chartType,
    configuration: configuration4,
    dimensions,
    values: values3
  } = chartStateLookup[activeChartId] ?? EMPTY_CHART_INTEGRATION_CONTEXT_STATE;
  const handleChange = (field, value) => {
    setChartState(activeChartId, _extends({}, configuration4, {
      configuration: _extends({}, configuration4, {
        [field]: value
      })
    }));
  };
  if (chartType === "") {
    return (0, import_jsx_runtime253.jsx)(GridOverlay, {
      children: apiRef.current.getLocaleText("chartsChartNotSelected")
    });
  }
  return (0, import_jsx_runtime253.jsx)(GridChartsPanelCustomizeRoot, {
    children: sections.map((section, index) => (0, import_jsx_runtime253.jsxs)(GridChartsPanelCustomizeSection, {
      initiallyOpen: index === 0,
      ownerState: rootProps,
      children: [(0, import_jsx_runtime253.jsx)(CollapsibleTrigger, {
        children: (0, import_jsx_runtime253.jsx)(GridChartsPanelCustomizePanelTitle, {
          ownerState: rootProps,
          children: section.label
        })
      }), (0, import_jsx_runtime253.jsx)(GridChartsPanelCustomizePanel, {
        ownerState: rootProps,
        children: Object.entries(section.controls).map(([key, optRaw]) => {
          var _a, _b, _c, _d, _e;
          const opt = optRaw;
          const context = {
            configuration: configuration4,
            dimensions,
            values: values3
          };
          const isHidden = ((_a = opt.isHidden) == null ? void 0 : _a.call(opt, context)) ?? false;
          if (isHidden) {
            return null;
          }
          const isDisabled = ((_b = opt.isDisabled) == null ? void 0 : _b.call(opt, context)) ?? false;
          if (opt.type === "boolean") {
            return (0, import_jsx_runtime253.jsx)(rootProps.slots.baseSwitch, _extends({
              checked: Boolean(configuration4[key] ?? opt.default),
              onChange: (event) => handleChange(key, event.target.checked),
              size: "small",
              label: opt.label,
              disabled: isDisabled
            }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseSwitch), key);
          }
          if (opt.type === "select") {
            return (0, import_jsx_runtime253.jsx)(rootProps.slots.baseSelect, _extends({
              fullWidth: true,
              size: "small",
              label: opt.label,
              value: configuration4[key] ?? opt.default,
              onChange: (event) => handleChange(key, event.target.value),
              disabled: isDisabled,
              slotProps: {
                htmlInput: _extends({}, opt.htmlAttributes)
              }
            }, (_d = rootProps.slotProps) == null ? void 0 : _d.baseSelect, {
              children: (opt.options || []).map((option) => (0, import_jsx_runtime253.jsx)(rootProps.slots.baseSelectOption, {
                value: option.value,
                native: false,
                children: option.content
              }, option.value))
            }), key);
          }
          return (0, import_jsx_runtime253.jsx)(rootProps.slots.baseTextField, _extends({
            "aria-label": opt.label,
            placeholder: opt.label,
            label: opt.label,
            type: opt.type === "number" ? "number" : "text",
            size: "small",
            fullWidth: true,
            disabled: isDisabled,
            slotProps: {
              htmlInput: _extends({}, opt.htmlAttributes)
            }
          }, (_e = rootProps.slotProps) == null ? void 0 : _e.baseTextField, {
            value: (configuration4[key] ?? opt.default ?? "").toString(),
            onChange: (event) => handleChange(key, opt.type === "number" ? Number(event.target.value) : event.target.value)
          }), key);
        })
      })]
    }, section.id))
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelData.js
var React352 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataBody.js
var React349 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataField.js
var React348 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataFieldMenu.js
var React347 = __toESM(require_react(), 1);
var import_jsx_runtime254 = __toESM(require_jsx_runtime(), 1);
function GridChartsPanelDataFieldMenu(props) {
  var _a, _b;
  const {
    field,
    section,
    blockedSections,
    dimensionsLabel,
    valuesLabel
  } = props;
  const rootProps = useGridRootProps3();
  const [open, setOpen] = React347.useState(false);
  const apiRef = useGridPrivateApiContext3();
  const activeChartId = useGridSelector(apiRef, gridChartsIntegrationActiveChartIdSelector);
  const dimensions = useGridSelector(apiRef, gridChartsDimensionsSelector, activeChartId);
  const values3 = useGridSelector(apiRef, gridChartsValuesSelector, activeChartId);
  const isAvailableField = section === null;
  const fieldIndexInModel = !isAvailableField ? (section === "dimensions" ? dimensions : values3).findIndex((item) => item.field === field) : -1;
  const modelLength = !isAvailableField ? (section === "dimensions" ? dimensions : values3).length : 0;
  const canMoveUp = fieldIndexInModel > 0;
  const canMoveDown = !isAvailableField && fieldIndexInModel < modelLength - 1;
  const menuId = useId();
  const triggerId = useId();
  const triggerRef = React347.useRef(null);
  const menuItems = React347.useMemo(() => {
    if (isAvailableField) {
      return [{
        key: "dimensions",
        label: apiRef.current.getLocaleText("chartsMenuAddToDimensions")(dimensionsLabel)
      }, {
        key: "values",
        label: apiRef.current.getLocaleText("chartsMenuAddToValues")(valuesLabel)
      }].filter((item) => !(blockedSections == null ? void 0 : blockedSections.includes(item.key)));
    }
    const moveMenuItems = [{
      key: "up",
      label: apiRef.current.getLocaleText("chartsMenuMoveUp"),
      icon: (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuMoveUpIcon, {}),
      disabled: !canMoveUp
    }, {
      key: "down",
      label: apiRef.current.getLocaleText("chartsMenuMoveDown"),
      icon: (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuMoveDownIcon, {}),
      disabled: !canMoveDown
    }, {
      divider: true
    }, {
      key: "top",
      label: apiRef.current.getLocaleText("chartsMenuMoveToTop"),
      icon: (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuMoveToTopIcon, {}),
      disabled: !canMoveUp
    }, {
      key: "bottom",
      label: apiRef.current.getLocaleText("chartsMenuMoveToBottom"),
      icon: (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuMoveToBottomIcon, {}),
      disabled: !canMoveDown
    }, {
      divider: true
    }];
    const removeMenuItem = [{
      key: null,
      label: apiRef.current.getLocaleText("chartsMenuRemove"),
      icon: (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuRemoveIcon, {})
    }];
    const addToSectionMenuItems = [{
      key: "dimensions",
      label: apiRef.current.getLocaleText("chartsMenuAddToDimensions")(dimensionsLabel),
      icon: (0, import_jsx_runtime254.jsx)("span", {})
    }, {
      key: "values",
      label: apiRef.current.getLocaleText("chartsMenuAddToValues")(valuesLabel),
      icon: (0, import_jsx_runtime254.jsx)("span", {})
    }].filter((item) => item.key !== section && !(blockedSections == null ? void 0 : blockedSections.includes(item.key)));
    if (addToSectionMenuItems.length > 0) {
      addToSectionMenuItems.push({
        divider: true
      });
    }
    return [...moveMenuItems, ...addToSectionMenuItems, ...removeMenuItem];
  }, [isAvailableField, apiRef, rootProps, canMoveUp, canMoveDown, section, blockedSections, dimensionsLabel, valuesLabel]);
  if (menuItems.length === 0) {
    return null;
  }
  const handleClick = () => {
    setOpen(!open);
  };
  const handleClose = () => {
    setOpen(false);
  };
  const handleMove = (to) => {
    handleClose();
    if (to === section) {
      return;
    }
    const items = section === "dimensions" ? dimensions : values3;
    let targetField;
    let targetFieldPosition = null;
    let targetSection = section;
    switch (to) {
      case "up":
        targetField = items[fieldIndexInModel - 1].field;
        targetFieldPosition = "top";
        break;
      case "down":
        targetField = items[fieldIndexInModel + 1].field;
        targetFieldPosition = "bottom";
        break;
      case "top":
        targetField = items[0].field;
        targetFieldPosition = "top";
        break;
      case "bottom":
        targetField = items[modelLength - 1].field;
        targetFieldPosition = "bottom";
        break;
      case "dimensions":
      case "values":
      case null:
        targetSection = to;
        break;
      default:
        break;
    }
    apiRef.current.chartsIntegration.updateDataReference(field, section, targetSection, targetField, targetFieldPosition || void 0);
  };
  return (0, import_jsx_runtime254.jsxs)(React347.Fragment, {
    children: [(0, import_jsx_runtime254.jsx)(rootProps.slots.baseIconButton, _extends({
      size: "small"
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseIconButton, {
      id: triggerId,
      "aria-haspopup": "true",
      "aria-controls": open ? menuId : void 0,
      "aria-expanded": open ? "true" : void 0,
      "aria-label": apiRef.current.getLocaleText("chartsMenuOptions"),
      onClick: handleClick,
      ref: triggerRef,
      children: isAvailableField ? (0, import_jsx_runtime254.jsx)(rootProps.slots.chartsMenuAddIcon, {
        fontSize: "small"
      }) : (0, import_jsx_runtime254.jsx)(rootProps.slots.columnMenuIcon, {
        fontSize: "small"
      })
    })), (0, import_jsx_runtime254.jsx)(GridMenu, {
      target: triggerRef.current,
      open,
      onClose: handleClose,
      position: "bottom-start",
      children: (0, import_jsx_runtime254.jsx)(rootProps.slots.baseMenuList, _extends({
        id: menuId,
        "aria-labelledby": triggerId,
        autoFocusItem: true
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseMenuList, {
        children: menuItems.map((item, index) => {
          var _a2;
          return "divider" in item ? (0, import_jsx_runtime254.jsx)(rootProps.slots.baseDivider, {}, `divider-${index}`) : (0, import_jsx_runtime254.jsx)(rootProps.slots.baseMenuItem, _extends({
            disabled: item.disabled,
            onClick: () => handleMove(item.key),
            iconStart: item.icon
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem, {
            children: item.label
          }), item.key);
        })
      }))
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aggregation/gridAggregationFunctions.js
var sumAgg = {
  apply: ({
    values: values3
  }) => {
    let sum = 0;
    for (let i2 = 0; i2 < values3.length; i2 += 1) {
      const value = values3[i2];
      if (typeof value === "number" && !Number.isNaN(value)) {
        sum += value;
      }
    }
    return sum;
  },
  columnTypes: ["number"]
};
var avgAgg = {
  apply: ({
    values: values3
  }) => {
    if (values3.length === 0) {
      return null;
    }
    let sum = 0;
    let valuesCount = 0;
    for (let i2 = 0; i2 < values3.length; i2 += 1) {
      const value = values3[i2];
      if (typeof value === "number" && !Number.isNaN(value)) {
        valuesCount += 1;
        sum += value;
      }
    }
    if (sum === 0) {
      return null;
    }
    return sum / valuesCount;
  },
  columnTypes: ["number"]
};
var minAgg = {
  apply: ({
    values: values3
  }) => {
    if (values3.length === 0) {
      return null;
    }
    let hasValidValue = false;
    let min2 = Infinity;
    for (let i2 = 0; i2 < values3.length; i2 += 1) {
      const value = values3[i2];
      if (value != null && value < min2) {
        min2 = value;
        hasValidValue = true;
      }
    }
    if (!hasValidValue) {
      return null;
    }
    return min2;
  },
  columnTypes: ["number", "date", "dateTime"]
};
var maxAgg = {
  apply: ({
    values: values3
  }) => {
    if (values3.length === 0) {
      return null;
    }
    let hasValidValue = false;
    let max2 = -Infinity;
    for (let i2 = 0; i2 < values3.length; i2 += 1) {
      const value = values3[i2];
      if (value != null && value > max2) {
        max2 = value;
        hasValidValue = true;
      }
    }
    if (!hasValidValue) {
      return null;
    }
    return max2;
  },
  columnTypes: ["number", "date", "dateTime"]
};
var sizeAgg = {
  apply: ({
    values: values3
  }) => {
    return values3.filter((value) => typeof value !== "undefined").length;
  },
  valueFormatter: (value) => {
    if (value == null || !isNumber(value)) {
      return value;
    }
    return value.toLocaleString();
  },
  hasCellUnit: false
};
var sizeTrueAgg = {
  apply: ({
    values: values3
  }) => {
    return values3.filter((value) => value === true).length;
  },
  columnTypes: ["boolean"],
  label: "size(true)",
  valueFormatter: (value) => {
    if (value == null || !isNumber(value)) {
      return value;
    }
    return value.toLocaleString();
  },
  hasCellUnit: false
};
var sizeFalseAgg = {
  apply: ({
    values: values3
  }) => {
    return values3.filter((value) => value === false).length;
  },
  columnTypes: ["boolean"],
  label: "size(false)",
  valueFormatter: (value) => {
    if (value == null || !isNumber(value)) {
      return value;
    }
    return value.toLocaleString();
  },
  hasCellUnit: false
};
var GRID_AGGREGATION_FUNCTIONS = {
  sum: sumAgg,
  avg: avgAgg,
  min: minAgg,
  max: maxAgg,
  size: sizeAgg,
  sizeTrue: sizeTrueAgg,
  sizeFalse: sizeFalseAgg
};

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataField.js
var import_jsx_runtime255 = __toESM(require_jsx_runtime(), 1);
var AGGREGATION_FUNCTION_NONE = "none";
var useUtilityClasses127 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["chartsPanelDataField"],
    name: ["chartsPanelDataFieldName"],
    actionContainer: ["chartsPanelDataFieldActionContainer"],
    dragIcon: ["chartsPanelDataFieldDragIcon"],
    checkbox: ["chartsPanelDataFieldCheckbox"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridChartsPanelDataFieldRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataField"
})(({
  disabled
}) => ({
  flexShrink: 0,
  position: "relative",
  padding: vars.spacing(0, 1, 0, 2),
  height: 32,
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(0.5),
  borderWidth: 0,
  borderTopWidth: 2,
  borderBottomWidth: 2,
  borderStyle: "solid",
  borderColor: "transparent",
  margin: "-1px 0",
  // collapse vertical borders
  cursor: disabled ? "not-allowed" : "grab",
  opacity: disabled ? 0.5 : 1,
  variants: [{
    props: {
      dropPosition: "top"
    },
    style: {
      borderTopColor: vars.colors.interactive.selected
    }
  }, {
    props: {
      dropPosition: "bottom"
    },
    style: {
      borderBottomColor: vars.colors.interactive.selected
    }
  }, {
    props: {
      section: null
    },
    style: {
      borderTopColor: "transparent",
      borderBottomColor: "transparent"
    }
  }],
  "&:hover": {
    backgroundColor: vars.colors.interactive.hover
  }
}));
var GridChartsPanelDataFieldName = styled_default2("span", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataFieldName"
})({
  flex: 1,
  overflow: "hidden",
  textOverflow: "ellipsis",
  whiteSpace: "nowrap"
});
var GridChartsPanelDataFieldActionContainer = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataFieldActionContainer"
})({
  display: "flex",
  alignItems: "center"
});
var GridChartsPanelDataFieldDragIcon = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataFieldDragIcon"
})({
  position: "absolute",
  left: -1,
  width: 16,
  display: "flex",
  justifyContent: "center",
  color: vars.colors.foreground.base,
  opacity: 0,
  '[draggable="true"]:hover > &': {
    opacity: 0.3
  }
});
var GridChartsPanelDataFieldCheckbox = styled_default2(NotRendered, {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataFieldCheckbox"
})({
  flex: 1,
  position: "relative",
  margin: vars.spacing(0, 0, 0, -1),
  cursor: "grab"
});
function AggregationSelect2({
  aggFunc,
  field
}) {
  var _a;
  const rootProps = useGridRootProps3();
  const [aggregationMenuOpen, setAggregationMenuOpen] = React348.useState(false);
  const aggregationMenuTriggerRef = React348.useRef(null);
  const aggregationMenuTriggerId = useId();
  const aggregationMenuId = useId();
  const apiRef = useGridApiContext3();
  const aggregationModel = gridAggregationModelSelector(apiRef);
  const pivotActive = gridPivotActiveSelector(apiRef);
  const getActualFieldName = React348.useCallback((fieldName) => pivotActive ? fieldName.split(COLUMN_GROUP_ID_SEPARATOR).slice(-1)[0] : fieldName, [pivotActive]);
  const colDef = React348.useCallback((fieldName) => apiRef.current.getColumn(getActualFieldName(fieldName)), [apiRef, getActualFieldName]);
  const availableAggregationFunctions = React348.useMemo(() => [...pivotActive ? [] : [AGGREGATION_FUNCTION_NONE], ...getAvailableAggregationFunctions({
    aggregationFunctions: rootProps.aggregationFunctions,
    colDef: colDef(field),
    isDataSource: !!rootProps.dataSource
  })], [colDef, field, pivotActive, rootProps.aggregationFunctions, rootProps.dataSource]);
  const handleClick = React348.useCallback((func) => {
    if (pivotActive) {
      const fieldName = getActualFieldName(field);
      apiRef.current.setPivotModel((prev) => _extends({}, prev, {
        values: prev.values.map((col) => {
          if (col.field === fieldName) {
            return _extends({}, col, {
              aggFunc: func
            });
          }
          return col;
        })
      }));
    } else if (func === AGGREGATION_FUNCTION_NONE) {
      const updatedAggregationModel = _extends({}, aggregationModel);
      delete updatedAggregationModel[field];
      apiRef.current.setAggregationModel(updatedAggregationModel);
    } else {
      apiRef.current.setAggregationModel(_extends({}, aggregationModel, {
        [field]: func
      }));
    }
    setAggregationMenuOpen(false);
  }, [apiRef, field, getActualFieldName, pivotActive, aggregationModel, setAggregationMenuOpen]);
  return availableAggregationFunctions.length > 0 ? (0, import_jsx_runtime255.jsxs)(React348.Fragment, {
    children: [(0, import_jsx_runtime255.jsx)(rootProps.slots.baseChip, {
      label: getAggregationFunctionLabel({
        apiRef,
        aggregationRule: {
          aggregationFunctionName: aggFunc,
          aggregationFunction: rootProps.aggregationFunctions[aggFunc] || {}
        }
      }),
      size: "small",
      variant: "outlined",
      ref: aggregationMenuTriggerRef,
      id: aggregationMenuTriggerId,
      "aria-haspopup": "true",
      "aria-controls": aggregationMenuOpen ? aggregationMenuId : void 0,
      "aria-expanded": aggregationMenuOpen ? "true" : void 0,
      onClick: () => setAggregationMenuOpen(!aggregationMenuOpen)
    }), (0, import_jsx_runtime255.jsx)(GridMenu, {
      open: aggregationMenuOpen,
      onClose: () => setAggregationMenuOpen(false),
      target: aggregationMenuTriggerRef.current,
      position: "bottom-start",
      children: (0, import_jsx_runtime255.jsx)(rootProps.slots.baseMenuList, _extends({
        id: aggregationMenuId,
        "aria-labelledby": aggregationMenuTriggerId,
        autoFocusItem: true
      }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseMenuList, {
        children: availableAggregationFunctions.map((func) => {
          var _a2;
          return (0, import_jsx_runtime255.jsx)(rootProps.slots.baseMenuItem, _extends({
            selected: aggFunc === func,
            onClick: () => handleClick(func)
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem, {
            children: getAggregationFunctionLabel({
              apiRef,
              aggregationRule: {
                aggregationFunctionName: func,
                aggregationFunction: rootProps.aggregationFunctions[func] || {}
              }
            })
          }), func);
        })
      }))
    })]
  }) : null;
}
function GridChartsPanelDataField(props) {
  var _a, _b;
  const {
    children: children2,
    field,
    section,
    blockedSections,
    dimensionsLabel,
    valuesLabel,
    selected,
    disabled,
    onChange,
    onDragStart,
    onDragEnd
  } = props;
  const rootProps = useGridRootProps3();
  const [dropPosition, setDropPosition] = React348.useState(null);
  const ownerState = _extends({}, props, {
    classes: rootProps.classes,
    dropPosition,
    section
  });
  const classes2 = useUtilityClasses127(ownerState);
  const apiRef = useGridPrivateApiContext3();
  const aggregationModel = useGridSelector(apiRef, gridAggregationModelSelector);
  const rowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  const isRowGroupingEnabled = React348.useMemo(() => rowGroupingModel.length > 0, [rowGroupingModel]);
  const handleDragStart = React348.useCallback((event) => {
    const data = {
      field,
      section
    };
    event.dataTransfer.setData("text/plain", JSON.stringify(data));
    event.dataTransfer.dropEffect = "move";
    onDragStart(field, section);
  }, [field, onDragStart, section]);
  const getDropPosition = React348.useCallback((event) => {
    const rect = event.target.getBoundingClientRect();
    const y = event.clientY - rect.top;
    if (y < rect.height / 2) {
      return "top";
    }
    return "bottom";
  }, []);
  const handleDragOver = React348.useCallback((event) => {
    if (disabled) {
      return;
    }
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDropPosition(getDropPosition(event));
    }
  }, [disabled, getDropPosition]);
  const handleDragLeave = React348.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDropPosition(null);
    }
  }, []);
  const handleDrop = React348.useCallback((event) => {
    setDropPosition(null);
    if (!event.currentTarget.contains(event.relatedTarget)) {
      event.preventDefault();
      const position = getDropPosition(event);
      const {
        field: droppedField,
        section: originSection
      } = JSON.parse(event.dataTransfer.getData("text/plain"));
      apiRef.current.chartsIntegration.updateDataReference(droppedField, originSection, section, field, position || void 0);
    }
  }, [getDropPosition, apiRef, field, section]);
  const hideable = section !== null;
  return (0, import_jsx_runtime255.jsx)(rootProps.slots.baseTooltip, _extends({
    title: disabled ? apiRef.current.getLocaleText("chartsFieldBlocked") : void 0,
    enterDelay: 1e3
  }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTooltip, {
    children: (0, import_jsx_runtime255.jsxs)(GridChartsPanelDataFieldRoot, {
      ownerState,
      className: classes2.root,
      onDragOver: handleDragOver,
      onDragLeave: handleDragLeave,
      onDrop: handleDrop,
      onDragStart: handleDragStart,
      onDragEnd,
      draggable: !disabled,
      disabled: !!disabled,
      children: [(0, import_jsx_runtime255.jsx)(GridChartsPanelDataFieldDragIcon, {
        ownerState,
        className: classes2.dragIcon,
        children: (0, import_jsx_runtime255.jsx)(rootProps.slots.columnReorderIcon, {
          fontSize: "small"
        })
      }), hideable ? (0, import_jsx_runtime255.jsx)(GridChartsPanelDataFieldCheckbox, _extends({
        ownerState,
        className: classes2.checkbox,
        as: rootProps.slots.baseCheckbox,
        size: "small",
        density: "compact"
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseCheckbox, {
        checked: selected || false,
        onChange: () => onChange && onChange(field, section),
        label: children2
      })) : (0, import_jsx_runtime255.jsx)(GridChartsPanelDataFieldName, {
        ownerState,
        className: classes2.name,
        children: children2
      }), (0, import_jsx_runtime255.jsxs)(GridChartsPanelDataFieldActionContainer, {
        ownerState,
        className: classes2.actionContainer,
        children: [isRowGroupingEnabled && section === "values" && (0, import_jsx_runtime255.jsx)(AggregationSelect2, {
          aggFunc: aggregationModel[field] ?? AGGREGATION_FUNCTION_NONE,
          field
        }), (0, import_jsx_runtime255.jsx)(GridChartsPanelDataFieldMenu, {
          field,
          section,
          blockedSections,
          dimensionsLabel,
          valuesLabel
        })]
      })]
    })
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataBody.js
var import_jsx_runtime256 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses128 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["chartsPanelDataBody"],
    availableFields: ["chartsPanelDataAvailableFields"],
    sections: ["chartsPanelDataSections"],
    scrollArea: ["chartsPanelDataScrollArea"],
    section: ["chartsPanelDataSection"],
    sectionTitle: ["chartsPanelDataSectionTitle"],
    fieldList: ["chartsPanelDataFieldList"],
    placeholder: ["chartsPanelDataPlaceholder"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridChartsPanelDataBodyRoot = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataBody"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  overflow: "hidden"
});
var GridChartsPanelDataAvailableFields = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataAvailableFields"
})({
  flex: 1,
  minHeight: 84,
  transition: vars.transition(["background-color"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  }),
  '&[data-drag-over="true"]': {
    backgroundColor: vars.colors.interactive.hover
  }
});
var GridChartsPanelDataSections = styled_default2(ResizablePanel, {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataSections"
})({
  position: "relative",
  minHeight: 158,
  overflow: "hidden",
  display: "flex",
  flexDirection: "column"
});
var GridChartsPanelDataScrollArea = styled_default2(GridShadowScrollArea, {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataScrollArea"
})({
  height: "100%"
});
var GridChartsPanelDataSection = styled_default2(Collapsible, {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataSection",
  shouldForwardProp: (prop) => prop !== "disabled"
})(({
  disabled
}) => ({
  opacity: disabled ? 0.5 : 1,
  margin: vars.spacing(0.5, 1),
  transition: vars.transition(["border-color", "background-color"], {
    duration: vars.transitions.duration.short,
    easing: vars.transitions.easing.easeInOut
  }),
  '&[data-drag-over="true"]': {
    backgroundColor: vars.colors.interactive.hover,
    outline: `2px solid ${vars.colors.interactive.selected}`
  }
}));
var GridChartsPanelDataSectionTitle = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataSectionTitle"
})({
  flex: 1,
  marginRight: vars.spacing(1.75),
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  gap: vars.spacing(1),
  font: vars.typography.font.body,
  fontWeight: vars.typography.fontWeight.medium
});
var GridChartsPanelDataFieldList = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataFieldList"
})({
  flex: 1,
  display: "flex",
  flexDirection: "column",
  padding: vars.spacing(0.5, 0)
});
var GridChartsPanelDataPlaceholder = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataPlaceholder"
})({
  flex: 1,
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  textWrap: "balance",
  textAlign: "center",
  minHeight: 38,
  height: "100%",
  padding: vars.spacing(0, 1),
  color: vars.colors.foreground.muted,
  font: vars.typography.font.body
});
var INITIAL_DRAG_STATE2 = {
  active: false,
  field: null,
  dropSection: null,
  initialSection: null
};
var SECTION_COUNT = 2;
function GridChartsPanelDataBody(props) {
  var _a, _b, _c, _d, _e, _f;
  const {
    searchValue
  } = props;
  const apiRef = useGridPrivateApiContext3();
  const rootProps = useGridRootProps3();
  const rowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  const pivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const pivotModel = useGridSelector(apiRef, gridPivotModelSelector);
  const activeChartId = useGridSelector(apiRef, gridChartsIntegrationActiveChartIdSelector);
  const {
    chartStateLookup
  } = useGridChartsIntegrationContext();
  const dimensions = useGridSelector(apiRef, gridChartsDimensionsSelector, activeChartId);
  const values3 = useGridSelector(apiRef, gridChartsValuesSelector, activeChartId);
  const classes2 = useUtilityClasses128(rootProps);
  const chartableColumns = useGridSelector(apiRef, gridChartableColumnsSelector);
  const dimensionsLabel = React349.useMemo(() => {
    var _a2;
    return ((_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.dimensionsLabel) || apiRef.current.getLocaleText("chartsCategories");
  }, [chartStateLookup, activeChartId, apiRef]);
  const valuesLabel = React349.useMemo(() => {
    var _a2;
    return ((_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.valuesLabel) || apiRef.current.getLocaleText("chartsSeries");
  }, [chartStateLookup, activeChartId, apiRef]);
  const fullSections = React349.useMemo(() => {
    var _a2, _b2, _c2, _d2;
    const sections = [];
    if (((_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.maxDimensions) && dimensions.length >= ((_b2 = chartStateLookup[activeChartId]) == null ? void 0 : _b2.maxDimensions)) {
      sections.push("dimensions");
    }
    if (((_c2 = chartStateLookup[activeChartId]) == null ? void 0 : _c2.maxValues) && values3.length >= ((_d2 = chartStateLookup[activeChartId]) == null ? void 0 : _d2.maxValues)) {
      sections.push("values");
    }
    return sections;
  }, [dimensions, values3, chartStateLookup, activeChartId]);
  const blockedSectionsLookup = React349.useMemo(() => new Map(Object.values(chartableColumns).map((column) => [column.field, Array.from(/* @__PURE__ */ new Set([...getBlockedSections(column, rowGroupingModel, pivotActive ? pivotModel : void 0), ...fullSections]))])), [rowGroupingModel, chartableColumns, pivotActive, pivotModel, fullSections]);
  const availableFields = React349.useMemo(() => {
    const notUsedFields = Object.keys(chartableColumns).filter((field) => !dimensions.some((dimension) => dimension.field === field) && !values3.some((value) => value.field === field));
    if (searchValue) {
      return notUsedFields.filter((field) => {
        const fieldName = apiRef.current.chartsIntegration.getColumnName(field);
        return fieldName.toLowerCase().includes(searchValue.toLowerCase());
      });
    }
    return notUsedFields.sort((a, b) => {
      const aBlockedSections = blockedSectionsLookup.get(a).length;
      const bBlockedSections = blockedSectionsLookup.get(b).length;
      return (aBlockedSections >= SECTION_COUNT ? 1 : 0) - (bBlockedSections >= SECTION_COUNT ? 1 : 0);
    });
  }, [apiRef, searchValue, chartableColumns, dimensions, values3, blockedSectionsLookup]);
  const [drag, setDrag] = React349.useState(INITIAL_DRAG_STATE2);
  const disabledSections = React349.useMemo(() => {
    if (!drag.field) {
      return /* @__PURE__ */ new Set();
    }
    return new Set(blockedSectionsLookup.get(drag.field));
  }, [blockedSectionsLookup, drag.field]);
  const handleDragStart = (field, section) => {
    setDrag({
      active: true,
      field,
      initialSection: section,
      dropSection: null
    });
  };
  const handleDragEnd = () => {
    setDrag(INITIAL_DRAG_STATE2);
  };
  const handleDrop = (event) => {
    setDrag(INITIAL_DRAG_STATE2);
    if (event.defaultPrevented) {
      return;
    }
    event.preventDefault();
    const {
      field,
      section: originSection
    } = JSON.parse(event.dataTransfer.getData("text/plain"));
    const targetSection = event.currentTarget.getAttribute("data-section");
    if (originSection === targetSection) {
      return;
    }
    apiRef.current.chartsIntegration.updateDataReference(field, originSection, targetSection);
  };
  const handleDragOver = React349.useCallback((event) => {
    event.preventDefault();
    event.dataTransfer.dropEffect = "move";
  }, []);
  const handleDragEnter = React349.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      const dropSection = event.currentTarget.getAttribute("data-section");
      setDrag((v) => _extends({}, v, {
        active: true,
        dropSection
      }));
    }
  }, []);
  const handleDragLeave = React349.useCallback((event) => {
    if (!event.currentTarget.contains(event.relatedTarget)) {
      setDrag((v) => _extends({}, v, {
        active: true,
        dropSection: v.initialSection
      }));
    }
  }, []);
  const handleChange = React349.useCallback((field, section) => {
    const apiMethod = section === "dimensions" ? apiRef.current.updateChartDimensionsData : apiRef.current.updateChartValuesData;
    apiMethod(activeChartId, (currentItems) => currentItems.map((item) => item.field === field ? _extends({}, item, {
      hidden: item.hidden !== true
    }) : item));
  }, [apiRef, activeChartId]);
  return (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataBodyRoot, {
    ownerState: rootProps,
    className: classes2.root,
    "data-dragging": drag.active,
    onDragLeave: handleDragLeave,
    children: [(0, import_jsx_runtime256.jsxs)(GridChartsPanelDataAvailableFields, {
      ownerState: rootProps,
      className: classes2.availableFields,
      onDrop: handleDrop,
      onDragEnter: handleDragEnter,
      onDragOver: handleDragOver,
      "data-section": null,
      "data-drag-over": drag.active && drag.dropSection === null,
      children: [availableFields.length === 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataPlaceholder, {
        ownerState: rootProps,
        className: classes2.placeholder,
        children: apiRef.current.getLocaleText("chartsNoFields")
      }), availableFields.length > 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataFieldList, {
        ownerState: rootProps,
        className: classes2.fieldList,
        children: availableFields.map((field) => (0, import_jsx_runtime256.jsx)(GridChartsPanelDataField, {
          field,
          section: null,
          disabled: blockedSectionsLookup.get(field).length >= SECTION_COUNT,
          blockedSections: blockedSectionsLookup.get(field),
          dimensionsLabel,
          valuesLabel,
          onDragStart: handleDragStart,
          onDragEnd: handleDragEnd,
          children: apiRef.current.chartsIntegration.getColumnName(field)
        }, field))
      })]
    }), (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataSections, {
      ownerState: rootProps,
      className: classes2.sections,
      direction: "vertical",
      children: [(0, import_jsx_runtime256.jsx)(ResizablePanelHandle, {}), (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataScrollArea, {
        ownerState: rootProps,
        className: classes2.scrollArea,
        children: [(0, import_jsx_runtime256.jsxs)(GridChartsPanelDataSection, {
          ownerState: rootProps,
          className: classes2.section,
          onDrop: handleDrop,
          onDragEnter: handleDragEnter,
          onDragOver: handleDragOver,
          disabled: disabledSections.has("dimensions"),
          "data-section": "dimensions",
          "data-drag-over": !disabledSections.has("dimensions") && drag.dropSection === "dimensions",
          children: [(0, import_jsx_runtime256.jsx)(CollapsibleTrigger, {
            "aria-label": dimensionsLabel,
            children: (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataSectionTitle, {
              ownerState: rootProps,
              className: classes2.sectionTitle,
              children: [dimensionsLabel, (((_a = chartStateLookup[activeChartId]) == null ? void 0 : _a.maxDimensions) || dimensions.length > 0) && (0, import_jsx_runtime256.jsx)(rootProps.slots.baseBadge, {
                badgeContent: ((_b = chartStateLookup[activeChartId]) == null ? void 0 : _b.maxDimensions) ? `${dimensions.length}/${(_c = chartStateLookup[activeChartId]) == null ? void 0 : _c.maxDimensions}` : dimensions.length
              })]
            })
          }), (0, import_jsx_runtime256.jsxs)(CollapsiblePanel, {
            children: [dimensions.length === 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataPlaceholder, {
              ownerState: rootProps,
              className: classes2.placeholder,
              children: apiRef.current.getLocaleText("chartsDragToDimensions")(dimensionsLabel)
            }), dimensions.length > 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataFieldList, {
              ownerState: rootProps,
              className: classes2.fieldList,
              children: dimensions.map((dimension) => (0, import_jsx_runtime256.jsx)(GridChartsPanelDataField, {
                field: dimension.field,
                selected: dimension.hidden !== true,
                onChange: handleChange,
                section: "dimensions",
                blockedSections: blockedSectionsLookup.get(dimension.field),
                dimensionsLabel,
                valuesLabel,
                disabled: disabledSections.has("dimensions"),
                onDragStart: handleDragStart,
                onDragEnd: handleDragEnd,
                children: apiRef.current.chartsIntegration.getColumnName(dimension.field)
              }, dimension.field))
            })]
          })]
        }), (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataSection, {
          ownerState: rootProps,
          className: classes2.section,
          onDrop: handleDrop,
          onDragEnter: handleDragEnter,
          onDragOver: handleDragOver,
          disabled: disabledSections.has("values"),
          "data-section": "values",
          "data-drag-over": !disabledSections.has("values") && drag.dropSection === "values",
          children: [(0, import_jsx_runtime256.jsx)(CollapsibleTrigger, {
            "aria-label": valuesLabel,
            children: (0, import_jsx_runtime256.jsxs)(GridChartsPanelDataSectionTitle, {
              ownerState: rootProps,
              className: classes2.sectionTitle,
              children: [valuesLabel, (((_d = chartStateLookup[activeChartId]) == null ? void 0 : _d.maxValues) || values3.length > 0) && (0, import_jsx_runtime256.jsx)(rootProps.slots.baseBadge, {
                badgeContent: ((_e = chartStateLookup[activeChartId]) == null ? void 0 : _e.maxValues) ? `${values3.length}/${(_f = chartStateLookup[activeChartId]) == null ? void 0 : _f.maxValues}` : values3.length
              })]
            })
          }), (0, import_jsx_runtime256.jsxs)(CollapsiblePanel, {
            children: [values3.length === 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataPlaceholder, {
              ownerState: rootProps,
              className: classes2.placeholder,
              children: apiRef.current.getLocaleText("chartsDragToValues")(valuesLabel)
            }), values3.length > 0 && (0, import_jsx_runtime256.jsx)(GridChartsPanelDataFieldList, {
              ownerState: rootProps,
              className: classes2.fieldList,
              children: values3.map((value) => (0, import_jsx_runtime256.jsx)(GridChartsPanelDataField, {
                field: value.field,
                selected: value.hidden !== true,
                onChange: handleChange,
                section: "values",
                blockedSections: blockedSectionsLookup.get(value.field),
                dimensionsLabel,
                valuesLabel,
                disabled: disabledSections.has("values"),
                onDragStart: handleDragStart,
                onDragEnd: handleDragEnd,
                children: apiRef.current.chartsIntegration.getColumnName(value.field)
              }, value.field))
            })]
          })]
        })]
      })]
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataHeader.js
var React351 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataSearch.js
var React350 = __toESM(require_react(), 1);
var import_jsx_runtime257 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses129 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    container: ["chartsPanelDataSearchContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridChartsPanelDataSearchContainer = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelDataSearchContainer"
})({
  padding: vars.spacing(1)
});
function GridChartsPanelDataSearch(props) {
  var _a;
  const {
    onClear,
    value,
    onChange
  } = props;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const classes2 = useUtilityClasses129(rootProps);
  const handleKeyDown = (event) => {
    if (event.key === "Escape") {
      onClear();
    }
  };
  return (0, import_jsx_runtime257.jsx)(GridChartsPanelDataSearchContainer, {
    ownerState: rootProps,
    className: classes2.container,
    children: (0, import_jsx_runtime257.jsx)(rootProps.slots.baseTextField, _extends({
      size: "small",
      "aria-label": apiRef.current.getLocaleText("chartsSearchLabel"),
      placeholder: apiRef.current.getLocaleText("chartsSearchPlaceholder"),
      onKeyDown: handleKeyDown,
      fullWidth: true,
      slotProps: {
        input: {
          startAdornment: (0, import_jsx_runtime257.jsx)(rootProps.slots.chartsSearchIcon, {
            fontSize: "small"
          }),
          endAdornment: value ? (0, import_jsx_runtime257.jsx)(rootProps.slots.baseIconButton, {
            edge: "end",
            size: "small",
            onClick: onClear,
            "aria-label": apiRef.current.getLocaleText("chartsSearchClear"),
            children: (0, import_jsx_runtime257.jsx)(rootProps.slots.chartsSearchClearIcon, {
              fontSize: "small"
            })
          }) : null
        },
        htmlInput: {
          role: "searchbox"
        }
      }
    }, (_a = rootProps.slotProps) == null ? void 0 : _a.baseTextField, {
      value,
      onChange
    }))
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelDataHeader.js
var import_jsx_runtime258 = __toESM(require_jsx_runtime(), 1);
function GridChartsPanelDataHeader(props) {
  const {
    searchValue,
    onSearchValueChange
  } = props;
  return (0, import_jsx_runtime258.jsx)(SidebarHeader, {
    children: (0, import_jsx_runtime258.jsx)(GridChartsPanelDataSearch, {
      value: searchValue,
      onClear: () => onSearchValueChange(""),
      onChange: (event) => onSearchValueChange(event.target.value)
    })
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/data/GridChartsPanelData.js
var import_jsx_runtime259 = __toESM(require_jsx_runtime(), 1);
function GridChartsPanelData() {
  const [searchValue, setSearchValue] = React352.useState("");
  return (0, import_jsx_runtime259.jsxs)(React352.Fragment, {
    children: [(0, import_jsx_runtime259.jsx)(GridChartsPanelDataHeader, {
      searchValue,
      onSearchValueChange: setSearchValue
    }), (0, import_jsx_runtime259.jsx)(GridChartsPanelDataBody, {
      searchValue
    })]
  });
}

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/GridChartsPanel.js
var import_jsx_runtime260 = __toESM(require_jsx_runtime(), 1);
var GridChartsPanelHeader = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelHeader"
})({
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(0.25),
  padding: vars.spacing(1, 0.5, 0, 0.75),
  boxSizing: "border-box"
});
var GridChartsPanelTitle = styled_default2("div", {
  name: "MuiDataGrid",
  slot: "ChartsPanelTitle"
})({
  font: vars.typography.font.large,
  fontWeight: vars.typography.fontWeight.medium,
  marginLeft: vars.spacing(0.5),
  marginRight: "auto"
});
var GridChartsPanelChartSelection = styled_default2("button", {
  name: "MuiDataGrid",
  slot: "ChartsPanelChartSelection"
})({
  display: "flex",
  alignItems: "center",
  gap: vars.spacing(0.25),
  padding: vars.spacing(0.75, 0.5),
  borderRadius: vars.radius.base,
  font: vars.typography.font.large,
  fontWeight: vars.typography.fontWeight.medium,
  cursor: "pointer",
  border: "none",
  background: "none",
  outline: "none",
  marginRight: "auto",
  "&:hover, &:focus-visible": {
    backgroundColor: vars.colors.interactive.hover
  }
});
function GridChartsPanelChartSelector(props) {
  var _a, _b;
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const {
    activeChartId,
    chartEntries
  } = props;
  const triggerRef = React353.useRef(null);
  const [open, setOpen] = React353.useState(false);
  const menuId = useId();
  const triggerId = useId();
  const activeChart = chartEntries.find(([chartId]) => chartId === activeChartId);
  return (0, import_jsx_runtime260.jsxs)(React353.Fragment, {
    children: [(0, import_jsx_runtime260.jsxs)(GridChartsPanelChartSelection, {
      id: triggerId,
      "aria-haspopup": "true",
      "aria-controls": open ? menuId : void 0,
      "aria-expanded": open ? "true" : void 0,
      ownerState: rootProps,
      onClick: () => setOpen(!open),
      ref: triggerRef,
      children: [(_a = activeChart == null ? void 0 : activeChart[1]) == null ? void 0 : _a.label, (0, import_jsx_runtime260.jsx)(rootProps.slots.promptChangesToggleIcon, {
        fontSize: "small"
      })]
    }), (0, import_jsx_runtime260.jsx)(GridMenu, {
      open,
      target: triggerRef.current,
      onClose: () => setOpen(false),
      position: "bottom-start",
      children: (0, import_jsx_runtime260.jsx)(rootProps.slots.baseMenuList, _extends({
        id: menuId,
        "aria-labelledby": triggerId,
        autoFocusItem: true
      }, (_b = rootProps.slotProps) == null ? void 0 : _b.baseMenuList, {
        children: chartEntries.map(([chartId, chartState]) => {
          var _a2;
          return (0, import_jsx_runtime260.jsx)(rootProps.slots.baseMenuItem, _extends({
            value: chartId,
            onClick: () => {
              apiRef.current.setActiveChartId(chartId);
              setOpen(false);
            },
            selected: chartId === activeChartId
          }, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem, {
            children: chartState.label || chartId
          }), chartId);
        })
      }))
    })]
  });
}
true ? GridChartsPanelChartSelector.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  activeChartId: import_prop_types161.default.string.isRequired,
  chartEntries: import_prop_types161.default.arrayOf(import_prop_types161.default.arrayOf(import_prop_types161.default.oneOfType([import_prop_types161.default.shape({
    configuration: import_prop_types161.default.object.isRequired,
    dimensions: import_prop_types161.default.arrayOf(import_prop_types161.default.shape({
      data: import_prop_types161.default.arrayOf(import_prop_types161.default.oneOfType([import_prop_types161.default.number, import_prop_types161.default.string])).isRequired,
      id: import_prop_types161.default.string.isRequired,
      label: import_prop_types161.default.string.isRequired
    })).isRequired,
    dimensionsLabel: import_prop_types161.default.string,
    label: import_prop_types161.default.string,
    maxDimensions: import_prop_types161.default.number,
    maxValues: import_prop_types161.default.number,
    synced: import_prop_types161.default.bool.isRequired,
    type: import_prop_types161.default.string.isRequired,
    values: import_prop_types161.default.arrayOf(import_prop_types161.default.shape({
      data: import_prop_types161.default.arrayOf(import_prop_types161.default.number).isRequired,
      id: import_prop_types161.default.string.isRequired,
      label: import_prop_types161.default.string.isRequired
    })).isRequired,
    valuesLabel: import_prop_types161.default.string
  }), import_prop_types161.default.string]).isRequired)).isRequired
} : void 0;
function GridChartsPanel(props) {
  var _a, _b, _c, _d;
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps3();
  const {
    schema = {}
  } = props;
  const activeChartId = useGridSelector(apiRef, gridChartsIntegrationActiveChartIdSelector);
  const {
    chartStateLookup
  } = useGridChartsIntegrationContext();
  const [activeTab, setActiveTab] = React353.useState("chart");
  const chartEntries = React353.useMemo(() => Object.entries(chartStateLookup), [chartStateLookup]);
  const activeChartType = React353.useMemo(() => {
    var _a2;
    return ((_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.type) || "";
  }, [chartStateLookup, activeChartId]);
  const currentChartConfiguration = React353.useMemo(() => {
    return schema[activeChartType] || {};
  }, [schema, activeChartType]);
  const handleChartSyncChange = React353.useCallback((newSyncState) => {
    apiRef.current.setChartSynchronizationState(activeChartId, newSyncState);
  }, [apiRef, activeChartId]);
  const handleChartTypeChange = React353.useCallback((type) => {
    apiRef.current.setChartType(activeChartId, type);
  }, [apiRef, activeChartId]);
  const tabItems = React353.useMemo(() => {
    var _a2;
    return [{
      value: "chart",
      label: apiRef.current.getLocaleText("chartsTabChart"),
      children: (0, import_jsx_runtime260.jsx)(GridChartsPanelChart, {
        schema,
        selectedChartType: (_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.type,
        onChartTypeChange: handleChartTypeChange
      })
    }, {
      value: "data",
      label: apiRef.current.getLocaleText("chartsTabFields"),
      children: (0, import_jsx_runtime260.jsx)(GridChartsPanelData, {})
    }, {
      value: "customize",
      label: apiRef.current.getLocaleText("chartsTabCustomize"),
      children: (0, import_jsx_runtime260.jsx)(GridChartsPanelCustomize, {
        activeChartId,
        sections: currentChartConfiguration.customization || []
      })
    }];
  }, [apiRef, activeChartId, chartStateLookup, handleChartTypeChange, schema, currentChartConfiguration]);
  return (0, import_jsx_runtime260.jsxs)(React353.Fragment, {
    children: [(0, import_jsx_runtime260.jsxs)(GridChartsPanelHeader, {
      ownerState: rootProps,
      children: [chartEntries.length > 1 ? (0, import_jsx_runtime260.jsx)(GridChartsPanelChartSelector, {
        activeChartId,
        chartEntries
      }) : (0, import_jsx_runtime260.jsx)(GridChartsPanelTitle, {
        ownerState: rootProps,
        children: "Charts"
      }), chartEntries.length > 0 && (0, import_jsx_runtime260.jsx)(rootProps.slots.baseTooltip, {
        title: rootProps.localeText.chartsSyncButtonLabel,
        children: (0, import_jsx_runtime260.jsx)(rootProps.slots.baseToggleButton, {
          value: "sync",
          "aria-label": rootProps.localeText.chartsSyncButtonLabel,
          selected: (_a = chartStateLookup[activeChartId]) == null ? void 0 : _a.synced,
          onClick: () => {
            var _a2;
            handleChartSyncChange(!((_a2 = chartStateLookup[activeChartId]) == null ? void 0 : _a2.synced));
          },
          children: ((_b = chartStateLookup[activeChartId]) == null ? void 0 : _b.synced) ? (0, import_jsx_runtime260.jsx)(rootProps.slots.chartsSyncIcon, {
            fontSize: "small"
          }) : (0, import_jsx_runtime260.jsx)(rootProps.slots.chartsSyncDisabledIcon, {
            fontSize: "small"
          })
        })
      }), (0, import_jsx_runtime260.jsx)(rootProps.slots.baseIconButton, _extends({
        onClick: () => {
          apiRef.current.setChartsPanelOpen(false);
        },
        "aria-label": apiRef.current.getLocaleText("chartsCloseButton")
      }, (_c = rootProps.slotProps) == null ? void 0 : _c.baseIconButton, {
        children: (0, import_jsx_runtime260.jsx)(rootProps.slots.sidebarCloseIcon, {
          fontSize: "small"
        })
      }))]
    }), chartEntries.length > 0 ? (0, import_jsx_runtime260.jsx)(rootProps.slots.baseTabs, _extends({
      items: tabItems,
      value: activeTab,
      onChange: (_event, value) => {
        setActiveTab(value);
      }
    }, (_d = rootProps.slotProps) == null ? void 0 : _d.baseTabs)) : (0, import_jsx_runtime260.jsx)(GridOverlay, {
      children: apiRef.current.getLocaleText("chartsNoCharts")
    })]
  });
}
true ? GridChartsPanel.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * Override the default column name generation logic. Use field in combination with the grid state to determine the name of the column that will be shown to the user.
   * @param {string} field The field name
   * @returns {string | undefined} The name of the column or undefined if the column name should be determined by the grid
   */
  getColumnName: import_prop_types161.default.func,
  /**
   * The schema of the charts configuration.
   * @type {GridChartsConfigurationOptions}
   * @default {}
   */
  schema: import_prop_types161.default.object
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/components/chartsPanel/ChartsPanelTrigger.js
var React354 = __toESM(require_react(), 1);
var import_prop_types162 = __toESM(require_prop_types(), 1);
var import_jsx_runtime261 = __toESM(require_jsx_runtime(), 1);
var _excluded99 = ["render", "className", "onClick", "onPointerUp"];
var ChartsPanelTrigger = forwardRef2(function ChartsPanelTrigger2(props, ref) {
  var _a;
  const {
    render,
    className,
    onClick
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded99);
  const rootProps = useGridRootProps3();
  const buttonId = useId();
  const panelId = useId();
  const apiRef = useGridApiContext3();
  const open = useGridSelector(apiRef, gridChartsPanelOpenSelector);
  const state = {
    open
  };
  const resolvedClassName = typeof className === "function" ? className(state) : className;
  const handleClick = (event) => {
    apiRef.current.setChartsPanelOpen(!open);
    onClick == null ? void 0 : onClick(event);
  };
  const element = useComponentRenderer(rootProps.slots.baseButton, render, _extends({}, (_a = rootProps.slotProps) == null ? void 0 : _a.baseButton, {
    id: buttonId,
    // TODO: Hook up the panel/trigger IDs to the charts configuration panel
    "aria-haspopup": "true",
    "aria-expanded": open ? "true" : void 0,
    "aria-controls": open ? panelId : void 0,
    onClick: handleClick,
    className: resolvedClassName
  }, other, {
    ref
  }), state);
  return (0, import_jsx_runtime261.jsx)(React354.Fragment, {
    children: element
  });
});
if (true) ChartsPanelTrigger.displayName = "ChartsPanelTrigger";
true ? ChartsPanelTrigger.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * A function to customize rendering of the component.
   */
  className: import_prop_types162.default.oneOfType([import_prop_types162.default.func, import_prop_types162.default.string]),
  disabled: import_prop_types162.default.bool,
  id: import_prop_types162.default.string,
  /**
   * A function to customize rendering of the component.
   */
  render: import_prop_types162.default.oneOfType([import_prop_types162.default.element, import_prop_types162.default.func]),
  role: import_prop_types162.default.string,
  size: import_prop_types162.default.oneOf(["large", "medium", "small"]),
  startIcon: import_prop_types162.default.node,
  style: import_prop_types162.default.object,
  tabIndex: import_prop_types162.default.number,
  title: import_prop_types162.default.string,
  touchRippleRef: import_prop_types162.default.any
} : void 0;

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/export/useGridExcelExport.js
var import_jsx_runtime262 = __toESM(require_jsx_runtime(), 1);
var _excluded100 = ["worker", "exceljsPostProcess", "exceljsPreProcess", "columnsStyles", "includeHeaders", "getRowsToExport", "valueOptionsSheetName"];
var useGridExcelExport = (apiRef, props) => {
  const logger = useGridLogger(apiRef, "useGridExcelExport");
  const getDataAsExcel = React355.useCallback((options3 = {}) => {
    logger.debug(`Get data as excel`);
    const getRowsToExport = options3.getRowsToExport ?? defaultGetRowsToExport;
    const exportedRowIds = getRowsToExport({
      apiRef
    });
    const exportedColumns = getColumnsToExport({
      apiRef,
      options: options3
    });
    return buildExcel({
      columns: exportedColumns,
      rowIds: exportedRowIds,
      includeHeaders: options3.includeHeaders ?? true,
      includeColumnGroupsHeaders: options3.includeColumnGroupsHeaders ?? true,
      valueOptionsSheetName: (options3 == null ? void 0 : options3.valueOptionsSheetName) || "Options",
      columnsStyles: options3 == null ? void 0 : options3.columnsStyles,
      exceljsPreProcess: options3 == null ? void 0 : options3.exceljsPreProcess,
      exceljsPostProcess: options3 == null ? void 0 : options3.exceljsPostProcess,
      escapeFormulas: options3.escapeFormulas ?? true
    }, apiRef);
  }, [logger, apiRef]);
  const exportDataAsExcel = React355.useCallback(async (options3 = {}) => {
    const {
      worker: workerFn,
      exceljsPostProcess,
      exceljsPreProcess,
      getRowsToExport = defaultGetRowsToExport,
      valueOptionsSheetName = "Options"
    } = options3, cloneableOptions = _objectWithoutPropertiesLoose(options3, _excluded100);
    const sendExcelToUser = (buffer2) => {
      const blob = new Blob([buffer2], {
        type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
      });
      exportAs(blob, "xlsx", options3 == null ? void 0 : options3.fileName);
    };
    if (!workerFn) {
      apiRef.current.publishEvent("excelExportStateChange", "pending");
      const workbook = await getDataAsExcel(options3);
      if (workbook === null) {
        return;
      }
      const content = await workbook.xlsx.writeBuffer();
      apiRef.current.publishEvent("excelExportStateChange", "finished");
      sendExcelToUser(content);
      return;
    }
    if (true) {
      if (exceljsPostProcess) {
        console.warn([`MUI X: The exceljsPostProcess option is not supported when a web worker is used.`, "As alternative, pass the callback to the same option in setupExcelExportWebWorker."].join("\n"));
      }
      if (exceljsPreProcess) {
        console.warn([`MUI X: The exceljsPreProcess option is not supported when a web worker is used.`, "As alternative, pass the callback to the same option in setupExcelExportWebWorker."].join("\n"));
      }
    }
    const worker = workerFn();
    apiRef.current.publishEvent("excelExportStateChange", "pending");
    worker.onmessage = async (event) => {
      sendExcelToUser(event.data);
      apiRef.current.publishEvent("excelExportStateChange", "finished");
      worker.terminate();
    };
    const exportedRowIds = getRowsToExport({
      apiRef
    });
    const exportedColumns = getColumnsToExport({
      apiRef,
      options: options3
    });
    const valueOptionsData = await getDataForValueOptionsSheet(exportedColumns, valueOptionsSheetName, apiRef.current);
    const serializedColumns = serializeColumns(exportedColumns, options3.columnsStyles || {});
    apiRef.current.resetColSpan();
    const serializedRows = [];
    for (let i2 = 0; i2 < exportedRowIds.length; i2 += 1) {
      const id = exportedRowIds[i2];
      const serializedRow = serializeRowUnsafe(id, exportedColumns, apiRef, valueOptionsData, {
        escapeFormulas: options3.escapeFormulas ?? true
      });
      serializedRows.push(serializedRow);
    }
    apiRef.current.resetColSpan();
    const columnGroupPaths = exportedColumns.reduce((acc, column) => {
      acc[column.field] = apiRef.current.getColumnGroupPath(column.field);
      return acc;
    }, {});
    const message = {
      // workers share the pub-sub channel namespace. Use this property to filter out messages.
      namespace: "mui-x-data-grid-export",
      serializedColumns,
      serializedRows,
      valueOptionsData,
      columnGroupPaths,
      columnGroupDetails: apiRef.current.getAllGroupDetails(),
      options: cloneableOptions,
      valueOptionsSheetName
    };
    worker.postMessage(message);
  }, [apiRef, getDataAsExcel]);
  const excelExportApi = {
    getDataAsExcel,
    exportDataAsExcel
  };
  useGridApiMethod(apiRef, excelExportApi, "public");
  const addExportMenuButtons = React355.useCallback((initialValue, options3) => {
    var _a;
    if ((_a = options3.excelOptions) == null ? void 0 : _a.disableToolbarButton) {
      return initialValue;
    }
    return [...initialValue, {
      component: (0, import_jsx_runtime262.jsx)(GridExcelExportMenuItem, {
        options: options3.excelOptions
      }),
      componentName: "excelExport"
    }];
  }, []);
  useGridRegisterPipeProcessor(apiRef, "exportMenu", addExportMenuButtons);
  useGridEventPriority(apiRef, "excelExportStateChange", props.onExcelExportStateChange);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/cellSelection/useGridCellSelection.js
var React356 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/cellSelection/gridCellSelectionSelector.js
var gridCellSelectionStateSelector = createRootSelector((state) => state.cellSelection);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/cellSelection/useGridCellSelection.js
var cellSelectionStateInitializer = (state, props) => {
  var _a;
  return _extends({}, state, {
    cellSelection: _extends({}, props.cellSelectionModel ?? ((_a = props.initialState) == null ? void 0 : _a.cellSelection))
  });
};
function isKeyboardEvent3(event) {
  return !!event.key;
}
var AUTO_SCROLL_SENSITIVITY = 50;
var AUTO_SCROLL_SPEED = 20;
var useGridCellSelection = (apiRef, props) => {
  const hasRootReference = apiRef.current.rootElementRef.current !== null;
  const cellWithVirtualFocus = React356.useRef(null);
  const lastMouseDownCell = React356.useRef(null);
  const mousePosition = React356.useRef(null);
  const autoScrollRAF = React356.useRef(null);
  const totalHeaderHeight = getTotalHeaderHeight(apiRef, props);
  const ignoreValueFormatterProp = props.ignoreValueFormatterDuringExport;
  const ignoreValueFormatter = (typeof ignoreValueFormatterProp === "object" ? ignoreValueFormatterProp == null ? void 0 : ignoreValueFormatterProp.clipboardExport : ignoreValueFormatterProp) || false;
  const clipboardCopyCellDelimiter = props.clipboardCopyCellDelimiter;
  apiRef.current.registerControlState({
    stateId: "cellSelection",
    propModel: props.cellSelectionModel,
    propOnChange: props.onCellSelectionModelChange,
    stateSelector: gridCellSelectionStateSelector,
    changeEvent: "cellSelectionChange"
  });
  const runIfCellSelectionIsEnabled = (callback) => (...args) => {
    if (props.cellSelection) {
      callback(...args);
    }
  };
  const isCellSelected = React356.useCallback((id, field) => {
    if (!props.cellSelection) {
      return false;
    }
    const cellSelectionModel = gridCellSelectionStateSelector(apiRef);
    return cellSelectionModel[id] ? !!cellSelectionModel[id][field] : false;
  }, [apiRef, props.cellSelection]);
  const getCellSelectionModel = React356.useCallback(() => {
    return gridCellSelectionStateSelector(apiRef);
  }, [apiRef]);
  const setCellSelectionModel = React356.useCallback((newModel) => {
    if (!props.cellSelection) {
      return;
    }
    apiRef.current.setState((prevState) => _extends({}, prevState, {
      cellSelection: newModel
    }));
  }, [apiRef, props.cellSelection]);
  const selectCellRange = React356.useCallback((start2, end2, keepOtherSelected = false) => {
    const startRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(start2.id);
    const startColumnIndex = apiRef.current.getColumnIndex(start2.field);
    const endRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(end2.id);
    const endColumnIndex = apiRef.current.getColumnIndex(end2.field);
    let finalStartRowIndex = startRowIndex;
    let finalStartColumnIndex = startColumnIndex;
    let finalEndRowIndex = endRowIndex;
    let finalEndColumnIndex = endColumnIndex;
    if (finalStartRowIndex > finalEndRowIndex) {
      finalStartRowIndex = endRowIndex;
      finalEndRowIndex = startRowIndex;
    }
    if (finalStartColumnIndex > finalEndColumnIndex) {
      finalStartColumnIndex = endColumnIndex;
      finalEndColumnIndex = startColumnIndex;
    }
    const visibleColumns = apiRef.current.getVisibleColumns();
    const visibleRows = getVisibleRows(apiRef);
    const rowsInRange = visibleRows.rows.slice(finalStartRowIndex, finalEndRowIndex + 1);
    const columnsInRange = visibleColumns.slice(finalStartColumnIndex, finalEndColumnIndex + 1);
    const newModel = keepOtherSelected ? _extends({}, apiRef.current.getCellSelectionModel()) : {};
    rowsInRange.forEach((row) => {
      if (!newModel[row.id]) {
        newModel[row.id] = {};
      }
      columnsInRange.forEach((column) => {
        newModel[row.id][column.field] = true;
      }, {});
    });
    apiRef.current.setCellSelectionModel(newModel);
  }, [apiRef]);
  const getSelectedCellsAsArray = React356.useCallback(() => {
    const selectionModel = apiRef.current.getCellSelectionModel();
    const currentVisibleRows = getVisibleRows(apiRef, props);
    const sortedEntries = currentVisibleRows.rows.reduce((result, row) => {
      if (row.id in selectionModel) {
        result.push([row.id, selectionModel[row.id]]);
      }
      return result;
    }, []);
    return sortedEntries.reduce((selectedCells, [id, fields]) => {
      selectedCells.push(...Object.entries(fields).reduce((selectedFields, [field, isSelected]) => {
        if (isSelected) {
          selectedFields.push({
            id,
            field
          });
        }
        return selectedFields;
      }, []));
      return selectedCells;
    }, []);
  }, [apiRef, props]);
  const cellSelectionApi = {
    isCellSelected,
    getCellSelectionModel,
    setCellSelectionModel,
    selectCellRange,
    getSelectedCellsAsArray
  };
  useGridApiMethod(apiRef, cellSelectionApi, "public");
  const hasClickedValidCellForRangeSelection = React356.useCallback((params) => {
    if (params.field === GRID_CHECKBOX_SELECTION_COL_DEF.field) {
      return false;
    }
    if (params.field === GRID_DETAIL_PANEL_TOGGLE_FIELD) {
      return false;
    }
    const column = apiRef.current.getColumn(params.field);
    if (column.type === GRID_ACTIONS_COLUMN_TYPE) {
      return false;
    }
    return params.rowNode.type !== "pinnedRow";
  }, [apiRef]);
  const handleMouseUp = useEventCallback_default(() => {
    var _a, _b;
    lastMouseDownCell.current = null;
    (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.classList.remove(gridClasses["root--disableUserSelection"]);
    stopAutoScroll();
  });
  const handleCellMouseDown = React356.useCallback((params, event) => {
    var _a, _b, _c;
    const isMacOs = window.navigator.platform.toUpperCase().indexOf("MAC") >= 0;
    if (event.button !== 0 || event.ctrlKey && isMacOs) {
      return;
    }
    if (params.field === GRID_REORDER_COL_DEF.field) {
      return;
    }
    const focusedCell = gridFocusCellSelector(apiRef);
    if (hasClickedValidCellForRangeSelection(params) && event.shiftKey && focusedCell) {
      event.preventDefault();
    }
    lastMouseDownCell.current = {
      id: params.id,
      field: params.field
    };
    (_b = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current) == null ? void 0 : _b.classList.add(gridClasses["root--disableUserSelection"]);
    const document2 = ownerDocument((_c = apiRef.current.rootElementRef) == null ? void 0 : _c.current);
    document2.addEventListener("mouseup", handleMouseUp, {
      once: true
    });
  }, [apiRef, handleMouseUp, hasClickedValidCellForRangeSelection]);
  const stopAutoScroll = React356.useCallback(() => {
    if (autoScrollRAF.current) {
      cancelAnimationFrame(autoScrollRAF.current);
      autoScrollRAF.current = null;
    }
  }, []);
  const handleCellFocusIn = React356.useCallback((params) => {
    cellWithVirtualFocus.current = {
      id: params.id,
      field: params.field
    };
  }, []);
  const startAutoScroll = React356.useCallback(() => {
    var _a;
    if (autoScrollRAF.current) {
      return;
    }
    if (!((_a = apiRef.current.virtualScrollerRef) == null ? void 0 : _a.current)) {
      return;
    }
    function autoScroll() {
      var _a2;
      if (!mousePosition.current || !((_a2 = apiRef.current.virtualScrollerRef) == null ? void 0 : _a2.current)) {
        return;
      }
      const dimensions = gridDimensionsSelector(apiRef);
      const {
        x: mouseX,
        y: mouseY
      } = mousePosition.current;
      const {
        width,
        height: viewportOuterHeight
      } = dimensions.viewportOuterSize;
      const height = viewportOuterHeight - totalHeaderHeight;
      let deltaX = 0;
      let deltaY = 0;
      let factor = 0;
      if (mouseY <= AUTO_SCROLL_SENSITIVITY && dimensions.hasScrollY) {
        factor = (AUTO_SCROLL_SENSITIVITY - mouseY) / -AUTO_SCROLL_SENSITIVITY;
        deltaY = AUTO_SCROLL_SPEED;
      } else if (mouseY >= height - AUTO_SCROLL_SENSITIVITY && dimensions.hasScrollY) {
        factor = (mouseY - (height - AUTO_SCROLL_SENSITIVITY)) / AUTO_SCROLL_SENSITIVITY;
        deltaY = AUTO_SCROLL_SPEED;
      } else if (mouseX <= AUTO_SCROLL_SENSITIVITY && dimensions.hasScrollX) {
        factor = (AUTO_SCROLL_SENSITIVITY - mouseX) / -AUTO_SCROLL_SENSITIVITY;
        deltaX = AUTO_SCROLL_SPEED;
      } else if (mouseX >= width - AUTO_SCROLL_SENSITIVITY && dimensions.hasScrollX) {
        factor = (mouseX - (width - AUTO_SCROLL_SENSITIVITY)) / AUTO_SCROLL_SENSITIVITY;
        deltaX = AUTO_SCROLL_SPEED;
      }
      if (deltaX !== 0 || deltaY !== 0) {
        const {
          scrollLeft,
          scrollTop
        } = apiRef.current.virtualScrollerRef.current;
        apiRef.current.scroll({
          top: scrollTop + deltaY * factor,
          left: scrollLeft + deltaX * factor
        });
      }
      autoScrollRAF.current = requestAnimationFrame(autoScroll);
    }
    autoScroll();
  }, [apiRef, totalHeaderHeight]);
  const handleCellMouseOver = React356.useCallback((params, event) => {
    var _a, _b;
    if (!lastMouseDownCell.current) {
      return;
    }
    const {
      id,
      field
    } = params;
    apiRef.current.selectCellRange(lastMouseDownCell.current, {
      id,
      field
    }, event.ctrlKey || event.metaKey);
    const virtualScrollerRect = (_b = (_a = apiRef.current.virtualScrollerRef) == null ? void 0 : _a.current) == null ? void 0 : _b.getBoundingClientRect();
    if (!virtualScrollerRect) {
      return;
    }
    const dimensions = gridDimensionsSelector(apiRef);
    const {
      x,
      y
    } = virtualScrollerRect;
    const {
      width,
      height: viewportOuterHeight
    } = dimensions.viewportOuterSize;
    const height = viewportOuterHeight - totalHeaderHeight;
    const mouseX = event.clientX - x;
    const mouseY = event.clientY - y - totalHeaderHeight;
    mousePosition.current = {
      x: mouseX,
      y: mouseY
    };
    const hasEnteredVerticalSensitivityArea = mouseY <= AUTO_SCROLL_SENSITIVITY || mouseY >= height - AUTO_SCROLL_SENSITIVITY;
    const hasEnteredHorizontalSensitivityArea = mouseX <= AUTO_SCROLL_SENSITIVITY || mouseX >= width - AUTO_SCROLL_SENSITIVITY;
    const hasEnteredSensitivityArea = hasEnteredVerticalSensitivityArea || hasEnteredHorizontalSensitivityArea;
    if (hasEnteredSensitivityArea) {
      startAutoScroll();
    } else {
      stopAutoScroll();
    }
  }, [apiRef, startAutoScroll, stopAutoScroll, totalHeaderHeight]);
  const handleCellClick = useEventCallback_default((params, event) => {
    const {
      id,
      field
    } = params;
    if (!hasClickedValidCellForRangeSelection(params)) {
      return;
    }
    const focusedCell = gridFocusCellSelector(apiRef);
    if (event.shiftKey && focusedCell) {
      apiRef.current.selectCellRange(focusedCell, {
        id,
        field
      });
      cellWithVirtualFocus.current = {
        id,
        field
      };
      return;
    }
    if (event.ctrlKey || event.metaKey) {
      const prevModel = apiRef.current.getCellSelectionModel();
      apiRef.current.setCellSelectionModel(_extends({}, prevModel, {
        [id]: _extends({}, prevModel[id], {
          [field]: !apiRef.current.isCellSelected(id, field)
        })
      }));
    } else {
      apiRef.current.setCellSelectionModel({
        [id]: {
          [field]: true
        }
      });
    }
  });
  const handleCellKeyDown = useEventCallback_default((params, event) => {
    if (!isNavigationKey(event.key) || !cellWithVirtualFocus.current) {
      return;
    }
    if (event.key === " " && params.cellMode === GridCellModes.Edit) {
      return;
    }
    if (!event.shiftKey) {
      apiRef.current.setCellSelectionModel({});
      return;
    }
    const {
      current: otherCell
    } = cellWithVirtualFocus;
    let endRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(otherCell.id);
    let endColumnIndex = apiRef.current.getColumnIndex(otherCell.field);
    if (event.key === "ArrowDown") {
      endRowIndex += 1;
    } else if (event.key === "ArrowUp") {
      endRowIndex -= 1;
    } else if (event.key === "ArrowRight") {
      endColumnIndex += 1;
    } else if (event.key === "ArrowLeft") {
      endColumnIndex -= 1;
    }
    const visibleRows = getVisibleRows(apiRef);
    if (endRowIndex < 0 || endRowIndex >= visibleRows.rows.length) {
      return;
    }
    const visibleColumns = apiRef.current.getVisibleColumns();
    if (endColumnIndex < 0 || endColumnIndex >= visibleColumns.length) {
      return;
    }
    cellWithVirtualFocus.current = {
      id: visibleRows.rows[endRowIndex].id,
      field: visibleColumns[endColumnIndex].field
    };
    apiRef.current.scrollToIndexes({
      rowIndex: endRowIndex,
      colIndex: endColumnIndex
    });
    const {
      id,
      field
    } = params;
    apiRef.current.selectCellRange({
      id,
      field
    }, cellWithVirtualFocus.current);
  });
  useGridEvent(apiRef, "cellClick", runIfCellSelectionIsEnabled(handleCellClick));
  useGridEvent(apiRef, "cellFocusIn", runIfCellSelectionIsEnabled(handleCellFocusIn));
  useGridEvent(apiRef, "cellKeyDown", runIfCellSelectionIsEnabled(handleCellKeyDown));
  useGridEvent(apiRef, "cellMouseDown", runIfCellSelectionIsEnabled(handleCellMouseDown));
  useGridEvent(apiRef, "cellMouseOver", runIfCellSelectionIsEnabled(handleCellMouseOver));
  React356.useEffect(() => {
    if (props.cellSelectionModel) {
      apiRef.current.setCellSelectionModel(props.cellSelectionModel);
    }
  }, [apiRef, props.cellSelectionModel]);
  React356.useEffect(() => {
    var _a;
    const rootRef = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current;
    return () => {
      stopAutoScroll();
      const document2 = ownerDocument(rootRef);
      document2.removeEventListener("mouseup", handleMouseUp);
    };
  }, [apiRef, hasRootReference, handleMouseUp, stopAutoScroll]);
  const checkIfCellIsSelected = React356.useCallback((isSelected, {
    id,
    field
  }) => {
    return apiRef.current.isCellSelected(id, field);
  }, [apiRef]);
  const addClassesToCells = React356.useCallback((classes2, {
    id,
    field
  }) => {
    const visibleRows = getVisibleRows(apiRef);
    if (!visibleRows.range || !apiRef.current.isCellSelected(id, field)) {
      return classes2;
    }
    const newClasses = [...classes2];
    const rowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(id);
    const columnIndex = apiRef.current.getColumnIndex(field);
    const visibleColumns = apiRef.current.getVisibleColumns();
    if (rowIndex > 0) {
      const {
        id: previousRowId
      } = visibleRows.rows[rowIndex - 1];
      if (!apiRef.current.isCellSelected(previousRowId, field)) {
        newClasses.push(gridClasses["cell--rangeTop"]);
      }
    } else {
      newClasses.push(gridClasses["cell--rangeTop"]);
    }
    if (rowIndex + visibleRows.range.firstRowIndex < visibleRows.range.lastRowIndex) {
      const {
        id: nextRowId
      } = visibleRows.rows[rowIndex + 1];
      if (!apiRef.current.isCellSelected(nextRowId, field)) {
        newClasses.push(gridClasses["cell--rangeBottom"]);
      }
    } else {
      newClasses.push(gridClasses["cell--rangeBottom"]);
    }
    if (columnIndex > 0) {
      const {
        field: previousColumnField
      } = visibleColumns[columnIndex - 1];
      if (!apiRef.current.isCellSelected(id, previousColumnField)) {
        newClasses.push(gridClasses["cell--rangeLeft"]);
      }
    } else {
      newClasses.push(gridClasses["cell--rangeLeft"]);
    }
    if (columnIndex < visibleColumns.length - 1) {
      const {
        field: nextColumnField
      } = visibleColumns[columnIndex + 1];
      if (!apiRef.current.isCellSelected(id, nextColumnField)) {
        newClasses.push(gridClasses["cell--rangeRight"]);
      }
    } else {
      newClasses.push(gridClasses["cell--rangeRight"]);
    }
    return newClasses;
  }, [apiRef]);
  const canUpdateFocus = React356.useCallback((initialValue, {
    event,
    cell
  }) => {
    if (!cell || !props.cellSelection || !event.shiftKey) {
      return initialValue;
    }
    if (isKeyboardEvent3(event)) {
      return isNavigationKey(event.key) ? false : initialValue;
    }
    const focusedCell = gridFocusCellSelector(apiRef);
    if (hasClickedValidCellForRangeSelection(cell) && focusedCell) {
      return false;
    }
    return initialValue;
  }, [apiRef, props.cellSelection, hasClickedValidCellForRangeSelection]);
  const handleClipboardCopy = React356.useCallback((value) => {
    if (apiRef.current.getSelectedCellsAsArray().length <= 1) {
      return value;
    }
    const sortedRowIds = gridSortedRowIdsSelector(apiRef);
    const cellSelectionModel = apiRef.current.getCellSelectionModel();
    const unsortedSelectedRowIds = Object.keys(cellSelectionModel);
    const sortedSelectedRowIds = sortedRowIds.filter((id) => unsortedSelectedRowIds.includes(`${id}`));
    const copyData = sortedSelectedRowIds.reduce((acc, rowId) => {
      const fieldsMap = cellSelectionModel[rowId];
      const rowValues = Object.keys(fieldsMap).map((field) => {
        let cellData;
        if (fieldsMap[field]) {
          const cellParams = apiRef.current.getCellParams(rowId, field);
          cellData = serializeCellValue(cellParams, {
            csvOptions: {
              delimiter: clipboardCopyCellDelimiter,
              shouldAppendQuotes: false,
              escapeFormulas: false
            },
            ignoreValueFormatter
          });
        } else {
          cellData = "";
        }
        return cellData;
      }, "");
      const rowString = rowValues.join(clipboardCopyCellDelimiter);
      return acc === "" ? rowString : [acc, rowString].join("\r\n");
    }, "");
    return copyData;
  }, [apiRef, ignoreValueFormatter, clipboardCopyCellDelimiter]);
  useGridRegisterPipeProcessor(apiRef, "isCellSelected", checkIfCellIsSelected);
  useGridRegisterPipeProcessor(apiRef, "cellClassName", addClassesToCells);
  useGridRegisterPipeProcessor(apiRef, "canUpdateFocus", canUpdateFocus);
  useGridRegisterPipeProcessor(apiRef, "clipboardCopy", handleClipboardCopy);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/clipboard/useGridClipboardImport.js
var React357 = __toESM(require_react(), 1);
var columnFieldsToExcludeFromPaste = [GRID_CHECKBOX_SELECTION_FIELD, GRID_REORDER_COL_DEF.field, GRID_DETAIL_PANEL_TOGGLE_FIELD];
function batchRowUpdates(func, wait) {
  let rows = [];
  const debounced = debounce(() => {
    func(rows);
    rows = [];
  }, wait);
  return (row) => {
    rows.push(row);
    debounced();
  };
}
async function getTextFromClipboard(rootEl) {
  return new Promise((resolve) => {
    const focusedCell = getActiveElement(document);
    const el = document.createElement("input");
    el.style.width = "0px";
    el.style.height = "0px";
    el.style.border = "none";
    el.style.margin = "0";
    el.style.padding = "0";
    el.style.outline = "none";
    el.style.position = "absolute";
    el.style.top = "0";
    el.style.left = "0";
    const handlePasteEvent = (event) => {
      var _a;
      el.removeEventListener("paste", handlePasteEvent);
      const text = (_a = event.clipboardData) == null ? void 0 : _a.getData("text/plain");
      if (focusedCell instanceof HTMLElement) {
        focusedCell.focus({
          preventScroll: true
        });
      }
      el.remove();
      resolve(text || "");
    };
    el.addEventListener("paste", handlePasteEvent);
    rootEl.appendChild(el);
    el.focus({
      preventScroll: true
    });
  });
}
var CellValueUpdater = class {
  constructor(options3) {
    __publicField(this, "rowsToUpdate", /* @__PURE__ */ (() => /* @__PURE__ */ new Map())());
    this.options = options3;
    this.updateRow = batchRowUpdates(options3.apiRef.current.updateRows, 50);
  }
  updateCell({
    rowId,
    field,
    pastedCellValue
  }) {
    if (pastedCellValue === void 0) {
      return;
    }
    const {
      apiRef,
      getRowId
    } = this.options;
    const colDef = apiRef.current.getColumn(field);
    if (!colDef || !colDef.editable) {
      return;
    }
    const row = this.rowsToUpdate.get(rowId) || _extends({}, apiRef.current.getRow(rowId));
    if (!row) {
      return;
    }
    const cellParams = apiRef.current.getCellParams(rowId, field);
    if (!apiRef.current.isCellEditable(cellParams)) {
      return;
    }
    let parsedValue = pastedCellValue;
    if (colDef.pastedValueParser) {
      parsedValue = colDef.pastedValueParser(pastedCellValue, row, colDef, apiRef);
    } else if (colDef.valueParser) {
      parsedValue = colDef.valueParser(parsedValue, row, colDef, apiRef);
    }
    if (parsedValue === void 0) {
      return;
    }
    let rowCopy = _extends({}, row);
    if (typeof colDef.valueSetter === "function") {
      rowCopy = colDef.valueSetter(parsedValue, rowCopy, colDef, apiRef);
    } else {
      rowCopy[field] = parsedValue;
    }
    const newRowId = getRowIdFromRowModel(rowCopy, getRowId);
    if (String(newRowId) !== String(rowId)) {
      return;
    }
    this.rowsToUpdate.set(rowId, rowCopy);
  }
  applyUpdates() {
    const {
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError
    } = this.options;
    const rowsToUpdate = this.rowsToUpdate;
    const rowIdsToUpdate = Array.from(rowsToUpdate.keys());
    if (rowIdsToUpdate.length === 0) {
      apiRef.current.publishEvent("clipboardPasteEnd", {
        oldRows: /* @__PURE__ */ new Map(),
        newRows: /* @__PURE__ */ new Map()
      });
      return;
    }
    const oldRows = /* @__PURE__ */ new Map();
    const newRows = /* @__PURE__ */ new Map();
    const handleRowUpdate = async (rowId) => {
      const oldRow = apiRef.current.getRow(rowId);
      const newRow = rowsToUpdate.get(rowId);
      oldRows.set(rowId, oldRow);
      if (typeof processRowUpdate === "function") {
        const handleError = (errorThrown) => {
          if (onProcessRowUpdateError) {
            onProcessRowUpdateError(errorThrown);
          } else if (true) {
            warnOnce(["MUI X: A call to `processRowUpdate()` threw an error which was not handled because `onProcessRowUpdateError()` is missing.", "To handle the error pass a callback to the `onProcessRowUpdateError()` prop, for example `<DataGrid onProcessRowUpdateError={(error) => ...} />`.", "For more detail, see https://mui.com/x/react-data-grid/editing/persistence/."], "error");
          }
        };
        try {
          const finalRowUpdate = await processRowUpdate(newRow, oldRow, {
            rowId
          });
          newRows.set(rowId, finalRowUpdate);
          this.updateRow(finalRowUpdate);
        } catch (error) {
          handleError(error);
        }
      } else {
        newRows.set(rowId, newRow);
        this.updateRow(newRow);
      }
    };
    const promises = rowIdsToUpdate.map((rowId) => {
      return new Promise((resolve) => {
        handleRowUpdate(rowId).then(resolve).catch(resolve);
      });
    });
    Promise.all(promises).then(() => {
      apiRef.current.publishEvent("clipboardPasteEnd", {
        oldRows,
        newRows
      });
      this.rowsToUpdate.clear();
    });
  }
};
function defaultPasteResolver({
  pastedData,
  apiRef,
  updateCell,
  pagination,
  paginationMode
}) {
  const isSingleValuePasted = pastedData.length === 1 && pastedData[0].length === 1;
  const cellSelectionModel = apiRef.current.getCellSelectionModel();
  const selectedCellsArray = apiRef.current.getSelectedCellsAsArray();
  if (cellSelectionModel && selectedCellsArray.length > 1) {
    let lastRowId = selectedCellsArray[0].id;
    let rowIndex = 0;
    let colIndex = 0;
    selectedCellsArray.forEach(({
      id: rowId,
      field
    }) => {
      if (rowId !== lastRowId) {
        lastRowId = rowId;
        rowIndex += 1;
        colIndex = 0;
      }
      const rowDataArr = pastedData[isSingleValuePasted ? 0 : rowIndex];
      const hasRowData = isSingleValuePasted ? true : rowDataArr !== void 0;
      if (hasRowData) {
        const cellValue = isSingleValuePasted ? rowDataArr[0] : rowDataArr[colIndex];
        updateCell({
          rowId,
          field,
          pastedCellValue: cellValue
        });
      }
      colIndex += 1;
    });
    return;
  }
  const visibleColumnFields = gridVisibleColumnFieldsSelector(apiRef).filter((field) => {
    if (columnFieldsToExcludeFromPaste.includes(field)) {
      return false;
    }
    return true;
  });
  if (gridRowSelectionCountSelector(apiRef) > 0 && !isSingleValuePasted) {
    const pastedRowsDataCount = pastedData.length;
    const selectedRows = gridRowSelectionIdsSelector(apiRef);
    selectedRows.forEach((row, rowId) => {
      let rowData;
      if (pastedRowsDataCount === 1) {
        rowData = pastedData[0];
      } else {
        rowData = pastedData.shift();
      }
      if (rowData === void 0) {
        return;
      }
      rowData.forEach((newCellValue, cellIndex) => {
        updateCell({
          rowId,
          field: visibleColumnFields[cellIndex],
          pastedCellValue: newCellValue
        });
      });
    });
    return;
  }
  let selectedCell = gridFocusCellSelector(apiRef);
  if (!selectedCell && selectedCellsArray.length === 1) {
    selectedCell = selectedCellsArray[0];
  }
  if (!selectedCell) {
    return;
  }
  if (columnFieldsToExcludeFromPaste.includes(selectedCell.field)) {
    return;
  }
  const selectedRowId = selectedCell.id;
  const selectedRowIndex = apiRef.current.getRowIndexRelativeToVisibleRows(selectedRowId);
  const visibleRowIds = pagination && paginationMode === "client" ? gridPaginatedVisibleSortedGridRowIdsSelector(apiRef) : gridExpandedSortedRowIdsSelector(apiRef);
  const selectedFieldIndex = visibleColumnFields.indexOf(selectedCell.field);
  pastedData.forEach((rowData, index) => {
    const rowId = visibleRowIds[selectedRowIndex + index];
    if (typeof rowId === "undefined") {
      return;
    }
    for (let i2 = selectedFieldIndex; i2 < visibleColumnFields.length; i2 += 1) {
      const field = visibleColumnFields[i2];
      const stringValue = rowData[i2 - selectedFieldIndex];
      updateCell({
        rowId,
        field,
        pastedCellValue: stringValue
      });
    }
  });
}
var useGridClipboardImport = (apiRef, props) => {
  const processRowUpdate = props.processRowUpdate;
  const onProcessRowUpdateError = props.onProcessRowUpdateError;
  const getRowId = props.getRowId;
  const enableClipboardPaste = !props.disableClipboardPaste;
  const logger = useGridLogger(apiRef, "useGridClipboardImport");
  const {
    clipboardCopyCellDelimiter,
    splitClipboardPastedText,
    pagination,
    paginationMode,
    onBeforeClipboardPasteStart
  } = props;
  const handlePaste = React357.useCallback(async (params, event) => {
    var _a;
    if (!enableClipboardPaste) {
      return;
    }
    if (!isPasteShortcut(event)) {
      return;
    }
    const focusedCell = gridFocusCellSelector(apiRef);
    if (focusedCell !== null) {
      const cellMode = apiRef.current.getCellMode(focusedCell.id, focusedCell.field);
      if (cellMode === "edit") {
        return;
      }
    }
    const rootEl = (_a = apiRef.current.rootElementRef) == null ? void 0 : _a.current;
    if (!rootEl) {
      return;
    }
    const text = await getTextFromClipboard(rootEl);
    if (!text) {
      return;
    }
    const pastedData = splitClipboardPastedText(text, clipboardCopyCellDelimiter);
    if (!pastedData) {
      return;
    }
    if (onBeforeClipboardPasteStart) {
      try {
        await onBeforeClipboardPasteStart({
          data: pastedData
        });
      } catch (error) {
        logger.debug("Clipboard paste operation cancelled");
        return;
      }
    }
    const cellUpdater = new CellValueUpdater({
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError,
      getRowId
    });
    apiRef.current.publishEvent("clipboardPasteStart", {
      data: pastedData
    });
    defaultPasteResolver({
      pastedData,
      apiRef: getPublicApiRef(apiRef),
      updateCell: (...args) => {
        cellUpdater.updateCell(...args);
      },
      pagination,
      paginationMode
    });
    cellUpdater.applyUpdates();
  }, [apiRef, processRowUpdate, onProcessRowUpdateError, getRowId, enableClipboardPaste, splitClipboardPastedText, clipboardCopyCellDelimiter, pagination, paginationMode, onBeforeClipboardPasteStart, logger]);
  const checkIfCanStartEditing = React357.useCallback((initialValue, {
    event
  }) => {
    if (isPasteShortcut(event) && enableClipboardPaste) {
      return false;
    }
    return initialValue;
  }, [enableClipboardPaste]);
  useGridEvent(apiRef, "cellKeyDown", handlePaste);
  useGridEventPriority(apiRef, "clipboardPasteStart", props.onClipboardPasteStart);
  useGridEventPriority(apiRef, "clipboardPasteEnd", props.onClipboardPasteEnd);
  useGridRegisterPipeProcessor(apiRef, "canStartEditing", checkIfCanStartEditing);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/pivoting/useGridPivoting.js
var React358 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/pivoting/utils.js
var defaultGetPivotDerivedColumns = (column, getLocaleText) => {
  if (column.type === "date") {
    const field = column.field;
    return [{
      // String column type to avoid formatting the value as 2,025 instead of 2025
      field: `${field}-year`,
      headerName: `${column.headerName} ${getLocaleText("pivotYearColumnHeaderName")}`,
      valueGetter: (value, row) => new Date(row[field]).getFullYear()
    }, {
      field: `${field}-quarter`,
      headerName: `${column.headerName} ${getLocaleText("pivotQuarterColumnHeaderName")}`,
      valueGetter: (value, row) => `Q${Math.floor(new Date(row[field]).getMonth() / 3) + 1}`
    }];
  }
  return void 0;
};
var getInitialColumns = (originalColumns, getPivotDerivedColumns, getLocaleText) => {
  const initialColumns = /* @__PURE__ */ new Map();
  for (let i2 = 0; i2 < originalColumns.length; i2 += 1) {
    const originalColumn = originalColumns[i2];
    const column = _extends({}, getDefaultColTypeDef(originalColumn.type), originalColumn);
    const field = column.field;
    if (!isGroupingColumn(field)) {
      initialColumns.set(field, column);
      const derivedColumns = getPivotDerivedColumns == null ? void 0 : getPivotDerivedColumns(column, getLocaleText);
      if (derivedColumns) {
        derivedColumns.forEach((col) => initialColumns.set(col.field, col));
      }
    }
  }
  return initialColumns;
};
var sortColumnGroups = (columnGroups, pivotModelColumns, depth = 0) => {
  var _a;
  if (depth > pivotModelColumns.length - 1) {
    return;
  }
  const sort = pivotModelColumns[depth].sort;
  if (columnGroups.length < 2) {
    if ((_a = columnGroups[0]) == null ? void 0 : _a.children) {
      sortColumnGroups(columnGroups[0].children, pivotModelColumns, depth + 1);
    }
    return;
  }
  columnGroups.sort((a, b) => {
    if (isLeaf(a) || isLeaf(b)) {
      return 0;
    }
    if (a.children) {
      sortColumnGroups(a.children, pivotModelColumns, depth + 1);
    }
    if (b.children) {
      sortColumnGroups(b.children, pivotModelColumns, depth + 1);
    }
    if (sort === void 0) {
      return 0;
    }
    return (sort === "asc" ? 1 : -1) * gridStringOrNumberComparator(a.rawHeaderName, b.rawHeaderName, {}, {});
  });
};
var getPivotForcedProps = (pivotModel, columns, groupingColDef) => {
  const visibleRows = pivotModel.rows.filter((row) => !row.hidden);
  const visibleColumns = pivotModel.columns.filter((column) => !column.hidden);
  const visibleValues = pivotModel.values.filter((value) => !value.hidden);
  const columnVisibilityModel = {};
  for (const column of columns.values()) {
    columnVisibilityModel[column.field] = false;
  }
  if (visibleColumns.length === 0) {
    visibleValues.forEach((value) => {
      delete columnVisibilityModel[value.field];
    });
  }
  const groupingColDefOverrides = (params) => _extends({}, typeof groupingColDef === "function" ? groupingColDef(params) : groupingColDef || {}, {
    filterable: false,
    aggregable: false,
    hideable: false
  });
  return {
    columnVisibilityModel,
    rowGroupingModel: visibleRows.map((row) => row.field),
    getAggregationPosition: defaultGetAggregationPosition,
    groupingColDef: groupingColDefOverrides,
    headerFilters: false,
    disableAggregation: false,
    disableRowGrouping: false
  };
};
var createPivotPropsFromRows = ({
  rows,
  columns,
  pivotModel,
  pivotingColDef,
  apiRef
}) => {
  const visibleColumns = pivotModel.columns.filter((column) => !column.hidden);
  const visibleRows = pivotModel.rows.filter((row) => !row.hidden);
  const visibleValues = pivotModel.values.filter((value) => !value.hidden);
  let pivotColumns = [];
  const pivotColumnsIncludedInPivotValues = [];
  const initialColumns = /* @__PURE__ */ new Map();
  for (const column of columns.values()) {
    if (!isGroupingColumn(column.field)) {
      initialColumns.set(column.field, column);
      const pivotValueIndex = visibleValues.findIndex(({
        field
      }) => field === column.field);
      const isVisiblePivotValueField = pivotValueIndex !== -1;
      const columnToAdd = _extends({}, column, {
        aggregable: false,
        groupable: false,
        hideable: false,
        editable: false,
        disableReorder: true
      });
      if (isVisiblePivotValueField) {
        pivotColumnsIncludedInPivotValues[pivotValueIndex] = columnToAdd;
      } else {
        pivotColumns.push(columnToAdd);
      }
    }
  }
  pivotColumns = pivotColumns.concat(pivotColumnsIncludedInPivotValues);
  const getAttributesFromInitialColumn = (field) => {
    const initialColumn = initialColumns.get(field);
    if (!initialColumn) {
      return void 0;
    }
    const attributes = {
      width: initialColumn.width,
      minWidth: initialColumn.minWidth,
      maxWidth: initialColumn.maxWidth,
      valueFormatter: initialColumn.valueFormatter,
      headerName: initialColumn.headerName,
      renderCell: initialColumn.renderCell,
      display: initialColumn.display
    };
    return attributes;
  };
  const aggregationModel = {};
  const columnGroupingModel = [];
  const columnGroupingModelLookup = /* @__PURE__ */ new Map();
  let newRows = [];
  if (visibleColumns.length === 0) {
    newRows = rows;
    visibleValues.forEach((pivotValue) => {
      aggregationModel[pivotValue.field] = pivotValue.aggFunc;
    });
  } else {
    for (let i2 = 0; i2 < rows.length; i2 += 1) {
      const row = rows[i2];
      const newRow = _extends({}, row);
      const columnGroupPath = [];
      for (let j = 0; j < visibleColumns.length; j += 1) {
        const {
          field: colGroupField
        } = visibleColumns[j];
        const depth = j;
        const column = initialColumns.get(colGroupField);
        if (!column) {
          continue;
        }
        const noValueString = "(No value)";
        let colValue = apiRef.current.getRowValue(row, column) ?? noValueString;
        if (colValue === "") {
          colValue = noValueString;
        }
        if (column.type === "singleSelect") {
          const singleSelectColumn = column;
          if (singleSelectColumn.getOptionLabel) {
            colValue = singleSelectColumn.getOptionLabel(colValue);
          }
        }
        if (column.type !== "number") {
          colValue = String(colValue);
        }
        const formattedHeaderName = apiRef.current.getRowFormattedValue(row, column) || colValue;
        columnGroupPath.push(colValue);
        const groupId = columnGroupPath.join(COLUMN_GROUP_ID_SEPARATOR);
        if (!columnGroupingModelLookup.has(groupId)) {
          const columnGroup = {
            groupId,
            headerName: formattedHeaderName,
            rawHeaderName: colValue,
            children: []
          };
          columnGroupingModelLookup.set(groupId, columnGroup);
          if (depth === 0) {
            columnGroupingModel.push(columnGroup);
          } else {
            const parentGroupId = columnGroupPath.slice(0, -1).join(COLUMN_GROUP_ID_SEPARATOR);
            const parentGroup = columnGroupingModelLookup.get(parentGroupId);
            if (parentGroup) {
              parentGroup.children.push(columnGroup);
            }
          }
        }
        const isLastColumnGroupLevel = depth === visibleColumns.length - 1;
        if (isLastColumnGroupLevel) {
          visibleValues.forEach((pivotValue) => {
            const valueField = pivotValue.field;
            const originalColumn = initialColumns.get(valueField);
            if (!originalColumn) {
              return;
            }
            const valueKey = `${columnGroupPath.join(COLUMN_GROUP_ID_SEPARATOR)}${COLUMN_GROUP_ID_SEPARATOR}${valueField}`;
            newRow[valueKey] = apiRef.current.getRowValue(row, originalColumn);
          });
        }
      }
      newRows.push(newRow);
    }
    sortColumnGroups(columnGroupingModel, visibleColumns);
  }
  function createColumns(columnGroups, depth = 0) {
    for (let i2 = 0; i2 < columnGroups.length; i2 += 1) {
      const columnGroup = columnGroups[i2];
      if (isLeaf(columnGroup)) {
        continue;
      }
      const isLastColumnGroupLevel = depth === visibleColumns.length - 1;
      if (isLastColumnGroupLevel) {
        if (visibleValues.length === 0) {
          const emptyColumnField = `${columnGroup.groupId}${COLUMN_GROUP_ID_SEPARATOR}empty`;
          const emptyColumn = {
            field: emptyColumnField,
            headerName: "",
            sortable: false,
            filterable: false,
            groupable: false,
            aggregable: false,
            hideable: false,
            disableColumnMenu: true
          };
          pivotColumns.push(emptyColumn);
          if (columnGroup) {
            columnGroup.children.push({
              field: emptyColumnField
            });
          }
        } else {
          visibleValues.forEach((pivotValue) => {
            const valueField = pivotValue.field;
            const mapValueKey = `${columnGroup.groupId}${COLUMN_GROUP_ID_SEPARATOR}${valueField}`;
            const overrides = typeof pivotingColDef === "function" ? pivotingColDef(valueField, columnGroup.groupId.split(COLUMN_GROUP_ID_SEPARATOR)) : pivotingColDef;
            const column = _extends({
              headerName: String(valueField)
            }, getAttributesFromInitialColumn(pivotValue.field), overrides, {
              // pivoting values are always numbers
              type: "number",
              field: mapValueKey,
              aggregable: false,
              groupable: false,
              filterable: false,
              hideable: false,
              editable: false,
              disableReorder: true,
              availableAggregationFunctions: [pivotValue.aggFunc]
            });
            pivotColumns.push(column);
            aggregationModel[mapValueKey] = pivotValue.aggFunc;
            if (columnGroup) {
              columnGroup.children.push({
                field: mapValueKey
              });
            }
          });
        }
      } else {
        createColumns(columnGroup.children, depth + 1);
      }
    }
  }
  createColumns(columnGroupingModel);
  return {
    rows: visibleRows.length > 0 ? newRows : [],
    columns: pivotColumns,
    columnGroupingModel,
    aggregationModel
  };
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/pivoting/useGridPivoting.js
var import_jsx_runtime263 = __toESM(require_jsx_runtime(), 1);
var emptyPivotModel = {
  rows: [],
  columns: [],
  values: []
};
var pivotingStateInitializer = (state, props, apiRef) => {
  var _a, _b, _c, _d, _e, _f;
  apiRef.current.caches.pivoting = {
    exportedStateRef: {
      current: null
    },
    nonPivotDataRef: {
      current: void 0
    }
  };
  if (props.disablePivoting) {
    return _extends({}, state, {
      pivoting: {
        active: false,
        model: emptyPivotModel
      }
    });
  }
  const initialColumns = getInitialColumns(props.columns ?? [], props.getPivotDerivedColumns, apiRef.current.getLocaleText);
  const open = props.pivotPanelOpen ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.pivoting) == null ? void 0 : _b.panelOpen) ?? false;
  const sidebarStateUpdate = open ? {
    open,
    value: GridSidebarValue.Pivot
  } : {};
  return _extends({}, state, {
    pivoting: {
      active: props.pivotActive ?? ((_d = (_c = props.initialState) == null ? void 0 : _c.pivoting) == null ? void 0 : _d.enabled) ?? false,
      model: props.pivotModel ?? ((_f = (_e = props.initialState) == null ? void 0 : _e.pivoting) == null ? void 0 : _f.model) ?? emptyPivotModel,
      initialColumns
    },
    sidebar: _extends({}, state.sidebar, sidebarStateUpdate)
  });
};
var useGridPivoting = (apiRef, props, originalColumnsProp, originalRowsProp) => {
  const isPivotActive = useGridSelector(apiRef, gridPivotActiveSelector);
  const isLoading = props.loading ?? gridRowsLoadingSelector(apiRef);
  const {
    exportedStateRef,
    nonPivotDataRef
  } = apiRef.current.caches.pivoting;
  const isPivotingAvailable = !props.disablePivoting;
  apiRef.current.registerControlState({
    stateId: "pivotModel",
    propModel: props.pivotModel,
    propOnChange: props.onPivotModelChange,
    stateSelector: gridPivotModelSelector,
    changeEvent: "pivotModelChange"
  });
  apiRef.current.registerControlState({
    stateId: "pivotMode",
    propModel: props.pivotActive,
    propOnChange: props.onPivotActiveChange,
    stateSelector: gridPivotActiveSelector,
    changeEvent: "pivotModeChange"
  });
  apiRef.current.registerControlState({
    stateId: "pivotPanelOpen",
    propModel: props.pivotPanelOpen,
    propOnChange: props.onPivotPanelOpenChange,
    stateSelector: gridPivotPanelOpenSelector,
    changeEvent: "pivotPanelOpenChange"
  });
  const getInitialData = React358.useCallback(() => {
    if (!exportedStateRef.current) {
      exportedStateRef.current = apiRef.current.exportState();
    }
    let rows = [];
    if (!props.dataSource) {
      const rowIds = gridDataRowIdsSelector(apiRef);
      const rowsLookup = gridRowsLookupSelector(apiRef);
      rows = rowIds.map((id) => rowsLookup[id]);
    }
    const initialColumns = getInitialColumns(originalColumnsProp, props.getPivotDerivedColumns, apiRef.current.getLocaleText);
    return {
      rows,
      columns: initialColumns,
      originalRowsProp
    };
  }, [apiRef, props.getPivotDerivedColumns, originalColumnsProp, originalRowsProp, exportedStateRef, props.dataSource]);
  const computePivotingState = React358.useCallback(({
    active,
    model: pivotModel
  }) => {
    if (active && pivotModel) {
      const {
        rows,
        columns
      } = nonPivotDataRef.current || {
        rows: [],
        columns: /* @__PURE__ */ new Map()
      };
      let propsOverrides = getPivotForcedProps(pivotModel, columns, props.groupingColDef);
      if (!isLoading && !props.dataSource) {
        propsOverrides = _extends({}, propsOverrides, createPivotPropsFromRows({
          rows,
          columns,
          pivotModel,
          pivotingColDef: props.pivotingColDef,
          apiRef
        }));
      }
      return {
        initialColumns: columns,
        propsOverrides
      };
    }
    return {};
  }, [apiRef, isLoading, props.dataSource, props.pivotingColDef, props.groupingColDef, nonPivotDataRef]);
  useOnMount(() => {
    if (!isPivotingAvailable || !isPivotActive) {
      return void 0;
    }
    nonPivotDataRef.current = getInitialData();
    apiRef.current.setState((state) => {
      const {
        initialColumns,
        propsOverrides
      } = computePivotingState(state.pivoting);
      const pivotingState = _extends({}, state.pivoting, {
        initialColumns: initialColumns || state.pivoting.initialColumns,
        propsOverrides: _extends({}, state.pivoting.propsOverrides, propsOverrides)
      });
      return _extends({}, state, {
        pivoting: pivotingState
      });
    });
    return void 0;
  });
  useEnhancedEffect_default(() => {
    if (!isPivotingAvailable || !isPivotActive) {
      if (nonPivotDataRef.current) {
        apiRef.current.caches.rows.rowsBeforePartialUpdates = nonPivotDataRef.current.originalRowsProp;
        apiRef.current.setRows(nonPivotDataRef.current.rows);
        nonPivotDataRef.current = void 0;
      }
      if (exportedStateRef.current) {
        apiRef.current.restoreState(exportedStateRef.current);
        exportedStateRef.current = null;
      }
    }
  }, [isPivotActive, apiRef, isPivotingAvailable, nonPivotDataRef, exportedStateRef]);
  const setPivotModel = React358.useCallback((callback) => {
    if (!isPivotingAvailable) {
      return;
    }
    apiRef.current.setState((state) => {
      var _a, _b;
      const newPivotModel = typeof callback === "function" ? callback((_a = state.pivoting) == null ? void 0 : _a.model) : callback;
      if (((_b = state.pivoting) == null ? void 0 : _b.model) === newPivotModel) {
        return state;
      }
      const {
        initialColumns,
        propsOverrides
      } = computePivotingState(_extends({}, state.pivoting, {
        model: newPivotModel
      }));
      const newPivotingState = _extends({}, state.pivoting, {
        initialColumns: initialColumns || state.pivoting.initialColumns,
        propsOverrides: _extends({}, state.pivoting.propsOverrides, propsOverrides),
        model: newPivotModel
      });
      return _extends({}, state, {
        pivoting: newPivotingState
      });
    });
  }, [apiRef, computePivotingState, isPivotingAvailable]);
  const updatePivotModel = React358.useCallback(({
    field,
    targetSection,
    originSection,
    targetField,
    targetFieldPosition
  }) => {
    if (field === targetField) {
      return;
    }
    apiRef.current.setPivotModel((prev) => {
      var _a, _b, _c;
      const newModel = _extends({}, prev);
      const isSameSection = targetSection === originSection;
      const hidden = originSection === null ? false : ((_a = prev[originSection].find((item) => item.field === field)) == null ? void 0 : _a.hidden) ?? false;
      if (targetSection) {
        const newSectionArray = [...prev[targetSection]];
        let toIndex = newSectionArray.length;
        if (targetField) {
          const fromIndex = newSectionArray.findIndex((item) => item.field === field);
          if (fromIndex > -1) {
            newSectionArray.splice(fromIndex, 1);
          }
          toIndex = newSectionArray.findIndex((item) => item.field === targetField);
          if (targetFieldPosition === "bottom") {
            toIndex += 1;
          }
        }
        if (targetSection === "values") {
          const initialColumns = gridPivotInitialColumnsSelector(apiRef);
          const aggFunc = isSameSection ? (_b = prev.values.find((item) => item.field === field)) == null ? void 0 : _b.aggFunc : getAvailableAggregationFunctions({
            aggregationFunctions: props.aggregationFunctions,
            colDef: initialColumns.get(field),
            isDataSource: !!props.dataSource
          })[0];
          newSectionArray.splice(toIndex, 0, {
            field,
            aggFunc,
            hidden
          });
          newModel.values = newSectionArray;
        } else if (targetSection === "columns") {
          const sort = isSameSection ? (_c = prev.columns.find((item) => item.field === field)) == null ? void 0 : _c.sort : void 0;
          newSectionArray.splice(toIndex, 0, {
            field,
            sort,
            hidden
          });
          newModel.columns = newSectionArray;
        } else if (targetSection === "rows") {
          newSectionArray.splice(toIndex, 0, {
            field,
            hidden
          });
          newModel.rows = newSectionArray;
        }
      }
      if (!isSameSection && originSection) {
        newModel[originSection] = prev[originSection].filter((f) => f.field !== field);
      }
      return newModel;
    });
  }, [apiRef, props.aggregationFunctions, props.dataSource]);
  const setPivotActive = React358.useCallback((callback) => {
    if (!isPivotingAvailable) {
      return;
    }
    apiRef.current.setState((state) => {
      var _a, _b;
      const newPivotMode = typeof callback === "function" ? callback((_a = state.pivoting) == null ? void 0 : _a.active) : callback;
      if (((_b = state.pivoting) == null ? void 0 : _b.active) === newPivotMode) {
        return state;
      }
      if (newPivotMode) {
        nonPivotDataRef.current = getInitialData();
      }
      const {
        initialColumns,
        propsOverrides
      } = computePivotingState(_extends({}, state.pivoting, {
        active: newPivotMode
      }));
      const newPivotingState = _extends({}, state.pivoting, {
        initialColumns: initialColumns || state.pivoting.initialColumns,
        propsOverrides: _extends({}, state.pivoting.propsOverrides, propsOverrides),
        active: newPivotMode
      });
      const newState = _extends({}, state, {
        pivoting: newPivotingState
      });
      return newState;
    });
    apiRef.current.selectRows([], false, true);
  }, [apiRef, computePivotingState, getInitialData, isPivotingAvailable, nonPivotDataRef]);
  const setPivotPanelOpen = React358.useCallback((callback) => {
    if (!isPivotingAvailable) {
      return;
    }
    const panelOpen = gridPivotPanelOpenSelector(apiRef);
    const newPanelOpen = typeof callback === "function" ? callback(panelOpen) : callback;
    if (panelOpen === newPanelOpen) {
      return;
    }
    if (newPanelOpen) {
      apiRef.current.showSidebar(GridSidebarValue.Pivot);
    } else {
      apiRef.current.hideSidebar();
    }
  }, [apiRef, isPivotingAvailable]);
  const addColumnMenuButton = React358.useCallback((menuItems) => {
    if (isPivotingAvailable) {
      return [...menuItems, "columnMenuManagePanelItem"];
    }
    return menuItems;
  }, [isPivotingAvailable]);
  useGridRegisterPipeProcessor(apiRef, "columnMenu", addColumnMenuButton);
  const updateNonPivotColumns = React358.useCallback((columns, keepPreviousColumns = true) => {
    if (!nonPivotDataRef.current || !isPivotingAvailable) {
      return;
    }
    if (keepPreviousColumns) {
      getInitialColumns(columns, props.getPivotDerivedColumns, apiRef.current.getLocaleText).forEach((col) => {
        nonPivotDataRef.current.columns.set(col.field, col);
      });
    } else {
      nonPivotDataRef.current.columns = getInitialColumns(columns, props.getPivotDerivedColumns, apiRef.current.getLocaleText);
    }
    apiRef.current.setState((state) => {
      var _a;
      const {
        propsOverrides
      } = computePivotingState(state.pivoting);
      return _extends({}, state, {
        pivoting: _extends({}, state.pivoting, {
          initialColumns: (_a = nonPivotDataRef.current) == null ? void 0 : _a.columns,
          propsOverrides: _extends({}, state.pivoting.propsOverrides, propsOverrides)
        })
      });
    });
  }, [isPivotingAvailable, apiRef, props.getPivotDerivedColumns, computePivotingState, nonPivotDataRef]);
  const updateNonPivotRows = React358.useCallback((rows, keepPreviousRows = true) => {
    if (!nonPivotDataRef.current || props.dataSource || !isPivotingAvailable || !rows || rows.length === 0) {
      return;
    }
    if (keepPreviousRows) {
      const rowsMap = /* @__PURE__ */ new Map();
      nonPivotDataRef.current.rows.forEach((row) => {
        rowsMap.set(gridRowIdSelector(apiRef, row), row);
      });
      rows.forEach((row) => {
        const rowId = gridRowIdSelector(apiRef, row);
        if (row._action === "delete") {
          rowsMap.delete(rowId);
        } else {
          rowsMap.set(rowId, row);
        }
      });
      nonPivotDataRef.current.rows = Array.from(rowsMap.values());
    } else {
      nonPivotDataRef.current.rows = rows;
    }
    apiRef.current.setState((state) => {
      const {
        initialColumns,
        propsOverrides
      } = computePivotingState(state.pivoting);
      return _extends({}, state, {
        pivoting: _extends({}, state.pivoting, {
          initialColumns: initialColumns || state.pivoting.initialColumns,
          propsOverrides: _extends({}, state.pivoting.propsOverrides, propsOverrides)
        })
      });
    });
  }, [apiRef, computePivotingState, isPivotingAvailable, nonPivotDataRef, props.dataSource]);
  const addPivotingPanel = React358.useCallback((initialValue, value) => {
    if (isPivotingAvailable && value === GridSidebarValue.Pivot) {
      return (0, import_jsx_runtime263.jsx)(GridPivotPanel, {});
    }
    return initialValue;
  }, [isPivotingAvailable]);
  const addGetRowsParams = React358.useCallback((params) => {
    if (!isPivotingAvailable || !isPivotActive) {
      return params;
    }
    const pivotModel = gridPivotModelSelector(apiRef);
    const visibleColumns = pivotModel.columns.filter((column) => !column.hidden);
    const visibleRows = pivotModel.rows.filter((row) => !row.hidden);
    const visibleValues = pivotModel.values.filter((value) => !value.hidden);
    return _extends({}, params, {
      pivotModel: {
        columns: visibleColumns,
        rows: visibleRows,
        values: visibleValues
      }
    });
  }, [apiRef, isPivotingAvailable, isPivotActive]);
  useGridRegisterPipeProcessor(apiRef, "sidebar", addPivotingPanel);
  useGridRegisterPipeProcessor(apiRef, "getRowsParams", addGetRowsParams);
  useGridApiMethod(apiRef, {
    setPivotModel,
    setPivotActive,
    setPivotPanelOpen
  }, "public");
  useGridApiMethod(apiRef, {
    updatePivotModel,
    updateNonPivotColumns,
    updateNonPivotRows
  }, "private");
  useEnhancedEffect_default(() => {
    apiRef.current.updateNonPivotColumns(originalColumnsProp, false);
  }, [originalColumnsProp, apiRef]);
  useEnhancedEffect_default(() => {
    apiRef.current.updateNonPivotRows(originalRowsProp, false);
    if (nonPivotDataRef.current) {
      nonPivotDataRef.current.originalRowsProp = originalRowsProp;
    }
  }, [originalRowsProp, apiRef, nonPivotDataRef]);
  useEnhancedEffect_default(() => {
    if (props.pivotModel !== void 0) {
      apiRef.current.setPivotModel(props.pivotModel);
    }
  }, [apiRef, props.pivotModel]);
  useEnhancedEffect_default(() => {
    if (props.pivotActive !== void 0) {
      apiRef.current.setPivotActive(props.pivotActive);
    }
  }, [apiRef, props.pivotActive]);
  useEnhancedEffect_default(() => {
    if (props.pivotPanelOpen !== void 0) {
      apiRef.current.setPivotPanelOpen(props.pivotPanelOpen);
    }
  }, [apiRef, props.pivotPanelOpen]);
};
var useGridPivotingExportState = (apiRef) => {
  const stateExportPreProcessing = React358.useCallback((state) => {
    const isPivotActive = gridPivotActiveSelector(apiRef);
    if (!isPivotActive) {
      return state;
    }
    const newState = _extends({}, state, apiRef.current.caches.pivoting.exportedStateRef.current, {
      sorting: state.sorting
    });
    return newState;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aiAssistant/useGridAiAssistant.js
var React359 = __toESM(require_react(), 1);
var import_jsx_runtime264 = __toESM(require_jsx_runtime(), 1);
var DEFAULT_SAMPLE_COUNT = 5;
var MAX_CHART_DATA_POINTS = 1e3;
var aiAssistantStateInitializer = (state, props) => {
  var _a, _b;
  if (!props.aiAssistant) {
    return _extends({}, state, {
      aiAssistant: {
        activeConversationIndex: 0,
        conversations: []
      }
    });
  }
  return _extends({}, state, {
    aiAssistant: {
      activeConversationIndex: 0,
      conversations: props.aiAssistantConversations ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.aiAssistant) == null ? void 0 : _b.conversations) ?? []
    }
  });
};
var useGridAiAssistant = (apiRef, props) => {
  const {
    onPrompt,
    allowAiAssistantDataSampling,
    slots,
    rowSelection,
    disableColumnFilter,
    disableRowGrouping,
    disableAggregation,
    disableColumnSorting,
    disablePivoting,
    chartsIntegration,
    experimentalFeatures,
    getPivotDerivedColumns
  } = props;
  const previousUnwrappedGroupingModel = React359.useRef([]);
  const activeChartId = gridChartsIntegrationActiveChartIdSelector(apiRef);
  const columnsLookup = gridColumnLookupSelector(apiRef);
  const columns = Object.values(columnsLookup);
  const rows = Object.values(gridRowsLookupSelector(apiRef));
  const isAiAssistantAvailable = !!props.aiAssistant;
  apiRef.current.registerControlState({
    stateId: "aiAssistantConversations",
    propModel: props.aiAssistantConversations,
    propOnChange: props.onAiAssistantConversationsChange,
    stateSelector: gridAiAssistantConversationsSelector,
    changeEvent: "aiAssistantConversationsChange"
  });
  apiRef.current.registerControlState({
    stateId: "aiAssistantActiveConversationIndex",
    propModel: props.aiAssistantActiveConversationIndex,
    propOnChange: props.onAiAssistantActiveConversationIndexChange,
    stateSelector: gridAiAssistantActiveConversationIndexSelector,
    changeEvent: "aiAssistantActiveConversationIndexChange"
  });
  const preferencePanelPreProcessing = React359.useCallback((initialValue, value) => {
    if (isAiAssistantAvailable && slots.aiAssistantPanel && value === GridPreferencePanelsValue.aiAssistant) {
      return (0, import_jsx_runtime264.jsx)(slots.aiAssistantPanel, {});
    }
    return initialValue;
  }, [isAiAssistantAvailable, slots]);
  const collectSampleData = React359.useCallback(() => {
    const columnExamples = {};
    columns.forEach((column) => {
      columnExamples[column.field] = Array.from({
        length: Math.min(DEFAULT_SAMPLE_COUNT, rows.length)
      }).map(() => {
        const row = rows[Math.floor(Math.random() * rows.length)];
        return apiRef.current.getRowValue(row, column);
      });
    });
    return columnExamples;
  }, [apiRef, columns, rows]);
  const getPromptContext = React359.useCallback((allowDataSampling = false) => {
    if (!isAiAssistantAvailable) {
      return "";
    }
    const examples = allowDataSampling ? collectSampleData() : {};
    const columnsContext = columns.reduce((acc, column) => {
      var _a;
      const columnContextWithoutExamples = {
        field: column.field,
        description: column.description ?? null,
        examples: [],
        type: column.type ?? "string",
        allowedOperators: ((_a = column.filterOperators) == null ? void 0 : _a.map((operator) => operator.value)) ?? []
      };
      acc.push(_extends({}, columnContextWithoutExamples, {
        examples: examples[column.field] ?? column.examples ?? []
      }));
      if (disablePivoting) {
        return acc;
      }
      ((getPivotDerivedColumns == null ? void 0 : getPivotDerivedColumns(column, apiRef.current.getLocaleText)) || []).forEach((col) => acc.push(_extends({}, columnContextWithoutExamples, col, {
        derivedFrom: column.field
      })));
      return acc;
    }, []);
    return JSON.stringify(columnsContext);
  }, [apiRef, columns, collectSampleData, getPivotDerivedColumns, isAiAssistantAvailable, disablePivoting]);
  const updateChart = React359.useCallback((result) => {
    if (!result.chart) {
      return;
    }
    apiRef.current.updateChartDimensionsData(activeChartId, result.chart.dimensions.map((item) => ({
      field: item
    })));
    apiRef.current.updateChartValuesData(activeChartId, result.chart.values.map((item) => ({
      field: item
    })));
  }, [apiRef, activeChartId]);
  const applyPromptResult = React359.useCallback((result) => {
    if (!isAiAssistantAvailable) {
      return;
    }
    const interestColumns = [];
    if (!disableColumnFilter) {
      apiRef.current.setFilterModel({
        items: result.filters.map((filter, index) => {
          const item = {
            id: index,
            field: filter.column,
            operator: filter.operator,
            value: filter.value
          };
          const column = columnsLookup[filter.column];
          if (column.type === "singleSelect") {
            const options3 = getValueOptions(column) ?? [];
            const found = options3.find((option) => typeof option === "object" && option.label === filter.value);
            if (found) {
              item.value = found.value;
            }
          }
          return item;
        }),
        logicOperator: result.filterOperator ?? GridLogicOperator.And,
        quickFilterValues: []
      });
      interestColumns.push(...result.filters.map((f) => f.column));
    } else {
      result.filters = [];
    }
    let appliedPivoting = false;
    if (!disablePivoting && "columns" in result.pivoting) {
      apiRef.current.setPivotActive(true);
      apiRef.current.setPivotModel({
        columns: result.pivoting.columns.map((c) => ({
          field: c.column,
          sort: c.direction
        })),
        rows: result.pivoting.rows.map((r) => ({
          field: r
        })),
        values: result.pivoting.values.map((valueObj) => {
          const [field] = Object.keys(valueObj);
          return {
            field,
            aggFunc: valueObj[field]
          };
        })
      });
      appliedPivoting = true;
    } else if ("columns" in result.pivoting) {
      apiRef.current.setPivotActive(false);
      result.pivoting.columns.forEach((c) => {
        result.grouping.push({
          column: c.column
        });
      });
      result.pivoting.rows.forEach((r) => {
        result.grouping.push({
          column: r
        });
      });
      result.pivoting.values.forEach((valueObj) => {
        const [field] = Object.keys(valueObj);
        result.aggregation[field] = valueObj[field];
      });
      result.pivoting = {};
    } else {
      apiRef.current.setPivotActive(false);
    }
    if (!disableRowGrouping && !appliedPivoting) {
      apiRef.current.setRowGroupingModel(result.grouping.map((g) => g.column));
    } else {
      result.grouping = [];
    }
    if (!disableAggregation && !appliedPivoting) {
      apiRef.current.setAggregationModel(result.aggregation);
      interestColumns.push(...Object.keys(result.aggregation));
    } else {
      result.aggregation = {};
    }
    if (!disableColumnSorting) {
      apiRef.current.setSortModel(result.sorting.map((s) => ({
        field: s.column,
        sort: s.direction
      })));
    } else {
      result.sorting = [];
    }
    if ((experimentalFeatures == null ? void 0 : experimentalFeatures.charts) && chartsIntegration && activeChartId && result.chart) {
      if (appliedPivoting) {
        const unsubscribe = apiRef.current.subscribeEvent("rowsSet", () => {
          const unwrappedGroupingModel = Object.keys(gridColumnGroupsUnwrappedModelSelector(apiRef));
          if (!result.chart || unwrappedGroupingModel.length === 0 || isDeepEqual(previousUnwrappedGroupingModel.current, unwrappedGroupingModel)) {
            return;
          }
          previousUnwrappedGroupingModel.current = unwrappedGroupingModel;
          const visibleRowsCount = gridVisibleRowsSelector(apiRef).rows.length;
          const maxColumns = Math.floor(MAX_CHART_DATA_POINTS / visibleRowsCount);
          result.chart.values = unwrappedGroupingModel.slice(0, maxColumns);
          updateChart(result);
          unsubscribe();
        });
      } else {
        updateChart(result);
      }
    }
    const visibleRowsData = getVisibleRows(apiRef);
    const rowSelectionModel = {
      type: "include",
      ids: /* @__PURE__ */ new Set()
    };
    const selection = rowSelection ? result.select : -1;
    if (selection !== -1) {
      for (let i2 = 0; i2 < result.select; i2 += 1) {
        const row = visibleRowsData.rows[i2];
        const id = apiRef.current.getRowId(row);
        rowSelectionModel.ids.add(id);
      }
    }
    apiRef.current.setRowSelectionModel(rowSelectionModel);
    const targetIndex = Number(columnsLookup[GRID_CHECKBOX_SELECTION_FIELD] !== void 0) + Number(result.grouping.length);
    interestColumns.reverse().forEach((c) => apiRef.current.setColumnIndex(c, targetIndex));
  }, [apiRef, updateChart, rowSelection, disableColumnFilter, disableRowGrouping, disableAggregation, disableColumnSorting, disablePivoting, columnsLookup, isAiAssistantAvailable, activeChartId, chartsIntegration, experimentalFeatures == null ? void 0 : experimentalFeatures.charts]);
  const setActiveConversationId = React359.useCallback((id) => {
    if (!isAiAssistantAvailable) {
      return;
    }
    const conversations = gridAiAssistantConversationsSelector(apiRef);
    const activeConversationIndex = gridAiAssistantActiveConversationIndexSelector(apiRef);
    if (!conversations[activeConversationIndex]) {
      return;
    }
    conversations[activeConversationIndex].id = id;
    apiRef.current.setState((state) => _extends({}, state, {
      aiAssistant: _extends({}, state.aiAssistant, {
        conversations
      })
    }));
  }, [apiRef, isAiAssistantAvailable]);
  const setConversationPrompts = React359.useCallback((index, callback) => {
    if (!isAiAssistantAvailable) {
      return;
    }
    const currentConversations = gridAiAssistantConversationsSelector(apiRef);
    const targetConversation = currentConversations[index];
    const newPrompts = typeof callback === "function" ? callback(targetConversation === void 0 ? [] : targetConversation.prompts) : callback;
    const newConversations = currentConversations.toSpliced(targetConversation === void 0 ? currentConversations.length : index, 1, _extends({}, targetConversation, {
      title: newPrompts[newPrompts.length - 1].value,
      // TODO: make the title configurable
      prompts: newPrompts
    }));
    apiRef.current.setState((state) => _extends({}, state, {
      aiAssistant: _extends({}, state.aiAssistant, {
        conversations: newConversations
      })
    }));
  }, [apiRef, isAiAssistantAvailable]);
  const processPrompt = React359.useCallback(async (value) => {
    if (!onPrompt) {
      return void 0;
    }
    const activeConversationIndex = gridAiAssistantActiveConversationIndexSelector(apiRef);
    const activeConversation = gridAiAssistantActiveConversationSelector(apiRef);
    const date = Date.now();
    apiRef.current.setLoading(true);
    setConversationPrompts(activeConversationIndex, (prevPrompts) => [...prevPrompts, {
      value,
      createdAt: new Date(date),
      variant: "processing",
      helperText: apiRef.current.getLocaleText("promptProcessing")
    }]);
    try {
      const response = await onPrompt(value, getPromptContext(allowAiAssistantDataSampling), activeConversation == null ? void 0 : activeConversation.id);
      applyPromptResult(response);
      setActiveConversationId(response.conversationId);
      setConversationPrompts(activeConversationIndex, (prevPrompts) => prevPrompts.map((item) => item.createdAt.getTime() === date ? _extends({}, item, {
        response,
        variant: "success",
        helperText: ""
      }) : item));
      return response;
    } catch (error) {
      setConversationPrompts(activeConversationIndex, (prevPrompts) => prevPrompts.map((item) => item.createdAt.getTime() === date ? _extends({}, item, {
        variant: "error",
        helperText: error.message
      }) : item));
      return error;
    } finally {
      apiRef.current.setLoading(false);
    }
  }, [apiRef, allowAiAssistantDataSampling, onPrompt, getPromptContext, applyPromptResult, setConversationPrompts, setActiveConversationId]);
  const setActiveConversationIndex = React359.useCallback((index) => {
    apiRef.current.setState((state) => _extends({}, state, {
      aiAssistant: _extends({}, state.aiAssistant, {
        activeConversationIndex: index
      })
    }));
    const conversation = gridAiAssistantActiveConversationSelector(apiRef);
    if (!conversation) {
      throw new Error("Conversation not found");
    }
    return conversation;
  }, [apiRef]);
  const setConversations = React359.useCallback((callback) => {
    if (!isAiAssistantAvailable) {
      return;
    }
    apiRef.current.setState((state) => {
      var _a;
      return _extends({}, state, {
        aiAssistant: _extends({}, state.aiAssistant, {
          conversations: typeof callback === "function" ? callback((_a = state.aiAssistant) == null ? void 0 : _a.conversations) : callback
        })
      });
    });
  }, [apiRef, isAiAssistantAvailable]);
  React359.useEffect(() => {
    if (props.aiAssistantConversations) {
      setConversations(props.aiAssistantConversations);
    }
  }, [apiRef, props.aiAssistantConversations, setConversations]);
  React359.useEffect(() => {
    if (props.aiAssistantActiveConversationIndex) {
      setActiveConversationIndex(props.aiAssistantActiveConversationIndex);
    }
  }, [apiRef, props.aiAssistantActiveConversationIndex, setActiveConversationIndex]);
  useGridRegisterPipeProcessor(apiRef, "preferencePanel", preferencePanelPreProcessing);
  useGridApiMethod(apiRef, {
    aiAssistant: {
      processPrompt,
      setConversations,
      setActiveConversationIndex
    }
  }, "public");
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/sidebar/useGridSidebar.js
var React360 = __toESM(require_react(), 1);
var sidebarStateInitializer = (state, props) => {
  var _a;
  return _extends({}, state, {
    sidebar: ((_a = props.initialState) == null ? void 0 : _a.sidebar) ?? {
      open: false
    }
  });
};
var useGridSidebar = (apiRef, props) => {
  var _a;
  const hideSidebar = React360.useCallback(() => {
    apiRef.current.setState((state) => {
      if (!state.sidebar.open || !state.sidebar.value) {
        return state;
      }
      apiRef.current.publishEvent("sidebarClose", {
        value: state.sidebar.value
      });
      return _extends({}, state, {
        sidebar: {
          open: false
        }
      });
    });
  }, [apiRef]);
  const showSidebar = React360.useCallback((newValue, sidebarId, labelId) => {
    apiRef.current.setState((state) => _extends({}, state, {
      sidebar: _extends({}, state.sidebar, {
        open: true,
        value: newValue,
        sidebarId,
        labelId
      })
    }));
    apiRef.current.publishEvent("sidebarOpen", {
      value: newValue
    });
  }, [apiRef]);
  useGridApiMethod(apiRef, {
    showSidebar,
    hideSidebar
  }, "public");
  const stateExportPreProcessing = React360.useCallback((prevState, context) => {
    var _a2;
    const sidebarToExport = gridSidebarStateSelector(apiRef);
    const shouldExportSidebar = (
      // Always export if the `exportOnlyDirtyModels` property is not activated
      !context.exportOnlyDirtyModels || // Always export if the sidebar was initialized
      ((_a2 = props.initialState) == null ? void 0 : _a2.sidebar) != null || // Always export if the sidebar is opened
      sidebarToExport.open
    );
    if (!shouldExportSidebar) {
      return prevState;
    }
    return _extends({}, prevState, {
      sidebar: sidebarToExport
    });
  }, [apiRef, (_a = props.initialState) == null ? void 0 : _a.sidebar]);
  const stateRestorePreProcessing = React360.useCallback((params, context) => {
    const sidebar = context.stateToRestore.sidebar;
    if (sidebar != null) {
      apiRef.current.setState((state) => _extends({}, state, {
        sidebar
      }));
    }
    return params;
  }, [apiRef]);
  useGridRegisterPipeProcessor(apiRef, "exportState", stateExportPreProcessing);
  useGridRegisterPipeProcessor(apiRef, "restoreState", stateRestorePreProcessing);
  useGridEventPriority(apiRef, "sidebarClose", props.onSidebarClose);
  useGridEventPriority(apiRef, "sidebarOpen", props.onSidebarOpen);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/history/useGridHistory.js
var React361 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/history/gridHistorySelectors.js
var gridHistoryStateSelector = createRootSelector((state) => state.history);
var gridHistoryEnabledSelector = createSelector3(gridHistoryStateSelector, (history) => history.enabled);
var gridHistoryStackSelector = createSelector3(gridHistoryStateSelector, (history) => history.stack);
var gridHistoryCurrentPositionSelector = createSelector3(gridHistoryStateSelector, (history) => history.currentPosition);
var gridHistoryCanUndoSelector = createSelector3(gridHistoryCurrentPositionSelector, (currentPosition) => currentPosition >= 0);
var gridHistoryCanRedoSelector = createSelector3(gridHistoryStackSelector, gridHistoryCurrentPositionSelector, (stack, currentPosition) => currentPosition < stack.length - 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/history/defaultHistoryHandlers.js
var createCellEditHistoryHandler = (apiRef) => {
  return {
    store: (params) => {
      const {
        id,
        field
      } = params;
      const oldValue = apiRef.current.getRow(id)[field];
      const newValue = apiRef.current.getRowWithUpdatedValues(id, field)[field];
      if (isDeepEqual(oldValue, newValue)) {
        return null;
      }
      return {
        id,
        field,
        oldValue,
        newValue
      };
    },
    validate: (data, direction) => {
      const {
        id,
        field,
        oldValue,
        newValue
      } = data;
      if (!gridVisibleColumnFieldsSelector(apiRef).includes(field)) {
        return false;
      }
      const {
        rowIdToIndexMap,
        range
      } = gridVisibleRowsSelector(apiRef);
      const rowIndex = rowIdToIndexMap.get(id);
      if (rowIndex === void 0 || rowIndex < ((range == null ? void 0 : range.firstRowIndex) || 0) || rowIndex > ((range == null ? void 0 : range.lastRowIndex) || rowIndex)) {
        return false;
      }
      const row = apiRef.current.getRow(id);
      const currentValue = row[field];
      const expectedValue = direction === "undo" ? newValue : oldValue;
      if (!isDeepEqual(currentValue, expectedValue)) {
        return false;
      }
      return true;
    },
    undo: async (data) => {
      var _a;
      const {
        id,
        field,
        oldValue
      } = data;
      if ((_a = apiRef.current.state.props.dataSource) == null ? void 0 : _a.updateRow) {
        const row = apiRef.current.getRow(id);
        await apiRef.current.dataSource.editRow({
          rowId: id,
          updatedRow: _extends({}, row, {
            [field]: oldValue
          }),
          previousRow: row
        });
      } else {
        apiRef.current.updateRows([{
          id,
          [field]: oldValue
        }]);
      }
      requestAnimationFrame(() => {
        apiRef.current.setCellFocus(id, field);
        apiRef.current.scrollToIndexes({
          rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(id),
          colIndex: apiRef.current.getColumnIndex(field)
        });
      });
    },
    redo: async (data) => {
      var _a;
      const {
        id,
        field,
        newValue
      } = data;
      if ((_a = apiRef.current.state.props.dataSource) == null ? void 0 : _a.updateRow) {
        const row = apiRef.current.getRow(id);
        await apiRef.current.dataSource.editRow({
          rowId: id,
          updatedRow: _extends({}, row, {
            [field]: newValue
          }),
          previousRow: row
        });
      } else {
        apiRef.current.updateRows([{
          id,
          [field]: newValue
        }]);
      }
      requestAnimationFrame(() => {
        apiRef.current.setCellFocus(id, field);
        apiRef.current.scrollToIndexes({
          rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(id),
          colIndex: apiRef.current.getColumnIndex(field)
        });
      });
    }
  };
};
var createRowEditHistoryHandler = (apiRef) => {
  return {
    store: (params) => {
      const {
        id
      } = params;
      const oldRow = apiRef.current.getRow(id) || {};
      const newRow = apiRef.current.getRowWithUpdatedValues(id, "");
      if (isDeepEqual(oldRow, newRow)) {
        return null;
      }
      return {
        id,
        oldRow,
        newRow
      };
    },
    validate: (data, direction) => {
      const {
        id,
        oldRow,
        newRow
      } = data;
      const {
        rowIdToIndexMap,
        range
      } = gridVisibleRowsSelector(apiRef);
      const rowIndex = rowIdToIndexMap.get(id);
      if (rowIndex === void 0 || rowIndex < ((range == null ? void 0 : range.firstRowIndex) || 0) || rowIndex > ((range == null ? void 0 : range.lastRowIndex) || rowIndex)) {
        return false;
      }
      const row = apiRef.current.getRow(id);
      const expectedRow = direction === "undo" ? newRow : oldRow;
      for (const field of Object.keys(expectedRow)) {
        if (!isDeepEqual(row[field], expectedRow[field])) {
          return false;
        }
      }
      return true;
    },
    undo: async (data) => {
      var _a;
      const {
        id,
        oldRow,
        newRow
      } = data;
      if ((_a = apiRef.current.state.props.dataSource) == null ? void 0 : _a.updateRow) {
        await apiRef.current.dataSource.editRow({
          rowId: id,
          updatedRow: oldRow,
          previousRow: newRow
        });
      } else {
        apiRef.current.updateRows([_extends({
          id
        }, oldRow)]);
      }
      requestAnimationFrame(() => {
        apiRef.current.setCellFocus(id, Object.keys(oldRow)[0]);
        apiRef.current.scrollToIndexes({
          rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(id),
          colIndex: 0
        });
      });
    },
    redo: async (data) => {
      var _a;
      const {
        id,
        oldRow,
        newRow
      } = data;
      if ((_a = apiRef.current.state.props.dataSource) == null ? void 0 : _a.updateRow) {
        await apiRef.current.dataSource.editRow({
          rowId: id,
          updatedRow: newRow,
          previousRow: oldRow
        });
      } else {
        apiRef.current.updateRows([_extends({
          id
        }, newRow)]);
      }
      requestAnimationFrame(() => {
        apiRef.current.setCellFocus(id, Object.keys(newRow)[0]);
        apiRef.current.scrollToIndexes({
          rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(id),
          colIndex: 0
        });
      });
    }
  };
};
var createClipboardPasteHistoryHandler = (apiRef) => {
  return {
    store: (params) => params,
    validate: (data, direction) => {
      const {
        oldRows,
        newRows
      } = data;
      const updatedRowIds = Array.from(newRows.keys());
      if (updatedRowIds.length === 0) {
        return false;
      }
      const {
        rowIdToIndexMap,
        range
      } = gridVisibleRowsSelector(apiRef);
      for (let i2 = 0; i2 < updatedRowIds.length; i2 += 1) {
        const rowId = updatedRowIds[i2];
        const rowIndex = rowIdToIndexMap.get(rowId);
        if (rowIndex === void 0 || rowIndex < ((range == null ? void 0 : range.firstRowIndex) || 0) || rowIndex > ((range == null ? void 0 : range.lastRowIndex) || rowIndex)) {
          return false;
        }
        const row = apiRef.current.getRow(rowId);
        if (!row) {
          return false;
        }
        const expectedRow = direction === "undo" ? newRows.get(rowId) : oldRows.get(rowId);
        for (const field of Object.keys(expectedRow)) {
          if (!isDeepEqual(row[field], expectedRow[field])) {
            return false;
          }
        }
      }
      return true;
    },
    undo: async (data) => {
      const {
        oldRows,
        newRows
      } = data;
      const oldRowsValues = Array.from(oldRows.values());
      const visibleColumns = apiRef.current.getVisibleColumns();
      if (oldRowsValues.length > 0 && visibleColumns.length > 0) {
        const columnOrder = gridColumnFieldsSelector(apiRef);
        const firstOldRow = Array.from(newRows.values())[0];
        const [firstNewRowId, firstNewRow] = Array.from(oldRows.entries())[0];
        let differentFieldIndex = columnOrder.length - 1;
        for (let i2 = 0; i2 < columnOrder.length; i2 += 1) {
          const field = columnOrder[i2];
          if (!isDeepEqual(firstOldRow[field], firstNewRow[field])) {
            differentFieldIndex = i2;
            break;
          }
        }
        apiRef.current.updateRows(oldRowsValues);
        if (differentFieldIndex >= 0) {
          requestAnimationFrame(() => {
            apiRef.current.setCellFocus(firstNewRowId, columnOrder[differentFieldIndex]);
            apiRef.current.scrollToIndexes({
              rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(firstNewRowId),
              colIndex: differentFieldIndex
            });
          });
        }
      }
    },
    redo: async (data) => {
      const {
        oldRows,
        newRows
      } = data;
      const newRowsValues = Array.from(newRows.values());
      const visibleColumns = apiRef.current.getVisibleColumns();
      if (newRowsValues.length > 0 && visibleColumns.length > 0) {
        const columnOrder = gridColumnFieldsSelector(apiRef);
        const firstOldRow = Array.from(oldRows.values())[0];
        const [firstNewRowId, firstNewRow] = Array.from(newRows.entries())[0];
        let differentFieldIndex = columnOrder.length - 1;
        for (let i2 = 0; i2 < columnOrder.length; i2 += 1) {
          const field = columnOrder[i2];
          if (!isDeepEqual(firstOldRow[field], firstNewRow[field])) {
            differentFieldIndex = i2;
            break;
          }
        }
        apiRef.current.updateRows(newRowsValues);
        if (differentFieldIndex >= 0) {
          requestAnimationFrame(() => {
            apiRef.current.setCellFocus(firstNewRowId, columnOrder[differentFieldIndex]);
            apiRef.current.scrollToIndexes({
              rowIndex: apiRef.current.getRowIndexRelativeToVisibleRows(firstNewRowId),
              colIndex: differentFieldIndex
            });
          });
        }
      }
    }
  };
};
var createDefaultHistoryHandlers = (apiRef, props) => {
  const handlers = {};
  const canHaveEditing = props.isCellEditable || props.columns.some((col) => col.editable);
  if (!canHaveEditing) {
    return handlers;
  }
  if (!props.dataSource || props.dataSource.updateRow) {
    handlers.cellEditStop = createCellEditHistoryHandler(apiRef);
    handlers.rowEditStop = createRowEditHistoryHandler(apiRef);
  }
  if (!props.dataSource) {
    handlers.clipboardPasteEnd = createClipboardPasteHistoryHandler(apiRef);
  }
  return handlers;
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/history/useGridHistory.js
var historyStateInitializer = (state) => {
  return _extends({}, state, {
    history: {
      stack: [],
      currentPosition: -1,
      enabled: false
    }
  });
};
var useGridHistory = (apiRef, props) => {
  const {
    historyStackSize,
    onUndo,
    onRedo,
    historyValidationEvents
  } = props;
  const historyEventHandlers = React361.useMemo(() => {
    if (props.historyEventHandlers && !isObjectEmpty(props.historyEventHandlers)) {
      return props.historyEventHandlers;
    }
    return createDefaultHistoryHandlers(apiRef, {
      dataSource: props.dataSource,
      columns: props.columns,
      isCellEditable: props.isCellEditable
    });
  }, [apiRef, props.columns, props.isCellEditable, props.dataSource, props.historyEventHandlers]);
  const isEnabled = React361.useMemo(() => historyStackSize > 0 && !isObjectEmpty(historyEventHandlers), [historyStackSize, historyEventHandlers]);
  const isValidationNeeded = React361.useMemo(() => isEnabled && historyValidationEvents.length > 0 && Object.values(historyEventHandlers).some((handler) => handler.validate), [isEnabled, historyEventHandlers, historyValidationEvents]);
  const operationStateRef = React361.useRef("idle");
  const eventUnsubscribersRef = React361.useRef([]);
  const validationEventUnsubscribersRef = React361.useRef([]);
  const updateHistoryState = React361.useCallback((newState) => {
    apiRef.current.setState((state) => _extends({}, state, {
      history: _extends({}, state.history, newState)
    }));
  }, [apiRef]);
  const addToStack = React361.useCallback((item) => {
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    let newStack = [...gridHistoryStackSelector(apiRef)];
    if (currentPosition < newStack.length - 1) {
      newStack = newStack.slice(0, currentPosition + 1);
    }
    newStack.push(item);
    if (newStack.length > historyStackSize) {
      newStack = newStack.slice(newStack.length - historyStackSize);
    }
    updateHistoryState({
      stack: newStack,
      currentPosition: newStack.length - 1
    });
  }, [apiRef, updateHistoryState, historyStackSize]);
  const clear = React361.useCallback(() => {
    updateHistoryState({
      stack: [],
      currentPosition: -1
    });
  }, [updateHistoryState]);
  const clearUndoItems = React361.useCallback(() => {
    const stack = gridHistoryStackSelector(apiRef);
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    if (currentPosition >= stack.length - 1) {
      clear();
    } else {
      updateHistoryState({
        stack: stack.slice(currentPosition + 1),
        currentPosition: -1
      });
    }
  }, [apiRef, clear, updateHistoryState]);
  const clearRedoItems = React361.useCallback(() => {
    const stack = gridHistoryStackSelector(apiRef);
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    updateHistoryState({
      stack: stack.slice(0, currentPosition + 1)
    });
  }, [apiRef, updateHistoryState]);
  const canUndo = React361.useCallback(() => gridHistoryCanUndoSelector(apiRef), [apiRef]);
  const canRedo = React361.useCallback(() => gridHistoryCanRedoSelector(apiRef), [apiRef]);
  const validateStackItems = React361.useCallback(() => {
    if (operationStateRef.current !== "idle") {
      if (operationStateRef.current === "waiting-replay") {
        operationStateRef.current = "idle";
      }
      return;
    }
    const stack = gridHistoryStackSelector(apiRef);
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    if (historyStackSize === 0) {
      if (stack.length > 0) {
        clear();
      }
      return;
    }
    if (stack.length === 0) {
      return;
    }
    const newStack = [...stack];
    if (currentPosition + 1 < newStack.length) {
      const item = newStack[currentPosition + 1];
      const handler = historyEventHandlers[item.eventName];
      if (!handler) {
        clearRedoItems();
      } else {
        const isValid = handler.validate ? handler.validate(item.data, "redo") : true;
        if (!isValid) {
          clearRedoItems();
        }
      }
    }
    if (currentPosition >= 0) {
      const item = newStack[currentPosition];
      const handler = historyEventHandlers[item.eventName];
      if (!handler) {
        clearUndoItems();
      } else {
        const isValid = handler.validate ? handler.validate(item.data, "undo") : true;
        if (!isValid) {
          clearUndoItems();
        }
      }
    }
  }, [apiRef, historyEventHandlers, historyStackSize, clear, clearUndoItems, clearRedoItems]);
  const debouncedValidateStackItems = React361.useMemo(() => debounce(validateStackItems, 0), [validateStackItems]);
  const apply = React361.useCallback(async (item, operation) => {
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    const clearMethod = operation === "undo" ? clearUndoItems : clearRedoItems;
    const {
      eventName,
      data
    } = item;
    const handler = historyEventHandlers[eventName];
    if (!handler) {
      return false;
    }
    const isValid = handler.validate ? handler.validate(data, operation) : true;
    if (!isValid) {
      clearMethod();
      return false;
    }
    operationStateRef.current = "in-progress";
    await handler[operation](data);
    operationStateRef.current = "waiting-replay";
    updateHistoryState({
      currentPosition: operation === "undo" ? currentPosition - 1 : currentPosition + 1
    });
    apiRef.current.publishEvent(operation, {
      eventName,
      data
    });
    if (isValidationNeeded) {
      validateStackItems();
    } else {
      operationStateRef.current = "idle";
    }
    return true;
  }, [apiRef, isValidationNeeded, historyEventHandlers, clearUndoItems, clearRedoItems, updateHistoryState, validateStackItems]);
  const undo = React361.useCallback(async () => {
    if (!canUndo()) {
      return false;
    }
    const stack = gridHistoryStackSelector(apiRef);
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    return apply(stack[currentPosition], "undo");
  }, [apiRef, apply, canUndo]);
  const redo = React361.useCallback(async () => {
    if (!canRedo()) {
      return false;
    }
    const stack = gridHistoryStackSelector(apiRef);
    const currentPosition = gridHistoryCurrentPositionSelector(apiRef);
    return apply(stack[currentPosition + 1], "redo");
  }, [apiRef, apply, canRedo]);
  const historyApi = {
    undo,
    redo,
    clear,
    canUndo,
    canRedo
  };
  useGridApiMethod(apiRef, {
    history: historyApi
  }, "public");
  const handleKeyDown = React361.useCallback(async (event) => {
    if (!isUndoShortcut(event) && !isRedoShortcut(event)) {
      return;
    }
    const action = isUndoShortcut(event) ? apiRef.current.history.undo : apiRef.current.history.redo;
    event.preventDefault();
    event.stopPropagation();
    await action();
  }, [apiRef]);
  useGridNativeEventListener(apiRef, () => apiRef.current.rootElementRef.current, "keydown", runIf(isEnabled, handleKeyDown));
  useGridEvent(apiRef, "undo", onUndo);
  useGridEvent(apiRef, "redo", onRedo);
  React361.useEffect(() => {
    updateHistoryState({
      enabled: isEnabled
    });
  }, [isEnabled, updateHistoryState]);
  React361.useEffect(() => {
    if (!isValidationNeeded) {
      return () => {
      };
    }
    historyValidationEvents.forEach((eventName) => {
      validationEventUnsubscribersRef.current.push(apiRef.current.subscribeEvent(eventName, debouncedValidateStackItems));
    });
    return () => {
      validationEventUnsubscribersRef.current.forEach((unsubscribe) => unsubscribe());
      validationEventUnsubscribersRef.current = [];
    };
  }, [apiRef, isValidationNeeded, historyValidationEvents, debouncedValidateStackItems]);
  React361.useEffect(() => {
    if (historyStackSize === 0) {
      return () => {
      };
    }
    const events = Object.keys(historyEventHandlers);
    events.forEach((eventName) => {
      const handler = historyEventHandlers[eventName];
      const unsubscribe = apiRef.current.subscribeEvent(eventName, (...params) => {
        if (operationStateRef.current !== "idle") {
          return;
        }
        const data = handler.store(...params);
        if (data !== null) {
          addToStack({
            eventName,
            data
          });
        }
      });
      eventUnsubscribersRef.current.push(unsubscribe);
    });
    return () => {
      eventUnsubscribersRef.current.forEach((unsubscribe) => unsubscribe());
      eventUnsubscribersRef.current = [];
    };
  }, [apiRef, historyEventHandlers, historyStackSize, addToStack]);
  React361.useEffect(() => {
    const currentStackSize = gridHistoryStackSelector(apiRef).length;
    if (currentStackSize > historyStackSize) {
      clear();
    }
  }, [apiRef, historyStackSize, clear]);
};

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/useDataGridPremiumComponent.js
var useDataGridPremiumComponent = (apiRef, inProps, configuration4) => {
  const pivotPropsOverrides = useGridSelector(apiRef, gridPivotPropsOverridesSelector);
  const props = React362.useMemo(() => {
    if (pivotPropsOverrides) {
      return _extends({}, inProps, pivotPropsOverrides, {
        initialState: _extends({}, inProps.initialState, {
          columns: void 0
        })
      });
    }
    return inProps;
  }, [inProps, pivotPropsOverrides]);
  useGridInitialization(apiRef, props);
  const key = pivotPropsOverrides ? "pivoting" : void 0;
  useGridDetailPanelPreProcessors(apiRef, props);
  useGridRowGroupingPreProcessors(apiRef, props);
  useGridDataSourceRowGroupingPreProcessors(apiRef, props);
  useGridTreeDataPreProcessors(apiRef, props);
  useGridDataSourceTreeDataPreProcessors(apiRef, props);
  useGridRowSelectionPreProcessors(apiRef, props);
  useGridLazyLoaderPreProcessors(apiRef, props);
  useGridRowPinningPreProcessors(apiRef);
  useGridAggregationPreProcessors(apiRef, props);
  useGridRowReorderPreProcessors(apiRef, props);
  useGridColumnPinningPreProcessors(apiRef, props);
  useGridRowsPreProcessors(apiRef);
  useGridInitializeState(propsStateInitializer, apiRef, props);
  useGridInitializeState(headerFilteringStateInitializer, apiRef, props, key);
  useGridInitializeState(rowGroupingStateInitializer, apiRef, props, key);
  useGridInitializeState(aggregationStateInitializer, apiRef, props, key);
  useGridInitializeState(rowSelectionStateInitializer, apiRef, props);
  useGridInitializeState(rowReorderStateInitializer, apiRef, props);
  useGridInitializeState(cellSelectionStateInitializer, apiRef, props);
  useGridInitializeState(detailPanelStateInitializer, apiRef, props);
  useGridInitializeState(columnPinningStateInitializer, apiRef, props, key);
  useGridInitializeState(columnsStateInitializer, apiRef, props, key);
  useGridInitializeState(sidebarStateInitializer, apiRef, props);
  useGridInitializeState(pivotingStateInitializer, apiRef, props);
  useGridInitializeState(rowPinningStateInitializer, apiRef, props);
  useGridInitializeState(rowsStateInitializer, apiRef, props);
  useGridInitializeState(paginationStateInitializer, apiRef, props);
  useGridInitializeState(editingStateInitializer, apiRef, props);
  useGridInitializeState(focusStateInitializer, apiRef, props);
  useGridInitializeState(sortingStateInitializer, apiRef, props);
  useGridInitializeState(preferencePanelStateInitializer, apiRef, props);
  useGridInitializeState(filterStateInitializer, apiRef, props);
  useGridInitializeState(rowSpanningStateInitializer, apiRef, props);
  useGridInitializeState(densityStateInitializer, apiRef, props);
  useGridInitializeState(columnReorderStateInitializer, apiRef, props);
  useGridInitializeState(columnResizeStateInitializer, apiRef, props);
  useGridInitializeState(columnMenuStateInitializer, apiRef, props);
  useGridInitializeState(columnGroupsStateInitializer, apiRef, props, key);
  useGridInitializeState(virtualizationStateInitializer, apiRef, props);
  useGridInitializeState(dataSourceStateInitializer, apiRef, props);
  useGridInitializeState(dimensionsStateInitializer, apiRef, props);
  useGridInitializeState(rowsMetaStateInitializer, apiRef, props);
  useGridInitializeState(listViewStateInitializer, apiRef, props);
  useGridInitializeState(aiAssistantStateInitializer, apiRef, props);
  useGridInitializeState(chartsIntegrationStateInitializer, apiRef, props);
  useGridInitializeState(historyStateInitializer, apiRef, props);
  useGridSidebar(apiRef, props);
  useGridPivoting(apiRef, props, inProps.columns, inProps.rows);
  useGridRowGrouping(apiRef, props);
  useGridHeaderFiltering(apiRef, props);
  useGridTreeData(apiRef, props);
  useGridAggregation(apiRef, props);
  useGridKeyboardNavigation(apiRef, props);
  useGridRowSelection(apiRef, props);
  useGridCellSelection(apiRef, props);
  useGridColumnPinning(apiRef, props);
  useGridRowPinning(apiRef, props);
  useGridColumns(apiRef, props);
  useGridRows(apiRef, props, configuration4);
  useGridRowSpanning(apiRef, props);
  useGridParamsApi(apiRef, props, configuration4);
  useGridDetailPanel(apiRef, props);
  useGridColumnSpanning(apiRef);
  useGridColumnGrouping(apiRef, props);
  useGridClipboardImport(apiRef, props);
  useGridEditing(apiRef, props, configuration4);
  useGridFocus(apiRef, props);
  useGridPreferencesPanel(apiRef, props);
  useGridFilter(apiRef, props, configuration4);
  useGridSorting(apiRef, props);
  useGridDensity(apiRef, props);
  useGridColumnReorder(apiRef, props);
  useGridColumnResize(apiRef, props);
  useGridPagination(apiRef, props);
  useGridRowReorder(apiRef, props);
  useGridScroll(apiRef, props);
  useGridInfiniteLoader(apiRef, props);
  useGridLazyLoader(apiRef, props);
  useGridDataSourceLazyLoader(apiRef, props);
  useGridInfiniteLoadingIntersection(apiRef, props);
  useGridColumnMenu(apiRef);
  useGridCsvExport(apiRef, props);
  useGridPrintExport(apiRef, props);
  useGridExcelExport(apiRef, props);
  useGridClipboard(apiRef, props);
  useGridDimensions(apiRef, props);
  useGridEvents(apiRef, props);
  useGridStatePersistence(apiRef);
  useGridDataSourcePremium(apiRef, props);
  useGridVirtualization(apiRef, props);
  useGridListView(apiRef, props);
  useGridAiAssistant(apiRef, props);
  useGridChartsIntegration(apiRef, props);
  useGridHistory(apiRef, props);
  useGridPivotingExportState(apiRef);
  useFirstRender(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
  React362.useEffect(() => {
    apiRef.current.runAppliersForPendingProcessors();
  });
  return props;
};

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/useDataGridPremiumProps.js
var React367 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/material/index.js
var iconsSlots = {
  collapsibleIcon: GridExpandMoreIcon,
  columnMenuUngroupIcon: GridWorkspacesIcon,
  columnMenuGroupIcon: GridGroupWorkIcon,
  columnMenuAggregationIcon: GridFunctionsIcon,
  pivotIcon: GridPivotIcon,
  pivotSearchIcon: GridSearchIcon,
  pivotSearchClearIcon: GridClearIcon,
  pivotMenuAddIcon: GridAddIcon,
  pivotMenuMoveUpIcon: GridExpandLessIcon,
  pivotMenuMoveDownIcon: GridExpandMoreIcon,
  pivotMenuMoveToTopIcon: GridMoveToTopIcon,
  pivotMenuMoveToBottomIcon: GridMoveToBottomIcon,
  pivotMenuCheckIcon: GridCheckIcon,
  pivotMenuRemoveIcon: GridDeleteIcon,
  sidebarCloseIcon: GridCloseIcon,
  aiAssistantIcon: GridAssistantIcon,
  aiAssistantPanelCloseIcon: GridCloseIcon,
  aiAssistantPanelNewConversationIcon: GridAddIcon,
  aiAssistantPanelHistoryIcon: GridHistoryIcon,
  promptIcon: GridPromptIcon,
  promptSendIcon: GridSendIcon,
  promptSpeechRecognitionIcon: GridMicIcon,
  promptSpeechRecognitionOffIcon: GridMicOffIcon,
  promptRerunIcon: GridRerunIcon,
  promptSortAscIcon: GridArrowUpwardIcon,
  promptSortDescIcon: GridArrowDownwardIcon,
  promptFilterIcon: GridFilterAltIcon,
  promptPivotIcon: GridPivotIcon,
  promptAggregationIcon: GridFunctionsIcon,
  promptGroupIcon: GridGroupWorkIcon,
  promptChartsIcon: GridChartsIcon,
  promptChangesToggleIcon: GridExpandMoreIcon,
  chartsIcon: GridChartsIcon,
  chartsSearchIcon: GridSearchIcon,
  chartsSearchClearIcon: GridClearIcon,
  chartsMenuMoveUpIcon: GridExpandLessIcon,
  chartsMenuMoveDownIcon: GridExpandMoreIcon,
  chartsMenuMoveToTopIcon: GridMoveToTopIcon,
  chartsMenuMoveToBottomIcon: GridMoveToBottomIcon,
  chartsMenuAddIcon: GridAddIcon,
  chartsMenuRemoveIcon: GridDeleteIcon,
  chartsSyncIcon: GridSyncIcon,
  chartsSyncDisabledIcon: GridSyncDisabledIcon
};
var materialSlots3 = _extends({}, iconsSlots);
var material_default3 = materialSlots3;

// node_modules/@mui/x-data-grid-premium/esm/components/GridBottomContainer.js
var React365 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/components/GridAggregationRowOverlay.js
var React363 = __toESM(require_react(), 1);
var import_jsx_runtime265 = __toESM(require_jsx_runtime(), 1);
var useUtilityClasses130 = (ownerState) => {
  const {
    classes: classes2
  } = ownerState;
  const slots = {
    root: ["aggregationRowOverlayWrapper"]
  };
  return composeClasses(slots, getDataGridUtilityClass, classes2);
};
var GridAggregationRowOverlay = forwardRef2(function GridAggregationRowOverlay2(props, forwardedRef) {
  const apiRef = useGridApiContext3();
  const rootProps = useGridRootProps2();
  const classes2 = useUtilityClasses130({
    classes: rootProps.classes
  });
  const aggregationModel = useGridSelector(apiRef, gridAggregationModelSelector);
  const visibleColumns = new Set(Object.keys(aggregationModel));
  return (0, import_jsx_runtime265.jsx)("div", {
    className: classes2.root,
    children: (0, import_jsx_runtime265.jsx)(GridSkeletonLoadingOverlayInner, _extends({}, props, {
      skeletonRowsCount: 1,
      visibleColumns,
      showFirstRowBorder: true,
      ref: forwardedRef
    }))
  });
});
if (true) GridAggregationRowOverlay.displayName = "GridAggregationRowOverlay";

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridApiRef.js
var useGridApiRef3 = useGridApiRef;

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/export/serializer/setupExcelExportWebWorker.js
function setupExcelExportWebWorker(workerOptions = {}) {
  globalThis.addEventListener("message", async (event) => {
    const {
      namespace,
      serializedColumns,
      serializedRows,
      options: options3,
      valueOptionsSheetName,
      valueOptionsData,
      columnGroupDetails,
      columnGroupPaths
    } = event.data;
    if (namespace !== "mui-x-data-grid-export") {
      return;
    }
    const {
      exceljsPostProcess,
      exceljsPreProcess
    } = workerOptions;
    const excelJS = await getExcelJs();
    const workbook = new excelJS.Workbook();
    const worksheet = workbook.addWorksheet("Sheet1");
    worksheet.columns = serializedColumns;
    if (exceljsPreProcess) {
      await exceljsPreProcess({
        workbook,
        worksheet
      });
    }
    if (options3.includeColumnGroupsHeaders) {
      addColumnGroupingHeaders(worksheet, serializedColumns, columnGroupPaths, columnGroupDetails);
    }
    const includeHeaders = options3.includeHeaders ?? true;
    if (includeHeaders) {
      worksheet.addRow(serializedColumns.map((column) => column.headerText));
    }
    createValueOptionsSheetIfNeeded(valueOptionsData, valueOptionsSheetName, workbook);
    serializedRows.forEach((serializedRow) => {
      addSerializedRowToWorksheet(serializedRow, worksheet);
    });
    if (exceljsPostProcess) {
      await exceljsPostProcess({
        workbook,
        worksheet
      });
    }
    postMessage(await workbook.xlsx.writeBuffer());
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/aiAssistant/api.js
function gridDefaultPromptResolver(url, query, context, conversationId, optionsOrAdditionalContext = "") {
  const options3 = typeof optionsOrAdditionalContext === "string" ? {
    additionalContext: optionsOrAdditionalContext
  } : optionsOrAdditionalContext;
  return fetch(url, {
    mode: "cors",
    method: "POST",
    headers: {
      "content-type": "application/json"
    },
    credentials: "include",
    body: JSON.stringify({
      context,
      query,
      conversationId,
      options: options3
    })
  }).then((result) => result.json()).then((result) => {
    if (result.ok === false) {
      return Promise.reject(new Error(result.message));
    }
    return result.data;
  });
}

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useKeepGroupedColumnsHidden.js
var React364 = __toESM(require_react(), 1);
var updateColumnVisibilityModel = (columnVisibilityModel, rowGroupingModel, prevRowGroupingModel) => {
  const newColumnVisibilityModel = _extends({}, columnVisibilityModel);
  rowGroupingModel == null ? void 0 : rowGroupingModel.forEach((field) => {
    if (!(prevRowGroupingModel == null ? void 0 : prevRowGroupingModel.includes(field))) {
      newColumnVisibilityModel[field] = false;
    }
  });
  prevRowGroupingModel == null ? void 0 : prevRowGroupingModel.forEach((field) => {
    if (!(rowGroupingModel == null ? void 0 : rowGroupingModel.includes(field))) {
      newColumnVisibilityModel[field] = true;
    }
  });
  return newColumnVisibilityModel;
};
var useKeepGroupedColumnsHidden = (props) => {
  var _a, _b;
  const initialProps = React364.useRef(props);
  const rowGroupingModel = React364.useRef(props.rowGroupingModel ?? ((_b = (_a = props.initialState) == null ? void 0 : _a.rowGrouping) == null ? void 0 : _b.model));
  React364.useEffect(() => {
    var _a2;
    (_a2 = props.apiRef.current) == null ? void 0 : _a2.subscribeEvent("rowGroupingModelChange", (newModel) => {
      var _a3;
      const columnVisibilityModel = updateColumnVisibilityModel(gridColumnVisibilityModelSelector(props.apiRef), newModel, rowGroupingModel.current);
      (_a3 = props.apiRef.current) == null ? void 0 : _a3.setColumnVisibilityModel(columnVisibilityModel);
      rowGroupingModel.current = newModel;
    });
  }, [props.apiRef]);
  return React364.useMemo(() => {
    var _a2;
    const invariantInitialState = initialProps.current.initialState;
    const columnVisibilityModel = updateColumnVisibilityModel((_a2 = invariantInitialState == null ? void 0 : invariantInitialState.columns) == null ? void 0 : _a2.columnVisibilityModel, rowGroupingModel.current, void 0);
    return _extends({}, invariantInitialState, {
      columns: _extends({}, invariantInitialState == null ? void 0 : invariantInitialState.columns, {
        columnVisibilityModel
      })
    });
  }, []);
};

// node_modules/@mui/x-data-grid-premium/esm/components/GridBottomContainer.js
var import_jsx_runtime266 = __toESM(require_jsx_runtime(), 1);
var _excluded101 = ["children"];
var useUtilityClasses131 = () => {
  const slots = {
    root: ["bottomContainer"]
  };
  return composeClasses(slots, getDataGridUtilityClass, {});
};
var Element5 = styled_default2("div", {
  slot: "internal",
  shouldForwardProp: void 0
})({
  position: "sticky",
  zIndex: 40,
  bottom: "calc(var(--DataGrid-hasScrollX) * var(--DataGrid-scrollbarSize))"
});
function GridBottomContainer2(props) {
  const classes2 = useUtilityClasses131();
  const rootProps = useGridRootProps3();
  const apiRef = useGridPrivateApiContext3();
  const isLoading = useGridSelector(apiRef, gridRowsLoadingSelector);
  const tree = useGridSelector(apiRef, gridRowTreeSelector);
  const aggregationModel = useGridSelector(apiRef, gridAggregationModelSelector);
  const aggregationPosition = rootProps.getAggregationPosition(tree[GRID_ROOT_GROUP_ID]);
  const hasAggregation = React365.useMemo(() => Object.keys(aggregationModel).length > 0, [aggregationModel]);
  const {
    children: children2
  } = props, other = _objectWithoutPropertiesLoose(props, _excluded101);
  return (0, import_jsx_runtime266.jsx)(Element5, _extends({}, other, {
    className: clsx_default(classes2.root, gridClasses["container--bottom"]),
    role: "presentation",
    children: hasAggregation && isLoading && aggregationPosition === "footer" ? (0, import_jsx_runtime266.jsx)(GridAggregationRowOverlay, {}) : children2
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/components/GridPremiumToolbar.js
var React366 = __toESM(require_react(), 1);
var import_jsx_runtime267 = __toESM(require_jsx_runtime(), 1);
var _excluded103 = ["excelOptions"];
function GridPremiumToolbar(props) {
  var _a, _b, _c, _d;
  const rootProps = useGridRootProps3();
  const apiRef = useGridApiContext3();
  const other = _objectWithoutPropertiesLoose(props, _excluded103);
  const historyEnabled = useGridSelector(apiRef, gridHistoryEnabledSelector);
  const showHistoryControls = ((_b = (_a = rootProps.slotProps) == null ? void 0 : _a.toolbar) == null ? void 0 : _b.showHistoryControls) !== false && historyEnabled;
  const canUndo = useGridSelector(apiRef, gridHistoryCanUndoSelector);
  const canRedo = useGridSelector(apiRef, gridHistoryCanRedoSelector);
  const mainControls = (0, import_jsx_runtime267.jsxs)(React366.Fragment, {
    children: [showHistoryControls && (0, import_jsx_runtime267.jsxs)(React366.Fragment, {
      children: [(0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarUndo"),
        children: (0, import_jsx_runtime267.jsx)("div", {
          children: (0, import_jsx_runtime267.jsx)(ToolbarButton, {
            disabled: !canUndo,
            onClick: () => apiRef.current.history.undo(),
            children: (0, import_jsx_runtime267.jsx)(rootProps.slots.undoIcon, {
              fontSize: "small"
            })
          })
        })
      }), (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarRedo"),
        children: (0, import_jsx_runtime267.jsx)("div", {
          children: (0, import_jsx_runtime267.jsx)(ToolbarButton, {
            disabled: !canRedo,
            onClick: () => apiRef.current.history.redo(),
            children: (0, import_jsx_runtime267.jsx)(rootProps.slots.redoIcon, {
              fontSize: "small"
            })
          })
        })
      })]
    }), showHistoryControls && (0, import_jsx_runtime267.jsx)(GridToolbarDivider, {}), !rootProps.disableColumnSelector && (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
      title: apiRef.current.getLocaleText("toolbarColumns"),
      children: (0, import_jsx_runtime267.jsx)(ColumnsPanelTrigger, {
        render: (0, import_jsx_runtime267.jsx)(ToolbarButton, {}),
        children: (0, import_jsx_runtime267.jsx)(rootProps.slots.columnSelectorIcon, {
          fontSize: "small"
        })
      })
    }), !rootProps.disableColumnFilter && (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
      title: apiRef.current.getLocaleText("toolbarFilters"),
      children: (0, import_jsx_runtime267.jsx)(FilterPanelTrigger, {
        render: (triggerProps, state) => (0, import_jsx_runtime267.jsx)(ToolbarButton, _extends({}, triggerProps, {
          color: state.filterCount > 0 ? "primary" : "default",
          children: (0, import_jsx_runtime267.jsx)(rootProps.slots.baseBadge, {
            badgeContent: state.filterCount,
            color: "primary",
            variant: "dot",
            children: (0, import_jsx_runtime267.jsx)(rootProps.slots.openFilterButtonIcon, {
              fontSize: "small"
            })
          })
        }))
      })
    }), !rootProps.disablePivoting && (0, import_jsx_runtime267.jsx)(PivotPanelTrigger, {
      render: (triggerProps, state) => (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarPivot"),
        children: (0, import_jsx_runtime267.jsx)(ToolbarButton, _extends({}, triggerProps, {
          color: state.active ? "primary" : "default",
          children: (0, import_jsx_runtime267.jsx)(rootProps.slots.pivotIcon, {
            fontSize: "small"
          })
        }))
      })
    }), ((_c = rootProps.experimentalFeatures) == null ? void 0 : _c.charts) && rootProps.chartsIntegration && (0, import_jsx_runtime267.jsx)(ChartsPanelTrigger, {
      render: (triggerProps) => (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarCharts"),
        children: (0, import_jsx_runtime267.jsx)(ToolbarButton, _extends({}, triggerProps, {
          color: "default",
          children: (0, import_jsx_runtime267.jsx)(rootProps.slots.chartsIcon, {
            fontSize: "small"
          })
        }))
      })
    }), rootProps.aiAssistant && (0, import_jsx_runtime267.jsx)(AiAssistantPanelTrigger, {
      render: (triggerProps) => (0, import_jsx_runtime267.jsx)(rootProps.slots.baseTooltip, {
        title: apiRef.current.getLocaleText("toolbarAssistant"),
        children: (0, import_jsx_runtime267.jsx)(ToolbarButton, _extends({}, triggerProps, {
          color: "default",
          children: (0, import_jsx_runtime267.jsx)(rootProps.slots.aiAssistantIcon, {
            fontSize: "small"
          })
        }))
      })
    })]
  });
  const additionalExportMenuItems = !((_d = props.excelOptions) == null ? void 0 : _d.disableToolbarButton) ? (onMenuItemClick) => {
    var _a2;
    return (0, import_jsx_runtime267.jsx)(ExportExcel, {
      render: (0, import_jsx_runtime267.jsx)(rootProps.slots.baseMenuItem, _extends({}, (_a2 = rootProps.slotProps) == null ? void 0 : _a2.baseMenuItem)),
      options: props.excelOptions,
      onClick: onMenuItemClick,
      children: apiRef.current.getLocaleText("toolbarExportExcel")
    });
  } : void 0;
  return (0, import_jsx_runtime267.jsx)(GridToolbar, _extends({}, other, {
    mainControls,
    additionalExportMenuItems
  }));
}

// node_modules/@mui/x-data-grid-premium/esm/constants/dataGridPremiumDefaultSlotsComponents.js
var DATA_GRID_PREMIUM_DEFAULT_SLOTS_COMPONENTS = _extends({}, DATA_GRID_PRO_DEFAULT_SLOTS_COMPONENTS, material_default3, {
  aiAssistantPanel: null,
  chartsPanel: null,
  columnMenu: GridPremiumColumnMenu,
  bottomContainer: GridBottomContainer2,
  emptyPivotOverlay: GridEmptyPivotOverlay,
  toolbar: GridPremiumToolbar
});

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/history/constants.js
var DEFAULT_HISTORY_VALIDATION_EVENTS = ["columnsChange", "rowsSet", "sortedRowsSet", "filteredRowsSet", "paginationModelChange"];

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/useDataGridPremiumProps.js
var getDataGridPremiumForcedProps = (themedProps) => _extends({
  signature: GridSignature.DataGridPremium
}, themedProps.dataSource ? {
  filterMode: "server",
  sortingMode: "server",
  paginationMode: "server"
} : {});
var DATA_GRID_PREMIUM_PROPS_DEFAULT_VALUES = _extends({}, DATA_GRID_PRO_PROPS_DEFAULT_VALUES, {
  cellSelection: false,
  disableAggregation: false,
  disableRowGrouping: false,
  rowGroupingColumnMode: "single",
  aggregationFunctions: GRID_AGGREGATION_FUNCTIONS,
  aggregationRowsScope: "filtered",
  getAggregationPosition: defaultGetAggregationPosition,
  disableClipboardPaste: false,
  splitClipboardPastedText: (pastedText, delimiter = "	") => {
    const text = pastedText.replace(/\r?\n$/, "");
    return text.split(/\r\n|\n|\r/).map((row) => row.split(delimiter));
  },
  disablePivoting: false,
  aiAssistant: false,
  chartsIntegration: false,
  historyStackSize: 30,
  historyEventHandlers: {},
  historyValidationEvents: DEFAULT_HISTORY_VALIDATION_EVENTS
});
var defaultSlots3 = DATA_GRID_PREMIUM_DEFAULT_SLOTS_COMPONENTS;
var useDataGridPremiumProps = (inProps) => {
  const theme = useTheme();
  const themedProps = React367.useMemo(() => getThemeProps({
    props: inProps,
    theme,
    name: "MuiDataGrid"
  }), [theme, inProps]);
  const localeText = React367.useMemo(() => _extends({}, GRID_DEFAULT_LOCALE_TEXT, themedProps.localeText), [themedProps.localeText]);
  const slots = React367.useMemo(() => computeSlots({
    defaultSlots: defaultSlots3,
    slots: themedProps.slots
  }), [themedProps.slots]);
  return React367.useMemo(() => _extends({}, DATA_GRID_PREMIUM_PROPS_DEFAULT_VALUES, themedProps.dataSource ? {
    aggregationFunctions: {}
  } : {
    getPivotDerivedColumns: defaultGetPivotDerivedColumns
  }, themedProps, {
    localeText,
    slots
  }, getDataGridPremiumForcedProps(themedProps)), [themedProps, localeText, slots]);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/utils/useGridAriaAttributes.js
var useGridAriaAttributesPremium = () => {
  const ariaAttributesPro = useGridAriaAttributesPro();
  const apiRef = useGridPrivateApiContext3();
  const gridRowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  const ariaAttributesPremium = gridRowGroupingModel.length > 0 ? {
    role: "treegrid"
  } : {};
  return _extends({}, ariaAttributesPro, ariaAttributesPremium);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rows/useGridRowAriaAttributes.js
var useGridRowAriaAttributesPremium = () => {
  const apiRef = useGridPrivateApiContext3();
  const gridRowGroupingModel = useGridSelector(apiRef, gridRowGroupingSanitizedModelSelector);
  return useGridRowAriaAttributesPro(gridRowGroupingModel.length > 0);
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rows/useGridRowsOverridableMethods.js
var React368 = __toESM(require_react(), 1);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowReorder/operations.js
var CrossParentLeafOperation2 = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "cross-parent-leaf");
  }
  detectOperation(ctx) {
    const {
      sourceRowId,
      placeholderIndex,
      sortedFilteredRowIds,
      rowTree,
      apiRef
    } = ctx;
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type === "footer") {
      return null;
    }
    let targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[placeholderIndex]);
    let isLastChild = false;
    if (!targetNode) {
      if (placeholderIndex >= sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
        targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[sortedFilteredRowIds.length - 1]);
        isLastChild = true;
      } else {
        return null;
      }
    }
    let adjustedTargetNode = targetNode;
    if (sourceNode.type === "leaf" && targetNode.type === "group" && targetNode.depth < sourceNode.depth) {
      const prevIndex = placeholderIndex - 1;
      if (prevIndex >= 0) {
        const prevRowId = sortedFilteredRowIds[prevIndex];
        const leafTargetNode = gridRowNodeSelector(apiRef, prevRowId);
        if (leafTargetNode && leafTargetNode.type === "leaf") {
          adjustedTargetNode = leafTargetNode;
          isLastChild = true;
        }
      }
    }
    const operationType = utils_exports.determineOperationType(sourceNode, adjustedTargetNode);
    if (operationType !== "cross-parent-leaf") {
      return null;
    }
    const actualTargetIndex = utils_exports.calculateTargetIndex(sourceNode, adjustedTargetNode, isLastChild, rowTree);
    targetNode = adjustedTargetNode;
    if (sourceNode.type === "leaf" && targetNode.type === "leaf") {
      if (sourceNode.depth !== targetNode.depth) {
        return null;
      }
    } else if (sourceNode.type === "leaf" && targetNode.type === "group") {
      if (targetNode.depth >= sourceNode.depth) {
        return null;
      }
    }
    return {
      sourceNode,
      targetNode: adjustedTargetNode,
      actualTargetIndex,
      isLastChild,
      operationType
    };
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode,
      isLastChild
    } = operation;
    const {
      apiRef,
      sourceRowId,
      processRowUpdate,
      onProcessRowUpdateError
    } = ctx;
    let target = targetNode;
    if (targetNode.type === "group") {
      const prevIndex = ctx.placeholderIndex - 1;
      if (prevIndex >= 0) {
        const prevRowId = ctx.sortedFilteredRowIds[prevIndex];
        const prevNode = gridRowNodeSelector(apiRef, prevRowId);
        if (prevNode && prevNode.type === "leaf") {
          target = prevNode;
        }
      }
    }
    const rowTree = gridRowTreeSelector(apiRef);
    const sourceGroup = rowTree[sourceNode.parent];
    const targetGroup = rowTree[target.parent];
    const sourceChildren = sourceGroup.children;
    const targetChildren = targetGroup.children;
    const sourceIndex = sourceChildren.findIndex((row) => row === sourceRowId);
    const targetIndex = targetChildren.findIndex((row) => row === target.id);
    if (sourceIndex === -1 || targetIndex === -1) {
      return;
    }
    const dataRowIdToModelLookup = gridRowsLookupSelector(apiRef);
    const columnsLookup = gridColumnLookupSelector(apiRef);
    const sanitizedRowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const originalSourceRow = dataRowIdToModelLookup[sourceRowId];
    let updatedSourceRow = _extends({}, originalSourceRow);
    const targetRow = dataRowIdToModelLookup[target.id];
    const groupingRules = getGroupingRules({
      sanitizedRowGroupingModel,
      columnsLookup
    });
    for (const groupingRule of groupingRules) {
      const colDef = columnsLookup[groupingRule.field];
      if (groupingRule.groupingValueSetter && colDef) {
        const targetGroupingValue = getCellGroupingCriteria({
          row: targetRow,
          colDef,
          groupingRule,
          apiRef
        }).key;
        updatedSourceRow = groupingRule.groupingValueSetter(targetGroupingValue, updatedSourceRow, colDef, apiRef);
      } else {
        updatedSourceRow[groupingRule.field] = targetRow[groupingRule.field];
      }
    }
    const updater = new utils_exports.BatchRowUpdater(apiRef, processRowUpdate, onProcessRowUpdateError);
    updater.queueUpdate(sourceRowId, originalSourceRow, updatedSourceRow);
    const {
      successful,
      updates
    } = await updater.executeAll();
    if (successful.length === 0) {
      return;
    }
    const finalSourceRow = updates[0];
    apiRef.current.setState((state) => {
      const updatedSourceChildren = sourceChildren.filter((rowId) => rowId !== sourceRowId);
      const updatedTree = _extends({}, state.rows.tree);
      const removedGroups = /* @__PURE__ */ new Set();
      let rootLevelRemovals = 0;
      if (updatedSourceChildren.length === 0) {
        removedGroups.add(sourceGroup.id);
        rootLevelRemovals = utils_exports.removeEmptyAncestors(sourceGroup.parent, updatedTree, removedGroups);
      }
      removedGroups.forEach((groupId) => {
        const group = updatedTree[groupId];
        if (group && group.parent && updatedTree[group.parent]) {
          const parent = updatedTree[group.parent];
          updatedTree[group.parent] = _extends({}, parent, {
            children: parent.children.filter((childId) => childId !== groupId)
          });
        }
        delete updatedTree[groupId];
      });
      if (!removedGroups.has(sourceGroup.id)) {
        updatedTree[sourceNode.parent] = _extends({}, sourceGroup, {
          children: updatedSourceChildren
        });
      }
      const updatedTargetChildren = isLastChild ? [...targetChildren, sourceRowId] : [...targetChildren.slice(0, targetIndex), sourceRowId, ...targetChildren.slice(targetIndex)];
      updatedTree[target.parent] = _extends({}, targetGroup, {
        children: updatedTargetChildren
      });
      updatedTree[sourceNode.id] = _extends({}, sourceNode, {
        parent: target.parent
      });
      return _extends({}, state, {
        rows: _extends({}, state.rows, {
          totalTopLevelRowCount: state.rows.totalTopLevelRowCount - rootLevelRemovals,
          tree: updatedTree
        })
      });
    });
    apiRef.current.updateRows([finalSourceRow]);
    apiRef.current.publishEvent("rowsSet");
  }
};
var CrossParentGroupOperation2 = class extends BaseReorderOperation {
  constructor() {
    super(...arguments);
    __publicField(this, "operationType", "cross-parent-group");
  }
  detectOperation(ctx) {
    const {
      sourceRowId,
      placeholderIndex,
      sortedFilteredRowIds,
      rowTree,
      apiRef
    } = ctx;
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode || sourceNode.type === "footer") {
      return null;
    }
    let targetIndex = placeholderIndex;
    let targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[placeholderIndex]);
    let isLastChild = false;
    if (!targetNode) {
      if (placeholderIndex >= sortedFilteredRowIds.length && sortedFilteredRowIds.length > 0) {
        targetNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[sortedFilteredRowIds.length - 1]);
        targetIndex = sortedFilteredRowIds.length - 1;
        isLastChild = true;
      } else {
        return null;
      }
    }
    let adjustedTargetNode = targetNode;
    if (sourceNode.type === "group" && targetNode.type === "group" && sourceNode.parent !== targetNode.parent && sourceNode.depth > targetNode.depth) {
      let prevIndex = targetIndex - 1;
      if (prevIndex < 0) {
        return null;
      }
      let prevNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[prevIndex]);
      if (prevNode && prevNode.depth !== sourceNode.depth) {
        while (prevNode.depth > sourceNode.depth && prevIndex >= 0) {
          prevIndex -= 1;
          prevNode = gridRowNodeSelector(apiRef, sortedFilteredRowIds[prevIndex]);
        }
      }
      if (!prevNode || prevNode.type !== "group" || prevNode.depth !== sourceNode.depth) {
        return null;
      }
      isLastChild = true;
      adjustedTargetNode = prevNode;
    }
    const operationType = utils_exports.determineOperationType(sourceNode, adjustedTargetNode);
    if (operationType !== "cross-parent-group") {
      return null;
    }
    const actualTargetIndex = utils_exports.calculateTargetIndex(sourceNode, adjustedTargetNode, isLastChild, rowTree);
    const operation = {
      sourceNode,
      targetNode: adjustedTargetNode,
      actualTargetIndex,
      isLastChild,
      operationType
    };
    targetNode = adjustedTargetNode;
    if (sourceNode.depth !== targetNode.depth) {
      return null;
    }
    return operation;
  }
  async executeOperation(operation, ctx) {
    const {
      sourceNode,
      targetNode,
      isLastChild
    } = operation;
    const {
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError
    } = ctx;
    const tree = gridRowTreeSelector(apiRef);
    const dataRowIdToModelLookup = gridRowsLookupSelector(apiRef);
    const columnsLookup = gridColumnLookupSelector(apiRef);
    const sanitizedRowGroupingModel = gridRowGroupingSanitizedModelSelector(apiRef);
    const allLeafIds = utils_exports.collectAllLeafDescendants(sourceNode, tree);
    if (allLeafIds.length === 0) {
      return;
    }
    const updater = new utils_exports.BatchRowUpdater(apiRef, processRowUpdate, onProcessRowUpdateError);
    const groupingRules = getGroupingRules({
      sanitizedRowGroupingModel,
      columnsLookup
    });
    const targetParentPath = utils_exports.getNodePathInTree({
      id: targetNode.parent,
      tree
    });
    for (const leafId of allLeafIds) {
      const originalRow = dataRowIdToModelLookup[leafId];
      let updatedRow = _extends({}, originalRow);
      for (let depth = 0; depth < targetParentPath.length; depth += 1) {
        const pathItem = targetParentPath[depth];
        if (pathItem.field) {
          const groupingRule = groupingRules.find((rule) => rule.field === pathItem.field);
          if (groupingRule) {
            const colDef = columnsLookup[groupingRule.field];
            if (groupingRule.groupingValueSetter && colDef) {
              updatedRow = groupingRule.groupingValueSetter(pathItem.key, updatedRow, colDef, apiRef);
            } else {
              updatedRow[groupingRule.field] = pathItem.key;
            }
          }
        }
      }
      updater.queueUpdate(leafId, originalRow, updatedRow);
    }
    const {
      successful,
      failed,
      updates
    } = await updater.executeAll();
    if (successful.length > 0) {
      apiRef.current.setState((state) => {
        const updatedTree = _extends({}, state.rows.tree);
        const treeDepths = _extends({}, state.rows.treeDepths);
        let rootLevelRemovals = 0;
        if (failed.length === 0) {
          const sourceParentNode = updatedTree[sourceNode.parent];
          if (!sourceParentNode) {
            const targetParentNode = updatedTree[targetNode.parent];
            const targetIndex = targetParentNode.children.indexOf(targetNode.id);
            const newTargetChildren = [...targetParentNode.children];
            if (isLastChild) {
              newTargetChildren.push(sourceNode.id);
            } else {
              newTargetChildren.splice(targetIndex, 0, sourceNode.id);
            }
            updatedTree[targetNode.parent] = _extends({}, targetParentNode, {
              children: newTargetChildren
            });
            updatedTree[sourceNode.id] = _extends({}, sourceNode, {
              parent: targetNode.parent
            });
          } else {
            const updatedSourceParentChildren = sourceParentNode.children.filter((id) => id !== sourceNode.id);
            if (updatedSourceParentChildren.length === 0) {
              const removedGroups = /* @__PURE__ */ new Set();
              removedGroups.add(sourceNode.parent);
              const parentOfSourceParent = updatedTree[sourceNode.parent].parent;
              if (parentOfSourceParent) {
                rootLevelRemovals = utils_exports.removeEmptyAncestors(parentOfSourceParent, updatedTree, removedGroups);
              }
              removedGroups.forEach((groupId) => {
                const group = updatedTree[groupId];
                if (group && group.parent && updatedTree[group.parent]) {
                  const parent = updatedTree[group.parent];
                  updatedTree[group.parent] = _extends({}, parent, {
                    children: parent.children.filter((childId) => childId !== groupId)
                  });
                }
                delete updatedTree[groupId];
              });
            } else {
              updatedTree[sourceNode.parent] = _extends({}, sourceParentNode, {
                children: updatedSourceParentChildren
              });
            }
            const targetParentNode = updatedTree[targetNode.parent];
            const sourceGroupNode = sourceNode;
            const existingGroup = sourceGroupNode.groupingKey !== null && sourceGroupNode.groupingField !== null ? utils_exports.findExistingGroupWithSameKey(targetParentNode, sourceGroupNode.groupingKey, sourceGroupNode.groupingField, updatedTree) : null;
            if (existingGroup) {
              const updatedExistingGroup = _extends({}, existingGroup, {
                children: [...existingGroup.children, ...sourceGroupNode.children]
              });
              updatedTree[existingGroup.id] = updatedExistingGroup;
              sourceGroupNode.children.forEach((childId) => {
                const childNode = updatedTree[childId];
                if (childNode) {
                  updatedTree[childId] = _extends({}, childNode, {
                    parent: existingGroup.id
                  });
                }
              });
              delete updatedTree[sourceNode.id];
            } else {
              const targetIndex = targetParentNode.children.indexOf(targetNode.id);
              const newTargetChildren = [...targetParentNode.children];
              if (isLastChild) {
                newTargetChildren.push(sourceNode.id);
              } else {
                newTargetChildren.splice(targetIndex, 0, sourceNode.id);
              }
              updatedTree[targetNode.parent] = _extends({}, targetParentNode, {
                children: newTargetChildren
              });
              updatedTree[sourceNode.id] = _extends({}, sourceNode, {
                parent: targetNode.parent
              });
            }
          }
        }
        return _extends({}, state, {
          rows: _extends({}, state.rows, {
            totalTopLevelRowCount: state.rows.totalTopLevelRowCount - rootLevelRemovals,
            tree: updatedTree,
            treeDepths
          })
        });
      });
      apiRef.current.updateRows(updates);
      apiRef.current.publishEvent("rowsSet");
    }
  }
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rowReorder/rowGroupingReorderExecutor.js
var rowGroupingReorderExecutor = new RowReorderExecutor([new SameParentSwapOperation(), new CrossParentLeafOperation2(), new CrossParentGroupOperation2()]);

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rows/useGridRowsOverridableMethods.js
var useGridRowsOverridableMethods3 = (apiRef, props) => {
  const {
    processRowUpdate,
    onProcessRowUpdateError
  } = props;
  const {
    setRowIndex: setRowIndexPlain,
    setRowPosition: setRowPositionPlain
  } = useGridRowsOverridableMethods(apiRef);
  const {
    setRowIndex: setRowIndexTreeData,
    setRowPosition: setRowPositionTreeData
  } = useGridRowsOverridableMethods2(apiRef, props);
  const flatTree = useGridSelector(apiRef, gridRowMaximumTreeDepthSelector) === 1;
  const setRowPosition = React368.useCallback(async (sourceRowId, targetRowId, position) => {
    const sortedFilteredRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const sortedFilteredRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    const targetNode = gridRowNodeSelector(apiRef, targetRowId);
    if (!sourceNode) {
      throw new Error(`MUI X: No row with id #${sourceRowId} found.`);
    }
    if (!targetNode) {
      throw new Error(`MUI X: No row with id #${targetRowId} found.`);
    }
    if (sourceNode.type === "footer") {
      throw new Error(`MUI X: The row reordering do not support reordering of footer rows.`);
    }
    const targetIndexUnadjusted = sortedFilteredRowIndexLookup[targetRowId];
    if (targetIndexUnadjusted === void 0) {
      throw new Error(`MUI X: Target row with id #${targetRowId} not found in current view.`);
    }
    const targetIndex = position === "below" ? targetIndexUnadjusted + 1 : targetIndexUnadjusted;
    const executionContext = {
      sourceRowId,
      dropPosition: position,
      placeholderIndex: targetIndex,
      sortedFilteredRowIds,
      sortedFilteredRowIndexLookup,
      rowTree,
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError
    };
    return rowGroupingReorderExecutor.execute(executionContext);
  }, [apiRef, processRowUpdate, onProcessRowUpdateError]);
  const setRowIndex = React368.useCallback(async (sourceRowId, targetOriginalIndex) => {
    const sortedFilteredRowIds = gridExpandedSortedRowIdsSelector(apiRef);
    const sortedFilteredRowIndexLookup = gridExpandedSortedRowIndexLookupSelector(apiRef);
    const rowTree = gridRowTreeSelector(apiRef);
    const sourceNode = gridRowNodeSelector(apiRef, sourceRowId);
    if (!sourceNode) {
      throw new Error(`MUI X: No row with id #${sourceRowId} found.`);
    }
    if (sourceNode.type === "footer") {
      throw new Error(`MUI X: The row reordering do not support reordering of footer rows.`);
    }
    const executionContext = {
      sourceRowId,
      dropPosition: "below",
      placeholderIndex: targetOriginalIndex,
      sortedFilteredRowIds,
      sortedFilteredRowIndexLookup,
      rowTree,
      apiRef,
      processRowUpdate,
      onProcessRowUpdateError
    };
    return rowGroupingReorderExecutor.execute(executionContext);
  }, [apiRef, processRowUpdate, onProcessRowUpdateError]);
  if (flatTree) {
    return {
      setRowIndex: setRowIndexPlain,
      setRowPosition: setRowPositionPlain
    };
  }
  if (props.treeData) {
    return {
      setRowIndex: setRowIndexTreeData,
      setRowPosition: setRowPositionTreeData
    };
  }
  return {
    setRowIndex,
    setRowPosition
  };
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/rows/useGridParamsOverridableMethods.js
var React369 = __toESM(require_react(), 1);
var useGridParamsOverridableMethods2 = (apiRef) => {
  const communityMethods = useGridParamsOverridableMethods(apiRef);
  const getCellValue2 = React369.useCallback((id, field) => {
    var _a;
    return ((_a = gridCellAggregationResultSelector(apiRef, {
      id,
      field
    })) == null ? void 0 : _a.value) ?? communityMethods.getCellValue(id, field);
  }, [apiRef, communityMethods]);
  const getRowValue2 = React369.useCallback((row, colDef) => {
    var _a;
    return ((_a = gridCellAggregationResultSelector(apiRef, {
      id: gridRowIdSelector(apiRef, row),
      field: colDef.field
    })) == null ? void 0 : _a.value) ?? communityMethods.getRowValue(row, colDef);
  }, [apiRef, communityMethods]);
  const getRowFormattedValue = React369.useCallback((row, colDef) => {
    var _a;
    const aggregationFormattedValue = (_a = gridCellAggregationResultSelector(apiRef, {
      id: gridRowIdSelector(apiRef, row),
      field: colDef.field
    })) == null ? void 0 : _a.formattedValue;
    if (aggregationFormattedValue !== void 0) {
      return aggregationFormattedValue;
    }
    const value = getRowValue2(row, colDef);
    if (!colDef || !colDef.valueFormatter) {
      return value;
    }
    return colDef.valueFormatter(value, row, colDef, apiRef);
  }, [apiRef, getRowValue2]);
  return {
    getCellValue: getCellValue2,
    getRowValue: getRowValue2,
    getRowFormattedValue
  };
};

// node_modules/@mui/x-data-grid-premium/esm/hooks/features/editing/useGridCellEditable.js
var React370 = __toESM(require_react(), 1);
var useIsCellEditable2 = (apiRef, props) => {
  const isCellEditableCommunity = useIsCellEditable();
  return React370.useCallback((params) => {
    const isCellEditable = isCellEditableCommunity(params);
    if (!isCellEditable) {
      return false;
    }
    if (!props.dataSource || props.disableAggregation || !props.treeData && props.disableRowGrouping) {
      return isCellEditable;
    }
    const aggregationModelFields = Object.keys(gridAggregationModelSelector(apiRef));
    if (!aggregationModelFields.includes(params.field)) {
      return isCellEditable;
    }
    return params.rowNode.type !== "group";
  }, [apiRef, props.dataSource, props.treeData, props.disableAggregation, props.disableRowGrouping, isCellEditableCommunity]);
};

// node_modules/@mui/x-data-grid-premium/esm/DataGridPremium/DataGridPremium.js
var import_jsx_runtime268 = __toESM(require_jsx_runtime(), 1);
var configuration3 = {
  hooks: {
    useCSSVariables: useMaterialCSSVariables,
    useGridAriaAttributes: useGridAriaAttributesPremium,
    useGridRowAriaAttributes: useGridRowAriaAttributesPremium,
    useCellAggregationResult: (id, field) => {
      const apiRef = useGridApiContext3();
      return useGridSelector(apiRef, gridCellAggregationResultSelector, {
        id,
        field
      });
    },
    useFilterValueGetter: (apiRef, props) => (row, column) => {
      if (props.aggregationRowsScope === "all") {
        return apiRef.current.getRowValue(row, column);
      }
      return getRowValue(row, column, apiRef);
    },
    useIsCellEditable: useIsCellEditable2,
    useGridRowsOverridableMethods: useGridRowsOverridableMethods3,
    useGridParamsOverridableMethods: useGridParamsOverridableMethods2
  }
};
var releaseInfo2 = "MTc3Mzk2NDgwMDAwMA==";
var watermark2 = (0, import_jsx_runtime268.jsx)(MemoizedWatermark, {
  packageName: "x-data-grid-premium",
  releaseInfo: releaseInfo2
});
var dataGridPremiumPropValidators;
if (true) {
  dataGridPremiumPropValidators = [...propValidatorsDataGrid, ...propValidatorsDataGridPro];
}
var DataGridPremiumRaw = forwardRef2(function DataGridPremium(inProps, ref) {
  var _a;
  const initialProps = useDataGridPremiumProps(inProps);
  const privateApiRef = useGridApiInitialization(initialProps.apiRef, initialProps);
  const props = useDataGridPremiumComponent(privateApiRef, initialProps, configuration3);
  useLicenseVerifier("x-data-grid-premium", releaseInfo2);
  if (true) {
    validateProps(props, dataGridPremiumPropValidators);
  }
  const sidebarOpen = useGridSelector(privateApiRef, gridSidebarOpenSelector);
  const sidePanel = sidebarOpen ? (0, import_jsx_runtime268.jsx)(Sidebar, {}) : null;
  return (0, import_jsx_runtime268.jsx)(GridContextProvider, {
    privateApiRef,
    configuration: configuration3,
    props,
    children: (0, import_jsx_runtime268.jsx)(MemoizedGridRoot, _extends({
      className: props.className,
      style: props.style,
      sx: props.sx
    }, (_a = props.slotProps) == null ? void 0 : _a.root, {
      ref,
      sidePanel,
      children: watermark2
    }))
  });
});
if (true) DataGridPremiumRaw.displayName = "DataGridPremiumRaw";
true ? DataGridPremiumRaw.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The id of the active chart.
   */
  activeChartId: import_prop_types163.default.string,
  /**
   * Aggregation functions available on the grid.
   * @default GRID_AGGREGATION_FUNCTIONS when `dataSource` is not provided, `{}` when `dataSource` is provided
   */
  aggregationFunctions: import_prop_types163.default.object,
  /**
   * Set the aggregation model of the grid.
   */
  aggregationModel: import_prop_types163.default.object,
  /**
   * Rows used to generate the aggregated value.
   * If `filtered`, the aggregated values are generated using only the rows currently passing the filtering process.
   * If `all`, the aggregated values are generated using all the rows.
   * @default "filtered"
   */
  aggregationRowsScope: import_prop_types163.default.oneOf(["all", "filtered"]),
  /**
   * If `true`, the AI Assistant is enabled.
   * @default false
   */
  aiAssistant: import_prop_types163.default.bool,
  /**
   * The index of the active AI Assistant conversation.
   */
  aiAssistantActiveConversationIndex: import_prop_types163.default.number,
  /**
   * The conversations with the AI Assistant.
   */
  aiAssistantConversations: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
    id: import_prop_types163.default.string,
    prompts: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
      createdAt: import_prop_types163.default.instanceOf(Date).isRequired,
      helperText: import_prop_types163.default.string,
      response: import_prop_types163.default.shape({
        aggregation: import_prop_types163.default.object.isRequired,
        chart: import_prop_types163.default.object,
        conversationId: import_prop_types163.default.string.isRequired,
        filterOperator: import_prop_types163.default.oneOf(["and", "or"]),
        filters: import_prop_types163.default.arrayOf(import_prop_types163.default.object).isRequired,
        grouping: import_prop_types163.default.arrayOf(import_prop_types163.default.object).isRequired,
        pivoting: import_prop_types163.default.object.isRequired,
        select: import_prop_types163.default.number.isRequired,
        sorting: import_prop_types163.default.arrayOf(import_prop_types163.default.object).isRequired
      }),
      value: import_prop_types163.default.string.isRequired,
      variant: import_prop_types163.default.oneOf(["error", "processing", "success"])
    })).isRequired,
    title: import_prop_types163.default.string
  })),
  /**
   * The suggestions of the AI Assistant.
   */
  aiAssistantSuggestions: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
    value: import_prop_types163.default.string.isRequired
  })),
  /**
   * If `true`, the AI Assistant is allowed to pick up values from random cells from each column to build the prompt context.
   */
  allowAiAssistantDataSampling: import_prop_types163.default.bool,
  /**
   * The ref object that allows grid manipulation. Can be instantiated with `useGridApiRef()`.
   */
  apiRef: import_prop_types163.default.shape({
    current: import_prop_types163.default.object
  }),
  /**
   * The `aria-label` of the Data Grid.
   */
  "aria-label": import_prop_types163.default.string,
  /**
   * The `id` of the element containing a label for the Data Grid.
   */
  "aria-labelledby": import_prop_types163.default.string,
  /**
   * If `true`, the Data Grid height is dynamic and takes as much space as it needs to display all rows.
   * Use it instead of a flex parent container approach, if:
   * - you don't need to set a minimum or maximum height for the Data Grid
   * - you want to avoid the scrollbar flickering when the content changes
   * @default false
   */
  autoHeight: import_prop_types163.default.bool,
  /**
   * If `true`, the pageSize is calculated according to the container size and the max number of rows to avoid rendering a vertical scroll bar.
   * @default false
   */
  autoPageSize: import_prop_types163.default.bool,
  /**
   * If `true`, columns are autosized after the datagrid is mounted.
   * @default false
   */
  autosizeOnMount: import_prop_types163.default.bool,
  /**
   * The options for autosize when user-initiated.
   */
  autosizeOptions: import_prop_types163.default.shape({
    columns: import_prop_types163.default.arrayOf(import_prop_types163.default.string),
    disableColumnVirtualization: import_prop_types163.default.bool,
    expand: import_prop_types163.default.bool,
    includeHeaderFilters: import_prop_types163.default.bool,
    includeHeaders: import_prop_types163.default.bool,
    includeOutliers: import_prop_types163.default.bool,
    outliersFactor: import_prop_types163.default.number
  }),
  /**
   * Controls the modes of the cells.
   */
  cellModesModel: import_prop_types163.default.object,
  /**
   * If `true`, the cell selection mode is enabled.
   * @default false
   */
  cellSelection: import_prop_types163.default.bool,
  /**
   * Set the cell selection model of the grid.
   */
  cellSelectionModel: import_prop_types163.default.object,
  /**
   * If `true`, the charts integration feature is enabled.
   * @default false
   */
  chartsIntegration: import_prop_types163.default.bool,
  /**
   * If `true`, the Data Grid will display an extra column with checkboxes for selecting rows.
   * @default false
   */
  checkboxSelection: import_prop_types163.default.bool,
  /**
   * If `true`, the "Select All" header checkbox selects only the rows on the current page. To be used in combination with `checkboxSelection`.
   * It only works if the pagination is enabled.
   * @default false
   */
  checkboxSelectionVisibleOnly: import_prop_types163.default.bool,
  /**
   * Override or extend the styles applied to the component.
   */
  classes: import_prop_types163.default.object,
  className: import_prop_types163.default.string,
  /**
   * The character used to separate cell values when copying to the clipboard.
   * @default '\t'
   */
  clipboardCopyCellDelimiter: import_prop_types163.default.string,
  /**
   * Column region in pixels to render before/after the viewport
   * @default 150
   */
  columnBufferPx: import_prop_types163.default.number,
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering in the columns menu.
   * @default 150
   */
  columnFilterDebounceMs: import_prop_types163.default.number,
  /**
   * Sets the height in pixels of the column group headers in the Data Grid.
   * Inherits the `columnHeaderHeight` value if not set.
   */
  columnGroupHeaderHeight: import_prop_types163.default.number,
  columnGroupingModel: import_prop_types163.default.arrayOf(import_prop_types163.default.object),
  /**
   * Sets the height in pixel of the column headers in the Data Grid.
   * @default 56
   */
  columnHeaderHeight: import_prop_types163.default.number,
  /**
   * Set of columns of type [[GridColDef]][].
   */
  columns: import_prop_types163.default.arrayOf(import_prop_types163.default.object).isRequired,
  /**
   * Set the column visibility model of the Data Grid.
   * If defined, the Data Grid will ignore the `hide` property in [[GridColDef]].
   */
  columnVisibilityModel: import_prop_types163.default.object,
  /**
   * Data source object.
   */
  dataSource: import_prop_types163.default.shape({
    getAggregatedValue: import_prop_types163.default.func,
    getChildrenCount: import_prop_types163.default.func,
    getGroupKey: import_prop_types163.default.func,
    getRows: import_prop_types163.default.func.isRequired,
    updateRow: import_prop_types163.default.func
  }),
  /**
   * Data source cache object.
   */
  dataSourceCache: import_prop_types163.default.shape({
    clear: import_prop_types163.default.func.isRequired,
    get: import_prop_types163.default.func.isRequired,
    set: import_prop_types163.default.func.isRequired
  }),
  /**
   * If positive, the Data Grid will periodically revalidate data source rows by re-fetching them from the server when the cache entry has expired.
   * If the refetched rows are different from the current rows, the grid will update the rows.
   * Set to `0` to disable polling.
   * @default 0
   */
  dataSourceRevalidateMs: import_prop_types163.default.number,
  /**
   * If above 0, the row children will be expanded up to this depth.
   * If equal to -1, all the row children will be expanded.
   * @default 0
   */
  defaultGroupingExpansionDepth: import_prop_types163.default.number,
  /**
   * Set the density of the Data Grid.
   * @default "standard"
   */
  density: import_prop_types163.default.oneOf(["comfortable", "compact", "standard"]),
  /**
   * The row ids to show the detail panel.
   */
  detailPanelExpandedRowIds: import_prop_types163.default.instanceOf(Set),
  /**
   * If `true`, aggregation is disabled.
   * @default false
   */
  disableAggregation: import_prop_types163.default.bool,
  /**
   * If `true`, column autosizing on header separator double-click is disabled.
   * @default false
   */
  disableAutosize: import_prop_types163.default.bool,
  /**
   * If `true`, the filtering will only be applied to the top level rows when grouping rows with the `treeData` prop.
   * @default false
   */
  disableChildrenFiltering: import_prop_types163.default.bool,
  /**
   * If `true`, the sorting will only be applied to the top level rows when grouping rows with the `treeData` prop.
   * @default false
   */
  disableChildrenSorting: import_prop_types163.default.bool,
  /**
   * If `true`, the clipboard paste is disabled.
   * @default false
   */
  disableClipboardPaste: import_prop_types163.default.bool,
  /**
   * If `true`, column filters are disabled.
   * @default false
   */
  disableColumnFilter: import_prop_types163.default.bool,
  /**
   * If `true`, the column menu is disabled.
   * @default false
   */
  disableColumnMenu: import_prop_types163.default.bool,
  /**
   * If `true`, the column pinning is disabled.
   * @default false
   */
  disableColumnPinning: import_prop_types163.default.bool,
  /**
   * If `true`, reordering columns is disabled.
   * @default false
   */
  disableColumnReorder: import_prop_types163.default.bool,
  /**
   * If `true`, resizing columns is disabled.
   * @default false
   */
  disableColumnResize: import_prop_types163.default.bool,
  /**
   * If `true`, hiding/showing columns is disabled.
   * @default false
   */
  disableColumnSelector: import_prop_types163.default.bool,
  /**
   * If `true`, the column sorting feature will be disabled.
   * @default false
   */
  disableColumnSorting: import_prop_types163.default.bool,
  /**
   * If `true`, the density selector is disabled.
   * @default false
   */
  disableDensitySelector: import_prop_types163.default.bool,
  /**
   * If `true`, `eval()` is not used for performance optimization.
   * @default false
   */
  disableEval: import_prop_types163.default.bool,
  /**
   * If `true`, filtering with multiple columns is disabled.
   * @default false
   */
  disableMultipleColumnsFiltering: import_prop_types163.default.bool,
  /**
   * If `true`, the sorting with multiple columns is disabled.
   * @default false
   */
  disableMultipleColumnsSorting: import_prop_types163.default.bool,
  /**
   * If `true`, multiple selection using the Ctrl/CMD or Shift key is disabled.
   * The MIT DataGrid will ignore this prop, unless `checkboxSelection` is enabled.
   * @default false (`!props.checkboxSelection` for MIT Data Grid)
   */
  disableMultipleRowSelection: import_prop_types163.default.bool,
  /**
   * If `true`, the pivoting feature is disabled.
   * @default false
   */
  disablePivoting: import_prop_types163.default.bool,
  /**
   * If `true`, the row grouping is disabled.
   * @default false
   */
  disableRowGrouping: import_prop_types163.default.bool,
  /**
   * If `true`, the Data Grid will not use the exclude model optimization when selecting all rows.
   * @default false
   */
  disableRowSelectionExcludeModel: import_prop_types163.default.bool,
  /**
   * If `true`, the selection on click on a row or cell is disabled.
   * @default false
   */
  disableRowSelectionOnClick: import_prop_types163.default.bool,
  /**
   * If `true`, the virtualization is disabled.
   * @default false
   */
  disableVirtualization: import_prop_types163.default.bool,
  /**
   * Controls whether to use the cell or row editing.
   * @default "cell"
   */
  editMode: import_prop_types163.default.oneOf(["cell", "row"]),
  /**
   * Use if the actual rowCount is not known upfront, but an estimation is available.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Applicable only with `paginationMode="server"` and when `rowCount="-1"`
   */
  estimatedRowCount: import_prop_types163.default.number,
  /**
   * Unstable features, breaking changes might be introduced.
   * For each feature, if the flag is not explicitly set to `true`, then the feature is fully disabled, and neither property nor method calls will have any effect.
   */
  experimentalFeatures: import_prop_types163.default.shape({
    charts: import_prop_types163.default.bool,
    warnIfFocusStateIsNotSynced: import_prop_types163.default.bool
  }),
  /**
   * The milliseconds delay to wait after a keystroke before triggering filtering.
   * @default 150
   */
  filterDebounceMs: import_prop_types163.default.number,
  /**
   * Filtering can be processed on the server or client-side.
   * Set it to 'server' if you would like to handle filtering on the server-side.
   * @default "client"
   */
  filterMode: import_prop_types163.default.oneOf(["client", "server"]),
  /**
   * Set the filter model of the Data Grid.
   */
  filterModel: import_prop_types163.default.shape({
    items: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
      field: import_prop_types163.default.string.isRequired,
      id: import_prop_types163.default.oneOfType([import_prop_types163.default.number, import_prop_types163.default.string]),
      operator: import_prop_types163.default.string.isRequired,
      value: import_prop_types163.default.any
    })).isRequired,
    logicOperator: import_prop_types163.default.oneOf(["and", "or"]),
    quickFilterExcludeHiddenColumns: import_prop_types163.default.bool,
    quickFilterLogicOperator: import_prop_types163.default.oneOf(["and", "or"]),
    quickFilterValues: import_prop_types163.default.array
  }),
  /**
   * Determines the position of an aggregated value.
   * @param {GridGroupNode} groupNode The current group.
   * @returns {GridAggregationPosition | null} Position of the aggregated value (if `null`, the group isn't aggregated).
   * @default (groupNode) => (groupNode.depth === -1 ? 'footer' : 'inline')
   */
  getAggregationPosition: import_prop_types163.default.func,
  /**
   * Function that applies CSS classes dynamically on cells.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {string} The CSS class to apply to the cell.
   */
  getCellClassName: import_prop_types163.default.func,
  /**
   * Function that returns the element to render in row detail.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {React.JSX.Element} The row detail element.
   */
  getDetailPanelContent: import_prop_types163.default.func,
  /**
   * Function that returns the height of the row detail panel.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {number | string} The height in pixels or "auto" to use the content height.
   * @default "() => 500"
   */
  getDetailPanelHeight: import_prop_types163.default.func,
  /**
   * Function that returns the estimated height for a row.
   * Only works if dynamic row height is used.
   * Once the row height is measured this value is discarded.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {number | null} The estimated row height value. If `null` or `undefined` then the default row height, based on the density, is applied.
   */
  getEstimatedRowHeight: import_prop_types163.default.func,
  /**
   * Allows to generate derived columns from actual columns that will be used for pivoting.
   * Useful e.g. for date columns to generate year, quarter, month, etc.
   * @param {GridColDef} column The column to generate derived columns for.
   * @param {GridLocaleTextApi['getLocaleText']} getLocaleText The function to get the locale text.
   * @returns {GridColDef[] | undefined} The derived columns.
   * @default {defaultGetPivotDerivedColumns | undefined} Creates year and quarter columns for date columns if not in server side mode.
   */
  getPivotDerivedColumns: import_prop_types163.default.func,
  /**
   * Function that applies CSS classes dynamically on rows.
   * @param {GridRowClassNameParams} params With all properties from [[GridRowClassNameParams]].
   * @returns {string} The CSS class to apply to the row.
   */
  getRowClassName: import_prop_types163.default.func,
  /**
   * Function that sets the row height per row.
   * @param {GridRowHeightParams} params With all properties from [[GridRowHeightParams]].
   * @returns {GridRowHeightReturnValue} The row height value. If `null` or `undefined` then the default row height is applied. If "auto" then the row height is calculated based on the content.
   */
  getRowHeight: import_prop_types163.default.func,
  /**
   * Return the id of a given [[GridRowModel]].
   * Ensure the reference of this prop is stable to avoid performance implications.
   * It could be done by either defining the prop outside of the component or by memoizing it.
   */
  getRowId: import_prop_types163.default.func,
  /**
   * Function that allows to specify the spacing between rows.
   * @param {GridRowSpacingParams} params With all properties from [[GridRowSpacingParams]].
   * @returns {GridRowSpacing} The row spacing values.
   */
  getRowSpacing: import_prop_types163.default.func,
  /**
   * Determines the path of a row in the tree data.
   * For instance, a row with the path ["A", "B"] is the child of the row with the path ["A"].
   * Note that all paths must contain at least one element.
   * @template R
   * @param {R} row The row from which we want the path.
   * @returns {string[]} The path to the row.
   */
  getTreeDataPath: import_prop_types163.default.func,
  /**
   * The grouping column used by the tree data.
   */
  groupingColDef: import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.object]),
  /**
   * Override the height of the header filters.
   */
  headerFilterHeight: import_prop_types163.default.number,
  /**
   * If `true`, the header filters feature is enabled.
   * @default false
   */
  headerFilters: import_prop_types163.default.bool,
  /**
   * If `true`, the footer component is hidden.
   * @default false
   */
  hideFooter: import_prop_types163.default.bool,
  /**
   * If `true`, the pagination component in the footer is hidden.
   * @default false
   */
  hideFooterPagination: import_prop_types163.default.bool,
  /**
   * If `true`, the row count in the footer is hidden.
   * It has no effect if the pagination is enabled.
   * @default false
   */
  hideFooterRowCount: import_prop_types163.default.bool,
  /**
   * If `true`, the selected row count in the footer is hidden.
   * @default false
   */
  hideFooterSelectedRowCount: import_prop_types163.default.bool,
  /**
   * Map of grid events to their undo/redo handlers.
   * @default Handlers for `rowEditStop`, `cellEditStop` and `clipboardPasteEnd` events
   */
  historyEventHandlers: import_prop_types163.default.object,
  /**
   * The maximum size of the history stack.
   * Set to 0 to disable the undo/redo feature.
   * @default 30
   */
  historyStackSize: import_prop_types163.default.number,
  /**
   * List of grid events after which the history stack items should be re-validated.
   * @default ['columnsChange', 'rowsSet', 'sortedRowsSet', 'filteredRowsSet', 'paginationModelChange']
   */
  historyValidationEvents: import_prop_types163.default.arrayOf(import_prop_types163.default.oneOf(["activeChartIdChange", "activeStrategyProcessorChange", "aggregationLookupSet", "aggregationModelChange", "aiAssistantActiveConversationIndexChange", "aiAssistantConversationsChange", "cellClick", "cellDoubleClick", "cellDragEnter", "cellDragOver", "cellEditStart", "cellEditStop", "cellFocusIn", "cellFocusOut", "cellKeyDown", "cellKeyUp", "cellModeChange", "cellModesModelChange", "cellMouseDown", "cellMouseOver", "cellMouseUp", "cellSelectionChange", "chartSynchronizationStateChange", "clipboardCopy", "clipboardPasteEnd", "clipboardPasteStart", "columnGroupHeaderBlur", "columnGroupHeaderFocus", "columnGroupHeaderKeyDown", "columnHeaderBlur", "columnHeaderClick", "columnHeaderContextMenu", "columnHeaderDoubleClick", "columnHeaderDragEnd", "columnHeaderDragEndNative", "columnHeaderDragEnter", "columnHeaderDragOver", "columnHeaderDragStart", "columnHeaderEnter", "columnHeaderFocus", "columnHeaderKeyDown", "columnHeaderLeave", "columnHeaderOut", "columnHeaderOver", "columnIndexChange", "columnOrderChange", "columnResize", "columnResizeStart", "columnResizeStop", "columnsChange", "columnSeparatorDoubleClick", "columnSeparatorMouseDown", "columnVisibilityModelChange", "columnWidthChange", "debouncedResize", "densityChange", "detailPanelsExpandedRowIdsChange", "excelExportStateChange", "fetchRows", "filteredRowsSet", "filterModelChange", "headerFilterBlur", "headerFilterClick", "headerFilterKeyDown", "headerFilterMouseDown", "headerSelectionCheckboxChange", "menuClose", "menuOpen", "paginationMetaChange", "paginationModelChange", "pinnedColumnsChange", "pivotModeChange", "pivotModelChange", "pivotPanelOpenChange", "preferencePanelClose", "preferencePanelOpen", "redo", "renderedRowsIntervalChange", "resize", "rootMount", "rowClick", "rowCountChange", "rowDoubleClick", "rowDragEnd", "rowDragOver", "rowDragStart", "rowEditStart", "rowEditStop", "rowExpansionChange", "rowGroupingModelChange", "rowModesModelChange", "rowMouseEnter", "rowMouseLeave", "rowMouseOut", "rowMouseOver", "rowOrderChange", "rowSelectionChange", "rowSelectionCheckboxChange", "rowsScrollEnd", "rowsScrollEndIntersection", "rowsSet", "scrollPositionChange", "sidebarClose", "sidebarOpen", "sortedRowsSet", "sortModelChange", "stateChange", "strategyAvailabilityChange", "undo", "unmount", "viewportInnerSizeChange", "virtualScrollerContentSizeChange", "virtualScrollerTouchMove", "virtualScrollerWheel"]).isRequired),
  /**
   * If `true`, the diacritics (accents) are ignored when filtering or quick filtering.
   * E.g. when filter value is `cafe`, the rows with `café` will be visible.
   * @default false
   */
  ignoreDiacritics: import_prop_types163.default.bool,
  /**
   * If `true`, the Data Grid will not use `valueFormatter` when exporting to CSV or copying to clipboard.
   * If an object is provided, you can choose to ignore the `valueFormatter` for CSV export or clipboard export.
   * @default false
   */
  ignoreValueFormatterDuringExport: import_prop_types163.default.oneOfType([import_prop_types163.default.shape({
    clipboardExport: import_prop_types163.default.bool,
    csvExport: import_prop_types163.default.bool
  }), import_prop_types163.default.bool]),
  /**
   * The initial state of the DataGridPremium.
   * The data in it is set in the state on initialization but isn't controlled.
   * If one of the data in `initialState` is also being controlled, then the control state wins.
   */
  initialState: import_prop_types163.default.object,
  /**
   * Callback fired when a cell is rendered, returns true if the cell is editable.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @returns {boolean} A boolean indicating if the cell is editable.
   */
  isCellEditable: import_prop_types163.default.func,
  /**
   * Determines if a group should be expanded after its creation.
   * This prop takes priority over the `defaultGroupingExpansionDepth` prop.
   * @param {GridGroupNode} node The node of the group to test.
   * @returns {boolean} A boolean indicating if the group is expanded.
   */
  isGroupExpandedByDefault: import_prop_types163.default.func,
  /**
   * Indicates whether a row is reorderable.
   * @param {object} params With all properties from the row.
   * @param {R} params.row The row model of the row that the current cell belongs to.
   * @param {GridTreeNode} params.rowNode The node of the row that the current cell belongs to.
   * @returns {boolean} A boolean indicating if the row is reorderable.
   */
  isRowReorderable: import_prop_types163.default.func,
  /**
   * Determines if a row can be selected.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @returns {boolean} A boolean indicating if the row is selectable.
   */
  isRowSelectable: import_prop_types163.default.func,
  /**
   * Indicates if a row reorder attempt is valid.
   * Can be used to disable certain row reorder operations based on the context.
   * The internal validation is still applied, preventing unsupported use-cases.
   * Use `isValidRowReorder()` to add additional validation rules to the default ones.
   * @param {ReorderValidationContext} context The context object containing all information about the reorder operation.
   * @returns {boolean} A boolean indicating if the reorder operation should go through.
   */
  isValidRowReorder: import_prop_types163.default.func,
  /**
   * If `true`, moving the mouse pointer outside the grid before releasing the mouse button
   * in a column re-order action will not cause the column to jump back to its original position.
   * @default false
   */
  keepColumnPositionIfDraggedOutside: import_prop_types163.default.bool,
  /**
   * If `true`, the selection model will retain selected rows that do not exist.
   * Useful when using server side pagination and row selections need to be retained
   * when changing pages.
   * @default false
   */
  keepNonExistentRowsSelected: import_prop_types163.default.bool,
  /**
   * The label of the Data Grid.
   * If the `showToolbar` prop is `true`, the label will be displayed in the toolbar and applied to the `aria-label` attribute of the grid.
   * If the `showToolbar` prop is `false`, the label will not be visible but will be applied to the `aria-label` attribute of the grid.
   */
  label: import_prop_types163.default.string,
  /**
   * Used together with `dataSource` to enable lazy loading.
   * If enabled, the grid stops adding `paginationModel` to the data requests (`getRows`)
   * and starts sending `start` and `end` values depending on the loading mode and the scroll position.
   * @default false
   */
  lazyLoading: import_prop_types163.default.bool,
  /**
   * If positive, the Data Grid will throttle data source requests on rendered rows interval change.
   * @default 500
   */
  lazyLoadingRequestThrottleMs: import_prop_types163.default.number,
  /**
   * If `true`, displays the data in a list view.
   * Use in combination with `listViewColumn`.
   */
  listView: import_prop_types163.default.bool,
  /**
   * Definition of the column rendered when the `listView` prop is enabled.
   */
  listViewColumn: import_prop_types163.default.shape({
    align: import_prop_types163.default.oneOf(["center", "left", "right"]),
    cellClassName: import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.string]),
    display: import_prop_types163.default.oneOf(["flex", "text"]),
    field: import_prop_types163.default.string.isRequired,
    renderCell: import_prop_types163.default.func
  }),
  /**
   * If `true`, a loading overlay is displayed.
   * @default false
   */
  loading: import_prop_types163.default.bool,
  /**
   * Set the locale text of the Data Grid.
   * You can find all the translation keys supported in [the source](https://github.com/mui/mui-x/blob/HEAD/packages/x-data-grid/src/constants/localeTextConstants.ts) in the GitHub repository.
   */
  localeText: import_prop_types163.default.object,
  /**
   * Pass a custom logger in the components that implements the [[Logger]] interface.
   * @default console
   */
  logger: import_prop_types163.default.shape({
    debug: import_prop_types163.default.func.isRequired,
    error: import_prop_types163.default.func.isRequired,
    info: import_prop_types163.default.func.isRequired,
    warn: import_prop_types163.default.func.isRequired
  }),
  /**
   * Allows to pass the logging level or false to turn off logging.
   * @default "error" ("warn" in dev mode)
   */
  logLevel: import_prop_types163.default.oneOf(["debug", "error", "info", "warn", false]),
  /**
   * If set to "always", the multi-sorting is applied without modifier key.
   * Otherwise, the modifier key is required for multi-sorting to be applied.
   * @see See https://mui.com/x/react-data-grid/sorting/#multi-sorting
   * @default "withModifierKey"
   */
  multipleColumnsSortingMode: import_prop_types163.default.oneOf(["always", "withModifierKey"]),
  /**
   * Nonce of the inline styles for [Content Security Policy](https://www.w3.org/TR/2016/REC-CSP2-20161215/#script-src-the-nonce-attribute).
   */
  nonce: import_prop_types163.default.string,
  /**
   * Callback fired when the active chart changes.
   * @param {string} activeChartId The new active chart id.
   */
  onActiveChartIdChange: import_prop_types163.default.func,
  /**
   * Callback fired when the row grouping model changes.
   * @param {GridAggregationModel} model The aggregated columns.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onAggregationModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the AI Assistant active conversation index changes.
   * @param {number} aiAssistantActiveConversationIndex The new active conversation index.
   */
  onAiAssistantActiveConversationIndexChange: import_prop_types163.default.func,
  /**
   * Callback fired when the AI Assistant conversations change.
   * @param {Conversation[]} conversations The new AI Assistant conversations.
   */
  onAiAssistantConversationsChange: import_prop_types163.default.func,
  /**
   * Callback fired before the clipboard paste operation starts.
   * Use it to confirm or cancel the paste operation.
   * @param {object} params Params passed to the callback.
   * @param {string[][]} params.data The raw pasted data split by rows and cells.
   * @returns {Promise<any>} A promise that resolves to confirm the paste operation, and rejects to cancel it.
   */
  onBeforeClipboardPasteStart: import_prop_types163.default.func,
  /**
   * Callback fired when any cell is clicked.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellClick: import_prop_types163.default.func,
  /**
   * Callback fired when a double click event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellDoubleClick: import_prop_types163.default.func,
  /**
   * Callback fired when the cell turns to edit mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStart: import_prop_types163.default.func,
  /**
   * Callback fired when the cell turns to view mode.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onCellEditStop: import_prop_types163.default.func,
  /**
   * Callback fired when a keydown event comes from a cell element.
   * @param {GridCellParams} params With all properties from [[GridCellParams]].
   * @param {MuiEvent<React.KeyboardEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellKeyDown: import_prop_types163.default.func,
  /**
   * Callback fired when the `cellModesModel` prop changes.
   * @param {GridCellModesModel} cellModesModel Object containing which cells are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellModesModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the selection state of one or multiple cells changes.
   * @param {GridCellSelectionModel} cellSelectionModel Object in the shape of [[GridCellSelectionModel]] containing the selected cells.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onCellSelectionModelChange: import_prop_types163.default.func,
  /**
   * Callback called when the data is copied to the clipboard.
   * @param {string} data The data copied to the clipboard.
   */
  onClipboardCopy: import_prop_types163.default.func,
  /**
   * Callback fired when the clipboard paste operation ends.
   */
  onClipboardPasteEnd: import_prop_types163.default.func,
  /**
   * Callback fired when the clipboard paste operation starts.
   */
  onClipboardPasteStart: import_prop_types163.default.func,
  /**
   * Callback fired when a click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderClick: import_prop_types163.default.func,
  /**
   * Callback fired when a contextmenu event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   */
  onColumnHeaderContextMenu: import_prop_types163.default.func,
  /**
   * Callback fired when a double click event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderDoubleClick: import_prop_types163.default.func,
  /**
   * Callback fired when a mouse enter event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderEnter: import_prop_types163.default.func,
  /**
   * Callback fired when a mouse leave event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderLeave: import_prop_types163.default.func,
  /**
   * Callback fired when a mouseout event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOut: import_prop_types163.default.func,
  /**
   * Callback fired when a mouseover event comes from a column header element.
   * @param {GridColumnHeaderParams} params With all properties from [[GridColumnHeaderParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnHeaderOver: import_prop_types163.default.func,
  /**
   * Callback fired when a column is reordered.
   * @param {GridColumnOrderChangeParams} params With all properties from [[GridColumnOrderChangeParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnOrderChange: import_prop_types163.default.func,
  /**
   * Callback fired while a column is being resized.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnResize: import_prop_types163.default.func,
  /**
   * Callback fired when the column visibility model changes.
   * @param {GridColumnVisibilityModel} model The new model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnVisibilityModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the width of a column is changed.
   * @param {GridColumnResizeParams} params With all properties from [[GridColumnResizeParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onColumnWidthChange: import_prop_types163.default.func,
  /**
   * Callback fired when a data source request fails.
   * @param {GridGetRowsError | GridUpdateRowError} error The data source error object.
   */
  onDataSourceError: import_prop_types163.default.func,
  /**
   * Callback fired when the density changes.
   * @param {GridDensity} density New density value.
   */
  onDensityChange: import_prop_types163.default.func,
  /**
   * Callback fired when the detail panel of a row is opened or closed.
   * @param {GridRowId[]} ids The ids of the rows which have the detail panel open.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onDetailPanelExpandedRowIdsChange: import_prop_types163.default.func,
  /**
   * Callback fired when the state of the Excel export changes.
   * @param {string} inProgress Indicates if the task is in progress.
   */
  onExcelExportStateChange: import_prop_types163.default.func,
  /**
   * Callback fired when rowCount is set and the next batch of virtualized rows is rendered.
   * @param {GridFetchRowsParams} params With all properties from [[GridFetchRowsParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#viewport-loading Server-side data-Viewport loading} instead.
   */
  onFetchRows: import_prop_types163.default.func,
  /**
   * Callback fired when the Filter model changes before the filters are applied.
   * @param {GridFilterModel} model With all properties from [[GridFilterModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onFilterModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the menu is closed.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuClose: import_prop_types163.default.func,
  /**
   * Callback fired when the menu is opened.
   * @param {GridMenuParams} params With all properties from [[GridMenuParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onMenuOpen: import_prop_types163.default.func,
  /**
   * Callback fired when the pagination meta has changed.
   * @param {GridPaginationMeta} paginationMeta Updated pagination meta.
   */
  onPaginationMetaChange: import_prop_types163.default.func,
  /**
   * Callback fired when the pagination model has changed.
   * @param {GridPaginationModel} model Updated pagination model.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPaginationModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the pinned columns have changed.
   * @param {GridPinnedColumnFields} pinnedColumns The changed pinned columns.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPinnedColumnsChange: import_prop_types163.default.func,
  /**
   * Callback fired when the pivot active state changes.
   * @param {boolean} isPivotActive Whether the data grid is in pivot mode.
   */
  onPivotActiveChange: import_prop_types163.default.func,
  /**
   * Callback fired when the pivot model changes.
   * @param {GridPivotModel} pivotModel The new pivot model.
   */
  onPivotModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the pivot side panel open state changes.
   * @param {boolean} pivotPanelOpen Whether the pivot side panel is visible.
   * @deprecated Use the `sidebarOpen` and `sidebarClose` events or corresponding event handlers `onSidebarOpen()` and `onSidebarClose()` instead.
   */
  onPivotPanelOpenChange: import_prop_types163.default.func,
  /**
   * Callback fired when the preferences panel is closed.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelClose: import_prop_types163.default.func,
  /**
   * Callback fired when the preferences panel is opened.
   * @param {GridPreferencePanelParams} params With all properties from [[GridPreferencePanelParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onPreferencePanelOpen: import_prop_types163.default.func,
  /**
   * Callback called when `processRowUpdate()` throws an error or rejects.
   * @param {any} error The error thrown.
   */
  onProcessRowUpdateError: import_prop_types163.default.func,
  /**
   * The function to be used to process the prompt.
   * @param {string} prompt The prompt to be processed.
   * @param {string} promptContext The prompt context.
   * @param {string} conversationId The id of the conversation the prompt is part of. If not passed, prompt response will return a new conversation id that can be used to continue the newly started conversation.
   * @returns {Promise<PromptResponse>} The prompt response.
   */
  onPrompt: import_prop_types163.default.func,
  /**
   * Callback fired when a redo operation is executed.
   */
  onRedo: import_prop_types163.default.func,
  /**
   * Callback fired when the Data Grid is resized.
   * @param {ElementSize} containerSize With all properties from [[ElementSize]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onResize: import_prop_types163.default.func,
  /**
   * Callback fired when a row is clicked.
   * Not called if the target clicked is an interactive element added by the built-in columns.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowClick: import_prop_types163.default.func,
  /**
   * Callback fired when the row count has changed.
   * @param {number} count Updated row count.
   */
  onRowCountChange: import_prop_types163.default.func,
  /**
   * Callback fired when a double click event comes from a row container element.
   * @param {GridRowParams} params With all properties from [[RowParams]].
   * @param {MuiEvent<React.MouseEvent>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowDoubleClick: import_prop_types163.default.func,
  /**
   * Callback fired when the row turns to edit mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<React.KeyboardEvent | React.MouseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStart: import_prop_types163.default.func,
  /**
   * Callback fired when the row turns to view mode.
   * @param {GridRowParams} params With all properties from [[GridRowParams]].
   * @param {MuiEvent<MuiBaseEvent>} event The event that caused this prop to be called.
   */
  onRowEditStop: import_prop_types163.default.func,
  /**
   * Callback fired when the row grouping model changes.
   * @param {GridRowGroupingModel} model Columns used as grouping criteria.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowGroupingModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the `rowModesModel` prop changes.
   * @param {GridRowModesModel} rowModesModel Object containing which rows are in "edit" mode.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowModesModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when a row is being reordered.
   * @param {GridRowOrderChangeParams} params With all properties from [[GridRowOrderChangeParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowOrderChange: import_prop_types163.default.func,
  /**
   * Callback fired when the selection state of one or multiple rows changes.
   * @param {GridRowSelectionModel} rowSelectionModel With all the row ids [[GridSelectionModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onRowSelectionModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when scrolling to the bottom of the grid viewport.
   * @param {GridRowScrollEndParams} params With all properties from [[GridRowScrollEndParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#infinite-loading Server-side data-Infinite loading} instead.
   */
  onRowsScrollEnd: import_prop_types163.default.func,
  /**
   * Callback fired when the sidebar is closed.
   * @param {GridSidebarParams} params With all properties from [[GridSidebarParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onSidebarClose: import_prop_types163.default.func,
  /**
   * Callback fired when the sidebar is opened.
   * @param {GridSidebarParams} params With all properties from [[GridSidebarParams]].
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onSidebarOpen: import_prop_types163.default.func,
  /**
   * Callback fired when the sort model changes before a column is sorted.
   * @param {GridSortModel} model With all properties from [[GridSortModel]].
   * @param {GridCallbackDetails} details Additional details for this callback.
   */
  onSortModelChange: import_prop_types163.default.func,
  /**
   * Callback fired when the state of the Data Grid is updated.
   * @param {GridState} state The new state.
   * @param {MuiEvent<{}>} event The event object.
   * @param {GridCallbackDetails} details Additional details for this callback.
   * @ignore - do not document.
   */
  onStateChange: import_prop_types163.default.func,
  /**
   * Callback fired when an undo operation is executed.
   */
  onUndo: import_prop_types163.default.func,
  /**
   * Select the pageSize dynamically using the component UI.
   * @default [25, 50, 100]
   */
  pageSizeOptions: import_prop_types163.default.arrayOf(import_prop_types163.default.oneOfType([import_prop_types163.default.number, import_prop_types163.default.shape({
    label: import_prop_types163.default.string.isRequired,
    value: import_prop_types163.default.number.isRequired
  })]).isRequired),
  /**
   * If `true`, pagination is enabled.
   * @default false
   */
  pagination: import_prop_types163.default.bool,
  /**
   * The extra information about the pagination state of the Data Grid.
   * Only applicable with `paginationMode="server"`.
   */
  paginationMeta: import_prop_types163.default.shape({
    hasNextPage: import_prop_types163.default.bool
  }),
  /**
   * Pagination can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle the pagination on the client-side.
   * Set it to 'server' if you would like to handle the pagination on the server-side.
   * @default "client"
   */
  paginationMode: import_prop_types163.default.oneOf(["client", "server"]),
  /**
   * The pagination model of type [[GridPaginationModel]] which refers to current `page` and `pageSize`.
   */
  paginationModel: import_prop_types163.default.shape({
    page: import_prop_types163.default.number.isRequired,
    pageSize: import_prop_types163.default.number.isRequired
  }),
  /**
   * The column fields to display pinned to left or right.
   */
  pinnedColumns: import_prop_types163.default.object,
  /**
   * Sets the type of separator between pinned columns and non-pinned columns.
   * @default 'border-and-shadow'
   */
  pinnedColumnsSectionSeparator: import_prop_types163.default.oneOf(["border-and-shadow", "border", "shadow"]),
  /**
   * Rows data to pin on top or bottom.
   */
  pinnedRows: import_prop_types163.default.shape({
    bottom: import_prop_types163.default.arrayOf(import_prop_types163.default.object),
    top: import_prop_types163.default.arrayOf(import_prop_types163.default.object)
  }),
  /**
   * Sets the type of separator between pinned rows and non-pinned rows.
   * @default 'border-and-shadow'
   */
  pinnedRowsSectionSeparator: import_prop_types163.default.oneOf(["border-and-shadow", "border"]),
  /**
   * If `true`, the data grid will show data in pivot mode using the `pivotModel`.
   * @default false
   */
  pivotActive: import_prop_types163.default.bool,
  /**
   * The column definition overrides for the columns generated by the pivoting feature.
   * Pass either a partial column definition to apply the same overrides to all pivot columns, or a callback to apply different overrides to each pivot column.
   * For server-side pivoting, only the `PivotingColDefCallback` signature is supported, and the prop is required.
   * @type {Partial<GridPivotingColDefOverrides> | PivotingColDefCallback}
   * @default undefined
   * @throws {Error} If `undefined` and `dataSource` is provided.
   */
  pivotingColDef: import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.shape({
    align: import_prop_types163.default.oneOf(["center", "left", "right"]),
    cellClassName: import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.string]),
    description: import_prop_types163.default.string,
    display: import_prop_types163.default.oneOf(["flex", "text"]),
    field: import_prop_types163.default.string,
    flex: import_prop_types163.default.number,
    headerAlign: import_prop_types163.default.oneOf(["center", "left", "right"]),
    headerClassName: import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.string]),
    headerName: import_prop_types163.default.string,
    maxWidth: import_prop_types163.default.number,
    minWidth: import_prop_types163.default.number,
    resizable: import_prop_types163.default.bool,
    sortingOrder: import_prop_types163.default.arrayOf(import_prop_types163.default.oneOf(["asc", "desc"])),
    width: import_prop_types163.default.number
  })]),
  /**
   * The pivot model of the grid.
   * Will be used to generate the pivot data.
   * In case of `pivotActive` being `false`, the pivot model is still used to populate the pivot panel.
   */
  pivotModel: import_prop_types163.default.shape({
    columns: import_prop_types163.default.arrayOf(import_prop_types163.default.object).isRequired,
    rows: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
      field: import_prop_types163.default.string.isRequired,
      hidden: import_prop_types163.default.bool
    })).isRequired,
    values: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
      aggFunc: import_prop_types163.default.string.isRequired,
      field: import_prop_types163.default.string.isRequired,
      hidden: import_prop_types163.default.bool
    })).isRequired
  }),
  /**
   * If `true`, the pivot side panel is visible.
   * @default false
   * @deprecated Use `initialState.sidebar.open` instead.
   */
  pivotPanelOpen: import_prop_types163.default.bool,
  /**
   * Callback called before updating a row with new values in the row and cell editing.
   * @template R
   * @param {R} newRow Row object with the new values.
   * @param {R} oldRow Row object with the old values.
   * @param {{ rowId: GridRowId }} params Additional parameters.
   * @returns {Promise<R> | R} The final values to update the row.
   */
  processRowUpdate: import_prop_types163.default.func,
  /**
   * The milliseconds throttle delay for resizing the grid.
   * @default 60
   */
  resizeThrottleMs: import_prop_types163.default.number,
  /**
   * Row region in pixels to render before/after the viewport
   * @default 150
   */
  rowBufferPx: import_prop_types163.default.number,
  /**
   * Set the total number of rows, if it is different from the length of the value `rows` prop.
   * If some rows have children (for instance in the tree data), this number represents the amount of top level rows.
   * Only works with `paginationMode="server"`, ignored when `paginationMode="client"`.
   */
  rowCount: import_prop_types163.default.number,
  /**
   * If `single`, all the columns that are grouped are represented in the same grid column.
   * If `multiple`, each column that is grouped is represented in its own grid column.
   * @default 'single'
   */
  rowGroupingColumnMode: import_prop_types163.default.oneOf(["multiple", "single"]),
  /**
   * Set the row grouping model of the grid.
   */
  rowGroupingModel: import_prop_types163.default.arrayOf(import_prop_types163.default.string),
  /**
   * Sets the height in pixel of a row in the Data Grid.
   * @default 52
   */
  rowHeight: import_prop_types163.default.number,
  /**
   * Controls the modes of the rows.
   */
  rowModesModel: import_prop_types163.default.object,
  /**
   * If `true`, the reordering of rows is enabled.
   * @default false
   */
  rowReordering: import_prop_types163.default.bool,
  /**
   * Set of rows of type [[GridRowsProp]].
   * @default []
   */
  rows: import_prop_types163.default.arrayOf(import_prop_types163.default.object),
  /**
   * If `false`, the row selection mode is disabled.
   * @default true
   */
  rowSelection: import_prop_types163.default.bool,
  /**
   * Sets the row selection model of the Data Grid.
   */
  rowSelectionModel: import_prop_types163.default.shape({
    ids: import_prop_types163.default.instanceOf(Set).isRequired,
    type: import_prop_types163.default.oneOf(["exclude", "include"]).isRequired
  }),
  /**
   * When `rowSelectionPropagation.descendants` is set to `true`.
   * - Selecting a parent selects all its filtered descendants automatically.
   * - Deselecting a parent row deselects all its filtered descendants automatically.
   *
   * When `rowSelectionPropagation.parents` is set to `true`
   * - Selecting all the filtered descendants of a parent selects the parent automatically.
   * - Deselecting a descendant of a selected parent deselects the parent automatically.
   *
   * Works with tree data and row grouping on the client-side only.
   * @default { parents: true, descendants: true }
   */
  rowSelectionPropagation: import_prop_types163.default.shape({
    descendants: import_prop_types163.default.bool,
    parents: import_prop_types163.default.bool
  }),
  /**
   * Loading rows can be processed on the server or client-side.
   * Set it to 'client' if you would like enable infnite loading.
   * Set it to 'server' if you would like to enable lazy loading.
   * @default "client"
   * @deprecated Use the {@link https://mui.com/x/react-data-grid/server-side-data/lazy-loading/#viewport-loading Server-side data-Viewport loading} instead.
   */
  rowsLoadingMode: import_prop_types163.default.oneOf(["client", "server"]),
  /**
   * Sets the type of space between rows added by `getRowSpacing`.
   * @default "margin"
   */
  rowSpacingType: import_prop_types163.default.oneOf(["border", "margin"]),
  /**
   * If `true`, the Data Grid will auto span the cells over the rows having the same value.
   * @default false
   */
  rowSpanning: import_prop_types163.default.bool,
  /**
   * Override the height/width of the Data Grid inner scrollbar.
   */
  scrollbarSize: import_prop_types163.default.number,
  /**
   * Set the area in `px` at the bottom of the grid viewport where onRowsScrollEnd is called.
   * If combined with `lazyLoading`, it defines the area where the next data request is triggered.
   * @default 80
   */
  scrollEndThreshold: import_prop_types163.default.number,
  /**
   * Updates the tree path in a row model.
   * Used when reordering rows across different parents in tree data.
   * @template R
   * @param {string[]} path The new path for the row.
   * @param {R} row The row model to update.
   * @returns {R} The updated row model with the new path.
   */
  setTreeDataPath: import_prop_types163.default.func,
  /**
   * If `true`, vertical borders will be displayed between cells.
   * @default false
   */
  showCellVerticalBorder: import_prop_types163.default.bool,
  /**
   * If `true`, vertical borders will be displayed between column header items.
   * @default false
   */
  showColumnVerticalBorder: import_prop_types163.default.bool,
  /**
   * If `true`, the toolbar is displayed.
   * @default false
   */
  showToolbar: import_prop_types163.default.bool,
  /**
   * Overridable components props dynamically passed to the component at rendering.
   */
  slotProps: import_prop_types163.default.object,
  /**
   * Overridable components.
   */
  slots: import_prop_types163.default.object,
  /**
   * Sorting can be processed on the server or client-side.
   * Set it to 'client' if you would like to handle sorting on the client-side.
   * Set it to 'server' if you would like to handle sorting on the server-side.
   * @default "client"
   */
  sortingMode: import_prop_types163.default.oneOf(["client", "server"]),
  /**
   * The order of the sorting sequence.
   * @default ['asc', 'desc', null]
   */
  sortingOrder: import_prop_types163.default.arrayOf(import_prop_types163.default.oneOf(["asc", "desc"])),
  /**
   * Set the sort model of the Data Grid.
   */
  sortModel: import_prop_types163.default.arrayOf(import_prop_types163.default.shape({
    field: import_prop_types163.default.string.isRequired,
    sort: import_prop_types163.default.oneOf(["asc", "desc"])
  })),
  /**
   * The function is used to split the pasted text into rows and cells.
   * @param {string} text The text pasted from the clipboard.
   * @param {string} delimiter The delimiter used to split the text. Default is the tab character and can be set with the `clipboardCopyCellDelimiter` prop.
   * @returns {string[][] | null} A 2D array of strings. The first dimension is the rows, the second dimension is the columns.
   * @default (pastedText, delimiter = '\t') => { const text = pastedText.replace(/\r?\n$/, ''); return text.split(/\r\n|\n|\r/).map((row) => row.split(delimiter)); }
   */
  splitClipboardPastedText: import_prop_types163.default.func,
  style: import_prop_types163.default.object,
  /**
   * The system prop that allows defining system overrides as well as additional CSS styles.
   */
  sx: import_prop_types163.default.oneOfType([import_prop_types163.default.arrayOf(import_prop_types163.default.oneOfType([import_prop_types163.default.func, import_prop_types163.default.object, import_prop_types163.default.bool])), import_prop_types163.default.func, import_prop_types163.default.object]),
  /**
   * Sets the tab navigation behavior for the Data Grid.
   * - "none": No Data Grid specific tab navigation. Pressing the tab key will move the focus to the next element in the tab sequence.
   * - "content": Pressing the tab key will move the focus to the next cell in the same row or the first cell in the next row. Shift+Tab will move the focus to the previous cell in the same row or the last cell in the previous row. Tab navigation is not enabled for the header.
   * - "header": Pressing the tab key will move the focus to the next column group, column header or header filter. Shift+Tab will move the focus to the previous column group, column header or header filter. Tab navigation is not enabled for the content.
   * - "all": Combines the "content" and "header" behavior.
   * @default "none"
   */
  tabNavigation: import_prop_types163.default.oneOf(["all", "content", "header", "none"]),
  /**
   * If positive, the Data Grid will throttle updates coming from `apiRef.current.updateRows` and `apiRef.current.setRows`.
   * It can be useful if you have a high update rate but do not want to do heavy work like filtering / sorting or rendering on each  individual update.
   * @default 0
   */
  throttleRowsMs: import_prop_types163.default.number,
  /**
   * If `true`, the rows will be gathered in a tree structure according to the `getTreeDataPath` prop.
   * @default false
   */
  treeData: import_prop_types163.default.bool,
  /**
   * If `true`, the Data Grid enables column virtualization when `getRowHeight` is set to `() => 'auto'`.
   * By default, column virtualization is disabled when dynamic row height is enabled to measure the row height correctly.
   * For datasets with a large number of columns, this can cause performance issues.
   * The downside of enabling this prop is that the row height will be estimated based the cells that are currently rendered, which can cause row height change when scrolling horizontally.
   * @default false
   */
  virtualizeColumnsWithAutoRowHeight: import_prop_types163.default.bool
} : void 0;
var DataGridPremium2 = React371.memo(DataGridPremiumRaw);
if (true) DataGridPremium2.displayName = "DataGridPremium";

// node_modules/@mui/x-data-grid-premium/esm/context/GridChartsIntegrationContextProvider.js
var React372 = __toESM(require_react(), 1);
var import_jsx_runtime269 = __toESM(require_jsx_runtime(), 1);
function GridChartsIntegrationContextProvider({
  children: children2
}) {
  const [chartStateLookup, setChartStateLookup] = React372.useState({});
  const setChartState = React372.useCallback((id, state) => {
    if (id === "") {
      return;
    }
    setChartStateLookup((prev) => _extends({}, prev, {
      [id]: _extends({}, prev[id] || EMPTY_CHART_INTEGRATION_CONTEXT_STATE, state)
    }));
  }, []);
  const value = React372.useMemo(() => ({
    chartStateLookup,
    setChartState
  }), [chartStateLookup, setChartState]);
  return (0, import_jsx_runtime269.jsx)(GridChartsIntegrationContext.Provider, {
    value,
    children: children2
  });
}

// node_modules/@mui/x-data-grid-premium/esm/context/GridChartsRendererProxy.js
var React373 = __toESM(require_react(), 1);
var import_prop_types164 = __toESM(require_prop_types(), 1);
var import_jsx_runtime270 = __toESM(require_jsx_runtime(), 1);
function GridChartsRendererProxy(props) {
  const {
    renderer: Renderer,
    id,
    label,
    onRender
  } = props;
  const {
    chartStateLookup,
    setChartState
  } = useGridChartsIntegrationContext();
  React373.useEffect(() => {
    if (!chartStateLookup[id]) {
      setChartState(id, _extends({}, EMPTY_CHART_INTEGRATION_CONTEXT_STATE, {
        label
      }));
    }
    return () => {
      delete chartStateLookup[id];
    };
  }, [id, label, setChartState, chartStateLookup]);
  if (!chartStateLookup[id]) {
    return null;
  }
  const {
    dimensions,
    values: values3,
    type,
    configuration: configuration4
  } = chartStateLookup[id];
  return (0, import_jsx_runtime270.jsx)(Renderer, {
    dimensions,
    values: values3,
    chartType: type,
    configuration: configuration4,
    onRender
  });
}
true ? GridChartsRendererProxy.propTypes = {
  // ----------------------------- Warning --------------------------------
  // | These PropTypes are generated from the TypeScript type definitions |
  // | To update them edit the TypeScript types and run "pnpm proptypes"  |
  // ----------------------------------------------------------------------
  /**
   * The unique identifier for the chart.
   */
  id: import_prop_types164.default.string.isRequired,
  /**
   * The label for the chart.
   */
  label: import_prop_types164.default.string,
  /**
   * Callback function called when the chart is about to be rendered.
   * Use this to check and modify the chart props before it is rendered.
   */
  onRender: import_prop_types164.default.func,
  /**
   * The renderer component that will render the chart.
   */
  renderer: import_prop_types164.default.func.isRequired
} : void 0;
export {
  AiAssistantPanelTrigger,
  COMFORTABLE_DENSITY_FACTOR,
  COMPACT_DENSITY_FACTOR,
  ChartsPanelTrigger,
  ColumnsPanelTrigger,
  DATA_GRID_PREMIUM_PROPS_DEFAULT_VALUES,
  DEFAULT_GRID_AUTOSIZE_OPTIONS,
  DEFAULT_GRID_COL_TYPE_KEY,
  DataGrid3 as DataGrid,
  DataGridPremium2 as DataGridPremium,
  DataGridPro,
  EMPTY_PINNED_COLUMN_FIELDS,
  EMPTY_RENDER_CONTEXT,
  ExportCsv,
  ExportExcel,
  ExportPrint,
  FilterPanelTrigger,
  GRID_ACTIONS_COLUMN_TYPE,
  GRID_ACTIONS_COL_DEF,
  GRID_AGGREGATION_FUNCTIONS,
  GRID_AGGREGATION_ROOT_FOOTER_ROW_ID,
  GRID_BOOLEAN_COL_DEF,
  GRID_CHECKBOX_SELECTION_COL_DEF,
  GRID_CHECKBOX_SELECTION_FIELD,
  GRID_COLUMN_MENU_SLOTS_PREMIUM as GRID_COLUMN_MENU_SLOTS,
  GRID_COLUMN_MENU_SLOT_PROPS_PREMIUM as GRID_COLUMN_MENU_SLOT_PROPS,
  GRID_DATETIME_COL_DEF,
  GRID_DATE_COL_DEF,
  GRID_DEFAULT_LOCALE_TEXT,
  GRID_DETAIL_PANEL_TOGGLE_COL_DEF,
  GRID_DETAIL_PANEL_TOGGLE_FIELD,
  GRID_EXPERIMENTAL_ENABLED,
  GRID_LONG_TEXT_COL_DEF,
  GRID_NUMERIC_COL_DEF,
  GRID_REORDER_COL_DEF,
  GRID_ROOT_GROUP_ID,
  GRID_ROW_GROUPING_SINGLE_GROUPING_FIELD,
  GRID_SINGLE_SELECT_COL_DEF,
  GRID_STRING_COL_DEF,
  GRID_TREE_DATA_GROUPING_FIELD,
  GridActionsCell,
  GridActionsCellItem,
  GridAddIcon,
  GridAiAssistantPanel,
  GridApiContext,
  GridArrowDownwardIcon,
  GridArrowUpwardIcon,
  GridAssistantIcon,
  GridVirtualScroller as GridBody,
  GridBooleanCell,
  MemoizedGridCell as GridCell,
  GridCellCheckboxForwardRef,
  GridCellCheckboxRenderer2 as GridCellCheckboxRenderer,
  GridCellEditStartReasons,
  GridCellEditStopReasons,
  GridCellModes,
  GridChartsIcon,
  GridChartsIntegrationContextProvider,
  GridChartsPanel,
  GridChartsRendererProxy,
  GridCheckCircleIcon,
  GridCheckIcon,
  GridClearIcon,
  GridCloseIcon,
  GridColumnHeaderFilterIconButtonWrapped as GridColumnHeaderFilterIconButton,
  Memoized3 as GridColumnHeaderItem,
  GridColumnHeaderMenu,
  GridColumnHeaderSeparator,
  GridColumnHeaderSeparatorSides,
  GridColumnHeaderSortIcon,
  GridColumnHeaderTitle,
  GridColumnHeaders4 as GridColumnHeaders,
  GridColumnIcon,
  GridPremiumColumnMenu as GridColumnMenu,
  GridColumnMenuAggregationItem,
  GridColumnMenuColumnsItem,
  GridColumnMenuContainer,
  GridColumnMenuFilterItem,
  GridColumnMenuGroupingItem,
  GridColumnMenuHideItem,
  GridColumnMenuManageItem,
  GridColumnMenuPinningItem,
  GridColumnMenuSortItem,
  GridColumnsManagement,
  GridColumnsPanel,
  GridContextProvider,
  GridCsvExportMenuItem,
  GridDataSourceCacheDefault,
  GridDeleteForeverIcon,
  GridDeleteIcon,
  GridDetailPanelToggleCell,
  GridDownloadIcon,
  GridDragIcon,
  GridEditBooleanCell,
  GridEditDateCell,
  GridEditInputCell,
  GridEditLongTextCell,
  GridEditModes,
  GridEditSingleSelectCell,
  GridEmptyPivotOverlay,
  GridExcelExportMenuItem,
  GridExpandLessIcon,
  GridExpandMoreIcon,
  GridFilterAltIcon,
  GridFilterForm,
  GridFilterInputBoolean,
  GridFilterInputDate,
  GridFilterInputMultipleSingleSelect,
  GridFilterInputMultipleValue,
  GridFilterInputSingleSelect,
  GridFilterInputValue,
  GridFilterListIcon,
  GridFilterPanel,
  GridFooter,
  GridFooterContainer,
  GridFooterPlaceholder,
  GridFunctionsIcon,
  GridGenericColumnMenu,
  GridGetRowsError,
  GridGroupWorkIcon,
  GridHeader,
  GridHeaderCheckbox,
  Memoized4 as GridHeaderFilterCell,
  GridHeaderFilterMenu,
  GridHeaderFilterMenuContainer,
  GridHistoryIcon,
  GridKeyboardArrowRight,
  GridLoadIcon,
  GridLoadingOverlay,
  GridLogicOperator,
  GridLongTextCell,
  GridLongTextCellCollapseIcon,
  GridLongTextCellExpandIcon,
  GridMenu,
  GridMenuIcon,
  GridMicIcon,
  GridMicOffIcon,
  GridMoreVertIcon,
  GridMoveToBottomIcon,
  GridMoveToTopIcon,
  GridNoColumnsOverlay,
  GridNoRowsOverlay,
  GridOverlay,
  GridPagination,
  GridPanel,
  GridPanelContent2 as GridPanelContent,
  GridPanelFooter,
  GridPanelHeader,
  GridPanelWrapper,
  GridPinnedColumnPosition,
  GridPivotIcon,
  GridPivotPanel,
  GridPortalWrapper,
  GridPreferencePanelsValue,
  GridPrintExportMenuItem,
  GridPromptIcon,
  GridPushPinLeftIcon,
  GridPushPinRightIcon,
  GridRedoIcon,
  GridRemoveIcon,
  GridRerunIcon,
  MemoizedGridRoot as GridRoot,
  MemoizedGridRow as GridRow,
  GridRowCount,
  GridRowEditStartReasons,
  GridRowEditStopReasons,
  GridRowModes,
  GridRowReorderCell,
  GridSearchIcon,
  GridSelectedRowCount,
  GridSendIcon,
  GridSeparatorIcon,
  GridShadowScrollArea,
  GridSidebarValue,
  GridSignature,
  Memoized2 as GridSkeletonCell,
  GridSyncDisabledIcon,
  GridSyncIcon,
  GridTableRowsIcon,
  GridToolbar2 as GridToolbar,
  GridToolbarColumnsButton,
  GridToolbarContainer,
  GridToolbarDensitySelector,
  GridToolbarExport,
  GridToolbarExportContainer,
  GridToolbarFilterButton,
  GridToolbarQuickFilter,
  GridTreeDataGroupingCell,
  GridTripleDotsVerticalIcon,
  GridUndoIcon,
  GridUpdateRowError,
  GridViewColumnIcon,
  GridViewHeadlineIcon,
  GridViewStreamIcon,
  GridVisibilityOffIcon,
  GridWorkspacesIcon,
  IS_SPEECH_RECOGNITION_SUPPORTED,
  PivotPanelTrigger,
  PromptField,
  PromptFieldControl,
  PromptFieldRecord,
  PromptFieldSend,
  QuickFilter,
  QuickFilterClear,
  QuickFilterControl,
  QuickFilterTrigger,
  Toolbar,
  ToolbarButton,
  checkGridRowIdIsValid,
  createCellEditHistoryHandler,
  createClipboardPasteHistoryHandler,
  createRowEditHistoryHandler,
  createRowSelectionManager,
  getAggregationFooterRowIdFromGroupId,
  getDataGridUtilityClass,
  getDefaultGridFilterModel,
  getGridBooleanOperators,
  getGridDateOperators,
  getGridDefaultColumnTypes,
  getGridNumericOperators,
  getGridNumericQuickFilterFn,
  getGridSingleSelectOperators,
  getGridStringOperators,
  getGridStringQuickFilterFn,
  getGroupRowIdFromPath,
  getRowGroupingFieldFromGroupingCriteria,
  gridAggregationLookupSelector,
  gridAggregationModelSelector,
  gridAggregationStateSelector,
  gridClasses,
  gridColumnDefinitionsSelector,
  gridColumnFieldsSelector,
  gridColumnGroupingSelector,
  gridColumnGroupsHeaderMaxDepthSelector,
  gridColumnGroupsHeaderStructureSelector,
  gridColumnGroupsLookupSelector,
  gridColumnGroupsUnwrappedModelSelector,
  gridColumnLookupSelector,
  gridColumnMenuSelector,
  gridColumnPositionsSelector,
  gridColumnReorderDragColSelector,
  gridColumnReorderSelector,
  gridColumnResizeSelector,
  gridColumnVisibilityModelSelector,
  gridColumnsStateSelector,
  gridColumnsTotalWidthSelector,
  gridDataRowIdsSelector,
  gridDateComparator,
  gridDateFormatter,
  gridDateTimeFormatter,
  gridDensityFactorSelector,
  gridDensitySelector,
  gridDetailPanelExpandedRowIdsSelector,
  gridDetailPanelExpandedRowsContentCacheSelector,
  gridDimensionsSelector,
  gridEditCellStateSelector,
  gridEditRowsStateSelector,
  gridExpandedRowCountSelector,
  gridExpandedSortedRowEntriesSelector,
  gridExpandedSortedRowIdsSelector,
  gridExpandedSortedRowIndexLookupSelector,
  gridFilterActiveItemsLookupSelector,
  gridFilterActiveItemsSelector,
  gridFilterModelSelector,
  gridFilterableColumnDefinitionsSelector,
  gridFilterableColumnLookupSelector,
  gridFilteredDescendantCountLookupSelector,
  gridFilteredDescendantRowCountSelector,
  gridFilteredRowCountSelector,
  gridFilteredRowsLookupSelector,
  gridFilteredSortedRowEntriesSelector,
  gridFilteredSortedRowIdsSelector,
  gridFilteredSortedTopLevelRowEntriesSelector,
  gridFilteredTopLevelRowCountSelector,
  gridFocusCellSelector,
  gridFocusColumnGroupHeaderSelector,
  gridFocusColumnHeaderFilterSelector,
  gridFocusColumnHeaderSelector,
  gridFocusStateSelector,
  gridHasColSpanSelector,
  gridHeaderFilteringEditFieldSelector,
  gridHeaderFilteringEnabledSelector,
  gridHeaderFilteringMenuSelector,
  gridHeaderFilteringStateSelector,
  gridHistoryCanRedoSelector,
  gridHistoryCanUndoSelector,
  gridHistoryEnabledSelector,
  gridListColumnSelector,
  gridListViewSelector,
  gridNumberComparator,
  gridPageCountSelector,
  gridPageSelector,
  gridPageSizeSelector,
  gridPaginatedVisibleSortedGridRowEntriesSelector,
  gridPaginatedVisibleSortedGridRowIdsSelector,
  gridPaginationEnabledClientSideSelector,
  gridPaginationMetaSelector,
  gridPaginationModelSelector,
  gridPaginationRowCountSelector,
  gridPaginationRowRangeSelector,
  gridPaginationSelector,
  gridPanelClasses,
  gridPinnedColumnsSelector,
  gridPivotModelSelector,
  gridPreferencePanelStateSelector,
  gridQuickFilterValuesSelector,
  gridRenderContextColumnsSelector,
  gridRenderContextSelector,
  gridResizingColumnFieldSelector,
  gridRowCountSelector,
  gridRowGroupingModelSelector,
  gridRowGroupingNameSelector,
  gridRowGroupingSanitizedModelSelector,
  gridRowIdSelector,
  gridRowIsEditingSelector,
  gridRowMaximumTreeDepthSelector,
  gridRowNodeSelector,
  gridRowSelectionCountSelector,
  gridRowSelectionIdsSelector,
  gridRowSelectionManagerSelector,
  gridRowSelectionStateSelector,
  gridRowSelector,
  gridRowTreeDepthsSelector,
  gridRowTreeSelector,
  gridRowsLoadingSelector,
  gridRowsLookupSelector,
  gridRowsMetaSelector,
  gridSidebarContentSelector,
  gridSidebarOpenSelector,
  gridSidebarStateSelector,
  gridSortColumnLookupSelector,
  gridSortModelSelector,
  gridSortedRowEntriesSelector,
  gridSortedRowIdsSelector,
  gridStringOrNumberComparator,
  gridTabIndexCellSelector,
  gridTabIndexColumnGroupHeaderSelector,
  gridTabIndexColumnHeaderFilterSelector,
  gridTabIndexColumnHeaderSelector,
  gridTabIndexStateSelector,
  gridTopLevelRowCountSelector,
  gridVirtualizationColumnEnabledSelector,
  gridVirtualizationEnabledSelector,
  gridVirtualizationRowEnabledSelector,
  gridVirtualizationSelector,
  gridVisibleColumnDefinitionsSelector,
  gridVisibleColumnFieldsSelector,
  gridVisiblePinnedColumnDefinitionsSelector,
  gridVisibleRowsLookupSelector,
  gridVisibleRowsSelector,
  isAutogeneratedRow,
  isGroupingColumn,
  isLeaf,
  renderActionsCell,
  renderBooleanCell,
  renderEditBooleanCell,
  renderEditDateCell,
  renderEditInputCell,
  renderEditLongTextCell,
  renderEditSingleSelectCell,
  renderLongTextCell,
  renderRowReorderCell,
  setupExcelExportWebWorker,
  gridDefaultPromptResolver as unstable_gridDefaultPromptResolver,
  unstable_resetCleanupTracking,
  useGridPivoting as unstable_useGridPivoting,
  useFirstRender,
  useGridApiContext3 as useGridApiContext,
  useGridApiMethod,
  useGridApiRef3 as useGridApiRef,
  useGridChartsIntegrationContext,
  useGridEvent,
  useGridEventPriority,
  useGridLogger,
  useGridNativeEventListener,
  useGridRootProps3 as useGridRootProps,
  useGridSelector,
  useGridVirtualization,
  useKeepGroupedColumnsHidden,
  useOnMount,
  useRunOnce,
  useRunOncePerLoop,
  virtualizationStateInitializer
};
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js:
  (**
   * @license React
   * use-sync-external-store-shim/with-selector.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-virtualizer/esm/index.js:
  (**
   * @mui/x-virtualizer v0.3.3
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-data-grid/esm/index.js:
  (**
   * @mui/x-data-grid v8.28.0
   *
   * @license MIT
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-telemetry/esm/index.js:
  (**
   * @mui/x-telemetry v8.20.0
   *
   * @license SEE LICENSE IN LICENSE
   * This source code is licensed under the SEE LICENSE IN LICENSE license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-license/esm/index.js:
  (**
   * @mui/x-license v8.26.0
   *
   * @license SEE LICENSE IN LICENSE
   * This source code is licensed under the SEE LICENSE IN LICENSE license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-data-grid-pro/esm/index.js:
  (**
   * @mui/x-data-grid-pro v8.28.0
   *
   * @license SEE LICENSE IN LICENSE
   * This source code is licensed under the SEE LICENSE IN LICENSE license found in the
   * LICENSE file in the root directory of this source tree.
   *)

@mui/x-data-grid-premium/esm/index.js:
  (**
   * @mui/x-data-grid-premium v8.28.0
   *
   * @license SEE LICENSE IN LICENSE
   * This source code is licensed under the SEE LICENSE IN LICENSE license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=@mui_x-data-grid-premium.js.map
